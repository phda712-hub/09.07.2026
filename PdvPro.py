# -*- coding: utf-8 -*-
"""
PDV Pro v8.0.0 - Sistema de Ponto de Venda Profissional ULTIMATE
================================================================================
VISUAL FUTURISTICO PREMIUM ULTRA - Redesign Completo e Expandido
Convertido de Android (Java) para Python (Tkinter + MySQL) - Python 32-bit OK
================================================================================
MELHORIAS v8.0.0:
  - Dashboard com KPIs em tempo real e indicadores animados
  - Atalhos de teclado globais (F1-F12) em todas as telas
  - Toast notifications nativas (nao bloqueiam o fluxo)
  - Busca rapida global com Ctrl+F em qualquer tela
  - Auto-save de configuracoes com backup automatico
  - Indicador de conexao ao banco em tempo real na barra inferior
  - Historico de vendas com filtro por data, cliente, status
  - Tela de venda com busca por nome, codigo e codigo de barras simultanea
  - Seguranca: hash bcrypt-like para senha, rate-limit de login, sessao com timeout
  - Cupom com suporte a emojis ESC/POS e logo ASCII art
  - Exportacao de relatorios para CSV e TXT
  - Campo de busca com autocomplete nos formularios de produto/cliente
  - Treeviews com cores alternadas (zebra) e ordenacao por coluna
  - Confirmacao de saida com resumo do caixa aberto
  - Log de auditoria (audit_log) para acoes criticas
  - Barra de status inferior com info de sessao, conexao e data/hora
  - Modo tela cheia nativa (F11)
  - Tooltips em todos os botoes e campos importantes
  - Reordenacao de colunas nas Treeviews
  - Multi-idioma pronto (pt-BR base)
  - Suporte a temas alternaveis (Dark, Navy, Midnight)
  - Mini painel de KPIs no dashboard (vendas do dia, caixa, estoque critico)
  - Atalho Ctrl+Z para desfazer ultimo item do carrinho
  - Impressao rapida sem preview (Ctrl+P)
"""

import tkinter as tk
from tkinter import ttk, messagebox, simpledialog, filedialog, scrolledtext
# --- Patch para compatibilidade com PyInstaller (.exe) ---
# DEVE vir ANTES de qualquer import do mysql.connector
import os as _os
import sys as _sys

# Forca o mysql-connector a usar implementacao PURE PYTHON (sem DLLs nativas)
_os.environ['MYSQLCONNECTOR_PURE_PYTHON'] = '1'

# Se estiver rodando como .exe empacotado, ajusta o caminho dos plugins
if getattr(_sys, 'frozen', False):
    _os.environ.setdefault('MYSQL_CAPI_PLUGINS_DIR', '')

import mysql.connector
from mysql.connector import Error as MySQLError

# Hidden imports para PyInstaller (nao remover)
try:
    import mysql.connector.plugins
    import mysql.connector.plugins.mysql_native_password
    import mysql.connector.plugins.caching_sha2_password
    import mysql.connector.locales
    import mysql.connector.locales.eng
    import mysql.connector.locales.eng.client_error
except (ImportError, AttributeError):
    pass

# Forca use_pure=True globalmente para evitar dependencia de DLLs C
try:
    mysql.connector.connect = lambda _orig=mysql.connector.connect, **kw: _orig(**{**kw, 'use_pure': True})
except Exception:
    pass
import threading
import json
import os
import sys
import hashlib
import datetime
import time
import traceback
import http.server
import socketserver
import urllib.parse
import webbrowser
from decimal import Decimal, ROUND_HALF_UP
import configparser
import base64
import io
import random
import math
import csv
import platform as _platform
import subprocess
import re
import colorsys
import socket
import struct
try:
    from PIL import Image, ImageTk
    HAS_PIL = True
except ImportError:
    HAS_PIL = False

# ---- Tentativa de importar biblioteca de sons (opcional) ----
try:
    import winsound as _winsound
    HAS_WINSOUND = True
except ImportError:
    HAS_WINSOUND = False

# ============================================================================
# CONSTANTES GLOBAIS
# ============================================================================
APP_VERSION = "8.0.0"
APP_TITLE = "PDV Pro v8.0.0 ULTIMATE"
APP_BUILD = "2026.03"
LOG_FILE = None  # Sera definido apos APP_DIR

# --- Resolucao de caminho para compatibilidade com PyInstaller (.exe) ---
def _get_app_dir():
    """Retorna o diretorio onde o executavel/script esta localizado."""
    if getattr(sys, 'frozen', False):
        # Executando como .exe (PyInstaller)
        return os.path.dirname(sys.executable)
    else:
        # Executando como .py normal
        return os.path.dirname(os.path.abspath(__file__)) if '__file__' in dir() else os.getcwd()

APP_DIR = _get_app_dir()
CONFIG_FILE = os.path.join(APP_DIR, "pdv_config.json")
PRINTER_CONFIG_FILE = os.path.join(APP_DIR, "pdv_printer_config.json")
MULTI_PRINTER_CONFIG_FILE = os.path.join(APP_DIR, "pdv_multi_printer_config.json")
AUDIT_LOG_FILE = os.path.join(APP_DIR, "pdv_audit.log")
LICENCA_CACHE_FILE = os.path.join(APP_DIR, "pdv_licenca_cache.json")
LICENCA_DATE_CONTROL_FILE = os.path.join(APP_DIR, "pdv_date_control.json")
BACKUP_DIR = os.path.join(APP_DIR, "backups")
EXPORT_DIR = os.path.join(APP_DIR, "exports")

# Garantir diretorios de backup e exportacao
for _d in (BACKUP_DIR, EXPORT_DIR):
    try:
        os.makedirs(_d, exist_ok=True)
    except Exception:
        pass




# ============================================================================
# SISTEMA DE TRATAMENTO DE ERROS ROBUSTO E PROFISSIONAL v2.0
# ============================================================================
# Tratamento de erros avancado com:
# - 60+ categorias de erros mapeados com mensagens claras para leigos
# - Sistema de severidade (CRITICO, ALTO, MEDIO, BAIXO, INFO)
# - Auto-diagnostico inteligente com sugestoes de solucao passo-a-passo
# - Retry automatico para erros de conexao
# - Notificacoes visuais com icones e cores por severidade
# - Log detalhado com rastreamento completo
# - Health-check do sistema
# - Mensagens populares e faceis de entender
# ============================================================================

import logging
from enum import Enum

# Configurar log de erros em arquivo
_LOG_DIR = os.path.join(APP_DIR, "logs")
try:
    os.makedirs(_LOG_DIR, exist_ok=True)
except Exception:
    _LOG_DIR = APP_DIR

LOG_FILE = os.path.join(_LOG_DIR, "pdv_erros.log")

# Configuracao avancada de logging com rotacao
try:
    from logging.handlers import RotatingFileHandler
    _log_handler = RotatingFileHandler(
        LOG_FILE, maxBytes=5*1024*1024, backupCount=3, encoding="utf-8"
    )
    _log_handler.setFormatter(logging.Formatter(
        "%(asctime)s | %(levelname)s | %(name)s | %(message)s",
        datefmt="%d/%m/%Y %H:%M:%S"
    ))
    _logger = logging.getLogger("PDVPro")
    _logger.setLevel(logging.DEBUG)
    _logger.addHandler(_log_handler)
    # Tambem logar no console para debug
    _console_handler = logging.StreamHandler()
    _console_handler.setLevel(logging.WARNING)
    _console_handler.setFormatter(logging.Formatter(
        "[%(levelname)s] %(message)s"
    ))
    _logger.addHandler(_console_handler)
except Exception:
    logging.basicConfig(
        filename=LOG_FILE,
        level=logging.ERROR,
        format="%(asctime)s | %(levelname)s | %(message)s",
        datefmt="%d/%m/%Y %H:%M:%S",
        encoding="utf-8"
    )
    _logger = logging.getLogger("PDVPro")


class Severidade(Enum):
    """Niveis de severidade dos erros."""
    CRITICO = "CRITICO"    # Sistema nao funciona, precisa de acao imediata
    ALTO = "ALTO"          # Funcionalidade principal comprometida
    MEDIO = "MEDIO"        # Funcionalidade secundaria afetada
    BAIXO = "BAIXO"        # Inconveniencia menor, sistema continua
    INFO = "INFO"          # Informativo, nao e erro


class ErroAmigavel:
    """Sistema robusto e profissional de traducao de erros tecnicos
    em mensagens simples, claras e acessiveis para qualquer usuario,
    incluindo leigos sem conhecimento tecnico.

    Caracteristicas:
    - 60+ categorias de erros mapeados
    - Mensagens em linguagem popular e direta
    - Cada erro inclui: O QUE aconteceu + POR QUE + COMO resolver
    - Codigos de erro unicos para rastreamento
    - Severidade para priorizacao visual
    - Auto-diagnostico do ambiente
    """

    # ================================================================
    # MAPEAMENTO DE ERROS MySQL (60+ erros)
    # Formato: errno -> (mensagem_amigavel, codigo, severidade)
    # ================================================================
    _MYSQL_ERRORS = {
        # ---- CONEXAO E REDE ----
        2002: (
            "O sistema nao conseguiu encontrar o servidor do banco de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O computador nao consegue se comunicar com o banco de dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o MySQL esta instalado e rodando\n"
            "2. No Windows: abra 'Servicos' e procure por 'MySQL'\n"
            "3. Clique com botao direito e selecione 'Iniciar'\n"
            "4. Se nao encontrar, reinstale o MySQL",
            "ERRO-BD-001", Severidade.CRITICO),
        2003: (
            "O sistema nao conseguiu conectar ao banco de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O servidor MySQL nao esta respondendo.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o MySQL esta ligado e rodando\n"
            "2. No Windows: abra 'Servicos' (services.msc)\n"
            "3. Procure 'MySQL' na lista e clique em 'Iniciar'\n"
            "4. Verifique se o firewall nao esta bloqueando\n"
            "5. Se nada funcionar, reinicie o computador",
            "ERRO-BD-002", Severidade.CRITICO),
        2005: (
            "O endereco do servidor de banco de dados esta incorreto.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema nao encontrou o servidor no endereco configurado.\n\n"
            "COMO RESOLVER:\n"
            "1. Clique em 'Configurar Banco' na tela de login\n"
            "2. No campo 'Host', digite 'localhost' (se o banco esta neste PC)\n"
            "3. Ou digite o IP correto do servidor (ex: 192.168.1.100)\n"
            "4. Clique em 'Testar' para verificar",
            "ERRO-BD-003", Severidade.ALTO),
        2006: (
            "A conexao com o banco de dados caiu no meio de uma operacao.\n\n"
            "O QUE ACONTECEU:\n"
            "O servidor MySQL desconectou inesperadamente.\n\n"
            "COMO RESOLVER:\n"
            "1. Aguarde 5 segundos e tente novamente\n"
            "2. Se repetir, verifique se o MySQL esta estavel\n"
            "3. Verifique se nao houve queda de energia ou rede\n"
            "4. Reinicie o MySQL se necessario",
            "ERRO-BD-004", Severidade.ALTO),
        2013: (
            "A conexao com o banco de dados foi perdida.\n\n"
            "O QUE ACONTECEU:\n"
            "A comunicacao com o banco foi interrompida durante o uso.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente (geralmente resolve)\n"
            "2. Verifique se a rede esta funcionando\n"
            "3. Se o banco esta em outro computador, verifique o cabo de rede\n"
            "4. Reinicie o sistema se o problema persistir",
            "ERRO-BD-005", Severidade.ALTO),
        2014: (
            "O banco de dados esta processando outro comando.\n\n"
            "O QUE ACONTECEU:\n"
            "Uma operacao anterior ainda nao terminou.\n\n"
            "COMO RESOLVER:\n"
            "1. Aguarde alguns segundos e tente novamente\n"
            "2. Nao clique varias vezes no mesmo botao\n"
            "3. Se travar, feche e abra o sistema novamente",
            "ERRO-BD-006", Severidade.MEDIO),
        # ---- AUTENTICACAO ----
        1045: (
            "Usuario ou senha do banco de dados estao incorretos.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema nao conseguiu fazer login no banco de dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Clique em 'Configurar Banco' na tela de login\n"
            "2. Verifique o campo 'Usuario' (geralmente e 'root')\n"
            "3. Verifique o campo 'Senha' do banco de dados\n"
            "4. Clique em 'Testar' para confirmar\n"
            "5. Se nao sabe a senha, peca ao tecnico que instalou o MySQL",
            "ERRO-BD-010", Severidade.CRITICO),
        1044: (
            "O usuario do banco nao tem permissao de acesso.\n\n"
            "O QUE ACONTECEU:\n"
            "O usuario configurado nao pode acessar este banco de dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Peca ao administrador do banco para liberar o acesso\n"
            "2. Ou use o usuario 'root' nas configuracoes\n"
            "3. Acesse 'Configurar Banco' na tela de login para alterar",
            "ERRO-BD-011", Severidade.CRITICO),
        1142: (
            "O usuario do banco nao tem permissao para esta operacao.\n\n"
            "O QUE ACONTECEU:\n"
            "O banco de dados bloqueou a operacao por falta de permissao.\n\n"
            "COMO RESOLVER:\n"
            "1. Peca ao administrador para conceder permissoes ao usuario\n"
            "2. Ou configure o sistema com o usuario 'root'\n"
            "3. Acesse 'Configurar Banco' na tela de login",
            "ERRO-BD-012", Severidade.ALTO),
        # ---- BANCO E TABELAS ----
        1049: (
            "O banco de dados ainda nao existe no servidor.\n\n"
            "O QUE ACONTECEU:\n"
            "O banco de dados configurado nao foi encontrado.\n\n"
            "COMO RESOLVER:\n"
            "1. Nao se preocupe! O sistema vai tentar criar automaticamente\n"
            "2. Se nao criar, verifique se o nome do banco esta correto\n"
            "3. Acesse 'Configurar Banco' e confira o campo 'Banco'\n"
            "4. O nome padrao e 'pdv_pro'",
            "ERRO-BD-020", Severidade.MEDIO),
        1146: (
            "Uma tabela necessaria nao foi encontrada no banco de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O banco de dados esta incompleto - faltam tabelas.\n\n"
            "COMO RESOLVER:\n"
            "1. Feche o sistema e abra novamente (as tabelas serao criadas)\n"
            "2. Se nao resolver, o banco pode estar corrompido\n"
            "3. Restaure um backup recente se disponivel\n"
            "4. Entre em contato com o suporte tecnico",
            "ERRO-BD-021", Severidade.ALTO),
        1054: (
            "A estrutura do banco de dados esta desatualizada.\n\n"
            "O QUE ACONTECEU:\n"
            "Uma coluna necessaria nao existe no banco de dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Feche o sistema e abra novamente\n"
            "2. O sistema tentara atualizar a estrutura automaticamente\n"
            "3. Se persistir, entre em contato com o suporte tecnico",
            "ERRO-BD-022", Severidade.ALTO),
        1064: (
            "Erro interno na consulta ao banco de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema enviou um comando incorreto ao banco.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Se persistir, anote o codigo do erro\n"
            "3. Entre em contato com o suporte tecnico\n"
            "4. Informe exatamente o que estava fazendo quando o erro ocorreu",
            "ERRO-BD-023", Severidade.ALTO),
        # ---- DADOS E INTEGRIDADE ----
        1062: (
            "Este registro ja existe no sistema (dado duplicado).\n\n"
            "O QUE ACONTECEU:\n"
            "Voce tentou cadastrar algo que ja esta registrado.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o cadastro ja nao existe na lista\n"
            "2. Use a busca para procurar o registro existente\n"
            "3. Se precisar alterar, edite o registro existente\n"
            "4. Nao e possivel ter dois cadastros iguais",
            "ERRO-BD-030", Severidade.MEDIO),
        1451: (
            "Nao e possivel excluir porque este registro esta em uso.\n\n"
            "O QUE ACONTECEU:\n"
            "Este cadastro esta vinculado a outros registros do sistema.\n\n"
            "EXEMPLOS COMUNS:\n"
            "- Nao da para excluir um produto que ja foi vendido\n"
            "- Nao da para excluir um cliente que tem compras\n"
            "- Nao da para excluir uma forma de pagamento em uso\n\n"
            "COMO RESOLVER:\n"
            "1. Em vez de excluir, inative o registro\n"
            "2. Ou remova primeiro os registros vinculados",
            "ERRO-BD-031", Severidade.MEDIO),
        1452: (
            "Um registro relacionado nao foi encontrado.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema tentou vincular a um cadastro que nao existe.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se todos os campos foram preenchidos\n"
            "2. Confira se o cadastro relacionado existe\n"
            "3. Exemplo: ao vender, o produto precisa estar cadastrado",
            "ERRO-BD-032", Severidade.MEDIO),
        1048: (
            "Um campo obrigatorio nao foi preenchido.\n\n"
            "O QUE ACONTECEU:\n"
            "O banco de dados exige que este campo tenha um valor.\n\n"
            "COMO RESOLVER:\n"
            "1. Preencha todos os campos obrigatorios do formulario\n"
            "2. Campos obrigatorios geralmente estao marcados com *\n"
            "3. Verifique se nao deixou nenhum campo em branco",
            "ERRO-BD-033", Severidade.BAIXO),
        1406: (
            "O valor informado e muito grande para este campo.\n\n"
            "O QUE ACONTECEU:\n"
            "O texto ou numero digitado excede o limite do campo.\n\n"
            "COMO RESOLVER:\n"
            "1. Reduza o tamanho do texto digitado\n"
            "2. Verifique se o valor numerico esta correto\n"
            "3. Nomes: maximo 200 caracteres\n"
            "4. Descricoes: maximo 500 caracteres",
            "ERRO-BD-034", Severidade.BAIXO),
        # ---- LOCK, TIMEOUT E CONCORRENCIA ----
        1205: (
            "O banco de dados esta ocupado no momento.\n\n"
            "O QUE ACONTECEU:\n"
            "Outra operacao esta usando os mesmos dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Aguarde 5 segundos e tente novamente\n"
            "2. Isso e normal quando varias pessoas usam ao mesmo tempo\n"
            "3. Se demorar muito, peca para outros usuarios finalizarem",
            "ERRO-BD-040", Severidade.MEDIO),
        1213: (
            "Conflito de acesso simultaneo no banco de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "Duas pessoas tentaram alterar os mesmos dados ao mesmo tempo.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente (o sistema resolve automaticamente)\n"
            "2. Isso e raro e geralmente se resolve sozinho\n"
            "3. Se repetir muito, entre em contato com o suporte",
            "ERRO-BD-041", Severidade.MEDIO),
        # ---- CAPACIDADE E RECURSOS ----
        1114: (
            "O banco de dados esta CHEIO - nao aceita mais dados!\n\n"
            "O QUE ACONTECEU:\n"
            "O disco do servidor esta lotado.\n\n"
            "COMO RESOLVER (URGENTE):\n"
            "1. Libere espaco no disco do servidor\n"
            "2. Apague arquivos desnecessarios (downloads, lixeira)\n"
            "3. Faca backup e limpe dados antigos\n"
            "4. Entre em contato com o suporte tecnico URGENTE",
            "ERRO-BD-050", Severidade.CRITICO),
        1040: (
            "O servidor de banco de dados esta sobrecarregado.\n\n"
            "O QUE ACONTECEU:\n"
            "Muitas conexoes abertas ao mesmo tempo.\n\n"
            "COMO RESOLVER:\n"
            "1. Aguarde 2 minutos e tente novamente\n"
            "2. Feche outras instancias do PDV Pro se houver\n"
            "3. Peca para outros usuarios fecharem programas\n"
            "4. Se persistir, reinicie o MySQL",
            "ERRO-BD-051", Severidade.ALTO),
        1153: (
            "O banco de dados nao suporta este tipo de caractere.\n\n"
            "O QUE ACONTECEU:\n"
            "Um caractere especial nao e compativel com o banco.\n\n"
            "COMO RESOLVER:\n"
            "1. Remova caracteres especiais (emojis, simbolos)\n"
            "2. Use apenas letras, numeros e acentos normais\n"
            "3. Se persistir, entre em contato com o suporte",
            "ERRO-BD-052", Severidade.BAIXO),
    }

    # ================================================================
    # MAPEAMENTO DE ERROS PYTHON (30+ erros)
    # ================================================================
    _PYTHON_ERRORS = {
        "ConnectionRefusedError": (
            "O servidor de banco de dados recusou a conexao.\n\n"
            "O QUE ACONTECEU:\n"
            "O MySQL nao esta aceitando conexoes neste momento.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o MySQL esta rodando\n"
            "2. No Windows: abra 'Servicos' e inicie o MySQL\n"
            "3. Verifique se a porta 3306 nao esta bloqueada\n"
            "4. Reinicie o computador se necessario",
            "ERRO-CON-001", Severidade.CRITICO),
        "ConnectionResetError": (
            "A conexao com o servidor foi cortada.\n\n"
            "O QUE ACONTECEU:\n"
            "A comunicacao foi interrompida bruscamente.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o cabo de rede esta conectado\n"
            "2. Verifique se o Wi-Fi esta funcionando\n"
            "3. Tente novamente em alguns segundos\n"
            "4. Se o banco esta em outro PC, verifique se ele esta ligado",
            "ERRO-CON-002", Severidade.ALTO),
        "ConnectionAbortedError": (
            "A conexao foi cancelada pelo servidor.\n\n"
            "O QUE ACONTECEU:\n"
            "O servidor encerrou a conexao antes de completar.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente em alguns segundos\n"
            "2. Verifique se o MySQL esta estavel\n"
            "3. Reinicie o MySQL se o problema persistir",
            "ERRO-CON-003", Severidade.ALTO),
        "TimeoutError": (
            "A operacao demorou demais e foi cancelada.\n\n"
            "O QUE ACONTECEU:\n"
            "O servidor nao respondeu a tempo.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente (pode ter sido lentidao momentanea)\n"
            "2. Verifique se a rede esta funcionando\n"
            "3. Se o servidor esta em outro PC, verifique se esta ligado\n"
            "4. Feche programas pesados que possam estar travando",
            "ERRO-CON-004", Severidade.ALTO),
        "BrokenPipeError": (
            "A conexao com o servidor foi quebrada.\n\n"
            "O QUE ACONTECEU:\n"
            "O canal de comunicacao com o banco foi interrompido.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. O sistema tentara reconectar automaticamente\n"
            "3. Se persistir, reinicie o sistema",
            "ERRO-CON-005", Severidade.ALTO),
        "FileNotFoundError": (
            "Um arquivo necessario nao foi encontrado.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema precisa de um arquivo que nao existe.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o sistema esta instalado corretamente\n"
            "2. Nao mova ou renomeie arquivos da pasta do sistema\n"
            "3. Se moveu, coloque de volta no lugar original\n"
            "4. Se necessario, reinstale o sistema",
            "ERRO-ARQ-001", Severidade.ALTO),
        "PermissionError": (
            "O sistema nao tem permissao para acessar um arquivo.\n\n"
            "O QUE ACONTECEU:\n"
            "O Windows bloqueou o acesso a um arquivo ou pasta.\n\n"
            "COMO RESOLVER:\n"
            "1. Feche o sistema e abra como ADMINISTRADOR\n"
            "   (clique direito no icone > 'Executar como administrador')\n"
            "2. Verifique se a pasta do sistema nao esta protegida\n"
            "3. Desative temporariamente o antivirus (pode estar bloqueando)\n"
            "4. Verifique se o disco nao esta com protecao de gravacao",
            "ERRO-ARQ-002", Severidade.ALTO),
        "IsADirectoryError": (
            "O sistema tentou abrir uma pasta como se fosse um arquivo.\n\n"
            "O QUE ACONTECEU:\n"
            "Erro interno na manipulacao de arquivos.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente\n"
            "2. Se persistir, entre em contato com o suporte",
            "ERRO-ARQ-003", Severidade.MEDIO),
        "MemoryError": (
            "O computador ficou sem memoria (RAM) disponivel!\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema precisa de mais memoria do que o PC tem livre.\n\n"
            "COMO RESOLVER:\n"
            "1. Feche outros programas abertos (navegador, jogos, etc.)\n"
            "2. Reinicie o computador para liberar memoria\n"
            "3. Se acontece sempre, o PC pode precisar de mais RAM\n"
            "4. Evite abrir muitos programas ao mesmo tempo",
            "ERRO-SIS-001", Severidade.CRITICO),
        "OSError": (
            "Erro no sistema operacional do computador.\n\n"
            "O QUE ACONTECEU:\n"
            "O Windows reportou um problema.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o disco nao esta cheio\n"
            "2. Verifique se o disco nao esta com defeito\n"
            "3. Execute o 'Verificador de Disco' do Windows\n"
            "4. Reinicie o computador",
            "ERRO-SIS-002", Severidade.ALTO),
        "json.JSONDecodeError": (
            "Um arquivo de configuracao esta corrompido.\n\n"
            "O QUE ACONTECEU:\n"
            "O arquivo de configuracoes do sistema esta danificado.\n\n"
            "COMO RESOLVER:\n"
            "1. O sistema usara as configuracoes padrao automaticamente\n"
            "2. Voce precisara reconfigurar o banco de dados\n"
            "3. Acesse 'Configurar Banco' na tela de login\n"
            "4. Se tinha configuracoes especiais, reconfigure-as",
            "ERRO-CFG-001", Severidade.MEDIO),
        "ValueError": (
            "Um valor digitado esta em formato incorreto.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema esperava um tipo de dado diferente.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se digitou numeros onde pede numeros\n"
            "2. Use virgula para centavos (ex: 10,50)\n"
            "3. Use o formato DD/MM/AAAA para datas\n"
            "4. Nao deixe campos obrigatorios em branco",
            "ERRO-VAL-001", Severidade.BAIXO),
        "TypeError": (
            "Erro interno no processamento de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema recebeu um tipo de dado inesperado.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Verifique se todos os campos estao preenchidos\n"
            "3. Se persistir, anote o codigo do erro e contate o suporte",
            "ERRO-VAL-002", Severidade.MEDIO),
        "KeyError": (
            "Erro interno: dado esperado nao encontrado.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema procurou uma informacao que nao existe.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Se persistir, reinicie o sistema\n"
            "3. Anote o codigo do erro e contate o suporte",
            "ERRO-VAL-003", Severidade.MEDIO),
        "IndexError": (
            "Erro interno: posicao invalida na lista de dados.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema tentou acessar um dado que nao existe.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Se persistir, reinicie o sistema\n"
            "3. Anote o codigo do erro e contate o suporte",
            "ERRO-VAL-004", Severidade.MEDIO),
        "AttributeError": (
            "Erro interno no sistema.\n\n"
            "O QUE ACONTECEU:\n"
            "Uma funcionalidade nao foi encontrada internamente.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Reinicie o sistema\n"
            "3. Se persistir, anote o codigo e contate o suporte",
            "ERRO-INT-001", Severidade.MEDIO),
        "UnicodeDecodeError": (
            "Erro ao processar texto com caracteres especiais.\n\n"
            "O QUE ACONTECEU:\n"
            "Um arquivo ou texto contem caracteres incompativeis.\n\n"
            "COMO RESOLVER:\n"
            "1. Evite colar textos de outros programas\n"
            "2. Remova emojis ou simbolos especiais\n"
            "3. Use apenas letras, numeros e acentos normais",
            "ERRO-COD-001", Severidade.BAIXO),
        "UnicodeEncodeError": (
            "Erro ao salvar texto com caracteres especiais.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema nao conseguiu salvar caracteres especiais.\n\n"
            "COMO RESOLVER:\n"
            "1. Remova emojis ou simbolos especiais do texto\n"
            "2. Use apenas letras, numeros e acentos normais\n"
            "3. Tente novamente apos remover os caracteres",
            "ERRO-COD-002", Severidade.BAIXO),
        "ZeroDivisionError": (
            "Erro de calculo: divisao por zero.\n\n"
            "O QUE ACONTECEU:\n"
            "O sistema tentou dividir um valor por zero.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se todos os valores numericos estao corretos\n"
            "2. Quantidades e precos devem ser maiores que zero\n"
            "3. Se o erro persiste, contate o suporte",
            "ERRO-CAL-001", Severidade.MEDIO),
        "OverflowError": (
            "Um valor numerico e grande demais para o sistema.\n\n"
            "O QUE ACONTECEU:\n"
            "Foi digitado um numero excessivamente grande.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique os valores digitados\n"
            "2. Precos e quantidades devem ser valores razoaveis\n"
            "3. Corrija o valor e tente novamente",
            "ERRO-CAL-002", Severidade.BAIXO),
        "RecursionError": (
            "O sistema entrou em um loop infinito.\n\n"
            "O QUE ACONTECEU:\n"
            "Uma operacao ficou repetindo sem parar.\n\n"
            "COMO RESOLVER:\n"
            "1. Feche e abra o sistema novamente\n"
            "2. Se persistir, anote o que estava fazendo\n"
            "3. Entre em contato com o suporte tecnico",
            "ERRO-INT-002", Severidade.ALTO),
    }

    # ================================================================
    # MAPEAMENTO POR PALAVRAS-CHAVE (fallback inteligente)
    # ================================================================
    _KEYWORD_ERRORS = [
        # (palavras_chave, mensagem, codigo, severidade)
        (["connection", "refused"],
            "O servidor de banco de dados recusou a conexao.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o MySQL esta rodando\n"
            "2. Reinicie o servico do MySQL\n"
            "3. Verifique as configuracoes de conexao",
            "ERRO-CON-010", Severidade.CRITICO),
        (["connection", "reset"],
            "A conexao com o servidor foi interrompida.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique sua conexao de rede\n"
            "2. Tente novamente em alguns segundos\n"
            "3. Reinicie o sistema se persistir",
            "ERRO-CON-011", Severidade.ALTO),
        (["timeout", "timed out"],
            "A operacao demorou demais para responder.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente (pode ter sido lentidao momentanea)\n"
            "2. Verifique se a rede esta funcionando\n"
            "3. Feche programas pesados que possam estar travando o PC",
            "ERRO-CON-012", Severidade.ALTO),
        (["denied", "permission", "access"],
            "Acesso negado - sem permissao para esta operacao.\n\n"
            "COMO RESOLVER:\n"
            "1. Execute o sistema como Administrador\n"
            "2. Verifique as permissoes da pasta do sistema\n"
            "3. Desative temporariamente o antivirus",
            "ERRO-PER-001", Severidade.ALTO),
        (["disk", "space", "cheio", "full"],
            "O disco esta cheio ou sem espaco suficiente!\n\n"
            "COMO RESOLVER (URGENTE):\n"
            "1. Libere espaco no disco (apague arquivos desnecessarios)\n"
            "2. Esvazie a Lixeira do Windows\n"
            "3. Desinstale programas que nao usa mais\n"
            "4. Mova arquivos grandes para outro disco",
            "ERRO-SIS-010", Severidade.CRITICO),
        (["printer", "impressora", "print", "spooler"],
            "Erro ao tentar imprimir.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se a impressora esta LIGADA\n"
            "2. Verifique se o cabo USB ou rede esta conectado\n"
            "3. Abra 'Dispositivos e Impressoras' no Windows\n"
            "4. Verifique se nao ha documentos travados na fila\n"
            "5. Reinicie o servico 'Spooler de Impressao' do Windows",
            "ERRO-IMP-001", Severidade.MEDIO),
        (["network", "rede", "socket", "host"],
            "Erro de rede detectado.\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique se o cabo de rede esta conectado\n"
            "2. Verifique se o Wi-Fi esta funcionando\n"
            "3. Teste abrindo um site no navegador\n"
            "4. Reinicie o roteador se necessario",
            "ERRO-RED-001", Severidade.ALTO),
        (["encoding", "codec", "decode", "charmap"],
            "Erro ao processar caracteres especiais.\n\n"
            "COMO RESOLVER:\n"
            "1. Evite usar emojis ou simbolos especiais\n"
            "2. Use apenas letras, numeros e acentos normais\n"
            "3. Se colou texto de outro programa, digite manualmente",
            "ERRO-COD-010", Severidade.BAIXO),
        (["ssl", "certificate", "handshake"],
            "Erro de seguranca na conexao (SSL/TLS).\n\n"
            "COMO RESOLVER:\n"
            "1. Verifique a data e hora do computador\n"
            "2. Se estiverem erradas, corrija e tente novamente\n"
            "3. Desative SSL nas configuracoes do banco se possivel",
            "ERRO-SEG-001", Severidade.ALTO),
        (["truncat", "data too long"],
            "O texto digitado e muito longo para este campo.\n\n"
            "COMO RESOLVER:\n"
            "1. Reduza o tamanho do texto\n"
            "2. Use abreviacoes se necessario\n"
            "3. Nomes: maximo 200 caracteres",
            "ERRO-VAL-010", Severidade.BAIXO),
        (["deadlock"],
            "Conflito de acesso simultaneo ao banco de dados.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente novamente (resolve automaticamente)\n"
            "2. Isso e normal quando varias pessoas usam ao mesmo tempo",
            "ERRO-BD-060", Severidade.MEDIO),
        (["corrupt", "corrompido", "damaged"],
            "Um arquivo do sistema pode estar corrompido.\n\n"
            "COMO RESOLVER:\n"
            "1. Reinicie o sistema\n"
            "2. Se persistir, restaure um backup\n"
            "3. Em ultimo caso, reinstale o sistema",
            "ERRO-ARQ-010", Severidade.ALTO),
    ]

    # ================================================================
    # CONTADORES PARA MONITORAMENTO
    # ================================================================
    _erro_counter = {}  # {codigo: count}
    _ultimo_erro_time = {}  # {codigo: timestamp}

    @staticmethod
    def traduzir(erro, contexto=""):
        """Traduz um erro tecnico em mensagem amigavel para leigos.
        Retorna (mensagem_amigavel, codigo_erro, detalhes_tecnicos)."""
        detalhes = str(erro)
        tipo_erro = type(erro).__name__

        # 1. Tentar identificar erro MySQL pelo codigo numerico
        if hasattr(erro, 'errno') and erro.errno is not None:
            errno = erro.errno
            if errno in ErroAmigavel._MYSQL_ERRORS:
                msg, cod, sev = ErroAmigavel._MYSQL_ERRORS[errno]
                ErroAmigavel._registrar_ocorrencia(cod)
                return msg, cod, detalhes

        # 2. Tentar identificar pelo tipo de excecao Python
        for tipo_nome, dados in ErroAmigavel._PYTHON_ERRORS.items():
            msg, cod, sev = dados
            if tipo_erro == tipo_nome or tipo_nome in tipo_erro:
                ErroAmigavel._registrar_ocorrencia(cod)
                return msg, cod, detalhes

        # 3. Tentar identificar por palavras-chave na mensagem
        err_lower = detalhes.lower()
        for keywords, msg, cod, sev in ErroAmigavel._KEYWORD_ERRORS:
            if any(kw in err_lower for kw in keywords):
                ErroAmigavel._registrar_ocorrencia(cod)
                return msg, cod, detalhes

        # 4. Erro generico com mensagem amigavel
        ErroAmigavel._registrar_ocorrencia("ERRO-GEN-001")
        return (
            "Ocorreu um erro inesperado no sistema.\n\n"
            "O QUE ACONTECEU:\n"
            "Algo nao funcionou como esperado.\n\n"
            "COMO RESOLVER:\n"
            "1. Tente a operacao novamente\n"
            "2. Se persistir, reinicie o sistema\n"
            "3. Anote o codigo do erro abaixo\n"
            "4. Entre em contato com o suporte tecnico",
            "ERRO-GEN-001", detalhes)

    @staticmethod
    def _registrar_ocorrencia(codigo):
        """Registra ocorrencia de erro para monitoramento."""
        ErroAmigavel._erro_counter[codigo] = ErroAmigavel._erro_counter.get(codigo, 0) + 1
        ErroAmigavel._ultimo_erro_time[codigo] = time.time()

    @staticmethod
    def obter_severidade(erro):
        """Retorna a severidade do erro."""
        if hasattr(erro, 'errno') and erro.errno is not None:
            if erro.errno in ErroAmigavel._MYSQL_ERRORS:
                return ErroAmigavel._MYSQL_ERRORS[erro.errno][2]
        tipo_erro = type(erro).__name__
        for tipo_nome, dados in ErroAmigavel._PYTHON_ERRORS.items():
            if tipo_erro == tipo_nome or tipo_nome in tipo_erro:
                return dados[2]
        return Severidade.MEDIO

    @staticmethod
    def log_erro(erro, contexto="", nivel=None):
        """Registra o erro no arquivo de log com detalhes tecnicos completos."""
        try:
            msg_amigavel, codigo, detalhes = ErroAmigavel.traduzir(erro, contexto)
            severidade = nivel or ErroAmigavel.obter_severidade(erro)
            log_msg = (
                f"[{codigo}] [{severidade.value}] "
                f"Contexto: {contexto} | "
                f"Tipo: {type(erro).__name__} | "
                f"Detalhes: {detalhes} | "
                f"Ocorrencias: {ErroAmigavel._erro_counter.get(codigo, 1)} | "
                f"Traceback: {traceback.format_exc()}"
            )
            if severidade in (Severidade.CRITICO, Severidade.ALTO):
                _logger.error(log_msg)
            elif severidade == Severidade.MEDIO:
                _logger.warning(log_msg)
            else:
                _logger.info(log_msg)
        except Exception:
            try:
                _logger.error(f"Erro nao catalogado: {erro} | Contexto: {contexto}")
            except Exception:
                pass

    @staticmethod
    def mostrar_erro(erro, contexto="", parent=None):
        """Exibe mensagem amigavel ao usuario e registra no log.
        A mensagem e clara, direta e inclui passos para resolver."""
        msg_amigavel, codigo, detalhes = ErroAmigavel.traduzir(erro, contexto)
        ErroAmigavel.log_erro(erro, contexto)
        severidade = ErroAmigavel.obter_severidade(erro)

        # Titulo baseado na severidade
        titulos = {
            Severidade.CRITICO: "Erro Critico - Atencao!",
            Severidade.ALTO: "Ops! Algo deu errado",
            Severidade.MEDIO: "Aviso - Problema Detectado",
            Severidade.BAIXO: "Atencao",
            Severidade.INFO: "Informacao",
        }
        titulo = titulos.get(severidade, "Ops! Algo deu errado")

        # Montar mensagem completa
        mensagem_completa = (
            f"{msg_amigavel}\n\n"
            f"{'='*40}\n"
            f"Codigo do erro: {codigo}\n"
            f"Se precisar de ajuda, informe este codigo ao suporte."
        )

        try:
            if severidade in (Severidade.CRITICO, Severidade.ALTO):
                if parent:
                    messagebox.showerror(titulo, mensagem_completa, parent=parent)
                else:
                    messagebox.showerror(titulo, mensagem_completa)
            else:
                if parent:
                    messagebox.showwarning(titulo, mensagem_completa, parent=parent)
                else:
                    messagebox.showwarning(titulo, mensagem_completa)
        except Exception:
            print(f"[ERRO {codigo}] {msg_amigavel}")

        return codigo

    @staticmethod
    def mostrar_erro_toast(erro, contexto=""):
        """Exibe erro como toast (nao bloqueante) e registra no log."""
        msg_amigavel, codigo, detalhes = ErroAmigavel.traduzir(erro, contexto)
        ErroAmigavel.log_erro(erro, contexto)
        severidade = ErroAmigavel.obter_severidade(erro)
        try:
            primeira_linha = msg_amigavel.split(chr(10))[0]
            if severidade in (Severidade.CRITICO, Severidade.ALTO):
                ToastManager.error(f"{primeira_linha} ({codigo})", duration=6000)
            else:
                ToastManager.warning(f"{primeira_linha} ({codigo})", duration=4000)
        except Exception:
            pass
        return codigo

    @staticmethod
    def mostrar_aviso(mensagem, titulo="Atencao", parent=None):
        """Exibe aviso amigavel ao usuario."""
        try:
            if parent:
                messagebox.showwarning(titulo, mensagem, parent=parent)
            else:
                messagebox.showwarning(titulo, mensagem)
        except Exception:
            pass

    @staticmethod
    def mostrar_info(mensagem, titulo="Informacao", parent=None):
        """Exibe mensagem informativa ao usuario."""
        try:
            if parent:
                messagebox.showinfo(titulo, mensagem, parent=parent)
            else:
                messagebox.showinfo(titulo, mensagem)
        except Exception:
            pass

    @staticmethod
    def mostrar_sucesso(mensagem, titulo="Sucesso", parent=None):
        """Exibe mensagem de sucesso ao usuario."""
        try:
            if parent:
                messagebox.showinfo(titulo, mensagem, parent=parent)
            else:
                messagebox.showinfo(titulo, mensagem)
        except Exception:
            pass

    @staticmethod
    def confirmar(mensagem, titulo="Confirmar", parent=None):
        """Exibe dialogo de confirmacao e retorna True/False."""
        try:
            if parent:
                return messagebox.askyesno(titulo, mensagem, parent=parent)
            else:
                return messagebox.askyesno(titulo, mensagem)
        except Exception:
            return False

    @staticmethod
    def validar_campo_obrigatorio(valor, nome_campo, parent=None):
        """Valida se um campo obrigatorio foi preenchido.
        Retorna True se valido, False se vazio (e mostra aviso)."""
        if not valor or (isinstance(valor, str) and not valor.strip()):
            ErroAmigavel.mostrar_aviso(
                f"O campo '{nome_campo}' e obrigatorio.\n\n"
                f"Por favor, preencha o campo '{nome_campo}' para continuar.",
                "Campo Obrigatorio", parent)
            return False
        return True

    @staticmethod
    def validar_valor_numerico(valor_str, nome_campo, minimo=0, parent=None):
        """Valida se um valor e numerico e esta dentro do range.
        Retorna o valor float se valido, None se invalido."""
        try:
            valor = float(str(valor_str).replace(".", "").replace(",", ".").strip())
            if valor < minimo:
                ErroAmigavel.mostrar_aviso(
                    f"O valor do campo '{nome_campo}' deve ser maior que {minimo}.\n\n"
                    f"Valor informado: {valor_str}\n"
                    f"Por favor, informe um valor valido.",
                    "Valor Invalido", parent)
                return None
            return valor
        except (ValueError, TypeError):
            ErroAmigavel.mostrar_aviso(
                f"O campo '{nome_campo}' deve conter apenas numeros.\n\n"
                f"Valor informado: '{valor_str}'\n"
                f"Use virgula para centavos. Exemplo: 10,50",
                "Valor Invalido", parent)
            return None

    @staticmethod
    def validar_selecao(selecao, nome_item="item", parent=None):
        """Valida se um item foi selecionado na lista.
        Retorna True se selecionado, False se nao."""
        if not selecao:
            ErroAmigavel.mostrar_aviso(
                f"Nenhum {nome_item} foi selecionado.\n\n"
                f"Por favor, clique sobre o {nome_item} desejado na lista\n"
                f"e tente novamente.",
                f"Selecione um {nome_item}", parent)
            return False
        return True

    @staticmethod
    def diagnosticar_conexao(host="localhost", port=3306):
        """Realiza diagnostico da conexao com o banco de dados.
        Retorna (ok, mensagem_diagnostico)."""
        diagnostico = []
        ok = True

        # 1. Verificar se o host e acessivel
        try:
            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            sock.settimeout(3)
            result = sock.connect_ex((host, port))
            sock.close()
            if result == 0:
                diagnostico.append("Servidor acessivel na porta " + str(port))
            else:
                diagnostico.append(f"PROBLEMA: Porta {port} nao esta aberta no servidor")
                diagnostico.append(f"  -> Verifique se o MySQL esta rodando")
                ok = False
        except socket.gaierror:
            diagnostico.append(f"PROBLEMA: Endereco '{host}' nao encontrado")
            diagnostico.append(f"  -> Verifique se o endereco esta correto")
            ok = False
        except Exception as e:
            diagnostico.append(f"PROBLEMA: Erro ao testar conexao: {e}")
            ok = False

        # 2. Verificar se e localhost
        if host in ("localhost", "127.0.0.1"):
            diagnostico.append("Banco de dados configurado localmente (neste PC)")
        else:
            diagnostico.append(f"Banco de dados em servidor remoto: {host}")

        return ok, "\n".join(diagnostico)

    @staticmethod
    def obter_estatisticas_erros():
        """Retorna estatisticas dos erros ocorridos na sessao."""
        if not ErroAmigavel._erro_counter:
            return "Nenhum erro registrado nesta sessao."
        stats = []
        for cod, count in sorted(ErroAmigavel._erro_counter.items(),
                                   key=lambda x: x[1], reverse=True):
            ultimo = ErroAmigavel._ultimo_erro_time.get(cod, 0)
            if ultimo:
                ultimo_str = datetime.datetime.fromtimestamp(ultimo).strftime("%H:%M:%S")
            else:
                ultimo_str = "N/A"
            stats.append(f"  {cod}: {count}x (ultimo: {ultimo_str})")
        return "Erros desta sessao:\n" + "\n".join(stats)



# ============================================================================
# PALETA DE CORES FUTURISTICA PREMIUM
# ============================================================================
# Fundo principal - gradiente escuro profundo
COR_FUNDO = "#0a0e1a"
COR_FUNDO2 = "#0d1225"
COR_FUNDO3 = "#111830"

# Cores de destaque neon
COR_PRIMARIA = "#00d4ff"          # Ciano neon brilhante
COR_PRIMARIA_GLOW = "#00a8cc"     # Ciano glow
COR_SECUNDARIA = "#7c3aed"        # Roxo vibrante
COR_ACCENT = "#f72585"            # Rosa neon
COR_ACCENT2 = "#4cc9f0"           # Azul claro neon

# Status
COR_SUCESSO = "#00f5a0"           # Verde neon
COR_ALERTA = "#fbbf24"            # Amarelo dourado
COR_ERRO = "#ff3860"              # Vermelho neon

# Texto
COR_TEXTO = "#e8eaed"
COR_TEXTO2 = "#8892a4"
COR_TEXTO_GLOW = "#ffffff"

# Cards e superficies - glassmorphism
COR_CARD = "#131a2e"
COR_CARD_HOVER = "#1a2340"
COR_CARD_BORDER = "#1e2d4a"
COR_GLASS = "#0f1629"
COR_GLASS_BORDER = "#2a3a5c"

# Botoes
COR_BOTAO = "#00d4ff"
COR_BOTAO_HOVER = "#33ddff"
COR_BOTAO_VERDE = "#00c853"
COR_BOTAO_AZUL = "#2979ff"
COR_BOTAO_LARANJA = "#ff6d00"

# Entrada
COR_ENTRADA = "#0f1629"
COR_ENTRADA_BORDER = "#2a3a5c"
COR_ENTRADA_FOCUS = "#00d4ff"

# Sidebar
COR_SIDEBAR = "#0b1020"
COR_SIDEBAR_HOVER = "#141e38"
COR_SIDEBAR_ACTIVE = "#00d4ff"

# Gradientes simulados
GRADIENT_START = "#0a0e1a"
GRADIENT_END = "#111830"

# ============================================================================
# SISTEMA DE TEMAS ALTERNAVEIS
# ============================================================================

THEMES = {
    "Dark Cyber": {
        "fundo": "#0a0e1a", "fundo2": "#0d1225", "fundo3": "#111830",
        "primaria": "#00d4ff", "primaria_glow": "#00a8cc",
        "secundaria": "#7c3aed", "accent": "#f72585", "accent2": "#4cc9f0",
        "sucesso": "#00f5a0", "alerta": "#fbbf24", "erro": "#ff3860",
        "texto": "#e8eaed", "texto2": "#8892a4",
        "card": "#131a2e", "card_hover": "#1a2340", "card_border": "#1e2d4a",
        "glass": "#0f1629", "glass_border": "#2a3a5c",
        "botao": "#00d4ff", "botao_verde": "#00c853",
        "botao_azul": "#2979ff", "botao_laranja": "#ff6d00",
        "entrada": "#0f1629", "entrada_border": "#2a3a5c",
    },
    "Navy Pro": {
        "fundo": "#050d1a", "fundo2": "#071022", "fundo3": "#0a1530",
        "primaria": "#3a9eff", "primaria_glow": "#2080dd",
        "secundaria": "#5c4ecc", "accent": "#e040fb", "accent2": "#29b6f6",
        "sucesso": "#26c6da", "alerta": "#ffca28", "erro": "#ef5350",
        "texto": "#eceff1", "texto2": "#78909c",
        "card": "#0d1b33", "card_hover": "#122240", "card_border": "#1a3050",
        "glass": "#091528", "glass_border": "#1e3a60",
        "botao": "#3a9eff", "botao_verde": "#00acc1",
        "botao_azul": "#1565c0", "botao_laranja": "#f4511e",
        "entrada": "#091528", "entrada_border": "#1e3a60",
    },
    "Midnight": {
        "fundo": "#0d0d0d", "fundo2": "#121212", "fundo3": "#1a1a1a",
        "primaria": "#bb86fc", "primaria_glow": "#9b66dc",
        "secundaria": "#03dac6", "accent": "#cf6679", "accent2": "#018786",
        "sucesso": "#03dac6", "alerta": "#f9a825", "erro": "#cf6679",
        "texto": "#e1e1e1", "texto2": "#888888",
        "card": "#1e1e1e", "card_hover": "#252525", "card_border": "#333333",
        "glass": "#181818", "glass_border": "#3a3a3a",
        "botao": "#bb86fc", "botao_verde": "#03dac6",
        "botao_azul": "#6200ee", "botao_laranja": "#ff6d00",
        "entrada": "#1a1a1a", "entrada_border": "#3a3a3a",
    },
}

_ACTIVE_THEME = "Dark Cyber"

def apply_theme(theme_name):
    """Aplica o tema selecionado atualizando todas as variaveis globais de cor."""
    global _ACTIVE_THEME
    global COR_FUNDO, COR_FUNDO2, COR_FUNDO3
    global COR_PRIMARIA, COR_PRIMARIA_GLOW, COR_SECUNDARIA, COR_ACCENT, COR_ACCENT2
    global COR_SUCESSO, COR_ALERTA, COR_ERRO
    global COR_TEXTO, COR_TEXTO2
    global COR_CARD, COR_CARD_HOVER, COR_CARD_BORDER
    global COR_GLASS, COR_GLASS_BORDER
    global COR_BOTAO, COR_BOTAO_VERDE, COR_BOTAO_AZUL, COR_BOTAO_LARANJA
    global COR_ENTRADA, COR_ENTRADA_BORDER, COR_ENTRADA_FOCUS
    global COR_SIDEBAR, COR_SIDEBAR_HOVER, COR_SIDEBAR_ACTIVE
    global GRADIENT_START, GRADIENT_END

    t = THEMES.get(theme_name, THEMES["Dark Cyber"])
    _ACTIVE_THEME = theme_name
    COR_FUNDO = t["fundo"]
    COR_FUNDO2 = t["fundo2"]
    COR_FUNDO3 = t["fundo3"]
    COR_PRIMARIA = t["primaria"]
    COR_PRIMARIA_GLOW = t["primaria_glow"]
    COR_SECUNDARIA = t["secundaria"]
    COR_ACCENT = t["accent"]
    COR_ACCENT2 = t["accent2"]
    COR_SUCESSO = t["sucesso"]
    COR_ALERTA = t["alerta"]
    COR_ERRO = t["erro"]
    COR_TEXTO = t["texto"]
    COR_TEXTO2 = t["texto2"]
    COR_CARD = t["card"]
    COR_CARD_HOVER = t["card_hover"]
    COR_CARD_BORDER = t["card_border"]
    COR_GLASS = t["glass"]
    COR_GLASS_BORDER = t["glass_border"]
    COR_BOTAO = t["botao"]
    COR_BOTAO_VERDE = t["botao_verde"]
    COR_BOTAO_AZUL = t["botao_azul"]
    COR_BOTAO_LARANJA = t["botao_laranja"]
    COR_ENTRADA = t["entrada"]
    COR_ENTRADA_BORDER = t["entrada_border"]
    COR_ENTRADA_FOCUS = t["primaria"]
    COR_SIDEBAR = t["fundo2"]
    COR_SIDEBAR_HOVER = t["card"]
    COR_SIDEBAR_ACTIVE = t["primaria"]
    GRADIENT_START = t["fundo"]
    GRADIENT_END = t["fundo3"]

def get_theme_preference():
    """Le preferencia de tema salva no config."""
    try:
        if os.path.exists(CONFIG_FILE):
            with open(CONFIG_FILE, "r") as f:
                cfg = json.load(f)
            return cfg.get("tema", "Dark Cyber")
    except Exception:
        pass
    return "Dark Cyber"

# Aplicar tema ao iniciar
apply_theme(get_theme_preference())

# ============================================================================
# GERENCIADOR DE LICENCA (Alinhado com LicencaManager.java do Android)
# ============================================================================

class LicencaManager:
    """Gerenciador de licenca do PDV Pro Desktop.
    Replica a logica do LicencaManager.java do Android:
    - Gera contra-chave unica por maquina (MD5 do machine_id + '_PDV')
    - Valida chave de ativacao via SHA-256(contraChave + SECRET_KEY + dataExpiracao)
    - Cache local em arquivo JSON (equivalente ao SharedPreferences do Android)
    - Verificacao no banco MySQL (tabela licenca)
    - Bloqueio do sistema quando licenca invalida, faltante ou expirada
    - Aceita licencas ativadas por QUALQUER dispositivo (Android ou Desktop)
      desde que os dados (contra_chave + chave_ativacao + data_expiracao) sejam
      consistentes entre si no banco MySQL.
    """
    SECRET_KEY = "PDV_PRO_V3_2024_SECRET"
    _cache = None
    _lock = threading.Lock()
    _last_known_date = None  # Maior data ja vista (anti-fraude de relogio)
    _date_fraud_detected = False  # Flag de fraude de data detectada

    # ---- PROTECAO ANTI-FRAUDE DE DATA ----

    @staticmethod
    def _obter_hora_ntp(timeout=3):
        """Obtem a data/hora atual de um servidor NTP (sem dependencias externas).
        Usa protocolo NTP puro via socket UDP.
        Retorna datetime.date ou None se falhar."""
        NTP_SERVERS = [
            "pool.ntp.org",
            "time.google.com",
            "time.windows.com",
            "time.nist.gov",
            "a.ntp.br",
        ]
        NTP_EPOCH = datetime.datetime(1900, 1, 1)
        for server in NTP_SERVERS:
            try:
                client = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
                client.settimeout(timeout)
                # Pacote NTP: 48 bytes, primeiro byte = 0x1B (LI=0, VN=3, Mode=3)
                data = b'\x1b' + 47 * b'\0'
                client.sendto(data, (server, 123))
                resp, _ = client.recvfrom(1024)
                client.close()
                if len(resp) >= 48:
                    # Transmit Timestamp esta nos bytes 40-47
                    t = struct.unpack('!12I', resp)[10]
                    # Converter NTP timestamp para datetime
                    ntp_time = NTP_EPOCH + datetime.timedelta(seconds=t)
                    print(f"[LICENCA] Hora NTP obtida de {server}: {ntp_time.date()}")
                    return ntp_time.date()
            except Exception as e:
                print(f"[LICENCA] Falha NTP ({server}): {e}")
                continue
        print("[LICENCA] Nao foi possivel obter hora de nenhum servidor NTP")
        return None

    @staticmethod
    def _obter_hora_mysql():
        """Obtem a data atual do servidor MySQL via SELECT CURDATE().
        Retorna datetime.date ou None se falhar."""
        try:
            db = DatabaseHelper.get_instance()
            rows = db.execute_query("SELECT CURDATE() AS hoje")
            if rows:
                hoje_db = rows[0].get("hoje")
                if hoje_db:
                    if isinstance(hoje_db, datetime.date):
                        print(f"[LICENCA] Hora MySQL: {hoje_db}")
                        return hoje_db
                    # Se vier como string
                    dt = datetime.datetime.strptime(str(hoje_db)[:10], "%Y-%m-%d").date()
                    print(f"[LICENCA] Hora MySQL (str): {dt}")
                    return dt
        except Exception as e:
            print(f"[LICENCA] Falha ao obter hora do MySQL: {e}")
        return None

    @staticmethod
    def _carregar_date_control():
        """Carrega o arquivo de controle de data (maior data ja vista)."""
        try:
            if os.path.exists(LICENCA_DATE_CONTROL_FILE):
                with open(LICENCA_DATE_CONTROL_FILE, "r", encoding="utf-8") as f:
                    data = json.load(f)
                if isinstance(data, dict):
                    last_str = data.get("last_known_date")
                    if last_str:
                        return datetime.datetime.strptime(last_str, "%Y-%m-%d").date()
        except Exception as e:
            print(f"[LICENCA] Erro ao carregar date_control: {e}")
        return None

    @staticmethod
    def _salvar_date_control(data_atual):
        """Salva a maior data ja vista no arquivo de controle."""
        try:
            data_str = data_atual.strftime("%Y-%m-%d") if isinstance(data_atual, datetime.date) else str(data_atual)
            control = {
                "last_known_date": data_str,
                "last_update_ts": time.time()
            }
            tmp_file = LICENCA_DATE_CONTROL_FILE + ".tmp"
            with open(tmp_file, "w", encoding="utf-8") as f:
                json.dump(control, f, indent=2)
                f.flush()
                os.fsync(f.fileno())
            if os.path.exists(LICENCA_DATE_CONTROL_FILE):
                try:
                    os.remove(LICENCA_DATE_CONTROL_FILE)
                except Exception:
                    pass
            os.rename(tmp_file, LICENCA_DATE_CONTROL_FILE)
            LicencaManager._last_known_date = data_atual
            print(f"[LICENCA] Date control salvo: {data_str}")
        except Exception as e:
            print(f"[LICENCA] Erro ao salvar date_control: {e}")

    @staticmethod
    def _obter_data_confiavel():
        """Obtem a data atual de forma confiavel, detectando manipulacao de relogio.
        Estrategia:
        1. Tenta obter data do MySQL (fonte mais confiavel, servidor externo)
        2. Tenta obter data via NTP (fonte online)
        3. Compara com last_known_date (maior data ja vista)
        4. Se data local < last_known_date e nao ha fonte externa, BLOQUEIA
        Retorna (datetime.date, bool_fraude_detectada)"""
        data_local = datetime.date.today()
        fraude = False

        # Carregar last_known_date do disco se ainda nao carregou
        if LicencaManager._last_known_date is None:
            LicencaManager._last_known_date = LicencaManager._carregar_date_control()

        last_known = LicencaManager._last_known_date

        # Tentar obter data de fonte externa confiavel
        data_externa = None
        # 1) MySQL
        data_externa = LicencaManager._obter_hora_mysql()
        # 2) NTP (se MySQL falhou)
        if data_externa is None:
            data_externa = LicencaManager._obter_hora_ntp()

        if data_externa is not None:
            # Temos data externa confiavel
            # Verificar se data local esta muito diferente (tolerancia de 1 dia)
            diff = abs((data_local - data_externa).days)
            if diff > 1:
                print(f"[LICENCA] ALERTA: Data local ({data_local}) difere da "
                      f"data externa ({data_externa}) em {diff} dias!")
                fraude = True
            # Usar a data externa como referencia
            data_confiavel = data_externa
        else:
            # Sem fonte externa - usar data local mas verificar contra last_known
            data_confiavel = data_local

        # Verificar contra last_known_date (anti-retrocesso)
        if last_known is not None:
            if data_confiavel < last_known:
                print(f"[LICENCA] FRAUDE DETECTADA: Data confiavel ({data_confiavel}) "
                      f"e ANTERIOR a ultima data conhecida ({last_known})!")
                fraude = True
                # Usar a last_known_date como data real (nao deixar voltar)
                data_confiavel = last_known

        # Atualizar last_known_date se a data atual e maior
        if last_known is None or data_confiavel > last_known:
            LicencaManager._salvar_date_control(data_confiavel)

        if fraude:
            LicencaManager._date_fraud_detected = True
            print(f"[LICENCA] Data confiavel final: {data_confiavel} (FRAUDE DETECTADA)")
        else:
            LicencaManager._date_fraud_detected = False
            print(f"[LICENCA] Data confiavel final: {data_confiavel}")

        return data_confiavel, fraude

    @staticmethod
    def _get_machine_id():
        """Gera um identificador unico da maquina (equivalente ao ANDROID_ID).
        Usa combinacao de hostname + MAC address para unicidade."""
        try:
            import uuid
            mac = uuid.getnode()
            hostname = _platform.node()
            machine_id = f"{hostname}_{mac}"
        except Exception:
            machine_id = "DEFAULT_DEVICE"
        return machine_id

    @staticmethod
    def gerar_contra_chave():
        """Gera a contra-chave unica desta maquina.
        Equivalente a LicencaManager.gerarContraChave() do Android.
        Retorna 16 caracteres hexadecimais maiusculos."""
        try:
            machine_id = LicencaManager._get_machine_id()
            raw = machine_id + "_PDV"
            md = hashlib.md5(raw.encode("utf-8")).digest()
            return "".join(f"{b:02X}" for b in md[:8])
        except Exception:
            return "0000000000000000"

    @staticmethod
    def gerar_chave_ativacao(contra_chave, data_expiracao):
        """Gera a chave de ativacao esperada.
        Equivalente a LicencaManager.gerarChaveAtivacao() do Android.
        Formato: XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX"""
        try:
            raw = contra_chave.strip() + LicencaManager.SECRET_KEY + data_expiracao.strip()
            digest = hashlib.sha256(raw.encode("utf-8")).digest()
            hex_str = "".join(f"{b:02X}" for b in digest[:16])
            # Formato: XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX
            parts = [hex_str[i:i+4] for i in range(0, 32, 4)]
            return "-".join(parts)
        except Exception:
            return ""

    @staticmethod
    def validar_chave(contra_chave, chave_ativacao, data_expiracao):
        """Valida se a chave de ativacao corresponde a contra-chave e data.
        Equivalente a LicencaManager.validarChave() do Android.
        Aplica strip() e upper() para evitar falhas por espacos ou case."""
        if not contra_chave or not chave_ativacao or not data_expiracao:
            return False
        ck = contra_chave.strip()
        ca = chave_ativacao.strip()
        de = data_expiracao.strip()
        chave_esperada = LicencaManager.gerar_chave_ativacao(ck, de)
        return chave_esperada.upper() == ca.upper()

    @staticmethod
    def _normalizar_data_exp(data_exp):
        """Normaliza a data de expiracao para o formato yyyy-MM-dd.
        Aceita formatos: 'yyyy-MM-dd', 'yyyy-MM-dd HH:mm:ss', etc.
        Retorna string no formato 'yyyy-MM-dd' ou None se invalida."""
        if not data_exp:
            return None
        data_exp = str(data_exp).strip()
        # Se ja esta no formato correto
        if len(data_exp) == 10:
            try:
                datetime.datetime.strptime(data_exp, "%Y-%m-%d")
                return data_exp
            except ValueError:
                pass
        # Tentar extrair apenas a parte da data (primeiros 10 chars)
        if len(data_exp) >= 10:
            parte_data = data_exp[:10]
            try:
                datetime.datetime.strptime(parte_data, "%Y-%m-%d")
                return parte_data
            except ValueError:
                pass
        # Tentar parse completo com hora
        for fmt in ("%Y-%m-%d %H:%M:%S", "%Y-%m-%dT%H:%M:%S", "%d/%m/%Y", "%Y-%m-%d"):
            try:
                dt = datetime.datetime.strptime(data_exp, fmt)
                return dt.strftime("%Y-%m-%d")
            except ValueError:
                continue
        return None

    @staticmethod
    def _salvar_cache_local(contra_chave, chave_ativacao, data_expiracao, valida):
        """Salva dados da licenca no cache local (arquivo JSON).
        Equivalente a salvarCacheLocal() do Android (SharedPreferences).
        Usa escrita atomica (temp + rename) para evitar corrupcao."""
        try:
            # Normalizar data para garantir formato consistente yyyy-MM-dd
            data_norm = LicencaManager._normalizar_data_exp(data_expiracao) if data_expiracao else data_expiracao
            cache = {
                "contra_chave": contra_chave.strip() if contra_chave else contra_chave,
                "chave_ativacao": chave_ativacao.strip() if chave_ativacao else chave_ativacao,
                "data_expiracao": (data_norm or data_expiracao).strip() if data_expiracao else data_expiracao,
                "licenca_valida": valida,
                "last_check_time": time.time()
            }
            # Escrita atomica: grava em arquivo temporario e renomeia
            tmp_file = LICENCA_CACHE_FILE + ".tmp"
            with open(tmp_file, "w", encoding="utf-8") as f:
                json.dump(cache, f, indent=2)
                f.flush()
                os.fsync(f.fileno())
            # Rename atomico (substitui o arquivo original)
            if os.path.exists(LICENCA_CACHE_FILE):
                try:
                    os.remove(LICENCA_CACHE_FILE)
                except Exception:
                    pass
            os.rename(tmp_file, LICENCA_CACHE_FILE)
            LicencaManager._cache = cache
            print(f"[LICENCA] Cache local salvo: {LICENCA_CACHE_FILE}")
        except Exception as e:
            print(f"[LICENCA] Erro ao salvar cache de licenca: {e}")
            # Fallback: tentar escrita direta se atomica falhou
            try:
                with open(LICENCA_CACHE_FILE, "w", encoding="utf-8") as f:
                    json.dump(cache, f, indent=2)
                LicencaManager._cache = cache
                print(f"[LICENCA] Cache local salvo (fallback direto): {LICENCA_CACHE_FILE}")
            except Exception as e2:
                print(f"[LICENCA] ERRO CRITICO ao salvar cache (fallback): {e2}")

    @staticmethod
    def _carregar_cache_local():
        """Carrega dados da licenca do cache local."""
        if LicencaManager._cache is not None:
            return LicencaManager._cache
        try:
            if os.path.exists(LICENCA_CACHE_FILE):
                print(f"[LICENCA] Carregando cache de: {LICENCA_CACHE_FILE}")
                with open(LICENCA_CACHE_FILE, "r", encoding="utf-8") as f:
                    data = json.load(f)
                if isinstance(data, dict) and data.get("contra_chave"):
                    LicencaManager._cache = data
                    print(f"[LICENCA] Cache carregado com sucesso "
                          f"(ck={data.get('contra_chave', '')[:8]}..., "
                          f"valida={data.get('licenca_valida')}, "
                          f"data_exp={data.get('data_expiracao')})")
                    return LicencaManager._cache
                else:
                    print(f"[LICENCA] Cache local existe mas esta vazio ou invalido")
            else:
                print(f"[LICENCA] Arquivo de cache nao encontrado: {LICENCA_CACHE_FILE}")
        except json.JSONDecodeError as e:
            print(f"[LICENCA] Cache local corrompido (JSON invalido): {e}")
            # Tentar remover cache corrompido
            try:
                os.remove(LICENCA_CACHE_FILE)
            except Exception:
                pass
        except Exception as e:
            print(f"[LICENCA] Erro ao carregar cache de licenca: {e}")
        return None

    @staticmethod
    def _limpar_cache_local():
        """Limpa o cache local da licenca."""
        print(f"[LICENCA] Limpando cache local (memoria e disco)...")
        LicencaManager._cache = None
        try:
            if os.path.exists(LICENCA_CACHE_FILE):
                os.remove(LICENCA_CACHE_FILE)
                print(f"[LICENCA] Arquivo de cache removido: {LICENCA_CACHE_FILE}")
            else:
                print(f"[LICENCA] Arquivo de cache nao existia: {LICENCA_CACHE_FILE}")
        except Exception as e:
            print(f"[LICENCA] Erro ao remover cache: {e}")

    @staticmethod
    def _verificar_cache_local():
        """Verifica a licenca usando o cache local.
        Equivalente a verificarCacheLocal() do Android."""
        try:
            cache = LicencaManager._carregar_cache_local()
            if not cache:
                print("[LICENCA] Cache local: nenhum cache disponivel")
                return False
            if not cache.get("licenca_valida", False):
                print("[LICENCA] Cache local: flag licenca_valida=False")
                return False
            contra_chave = cache.get("contra_chave")
            chave_ativacao = cache.get("chave_ativacao")
            data_exp = cache.get("data_expiracao")
            if not contra_chave or not chave_ativacao or not data_exp:
                print(f"[LICENCA] Cache local: campos vazios "
                      f"(ck={'OK' if contra_chave else 'VAZIO'}, "
                      f"ca={'OK' if chave_ativacao else 'VAZIO'}, "
                      f"de={'OK' if data_exp else 'VAZIO'})")
                return False
            # Normalizar data
            data_exp_original = data_exp
            data_exp = LicencaManager._normalizar_data_exp(data_exp)
            if not data_exp:
                print(f"[LICENCA] Cache local: data invalida '{data_exp_original}'")
                return False
            # Validar a chave localmente
            if not LicencaManager.validar_chave(contra_chave, chave_ativacao, data_exp):
                print(f"[LICENCA] Cache local: chave INVALIDA "
                      f"(ck={contra_chave[:8]}..., de={data_exp})")
                # Tentar validar com data original (caso normalização tenha alterado)
                if data_exp != data_exp_original:
                    if LicencaManager.validar_chave(contra_chave, chave_ativacao, data_exp_original):
                        print(f"[LICENCA] Cache local: chave VALIDA com data original '{data_exp_original}'")
                        # Atualizar cache com data normalizada e chave correta
                        chave_correta = LicencaManager.gerar_chave_ativacao(contra_chave, data_exp)
                        LicencaManager._salvar_cache_local(contra_chave, chave_correta, data_exp, True)
                        exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
                        hoje_confiavel, fraude = LicencaManager._obter_data_confiavel()
                        if fraude:
                            print("[LICENCA] Cache local: FRAUDE DE DATA detectada - licenca BLOQUEADA")
                            return False
                        return hoje_confiavel <= exp_date
                return False
            # Verificar se nao expirou (usando data confiavel anti-fraude)
            exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
            hoje, fraude = LicencaManager._obter_data_confiavel()
            if fraude:
                print("[LICENCA] Cache local: FRAUDE DE DATA detectada - licenca BLOQUEADA")
                return False
            valida = hoje <= exp_date
            if valida:
                print(f"[LICENCA] Cache local: licenca VALIDA ate {data_exp} "
                      f"({(exp_date - hoje).days} dias restantes)")
            else:
                print(f"[LICENCA] Cache local: licenca EXPIRADA em {data_exp}")
            return valida
        except Exception as e:
            print(f"[LICENCA] Erro ao verificar cache local de licenca: {e}")
            import traceback
            traceback.print_exc()
            return False

    @staticmethod
    def _verificar_licenca_banco():
        """Verifica a licenca diretamente no banco MySQL.
        Equivalente a verificarLicencaBanco() do Android.
        Aceita licencas ativadas por qualquer dispositivo (Android ou Desktop)
        desde que contra_chave + chave_ativacao + data_expiracao sejam consistentes."""
        try:
            from mysql.connector import Error as MySQLError
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT contra_chave, chave_ativacao, "
                "DATE_FORMAT(data_expiracao, '%%Y-%%m-%%d') AS data_exp "
                "FROM licenca ORDER BY id DESC LIMIT 1"
            )
            if rows:
                row = rows[0]
                contra_chave = (row.get("contra_chave") or "").strip()
                chave_ativacao = (row.get("chave_ativacao") or "").strip()
                data_exp = (row.get("data_exp") or "").strip()
                print(f"[LICENCA] Banco: encontrada licenca "
                      f"(ck={contra_chave[:8] if contra_chave else 'VAZIO'}..., "
                      f"data_exp={data_exp or 'VAZIO'})")
                if not contra_chave or not chave_ativacao or not data_exp:
                    print("[LICENCA] Banco: campos vazios na licenca")
                    return False
                # Normalizar data
                data_exp = LicencaManager._normalizar_data_exp(data_exp)
                if not data_exp:
                    print(f"[LICENCA] Banco: data de expiracao invalida")
                    return False
                # Validar chave - a contra_chave pode ser de qualquer dispositivo
                # (Android ou Desktop). A validacao verifica se os 3 campos sao
                # consistentes entre si, independente de quem gerou.
                if not LicencaManager.validar_chave(contra_chave, chave_ativacao, data_exp):
                    print(f"[LICENCA] Banco: chave de ativacao INVALIDA "
                          f"(contra_chave={contra_chave[:8]}..., "
                          f"data_exp={data_exp})")
                    return False
                # Verificar expiracao (usando data confiavel anti-fraude)
                exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
                hoje, fraude = LicencaManager._obter_data_confiavel()
                if fraude:
                    print("[LICENCA] Banco: FRAUDE DE DATA detectada - licenca BLOQUEADA")
                    LicencaManager._limpar_cache_local()
                    return False
                valida = hoje <= exp_date
                # Atualizar cache local com dados do banco
                # (inclui contra_chave do dispositivo que ativou, nao a local)
                if valida:
                    LicencaManager._salvar_cache_local(
                        contra_chave, chave_ativacao, data_exp, True)
                    print(f"[LICENCA] Banco: licenca VALIDA ate {data_exp} "
                          f"({(exp_date - hoje).days} dias restantes)")
                else:
                    LicencaManager._limpar_cache_local()
                    print(f"[LICENCA] Banco: licenca EXPIRADA em {data_exp}")
                return valida
            print("[LICENCA] Banco: nenhuma licenca encontrada")
            return False
        except Exception as e:
            print(f"[LICENCA] Erro ao verificar licenca no banco: {e}")
            raise

    @staticmethod
    def verificar_licenca():
        """Verifica se a licenca e valida.
        Estrategia robusta com fallback:
        1. Verifica cache local primeiro (rapido, offline)
        2. Tenta verificar no banco remoto
        3. Se banco responde com licenca valida, usa banco e atualiza cache
        4. Se banco responde sem licenca mas cache e valido, confia no cache
           (protege contra tabela vazia/recriada no banco)
        5. Se banco falha na conexao, confia no cache local
        Retorna True se licenca valida, False caso contrario."""
        with LicencaManager._lock:
            print("[LICENCA] Iniciando verificacao de licenca...")
            print(f"[LICENCA] APP_DIR: {APP_DIR}")
            print(f"[LICENCA] Cache file: {LICENCA_CACHE_FILE}")
            print(f"[LICENCA] Cache file existe: {os.path.exists(LICENCA_CACHE_FILE)}")

            # PASSO 1: Verificar cache local
            cache_valido = LicencaManager._verificar_cache_local()
            print(f"[LICENCA] Cache local: {'VALIDO' if cache_valido else 'INVALIDO/AUSENTE'}")

            # PASSO 2: Tentar verificar no banco remoto
            banco_verificado = False
            banco_resultado = False
            try:
                banco_resultado = LicencaManager._verificar_licenca_banco()
                banco_verificado = True
                print(f"[LICENCA] Banco: {'VALIDA' if banco_resultado else 'INVALIDA/AUSENTE'}")
            except Exception as e:
                print(f"[LICENCA] Banco: FALHA na conexao ({e})")

            # PASSO 3: Decidir resultado com logica robusta
            if banco_verificado and banco_resultado:
                # Banco confirmou licenca valida - melhor cenario
                print("[LICENCA] Decisao: BANCO confirmou licenca VALIDA")
                return True

            if banco_verificado and not banco_resultado:
                # Banco respondeu mas nao encontrou licenca valida
                if cache_valido:
                    # Cache local tem licenca valida - pode ser que o banco
                    # perdeu os dados (tabela recriada, migração, etc.)
                    # Restaurar a licenca do cache para o banco
                    print("[LICENCA] Banco sem licenca mas CACHE VALIDO - "
                          "restaurando licenca do cache para o banco...")
                    try:
                        cache = LicencaManager._carregar_cache_local()
                        if cache:
                            ck = cache.get("contra_chave", "").strip()
                            ca = cache.get("chave_ativacao", "").strip()
                            de = cache.get("data_expiracao", "").strip()
                            de = LicencaManager._normalizar_data_exp(de) or de
                            if ck and ca and de:
                                db = DatabaseHelper.get_instance()
                                db.execute_update("DELETE FROM licenca")
                                db.execute_update(
                                    "INSERT INTO licenca (contra_chave, chave_ativacao, "
                                    "data_ativacao, data_expiracao) "
                                    "VALUES (%s, %s, NOW(), %s)",
                                    (ck, ca, de)
                                )
                                print("[LICENCA] Licenca restaurada do cache para o banco")
                    except Exception as e:
                        print(f"[LICENCA] Nao foi possivel restaurar licenca no banco: {e}")
                    print("[LICENCA] Decisao: usando CACHE LOCAL valido -> LICENCA VALIDA")
                    return True
                else:
                    # Banco sem licenca e cache tambem invalido
                    print("[LICENCA] Decisao: banco sem licenca e cache invalido -> BLOQUEADO")
                    return False

            if not banco_verificado:
                # Banco indisponivel - usar cache como fallback
                if cache_valido:
                    print("[LICENCA] Decisao: banco indisponivel, usando CACHE LOCAL -> LICENCA VALIDA")
                    return True

            print("[LICENCA] Decisao: sem licenca valida em nenhuma fonte -> BLOQUEADO")
            return False

    @staticmethod
    def is_licenca_expirada():
        """Verifica se existe licenca cadastrada mas VENCIDA.
        Equivalente a isLicencaExpirada() do Android.
        Usa data confiavel anti-fraude."""
        try:
            hoje, _ = LicencaManager._obter_data_confiavel()
        except Exception:
            hoje = datetime.date.today()
        # Verificar cache local
        try:
            cache = LicencaManager._carregar_cache_local()
            if cache:
                data_exp = cache.get("data_expiracao")
                chave = cache.get("chave_ativacao")
                if data_exp and chave:
                    data_exp = LicencaManager._normalizar_data_exp(data_exp)
                    if data_exp:
                        exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
                        if exp_date < hoje:
                            return True
        except Exception:
            pass
        # Verificar banco
        try:
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT DATE_FORMAT(data_expiracao, '%%Y-%%m-%%d') AS data_exp "
                "FROM licenca ORDER BY id DESC LIMIT 1"
            )
            if rows:
                data_exp = (rows[0].get("data_exp") or "").strip()
                data_exp = LicencaManager._normalizar_data_exp(data_exp)
                if data_exp:
                    exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
                    if exp_date < hoje:
                        return True
        except Exception:
            pass
        return False

    @staticmethod
    def get_data_expiracao():
        """Retorna a data de expiracao da licenca (yyyy-MM-dd) ou None."""
        # Cache local primeiro
        try:
            cache = LicencaManager._carregar_cache_local()
            if cache:
                data_exp = cache.get("data_expiracao")
                if data_exp:
                    norm = LicencaManager._normalizar_data_exp(data_exp)
                    if norm:
                        return norm
        except Exception:
            pass
        # Banco
        try:
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT DATE_FORMAT(data_expiracao, '%%Y-%%m-%%d') AS data_exp "
                "FROM licenca ORDER BY id DESC LIMIT 1"
            )
            if rows:
                data_exp = (rows[0].get("data_exp") or "").strip()
                return LicencaManager._normalizar_data_exp(data_exp)
        except Exception:
            pass
        return None

    @staticmethod
    def get_dias_para_vencimento():
        """Retorna dias restantes ate o vencimento. Negativo se vencida.
        Usa data confiavel anti-fraude."""
        data_exp = LicencaManager.get_data_expiracao()
        if not data_exp:
            return -1
        try:
            exp_date = datetime.datetime.strptime(data_exp, "%Y-%m-%d").date()
            try:
                hoje, _ = LicencaManager._obter_data_confiavel()
            except Exception:
                hoje = datetime.date.today()
            return (exp_date - hoje).days
        except Exception:
            return -1

    @staticmethod
    def ativar_licenca(contra_chave, chave_ativacao, data_expiracao):
        """Ativa a licenca (salva no cache local e no banco).
        Equivalente a ativarLicenca() do Android.
        data_expiracao no formato yyyy-MM-dd.
        
        Fluxo:
        1. Normaliza e valida os inputs
        2. Valida a chave de ativacao criptograficamente
        3. Salva no cache local (JSON) para acesso rapido
        4. Salva no banco MySQL para persistencia e sincronizacao
        5. Verifica se o salvamento no banco foi bem-sucedido
        6. Atualiza o cache interno em memoria
        """
        # Normalizar inputs
        contra_chave = (contra_chave or "").strip()
        chave_ativacao = (chave_ativacao or "").strip()
        data_expiracao = (data_expiracao or "").strip()
        data_expiracao = LicencaManager._normalizar_data_exp(data_expiracao) or data_expiracao

        print(f"[LICENCA] Tentando ativar licenca...")
        print(f"[LICENCA] Contra-chave: {contra_chave}")
        print(f"[LICENCA] Data expiracao: {data_expiracao}")

        # Validar campos obrigatorios
        if not contra_chave or not chave_ativacao or not data_expiracao:
            print("[LICENCA] ERRO: Campos obrigatorios vazios")
            return False

        # Validar data nao retroativa (usando data confiavel)
        try:
            exp_date = datetime.datetime.strptime(data_expiracao, "%Y-%m-%d").date()
            try:
                hoje_confiavel, _ = LicencaManager._obter_data_confiavel()
            except Exception:
                hoje_confiavel = datetime.date.today()
            if exp_date < hoje_confiavel:
                print(f"[LICENCA] ERRO: Data expirada ({data_expiracao})")
                return False
        except Exception:
            print(f"[LICENCA] ERRO: Data invalida ({data_expiracao})")
            return False

        # Validar chave criptograficamente
        if not LicencaManager.validar_chave(contra_chave, chave_ativacao, data_expiracao):
            print("[LICENCA] ERRO: Chave de ativacao invalida")
            return False

        print("[LICENCA] Chave validada com sucesso!")

        # Salvar no cache local PRIMEIRO (garante acesso imediato)
        LicencaManager._salvar_cache_local(
            contra_chave, chave_ativacao, data_expiracao, True)
        print("[LICENCA] Cache local salvo com sucesso")

        # Verificar se o cache foi realmente persistido no disco
        try:
            if os.path.exists(LICENCA_CACHE_FILE):
                with open(LICENCA_CACHE_FILE, "r", encoding="utf-8") as f:
                    cache_verificado = json.load(f)
                if cache_verificado.get("licenca_valida") and cache_verificado.get("contra_chave"):
                    print(f"[LICENCA] Cache verificado no disco: OK "
                          f"(ck={cache_verificado.get('contra_chave', '')[:8]}...)")
                else:
                    print("[LICENCA] AVISO: Cache no disco parece incompleto")
            else:
                print(f"[LICENCA] AVISO: Arquivo de cache NAO encontrado apos salvamento: {LICENCA_CACHE_FILE}")
        except Exception as e:
            print(f"[LICENCA] AVISO: Erro ao verificar cache no disco: {e}")

        # Salvar no banco MySQL
        banco_salvo = False
        try:
            db = DatabaseHelper.get_instance()
            # Limpar licencas anteriores
            db.execute_update("DELETE FROM licenca")
            # Inserir nova licenca
            db.execute_update(
                "INSERT INTO licenca (contra_chave, chave_ativacao, data_ativacao, data_expiracao) "
                "VALUES (%s, %s, NOW(), %s)",
                (contra_chave, chave_ativacao, data_expiracao)
            )
            # Verificar se foi salvo corretamente no banco
            rows = db.execute_query(
                "SELECT contra_chave, chave_ativacao, "
                "DATE_FORMAT(data_expiracao, '%%Y-%%m-%%d') AS data_exp "
                "FROM licenca ORDER BY id DESC LIMIT 1"
            )
            if rows:
                row = rows[0]
                ck_banco = (row.get("contra_chave") or "").strip()
                ca_banco = (row.get("chave_ativacao") or "").strip()
                de_banco = (row.get("data_exp") or "").strip()
                if (ck_banco.upper() == contra_chave.upper() and
                    ca_banco.upper() == chave_ativacao.upper() and
                    de_banco == data_expiracao):
                    banco_salvo = True
                    print("[LICENCA] Licenca salva e verificada no banco com sucesso!")
                else:
                    print(f"[LICENCA] AVISO: Dados no banco divergem do esperado")
                    print(f"[LICENCA]   Banco: ck={ck_banco}, ca={ca_banco[:12]}..., de={de_banco}")
                    print(f"[LICENCA]   Local: ck={contra_chave}, ca={chave_ativacao[:12]}..., de={data_expiracao}")
                    # Mesmo com divergencia, se o banco tem dados, considerar salvo
                    if ck_banco and ca_banco and de_banco:
                        banco_salvo = True
                        print("[LICENCA] Banco tem dados - considerando salvo")
            else:
                print("[LICENCA] AVISO: Nenhum registro encontrado no banco apos INSERT")
        except Exception as e:
            print(f"[LICENCA] AVISO: Erro ao salvar licenca no banco: {e}")
            print(f"[LICENCA] Cache local ja foi salvo - sistema funcionara offline")

        if banco_salvo:
            print("[LICENCA] Ativacao completa (cache + banco)")
        else:
            print("[LICENCA] Ativacao parcial (apenas cache local)")

        # Forcar atualizacao do cache em memoria para garantir consistencia
        LicencaManager._cache = {
            "contra_chave": contra_chave,
            "chave_ativacao": chave_ativacao,
            "data_expiracao": data_expiracao,
            "licenca_valida": True,
            "last_check_time": time.time()
        }
        print("[LICENCA] Cache em memoria atualizado")

        return True

    @staticmethod
    def limpar_licenca(senha):
        """Limpa a licenca (requer senha de confirmacao)."""
        if senha != "2394":
            return False
        LicencaManager._limpar_cache_local()
        try:
            db = DatabaseHelper.get_instance()
            db.execute_update("DELETE FROM licenca")
        except Exception:
            pass
        return True


# ============================================================================
# LOGGER DE AUDITORIA
# ============================================================================

class AuditLogger:
    """Registra acoes criticas do sistema em arquivo de log para auditoria."""
    _lock = threading.Lock()

    @staticmethod
    def log(acao, detalhes="", usuario=""):
        """Grava uma entrada no log de auditoria."""
        try:
            with AuditLogger._lock:
                ts = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")
                user_info = usuario or "Sistema"
                linha = f"[{ts}] [{user_info}] {acao}"
                if detalhes:
                    linha += f" | {detalhes}"
                with open(AUDIT_LOG_FILE, "a", encoding="utf-8", errors="replace") as f:
                    f.write(linha + "\n")
        except Exception:
            pass

    @staticmethod
    def get_entries(limit=200):
        """Retorna as ultimas N entradas do log."""
        try:
            if not os.path.exists(AUDIT_LOG_FILE):
                return []
            with open(AUDIT_LOG_FILE, "r", encoding="utf-8", errors="replace") as f:
                linhas = f.readlines()
            return [l.strip() for l in linhas[-limit:] if l.strip()]
        except Exception:
            return []


# ============================================================================
# MOTOR DE ANIMACOES E EFEITOS VISUAIS
# ============================================================================

class AnimationEngine:
    """Motor de animacoes centralizado para efeitos visuais futuristicos."""

    @staticmethod
    def fade_in(widget, duration=400, steps=20, callback=None):
        """Efeito de fade-in suave usando opacidade simulada."""
        if not widget.winfo_exists():
            return
        step_time = max(1, duration // steps)
        current_step = [0]

        def _step():
            if not widget.winfo_exists():
                return
            current_step[0] += 1
            alpha = current_step[0] / steps
            try:
                # Simular fade alterando a cor de fundo gradualmente
                r = int(10 + alpha * (int(widget.cget("bg")[1:3], 16) - 10))
                g = int(14 + alpha * (int(widget.cget("bg")[3:5], 16) - 14))
                b = int(26 + alpha * (int(widget.cget("bg")[5:7], 16) - 26))
            except Exception:
                pass
            if current_step[0] < steps:
                widget.after(step_time, _step)
            elif callback:
                callback()

        _step()

    @staticmethod
    def pulse_glow(canvas, item_id, color1, color2, duration=2000, root=None):
        """Efeito de pulso/glow continuo em um item do canvas."""
        if root is None:
            return
        steps = 60
        step_time = max(1, duration // steps)
        current = [0]
        direction = [1]

        def _parse_color(c):
            return (int(c[1:3], 16), int(c[3:5], 16), int(c[5:7], 16))

        def _to_hex(r, g, b):
            return f"#{max(0,min(255,r)):02x}{max(0,min(255,g)):02x}{max(0,min(255,b)):02x}"

        r1, g1, b1 = _parse_color(color1)
        r2, g2, b2 = _parse_color(color2)

        def _step():
            try:
                if not canvas.winfo_exists():
                    return
                t = current[0] / steps
                # Suavizar com seno
                t = (math.sin(t * math.pi * 2 - math.pi / 2) + 1) / 2
                r = int(r1 + (r2 - r1) * t)
                g = int(g1 + (g2 - g1) * t)
                b = int(b1 + (b2 - b1) * t)
                canvas.itemconfig(item_id, fill=_to_hex(r, g, b))
                current[0] += direction[0]
                if current[0] >= steps:
                    direction[0] = -1
                elif current[0] <= 0:
                    direction[0] = 1
                root.after(step_time, _step)
            except Exception:
                pass

        _step()

    @staticmethod
    def slide_in(widget, direction="left", distance=50, duration=300, steps=15):
        """Efeito de slide-in suave."""
        if not widget.winfo_exists():
            return
        step_time = max(1, duration // steps)
        current_step = [0]

        def _step():
            if not widget.winfo_exists():
                return
            current_step[0] += 1
            progress = current_step[0] / steps
            # Easing: ease-out cubic
            progress = 1 - (1 - progress) ** 3
            if current_step[0] < steps:
                widget.after(step_time, _step)

        _step()

    @staticmethod
    def typewriter(label, text, speed=30, callback=None):
        """Efeito de digitacao (typewriter) em um label."""
        current = [0]

        def _type():
            if not label.winfo_exists():
                return
            current[0] += 1
            label.config(text=text[:current[0]])
            if current[0] < len(text):
                label.after(speed, _type)
            elif callback:
                callback()

        label.config(text="")
        label.after(speed, _type)


class ParticleSystem:
    """Sistema de particulas flutuantes para fundo animado."""

    def __init__(self, canvas, width, height, num_particles=40, root=None):
        self.canvas = canvas
        self.width = width
        self.height = height
        self.root = root
        self.particles = []
        self.running = True
        self._create_particles(num_particles)

    def _create_particles(self, num):
        colors = ["#00d4ff", "#7c3aed", "#f72585", "#4cc9f0", "#00f5a0",
                  "#1a3a5c", "#2a4a6c", "#0a2a4c"]
        for _ in range(num):
            x = random.randint(0, self.width)
            y = random.randint(0, self.height)
            size = random.uniform(1, 3.5)
            speed_x = random.uniform(-0.3, 0.3)
            speed_y = random.uniform(-0.5, -0.1)
            color = random.choice(colors)
            alpha = random.uniform(0.15, 0.6)
            # Ajustar cor com base na alpha simulada
            r, g, b = int(color[1:3], 16), int(color[3:5], 16), int(color[5:7], 16)
            r = int(r * alpha + 10 * (1 - alpha))
            g = int(g * alpha + 14 * (1 - alpha))
            b = int(b * alpha + 26 * (1 - alpha))
            actual_color = f"#{max(0,min(255,r)):02x}{max(0,min(255,g)):02x}{max(0,min(255,b)):02x}"

            particle_id = self.canvas.create_oval(
                x - size, y - size, x + size, y + size,
                fill=actual_color, outline=""
            )
            self.particles.append({
                "id": particle_id, "x": x, "y": y,
                "speed_x": speed_x, "speed_y": speed_y,
                "size": size, "base_color": color, "alpha": alpha
            })

    def animate(self):
        if not self.running:
            return
        try:
            if not self.canvas.winfo_exists():
                return
        except Exception:
            return

        for p in self.particles:
            p["x"] += p["speed_x"]
            p["y"] += p["speed_y"]

            # Wrap around
            if p["y"] < -10:
                p["y"] = self.height + 10
                p["x"] = random.randint(0, self.width)
            if p["x"] < -10:
                p["x"] = self.width + 10
            elif p["x"] > self.width + 10:
                p["x"] = -10

            try:
                self.canvas.coords(
                    p["id"],
                    p["x"] - p["size"], p["y"] - p["size"],
                    p["x"] + p["size"], p["y"] + p["size"]
                )
            except Exception:
                pass

        if self.root:
            self.root.after(50, self.animate)

    def stop(self):
        self.running = False

    def update_size(self, width, height):
        self.width = width
        self.height = height


class NeonBorderEffect:
    """Cria bordas neon brilhantes ao redor de widgets."""

    @staticmethod
    def apply(widget, color=COR_PRIMARIA, width=2):
        """Aplica borda neon a um widget."""
        widget.configure(
            highlightthickness=width,
            highlightcolor=color,
            highlightbackground=COR_CARD_BORDER
        )

    @staticmethod
    def apply_hover(widget, color_normal=COR_CARD_BORDER, color_hover=COR_PRIMARIA, width=2):
        """Aplica borda neon com efeito hover."""
        widget.configure(
            highlightthickness=width,
            highlightcolor=color_normal,
            highlightbackground=color_normal
        )
        widget.bind("<Enter>", lambda e: widget.configure(
            highlightcolor=color_hover, highlightbackground=color_hover))
        widget.bind("<Leave>", lambda e: widget.configure(
            highlightcolor=color_normal, highlightbackground=color_normal))


# ============================================================================
# UNICODE ICONS (substitui emojis por simbolos elegantes)
# ============================================================================
class Icons:
    """Icones Unicode para interface futuristica."""
    VENDAS = "\u25B6"       # ▶
    COMANDAS = "\u2630"     # ☰
    CAIXA = "\u25A3"        # ▣
    HISTORICO = "\u29D6"    # ⧖
    PRODUTOS = "\u2B22"     # ⬢
    CLIENTES = "\u2B24"     # ⬤
    RELATORIOS = "\u25CE"   # ◎
    CONFIG = "\u2699"       # ⚙
    SAIR = "\u2716"         # ✖
    VOLTAR = "\u25C0"       # ◀
    ADD = "\u271A"          # ✚
    EDIT = "\u270E"         # ✎
    DELETE = "\u2716"       # ✖
    SAVE = "\u2714"         # ✔
    SEARCH = "\u2315"       # ⌕
    USER = "\u2B24"         # ⬤
    LOCK = "\u26BF"         # ⚿
    MONEY = "\u25C6"        # ◆
    MESA = "\u25A6"         # ▦
    ARMARIO = "\u25A4"      # ▤
    IMPRESSORA = "\u2399"   # ⎙
    WEB = "\u2B22"          # ⬢
    EMPRESA = "\u2B23"      # ⬣
    ENTREGA = "\u27A4"      # ➤
    GARCOM = "\u263A"       # ☺
    PERFIL = "\u2B2A"       # ⬪
    SOBRE = "\u24D8"        # ⓘ
    CLOCK = "\u25F7"        # ◷
    STAR = "\u2605"         # ★
    ARROW_R = "\u25B6"      # ▶
    ARROW_D = "\u25BC"      # ▼
    CHECK = "\u2714"        # ✔
    CROSS = "\u2718"        # ✘
    REFRESH = "\u21BB"      # ↻
    TROCAR = "\u21C4"       # ⇄
    # Novos icones v8
    EXPORT = "\u21A5"       # ↥
    IMPORT = "\u21A7"       # ↧
    FILTER = "\u25BC"       # ▼
    SORT = "\u21C5"         # ⇅
    PRINT = "\u2399"        # ⎙
    BACKUP = "\u2B07"       # ⬇
    RESTORE = "\u2B06"      # ⬆
    ALERT = "\u26A0"        # ⚠
    INFO = "\u2139"         # ℹ
    THEME = "\u25D1"        # ◑
    AUDIT = "\u2637"        # ☷
    SHORTCUT = "\u2328"     # ⌨
    KPI = "\u25A0"          # ■
    CHART = "\u25F0"        # ◰
    CALENDAR = "\u25A1"     # □
    PIN = "\u25CF"          # ●
    ONLINE = "\u25CF"       # ●
    OFFLINE = "\u25CB"      # ○
    EXPAND = "\u2B1C"       # ⬜
    COLLAPSE = "\u25AA"     # ▪
    COPY = "\u2398"         # ⎘
    PASTE = "\u2397"        # ⎗
    UNDO = "\u21BA"         # ↺
    REDO = "\u21BB"         # ↻
    FULLSCREEN = "\u26F6"   # ⛶
    SETTINGS = "\u2699"     # ⚙
    DASHBOARD = "\u2317"    # ⌗
    HELP = "\u2753"         # ❓
    LOGOUT = "\u23FB"       # ⏻
    SHIELD = "\u26E8"       # ⛨
    KEY = "\u26BF"          # ⚿
    BLOCKED = "\u26D4"      # ⛔


# ============================================================================
# SISTEMA DE TOAST NOTIFICATIONS (nao bloqueia o fluxo)
# ============================================================================

class ToastManager:
    """Gerenciador de notificacoes toast nao-bloqueantes."""
    _toasts = []
    _root = None

    @classmethod
    def set_root(cls, root):
        cls._root = root

    @classmethod
    def show(cls, message, toast_type="info", duration=3000):
        """
        Exibe uma notificacao toast.
        toast_type: 'info' | 'success' | 'warning' | 'error'
        """
        if cls._root is None:
            return
        try:
            colors = {
                "info":    (COR_BOTAO_AZUL, "#ffffff"),
                "success": (COR_BOTAO_VERDE, "#ffffff"),
                "warning": (COR_ALERTA, "#000000"),
                "error":   (COR_ERRO, "#ffffff"),
            }
            icons_map = {
                "info": Icons.INFO,
                "success": Icons.CHECK,
                "warning": Icons.ALERT,
                "error": Icons.CROSS,
            }
            bg, fg = colors.get(toast_type, colors["info"])
            icon = icons_map.get(toast_type, Icons.INFO)

            toast = tk.Toplevel(cls._root)
            toast.overrideredirect(True)
            toast.attributes("-topmost", True)
            toast.configure(bg=bg)

            # Borda
            outer = tk.Frame(toast, bg=bg, padx=2, pady=2)
            outer.pack(fill="both", expand=True)
            inner = tk.Frame(outer, bg=bg, padx=14, pady=10)
            inner.pack(fill="both", expand=True)

            tk.Label(inner, text=f"{icon}  {message}", bg=bg, fg=fg,
                     font=("Segoe UI", 10, "bold"),
                     wraplength=340, justify="left").pack()

            # Posicionar no canto inferior direito
            cls._root.update_idletasks()
            sw = cls._root.winfo_screenwidth()
            sh = cls._root.winfo_screenheight()
            toast.update_idletasks()
            tw = toast.winfo_reqwidth()
            th = toast.winfo_reqheight()

            # Empilhar toasts
            offset_y = 20 + sum(getattr(t, '_toast_h', 50) + 8 for t in cls._toasts if t.winfo_exists())
            x = sw - tw - 20
            y = sh - offset_y - th - 50
            toast.geometry(f"+{max(0, x)}+{max(0, y)}")
            toast._toast_h = th

            cls._toasts.append(toast)

            def _destroy():
                try:
                    toast.destroy()
                    if toast in cls._toasts:
                        cls._toasts.remove(toast)
                except Exception:
                    pass

            # Auto-destruir apos duration ms
            toast.after(duration, _destroy)

            # Fechar ao clicar
            toast.bind("<Button-1>", lambda e: _destroy())

        except Exception as e:
            pass

    @classmethod
    def success(cls, msg, duration=3000):
        cls.show(msg, "success", duration)

    @classmethod
    def error(cls, msg, duration=4000):
        cls.show(msg, "error", duration)

    @classmethod
    def warning(cls, msg, duration=3500):
        cls.show(msg, "warning", duration)

    @classmethod
    def info(cls, msg, duration=3000):
        cls.show(msg, "info", duration)


# ============================================================================
# SISTEMA DE TOOLTIPS
# ============================================================================

class Tooltip:
    """Tooltip elegante que aparece ao passar o mouse sobre um widget."""

    def __init__(self, widget, text, delay=600):
        self.widget = widget
        self.text = text
        self.delay = delay
        self._tip_window = None
        self._id = None
        widget.bind("<Enter>", self._schedule, add="+")
        widget.bind("<Leave>", self._cancel, add="+")
        widget.bind("<ButtonPress>", self._cancel, add="+")

    def _schedule(self, event=None):
        self._cancel()
        self._id = self.widget.after(self.delay, self._show)

    def _cancel(self, event=None):
        if self._id:
            self.widget.after_cancel(self._id)
            self._id = None
        self._hide()

    def _show(self):
        if self._tip_window or not self.text:
            return
        try:
            x = self.widget.winfo_rootx() + 20
            y = self.widget.winfo_rooty() + self.widget.winfo_height() + 4
            self._tip_window = tw = tk.Toplevel(self.widget)
            tw.overrideredirect(True)
            tw.attributes("-topmost", True)
            tw.configure(bg=COR_CARD_BORDER)
            outer = tk.Frame(tw, bg=COR_CARD_BORDER, padx=1, pady=1)
            outer.pack()
            lbl = tk.Label(outer, text=self.text, bg=COR_GLASS, fg=COR_TEXTO2,
                           font=("Segoe UI", 9), padx=8, pady=5,
                           wraplength=280, justify="left")
            lbl.pack()
            tw.geometry(f"+{x}+{y}")
        except Exception:
            pass

    def _hide(self):
        if self._tip_window:
            try:
                self._tip_window.destroy()
            except Exception:
                pass
            self._tip_window = None


def add_tooltip(widget, text):
    """Helper para adicionar tooltip a qualquer widget."""
    if text:
        Tooltip(widget, text)
    return widget


class RichTooltipArmario:
    """Tooltip rico e detalhado para cards de armario, com layout formatado.
    Exibe numero, status, cliente, data/hora entrada, localizacao, descricao,
    observacao, total de consumo e lista de produtos/itens."""

    def __init__(self, widget, arm_data, status, delay=400):
        self.widget = widget
        self.arm = arm_data
        self.status = status
        self.delay = delay
        self._tip_window = None
        self._id = None
        self._itens_cache = None
        widget.bind("<Enter>", self._schedule, add="+")
        widget.bind("<Leave>", self._cancel, add="+")
        widget.bind("<ButtonPress>", self._cancel, add="+")

    def _schedule(self, event=None):
        self._cancel()
        self._id = self.widget.after(self.delay, self._show)

    def _cancel(self, event=None):
        if self._id:
            self.widget.after_cancel(self._id)
            self._id = None
        self._hide()

    def _show(self):
        if self._tip_window:
            return
        try:
            x = self.widget.winfo_rootx() + self.widget.winfo_width() + 5
            y = self.widget.winfo_rooty()

            self._tip_window = tw = tk.Toplevel(self.widget)
            tw.overrideredirect(True)
            tw.attributes("-topmost", True)
            tw.configure(bg="#1e2d4a")

            outer = tk.Frame(tw, bg="#1e2d4a", padx=2, pady=2)
            outer.pack()
            inner = tk.Frame(outer, bg="#0f1629", padx=12, pady=10)
            inner.pack(fill="both", expand=True)

            arm = self.arm
            status = self.status
            numero = arm.get("numero", "?")

            # Titulo
            status_cores = {"livre": "#00c853", "ocupado": "#9C27B0", "manutencao": "#fbbf24"}
            status_nomes = {"livre": "LIVRE", "ocupado": "OCUPADO", "manutencao": "MANUTENCAO"}
            cor_status = status_cores.get(status, "#e8eaed")

            titulo_frame = tk.Frame(inner, bg="#0f1629")
            titulo_frame.pack(fill="x", pady=(0, 6))
            tk.Label(titulo_frame, text=f"Armario {numero}",
                     bg="#0f1629", fg="#00d4ff",
                     font=("Segoe UI", 12, "bold")).pack(side="left")
            tk.Label(titulo_frame, text=f"  [{status_nomes.get(status, status.upper())}]",
                     bg="#0f1629", fg=cor_status,
                     font=("Segoe UI", 10, "bold")).pack(side="left")

            # Separador
            tk.Frame(inner, bg="#2a3a5c", height=1).pack(fill="x", pady=4)

            # Dados
            def _add_line(label, value, fg_val="#e8eaed"):
                if not value:
                    return
                f = tk.Frame(inner, bg="#0f1629")
                f.pack(fill="x", pady=1)
                tk.Label(f, text=f"{label}: ", bg="#0f1629", fg="#8892a4",
                         font=("Segoe UI", 9)).pack(side="left")
                tk.Label(f, text=str(value), bg="#0f1629", fg=fg_val,
                         font=("Segoe UI", 9, "bold")).pack(side="left")

            desc = arm.get("descricao") or ""
            loc = arm.get("localizacao") or ""
            cliente = arm.get("cliente_nome") or ""
            obs = arm.get("observacao") or ""
            data_entrada = arm.get("data_entrada")
            total_consumo = float(arm.get("total_consumo", 0))

            if desc:
                _add_line("Descricao", desc)
            if loc:
                _add_line("Localizacao", loc)

            if status == "ocupado":
                if cliente:
                    _add_line("Cliente", cliente, "#00f5a0")
                else:
                    _add_line("Cliente", "Nao informado", "#8892a4")
                if data_entrada:
                    _add_line("Entrada", FormatUtils.format_date(data_entrada), "#fbbf24")
                _add_line("Total Consumo", f"R$ {FormatUtils.format_money(total_consumo)}", "#00f5a0")
                if obs:
                    _add_line("Observacao", obs)

                # Itens/Produtos
                uso_id = arm.get("uso_id") or 0
                itens = arm.get("_itens_tooltip") or []
                if itens:
                    tk.Frame(inner, bg="#2a3a5c", height=1).pack(fill="x", pady=4)
                    tk.Label(inner, text=f"Produtos/Itens ({len(itens)}):",
                             bg="#0f1629", fg="#00d4ff",
                             font=("Segoe UI", 9, "bold")).pack(anchor="w", pady=(2, 2))
                    for idx, item in enumerate(itens[:15]):
                        qtd = item.get("quantidade", 1)
                        nome_prod = item.get("descricao_produto", "?")
                        preco = float(item.get("total", 0)) + float(item.get("adicionais_total", 0))
                        ad_desc = item.get("adicionais_descricao") or ""
                        item_text = f"  {idx+1}. {nome_prod} x{qtd} = R$ {FormatUtils.format_money(preco)}"
                        if ad_desc:
                            item_text += f" (+{ad_desc})"
                        tk.Label(inner, text=item_text,
                                 bg="#0f1629", fg="#e8eaed",
                                 font=("Consolas", 8), anchor="w").pack(anchor="w")
                    if len(itens) > 15:
                        tk.Label(inner, text=f"  ... e mais {len(itens) - 15} item(ns)",
                                 bg="#0f1629", fg="#8892a4",
                                 font=("Segoe UI", 8)).pack(anchor="w")
                elif uso_id and uso_id > 0:
                    tk.Frame(inner, bg="#2a3a5c", height=1).pack(fill="x", pady=4)
                    tk.Label(inner, text="Nenhum produto adicionado",
                             bg="#0f1629", fg="#8892a4",
                             font=("Segoe UI", 8, "italic")).pack(anchor="w")

            elif status == "manutencao":
                if obs:
                    _add_line("Motivo", obs, "#fbbf24")
                if data_entrada:
                    _add_line("Desde", FormatUtils.format_date(data_entrada))

            elif status == "livre":
                tk.Label(inner, text="Armario disponivel para uso",
                         bg="#0f1629", fg="#00c853",
                         font=("Segoe UI", 9, "italic")).pack(anchor="w", pady=2)

            # Rodape
            tk.Frame(inner, bg="#2a3a5c", height=1).pack(fill="x", pady=(6, 2))
            tk.Label(inner, text="Clique para gerenciar",
                     bg="#0f1629", fg="#8892a4",
                     font=("Segoe UI", 8, "italic")).pack(anchor="center")

            tw.update_idletasks()
            tw_width = tw.winfo_reqwidth()
            tw_height = tw.winfo_reqheight()
            screen_width = self.widget.winfo_screenwidth()
            screen_height = self.widget.winfo_screenheight()
            if x + tw_width > screen_width:
                x = self.widget.winfo_rootx() - tw_width - 5
            if y + tw_height > screen_height:
                y = screen_height - tw_height - 10
            tw.geometry(f"+{x}+{y}")
        except Exception:
            pass

    def _hide(self):
        if self._tip_window:
            try:
                self._tip_window.destroy()
            except Exception:
                pass
            self._tip_window = None


# ============================================================================
# BARRA DE STATUS INFERIOR
# ============================================================================

class StatusBar(tk.Frame):
    """Barra de status inferior com info de sessao, conexao e relogio."""

    def __init__(self, master, **kwargs):
        super().__init__(master, bg=COR_FUNDO3, height=24, **kwargs)
        self.pack_propagate(False)

        # Linha divisora superior
        div = tk.Frame(self, bg=COR_GLASS_BORDER, height=1)
        div.pack(fill="x", side="top")

        # Status de conexao
        self._conn_dot = tk.Label(self, text=Icons.ONLINE, bg=COR_FUNDO3,
                                   fg=COR_SUCESSO, font=("Segoe UI", 9))
        self._conn_dot.pack(side="left", padx=(8, 0))
        self._conn_lbl = tk.Label(self, text="BD: Conectado", bg=COR_FUNDO3,
                                   fg=COR_TEXTO2, font=("Segoe UI", 8))
        self._conn_lbl.pack(side="left", padx=(2, 12))
        add_tooltip(self._conn_lbl, "Status da conexao com o banco de dados")

        # Versao
        tk.Label(self, text=f"PDV Pro v{APP_VERSION}", bg=COR_FUNDO3,
                 fg=COR_TEXTO2, font=("Segoe UI", 8)).pack(side="left", padx=8)

        # Tema ativo
        self._theme_lbl = tk.Label(self, text=f"{Icons.THEME} {_ACTIVE_THEME}",
                                    bg=COR_FUNDO3, fg=COR_TEXTO2, font=("Segoe UI", 8),
                                    cursor="hand2")
        self._theme_lbl.pack(side="left", padx=8)

        # Separador
        tk.Label(self, text="|", bg=COR_FUNDO3, fg=COR_GLASS_BORDER,
                 font=("Segoe UI", 8)).pack(side="left")

        # Usuario logado
        self._user_lbl = tk.Label(self, text="", bg=COR_FUNDO3,
                                   fg=COR_PRIMARIA, font=("Segoe UI", 8, "bold"))
        self._user_lbl.pack(side="left", padx=8)

        # Caixa status
        self._caixa_lbl = tk.Label(self, text="", bg=COR_FUNDO3,
                                    fg=COR_TEXTO2, font=("Segoe UI", 8))
        self._caixa_lbl.pack(side="left", padx=4)

        # Relogio (direita)
        self._clock_lbl = tk.Label(self, text="", bg=COR_FUNDO3,
                                    fg=COR_TEXTO2, font=("Consolas", 8))
        self._clock_lbl.pack(side="right", padx=8)

        # F1 hint
        tk.Label(self, text="F1=Ajuda  F11=Tela Cheia  Ctrl+F=Busca",
                 bg=COR_FUNDO3, fg=COR_GLASS_BORDER,
                 font=("Segoe UI", 8)).pack(side="right", padx=8)

        self._update_clock()

    def _update_clock(self):
        try:
            if self.winfo_exists():
                now = datetime.datetime.now().strftime("%d/%m/%Y  %H:%M:%S")
                self._clock_lbl.config(text=now)
                self.after(1000, self._update_clock)
        except Exception:
            pass

    def set_connection(self, ok, host=""):
        try:
            if ok:
                self._conn_dot.config(fg=COR_SUCESSO, text=Icons.ONLINE)
                self._conn_lbl.config(text=f"BD: {host}" if host else "BD: Conectado")
            else:
                self._conn_dot.config(fg=COR_ERRO, text=Icons.OFFLINE)
                self._conn_lbl.config(text="BD: Desconectado")
        except Exception:
            pass

    def set_user(self, nome, nivel=""):
        try:
            info = f"{Icons.USER} {nome}"
            if nivel:
                info += f" [{nivel}]"
            self._user_lbl.config(text=info)
        except Exception:
            pass

    def set_caixa(self, aberto, caixa_id=0):
        try:
            if aberto:
                self._caixa_lbl.config(
                    text=f"{Icons.CAIXA} Caixa #{caixa_id} Aberto",
                    fg=COR_SUCESSO)
            else:
                self._caixa_lbl.config(text=f"{Icons.CAIXA} Caixa Fechado",
                                        fg=COR_ALERTA)
        except Exception:
            pass

    def update_theme_label(self):
        try:
            self._theme_lbl.config(text=f"{Icons.THEME} {_ACTIVE_THEME}")
        except Exception:
            pass

    def bind_theme_click(self, callback):
        self._theme_lbl.bind("<Button-1>", lambda e: callback())


# ============================================================================
# ATALHOS DE TECLADO GLOBAIS
# ============================================================================

class KeyboardShortcuts:
    """Gerencia atalhos de teclado globais para a aplicacao."""

    def __init__(self, root, app):
        self.root = root
        self.app = app
        self._register()

    def _register(self):
        r = self.root
        # F1 - Ajuda / atalhos
        r.bind_all("<F1>", lambda e: self._show_help())
        # F2 - Nova Venda rapida
        r.bind_all("<F2>", lambda e: self._safe_call(self.app.show_venda,
            "vendas.acessar"))
        # F3 - Caixa
        r.bind_all("<F3>", lambda e: self._safe_call(self.app.show_caixa,
            "caixa.acessar"))
        # F4 - Comandas
        r.bind_all("<F4>", lambda e: self._safe_call(self.app.show_comandas,
            "comandas.acessar"))
        # F5 - Historico
        r.bind_all("<F5>", lambda e: self._safe_call(self.app.show_historico,
            "historico.acessar"))
        # F6 - Produtos
        r.bind_all("<F6>", lambda e: self._safe_call(
            self.app.show_cadastro_produto, "produtos.acessar"))
        # F7 - Relatorios
        r.bind_all("<F7>", lambda e: self._safe_call(self.app.show_relatorios,
            "relatorios.acessar"))
        # F8 - Clientes
        r.bind_all("<F8>", lambda e: self._safe_call(
            self.app.show_cadastro_cliente, "clientes.acessar"))
        # F9 - Mesas
        r.bind_all("<F9>", lambda e: self._safe_call(
            self.app.show_gerenciar_mesas, "gerenciar_mesas.acessar"))
        # F10 - Dashboard / Menu principal
        r.bind_all("<F10>", lambda e: self.app.show_main_menu())
        # F11 - Tela cheia
        r.bind_all("<F11>", lambda e: self._toggle_fullscreen())
        # F12 - Configuracoes de impressora
        r.bind_all("<F12>", lambda e: self._safe_call(
            self.app.show_config_impressora, "config_impressora.acessar"))
        # Ctrl+L - Logoff
        r.bind_all("<Control-l>", lambda e: self.app.do_logoff())
        # Ctrl+Z - Desfazer ultimo item do carrinho (na tela de venda)
        r.bind_all("<Control-z>", lambda e: self._undo_carrinho())
        # Ctrl+P - Impressao rapida (na tela de venda)
        r.bind_all("<Control-p>", lambda e: self._imprimir_rapido())
        # Escape - Fechar dialogs
        r.bind_all("<Escape>", lambda e: self._fechar_dialog_ativo())

    def _safe_call(self, func, perm_chave=None):
        """Chama funcao apenas se o usuario tiver permissao."""
        try:
            from __main__ import Session
        except Exception:
            pass
        try:
            if perm_chave and not Session.is_admin():
                if not Session.has_permission(perm_chave):
                    ToastManager.warning("Sem permissao para esta acao.")
                    return
            func()
        except Exception:
            pass

    def _toggle_fullscreen(self):
        try:
            estado = self.root.attributes("-fullscreen")
            self.root.attributes("-fullscreen", not estado)
            if not estado:
                ToastManager.info("Tela cheia ativada. Pressione F11 para sair.")
        except Exception:
            try:
                estado = self.root.state()
                if estado == "zoomed":
                    self.root.state("normal")
                else:
                    self.root.state("zoomed")
            except Exception:
                pass

    def _undo_carrinho(self):
        try:
            if hasattr(self.app, "carrinho") and self.app.carrinho:
                removido = self.app.carrinho.pop()
                self.app.atualizar_tree_carrinho()
                ToastManager.info(f"Item removido: {removido.descricao_produto}")
        except Exception:
            pass

    def _imprimir_rapido(self):
        try:
            if hasattr(self.app, "_ultimo_cupom") and self.app._ultimo_cupom:
                def _do():
                    return self.app._imprimir_texto(self.app._ultimo_cupom)
                def _ok(r):
                    ok, msg = r
                    if ok:
                        ToastManager.success("Impresso com sucesso!")
                    else:
                        ToastManager.error(
                            f"Nao foi possivel imprimir. Verifique se a impressora esta ligada. ({msg})",
                            duration=5000)
                self.app.run_async(_do, _ok)
            else:
                ToastManager.warning("Nenhum cupom disponivel para impressao rapida.")
        except Exception:
            pass

    def _fechar_dialog_ativo(self):
        try:
            focused = self.root.focus_get()
            if focused:
                top = focused.winfo_toplevel()
                if top != self.root:
                    top.destroy()
        except Exception:
            pass

    def _show_help(self):
        """Exibe janela de ajuda com todos os atalhos."""
        try:
            dialog = tk.Toplevel(self.root)
            dialog.title("Atalhos de Teclado - PDV Pro")
            dialog.geometry("520x480")
            dialog.configure(bg=COR_FUNDO)
            dialog.transient(self.root)
            dialog.grab_set()

            tk.Label(dialog, text=f"{Icons.SHORTCUT} Atalhos de Teclado",
                     bg=COR_FUNDO, fg=COR_PRIMARIA,
                     font=("Segoe UI", 15, "bold")).pack(pady=(18, 5))

            from tkinter import ttk as _ttk
            tree = _ttk.Treeview(dialog, columns=("atalho", "acao"),
                                  show="headings", height=16)
            tree.heading("atalho", text="Atalho")
            tree.heading("acao", text="Acao")
            tree.column("atalho", width=130, anchor="center")
            tree.column("acao", width=330)

            atalhos = [
                ("F1", "Exibir esta janela de ajuda"),
                ("F2", "Nova Venda"),
                ("F3", "Caixa"),
                ("F4", "Comandas"),
                ("F5", "Historico de Vendas"),
                ("F6", "Cadastro de Produtos"),
                ("F7", "Relatorios"),
                ("F8", "Clientes"),
                ("F9", "Gerenciar Mesas"),
                ("F10", "Dashboard / Menu Principal"),
                ("F11", "Alternar Tela Cheia"),
                ("F12", "Configuracoes de Impressora"),
                ("Ctrl+Z", "Desfazer ultimo item do carrinho"),
                ("Ctrl+P", "Impressao rapida (ultimo cupom)"),
                ("Ctrl+L", "Logoff / Trocar usuario"),
                ("ESC", "Fechar dialog ativo"),
            ]
            for a, d in atalhos:
                tree.insert("", "end", values=(a, d))

            tree.pack(fill="both", expand=True, padx=20, pady=10)

            StyledButton(dialog, text=f"{Icons.CROSS} Fechar",
                         command=dialog.destroy, color="#2a3a5c",
                         width=10).pack(pady=10)
        except Exception as e:
            ToastManager.error(f"Erro ao abrir ajuda: {e}")


# ============================================================================
# EXPORTADOR CSV / TXT
# ============================================================================

class DataExporter:
    """Utilitario para exportar dados de Treeview ou lista para CSV/TXT."""

    @staticmethod
    def export_treeview_csv(tree, filename_hint="export", parent=None):
        """Exporta dados de um Treeview para CSV."""
        try:
            cols = tree["columns"]
            rows = []
            for child in tree.get_children():
                rows.append(tree.item(child)["values"])
            if not rows:
                ToastManager.warning("Nenhum dado para exportar.")
                return None

            ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
            default_name = f"{filename_hint}_{ts}.csv"
            filepath = filedialog.asksaveasfilename(
                parent=parent,
                initialdir=EXPORT_DIR,
                initialfile=default_name,
                defaultextension=".csv",
                filetypes=[("CSV", "*.csv"), ("Todos", "*.*")]
            )
            if not filepath:
                return None

            with open(filepath, "w", newline="", encoding="utf-8-sig") as f:
                writer = csv.writer(f, delimiter=";")
                # Cabecalho
                headers = [tree.heading(c)["text"] for c in cols]
                writer.writerow(headers)
                writer.writerows(rows)

            ToastManager.success(f"Exportado: {os.path.basename(filepath)}")
            AuditLogger.log("EXPORT_CSV", f"Arquivo: {filepath}")
            return filepath
        except Exception as e:
            ToastManager.error(f"Erro ao exportar: {e}")
            return None

    @staticmethod
    def export_text(content, filename_hint="relatorio", parent=None):
        """Exporta texto para arquivo TXT."""
        try:
            ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
            default_name = f"{filename_hint}_{ts}.txt"
            filepath = filedialog.asksaveasfilename(
                parent=parent,
                initialdir=EXPORT_DIR,
                initialfile=default_name,
                defaultextension=".txt",
                filetypes=[("Texto", "*.txt"), ("Todos", "*.*")]
            )
            if not filepath:
                return None
            with open(filepath, "w", encoding="utf-8", errors="replace") as f:
                f.write(content)
            ToastManager.success(f"Salvo: {os.path.basename(filepath)}")
            AuditLogger.log("EXPORT_TXT", f"Arquivo: {filepath}")
            return filepath
        except Exception as e:
            ToastManager.error(f"Erro ao salvar: {e}")
            return None



class Empresa:
    def __init__(self):
        self.id = 0
        self.razao_social = ""
        self.nome_fantasia = ""
        self.cnpj = ""
        self.ie = ""
        self.endereco = ""
        self.numero = ""
        self.bairro = ""
        self.cidade = ""
        self.uf = ""
        self.cep = ""
        self.telefone = ""
        self.email = ""

class Usuario:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.login = ""
        self.senha = ""
        self.nivel = "operador"
        self.ativo = True
        self.perfil_id = None

class Produto:
    def __init__(self):
        self.id = 0
        self.codigo = ""
        self.descricao = ""
        self.unidade = "UN"
        self.tipo_produto_id = None
        self.tipo_produto_desc = ""
        self.preco_custo = 0.0
        self.preco_venda = 0.0
        self.estoque = 0.0
        self.estoque_minimo = 0.0
        self.codigo_barras = ""
        self.foto_base64 = None
        self.ativo = True

class Cliente:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.cpf_cnpj = ""
        self.celular = ""
        self.endereco = ""
        self.numero = ""
        self.bairro = ""
        self.cidade = ""
        self.uf = ""
        self.cep = ""
        self.email = ""
        self.ativo = True

class Vendedor:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.celular = ""
        self.comissao = 0.0
        self.ativo = True

class Entregador:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.celular = ""
        self.ativo = True

class Garcom:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.celular = ""
        self.ativo = True

class FormaPagamento:
    def __init__(self):
        self.id = 0
        self.descricao = ""
        self.tipo = "dinheiro"
        self.permite_parcelamento = False
        self.exige_cliente = False
        self.ativo = True

class TipoProduto:
    def __init__(self):
        self.id = 0
        self.descricao = ""
        self.ativo = True

class Adicional:
    def __init__(self):
        self.id = 0
        self.descricao = ""
        self.preco = 0.0
        self.ativo = True

class Venda:
    def __init__(self):
        self.id = 0
        self.cliente_id = 0
        self.vendedor_id = 0
        self.entregador_id = 0
        self.caixa_id = 0
        self.data_venda = ""
        self.cliente_nome = ""
        self.vendedor_nome = ""
        self.entregador_nome = ""
        self.total_bruto = 0.0
        self.desconto_tipo = ""
        self.desconto_valor = 0.0
        self.acrescimo_tipo = ""
        self.acrescimo_valor = 0.0
        self.total_liquido = 0.0
        self.valor_recebido = 0.0
        self.troco = 0.0
        self.observacao = ""
        self.status = "finalizada"
        self.celular_whatsapp = ""
        self.para_entrega = False
        self.taxa_entrega = 0.0
        self.bairro_entrega = ""
        self.endereco_entrega = ""
        self.status_entrega = "pendente"

class ItemVenda:
    def __init__(self):
        self.id = 0
        self.venda_id = 0
        self.produto_id = 0
        self.descricao_produto = ""
        self.quantidade = 1.0
        self.preco_unitario = 0.0
        self.desconto = 0.0
        self.total = 0.0
        self.adicionais = []
        self.tipo_produto_id = None
        self.tipo_produto_desc = ""

class PagamentoVenda:
    def __init__(self):
        self.id = 0
        self.venda_id = 0
        self.forma_pagamento_id = 0
        self.forma_descricao = ""
        self.valor = 0.0
        self.parcelas = 1
        self.bandeira = ""

class Caixa:
    def __init__(self):
        self.id = 0
        self.usuario_id = 0
        self.data_abertura = ""
        self.data_fechamento = ""
        self.valor_abertura = 0.0
        self.valor_fechamento = 0.0
        self.status = "aberto"
        self.observacao = ""

class Comanda:
    def __init__(self):
        self.id = 0
        self.numero = 0
        self.cliente_id = 0
        self.vendedor_id = 0
        self.caixa_id = 0
        self.data_abertura = ""
        self.data_fechamento = ""
        self.total_itens = 0.0
        self.observacao = ""
        self.status = "aberta"

class ItemComanda:
    def __init__(self):
        self.id = 0
        self.comanda_id = 0
        self.produto_id = 0
        self.descricao_produto = ""
        self.quantidade = 1.0
        self.preco_unitario = 0.0
        self.total = 0.0
        self.observacao = ""
        self.data_hora = ""

class Mesa:
    def __init__(self):
        self.id = 0
        self.numero = 0
        self.descricao = ""
        self.capacidade = 4
        self.ativa = True

class ValeDebito:
    def __init__(self):
        self.id = 0
        self.caixa_id = 0
        self.descricao = ""
        self.valor = 0.0
        self.data = ""

class ContaReceber:
    def __init__(self):
        self.id = 0
        self.cliente_id = 0
        self.venda_id = 0
        self.valor_original = 0.0
        self.valor_pago = 0.0
        self.valor_pendente = 0.0
        self.data_venda = ""
        self.data_vencimento = ""
        self.data_pagamento = ""
        self.status = "pendente"
        self.observacao = ""
        self.cliente_nome = ""

class NotaEntrada:
    def __init__(self):
        self.id = 0
        self.numero_nota = ""
        self.fornecedor = ""
        self.data_entrada = ""
        self.total_nota = 0.0
        self.observacao = ""
        self.status = "pendente"
        self.usuario_id = 0

class ItemNotaEntrada:
    def __init__(self):
        self.id = 0
        self.nota_entrada_id = 0
        self.produto_id = 0
        self.descricao_produto = ""
        self.quantidade = 0.0
        self.custo_unitario = 0.0
        self.total = 0.0

class ObservacaoCupom:
    def __init__(self):
        self.id = 0
        self.texto = ""
        self.ativo = True

class Perfil:
    def __init__(self):
        self.id = 0
        self.nome = ""
        self.descricao = ""
        self.sistematico = False
        self.ativo = True
        self.personalizavel = False

class Permissao:
    def __init__(self, modulo="", acao="", chave="", descricao=""):
        self.id = 0
        self.modulo = modulo
        self.acao = acao
        self.chave = chave
        self.descricao = descricao
        self.concedida = False

class ArmarioSauna:
    def __init__(self):
        self.id = 0
        self.numero = 0
        self.descricao = ""
        self.localizacao = ""
        self.ativo = True

class TaxaEntregaBairro:
    def __init__(self):
        self.id = 0
        self.bairro = ""
        self.taxa = 0.0
        self.ativo = True

class Armario:
    def __init__(self):
        self.id = 0
        self.numero = 0
        self.descricao = ""
        self.status = "livre"
        self.ativo = True

class CaixaModel:
    def __init__(self):
        self.id = 0
        self.usuario_id = 0
        self.data_abertura = ""
        self.data_fechamento = ""
        self.valor_abertura = 0.0
        self.valor_fechamento = 0.0
        self.status = "aberto"
        self.observacao = ""

class EntradaNota:
    def __init__(self):
        self.id = 0
        self.produto_id = 0
        self.quantidade = 0.0
        self.custo_unitario = 0.0
        self.usuario_id = 0
        self.data_entrada = ""

class Chamado:
    def __init__(self):
        self.id = 0
        self.mesa_numero = 0
        self.tipo = "garcom"
        self.mensagem = ""
        self.status = "pendente"
        self.data_criacao = ""

class PedidoWeb:
    def __init__(self):
        self.id = 0
        self.mesa_numero = 0
        self.itens_json = ""
        self.observacao = ""
        self.status = "pendente"
        self.data_criacao = ""

class PermissaoUsuario:
    def __init__(self):
        self.id = 0
        self.usuario_id = 0
        self.permissao_id = 0
        self.tipo = "adicionar"  # 'adicionar' ou 'remover'
        self.criado_por = None


# ============================================================================
# UTILITARIOS DE FORMATACAO
# ============================================================================

class FormatUtils:
    @staticmethod
    def format_money(value):
        try:
            return f"{float(value):,.2f}".replace(",", "X").replace(".", ",").replace("X", ".")
        except (ValueError, TypeError):
            return "0,00"

    @staticmethod
    def format_date(date_str):
        if not date_str:
            return ""
        try:
            if isinstance(date_str, datetime.datetime):
                return date_str.strftime("%d/%m/%Y %H:%M")
            dt = datetime.datetime.strptime(str(date_str)[:19], "%Y-%m-%d %H:%M:%S")
            return dt.strftime("%d/%m/%Y %H:%M")
        except Exception:
            return str(date_str) if date_str else ""

    @staticmethod
    def safe_string(s):
        return s if s else ""

    @staticmethod
    def parse_money(text):
        try:
            clean = text.replace(".", "").replace(",", ".").strip()
            return float(clean)
        except (ValueError, TypeError):
            return 0.0

    @staticmethod
    def format_quantidade(value):
        """Formata quantidade (3 casas decimais, remove zeros a direita)."""
        try:
            v = float(value)
            s = f"{v:.3f}"
            # Remove zeros a direita, mas mantem pelo menos uma casa decimal
            if '.' in s:
                s = s.rstrip('0').rstrip('.')
            return s
        except (ValueError, TypeError):
            return "0"


class EanUtils:
    """Utilitario para geracao e validacao de codigos de barras EAN-13.
    Alinhado com EanUtils.java do Android.
    Estrutura EAN-13:
    - Digitos 1-3: Prefixo do pais (789 = Brasil)
    - Digitos 4-7: Codigo da empresa (gerado aleatoriamente para uso interno)
    - Digitos 8-12: Codigo do produto (gerado aleatoriamente)
    - Digito 13: Digito verificador (calculado pelo algoritmo padrao EAN)
    """

    @staticmethod
    def gerar_ean13():
        """Gera um codigo EAN-13 valido com prefixo Brasil (789)."""
        sb = "789"
        for _ in range(9):
            sb += str(random.randint(0, 9))
        digito = EanUtils.calcular_digito_verificador(sb)
        sb += str(digito)
        return sb

    @staticmethod
    def calcular_digito_verificador(doze_digitos):
        """Calcula o digito verificador EAN-13 a partir dos 12 primeiros digitos."""
        if not doze_digitos or len(doze_digitos) != 12:
            raise ValueError("Sao necessarios exatamente 12 digitos")
        soma = 0
        for i in range(12):
            digito = int(doze_digitos[i])
            if i % 2 == 0:
                soma += digito
            else:
                soma += digito * 3
        resto = soma % 10
        return 0 if resto == 0 else (10 - resto)

    @staticmethod
    def validar_ean13(ean13):
        """Valida se um codigo EAN-13 e valido verificando o digito verificador."""
        if not ean13 or len(ean13) != 13:
            return False
        if not ean13.isdigit():
            return False
        digito_esperado = EanUtils.calcular_digito_verificador(ean13[:12])
        digito_atual = int(ean13[12])
        return digito_esperado == digito_atual


# ============================================================================
# CONSTANTES DE PERMISSOES (alinhado com PermissionConstants.java do Android)
# ============================================================================

class PermissionConstants:
    """Classe central que define TODAS as permissoes do sistema PDV Pro.
    Alinhada 1:1 com PermissionConstants.java do Android.
    Estrutura: MODULO > ACAO > CHAVE UNICA
    Convencao de chaves: 'modulo.acao'
    """

    # DASHBOARD - Botoes da Tela Principal
    DASHBOARD_BTN_VENDAS = "dashboard.btn_vendas"
    DASHBOARD_BTN_COMANDAS = "dashboard.btn_comandas"
    DASHBOARD_BTN_PRODUTOS = "dashboard.btn_produtos"
    DASHBOARD_BTN_GERENCIAR_PRODUTOS = "dashboard.btn_gerenciar_produtos"
    DASHBOARD_BTN_CLIENTES = "dashboard.btn_clientes"
    DASHBOARD_BTN_CAIXA = "dashboard.btn_caixa"
    DASHBOARD_BTN_RELATORIOS = "dashboard.btn_relatorios"
    DASHBOARD_BTN_HISTORICO = "dashboard.btn_historico"
    DASHBOARD_BTN_EMPRESA = "dashboard.btn_empresa"
    DASHBOARD_BTN_VENDEDORES = "dashboard.btn_vendedores"
    DASHBOARD_BTN_ENTREGADORES = "dashboard.btn_entregadores"
    DASHBOARD_BTN_USUARIOS = "dashboard.btn_usuarios"
    DASHBOARD_BTN_PERFIS = "dashboard.btn_perfis"
    DASHBOARD_BTN_FORMAS_PAGAMENTO = "dashboard.btn_formas_pagamento"
    DASHBOARD_BTN_TIPOS_PRODUTO = "dashboard.btn_tipos_produto"
    DASHBOARD_BTN_OBSERVACOES = "dashboard.btn_observacoes"
    DASHBOARD_BTN_IMPRESSORA = "dashboard.btn_impressora"
    DASHBOARD_BTN_BACKUP = "dashboard.btn_backup"
    DASHBOARD_BTN_LICENCA = "dashboard.btn_licenca"
    DASHBOARD_BTN_TROCAR_SENHA = "dashboard.btn_trocar_senha"
    DASHBOARD_BTN_MODO_ENTREGADOR = "dashboard.btn_modo_entregador"
    DASHBOARD_BTN_GERENCIAR_ENTREGAS = "dashboard.btn_gerenciar_entregas"
    DASHBOARD_BTN_CONTAS_RECEBER = "dashboard.btn_contas_receber"
    DASHBOARD_BTN_WHATSBOT = "dashboard.btn_whatsbot"
    DASHBOARD_BTN_ENTRADA_NOTAS = "dashboard.btn_entrada_notas"
    DASHBOARD_BTN_TAXA_ENTREGA = "dashboard.btn_taxa_entrega"
    DASHBOARD_BTN_SOBRE = "dashboard.btn_sobre"
    DASHBOARD_BTN_PAINEL_CHAMADOS = "dashboard.btn_painel_chamados"
    DASHBOARD_BTN_GERENCIADOR_CHAMADOS = "dashboard.btn_gerenciador_chamados"
    DASHBOARD_BTN_PAINEL_COZINHA = "dashboard.btn_painel_cozinha"
    DASHBOARD_BTN_ADICIONAIS = "dashboard.btn_adicionais"
    DASHBOARD_BTN_GARCONS = "dashboard.btn_garcons"
    DASHBOARD_BTN_CADASTRO_MESAS = "dashboard.btn_cadastro_mesas"
    DASHBOARD_BTN_GERENCIAR_MESAS = "dashboard.btn_gerenciar_mesas"
    DASHBOARD_BTN_CADASTRO_ARMARIOS_SAUNA = "dashboard.btn_cadastro_armarios_sauna"
    DASHBOARD_BTN_GERENCIAR_ARMARIOS_SAUNA = "dashboard.btn_gerenciar_armarios_sauna"
    DASHBOARD_BTN_ATUALIZAR = "dashboard.btn_atualizar"
    DASHBOARD_BTN_SERVIDOR_WEB = "dashboard.btn_servidor_web"
    DASHBOARD_BTN_LOG_AUDITORIA = "dashboard.btn_log_auditoria"

    # PAINEL DA COZINHA
    PAINEL_COZINHA_ACESSAR = "painel_cozinha.acessar"

    # VENDAS
    VENDAS_ACESSAR = "vendas.acessar"
    VENDAS_CRIAR = "vendas.criar"
    VENDAS_APLICAR_DESCONTO = "vendas.aplicar_desconto"
    VENDAS_APLICAR_ACRESCIMO = "vendas.aplicar_acrescimo"
    VENDAS_ESCOLHER_CLIENTE = "vendas.escolher_cliente"
    VENDAS_ESCOLHER_VENDEDOR = "vendas.escolher_vendedor"
    VENDAS_ESCOLHER_ENTREGADOR = "vendas.escolher_entregador"

    # HISTORICO DE VENDAS
    HISTORICO_ACESSAR = "historico.acessar"
    HISTORICO_CANCELAR_VENDA = "historico.cancelar_venda"
    HISTORICO_REIMPRIMIR = "historico.reimprimir"
    HISTORICO_ENVIAR_WHATSAPP = "historico.enviar_whatsapp"

    # COMANDAS
    COMANDAS_ACESSAR = "comandas.acessar"
    COMANDAS_CRIAR = "comandas.criar"
    COMANDAS_EDITAR = "comandas.editar"
    COMANDAS_FECHAR = "comandas.fechar"
    COMANDAS_CANCELAR = "comandas.cancelar"
    COMANDAS_IMPRIMIR = "comandas.imprimir"

    # CAIXA
    CAIXA_ACESSAR = "caixa.acessar"
    CAIXA_ABRIR = "caixa.abrir"
    CAIXA_FECHAR = "caixa.fechar"
    CAIXA_VALE_DEBITO = "caixa.vale_debito"

    # PRODUTOS
    PRODUTOS_ACESSAR = "produtos.acessar"
    PRODUTOS_CRIAR = "produtos.criar"
    PRODUTOS_EDITAR = "produtos.editar"
    PRODUTOS_EXCLUIR = "produtos.excluir"

    # GERENCIAR PRODUTOS
    GERENCIAR_PRODUTOS_ACESSAR = "gerenciar_produtos.acessar"
    GERENCIAR_PRODUTOS_EDITAR = "gerenciar_produtos.editar"
    GERENCIAR_PRODUTOS_INATIVAR = "gerenciar_produtos.inativar"

    # CLIENTES
    CLIENTES_ACESSAR = "clientes.acessar"
    CLIENTES_CRIAR = "clientes.criar"
    CLIENTES_EDITAR = "clientes.editar"
    CLIENTES_EXCLUIR = "clientes.excluir"

    # VENDEDORES
    VENDEDORES_ACESSAR = "vendedores.acessar"
    VENDEDORES_CRIAR = "vendedores.criar"
    VENDEDORES_EDITAR = "vendedores.editar"
    VENDEDORES_EXCLUIR = "vendedores.excluir"

    # ENTREGADORES
    ENTREGADORES_ACESSAR = "entregadores.acessar"
    ENTREGADORES_CRIAR = "entregadores.criar"
    ENTREGADORES_EDITAR = "entregadores.editar"
    ENTREGADORES_EXCLUIR = "entregadores.excluir"

    # EMPRESA
    EMPRESA_ACESSAR = "empresa.acessar"
    EMPRESA_EDITAR = "empresa.editar"

    # USUARIOS
    USUARIOS_ACESSAR = "usuarios.acessar"
    USUARIOS_CRIAR = "usuarios.criar"
    USUARIOS_EDITAR = "usuarios.editar"
    USUARIOS_EXCLUIR = "usuarios.excluir"

    # PERFIS E PERMISSOES
    PERFIS_ACESSAR = "perfis.acessar"
    PERFIS_CRIAR = "perfis.criar"
    PERFIS_EDITAR = "perfis.editar"
    PERFIS_EXCLUIR = "perfis.excluir"
    PERFIS_GERENCIAR_PERMISSOES = "perfis.gerenciar_permissoes"
    PERFIS_PERMISSOES_USUARIO = "perfis.permissoes_usuario"

    # FORMAS DE PAGAMENTO
    FORMAS_PAGAMENTO_ACESSAR = "formas_pagamento.acessar"
    FORMAS_PAGAMENTO_CRIAR = "formas_pagamento.criar"
    FORMAS_PAGAMENTO_EDITAR = "formas_pagamento.editar"
    FORMAS_PAGAMENTO_EXCLUIR = "formas_pagamento.excluir"

    # TIPOS DE PRODUTO
    TIPOS_PRODUTO_ACESSAR = "tipos_produto.acessar"
    TIPOS_PRODUTO_CRIAR = "tipos_produto.criar"
    TIPOS_PRODUTO_EDITAR = "tipos_produto.editar"
    TIPOS_PRODUTO_EXCLUIR = "tipos_produto.excluir"

    # ADICIONAIS
    ADICIONAIS_ACESSAR = "adicionais.acessar"
    ADICIONAIS_CRIAR = "adicionais.criar"
    ADICIONAIS_EDITAR = "adicionais.editar"
    ADICIONAIS_EXCLUIR = "adicionais.excluir"

    # OBSERVACOES DO CUPOM
    OBSERVACOES_ACESSAR = "observacoes.acessar"
    OBSERVACOES_CRIAR = "observacoes.criar"
    OBSERVACOES_EDITAR = "observacoes.editar"
    OBSERVACOES_EXCLUIR = "observacoes.excluir"

    # RELATORIOS
    RELATORIOS_ACESSAR = "relatorios.acessar"
    RELATORIOS_VENDAS = "relatorios.vendas"
    RELATORIOS_LUCRATIVIDADE = "relatorios.lucratividade"
    RELATORIOS_VENDEDOR = "relatorios.vendedor"
    RELATORIOS_ENTREGADOR = "relatorios.entregador"
    RELATORIOS_CLIENTE = "relatorios.cliente"
    RELATORIOS_PRODUTOS = "relatorios.produtos"
    RELATORIOS_CAIXA = "relatorios.caixa"

    # CONFIGURACOES
    CONFIG_IMPRESSORA_ACESSAR = "config_impressora.acessar"
    CONFIG_IMPRESSORA_EDITAR = "config_impressora.editar"

    # BACKUP
    BACKUP_ACESSAR = "backup.acessar"
    BACKUP_REALIZAR = "backup.realizar"
    BACKUP_RESTAURAR = "backup.restaurar"
    BACKUP_CONFIGURAR = "backup.configurar"

    # LICENCA
    LICENCA_ACESSAR = "licenca.acessar"
    LICENCA_ATIVAR = "licenca.ativar"

    # WHATSAPP BOT
    WHATSBOT_ACESSAR = "whatsbot.acessar"
    WHATSBOT_CONFIGURAR = "whatsbot.configurar"

    # ENTREGAS
    ENTREGAS_ACESSAR = "entregas.acessar"
    ENTREGAS_GERENCIAR = "entregas.gerenciar"
    MODO_ENTREGADOR_ACESSAR = "modo_entregador.acessar"

    # CONTAS A RECEBER
    CONTAS_RECEBER_ACESSAR = "contas_receber.acessar"
    CONTAS_RECEBER_RECEBER = "contas_receber.receber"
    CONTAS_RECEBER_CANCELAR = "contas_receber.cancelar"
    CONTAS_RECEBER_RELATORIO = "contas_receber.relatorio"

    # ENTRADA DE NOTAS
    ENTRADA_NOTAS_ACESSAR = "entrada_notas.acessar"
    ENTRADA_NOTAS_CRIAR = "entrada_notas.criar"
    ENTRADA_NOTAS_CONFIRMAR = "entrada_notas.confirmar"
    ENTRADA_NOTAS_CANCELAR = "entrada_notas.cancelar"

    # TAXA DE ENTREGA POR BAIRRO
    TAXA_ENTREGA_ACESSAR = "taxa_entrega.acessar"
    TAXA_ENTREGA_CRIAR = "taxa_entrega.criar"
    TAXA_ENTREGA_EDITAR = "taxa_entrega.editar"
    TAXA_ENTREGA_EXCLUIR = "taxa_entrega.excluir"

    # PAINEL DE CHAMADOS
    PAINEL_CHAMADOS_ACESSAR = "painel_chamados.acessar"
    PAINEL_CHAMADOS_CRIAR = "painel_chamados.criar"
    PAINEL_CHAMADOS_CHAMAR = "painel_chamados.chamar"
    PAINEL_CHAMADOS_ATENDER = "painel_chamados.atender"

    # GERENCIADOR DE CHAMADOS
    GERENCIADOR_CHAMADOS_ACESSAR = "gerenciador_chamados.acessar"

    # GARCONS
    GARCONS_ACESSAR = "garcons.acessar"
    GARCONS_CRIAR = "garcons.criar"
    GARCONS_EDITAR = "garcons.editar"
    GARCONS_EXCLUIR = "garcons.excluir"

    # MESAS
    MESAS_ACESSAR = "mesas.acessar"
    MESAS_CRIAR = "mesas.criar"
    MESAS_EDITAR = "mesas.editar"
    MESAS_EXCLUIR = "mesas.excluir"

    # GERENCIAR MESAS
    GERENCIAR_MESAS_ACESSAR = "gerenciar_mesas.acessar"

    # ARMARIOS PARA SAUNA
    ARMARIOS_SAUNA_ACESSAR = "armarios_sauna.acessar"
    ARMARIOS_SAUNA_CRIAR = "armarios_sauna.criar"
    ARMARIOS_SAUNA_EDITAR = "armarios_sauna.editar"
    ARMARIOS_SAUNA_EXCLUIR = "armarios_sauna.excluir"

    # GERENCIAR ARMARIOS SAUNA
    GERENCIAR_ARMARIOS_SAUNA_ACESSAR = "gerenciar_armarios_sauna.acessar"

    # ATUALIZAR SISTEMA
    ATUALIZAR_SISTEMA_ACESSAR = "atualizar_sistema.acessar"

    # TROCAR SENHA
    TROCAR_SENHA = "conta.trocar_senha"

    @staticmethod
    def get_todas_permissoes():
        """Retorna TODAS as permissoes do sistema organizadas por modulo.
        Alinhado 1:1 com PermissionConstants.getTodasPermissoes() do Android."""
        PC = PermissionConstants
        lista = [
            # DASHBOARD - Botoes da Tela Principal
            Permissao("Dashboard", "Botao Vendas", PC.DASHBOARD_BTN_VENDAS, "Exibir botao Vendas no menu principal"),
            Permissao("Dashboard", "Botao Comandas", PC.DASHBOARD_BTN_COMANDAS, "Exibir botao Comandas no menu principal"),
            Permissao("Dashboard", "Botao Produtos", PC.DASHBOARD_BTN_PRODUTOS, "Exibir botao Produtos no menu principal"),
            Permissao("Dashboard", "Botao Gerenciar Produtos", PC.DASHBOARD_BTN_GERENCIAR_PRODUTOS, "Exibir botao Gerenciar Produtos no menu principal"),
            Permissao("Dashboard", "Botao Clientes", PC.DASHBOARD_BTN_CLIENTES, "Exibir botao Clientes no menu principal"),
            Permissao("Dashboard", "Botao Caixa", PC.DASHBOARD_BTN_CAIXA, "Exibir botao Caixa no menu principal"),
            Permissao("Dashboard", "Botao Relatorios", PC.DASHBOARD_BTN_RELATORIOS, "Exibir botao Relatorios no menu principal"),
            Permissao("Dashboard", "Botao Historico", PC.DASHBOARD_BTN_HISTORICO, "Exibir botao Historico no menu principal"),
            Permissao("Dashboard", "Botao Empresa", PC.DASHBOARD_BTN_EMPRESA, "Exibir botao Empresa no menu principal"),
            Permissao("Dashboard", "Botao Vendedores", PC.DASHBOARD_BTN_VENDEDORES, "Exibir botao Vendedores no menu principal"),
            Permissao("Dashboard", "Botao Entregadores", PC.DASHBOARD_BTN_ENTREGADORES, "Exibir botao Entregadores no menu principal"),
            Permissao("Dashboard", "Botao Usuarios", PC.DASHBOARD_BTN_USUARIOS, "Exibir botao Usuarios no menu principal"),
            Permissao("Dashboard", "Botao Perfis", PC.DASHBOARD_BTN_PERFIS, "Exibir botao Perfis e Permissoes no menu principal"),
            Permissao("Dashboard", "Botao Formas Pagamento", PC.DASHBOARD_BTN_FORMAS_PAGAMENTO, "Exibir botao Formas de Pagamento no menu principal"),
            Permissao("Dashboard", "Botao Tipos Produto", PC.DASHBOARD_BTN_TIPOS_PRODUTO, "Exibir botao Tipos de Produto no menu principal"),
            Permissao("Dashboard", "Botao Observacoes", PC.DASHBOARD_BTN_OBSERVACOES, "Exibir botao Obs. Cupom no menu principal"),
            Permissao("Dashboard", "Botao Impressora", PC.DASHBOARD_BTN_IMPRESSORA, "Exibir botao Impressora no menu principal"),
            Permissao("Dashboard", "Botao Backup", PC.DASHBOARD_BTN_BACKUP, "Exibir botao Backup no menu principal"),
            Permissao("Dashboard", "Botao Licenca", PC.DASHBOARD_BTN_LICENCA, "Exibir botao Licenca no menu principal"),
            Permissao("Dashboard", "Botao Trocar Senha", PC.DASHBOARD_BTN_TROCAR_SENHA, "Exibir botao Trocar Senha no menu principal"),
            Permissao("Dashboard", "Botao Modo Entregador", PC.DASHBOARD_BTN_MODO_ENTREGADOR, "Exibir botao Modo Entregador no menu principal"),
            Permissao("Dashboard", "Botao Gerenciar Entregas", PC.DASHBOARD_BTN_GERENCIAR_ENTREGAS, "Exibir botao Gerenciar Entregas no menu principal"),
            Permissao("Dashboard", "Botao Contas a Receber", PC.DASHBOARD_BTN_CONTAS_RECEBER, "Exibir botao Contas a Receber no menu principal"),
            Permissao("Dashboard", "Botao WhatsApp Bot", PC.DASHBOARD_BTN_WHATSBOT, "Exibir botao WhatsApp Bot no menu principal"),
            Permissao("Dashboard", "Botao Entrada Notas", PC.DASHBOARD_BTN_ENTRADA_NOTAS, "Exibir botao Entrada de Notas no menu principal"),
            Permissao("Dashboard", "Botao Taxa Entrega", PC.DASHBOARD_BTN_TAXA_ENTREGA, "Exibir botao Taxa de Entrega no menu principal"),
            Permissao("Dashboard", "Botao Sobre", PC.DASHBOARD_BTN_SOBRE, "Exibir botao Sobre no menu principal"),
            Permissao("Dashboard", "Botao Painel Chamados", PC.DASHBOARD_BTN_PAINEL_CHAMADOS, "Exibir botao Painel de Chamados no menu principal"),
            Permissao("Dashboard", "Botao Gerenciador Chamados", PC.DASHBOARD_BTN_GERENCIADOR_CHAMADOS, "Exibir botao Gerenciador de Chamados no menu principal"),
            Permissao("Dashboard", "Botao Painel Cozinha", PC.DASHBOARD_BTN_PAINEL_COZINHA, "Exibir botao Painel da Cozinha no menu principal"),
            # PAINEL DA COZINHA
            Permissao("Painel Cozinha", "Acessar", PC.PAINEL_COZINHA_ACESSAR, "Acessar o painel da cozinha para acompanhar pedidos"),
            # VENDAS
            Permissao("Vendas", "Acessar", PC.VENDAS_ACESSAR, "Acessar a tela de vendas"),
            Permissao("Vendas", "Criar", PC.VENDAS_CRIAR, "Criar nova venda e finalizar"),
            Permissao("Vendas", "Aplicar Desconto", PC.VENDAS_APLICAR_DESCONTO, "Aplicar desconto na venda"),
            Permissao("Vendas", "Aplicar Acrescimo", PC.VENDAS_APLICAR_ACRESCIMO, "Aplicar acrescimo na venda"),
            Permissao("Vendas", "Escolher Cliente", PC.VENDAS_ESCOLHER_CLIENTE, "Selecionar cliente na venda"),
            Permissao("Vendas", "Escolher Vendedor", PC.VENDAS_ESCOLHER_VENDEDOR, "Selecionar vendedor na venda"),
            Permissao("Vendas", "Escolher Entregador", PC.VENDAS_ESCOLHER_ENTREGADOR, "Selecionar entregador na venda"),
            # HISTORICO
            Permissao("Historico", "Acessar", PC.HISTORICO_ACESSAR, "Acessar historico de vendas"),
            Permissao("Historico", "Cancelar Venda", PC.HISTORICO_CANCELAR_VENDA, "Cancelar vendas no historico"),
            Permissao("Historico", "Reimprimir", PC.HISTORICO_REIMPRIMIR, "Reimprimir cupom de venda"),
            Permissao("Historico", "Enviar WhatsApp", PC.HISTORICO_ENVIAR_WHATSAPP, "Enviar cupom via WhatsApp"),
            # COMANDAS
            Permissao("Comandas", "Acessar", PC.COMANDAS_ACESSAR, "Acessar a tela de comandas"),
            Permissao("Comandas", "Criar", PC.COMANDAS_CRIAR, "Criar nova comanda"),
            Permissao("Comandas", "Editar", PC.COMANDAS_EDITAR, "Editar comanda (add/remover itens)"),
            Permissao("Comandas", "Fechar", PC.COMANDAS_FECHAR, "Fechar comanda e gerar venda"),
            Permissao("Comandas", "Cancelar", PC.COMANDAS_CANCELAR, "Cancelar comanda"),
            Permissao("Comandas", "Imprimir", PC.COMANDAS_IMPRIMIR, "Imprimir comanda"),
            # CAIXA
            Permissao("Caixa", "Acessar", PC.CAIXA_ACESSAR, "Acessar a tela de caixa"),
            Permissao("Caixa", "Abrir", PC.CAIXA_ABRIR, "Abrir caixa"),
            Permissao("Caixa", "Fechar", PC.CAIXA_FECHAR, "Fechar caixa"),
            Permissao("Caixa", "Vale/Debito", PC.CAIXA_VALE_DEBITO, "Adicionar vale/debito no caixa"),
            # PRODUTOS
            Permissao("Produtos", "Acessar", PC.PRODUTOS_ACESSAR, "Acessar cadastro de produtos"),
            Permissao("Produtos", "Criar", PC.PRODUTOS_CRIAR, "Cadastrar novo produto"),
            Permissao("Produtos", "Editar", PC.PRODUTOS_EDITAR, "Editar produto existente"),
            Permissao("Produtos", "Excluir", PC.PRODUTOS_EXCLUIR, "Excluir/inativar produto"),
            # GERENCIAR PRODUTOS
            Permissao("Gerenciar Produtos", "Acessar", PC.GERENCIAR_PRODUTOS_ACESSAR, "Acessar gerenciamento de produtos"),
            Permissao("Gerenciar Produtos", "Editar", PC.GERENCIAR_PRODUTOS_EDITAR, "Editar produto no gerenciamento"),
            Permissao("Gerenciar Produtos", "Inativar/Reativar", PC.GERENCIAR_PRODUTOS_INATIVAR, "Inativar ou reativar produtos"),
            # CLIENTES
            Permissao("Clientes", "Acessar", PC.CLIENTES_ACESSAR, "Acessar cadastro de clientes"),
            Permissao("Clientes", "Criar", PC.CLIENTES_CRIAR, "Cadastrar novo cliente"),
            Permissao("Clientes", "Editar", PC.CLIENTES_EDITAR, "Editar cliente existente"),
            Permissao("Clientes", "Excluir", PC.CLIENTES_EXCLUIR, "Excluir/inativar cliente"),
            # VENDEDORES
            Permissao("Vendedores", "Acessar", PC.VENDEDORES_ACESSAR, "Acessar cadastro de vendedores"),
            Permissao("Vendedores", "Criar", PC.VENDEDORES_CRIAR, "Cadastrar novo vendedor"),
            Permissao("Vendedores", "Editar", PC.VENDEDORES_EDITAR, "Editar vendedor existente"),
            Permissao("Vendedores", "Excluir", PC.VENDEDORES_EXCLUIR, "Excluir/inativar vendedor"),
            # ENTREGADORES
            Permissao("Entregadores", "Acessar", PC.ENTREGADORES_ACESSAR, "Acessar cadastro de entregadores"),
            Permissao("Entregadores", "Criar", PC.ENTREGADORES_CRIAR, "Cadastrar novo entregador"),
            Permissao("Entregadores", "Editar", PC.ENTREGADORES_EDITAR, "Editar entregador existente"),
            Permissao("Entregadores", "Excluir", PC.ENTREGADORES_EXCLUIR, "Excluir/inativar entregador"),
            # EMPRESA
            Permissao("Empresa", "Acessar", PC.EMPRESA_ACESSAR, "Acessar dados da empresa"),
            Permissao("Empresa", "Editar", PC.EMPRESA_EDITAR, "Editar dados da empresa"),
            # USUARIOS
            Permissao("Usuarios", "Acessar", PC.USUARIOS_ACESSAR, "Acessar cadastro de usuarios"),
            Permissao("Usuarios", "Criar", PC.USUARIOS_CRIAR, "Cadastrar novo usuario"),
            Permissao("Usuarios", "Editar", PC.USUARIOS_EDITAR, "Editar usuario existente"),
            Permissao("Usuarios", "Excluir", PC.USUARIOS_EXCLUIR, "Excluir/inativar usuario"),
            # PERFIS E PERMISSOES
            Permissao("Perfis", "Acessar", PC.PERFIS_ACESSAR, "Acessar gerenciamento de perfis"),
            Permissao("Perfis", "Criar", PC.PERFIS_CRIAR, "Criar novo perfil de acesso"),
            Permissao("Perfis", "Editar", PC.PERFIS_EDITAR, "Editar perfil de acesso"),
            Permissao("Perfis", "Excluir", PC.PERFIS_EXCLUIR, "Excluir perfil de acesso"),
            Permissao("Perfis", "Gerenciar Permissoes", PC.PERFIS_GERENCIAR_PERMISSOES, "Atribuir/remover permissoes dos perfis"),
            Permissao("Perfis", "Permissoes por Usuario", PC.PERFIS_PERMISSOES_USUARIO, "Gerenciar permissoes individuais por usuario"),
            # FORMAS DE PAGAMENTO
            Permissao("Formas Pagamento", "Acessar", PC.FORMAS_PAGAMENTO_ACESSAR, "Acessar formas de pagamento"),
            Permissao("Formas Pagamento", "Criar", PC.FORMAS_PAGAMENTO_CRIAR, "Cadastrar nova forma de pagamento"),
            Permissao("Formas Pagamento", "Editar", PC.FORMAS_PAGAMENTO_EDITAR, "Editar forma de pagamento"),
            Permissao("Formas Pagamento", "Excluir", PC.FORMAS_PAGAMENTO_EXCLUIR, "Excluir forma de pagamento"),
            # TIPOS DE PRODUTO
            Permissao("Tipos Produto", "Acessar", PC.TIPOS_PRODUTO_ACESSAR, "Acessar tipos de produto"),
            Permissao("Tipos Produto", "Criar", PC.TIPOS_PRODUTO_CRIAR, "Cadastrar novo tipo de produto"),
            Permissao("Tipos Produto", "Editar", PC.TIPOS_PRODUTO_EDITAR, "Editar tipo de produto"),
            Permissao("Tipos Produto", "Excluir", PC.TIPOS_PRODUTO_EXCLUIR, "Excluir tipo de produto"),
            # ADICIONAIS
            Permissao("Adicionais", "Acessar", PC.ADICIONAIS_ACESSAR, "Acessar cadastro de adicionais"),
            Permissao("Adicionais", "Criar", PC.ADICIONAIS_CRIAR, "Cadastrar novo adicional"),
            Permissao("Adicionais", "Editar", PC.ADICIONAIS_EDITAR, "Editar adicional existente"),
            Permissao("Adicionais", "Excluir", PC.ADICIONAIS_EXCLUIR, "Excluir/inativar adicional"),
            # DASHBOARD - Botao Adicionais
            Permissao("Dashboard", "Botao Adicionais", PC.DASHBOARD_BTN_ADICIONAIS, "Exibir botao Adicionais no menu principal"),
            # OBSERVACOES DO CUPOM
            Permissao("Observacoes Cupom", "Acessar", PC.OBSERVACOES_ACESSAR, "Acessar observacoes do cupom"),
            Permissao("Observacoes Cupom", "Criar", PC.OBSERVACOES_CRIAR, "Criar nova observacao"),
            Permissao("Observacoes Cupom", "Editar", PC.OBSERVACOES_EDITAR, "Editar observacao"),
            Permissao("Observacoes Cupom", "Excluir", PC.OBSERVACOES_EXCLUIR, "Excluir observacao"),
            # RELATORIOS
            Permissao("Relatorios", "Acessar", PC.RELATORIOS_ACESSAR, "Acessar tela de relatorios"),
            Permissao("Relatorios", "Vendas", PC.RELATORIOS_VENDAS, "Gerar relatorio de vendas"),
            Permissao("Relatorios", "Lucratividade", PC.RELATORIOS_LUCRATIVIDADE, "Gerar relatorio de lucratividade"),
            Permissao("Relatorios", "Por Vendedor", PC.RELATORIOS_VENDEDOR, "Gerar relatorio por vendedor"),
            Permissao("Relatorios", "Por Entregador", PC.RELATORIOS_ENTREGADOR, "Gerar relatorio por entregador"),
            Permissao("Relatorios", "Por Cliente", PC.RELATORIOS_CLIENTE, "Gerar relatorio por cliente"),
            Permissao("Relatorios", "Produtos", PC.RELATORIOS_PRODUTOS, "Gerar relatorio de produtos"),
            Permissao("Relatorios", "Caixa", PC.RELATORIOS_CAIXA, "Gerar relatorio de caixa"),
            # CONFIGURACOES
            Permissao("Impressora", "Acessar", PC.CONFIG_IMPRESSORA_ACESSAR, "Acessar config. de impressora"),
            Permissao("Impressora", "Editar", PC.CONFIG_IMPRESSORA_EDITAR, "Alterar config. de impressora"),
            # BACKUP
            Permissao("Backup", "Acessar", PC.BACKUP_ACESSAR, "Acessar tela de backup"),
            Permissao("Backup", "Realizar", PC.BACKUP_REALIZAR, "Realizar backup manualmente"),
            Permissao("Backup", "Restaurar", PC.BACKUP_RESTAURAR, "Restaurar backup"),
            Permissao("Backup", "Configurar", PC.BACKUP_CONFIGURAR, "Alterar config. de backup"),
            # LICENCA
            Permissao("Licenca", "Acessar", PC.LICENCA_ACESSAR, "Acessar tela de licenca"),
            Permissao("Licenca", "Ativar", PC.LICENCA_ATIVAR, "Ativar/renovar licenca"),
            # WHATSAPP BOT
            Permissao("WhatsApp Bot", "Acessar", PC.WHATSBOT_ACESSAR, "Acessar WhatsApp Bot"),
            Permissao("WhatsApp Bot", "Configurar", PC.WHATSBOT_CONFIGURAR, "Configurar WhatsApp Bot"),
            # ENTREGAS
            Permissao("Entregas", "Acessar", PC.ENTREGAS_ACESSAR, "Acessar gerenciamento de entregas"),
            Permissao("Entregas", "Gerenciar", PC.ENTREGAS_GERENCIAR, "Gerenciar entregas"),
            Permissao("Modo Entregador", "Acessar", PC.MODO_ENTREGADOR_ACESSAR, "Acessar modo entregador"),
            # CONTAS A RECEBER
            Permissao("Contas a Receber", "Acessar", PC.CONTAS_RECEBER_ACESSAR, "Acessar tela de contas a receber"),
            Permissao("Contas a Receber", "Receber", PC.CONTAS_RECEBER_RECEBER, "Registrar recebimento de contas"),
            Permissao("Contas a Receber", "Cancelar", PC.CONTAS_RECEBER_CANCELAR, "Cancelar contas a receber"),
            Permissao("Contas a Receber", "Relatorio", PC.CONTAS_RECEBER_RELATORIO, "Gerar relatorio de contas a receber"),
            # ENTRADA DE NOTAS
            Permissao("Entrada de Notas", "Acessar", PC.ENTRADA_NOTAS_ACESSAR, "Acessar tela de entrada de notas"),
            Permissao("Entrada de Notas", "Criar", PC.ENTRADA_NOTAS_CRIAR, "Criar nova nota de entrada"),
            Permissao("Entrada de Notas", "Confirmar", PC.ENTRADA_NOTAS_CONFIRMAR, "Confirmar nota e atualizar estoque"),
            Permissao("Entrada de Notas", "Cancelar", PC.ENTRADA_NOTAS_CANCELAR, "Cancelar nota de entrada"),
            # TAXA DE ENTREGA POR BAIRRO
            Permissao("Taxa Entrega", "Acessar", PC.TAXA_ENTREGA_ACESSAR, "Acessar cadastro de taxas de entrega por bairro"),
            Permissao("Taxa Entrega", "Criar", PC.TAXA_ENTREGA_CRIAR, "Cadastrar novo bairro com taxa"),
            Permissao("Taxa Entrega", "Editar", PC.TAXA_ENTREGA_EDITAR, "Editar bairro e taxa"),
            Permissao("Taxa Entrega", "Excluir", PC.TAXA_ENTREGA_EXCLUIR, "Excluir bairro"),
            # PAINEL DE CHAMADOS
            Permissao("Painel Chamados", "Acessar", PC.PAINEL_CHAMADOS_ACESSAR, "Acessar o painel de chamados dos clientes"),
            Permissao("Painel Chamados", "Criar", PC.PAINEL_CHAMADOS_CRIAR, "Criar novos chamados"),
            Permissao("Painel Chamados", "Chamar", PC.PAINEL_CHAMADOS_CHAMAR, "Chamar clientes"),
            Permissao("Painel Chamados", "Atender", PC.PAINEL_CHAMADOS_ATENDER, "Atender chamados"),
            # GERENCIADOR DE CHAMADOS
            Permissao("Gerenciador Chamados", "Acessar", PC.GERENCIADOR_CHAMADOS_ACESSAR, "Acessar o gerenciador de chamadas com voz e sinal sonoro"),
            # GARCONS
            Permissao("Garcons", "Acessar", PC.GARCONS_ACESSAR, "Acessar cadastro de garcons"),
            Permissao("Garcons", "Criar", PC.GARCONS_CRIAR, "Cadastrar novo garcom"),
            Permissao("Garcons", "Editar", PC.GARCONS_EDITAR, "Editar garcom existente"),
            Permissao("Garcons", "Excluir", PC.GARCONS_EXCLUIR, "Excluir/inativar garcom"),
            # MESAS
            Permissao("Mesas", "Acessar", PC.MESAS_ACESSAR, "Acessar cadastro de mesas"),
            Permissao("Mesas", "Criar", PC.MESAS_CRIAR, "Cadastrar nova mesa"),
            Permissao("Mesas", "Editar", PC.MESAS_EDITAR, "Editar mesa existente"),
            Permissao("Mesas", "Excluir", PC.MESAS_EXCLUIR, "Excluir/inativar mesa"),
            # GERENCIAR MESAS
            Permissao("Gerenciar Mesas", "Acessar", PC.GERENCIAR_MESAS_ACESSAR, "Acessar tela de gerenciamento visual de mesas"),
            # DASHBOARD - Botoes Garcons, Mesas e Gerenciar Mesas
            Permissao("Dashboard", "Botao Garcons", PC.DASHBOARD_BTN_GARCONS, "Exibir botao Garcons no menu principal"),
            Permissao("Dashboard", "Botao Cadastro Mesas", PC.DASHBOARD_BTN_CADASTRO_MESAS, "Exibir botao Cadastro de Mesas no menu principal"),
            Permissao("Dashboard", "Botao Gerenciar Mesas", PC.DASHBOARD_BTN_GERENCIAR_MESAS, "Exibir botao Gerenciar Mesas no menu principal"),
            # ARMARIOS PARA SAUNA
            Permissao("Armarios Sauna", "Acessar", PC.ARMARIOS_SAUNA_ACESSAR, "Acessar cadastro de armarios para sauna"),
            Permissao("Armarios Sauna", "Criar", PC.ARMARIOS_SAUNA_CRIAR, "Cadastrar novo armario para sauna"),
            Permissao("Armarios Sauna", "Editar", PC.ARMARIOS_SAUNA_EDITAR, "Editar armario existente"),
            Permissao("Armarios Sauna", "Excluir", PC.ARMARIOS_SAUNA_EXCLUIR, "Excluir/inativar armario"),
            # GERENCIAR ARMARIOS SAUNA
            Permissao("Gerenciar Armarios Sauna", "Acessar", PC.GERENCIAR_ARMARIOS_SAUNA_ACESSAR, "Acessar tela de gerenciamento visual de armarios para sauna"),
            # DASHBOARD - Botoes Armarios Sauna
            Permissao("Dashboard", "Botao Cadastro Armarios Sauna", PC.DASHBOARD_BTN_CADASTRO_ARMARIOS_SAUNA, "Exibir botao Cadastro de Armarios Sauna no menu principal"),
            Permissao("Dashboard", "Botao Gerenciar Armarios Sauna", PC.DASHBOARD_BTN_GERENCIAR_ARMARIOS_SAUNA, "Exibir botao Gerenciar Armarios Sauna no menu principal"),
            # ATUALIZAR SISTEMA
            Permissao("Atualizar Sistema", "Acessar", PC.ATUALIZAR_SISTEMA_ACESSAR, "Acessar funcao de atualizar o sistema via FTP"),
            # DASHBOARD - Botao Atualizar
            Permissao("Dashboard", "Botao Atualizar Sistema", PC.DASHBOARD_BTN_ATUALIZAR, "Exibir botao Atualizar Sistema no menu principal"),
            # DASHBOARD - Botao Servidor Web e Log Auditoria
            Permissao("Dashboard", "Botao Servidor Web", PC.DASHBOARD_BTN_SERVIDOR_WEB, "Exibir botao Servidor Web no menu principal"),
            Permissao("Dashboard", "Botao Log Auditoria", PC.DASHBOARD_BTN_LOG_AUDITORIA, "Exibir botao Log Auditoria no menu principal"),
            # TROCAR SENHA
            Permissao("Conta", "Trocar Senha", PC.TROCAR_SENHA, "Trocar a propria senha"),
        ]
        return lista

    @staticmethod
    def get_todas_chaves():
        """Retorna todas as chaves de permissao."""
        return [p.chave for p in PermissionConstants.get_todas_permissoes()]

    @staticmethod
    def get_permissoes_por_modulo():
        """Retorna as permissoes agrupadas por modulo."""
        mapa = {}
        for p in PermissionConstants.get_todas_permissoes():
            if p.modulo not in mapa:
                mapa[p.modulo] = []
            mapa[p.modulo].append(p)
        return mapa


# ============================================================================
# SETUP DO SISTEMA DE PERMISSOES (alinhado com PermissionDatabaseSetup.java)
# ============================================================================

class PermissionDatabaseSetup:
    """Classe responsavel por criar e atualizar as tabelas do sistema de permissoes.
    Alinhada com PermissionDatabaseSetup.java do Android."""

    @staticmethod
    def setup(db):
        """Executa a criacao/atualizacao completa do sistema de permissoes.
        Inclui retry automatico em caso de erro de conexao (bytearray, etc)."""
        max_retries = 2
        for attempt in range(max_retries + 1):
            try:
                PermissionDatabaseSetup._adicionar_coluna_personalizavel(db)
                PermissionDatabaseSetup._sincronizar_permissoes(db)
                PermissionDatabaseSetup._criar_perfis_default(db)
                PermissionDatabaseSetup._adicionar_coluna_perfil(db)
                PermissionDatabaseSetup._migrar_usuarios_existentes(db)
                PermissionDatabaseSetup._migrar_tabela_permissoes_usuario_antiga(db)
                print("Setup do sistema de permissoes concluido com sucesso!")
                return  # Sucesso, sair do loop
            except Exception as e:
                err_msg = str(e).lower()
                is_conn_error = ('bytearray' in err_msg or 'index out of range' in err_msg
                                 or 'not connected' in err_msg or 'connection' in err_msg)
                if attempt < max_retries and is_conn_error:
                    print(f"Aviso no setup de permissoes (tentativa {attempt+1}): {e} - reconectando...")
                    try:
                        db._safe_reconnect()
                    except Exception:
                        pass
                    time.sleep(0.5)
                else:
                    print(f"Erro no setup de permissoes: {e}")

    @staticmethod
    def _adicionar_coluna_personalizavel(db):
        try:
            rows = db.execute_query(
                "SELECT COUNT(*) as cnt FROM information_schema.COLUMNS "
                "WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'perfis' AND COLUMN_NAME = 'personalizavel'")
            if rows and rows[0].get("cnt", 0) == 0:
                db.execute_update("ALTER TABLE perfis ADD COLUMN personalizavel TINYINT(1) DEFAULT 0")
        except Exception as e:
            print(f"Aviso coluna personalizavel: {e}")

    @staticmethod
    def _adicionar_coluna_perfil(db):
        try:
            rows = db.execute_query(
                "SELECT COUNT(*) as cnt FROM information_schema.COLUMNS "
                "WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'usuarios' AND COLUMN_NAME = 'perfil_id'")
            if rows and rows[0].get("cnt", 0) == 0:
                db.execute_update("ALTER TABLE usuarios ADD COLUMN perfil_id INT DEFAULT NULL")
        except Exception as e:
            print(f"Aviso coluna perfil_id: {e}")

    @staticmethod
    def _sincronizar_permissoes(db):
        """Sincroniza as permissoes definidas em PermissionConstants com o banco."""
        todas = PermissionConstants.get_todas_permissoes()
        novas = 0
        atualizadas = 0
        for p in todas:
            rows = db.execute_query("SELECT COUNT(*) as cnt FROM permissoes WHERE chave = %s", (p.chave,))
            if rows and rows[0].get("cnt", 0) == 0:
                db.execute_update(
                    "INSERT INTO permissoes (modulo, acao, chave, descricao) VALUES (%s, %s, %s, %s)",
                    (p.modulo, p.acao, p.chave, p.descricao))
                novas += 1
            else:
                db.execute_update(
                    "UPDATE permissoes SET modulo = %s, acao = %s, descricao = %s WHERE chave = %s",
                    (p.modulo, p.acao, p.descricao, p.chave))
                atualizadas += 1
        print(f"Permissoes sincronizadas: {novas} novas, {atualizadas} atualizadas de {len(todas)} total")

    @staticmethod
    def _criar_perfis_default(db):
        """Cria os perfis padrao do sistema se nao existirem."""
        # Administrador sistematico
        PermissionDatabaseSetup._garantir_admin_sistematico(db)
        # Demais perfis
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Gerente",
            "Acesso amplo: vendas, caixa, relatorios, cadastros. Sem config. criticas.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Operacional",
            "Operacoes basicas do dia-a-dia: vendas, comandas, caixa, historico, clientes.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Caixa",
            "Focado em operacoes de caixa: abertura/fechamento, vendas, comandas, vale/debito.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Atendente",
            "Atendimento ao cliente: vendas, comandas, clientes, chamados.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Garcom",
            "Servico de mesa: comandas, gerenciar mesas, painel cozinha.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Balcao",
            "Vendas no balcao: vendas, comandas, produtos, clientes, caixa.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Vendedor",
            "Vendas e consultas: vendas, produtos, clientes, historico.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Estoquista",
            "Controle de estoque: produtos, gerenciar produtos, entrada de notas.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Entregador",
            "Apenas modo entregador e entregas.", False, False)
        PermissionDatabaseSetup._criar_perfil_se_nao_existe(db, "Personalizavel",
            "Perfil personalizavel: o administrador escolhe quais botoes do dashboard este perfil pode usar.", False, True)
        # Conceder permissoes
        for nome in ["Gerente", "Operacional", "Caixa", "Atendente", "Garcom", "Balcao", "Vendedor", "Estoquista", "Entregador"]:
            PermissionDatabaseSetup._conceder_permissoes_para_perfil(db, nome)
        # Migrar perfil antigo
        PermissionDatabaseSetup._migrar_perfil_antigo(db)

    @staticmethod
    def _garantir_admin_sistematico(db):
        rows = db.execute_query("SELECT id FROM perfis WHERE nome = 'Administrador' AND sistematico = 1")
        if not rows:
            admin_id = db.execute_update(
                "INSERT INTO perfis (nome, descricao, sistematico, ativo, personalizavel) "
                "VALUES ('Administrador', 'Acesso total ao sistema. Todas as permissoes.', 1, 1, 0)")
            PermissionDatabaseSetup._conceder_todas_permissoes(db, admin_id)
        else:
            PermissionDatabaseSetup._conceder_todas_permissoes(db, rows[0]["id"])

    @staticmethod
    def _conceder_todas_permissoes(db, perfil_id):
        db.execute_update(
            "INSERT IGNORE INTO perfil_permissoes (perfil_id, permissao_id) "
            "SELECT %s, id FROM permissoes", (perfil_id,))

    @staticmethod
    def _criar_perfil_se_nao_existe(db, nome, descricao, sistematico, personalizavel):
        rows = db.execute_query("SELECT COUNT(*) as cnt FROM perfis WHERE nome = %s", (nome,))
        if rows and rows[0].get("cnt", 0) == 0:
            db.execute_update(
                "INSERT INTO perfis (nome, descricao, sistematico, ativo, personalizavel) VALUES (%s, %s, %s, 1, %s)",
                (nome, descricao, 1 if sistematico else 0, 1 if personalizavel else 0))
        else:
            db.execute_update(
                "UPDATE perfis SET descricao = %s, personalizavel = %s WHERE nome = %s AND sistematico = 0",
                (descricao, 1 if personalizavel else 0, nome))

    @staticmethod
    def _conceder_permissoes(db, perfil_id, chaves):
        for chave in chaves:
            db.execute_update(
                "INSERT IGNORE INTO perfil_permissoes (perfil_id, permissao_id) "
                "SELECT %s, id FROM permissoes WHERE chave = %s", (perfil_id, chave))

    @staticmethod
    def _get_perfil_id(db, nome):
        rows = db.execute_query("SELECT id FROM perfis WHERE nome = %s AND ativo = 1", (nome,))
        return rows[0]["id"] if rows else 0

    @staticmethod
    def _conceder_permissoes_para_perfil(db, nome_perfil):
        perfil_id = PermissionDatabaseSetup._get_perfil_id(db, nome_perfil)
        if perfil_id <= 0:
            return
        # Verificar se ja tem permissoes
        rows = db.execute_query("SELECT COUNT(*) as cnt FROM perfil_permissoes WHERE perfil_id = %s", (perfil_id,))
        if rows and rows[0].get("cnt", 0) > 0:
            return
        PC = PermissionConstants
        chaves_map = {
            # ================================================================
            # GERENTE - Acesso amplo: vendas, caixa, relatorios, cadastros,
            # entregas, mesas, armarios, chamados, cozinha. Sem config. criticas
            # (backup, licenca, usuarios, perfis, whatsbot, atualizar).
            # ================================================================
            "Gerente": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_GERENCIAR_PRODUTOS, PC.DASHBOARD_BTN_CLIENTES, PC.DASHBOARD_BTN_CAIXA,
                PC.DASHBOARD_BTN_RELATORIOS, PC.DASHBOARD_BTN_HISTORICO, PC.DASHBOARD_BTN_EMPRESA,
                PC.DASHBOARD_BTN_VENDEDORES, PC.DASHBOARD_BTN_ENTREGADORES, PC.DASHBOARD_BTN_FORMAS_PAGAMENTO,
                PC.DASHBOARD_BTN_TIPOS_PRODUTO, PC.DASHBOARD_BTN_OBSERVACOES, PC.DASHBOARD_BTN_IMPRESSORA,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                PC.DASHBOARD_BTN_GERENCIAR_ENTREGAS, PC.DASHBOARD_BTN_CONTAS_RECEBER,
                PC.DASHBOARD_BTN_ENTRADA_NOTAS, PC.DASHBOARD_BTN_TAXA_ENTREGA,
                PC.DASHBOARD_BTN_ADICIONAIS, PC.DASHBOARD_BTN_GARCONS,
                PC.DASHBOARD_BTN_CADASTRO_MESAS, PC.DASHBOARD_BTN_GERENCIAR_MESAS,
                PC.DASHBOARD_BTN_CADASTRO_ARMARIOS_SAUNA, PC.DASHBOARD_BTN_GERENCIAR_ARMARIOS_SAUNA,
                PC.DASHBOARD_BTN_PAINEL_COZINHA,
                PC.DASHBOARD_BTN_PAINEL_CHAMADOS, PC.DASHBOARD_BTN_GERENCIADOR_CHAMADOS,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_APLICAR_DESCONTO, PC.VENDAS_APLICAR_ACRESCIMO,
                PC.VENDAS_ESCOLHER_CLIENTE, PC.VENDAS_ESCOLHER_VENDEDOR, PC.VENDAS_ESCOLHER_ENTREGADOR,
                # -- Historico --
                PC.HISTORICO_ACESSAR, PC.HISTORICO_CANCELAR_VENDA, PC.HISTORICO_REIMPRIMIR, PC.HISTORICO_ENVIAR_WHATSAPP,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_FECHAR,
                PC.COMANDAS_CANCELAR, PC.COMANDAS_IMPRIMIR,
                # -- Caixa --
                PC.CAIXA_ACESSAR, PC.CAIXA_ABRIR, PC.CAIXA_FECHAR, PC.CAIXA_VALE_DEBITO,
                # -- Produtos --
                PC.PRODUTOS_ACESSAR, PC.PRODUTOS_CRIAR, PC.PRODUTOS_EDITAR, PC.PRODUTOS_EXCLUIR,
                PC.GERENCIAR_PRODUTOS_ACESSAR, PC.GERENCIAR_PRODUTOS_EDITAR, PC.GERENCIAR_PRODUTOS_INATIVAR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR, PC.CLIENTES_EDITAR, PC.CLIENTES_EXCLUIR,
                # -- Vendedores --
                PC.VENDEDORES_ACESSAR, PC.VENDEDORES_CRIAR, PC.VENDEDORES_EDITAR, PC.VENDEDORES_EXCLUIR,
                # -- Entregadores --
                PC.ENTREGADORES_ACESSAR, PC.ENTREGADORES_CRIAR, PC.ENTREGADORES_EDITAR, PC.ENTREGADORES_EXCLUIR,
                # -- Empresa --
                PC.EMPRESA_ACESSAR, PC.EMPRESA_EDITAR,
                # -- Formas de Pagamento --
                PC.FORMAS_PAGAMENTO_ACESSAR, PC.FORMAS_PAGAMENTO_CRIAR, PC.FORMAS_PAGAMENTO_EDITAR, PC.FORMAS_PAGAMENTO_EXCLUIR,
                # -- Tipos de Produto --
                PC.TIPOS_PRODUTO_ACESSAR, PC.TIPOS_PRODUTO_CRIAR, PC.TIPOS_PRODUTO_EDITAR, PC.TIPOS_PRODUTO_EXCLUIR,
                # -- Adicionais --
                PC.ADICIONAIS_ACESSAR, PC.ADICIONAIS_CRIAR, PC.ADICIONAIS_EDITAR, PC.ADICIONAIS_EXCLUIR,
                # -- Observacoes Cupom --
                PC.OBSERVACOES_ACESSAR, PC.OBSERVACOES_CRIAR, PC.OBSERVACOES_EDITAR, PC.OBSERVACOES_EXCLUIR,
                # -- Relatorios --
                PC.RELATORIOS_ACESSAR, PC.RELATORIOS_VENDAS, PC.RELATORIOS_LUCRATIVIDADE, PC.RELATORIOS_VENDEDOR,
                PC.RELATORIOS_ENTREGADOR, PC.RELATORIOS_CLIENTE, PC.RELATORIOS_PRODUTOS, PC.RELATORIOS_CAIXA,
                # -- Impressora --
                PC.CONFIG_IMPRESSORA_ACESSAR, PC.CONFIG_IMPRESSORA_EDITAR,
                # -- Entregas --
                PC.ENTREGAS_ACESSAR, PC.ENTREGAS_GERENCIAR,
                # -- Entrada de Notas --
                PC.ENTRADA_NOTAS_ACESSAR, PC.ENTRADA_NOTAS_CRIAR, PC.ENTRADA_NOTAS_CONFIRMAR, PC.ENTRADA_NOTAS_CANCELAR,
                # -- Contas a Receber --
                PC.CONTAS_RECEBER_ACESSAR, PC.CONTAS_RECEBER_RECEBER, PC.CONTAS_RECEBER_CANCELAR, PC.CONTAS_RECEBER_RELATORIO,
                # -- Taxa de Entrega --
                PC.TAXA_ENTREGA_ACESSAR, PC.TAXA_ENTREGA_CRIAR, PC.TAXA_ENTREGA_EDITAR, PC.TAXA_ENTREGA_EXCLUIR,
                # -- Garcons --
                PC.GARCONS_ACESSAR, PC.GARCONS_CRIAR, PC.GARCONS_EDITAR, PC.GARCONS_EXCLUIR,
                # -- Mesas --
                PC.MESAS_ACESSAR, PC.MESAS_CRIAR, PC.MESAS_EDITAR, PC.MESAS_EXCLUIR,
                PC.GERENCIAR_MESAS_ACESSAR,
                # -- Armarios Sauna --
                PC.ARMARIOS_SAUNA_ACESSAR, PC.ARMARIOS_SAUNA_CRIAR, PC.ARMARIOS_SAUNA_EDITAR, PC.ARMARIOS_SAUNA_EXCLUIR,
                PC.GERENCIAR_ARMARIOS_SAUNA_ACESSAR,
                # -- Painel Cozinha --
                PC.PAINEL_COZINHA_ACESSAR,
                # -- Chamados --
                PC.PAINEL_CHAMADOS_ACESSAR, PC.PAINEL_CHAMADOS_CRIAR, PC.PAINEL_CHAMADOS_CHAMAR, PC.PAINEL_CHAMADOS_ATENDER,
                PC.GERENCIADOR_CHAMADOS_ACESSAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # OPERACIONAL - Operacoes basicas do dia-a-dia: vendas, comandas,
            # caixa, historico, clientes, painel cozinha.
            # ================================================================
            "Operacional": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_CLIENTES, PC.DASHBOARD_BTN_CAIXA, PC.DASHBOARD_BTN_HISTORICO,
                PC.DASHBOARD_BTN_PAINEL_COZINHA,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_APLICAR_DESCONTO, PC.VENDAS_ESCOLHER_CLIENTE,
                # -- Historico --
                PC.HISTORICO_ACESSAR, PC.HISTORICO_REIMPRIMIR,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_FECHAR, PC.COMANDAS_IMPRIMIR,
                # -- Caixa --
                PC.CAIXA_ACESSAR, PC.CAIXA_ABRIR, PC.CAIXA_FECHAR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR,
                # -- Painel Cozinha --
                PC.PAINEL_COZINHA_ACESSAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # CAIXA - Focado em operacoes de caixa: abertura/fechamento,
            # vendas, comandas, vale/debito, contas a receber (consulta).
            # ================================================================
            "Caixa": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_CAIXA,
                PC.DASHBOARD_BTN_HISTORICO, PC.DASHBOARD_BTN_CLIENTES, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_CONTAS_RECEBER,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_APLICAR_DESCONTO,
                PC.VENDAS_ESCOLHER_CLIENTE, PC.VENDAS_ESCOLHER_VENDEDOR,
                # -- Caixa --
                PC.CAIXA_ACESSAR, PC.CAIXA_ABRIR, PC.CAIXA_FECHAR, PC.CAIXA_VALE_DEBITO,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_FECHAR, PC.COMANDAS_IMPRIMIR,
                # -- Historico --
                PC.HISTORICO_ACESSAR, PC.HISTORICO_REIMPRIMIR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Contas a Receber (somente consulta e recebimento) --
                PC.CONTAS_RECEBER_ACESSAR, PC.CONTAS_RECEBER_RECEBER,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # ATENDENTE - Atendimento ao cliente: vendas, comandas, clientes,
            # chamados, painel cozinha.
            # ================================================================
            "Atendente": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_CLIENTES,
                PC.DASHBOARD_BTN_PRODUTOS, PC.DASHBOARD_BTN_HISTORICO,
                PC.DASHBOARD_BTN_PAINEL_CHAMADOS, PC.DASHBOARD_BTN_PAINEL_COZINHA,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_ESCOLHER_CLIENTE,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_IMPRIMIR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR, PC.CLIENTES_EDITAR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Historico (somente consulta) --
                PC.HISTORICO_ACESSAR,
                # -- Chamados --
                PC.PAINEL_CHAMADOS_ACESSAR, PC.PAINEL_CHAMADOS_CRIAR, PC.PAINEL_CHAMADOS_CHAMAR, PC.PAINEL_CHAMADOS_ATENDER,
                # -- Painel Cozinha --
                PC.PAINEL_COZINHA_ACESSAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # GARCOM - Servico de mesa: comandas, gerenciar mesas, painel
            # cozinha, chamados de mesa.
            # ================================================================
            "Garcom": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_GERENCIAR_MESAS,
                PC.DASHBOARD_BTN_PAINEL_COZINHA, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_PAINEL_CHAMADOS,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_IMPRIMIR,
                # -- Mesas --
                PC.GERENCIAR_MESAS_ACESSAR,
                # -- Painel Cozinha --
                PC.PAINEL_COZINHA_ACESSAR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Chamados (criar e visualizar) --
                PC.PAINEL_CHAMADOS_ACESSAR, PC.PAINEL_CHAMADOS_CRIAR, PC.PAINEL_CHAMADOS_CHAMAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # BALCAO - Vendas no balcao: vendas, comandas, produtos, clientes,
            # caixa, contas a receber.
            # ================================================================
            "Balcao": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_CLIENTES, PC.DASHBOARD_BTN_CAIXA, PC.DASHBOARD_BTN_HISTORICO,
                PC.DASHBOARD_BTN_CONTAS_RECEBER, PC.DASHBOARD_BTN_PAINEL_COZINHA,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_APLICAR_DESCONTO,
                PC.VENDAS_ESCOLHER_CLIENTE, PC.VENDAS_ESCOLHER_VENDEDOR,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_FECHAR, PC.COMANDAS_IMPRIMIR,
                # -- Caixa --
                PC.CAIXA_ACESSAR, PC.CAIXA_ABRIR, PC.CAIXA_FECHAR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR,
                # -- Historico --
                PC.HISTORICO_ACESSAR, PC.HISTORICO_REIMPRIMIR,
                # -- Contas a Receber (consulta) --
                PC.CONTAS_RECEBER_ACESSAR,
                # -- Painel Cozinha --
                PC.PAINEL_COZINHA_ACESSAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # VENDEDOR - Vendas e consultas: vendas, produtos, clientes,
            # historico.
            # ================================================================
            "Vendedor": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_VENDAS, PC.DASHBOARD_BTN_COMANDAS, PC.DASHBOARD_BTN_PRODUTOS,
                PC.DASHBOARD_BTN_CLIENTES, PC.DASHBOARD_BTN_HISTORICO,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Vendas --
                PC.VENDAS_ACESSAR, PC.VENDAS_CRIAR, PC.VENDAS_ESCOLHER_CLIENTE,
                # -- Comandas --
                PC.COMANDAS_ACESSAR, PC.COMANDAS_CRIAR, PC.COMANDAS_EDITAR, PC.COMANDAS_IMPRIMIR,
                # -- Produtos (somente consulta) --
                PC.PRODUTOS_ACESSAR,
                # -- Clientes --
                PC.CLIENTES_ACESSAR, PC.CLIENTES_CRIAR,
                # -- Historico (somente consulta) --
                PC.HISTORICO_ACESSAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # ESTOQUISTA - Controle de estoque: produtos, gerenciar produtos,
            # entrada de notas, tipos de produto, adicionais.
            # ================================================================
            "Estoquista": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_PRODUTOS, PC.DASHBOARD_BTN_GERENCIAR_PRODUTOS,
                PC.DASHBOARD_BTN_ENTRADA_NOTAS, PC.DASHBOARD_BTN_TIPOS_PRODUTO,
                PC.DASHBOARD_BTN_ADICIONAIS,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Produtos --
                PC.PRODUTOS_ACESSAR, PC.PRODUTOS_CRIAR, PC.PRODUTOS_EDITAR, PC.PRODUTOS_EXCLUIR,
                PC.GERENCIAR_PRODUTOS_ACESSAR, PC.GERENCIAR_PRODUTOS_EDITAR, PC.GERENCIAR_PRODUTOS_INATIVAR,
                # -- Entrada de Notas --
                PC.ENTRADA_NOTAS_ACESSAR, PC.ENTRADA_NOTAS_CRIAR, PC.ENTRADA_NOTAS_CONFIRMAR,
                # -- Tipos de Produto --
                PC.TIPOS_PRODUTO_ACESSAR, PC.TIPOS_PRODUTO_CRIAR, PC.TIPOS_PRODUTO_EDITAR,
                # -- Adicionais --
                PC.ADICIONAIS_ACESSAR, PC.ADICIONAIS_CRIAR, PC.ADICIONAIS_EDITAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
            # ================================================================
            # ENTREGADOR - Apenas modo entregador e entregas.
            # ================================================================
            "Entregador": [
                # -- Botoes Dashboard --
                PC.DASHBOARD_BTN_MODO_ENTREGADOR, PC.DASHBOARD_BTN_GERENCIAR_ENTREGAS,
                PC.DASHBOARD_BTN_TROCAR_SENHA, PC.DASHBOARD_BTN_SOBRE,
                # -- Entregas --
                PC.MODO_ENTREGADOR_ACESSAR, PC.ENTREGAS_ACESSAR, PC.ENTREGAS_GERENCIAR,
                # -- Conta --
                PC.TROCAR_SENHA,
            ],
        }
        chaves = chaves_map.get(nome_perfil, [])
        if chaves:
            PermissionDatabaseSetup._conceder_permissoes(db, perfil_id, chaves)

    @staticmethod
    def _migrar_perfil_antigo(db):
        """Migra usuarios do perfil antigo 'Operador de Caixa' ou 'Operador' para 'Operacional'."""
        try:
            for nome_antigo in ["Operador de Caixa", "Operador"]:
                rows = db.execute_query("SELECT id FROM perfis WHERE nome = %s", (nome_antigo,))
                if not rows:
                    continue
                antigo_id = rows[0]["id"]
                novo_id = PermissionDatabaseSetup._get_perfil_id(db, "Operacional")
                if novo_id <= 0:
                    continue
                db.execute_update("UPDATE usuarios SET perfil_id = %s WHERE perfil_id = %s", (novo_id, antigo_id))
                db.execute_update("UPDATE perfis SET ativo = 0 WHERE id = %s AND sistematico = 0", (antigo_id,))
        except Exception as e:
            print(f"Aviso migracao perfil antigo: {e}")

    @staticmethod
    def _migrar_usuarios_existentes(db):
        """Migra usuarios existentes para o sistema de perfis baseado no campo 'nivel'."""
        try:
            rows = db.execute_query(
                "SELECT COUNT(*) as cnt FROM usuarios WHERE perfil_id IS NULL OR perfil_id = 0")
            if not rows or rows[0].get("cnt", 0) == 0:
                return
            nivel_perfil_map = {
                "admin": "Administrador",
                "gerente": "Gerente",
                "operador": "Operacional",
                "caixa": "Caixa",
                "atendente": "Atendente",
                "garcom": "Garcom",
                "balcao": "Balcao",
                "vendedor": "Vendedor",
                "estoquista": "Estoquista",
                "entregador": "Entregador",
            }
            for nivel, nome_perfil in nivel_perfil_map.items():
                perfil_id = PermissionDatabaseSetup._get_perfil_id(db, nome_perfil)
                if perfil_id > 0:
                    db.execute_update(
                        "UPDATE usuarios SET perfil_id = %s WHERE nivel = %s AND (perfil_id IS NULL OR perfil_id = 0)",
                        (perfil_id, nivel))
            # Default para quem nao tem nivel definido
            op_id = PermissionDatabaseSetup._get_perfil_id(db, "Operacional")
            if op_id > 0:
                db.execute_update(
                    "UPDATE usuarios SET perfil_id = %s WHERE perfil_id IS NULL OR perfil_id = 0",
                    (op_id,))
        except Exception as e:
            print(f"Aviso migracao usuarios: {e}")

    @staticmethod
    def _migrar_tabela_permissoes_usuario_antiga(db):
        """Migra dados da tabela antiga permissoes_usuario para usuario_permissoes."""
        try:
            # Verificar se a tabela antiga existe
            rows = db.execute_query(
                "SELECT COUNT(*) as cnt FROM information_schema.TABLES "
                "WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'permissoes_usuario'")
            if not rows or rows[0].get("cnt", 0) == 0:
                return
            # Migrar dados
            antigas = db.execute_query("SELECT usuario_id, permissao FROM permissoes_usuario")
            for a in antigas:
                perm_rows = db.execute_query("SELECT id FROM permissoes WHERE chave = %s", (a["permissao"],))
                if perm_rows:
                    db.execute_update(
                        "INSERT IGNORE INTO usuario_permissoes (usuario_id, permissao_id, tipo) "
                        "VALUES (%s, %s, 'adicionar')",
                        (a["usuario_id"], perm_rows[0]["id"]))
        except Exception as e:
            print(f"Aviso migracao permissoes_usuario: {e}")


# ============================================================================
# GERENCIADOR DE BANCO DE DADOS
# ============================================================================

class DatabaseHelper:
    _instance = None
    _lock = threading.Lock()

    def __init__(self):
        self.connection = None
        self._conn_lock = threading.Lock()  # Lock para acesso thread-safe a conexao
        self.host = "localhost"
        self.database = "pdv_pro"
        self.username = "root"
        self.password = ""
        self.port = 3306
        self.initialized = False
        self.load_config()

    @classmethod
    def get_instance(cls):
        with cls._lock:
            if cls._instance is None:
                cls._instance = DatabaseHelper()
            return cls._instance

    def load_config(self):
        try:
            if os.path.exists(CONFIG_FILE):
                with open(CONFIG_FILE, "r") as f:
                    config = json.load(f)
                self.host = config.get("host", "localhost")
                self.database = config.get("database", "pdv_pro")
                self.username = config.get("username", "root")
                self.password = config.get("password", "")
                self.port = config.get("port", 3306)
        except json.JSONDecodeError as e:
            ErroAmigavel.log_erro(e, "load_config - arquivo de configuracao corrompido")
            print(f"[AVISO] Arquivo de configuracao corrompido. Usando configuracoes padrao.")
            # Tentar fazer backup do arquivo corrompido
            try:
                import shutil
                shutil.copy2(CONFIG_FILE, CONFIG_FILE + ".bak")
            except Exception:
                pass
        except Exception as e:
            ErroAmigavel.log_erro(e, "load_config")
            print(f"[AVISO] Erro ao carregar configuracoes: {e}")

    def save_config(self, host, database, username, password, port):
        self.host = host
        self.database = database
        self.username = username
        self.password = password
        self.port = port
        config = {
            "host": host,
            "database": database,
            "username": username,
            "password": password,
            "port": port
        }
        try:
            with open(CONFIG_FILE, "w") as f:
                json.dump(config, f, indent=2)
        except PermissionError as e:
            ErroAmigavel.log_erro(e, "save_config - sem permissao para salvar")
            print(f"[ERRO] Sem permissao para salvar configuracoes. Tente executar como Administrador.")
        except Exception as e:
            ErroAmigavel.log_erro(e, "save_config")
            print(f"[ERRO] Nao foi possivel salvar as configuracoes: {e}")
        self.close_connection()
        self.initialized = False

    def get_connection(self):
        """Retorna a conexao ativa ou cria uma nova.
        NOTA: Este metodo NAO adquire o _conn_lock; o lock deve ser
        adquirido pelo chamador (execute_query, execute_update, etc.)
        ou pelo bloco 'with self._conn_lock' em create_tables."""
        try:
            if self.connection and self.connection.is_connected():
                # Verificar se a conexao esta realmente funcional
                try:
                    self.connection.ping(reconnect=False)
                except Exception:
                    self.connection = None
                else:
                    return self.connection
        except Exception:
            self.connection = None

        for attempt in range(3):
            try:
                self.connection = mysql.connector.connect(
                    host=self.host,
                    database=self.database,
                    user=self.username,
                    password=self.password,
                    port=self.port,
                    charset='utf8mb4',
                    collation='utf8mb4_general_ci',
                    connect_timeout=10,
                    autocommit=True,
                    use_pure=True,
                    ssl_disabled=True,
                    auth_plugin='mysql_native_password'
                )
                return self.connection
            except MySQLError as e:
                if attempt < 2:
                    time.sleep(0.5 * (attempt + 1))
                else:
                    ErroAmigavel.log_erro(e, "DatabaseHelper.get_connection")
                    raise e
        return None

    def close_connection(self):
        try:
            if self.connection and self.connection.is_connected():
                self.connection.close()
        except Exception:
            pass
        self.connection = None

    def test_connection(self, host=None, database=None, username=None, password=None, port=None):
        """Testa a conexao com o banco de dados.
        Retorna True se conectou com sucesso, False caso contrario.
        Em caso de falha, registra o erro no log."""
        h = host or self.host
        db = database or self.database
        u = username or self.username
        p = password or self.password
        pt = port or self.port
        try:
            conn = mysql.connector.connect(
                host=h, database=db, user=u, password=p, port=pt,
                connect_timeout=8, charset='utf8mb4', use_pure=True,
                ssl_disabled=True, auth_plugin='mysql_native_password'
            )
            cursor = conn.cursor()
            cursor.execute("SELECT 1")
            cursor.fetchone()
            cursor.close()
            conn.close()
            return True
        except Exception as e:
            ErroAmigavel.log_erro(e, f"test_connection(host={h}, db={db}, port={pt})")
            return False

    def ensure_database_exists(self):
        """Garante que o banco de dados existe, criando se necessario.
        Retorna True se o banco existe ou foi criado, False em caso de erro."""
        try:
            conn = mysql.connector.connect(
                host=self.host, user=self.username,
                password=self.password, port=self.port,
                connect_timeout=8, use_pure=True,
                ssl_disabled=True, auth_plugin='mysql_native_password'
            )
            cursor = conn.cursor()
            cursor.execute(
                f"CREATE DATABASE IF NOT EXISTS `{self.database}` "
                f"CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci"
            )
            cursor.close()
            conn.close()
            _logger.info(f"Banco de dados '{self.database}' verificado/criado com sucesso")
            return True
        except MySQLError as e:
            ErroAmigavel.log_erro(e, "ensure_database_exists")
            if hasattr(e, 'errno'):
                if e.errno == 1045:
                    print(f"[ERRO] Usuario/senha do banco incorretos. Verifique as configuracoes.")
                elif e.errno == 2003:
                    print(f"[ERRO] MySQL nao esta rodando. Inicie o servico do MySQL.")
                else:
                    print(f"[ERRO] Nao foi possivel criar o banco de dados: {e}")
            else:
                print(f"[ERRO] Nao foi possivel criar o banco de dados: {e}")
            return False
        except Exception as e:
            ErroAmigavel.log_erro(e, "ensure_database_exists")
            print(f"[ERRO] Erro inesperado ao criar banco de dados: {e}")
            return False

    def get_table_definitions(self):
        tables = {}

        tables["empresa"] = """
            CREATE TABLE IF NOT EXISTS empresa (
                id INT AUTO_INCREMENT PRIMARY KEY,
                razao_social VARCHAR(200) NOT NULL,
                nome_fantasia VARCHAR(200) DEFAULT NULL,
                cnpj VARCHAR(20) DEFAULT NULL,
                ie VARCHAR(20) DEFAULT NULL,
                endereco VARCHAR(300) DEFAULT NULL,
                numero VARCHAR(20) DEFAULT NULL,
                bairro VARCHAR(100) DEFAULT NULL,
                cidade VARCHAR(100) DEFAULT NULL,
                uf VARCHAR(2) DEFAULT NULL,
                cep VARCHAR(10) DEFAULT NULL,
                telefone VARCHAR(20) DEFAULT NULL,
                email VARCHAR(100) DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["perfis"] = """
            CREATE TABLE IF NOT EXISTS perfis (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(100) NOT NULL,
                descricao VARCHAR(255) DEFAULT NULL,
                sistematico TINYINT(1) DEFAULT 0,
                ativo TINYINT(1) DEFAULT 1,
                personalizavel TINYINT(1) DEFAULT 0,
                criado_em DATETIME DEFAULT CURRENT_TIMESTAMP,
                atualizado_em DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["usuarios"] = """
            CREATE TABLE IF NOT EXISTS usuarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(100) NOT NULL,
                login VARCHAR(50) NOT NULL,
                senha VARCHAR(100) NOT NULL,
                nivel VARCHAR(20) DEFAULT 'operador',
                ativo TINYINT(1) DEFAULT 1,
                perfil_id INT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["permissoes"] = """
            CREATE TABLE IF NOT EXISTS permissoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                modulo VARCHAR(100) NOT NULL,
                acao VARCHAR(100) NOT NULL,
                chave VARCHAR(100) NOT NULL,
                descricao VARCHAR(255) DEFAULT NULL,
                criado_em DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["perfil_permissoes"] = """
            CREATE TABLE IF NOT EXISTS perfil_permissoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                perfil_id INT NOT NULL,
                permissao_id INT NOT NULL,
                criado_em DATETIME DEFAULT CURRENT_TIMESTAMP,
                UNIQUE KEY uk_perfil_permissao (perfil_id, permissao_id)
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["tipos_produto"] = """
            CREATE TABLE IF NOT EXISTS tipos_produto (
                id INT AUTO_INCREMENT PRIMARY KEY,
                descricao VARCHAR(100) NOT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["produtos"] = """
            CREATE TABLE IF NOT EXISTS produtos (
                id INT AUTO_INCREMENT PRIMARY KEY,
                codigo VARCHAR(50) DEFAULT NULL,
                descricao VARCHAR(200) NOT NULL,
                unidade VARCHAR(10) DEFAULT 'UN',
                tipo_produto_id INT DEFAULT NULL,
                preco_custo DECIMAL(10,2) DEFAULT 0,
                preco_venda DECIMAL(10,2) DEFAULT 0,
                estoque DECIMAL(10,3) DEFAULT 0,
                estoque_minimo DECIMAL(10,3) DEFAULT 0,
                codigo_barras VARCHAR(100) DEFAULT NULL,
                foto_base64 LONGTEXT DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["clientes"] = """
            CREATE TABLE IF NOT EXISTS clientes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(200) NOT NULL,
                cpf_cnpj VARCHAR(20) DEFAULT NULL,
                celular VARCHAR(20) DEFAULT NULL,
                endereco VARCHAR(300) DEFAULT NULL,
                numero VARCHAR(20) DEFAULT NULL,
                bairro VARCHAR(100) DEFAULT NULL,
                cidade VARCHAR(100) DEFAULT NULL,
                uf VARCHAR(2) DEFAULT NULL,
                cep VARCHAR(10) DEFAULT NULL,
                email VARCHAR(100) DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["vendedores"] = """
            CREATE TABLE IF NOT EXISTS vendedores (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(100) NOT NULL,
                celular VARCHAR(20) DEFAULT NULL,
                comissao DECIMAL(5,2) DEFAULT 0,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["entregadores"] = """
            CREATE TABLE IF NOT EXISTS entregadores (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(100) NOT NULL,
                celular VARCHAR(20) DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["formas_pagamento"] = """
            CREATE TABLE IF NOT EXISTS formas_pagamento (
                id INT AUTO_INCREMENT PRIMARY KEY,
                descricao VARCHAR(100) NOT NULL,
                tipo VARCHAR(20) DEFAULT 'dinheiro',
                permite_parcelamento TINYINT(1) DEFAULT 0,
                exige_cliente TINYINT(1) DEFAULT 0,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["observacoes_cupom"] = """
            CREATE TABLE IF NOT EXISTS observacoes_cupom (
                id INT AUTO_INCREMENT PRIMARY KEY,
                texto TEXT DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["caixa"] = """
            CREATE TABLE IF NOT EXISTS caixa (
                id INT AUTO_INCREMENT PRIMARY KEY,
                usuario_id INT DEFAULT NULL,
                data_abertura DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_fechamento DATETIME DEFAULT NULL,
                valor_abertura DECIMAL(10,2) DEFAULT 0,
                valor_fechamento DECIMAL(10,2) DEFAULT 0,
                status VARCHAR(20) DEFAULT 'aberto',
                observacao TEXT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["vendas"] = """
            CREATE TABLE IF NOT EXISTS vendas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                cliente_id INT DEFAULT NULL,
                cliente_nome VARCHAR(200) DEFAULT NULL,
                vendedor_id INT DEFAULT NULL,
                entregador_id INT DEFAULT NULL,
                caixa_id INT DEFAULT NULL,
                data_venda DATETIME DEFAULT CURRENT_TIMESTAMP,
                total_bruto DECIMAL(10,2) DEFAULT 0,
                desconto_tipo VARCHAR(20) DEFAULT NULL,
                desconto_valor DECIMAL(10,2) DEFAULT 0,
                acrescimo_tipo VARCHAR(20) DEFAULT NULL,
                acrescimo_valor DECIMAL(10,2) DEFAULT 0,
                total_liquido DECIMAL(10,2) DEFAULT 0,
                valor_recebido DECIMAL(10,2) DEFAULT 0,
                troco DECIMAL(10,2) DEFAULT 0,
                observacao TEXT DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'finalizada',
                celular_whatsapp VARCHAR(30) DEFAULT NULL,
                para_entrega TINYINT(1) DEFAULT 0,
                taxa_entrega DECIMAL(10,2) DEFAULT 0,
                bairro_entrega VARCHAR(150) DEFAULT NULL,
                endereco_entrega VARCHAR(500) DEFAULT NULL,
                uso_armario_id INT DEFAULT 0,
                status_entrega VARCHAR(20) DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_venda"] = """
            CREATE TABLE IF NOT EXISTS itens_venda (
                id INT AUTO_INCREMENT PRIMARY KEY,
                venda_id INT DEFAULT NULL,
                produto_id INT DEFAULT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                desconto DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["pagamentos_venda"] = """
            CREATE TABLE IF NOT EXISTS pagamentos_venda (
                id INT AUTO_INCREMENT PRIMARY KEY,
                venda_id INT DEFAULT NULL,
                forma_pagamento_id INT DEFAULT NULL,
                valor DECIMAL(10,2) DEFAULT 0,
                parcelas INT DEFAULT 1,
                bandeira VARCHAR(50) DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["vales_debito"] = """
            CREATE TABLE IF NOT EXISTS vales_debito (
                id INT AUTO_INCREMENT PRIMARY KEY,
                caixa_id INT DEFAULT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                valor DECIMAL(10,2) DEFAULT 0,
                data DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["sangrias"] = """
            CREATE TABLE IF NOT EXISTS sangrias (
                id INT AUTO_INCREMENT PRIMARY KEY,
                caixa_id INT DEFAULT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                valor DECIMAL(10,2) DEFAULT 0,
                admin_id INT DEFAULT NULL,
                admin_nome VARCHAR(100) DEFAULT NULL,
                operador_nome VARCHAR(100) DEFAULT NULL,
                data DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["entradas_caixa"] = """
            CREATE TABLE IF NOT EXISTS entradas_caixa (
                id INT AUTO_INCREMENT PRIMARY KEY,
                caixa_id INT DEFAULT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                valor DECIMAL(10,2) DEFAULT 0,
                origem VARCHAR(40) DEFAULT NULL,
                conta_receber_id INT DEFAULT NULL,
                operador_nome VARCHAR(100) DEFAULT NULL,
                data DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["comandas"] = """
            CREATE TABLE IF NOT EXISTS comandas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero INT NOT NULL,
                cliente_id INT DEFAULT 0,
                vendedor_id INT DEFAULT 0,
                caixa_id INT DEFAULT 0,
                data_abertura DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_fechamento DATETIME DEFAULT NULL,
                total_itens DECIMAL(10,2) DEFAULT 0,
                observacao TEXT DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'aberta'
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_comanda"] = """
            CREATE TABLE IF NOT EXISTS itens_comanda (
                id INT AUTO_INCREMENT PRIMARY KEY,
                comanda_id INT DEFAULT NULL,
                produto_id INT DEFAULT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0,
                observacao TEXT DEFAULT NULL,
                data_hora DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["licenca"] = """
            CREATE TABLE IF NOT EXISTS licenca (
                id INT AUTO_INCREMENT PRIMARY KEY,
                contra_chave VARCHAR(100) DEFAULT NULL,
                chave_ativacao VARCHAR(200) DEFAULT NULL,
                data_ativacao DATETIME DEFAULT NULL,
                data_expiracao DATETIME DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["rastreamento_entregador"] = """
            CREATE TABLE IF NOT EXISTS rastreamento_entregador (
                id INT AUTO_INCREMENT PRIMARY KEY,
                entregador_id INT NOT NULL,
                latitude DOUBLE DEFAULT 0,
                longitude DOUBLE DEFAULT 0,
                velocidade FLOAT DEFAULT 0,
                precisao FLOAT DEFAULT 0,
                data_hora DATETIME DEFAULT CURRENT_TIMESTAMP,
                ativo TINYINT(1) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["historico_localizacao"] = """
            CREATE TABLE IF NOT EXISTS historico_localizacao (
                id INT AUTO_INCREMENT PRIMARY KEY,
                entregador_id INT NOT NULL,
                latitude DOUBLE DEFAULT 0,
                longitude DOUBLE DEFAULT 0,
                velocidade FLOAT DEFAULT 0,
                precisao FLOAT DEFAULT 0,
                data_hora DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["contas_receber"] = """
            CREATE TABLE IF NOT EXISTS contas_receber (
                id INT AUTO_INCREMENT PRIMARY KEY,
                cliente_id INT NOT NULL,
                venda_id INT DEFAULT NULL,
                valor_original DECIMAL(10,2) DEFAULT 0,
                valor_pago DECIMAL(10,2) DEFAULT 0,
                valor_pendente DECIMAL(10,2) DEFAULT 0,
                data_venda DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_vencimento DATETIME DEFAULT NULL,
                data_pagamento DATETIME DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'pendente',
                observacao TEXT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["recebimentos_conta"] = """
            CREATE TABLE IF NOT EXISTS recebimentos_conta (
                id INT AUTO_INCREMENT PRIMARY KEY,
                conta_receber_id INT NOT NULL,
                valor DECIMAL(10,2) DEFAULT 0,
                data_recebimento DATETIME DEFAULT CURRENT_TIMESTAMP,
                forma_pagamento VARCHAR(50) DEFAULT NULL,
                observacao TEXT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["notas_entrada"] = """
            CREATE TABLE IF NOT EXISTS notas_entrada (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero_nota VARCHAR(100) DEFAULT NULL,
                fornecedor VARCHAR(200) NOT NULL,
                data_entrada DATETIME DEFAULT CURRENT_TIMESTAMP,
                total_nota DECIMAL(10,2) DEFAULT 0,
                observacao TEXT DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'pendente',
                usuario_id INT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_nota_entrada"] = """
            CREATE TABLE IF NOT EXISTS itens_nota_entrada (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nota_entrada_id INT NOT NULL,
                produto_id INT NOT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 0,
                custo_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["adicionais"] = """
            CREATE TABLE IF NOT EXISTS adicionais (
                id INT AUTO_INCREMENT PRIMARY KEY,
                descricao VARCHAR(200) NOT NULL,
                preco DECIMAL(10,2) DEFAULT 0,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["tipo_produto_adicionais"] = """
            CREATE TABLE IF NOT EXISTS tipo_produto_adicionais (
                id INT AUTO_INCREMENT PRIMARY KEY,
                tipo_produto_id INT NOT NULL,
                adicional_id INT NOT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_venda_adicionais"] = """
            CREATE TABLE IF NOT EXISTS itens_venda_adicionais (
                id INT AUTO_INCREMENT PRIMARY KEY,
                item_venda_id INT NOT NULL,
                adicional_id INT NOT NULL,
                descricao_adicional VARCHAR(200) DEFAULT NULL,
                preco DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["taxa_entrega_bairro"] = """
            CREATE TABLE IF NOT EXISTS taxa_entrega_bairro (
                id INT AUTO_INCREMENT PRIMARY KEY,
                bairro VARCHAR(150) NOT NULL,
                taxa DECIMAL(10,2) DEFAULT 0,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["garcons"] = """
            CREATE TABLE IF NOT EXISTS garcons (
                id INT AUTO_INCREMENT PRIMARY KEY,
                nome VARCHAR(150) NOT NULL,
                celular VARCHAR(30) DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["mesas"] = """
            CREATE TABLE IF NOT EXISTS mesas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero INT NOT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                capacidade INT DEFAULT 4,
                ativa TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["ocupacao_mesa"] = """
            CREATE TABLE IF NOT EXISTS ocupacao_mesa (
                id INT AUTO_INCREMENT PRIMARY KEY,
                mesa_id INT NOT NULL,
                garcom_id INT DEFAULT 0,
                qtd_pessoas INT DEFAULT 0,
                status VARCHAR(20) DEFAULT 'livre',
                data_abertura DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_fechamento DATETIME DEFAULT NULL,
                reservado_por_usuario_id INT DEFAULT 0,
                reservado_por_usuario_nome VARCHAR(150) DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_mesa"] = """
            CREATE TABLE IF NOT EXISTS itens_mesa (
                id INT AUTO_INCREMENT PRIMARY KEY,
                ocupacao_id INT NOT NULL,
                produto_id INT NOT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0,
                adicionais_descricao VARCHAR(500) DEFAULT NULL,
                adicionais_total DECIMAL(10,2) DEFAULT 0,
                impresso TINYINT(1) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_mesa_adicionais"] = """
            CREATE TABLE IF NOT EXISTS itens_mesa_adicionais (
                id INT AUTO_INCREMENT PRIMARY KEY,
                item_mesa_id INT NOT NULL,
                adicional_id INT NOT NULL,
                descricao_adicional VARCHAR(200) DEFAULT NULL,
                preco DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["armarios_sauna"] = """
            CREATE TABLE IF NOT EXISTS armarios_sauna (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero INT NOT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                localizacao VARCHAR(100) DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["uso_armario_sauna"] = """
            CREATE TABLE IF NOT EXISTS uso_armario_sauna (
                id INT AUTO_INCREMENT PRIMARY KEY,
                armario_id INT NOT NULL,
                cliente_nome VARCHAR(200) DEFAULT NULL,
                observacao VARCHAR(500) DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'ocupado',
                data_entrada DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_saida DATETIME DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_armario_sauna"] = """
            CREATE TABLE IF NOT EXISTS itens_armario_sauna (
                id INT AUTO_INCREMENT PRIMARY KEY,
                uso_armario_id INT NOT NULL,
                produto_id INT NOT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0,
                adicionais_descricao VARCHAR(500) DEFAULT NULL,
                adicionais_total DECIMAL(10,2) DEFAULT 0,
                impresso TINYINT(1) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_armario_sauna_adicionais"] = """
            CREATE TABLE IF NOT EXISTS itens_armario_sauna_adicionais (
                id INT AUTO_INCREMENT PRIMARY KEY,
                item_armario_id INT NOT NULL,
                adicional_id INT NOT NULL,
                descricao_adicional VARCHAR(200) DEFAULT NULL,
                preco DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["cliente_bairro_whatsapp"] = """
            CREATE TABLE IF NOT EXISTS cliente_bairro_whatsapp (
                id INT AUTO_INCREMENT PRIMARY KEY,
                celular_whatsapp VARCHAR(30) NOT NULL,
                bairro_id INT NOT NULL,
                data_atualizacao DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["chamados"] = """
            CREATE TABLE IF NOT EXISTS chamados (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero_chamado INT NOT NULL,
                cliente_nome VARCHAR(200) NOT NULL,
                comanda_numero INT DEFAULT 0,
                tipo VARCHAR(50) DEFAULT 'comanda',
                descricao TEXT,
                status VARCHAR(30) DEFAULT 'aguardando',
                prioridade VARCHAR(20) DEFAULT 'normal',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_chamada DATETIME DEFAULT NULL,
                data_atendido DATETIME DEFAULT NULL,
                vezes_chamado INT DEFAULT 0,
                observacao TEXT,
                atendente VARCHAR(100) DEFAULT ''
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["pedidos_web"] = """
            CREATE TABLE IF NOT EXISTS pedidos_web (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero_pedido INT NOT NULL,
                mesa_numero INT NOT NULL,
                cliente_nome VARCHAR(200) NOT NULL,
                observacao TEXT,
                total DECIMAL(10,2) DEFAULT 0,
                status VARCHAR(30) DEFAULT 'pendente',
                data_criacao DATETIME DEFAULT CURRENT_TIMESTAMP,
                data_preparo DATETIME DEFAULT NULL,
                data_pronto DATETIME DEFAULT NULL,
                data_entregue DATETIME DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_pedido_web"] = """
            CREATE TABLE IF NOT EXISTS itens_pedido_web (
                id INT AUTO_INCREMENT PRIMARY KEY,
                pedido_id INT NOT NULL,
                produto_id INT NOT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0,
                observacao TEXT DEFAULT NULL
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["entrada_notas"] = """
            CREATE TABLE IF NOT EXISTS entrada_notas (
                id INT AUTO_INCREMENT PRIMARY KEY,
                produto_id INT NOT NULL,
                quantidade DECIMAL(10,3) DEFAULT 0,
                custo_unitario DECIMAL(10,2) DEFAULT 0,
                usuario_id INT DEFAULT NULL,
                data_entrada DATETIME DEFAULT CURRENT_TIMESTAMP
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["usuario_permissoes"] = """
            CREATE TABLE IF NOT EXISTS usuario_permissoes (
                id INT AUTO_INCREMENT PRIMARY KEY,
                usuario_id INT NOT NULL,
                permissao_id INT NOT NULL,
                tipo ENUM('adicionar', 'remover') NOT NULL DEFAULT 'adicionar',
                criado_em DATETIME DEFAULT CURRENT_TIMESTAMP,
                criado_por INT DEFAULT NULL,
                UNIQUE KEY uk_usuario_permissao (usuario_id, permissao_id)
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["armarios"] = """
            CREATE TABLE IF NOT EXISTS armarios (
                id INT AUTO_INCREMENT PRIMARY KEY,
                numero INT NOT NULL,
                descricao VARCHAR(200) DEFAULT NULL,
                status VARCHAR(20) DEFAULT 'livre',
                data_abertura DATETIME DEFAULT NULL,
                ativo TINYINT(1) DEFAULT 1
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        tables["itens_armario"] = """
            CREATE TABLE IF NOT EXISTS itens_armario (
                id INT AUTO_INCREMENT PRIMARY KEY,
                armario_id INT NOT NULL,
                produto_id INT NOT NULL,
                descricao_produto VARCHAR(200) DEFAULT NULL,
                quantidade DECIMAL(10,3) DEFAULT 1,
                preco_unitario DECIMAL(10,2) DEFAULT 0,
                total DECIMAL(10,2) DEFAULT 0
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
        """

        return tables

    def _parse_columns_from_create_sql(self, create_sql):
        """Extrai nome e definicao das colunas a partir de um CREATE TABLE SQL.
        Retorna dict {nome_coluna: definicao_completa}."""
        columns = {}
        try:
            # Encontrar conteudo entre os parenteses do CREATE TABLE
            match = re.search(r'\((.+)\)\s*ENGINE', create_sql, re.DOTALL)
            if not match:
                return columns
            body = match.group(1)
            # Separar por linhas e processar cada uma
            for line in body.split('\n'):
                line = line.strip().rstrip(',')
                if not line:
                    continue
                # Ignorar linhas de constraint/index
                upper = line.upper()
                if any(upper.startswith(kw) for kw in [
                    'PRIMARY', 'UNIQUE', 'KEY', 'INDEX', 'CONSTRAINT',
                    'FOREIGN', 'CHECK'
                ]):
                    continue
                # Extrair nome da coluna (primeira palavra)
                parts = line.split()
                if len(parts) >= 2:
                    col_name = parts[0].strip('`').lower()
                    # Ignorar se parece keyword
                    if col_name.upper() in ('PRIMARY', 'UNIQUE', 'KEY', 'INDEX',
                                             'CONSTRAINT', 'FOREIGN', 'CHECK', 'ENGINE'):
                        continue
                    col_def = ' '.join(parts[1:])
                    # Remover trailing comma
                    col_def = col_def.rstrip(',')
                    columns[col_name] = col_def
        except Exception as e:
            print(f"[AUTO-MIGRACAO] Erro ao parsear SQL: {e}")
        return columns

    def auto_migrate_database(self):
        """Verificacao e auto-migracao do banco de dados.
        Executada ANTES de qualquer operacao do sistema.
        Verifica se todas as tabelas e colunas necessarias existem
        e cria/adiciona as faltantes automaticamente.
        Isso permite restaurar bancos de dados antigos e o sistema
        atualiza a estrutura automaticamente antes de iniciar."""
        print("[AUTO-MIGRACAO] Iniciando verificacao e atualizacao do banco de dados...")
        _logger.info("Auto-migracao: iniciando verificacao do banco de dados")

        try:
            # 1. Garantir que o banco de dados existe
            if not self.ensure_database_exists():
                _logger.error("Auto-migracao: nao foi possivel garantir a existencia do banco")
                print("[AUTO-MIGRACAO] ERRO: Nao foi possivel acessar/criar o banco de dados")
                return False

            # 2. Obter definicoes de todas as tabelas necessarias
            tables = self.get_table_definitions()

            # 3. Obter lista de tabelas existentes no banco
            with self._conn_lock:
                conn = self.get_connection()
                cursor = conn.cursor(dictionary=True)

                cursor.execute("SHOW TABLES")
                existing_tables_rows = cursor.fetchall()
                existing_tables = set()
                if existing_tables_rows:
                    key = list(existing_tables_rows[0].keys())[0]
                    existing_tables = {r[key].lower() for r in existing_tables_rows}

                tabelas_criadas = 0
                colunas_adicionadas = 0
                erros = 0
                
                # Log de diagnostico inicial
                print(f"[AUTO-MIGRACAO] Tabelas encontradas: {len(existing_tables)}")

                # 4. Para cada tabela necessaria, verificar e criar/atualizar
                for table_name, create_sql in tables.items():
                    try:
                        if table_name.lower() not in existing_tables:
                            # Tabela nao existe - criar
                            cursor.execute(create_sql)
                            tabelas_criadas += 1
                            print(f"[AUTO-MIGRACAO] Tabela '{table_name}' criada com sucesso")
                            _logger.info(f"Auto-migracao: tabela '{table_name}' criada")
                        else:
                            # Tabela existe - verificar colunas faltantes
                            expected_columns = self._parse_columns_from_create_sql(create_sql)
                            if not expected_columns:
                                continue

                            # Obter colunas existentes na tabela
                            cursor.execute(f"SHOW COLUMNS FROM `{table_name}`")
                            existing_cols_rows = cursor.fetchall()
                            existing_cols = {r['Field'].lower() for r in existing_cols_rows}

                            # Adicionar colunas faltantes ou corrigir 'id' sem AUTO_INCREMENT
                            for col_name, col_def in expected_columns.items():
                                col_name_lower = col_name.lower()
                                if col_name_lower not in existing_cols:
                                    try:
                                        alter_sql = f"ALTER TABLE `{table_name}` ADD COLUMN `{col_name}` {col_def}"
                                        cursor.execute(alter_sql)
                                        colunas_adicionadas += 1
                                        print(f"[AUTO-MIGRACAO] Coluna '{col_name}' adicionada na tabela '{table_name}'")
                                        _logger.info(f"Auto-migracao: coluna '{col_name}' adicionada em '{table_name}'")
                                    except Exception as e_col:
                                        print(f"[AUTO-MIGRACAO] Aviso ao adicionar coluna '{col_name}' em '{table_name}': {e_col}")
                                        _logger.warning(f"Auto-migracao: aviso coluna '{col_name}' em '{table_name}': {e_col}")
                                elif col_name_lower == 'id':
                                    # Verificar se o 'id' existente tem AUTO_INCREMENT
                                    has_auto_inc = False
                                    for row in existing_cols_rows:
                                        if row['Field'].lower() == 'id' and 'auto_increment' in row.get('Extra', '').lower():
                                            has_auto_inc = True
                                            break
                                    
                                    if not has_auto_inc:
                                        try:
                                            print(f"[AUTO-MIGRACAO] Corrigindo 'id' sem AUTO_INCREMENT na tabela '{table_name}'...")
                                            # Limpar a definicao da coluna para o MODIFY (remover PRIMARY KEY se ja existir)
                                            clean_def = col_def.upper().replace("PRIMARY KEY", "").strip()
                                            # Garantir que tenha AUTO_INCREMENT na definicao
                                            if "AUTO_INCREMENT" not in clean_def:
                                                clean_def += " AUTO_INCREMENT"
                                            
                                            alter_sql = f"ALTER TABLE `{table_name}` MODIFY COLUMN `{col_name}` {clean_def}"
                                            cursor.execute(alter_sql)
                                            print(f"[AUTO-MIGRACAO] Coluna 'id' corrigida com sucesso em '{table_name}'")
                                            _logger.info(f"Auto-migracao: coluna 'id' corrigida (AUTO_INCREMENT) em '{table_name}'")
                                        except Exception as e_id:
                                            # Se falhar o MODIFY, tenta uma abordagem mais drastica: adicionar PK se nao tiver
                                            try:
                                                cursor.execute(f"ALTER TABLE `{table_name}` ADD PRIMARY KEY (`{col_name}`)")
                                                cursor.execute(f"ALTER TABLE `{table_name}` MODIFY COLUMN `{col_name}` {clean_def}")
                                                print(f"[AUTO-MIGRACAO] Coluna 'id' corrigida (tentativa 2) em '{table_name}'")
                                            except Exception:
                                                print(f"[AUTO-MIGRACAO] Erro critico ao corrigir 'id' em '{table_name}': {e_id}")
                                                _logger.error(f"Auto-migracao: erro ao corrigir 'id' em '{table_name}': {e_id}")
                    except Exception as e_table:
                        erros += 1
                        print(f"[AUTO-MIGRACAO] Erro ao processar tabela '{table_name}': {e_table}")
                        _logger.error(f"Auto-migracao: erro tabela '{table_name}': {e_table}")

                # 5. Executar migracoes especificas (ALTER TABLE de alinhamento)
                migracoes_especificas = [
                    # Adicionar coluna data_abertura em armarios
                    ("ALTER TABLE armarios ADD COLUMN data_abertura DATETIME DEFAULT NULL",
                     "armarios.data_abertura"),
                    # Adicionar coluna uso_armario_id em vendas
                    ("ALTER TABLE vendas ADD COLUMN uso_armario_id INT DEFAULT 0",
                     "vendas.uso_armario_id"),
                    # Adicionar coluna cliente_nome em vendas
                    ("ALTER TABLE vendas ADD COLUMN cliente_nome VARCHAR(200) DEFAULT NULL",
                     "vendas.cliente_nome"),
                    # Adicionar coluna personalizavel em perfis
                    ("ALTER TABLE perfis ADD COLUMN personalizavel TINYINT(1) DEFAULT 0",
                     "perfis.personalizavel"),
                    # Adicionar coluna perfil_id em usuarios
                    ("ALTER TABLE usuarios ADD COLUMN perfil_id INT DEFAULT NULL",
                     "usuarios.perfil_id"),
                ]

                for mig_sql, mig_desc in migracoes_especificas:
                    try:
                        cursor.execute(mig_sql)
                        print(f"[AUTO-MIGRACAO] Migracao aplicada: {mig_desc}")
                    except Exception:
                        pass  # Coluna/tabela ja existe ou tabela nao existe ainda

                # 6. Migracoes de remocao/modificacao (seguras com try/except)
                migracoes_remocao = [
                    # Remover coluna mesa_id de itens_mesa (alinhamento Android)
                    ("ALTER TABLE itens_mesa DROP COLUMN mesa_id",
                     "remover itens_mesa.mesa_id"),
                    # Garantir ocupacao_id NOT NULL em itens_mesa
                    ("ALTER TABLE itens_mesa MODIFY COLUMN ocupacao_id INT NOT NULL",
                     "itens_mesa.ocupacao_id NOT NULL"),
                    # Remover coluna status de mesas (status fica em ocupacao_mesa)
                    ("ALTER TABLE mesas DROP COLUMN status",
                     "remover mesas.status"),
                    # Remover UNIQUE de permissoes.chave
                    ("ALTER TABLE permissoes DROP INDEX chave",
                     "remover UNIQUE permissoes.chave"),
                ]

                for mig_sql, mig_desc in migracoes_remocao:
                    try:
                        cursor.execute(mig_sql)
                        print(f"[AUTO-MIGRACAO] Migracao aplicada: {mig_desc}")
                    except Exception:
                        pass  # Ja aplicada ou nao aplicavel

                # 7. Migracao corretiva de dados
                try:
                    cursor.execute(
                        "UPDATE vendas v "
                        "LEFT JOIN uso_armario_sauna u ON u.id = v.uso_armario_id "
                        "SET v.cliente_nome = u.cliente_nome "
                        "WHERE (v.cliente_nome IS NULL OR TRIM(v.cliente_nome) = '') "
                        "AND v.uso_armario_id > 0 "
                        "AND u.cliente_nome IS NOT NULL AND TRIM(u.cliente_nome) <> ''"
                    )
                except Exception:
                    pass  # Tabelas podem nao existir ainda

                cursor.close()

            # Resumo
            resumo = (f"[AUTO-MIGRACAO] Concluido: "
                      f"{tabelas_criadas} tabela(s) criada(s), "
                      f"{colunas_adicionadas} coluna(s) adicionada(s), "
                      f"{erros} erro(s)")
            print(resumo)
            _logger.info(resumo)

            if erros == 0:
                print("[AUTO-MIGRACAO] Banco de dados verificado e atualizado com sucesso!")
            else:
                print(f"[AUTO-MIGRACAO] Banco atualizado com {erros} aviso(s). Verifique o log.")

            return True

        except MySQLError as e:
            ErroAmigavel.log_erro(e, "auto_migrate_database_mysql")
            _logger.error(f"Auto-migracao: erro MySQL: {e}")
            print(f"[AUTO-MIGRACAO] Erro MySQL: {e}")
            return False
        except Exception as e:
            ErroAmigavel.log_erro(e, "auto_migrate_database_geral")
            _logger.error(f"Auto-migracao: erro geral: {e}")
            print(f"[AUTO-MIGRACAO] Erro geral: {e}")
            return False

    def create_tables(self):
        """Cria todas as tabelas do sistema no banco de dados.
        Inclui tratamento individual por tabela para nao interromper
        a criacao em caso de erro em uma tabela especifica.
        NOTA: Agora chama auto_migrate_database() primeiro para garantir
        que bancos antigos/restaurados sejam atualizados automaticamente."""
        if self.initialized:
            return
        try:
            # ============================================================
            # AUTO-MIGRACAO: Verificar e atualizar banco ANTES de tudo
            # Isso garante que bancos antigos restaurados sejam
            # atualizados automaticamente com tabelas/colunas faltantes
            # ============================================================
            self.auto_migrate_database()

            # Executar CREATE TABLE IF NOT EXISTS para garantia extra
            tabelas_ok = 0
            tabelas_erro = 0
            with self._conn_lock:
                conn = self.get_connection()
                cursor = conn.cursor()
                tables = self.get_table_definitions()
                for table_name, create_sql in tables.items():
                    try:
                        cursor.execute(create_sql)
                        tabelas_ok += 1
                    except Exception as e:
                        tabelas_erro += 1
                        ErroAmigavel.log_erro(e, f"create_table_{table_name}")
                        _logger.warning(f"Aviso ao criar tabela '{table_name}': {e}")
                # As migracoes abaixo sao mantidas por seguranca redundante
                # (ja executadas pelo auto_migrate_database acima)
                # Migracao: adicionar coluna data_abertura em armarios existentes
                try:
                    cursor.execute(
                        "ALTER TABLE armarios ADD COLUMN data_abertura DATETIME DEFAULT NULL"
                    )
                except Exception:
                    pass  # Coluna ja existe
                # Migracao: remover coluna mesa_id de itens_mesa (alinhamento com Android)
                try:
                    cursor.execute(
                        "ALTER TABLE itens_mesa DROP COLUMN mesa_id"
                    )
                except Exception:
                    pass  # Coluna nao existe
                # Migracao: garantir que ocupacao_id seja NOT NULL em itens_mesa (alinhamento com Android)
                try:
                    cursor.execute(
                        "ALTER TABLE itens_mesa MODIFY COLUMN ocupacao_id INT NOT NULL"
                    )
                except Exception:
                    pass  # Coluna ja esta correta
                # Migracao: remover coluna status de mesas (alinhamento com Android - status fica em ocupacao_mesa)
                try:
                    cursor.execute(
                        "ALTER TABLE mesas DROP COLUMN status"
                    )
                except Exception:
                    pass  # Coluna nao existe
                # Migracao: adicionar coluna uso_armario_id em vendas (alinhamento com Android)
                try:
                    cursor.execute(
                        "ALTER TABLE vendas ADD COLUMN uso_armario_id INT DEFAULT 0"
                    )
                except Exception:
                    pass  # Coluna ja existe
                # Migracao: adicionar coluna cliente_nome em vendas para preservar nome avulso do cliente
                try:
                    cursor.execute(
                        "ALTER TABLE vendas ADD COLUMN cliente_nome VARCHAR(200) DEFAULT NULL"
                    )
                except Exception:
                    pass  # Coluna ja existe
                # Migracao: colunas do MODULO DE ENTREGA em vendas (garantia para bancos antigos)
                for _alter_entrega in (
                    "ALTER TABLE vendas ADD COLUMN para_entrega TINYINT(1) DEFAULT 0",
                    "ALTER TABLE vendas ADD COLUMN taxa_entrega DECIMAL(10,2) DEFAULT 0",
                    "ALTER TABLE vendas ADD COLUMN bairro_entrega VARCHAR(150) DEFAULT NULL",
                    "ALTER TABLE vendas ADD COLUMN endereco_entrega VARCHAR(500) DEFAULT NULL",
                    "ALTER TABLE vendas ADD COLUMN celular_whatsapp VARCHAR(30) DEFAULT NULL",
                    "ALTER TABLE vendas ADD COLUMN status_entrega VARCHAR(20) DEFAULT NULL",
                ):
                    try:
                        cursor.execute(_alter_entrega)
                    except Exception:
                        pass  # Coluna ja existe
                # Migracao corretiva: preencher cliente_nome em vendas de armario ja vinculadas
                try:
                    cursor.execute(
                        "UPDATE vendas v "
                        "LEFT JOIN uso_armario_sauna u ON u.id = v.uso_armario_id "
                        "SET v.cliente_nome = u.cliente_nome "
                        "WHERE (v.cliente_nome IS NULL OR TRIM(v.cliente_nome) = '') "
                        "AND v.uso_armario_id > 0 "
                        "AND u.cliente_nome IS NOT NULL AND TRIM(u.cliente_nome) <> ''"
                    )
                except Exception:
                    pass

                # Migracao: remover UNIQUE de permissoes.chave (alinhamento com Android)
                try:
                    cursor.execute(
                        "ALTER TABLE permissoes DROP INDEX chave"
                    )
                except Exception:
                    pass  # Index nao existe
                cursor.close()
            _logger.info(f"Tabelas criadas: {tabelas_ok} OK, {tabelas_erro} com aviso")
            # Inserir dados padrao (usa execute_query/execute_update que ja tem lock)
            self.insert_default_data()
            # Sincronizar armarios cadastrados pelo app APK
            self.sync_armarios_from_apk()
            # Setup do sistema de permissoes (alinhado com Android)
            PermissionDatabaseSetup.setup(self)
            self.initialized = True
        except MySQLError as e:
            ErroAmigavel.log_erro(e, "create_tables_mysql")
            _logger.error(f"Erro MySQL ao criar tabelas: {e}")
        except Exception as e:
            ErroAmigavel.log_erro(e, "create_tables_geral")
            _logger.error(f"Erro geral ao criar tabelas: {e}")

    def insert_default_data(self):
        """Insere dados padrao usando execute_query/execute_update (thread-safe)."""
        try:
            rows = self.execute_query("SELECT COUNT(*) as cnt FROM usuarios")
            count = rows[0].get("cnt", 0) if rows else 0
            if count == 0:
                self.execute_update(
                    "INSERT INTO usuarios (nome, login, senha, nivel, ativo) "
                    "VALUES ('Administrador', 'admin', 'admin', 'admin', 1)"
                )

            rows = self.execute_query("SELECT COUNT(*) as cnt FROM formas_pagamento")
            count = rows[0].get("cnt", 0) if rows else 0
            if count == 0:
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('Dinheiro', 'dinheiro', 0, 1)"
                )
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('Cartao de Credito', 'credito', 1, 1)"
                )
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('Cartao de Debito', 'debito', 0, 1)"
                )
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('PIX', 'pix', 0, 1)"
                )
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('Contas a Receber', 'conta_receber', 0, 1)"
                )

            # Garantir forma de pagamento Contas a Receber
            rows = self.execute_query("SELECT COUNT(*) as cnt FROM formas_pagamento WHERE tipo = 'conta_receber'")
            count = rows[0].get("cnt", 0) if rows else 0
            if count == 0:
                self.execute_update(
                    "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, ativo) "
                    "VALUES ('Contas a Receber', 'conta_receber', 0, 1)"
                )

            # === Cardapio do restaurante DELICIAS FOOD (itens cadastrados SEM preco) ===
            try:
                self.seed_cardapio_delicias_food()
            except Exception as _e_card:
                print(f"Aviso ao semear cardapio Delicias Food: {_e_card}")

            # Perfis padrao agora sao criados pelo PermissionDatabaseSetup.setup()
        except Exception as e:
            print(f"Aviso ao inserir dados padrao: {e}")

    # ========================================================================
    # CARDAPIO DELICIAS FOOD  -  cadastro automatico do cardapio (sem precos)
    # ========================================================================
    # Nomes das categorias (tipos_produto). Usados tambem pelo assistente
    # "MONTE SEU PRATO". Mantidos SEM acento para evitar qualquer divergencia
    # entre o cadastro e as consultas do assistente.
    CAT_MONTE_PRATO = "MONTE SEU PRATO"
    CAT_PROTEINAS = "Proteinas"
    CAT_ACOMPANHAMENTOS = "Acompanhamentos"
    CAT_SUCOS = "Sucos"
    CAT_REFRI_200 = "Refrigerantes 200ml"
    CAT_REFRI_350 = "Refrigerantes 350ml (Lata)"
    CAT_AGUAS = "Aguas"
    CAT_SOBREMESAS = "Sobremesas"

    def seed_cardapio_delicias_food(self):
        """Cadastra o cardapio do restaurante DELICIAS FOOD.

        - Todos os itens sao cadastrados SEM preco (preco_venda = 0). O preco
          deve ser definido depois pelo cliente no cadastro de produtos.
        - Idempotente: so insere o que ainda nao existe (pode rodar a cada
          inicializacao sem duplicar).
        - Se a categoria "MONTE SEU PRATO" ja existir, assume-se que o
          cardapio ja foi cadastrado e o seed e ignorado (evita overhead).
        """
        # Se ja existe a categoria principal, considera o cardapio cadastrado.
        ja_existe = self.execute_query(
            "SELECT id FROM tipos_produto WHERE descricao = %s LIMIT 1",
            (self.CAT_MONTE_PRATO,)
        )
        if ja_existe:
            return

        def _get_or_create_tipo(descricao):
            rows = self.execute_query(
                "SELECT id FROM tipos_produto WHERE descricao = %s LIMIT 1",
                (descricao,)
            )
            if rows:
                return rows[0]["id"]
            return self.execute_update(
                "INSERT INTO tipos_produto (descricao, ativo) VALUES (%s, 1)",
                (descricao,)
            )

        def _ensure_produto(descricao, tipo_id, codigo=None):
            rows = self.execute_query(
                "SELECT id FROM produtos WHERE descricao = %s AND tipo_produto_id = %s LIMIT 1",
                (descricao, tipo_id)
            )
            if rows:
                return rows[0]["id"]
            # preco_custo / preco_venda = 0 (cliente define depois). Estoque alto
            # para itens de restaurante (feitos na hora) nao bloquearem a venda.
            return self.execute_update(
                "INSERT INTO produtos (codigo, descricao, unidade, tipo_produto_id, "
                "preco_custo, preco_venda, estoque, estoque_minimo, ativo) "
                "VALUES (%s, %s, 'UN', %s, 0, 0, 99999, 0, 1)",
                (codigo, descricao, tipo_id)
            )

        # ---- MONTE SEU PRATO (tamanhos) ----
        # O codigo guarda o numero de proteinas que o tamanho permite escolher
        # (ultimo digito do codigo). Ex.: MSP-G2 = tamanho G com 2 proteinas.
        tipo_monte = _get_or_create_tipo(self.CAT_MONTE_PRATO)
        for desc, cod in [
            ("Tamanho P - 1 Proteina", "MSP-P1"),
            ("Tamanho P - 2 Proteinas", "MSP-P2"),
            ("Tamanho G - 1 Proteina", "MSP-G1"),
            ("Tamanho G - 2 Proteinas", "MSP-G2"),
        ]:
            _ensure_produto(desc, tipo_monte, cod)

        # ---- PROTEINAS ----
        tipo_prot = _get_or_create_tipo(self.CAT_PROTEINAS)
        proteinas = [
            "Lasanha bolonhesa", "Lasanha de frango", "Panqueca de carne do sol",
            "Panqueca de carne", "Panqueca de frango", "Escondidinho de frango",
            "Escondidinho de carne", "Escondidinho de carne do sol", "Peixada",
            "Peixe tilapia frita em postas", "Frango assado no forno",
            "Porco assado na brasa", "Porco ao molho", "Cozido com pirao",
            "Mao de vaca com pirao", "Frango a milanesa", "Frango parmegiana",
            "Filezinho grelhado", "Carne trinchada", "Porco trinchado",
            "Porco frito", "Porco frito no molho barbecue", "File de peixe frito",
            "Creme de galinha", "Vatapa de frango", "Vatapa de camarao",
            "Fricasse de frango", "Maninha assada", "Picanha assada",
            "Picanha de porco assada", "Costelinha suina assada",
            "Calabresa acebolada", "Escondidinho de calabresa",
            "Linguica Toscana assada", "Linguica Toscana no forno",
            "Linguica Toscana no molho barbecue", "Almondega", "Omelete de frango",
            "Omelete de carne", "Omelete de carne do sol", "Ovo cozido",
            "Ovo frito", "Feijoada", "Bobo de Camarao",
        ]
        for nome in proteinas:
            _ensure_produto(nome, tipo_prot)

        # ---- ACOMPANHAMENTOS (o codigo guarda o grupo) ----
        tipo_acomp = _get_or_create_tipo(self.CAT_ACOMPANHAMENTOS)
        acompanhamentos = [
            # (descricao, grupo)
            ("Arroz branco", "ARROZ"), ("Arroz temperado", "ARROZ"),
            ("Baiao de dois", "ARROZ"), ("Macarrao espaguete", "ARROZ"),
            ("Macarrao ninho", "ARROZ"),
            ("Feijao carioca", "FEIJAO"), ("Feijao de corda", "FEIJAO"),
            ("Feijao preto", "FEIJAO"),
            ("Salada verde", "SALADA"), ("Salada de legumes", "SALADA"),
            ("Salada de maionese", "SALADA"), ("Salada acelga com manga", "SALADA"),
            ("Salada tropical", "SALADA"), ("Salada de repolho refogado", "SALADA"),
            ("Salada de repolho cremoso", "SALADA"), ("Salada de repolho crua", "SALADA"),
            ("Beterraba", "SALADA"), ("Batata doce", "SALADA"),
            ("Abobora refogado", "SALADA"),
        ]
        for nome, grupo in acompanhamentos:
            _ensure_produto(nome, tipo_acomp, f"ACOMP-{grupo}")

        # ---- SUCOS (todos 400ml) ----
        tipo_sucos = _get_or_create_tipo(self.CAT_SUCOS)
        sucos = [
            "Suco de Caja 400ml", "Suco de Caju 400ml", "Suco de Manga 400ml",
            "Suco de Acerola 400ml", "Suco de Abacaxi 400ml", "Suco de Maracuja 400ml",
            "Suco de Goiaba 400ml", "Suco de Graviola 400ml", "Suco de Siriguela 400ml",
            "Suco de Tamarindo 400ml", "Suco de Sapoti 400ml",
            "Suco de Abacaxi com Hortela 400ml",
        ]
        for nome in sucos:
            _ensure_produto(nome, tipo_sucos)

        # ---- REFRIGERANTES 200ml ----
        tipo_r200 = _get_or_create_tipo(self.CAT_REFRI_200)
        refri_200 = [
            "Coca-Cola 200ml", "Coca-Cola Zero 200ml", "Sao Geraldo 200ml",
            "Fanta Uva 200ml", "Fanta Laranja 200ml", "Guarana 200ml",
        ]
        for nome in refri_200:
            _ensure_produto(nome, tipo_r200)

        # ---- REFRIGERANTES 350ml (LATA) ----
        tipo_r350 = _get_or_create_tipo(self.CAT_REFRI_350)
        refri_350 = [
            "Coca-Cola Lata 350ml", "Coca-Cola Lata Zero 350ml", "Fanta Uva Lata 350ml",
            "Fanta Laranja Lata 350ml", "Sao Geraldo Zero 350ml", "Sao Geraldo 350ml",
            "Guarana Lata 350ml",
        ]
        for nome in refri_350:
            _ensure_produto(nome, tipo_r350)

        # ---- AGUAS ----
        tipo_aguas = _get_or_create_tipo(self.CAT_AGUAS)
        for nome in ["Agua com gas 500ml", "Agua sem gas 500ml"]:
            _ensure_produto(nome, tipo_aguas)

        # ---- SOBREMESAS ----
        tipo_sobr = _get_or_create_tipo(self.CAT_SOBREMESAS)
        sobremesas = [
            "Delicia de abacaxi", "Musse de abacaxi c/ abacaxi caramelizado",
            "Palha italiana no pote", "Pudim", "Trufas", "Alfajor",
            "Escondidinho de morango", "Escondidinho de chocolate",
            "Musse de morango", "Bombom de uva", "Bombom de morango", "Brownie",
            "Musse de limao", "Musse de maracuja", "Musse de chocolate",
        ]
        for nome in sobremesas:
            _ensure_produto(nome, tipo_sobr)

        try:
            _logger.info("Cardapio Delicias Food cadastrado (itens sem preco).")
        except Exception:
            pass

    def _safe_reconnect(self):
        """Forca reconexao descartando a conexao atual (possivelmente corrompida)."""
        try:
            if self.connection:
                try:
                    self.connection.close()
                except Exception:
                    pass
                self.connection = None
        except Exception:
            self.connection = None
        return self.get_connection()

    def execute_query(self, sql, params=None):
        with self._conn_lock:
            for attempt in range(2):
                try:
                    conn = self.get_connection()
                    cursor = conn.cursor(dictionary=True)
                    cursor.execute(sql, params or ())
                    results = cursor.fetchall()
                    cursor.close()
                    return results
                except (MySQLError, IndexError, ValueError) as e:
                    err_msg = str(e).lower()
                    if attempt == 0 and ('bytearray' in err_msg or 'index out of range' in err_msg
                                         or 'not connected' in err_msg or 'connection' in err_msg
                                         or isinstance(e, (IndexError, ValueError))):
                        self._safe_reconnect()
                    else:
                        ErroAmigavel.log_erro(e, f"execute_query: {sql[:80]}")
                        raise
            return []

    def execute_update(self, sql, params=None):
        with self._conn_lock:
            for attempt in range(2):
                try:
                    conn = self.get_connection()
                    cursor = conn.cursor()
                    cursor.execute(sql, params or ())
                    conn.commit()
                    last_id = cursor.lastrowid
                    cursor.close()
                    return last_id
                except (MySQLError, IndexError, ValueError) as e:
                    err_msg = str(e).lower()
                    if attempt == 0 and ('bytearray' in err_msg or 'index out of range' in err_msg
                                         or 'not connected' in err_msg or 'connection' in err_msg
                                         or isinstance(e, (IndexError, ValueError))):
                        self._safe_reconnect()
                    else:
                        ErroAmigavel.log_erro(e, f"execute_update: {sql[:80]}")
                        raise
            return 0

    def execute_scalar(self, sql, params=None):
        with self._conn_lock:
            for attempt in range(2):
                try:
                    conn = self.get_connection()
                    cursor = conn.cursor()
                    cursor.execute(sql, params or ())
                    result = cursor.fetchone()
                    cursor.close()
                    return result[0] if result else None
                except (MySQLError, IndexError, ValueError) as e:
                    err_msg = str(e).lower()
                    if attempt == 0 and ('bytearray' in err_msg or 'index out of range' in err_msg
                                         or 'not connected' in err_msg or 'connection' in err_msg
                                         or isinstance(e, (IndexError, ValueError))):
                        self._safe_reconnect()
                    else:
                        ErroAmigavel.log_erro(e, f"execute_scalar: {sql[:80]}")
                        raise
            return None

    def sync_armarios_from_apk(self):
        """Mantido por compatibilidade. A sincronizacao agora e direta
        pois o Python usa as mesmas tabelas do APK (armarios_sauna, uso_armario_sauna,
        itens_armario_sauna)."""
        pass


# ============================================================================
# SESSAO DO USUARIO
# ============================================================================

class Session:
    """Sessao do usuario logado. Alinhada com PermissionManager.java do Android.
    Carrega permissoes do perfil + overrides individuais (adicionar/remover)."""
    user_id = 0
    user_nome = ""
    user_nivel = ""
    user_login = ""
    user_perfil_id = 0
    caixa_id = 0
    permissoes = set()

    @classmethod
    def clear(cls):
        cls.user_id = 0
        cls.user_nome = ""
        cls.user_nivel = ""
        cls.user_login = ""
        cls.user_perfil_id = 0
        cls.caixa_id = 0
        cls.permissoes = set()

    @classmethod
    def is_admin(cls):
        return cls.user_nivel == "admin"

    @classmethod
    def has_permission(cls, chave):
        if cls.is_admin():
            return True
        return chave in cls.permissoes

    @classmethod
    def load_permissions(cls):
        """Carrega permissoes do perfil + overrides individuais do usuario.
        Alinhado com PermissionManager.loadPermissions() do Android."""
        if cls.is_admin():
            cls.permissoes = set(PermissionConstants.get_todas_chaves())
            return
        try:
            db = DatabaseHelper.get_instance()
            # 1. Obter perfil_id do usuario
            rows = db.execute_query(
                "SELECT u.perfil_id FROM usuarios u WHERE u.id = %s", (cls.user_id,)
            )
            perfil_id = 0
            if rows and rows[0].get("perfil_id"):
                perfil_id = rows[0]["perfil_id"]
                cls.user_perfil_id = perfil_id

            # 2. Carregar permissoes do perfil
            perms_perfil = set()
            if perfil_id > 0:
                perms = db.execute_query(
                    "SELECT p.chave FROM permissoes p "
                    "JOIN perfil_permissoes pp ON pp.permissao_id = p.id "
                    "WHERE pp.perfil_id = %s", (perfil_id,)
                )
                perms_perfil = {r["chave"] for r in perms}

            # 3. Carregar overrides individuais do usuario (usuario_permissoes)
            overrides = db.execute_query(
                "SELECT p.chave, up.tipo FROM usuario_permissoes up "
                "JOIN permissoes p ON p.id = up.permissao_id "
                "WHERE up.usuario_id = %s", (cls.user_id,)
            )
            for ov in overrides:
                if ov["tipo"] == "adicionar":
                    perms_perfil.add(ov["chave"])
                elif ov["tipo"] == "remover":
                    perms_perfil.discard(ov["chave"])

            cls.permissoes = perms_perfil
        except Exception as e:
            ErroAmigavel.log_erro(e, "Session.load_permissions")
            print(f"[AVISO] Nao foi possivel carregar as permissoes do usuario: {e}")


# ============================================================================
# GERADOR DE CUPOM
# ============================================================================

class CupomGenerator:
    @staticmethod
    def gerar_cupom(venda, itens, pagamentos, empresa=None, obs_cupom="",
                    colunas=48, margem_esq=0, margem_dir=0, espacamento=1):
        """Gera o texto do cupom nao fiscal com layout dinamico.
        - colunas: largura total do papel em caracteres
        - margem_esq / margem_dir: caracteres de margem (espacos) em cada lado
        - espacamento: 1 = normal, 2 = linha extra entre cada linha
        O texto e formatado para caber exatamente na largura util,
        com word-wrap automatico para textos longos.
        """
        try:
            # Largura util = total - margens
            largura_util = max(20, colunas - margem_esq - margem_dir)
            prefixo = " " * margem_esq  # espacos de margem esquerda

            line_sep = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            sb = []

            def _add(texto):
                """Adiciona uma linha ao cupom com margem esquerda, truncada na largura util."""
                sb.append(prefixo + texto[:largura_util])

            def _add_center(texto):
                """Adiciona texto centralizado na largura util."""
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    sb.append(prefixo + CupomGenerator._center(ln, largura_util))

            def _add_right(texto):
                """Adiciona texto alinhado a direita na largura util."""
                sb.append(prefixo + CupomGenerator._right_align(texto, largura_util))

            def _add_wrap(texto):
                """Adiciona texto com word-wrap automatico."""
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    _add(ln)

            # === CABECALHO ===
            sb.append(line_eq)

            if empresa:
                # Helper para acessar dados da empresa (suporta objeto ou dict)
                def _get_emp(attr):
                    try:
                        if isinstance(empresa, dict):
                            return empresa.get(attr)
                        return getattr(empresa, attr, None)
                    except Exception:
                        return None

                nome_f = _get_emp("nome_fantasia")
                razao = _get_emp("razao_social")
                cnpj = _get_emp("cnpj")
                end = _get_emp("endereco")
                num = _get_emp("numero")
                bairro = _get_emp("bairro")
                cidade = _get_emp("cidade")
                uf = _get_emp("uf")
                fone = _get_emp("telefone")

                if nome_f:
                    _add_center(str(nome_f))
                if razao:
                    _add_center(str(razao))
                if cnpj:
                    _add_center(f"CNPJ: {cnpj}")
                
                endereco_parts = []
                if end: endereco_parts.append(str(end))
                if num: endereco_parts.append(f"N {num}")
                if bairro: endereco_parts.append(str(bairro))
                if endereco_parts:
                    _add_center(", ".join(endereco_parts))
                
                cidade_parts = []
                if cidade: cidade_parts.append(str(cidade))
                if uf: cidade_parts.append(str(uf))
                if cidade_parts:
                    _add_center(" - ".join(cidade_parts))
                
                if fone:
                    _add_center(f"Fone: {fone}")

            sb.append(line_sep)
            _add_center("CUPOM NAO FISCAL")
            sb.append(line_sep)

            # === DADOS DA VENDA ===
            _add_wrap(f"Venda: #{venda.id}")
            _add_wrap(f"Data: {FormatUtils.format_date(venda.data_venda)}")
            if venda.cliente_nome:
                _add_wrap(f"Cliente: {venda.cliente_nome}")
            if venda.vendedor_nome:
                _add_wrap(f"Vendedor: {venda.vendedor_nome}")
            sb.append(line_sep)

            # === ITENS ===
            def _item_nome_extras(item):
                """Separa o nome principal do item dos detalhes (proteinas e
                acompanhamentos do MONTE SEU PRATO), para impressao no cupom."""
                raw = (item.descricao_produto or "").strip()
                if " | " in raw:
                    partes = [p.strip() for p in raw.split(" | ") if p.strip()]
                    return partes[0], partes[1:]
                ad = getattr(item, "adicionais", None) or []
                extras = [a if isinstance(a, str) else str(a) for a in ad]
                return raw, extras

            def _print_extras(extras):
                """Imprime as proteinas/acompanhamentos indentados, com fonte
                visivel e quebra de linha automatica (abaixo do prato)."""
                for ex in extras:
                    for ln in CupomGenerator._wrap_text(ex, largura_util - 3):
                        _add("   " + ln)

            # Layout adaptativo conforme largura
            if largura_util >= 46:
                # Layout padrao: ITEM DESCRICAO QTD UNIT TOTAL em 1 linha
                col_item = 4
                col_total_val = 9  # espaco para TOTAL (ex: 1.234,56)
                col_unit = 9
                col_qty = 5
                col_desc = largura_util - col_item - col_qty - col_unit - col_total_val - 4  # 4 espacos separadores
                if col_desc < 8:
                    col_desc = 8

                header = (f"{'ITEM':<{col_item}} "
                          f"{'DESCRICAO':<{col_desc}} "
                          f"{'QTD':>{col_qty}} "
                          f"{'UNIT':>{col_unit}} "
                          f"{'TOTAL':>{col_total_val}}")
                _add(header[:largura_util])
                sb.append(line_sep)

                for i, item in enumerate(itens, 1):
                    nome, extras = _item_nome_extras(item)
                    qty = f"{item.quantidade:.0f}" if item.quantidade == int(item.quantidade) else f"{item.quantidade:.3f}"
                    unit = FormatUtils.format_money(item.preco_unitario)
                    total = FormatUtils.format_money(item.total)
                    if extras or len(nome) > col_desc:
                        # Prato montado (ou nome longo): nome em linha propria,
                        # detalhes indentados e valores alinhados a direita
                        _add_wrap(f"{i}. {nome}")
                        _print_extras(extras)
                        _add_right(f"{qty} x {unit} = R$ {total}")
                    else:
                        desc = nome[:col_desc]
                        linha = (f"{i:<{col_item}} "
                                 f"{desc:<{col_desc}} "
                                 f"{qty:>{col_qty}} "
                                 f"{unit:>{col_unit}} "
                                 f"{total:>{col_total_val}}")
                        _add(linha[:largura_util])

            elif largura_util >= 32:
                # Layout compacto: 2 linhas por item
                # Linha 1: NUM. DESCRICAO
                # Linha 2:          QTD x UNIT = TOTAL
                _add("ITEM DESCRICAO")
                sb.append(line_sep)

                for i, item in enumerate(itens, 1):
                    nome, extras = _item_nome_extras(item)
                    qty = f"{item.quantidade:.0f}" if item.quantidade == int(item.quantidade) else f"{item.quantidade:.3f}"
                    unit = FormatUtils.format_money(item.preco_unitario)
                    total = FormatUtils.format_money(item.total)
                    _add_wrap(f"{i:>3}. {nome}")
                    _print_extras(extras)
                    _add_right(f"{qty} x {unit} = R$ {total}")

            else:
                # Layout ultra-compacto (< 32 colunas)
                _add("ITENS:")
                sb.append(line_sep)
                for i, item in enumerate(itens, 1):
                    nome, extras = _item_nome_extras(item)
                    qty = f"{item.quantidade:.0f}" if item.quantidade == int(item.quantidade) else f"{item.quantidade:.3f}"
                    total = FormatUtils.format_money(item.total)
                    _add_wrap(f"{i}. {nome}")
                    _print_extras(extras)
                    _add_right(f"{qty} R$ {total}")

            sb.append(line_sep)

            # === DADOS DE ENTREGA ===
            if getattr(venda, "para_entrega", False):
                _add("** ENTREGA **")
                if getattr(venda, "bairro_entrega", ""):
                    _add_wrap(f"Bairro: {venda.bairro_entrega}")
                if getattr(venda, "endereco_entrega", ""):
                    _add_wrap(f"Endereco: {venda.endereco_entrega}")
                if getattr(venda, "celular_whatsapp", ""):
                    _add_wrap(f"WhatsApp: {venda.celular_whatsapp}")
                if getattr(venda, "entregador_nome", ""):
                    _add_wrap(f"Entregador: {venda.entregador_nome}")
                sb.append(line_sep)

            # === TOTAIS ===
            if venda.desconto_valor > 0:
                _add_right(f"SUBTOTAL: R$ {FormatUtils.format_money(venda.total_bruto)}")
                _add_right(f"DESCONTO: -R$ {FormatUtils.format_money(venda.desconto_valor)}")
            if venda.acrescimo_valor > 0:
                if venda.desconto_valor <= 0:
                    _add_right(f"SUBTOTAL: R$ {FormatUtils.format_money(venda.total_bruto)}")
                _add_right(f"ACRESCIMO: +R$ {FormatUtils.format_money(venda.acrescimo_valor)}")
            if venda.para_entrega and venda.taxa_entrega > 0:
                _add_right(f"TAXA ENTREGA: +R$ {FormatUtils.format_money(venda.taxa_entrega)}")

            _add_right(f"TOTAL: R$ {FormatUtils.format_money(venda.total_liquido)}")
            sb.append(line_sep)

            # === PAGAMENTO ===
            if venda.valor_recebido > 0:
                _add_right(f"RECEBIDO: R$ {FormatUtils.format_money(venda.valor_recebido)}")
            if venda.troco > 0:
                _add_right(f"TROCO: R$ {FormatUtils.format_money(venda.troco)}")

            if pagamentos:
                sb.append(line_sep)
                _add("FORMAS DE PAGAMENTO:")
                for pag in pagamentos:
                    forma = pag.forma_descricao or "N/A"
                    parcela = f" ({pag.parcelas}x)" if pag.parcelas > 1 else ""
                    pag_text = f"  {forma}{parcela}: R$ {FormatUtils.format_money(pag.valor)}"
                    _add_wrap(pag_text)

            sb.append(line_sep)

            # === OBSERVACAO ===
            if venda.observacao:
                _add("OBSERVACAO:")
                _add_wrap(f"  {venda.observacao}")
                sb.append(line_sep)

            # === OBS CUPOM ===
            if obs_cupom:
                for obs_line in CupomGenerator._wrap_text(obs_cupom, largura_util):
                    _add_center(obs_line)
                sb.append(line_sep)

            # === RODAPE ===
            _add_center(f"PDV Pro v{APP_VERSION}")
            _add_center("Obrigado pela preferencia!")
            _add_center("Contato sistema: (85) 98123-7727")
            sb.append(line_eq)

            # Aplicar espacamento entre linhas
            if espacamento and espacamento > 1:
                spaced = []
                for ln in sb:
                    spaced.append(ln)
                    for _ in range(espacamento - 1):
                        spaced.append("")  # linha em branco extra
                sb = spaced

            return "\n".join(sb)
        except Exception as e:
            return f"Erro ao gerar cupom: {e}"

    @staticmethod
    def _wrap_text(text, width):
        """Quebra texto longo em multiplas linhas respeitando a largura.
        Tenta quebrar em espacos; se nao couber, corta no limite."""
        if not text:
            return [""]
        text = str(text)
        if len(text) <= width:
            return [text]

        lines = []
        while len(text) > width:
            # Procurar ultimo espaco dentro da largura
            pos = text.rfind(" ", 0, width)
            if pos <= 0:
                # Sem espaco, cortar no limite
                pos = width
            lines.append(text[:pos])
            text = text[pos:].lstrip()
        if text:
            lines.append(text)
        return lines

    @staticmethod
    def _center(text, width):
        if not text:
            text = ""
        if len(text) >= width:
            return text[:width]
        pad = (width - len(text)) // 2
        return " " * pad + text

    @staticmethod
    def _right_align(text, width):
        if not text:
            text = ""
        if len(text) >= width:
            return text[:width]
        return " " * (width - len(text)) + text

    @staticmethod
    def gerar_cupom_parcial(venda, itens, nome_impressora="", nome_setor="",
                            empresa=None, colunas=48, margem_esq=0, margem_dir=0,
                            espacamento=1):
        """Gera cupom parcial contendo apenas os itens filtrados para uma impressora/setor.
        Usado no sistema de multiplas impressoras por categoria.
        Exibe: cabecalho da empresa, dados da venda, apenas os itens filtrados e o setor destino."""
        try:
            largura_util = max(20, colunas - margem_esq - margem_dir)
            prefixo = " " * margem_esq

            line_sep = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            sb = []

            def _add(texto):
                sb.append(prefixo + texto[:largura_util])

            def _add_center(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    sb.append(prefixo + CupomGenerator._center(ln, largura_util))

            def _add_right(texto):
                sb.append(prefixo + CupomGenerator._right_align(texto, largura_util))

            def _add_wrap(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    _add(ln)

            # === CABECALHO ===
            sb.append(line_eq)
            if empresa:
                # Helper para acessar dados da empresa (suporta objeto ou dict)
                def _get_emp(attr):
                    try:
                        if isinstance(empresa, dict):
                            return empresa.get(attr)
                        return getattr(empresa, attr, None)
                    except Exception:
                        return None
                
                nome_f = _get_emp("nome_fantasia") or _get_emp("razao_social")
                if nome_f:
                    _add_center(str(nome_f))
            sb.append(line_sep)

            # Titulo do setor/impressora
            titulo_setor = nome_setor or nome_impressora or "PRODUCAO"
            _add_center(f"*** {titulo_setor.upper()} ***")
            sb.append(line_sep)

            # Dados da venda
            _add_wrap(f"Venda: #{venda.id}")
            _add_wrap(f"Data: {FormatUtils.format_date(venda.data_venda)}")
            if venda.cliente_nome:
                _add_wrap(f"Cliente: {venda.cliente_nome}")
            if venda.observacao:
                _add_wrap(f"Obs: {venda.observacao}")
            sb.append(line_sep)

            # === ITENS ===
            _add("ITENS:")
            sb.append(line_sep)

            for i, item in enumerate(itens, 1):
                desc = (item.descricao_produto or "")[:largura_util - 5]
                qty = f"{item.quantidade:.0f}" if item.quantidade == int(item.quantidade) else f"{item.quantidade:.3f}"
                _add(f"{i:>3}. {desc}")
                _add(f"     Qtd: {qty}")
                if item.adicionais:
                    for ad in item.adicionais:
                        ad_desc = ad if isinstance(ad, str) else str(ad)
                        _add(f"     + {ad_desc}")

            sb.append(line_sep)
            _add(f"Total de itens: {len(itens)}")
            sb.append(line_eq)
            data_hora = datetime.datetime.now().strftime("%d/%m/%Y %H:%M:%S")
            _add_center(f"Impresso: {data_hora}")
            sb.append(line_eq)

            # Espacamento
            if espacamento and espacamento > 1:
                spaced = []
                for ln in sb:
                    spaced.append(ln)
                    for _ in range(espacamento - 1):
                        spaced.append("")
                sb = spaced

            return "\n".join(sb)
        except Exception as e:
            return f"Erro ao gerar cupom parcial: {e}"

    @staticmethod
    def gerar_cupom_abertura_caixa(caixa_id, usuario_nome, valor_abertura,
                                    data_abertura, empresa=None,
                                    colunas=48, margem_esq=0, margem_dir=0, espacamento=1):
        """Gera o texto do cupom de ABERTURA DE CAIXA para impressao.
        Inclui dados da empresa, identificacao do caixa, operador e valor de abertura."""
        try:
            largura_util = max(20, colunas - margem_esq - margem_dir)
            prefixo = " " * margem_esq

            line_sep = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            sb = []

            def _add(texto):
                sb.append(prefixo + texto[:largura_util])

            def _add_center(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    sb.append(prefixo + CupomGenerator._center(ln, largura_util))

            def _add_right(texto):
                sb.append(prefixo + CupomGenerator._right_align(texto, largura_util))

            def _add_wrap(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    _add(ln)

            # === CABECALHO ===
            sb.append(line_eq)

            if empresa:
                if empresa.get("nome_fantasia"):
                    _add_center(empresa["nome_fantasia"])
                if empresa.get("razao_social"):
                    _add_center(empresa["razao_social"])
                if empresa.get("cnpj"):
                    _add_center(f"CNPJ: {empresa['cnpj']}")
                endereco_parts = []
                if empresa.get("endereco"):
                    endereco_parts.append(empresa["endereco"])
                if empresa.get("numero"):
                    endereco_parts.append(f"N {empresa['numero']}")
                if empresa.get("bairro"):
                    endereco_parts.append(empresa["bairro"])
                if endereco_parts:
                    _add_center(", ".join(endereco_parts))
                cidade_parts = []
                if empresa.get("cidade"):
                    cidade_parts.append(empresa["cidade"])
                if empresa.get("uf"):
                    cidade_parts.append(empresa["uf"])
                if cidade_parts:
                    _add_center(" - ".join(cidade_parts))
                if empresa.get("telefone"):
                    _add_center(f"Fone: {empresa['telefone']}")

            sb.append(line_sep)
            _add_center("*** ABERTURA DE CAIXA ***")
            sb.append(line_sep)

            # === DADOS DA ABERTURA ===
            _add_wrap(f"Caixa: #{caixa_id}")
            _add_wrap(f"Operador: {usuario_nome}")
            _add_wrap(f"Data/Hora: {FormatUtils.format_date(data_abertura)}")
            sb.append(line_sep)

            _add_right(f"VALOR DE ABERTURA: R$ {FormatUtils.format_money(valor_abertura)}")

            sb.append(line_sep)

            # === RODAPE ===
            _add_center(f"PDV Pro v{APP_VERSION}")
            _add_center("Bom trabalho!")
            sb.append(line_eq)

            # Aplicar espacamento entre linhas
            if espacamento and espacamento > 1:
                spaced = []
                for ln in sb:
                    spaced.append(ln)
                    for _ in range(espacamento - 1):
                        spaced.append("")
                sb = spaced

            return "\n".join(sb)
        except Exception as e:
            return f"Erro ao gerar cupom de abertura: {e}"

    @staticmethod
    def gerar_cupom_sangria(sangria_id, caixa_id, operador_nome, descricao, valor,
                            data_sangria, empresa=None,
                            colunas=48, margem_esq=0, margem_dir=0, espacamento=1):
        """Gera o texto do cupom/comprovante de SANGRIA para impressao.
        Inclui dados da empresa, identificacao do caixa, operador, motivo e valor."""
        try:
            largura_util = max(20, colunas - margem_esq - margem_dir)
            prefixo = " " * margem_esq

            line_sep = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            sb = []

            def _add(texto):
                sb.append(prefixo + texto[:largura_util])

            def _add_center(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    sb.append(prefixo + CupomGenerator._center(ln, largura_util))

            def _add_right(texto):
                sb.append(prefixo + CupomGenerator._right_align(texto, largura_util))

            def _add_wrap(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    _add(ln)

            # === CABECALHO ===
            sb.append(line_eq)

            if empresa:
                if empresa.get("nome_fantasia"):
                    _add_center(empresa["nome_fantasia"])
                if empresa.get("razao_social"):
                    _add_center(empresa["razao_social"])
                if empresa.get("cnpj"):
                    _add_center(f"CNPJ: {empresa['cnpj']}")
                endereco_parts = []
                if empresa.get("endereco"):
                    endereco_parts.append(empresa["endereco"])
                if empresa.get("numero"):
                    endereco_parts.append(f"N {empresa['numero']}")
                if empresa.get("bairro"):
                    endereco_parts.append(empresa["bairro"])
                if endereco_parts:
                    _add_center(", ".join(endereco_parts))
                cidade_parts = []
                if empresa.get("cidade"):
                    cidade_parts.append(empresa["cidade"])
                if empresa.get("uf"):
                    cidade_parts.append(empresa["uf"])
                if cidade_parts:
                    _add_center(" - ".join(cidade_parts))
                if empresa.get("telefone"):
                    _add_center(f"Fone: {empresa['telefone']}")

            sb.append(line_sep)
            _add_center("*** COMPROVANTE DE SANGRIA ***")
            sb.append(line_sep)

            # === DADOS DA SANGRIA ===
            _add_wrap(f"Sangria: #{sangria_id}")
            _add_wrap(f"Caixa: #{caixa_id}")
            _add_wrap(f"Operador: {operador_nome}")
            _add_wrap(f"Data/Hora: {FormatUtils.format_date(data_sangria)}")
            sb.append(line_sep)

            _add_wrap(f"Motivo: {descricao}")
            sb.append(line_sep)

            _add_right(f"VALOR DA SANGRIA: R$ {FormatUtils.format_money(valor)}")

            sb.append(line_sep)

            # === RODAPE ===
            _add_center(f"PDV Pro v{APP_VERSION}")
            _add_center("Sangria registrada com sucesso!")
            sb.append(line_eq)

            # Aplicar espacamento entre linhas
            if espacamento and espacamento > 1:
                spaced = []
                for ln in sb:
                    spaced.append(ln)
                    for _ in range(espacamento - 1):
                        spaced.append("")
                sb = spaced

            return "\n".join(sb)
        except Exception as e:
            return f"Erro ao gerar cupom de sangria: {e}"

    @staticmethod
    def gerar_cupom_fechamento_caixa(caixa_id, usuario_nome, valor_abertura,
                                      valor_fechamento, data_abertura, data_fechamento,
                                      total_vendas, qtd_vendas, total_vales, qtd_vales,
                                      total_sangrias=0, qtd_sangrias=0,
                                      total_entradas=0, qtd_entradas=0,
                                      pagamentos_resumo=None, empresa=None,
                                      colunas=48, margem_esq=0, margem_dir=0, espacamento=1):
        """Gera o texto do cupom de FECHAMENTO DE CAIXA para impressao.
        Inclui dados da empresa, resumo completo de vendas, formas de pagamento,
        vales/debitos, sangrias e totais de abertura/fechamento."""
        try:
            largura_util = max(20, colunas - margem_esq - margem_dir)
            prefixo = " " * margem_esq

            line_sep = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            sb = []

            def _add(texto):
                sb.append(prefixo + texto[:largura_util])

            def _add_center(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    sb.append(prefixo + CupomGenerator._center(ln, largura_util))

            def _add_right(texto):
                sb.append(prefixo + CupomGenerator._right_align(texto, largura_util))

            def _add_wrap(texto):
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    _add(ln)

            # === CABECALHO ===
            sb.append(line_eq)

            if empresa:
                if empresa.get("nome_fantasia"):
                    _add_center(empresa["nome_fantasia"])
                if empresa.get("razao_social"):
                    _add_center(empresa["razao_social"])
                if empresa.get("cnpj"):
                    _add_center(f"CNPJ: {empresa['cnpj']}")
                endereco_parts = []
                if empresa.get("endereco"):
                    endereco_parts.append(empresa["endereco"])
                if empresa.get("numero"):
                    endereco_parts.append(f"N {empresa['numero']}")
                if empresa.get("bairro"):
                    endereco_parts.append(empresa["bairro"])
                if endereco_parts:
                    _add_center(", ".join(endereco_parts))
                cidade_parts = []
                if empresa.get("cidade"):
                    cidade_parts.append(empresa["cidade"])
                if empresa.get("uf"):
                    cidade_parts.append(empresa["uf"])
                if cidade_parts:
                    _add_center(" - ".join(cidade_parts))
                if empresa.get("telefone"):
                    _add_center(f"Fone: {empresa['telefone']}")

            sb.append(line_sep)
            _add_center("*** FECHAMENTO DE CAIXA ***")
            sb.append(line_sep)

            # === DADOS DO CAIXA ===
            _add_wrap(f"Caixa: #{caixa_id}")
            _add_wrap(f"Operador: {usuario_nome}")
            _add_wrap(f"Abertura: {FormatUtils.format_date(data_abertura)}")
            _add_wrap(f"Fechamento: {FormatUtils.format_date(data_fechamento)}")
            sb.append(line_sep)

            # === RESUMO DE VENDAS ===
            _add_center("RESUMO DE VENDAS")
            sb.append(line_sep)
            _add_wrap(f"Quantidade de Vendas: {qtd_vendas}")
            _add_right(f"Total Vendas: R$ {FormatUtils.format_money(total_vendas)}")
            sb.append(line_sep)

            # === FORMAS DE PAGAMENTO ===
            if pagamentos_resumo:
                _add_center("FORMAS DE PAGAMENTO")
                sb.append(line_sep)
                for pag in pagamentos_resumo:
                    desc = pag.get("descricao", "N/A")
                    total_pag = pag.get("total", 0)
                    _add_right(f"{desc}: R$ {FormatUtils.format_money(total_pag)}")
                sb.append(line_sep)

            # === VALES/DEBITOS ===
            if qtd_vales > 0:
                _add_center("VALES / DEBITOS")
                sb.append(line_sep)
                _add_wrap(f"Quantidade: {qtd_vales}")
                _add_right(f"Total Vales: R$ {FormatUtils.format_money(total_vales)}")
                sb.append(line_sep)

            # === SANGRIAS ===
            if qtd_sangrias > 0:
                _add_center("SANGRIAS")
                sb.append(line_sep)
                _add_wrap(f"Quantidade: {qtd_sangrias}")
                _add_right(f"Total Sangrias: R$ {FormatUtils.format_money(total_sangrias)}")
                sb.append(line_sep)

            # === ENTRADAS / RECEBIMENTOS (contas a receber -> credito no caixa) ===
            if qtd_entradas > 0:
                _add_center("ENTRADAS / RECEBIMENTOS")
                sb.append(line_sep)
                _add_wrap(f"Quantidade: {qtd_entradas}")
                _add_right(f"Total Entradas: R$ {FormatUtils.format_money(total_entradas)}")
                sb.append(line_sep)

            # === TOTAIS ===
            _add_center("TOTALIZACAO")
            sb.append(line_sep)
            _add_right(f"Valor Abertura: R$ {FormatUtils.format_money(valor_abertura)}")
            _add_right(f"(+) Vendas: R$ {FormatUtils.format_money(total_vendas)}")
            if total_entradas > 0:
                _add_right(f"(+) Entradas/Receb.: R$ {FormatUtils.format_money(total_entradas)}")
            if total_vales > 0:
                _add_right(f"(-) Vales/Debitos: R$ {FormatUtils.format_money(total_vales)}")
            if total_sangrias > 0:
                _add_right(f"(-) Sangrias: R$ {FormatUtils.format_money(total_sangrias)}")
            _add_right(f"VALOR FECHAMENTO: R$ {FormatUtils.format_money(valor_fechamento)}")
            sb.append(line_sep)

            # Diferenca (se houver)
            esperado = float(valor_abertura) + float(total_vendas) + float(total_entradas) - float(total_vales) - float(total_sangrias)
            diferenca = float(valor_fechamento) - esperado
            if abs(diferenca) > 0.01:
                if diferenca > 0:
                    _add_right(f"SOBRA: R$ {FormatUtils.format_money(diferenca)}")
                else:
                    _add_right(f"FALTA: R$ {FormatUtils.format_money(abs(diferenca))}")
                sb.append(line_sep)

            # === RODAPE ===
            _add_center(f"PDV Pro v{APP_VERSION}")
            _add_center("Caixa encerrado com sucesso!")
            sb.append(line_eq)

            # Aplicar espacamento entre linhas
            if espacamento and espacamento > 1:
                spaced = []
                for ln in sb:
                    spaced.append(ln)
                    for _ in range(espacamento - 1):
                        spaced.append("")
                sb = spaced

            return "\n".join(sb)
        except Exception as e:
            return f"Erro ao gerar cupom de fechamento: {e}"



# ============================================================================
# WIDGETS PERSONALIZADOS FUTURISTICOS
# ============================================================================

class StyledButton(tk.Canvas):
    """Botao futuristico com efeito neon glow, gradiente e animacao de hover."""
    def __init__(self, master, text="", command=None, color=COR_BOTAO,
                 hover_color=None, width=20, **kwargs):
        # Calcular dimensoes
        char_w = 9
        btn_w = max(width * char_w + 24, 100)
        btn_h = 38
        super().__init__(master, width=btn_w, height=btn_h,
                         bg=self._get_parent_bg(master), highlightthickness=0,
                         bd=0, **kwargs)

        self._color = color
        self._hover_color = hover_color or self._lighten(color, 30)
        self._text = text
        self._command = command
        self._btn_w = btn_w
        self._btn_h = btn_h
        self._pressed = False
        self._hovered = False

        self._draw()

        self.bind("<Enter>", self._on_enter)
        self.bind("<Leave>", self._on_leave)
        self.bind("<ButtonPress-1>", self._on_press)
        self.bind("<ButtonRelease-1>", self._on_release)

    def _get_parent_bg(self, master):
        try:
            return master.cget("bg")
        except Exception:
            return COR_FUNDO

    def _lighten(self, color, amount):
        try:
            r = min(255, int(color[1:3], 16) + amount)
            g = min(255, int(color[3:5], 16) + amount)
            b = min(255, int(color[5:7], 16) + amount)
            return f"#{r:02x}{g:02x}{b:02x}"
        except Exception:
            return color

    def _darken(self, color, amount):
        try:
            r = max(0, int(color[1:3], 16) - amount)
            g = max(0, int(color[3:5], 16) - amount)
            b = max(0, int(color[5:7], 16) - amount)
            return f"#{r:02x}{g:02x}{b:02x}"
        except Exception:
            return color

    def _draw(self):
        self.delete("all")
        w, h = self._btn_w, self._btn_h
        r = 8  # border radius

        if self._hovered:
            fill = self._hover_color
            outline = self._lighten(self._color, 50)
        elif self._pressed:
            fill = self._darken(self._color, 30)
            outline = self._color
        else:
            fill = self._color
            outline = self._darken(self._color, 20)

        # Sombra glow
        if self._hovered:
            glow_color = self._darken(self._color, 60)
            self.create_oval(4, h - 6, w - 4, h + 4, fill=glow_color, outline="")

        # Corpo do botao com cantos arredondados
        self._round_rect(2, 2, w - 2, h - 2, r, fill=fill, outline=outline, width=1)

        # Linha de brilho superior (highlight)
        highlight = self._lighten(fill, 40)
        self.create_line(r + 2, 4, w - r - 2, 4, fill=highlight, width=1)

        # Texto
        text_color = "#ffffff" if not self._is_light(fill) else "#0a0e1a"
        self.create_text(w // 2, h // 2, text=self._text,
                        fill=text_color, font=("Segoe UI", 10, "bold"))

    def _round_rect(self, x1, y1, x2, y2, r, **kwargs):
        """Desenha retangulo com cantos arredondados."""
        points = [
            x1 + r, y1,
            x2 - r, y1,
            x2, y1,
            x2, y1 + r,
            x2, y2 - r,
            x2, y2,
            x2 - r, y2,
            x1 + r, y2,
            x1, y2,
            x1, y2 - r,
            x1, y1 + r,
            x1, y1,
            x1 + r, y1,
        ]
        return self.create_polygon(points, smooth=True, **kwargs)

    def _is_light(self, color):
        try:
            r = int(color[1:3], 16)
            g = int(color[3:5], 16)
            b = int(color[5:7], 16)
            return (r * 299 + g * 587 + b * 114) / 1000 > 186
        except Exception:
            return False

    def _on_enter(self, event):
        self._hovered = True
        self.config(cursor="hand2")
        self._draw()

    def _on_leave(self, event):
        self._hovered = False
        self._pressed = False
        self.config(cursor="")
        self._draw()

    def _on_press(self, event):
        self._pressed = True
        self._draw()

    def _on_release(self, event):
        self._pressed = False
        self._draw()
        if self._hovered and self._command:
            self._command()

    def config(self, **kwargs):
        if "text" in kwargs:
            self._text = kwargs.pop("text")
            self._draw()
        if "command" in kwargs:
            self._command = kwargs.pop("command")
        if "state" in kwargs:
            state = kwargs.pop("state")
            if state == "disabled":
                self._command = None
                self._color = "#2a3a5c"
                self._hover_color = "#2a3a5c"
                self._draw()
        if "bg" in kwargs:
            self._color = kwargs.pop("bg")
            self._hover_color = self._lighten(self._color, 30)
            self._draw()
        super().config(**{k: v for k, v in kwargs.items() if k in ("cursor",)})

    configure = config


class StyledEntry(tk.Entry):
    """Campo de entrada futuristico com borda neon e efeito de foco."""
    def __init__(self, master, **kwargs):
        super().__init__(master, bg=COR_ENTRADA, fg=COR_TEXTO,
                         insertbackground=COR_PRIMARIA, font=("Segoe UI", 11),
                         relief="flat", bd=0, highlightthickness=2,
                         highlightcolor=COR_PRIMARIA,
                         highlightbackground=COR_ENTRADA_BORDER,
                         selectbackground=COR_PRIMARIA,
                         selectforeground="#ffffff",
                         **kwargs)
        # Efeito de foco neon
        self.bind("<FocusIn>", self._on_focus_in)
        self.bind("<FocusOut>", self._on_focus_out)

    def _on_focus_in(self, event):
        self.configure(highlightcolor=COR_PRIMARIA,
                      highlightbackground=COR_PRIMARIA)

    def _on_focus_out(self, event):
        self.configure(highlightcolor=COR_ENTRADA_BORDER,
                      highlightbackground=COR_ENTRADA_BORDER)


class DatePickerEntry(tk.Frame):
    """Campo de data com calendario popup futuristico.
    Exibe um Entry readonly + botao de calendario.
    Ao clicar, abre um popup com calendario visual para selecionar a data.
    Formato exibido: DD/MM/AAAA. Metodo get() retorna DD/MM/AAAA."""

    def __init__(self, master, width=15, **kwargs):
        bg = COR_GLASS
        try:
            bg = master.cget("bg")
        except Exception:
            pass
        super().__init__(master, bg=bg, **kwargs)

        self._selected_date = None  # datetime.date ou None
        self._popup = None

        # Frame interno para entry + botao
        inner = tk.Frame(self, bg=bg)
        inner.pack(anchor="w")

        self._entry = StyledEntry(inner, width=width)
        self._entry.pack(side="left")
        self._entry.config(state="readonly", readonlybackground=COR_ENTRADA,
                           cursor="hand2")
        self._entry.bind("<Button-1>", lambda e: self._toggle_popup())

        self._btn = tk.Button(inner, text=Icons.CALENDAR, bg=COR_ENTRADA,
                              fg=COR_PRIMARIA, relief="flat",
                              font=("Segoe UI", 12), cursor="hand2",
                              activebackground=COR_ENTRADA,
                              activeforeground=COR_ACCENT, bd=0,
                              command=self._toggle_popup)
        self._btn.pack(side="left", padx=(5, 0))

    def get(self):
        """Retorna a data selecionada no formato DD/MM/AAAA ou string vazia."""
        if self._selected_date:
            return self._selected_date.strftime("%d/%m/%Y")
        return ""

    def set_date(self, date_obj):
        """Define a data programaticamente (datetime.date)."""
        self._selected_date = date_obj
        self._update_entry()

    def _update_entry(self):
        self._entry.config(state="normal")
        self._entry.delete(0, "end")
        if self._selected_date:
            self._entry.insert(0, self._selected_date.strftime("%d/%m/%Y"))
        self._entry.config(state="readonly", readonlybackground=COR_ENTRADA)

    def _toggle_popup(self):
        if self._popup and self._popup.winfo_exists():
            self._popup.destroy()
            self._popup = None
            return
        self._show_popup()

    def _show_popup(self):
        """Abre o popup do calendario."""
        if self._popup and self._popup.winfo_exists():
            self._popup.destroy()

        self._popup = tk.Toplevel(self.winfo_toplevel())
        self._popup.overrideredirect(True)
        self._popup.configure(bg=COR_GLASS_BORDER)
        self._popup.attributes("-topmost", True)

        # Posicionar abaixo do entry
        x = self._entry.winfo_rootx()
        y = self._entry.winfo_rooty() + self._entry.winfo_height() + 4
        self._popup.geometry(f"+{x}+{y}")

        # Container principal
        container = tk.Frame(self._popup, bg=COR_GLASS, bd=0,
                             highlightthickness=2,
                             highlightbackground=COR_PRIMARIA)
        container.pack(fill="both", expand=True, padx=1, pady=1)

        # Data inicial para exibir
        if self._selected_date:
            self._cal_year = self._selected_date.year
            self._cal_month = self._selected_date.month
        else:
            today = datetime.date.today()
            self._cal_year = today.year
            self._cal_month = today.month

        self._cal_container = container
        self._draw_calendar()

        # Fechar ao clicar fora
        self._popup.bind("<FocusOut>", self._on_popup_focus_out)
        self._popup.focus_set()

    def _on_popup_focus_out(self, event):
        """Fecha o popup quando perde o foco (clicou fora)."""
        try:
            if self._popup and self._popup.winfo_exists():
                # Verificar se o foco foi para um widget filho do popup
                focused = self._popup.focus_get()
                if focused and str(focused).startswith(str(self._popup)):
                    return
                self._popup.after(150, self._close_popup_if_not_focused)
        except Exception:
            pass

    def _close_popup_if_not_focused(self):
        try:
            if self._popup and self._popup.winfo_exists():
                focused = self._popup.focus_get()
                if focused and str(focused).startswith(str(self._popup)):
                    return
                self._popup.destroy()
                self._popup = None
        except Exception:
            pass

    def _draw_calendar(self):
        """Desenha o calendario no popup."""
        # Limpar container
        for w in self._cal_container.winfo_children():
            w.destroy()

        container = self._cal_container

        # ---- Header: navegacao de mes/ano ----
        header = tk.Frame(container, bg=COR_GLASS)
        header.pack(fill="x", padx=8, pady=(8, 4))

        MESES_PT = ["", "Janeiro", "Fevereiro", "Marco", "Abril", "Maio",
                    "Junho", "Julho", "Agosto", "Setembro", "Outubro",
                    "Novembro", "Dezembro"]

        btn_prev_year = tk.Button(header, text="\u00AB", bg=COR_GLASS,
                                  fg=COR_PRIMARIA, relief="flat",
                                  font=("Segoe UI", 11, "bold"),
                                  cursor="hand2", bd=0,
                                  activebackground=COR_CARD_HOVER,
                                  activeforeground=COR_ACCENT,
                                  command=lambda: self._nav_calendar(-12))
        btn_prev_year.pack(side="left", padx=(0, 2))

        btn_prev = tk.Button(header, text="\u2039", bg=COR_GLASS,
                             fg=COR_PRIMARIA, relief="flat",
                             font=("Segoe UI", 14, "bold"),
                             cursor="hand2", bd=0,
                             activebackground=COR_CARD_HOVER,
                             activeforeground=COR_ACCENT,
                             command=lambda: self._nav_calendar(-1))
        btn_prev.pack(side="left", padx=(0, 4))

        month_year_text = f"{MESES_PT[self._cal_month]} {self._cal_year}"
        lbl_month = tk.Label(header, text=month_year_text, bg=COR_GLASS,
                             fg=COR_TEXTO, font=("Segoe UI", 11, "bold"))
        lbl_month.pack(side="left", expand=True)

        btn_next = tk.Button(header, text="\u203A", bg=COR_GLASS,
                             fg=COR_PRIMARIA, relief="flat",
                             font=("Segoe UI", 14, "bold"),
                             cursor="hand2", bd=0,
                             activebackground=COR_CARD_HOVER,
                             activeforeground=COR_ACCENT,
                             command=lambda: self._nav_calendar(1))
        btn_next.pack(side="right", padx=(4, 0))

        btn_next_year = tk.Button(header, text="\u00BB", bg=COR_GLASS,
                                  fg=COR_PRIMARIA, relief="flat",
                                  font=("Segoe UI", 11, "bold"),
                                  cursor="hand2", bd=0,
                                  activebackground=COR_CARD_HOVER,
                                  activeforeground=COR_ACCENT,
                                  command=lambda: self._nav_calendar(12))
        btn_next_year.pack(side="right", padx=(0, 2))

        # ---- Dias da semana ----
        days_frame = tk.Frame(container, bg=COR_GLASS)
        days_frame.pack(fill="x", padx=8, pady=(2, 0))

        dias_semana = ["Seg", "Ter", "Qua", "Qui", "Sex", "Sab", "Dom"]
        for d in dias_semana:
            cor_dia = COR_ERRO if d in ("Sab", "Dom") else COR_TEXTO2
            tk.Label(days_frame, text=d, bg=COR_GLASS, fg=cor_dia,
                     font=("Segoe UI", 9, "bold"), width=4).pack(side="left", padx=1)

        # ---- Grid de dias ----
        import calendar as _cal_mod
        cal = _cal_mod.Calendar(firstweekday=0)  # Segunda = 0
        weeks = cal.monthdayscalendar(self._cal_year, self._cal_month)

        today = datetime.date.today()

        for week in weeks:
            week_frame = tk.Frame(container, bg=COR_GLASS)
            week_frame.pack(fill="x", padx=8, pady=1)

            for i, day in enumerate(week):
                if day == 0:
                    # Dia vazio
                    tk.Label(week_frame, text="", bg=COR_GLASS,
                             width=4, height=1,
                             font=("Segoe UI", 10)).pack(side="left", padx=1)
                else:
                    d = datetime.date(self._cal_year, self._cal_month, day)
                    is_today = (d == today)
                    is_selected = (d == self._selected_date)
                    is_weekend = (i >= 5)  # Sab=5, Dom=6

                    if is_selected:
                        bg_cor = COR_PRIMARIA
                        fg_cor = "#000000"
                        font_w = "bold"
                    elif is_today:
                        bg_cor = COR_CARD_HOVER
                        fg_cor = COR_ACCENT
                        font_w = "bold"
                    else:
                        bg_cor = COR_GLASS
                        fg_cor = COR_ERRO if is_weekend else COR_TEXTO
                        font_w = "normal"

                    btn_day = tk.Label(week_frame, text=str(day), bg=bg_cor,
                                       fg=fg_cor, width=4, height=1,
                                       font=("Segoe UI", 10, font_w),
                                       cursor="hand2", relief="flat")
                    btn_day.pack(side="left", padx=1)

                    # Bind de clique
                    btn_day.bind("<Button-1>",
                                 lambda e, dt=d: self._select_date(dt))

                    # Hover
                    def _enter(e, w=btn_day, sel=is_selected):
                        if not sel:
                            w.config(bg=COR_CARD_HOVER)
                    def _leave(e, w=btn_day, sel=is_selected, orig_bg=bg_cor):
                        if not sel:
                            w.config(bg=orig_bg)
                    btn_day.bind("<Enter>", _enter)
                    btn_day.bind("<Leave>", _leave)

        # ---- Botao Hoje ----
        btn_hoje_frame = tk.Frame(container, bg=COR_GLASS)
        btn_hoje_frame.pack(fill="x", padx=8, pady=(6, 8))

        btn_hoje = tk.Button(btn_hoje_frame, text="Hoje", bg=COR_CARD_HOVER,
                             fg=COR_PRIMARIA, relief="flat",
                             font=("Segoe UI", 9, "bold"),
                             cursor="hand2", bd=0,
                             activebackground=COR_PRIMARIA,
                             activeforeground="#000000",
                             command=lambda: self._select_date(today))
        btn_hoje.pack(expand=True)

    def _nav_calendar(self, delta_months):
        """Navega o calendario por delta meses."""
        m = self._cal_month + delta_months
        y = self._cal_year
        while m > 12:
            m -= 12
            y += 1
        while m < 1:
            m += 12
            y -= 1
        self._cal_month = m
        self._cal_year = y
        self._draw_calendar()

    def _select_date(self, date_obj):
        """Seleciona uma data e fecha o popup."""
        self._selected_date = date_obj
        self._update_entry()
        if self._popup and self._popup.winfo_exists():
            self._popup.destroy()
            self._popup = None


class StyledLabel(tk.Label):
    """Label estilizado futuristico."""
    def __init__(self, master, text="", font_size=11, bold=False, color=COR_TEXTO, **kwargs):
        font_weight = "bold" if bold else "normal"
        super().__init__(master, text=text, bg=COR_FUNDO, fg=color,
                         font=("Segoe UI", font_size, font_weight), **kwargs)


class CardFrame(tk.Frame):
    """Card com efeito glassmorphism e borda neon sutil."""
    def __init__(self, master, **kwargs):
        super().__init__(master, bg=COR_CARD, relief="flat", bd=0,
                         highlightthickness=1, highlightbackground=COR_GLASS_BORDER,
                         padx=15, pady=10, **kwargs)
        # Efeito hover com borda neon
        self.bind("<Enter>", self._on_enter)
        self.bind("<Leave>", self._on_leave)

    def _on_enter(self, event):
        self.configure(highlightbackground=COR_PRIMARIA_GLOW)

    def _on_leave(self, event):
        self.configure(highlightbackground=COR_GLASS_BORDER)


class GlassCard(tk.Frame):
    """Card premium com efeito glassmorphism avancado."""
    def __init__(self, master, glow_color=COR_PRIMARIA, **kwargs):
        super().__init__(master, bg=COR_GLASS, relief="flat", bd=0,
                         highlightthickness=2,
                         highlightbackground=COR_GLASS_BORDER,
                         padx=18, pady=12, **kwargs)
        self._glow_color = glow_color
        self.bind("<Enter>", self._on_enter)
        self.bind("<Leave>", self._on_leave)

    def _on_enter(self, event):
        self.configure(highlightbackground=self._glow_color, bg=COR_CARD_HOVER)

    def _on_leave(self, event):
        self.configure(highlightbackground=COR_GLASS_BORDER, bg=COR_GLASS)


class ScrollableFrame(tk.Frame):
    """Frame com scroll e visual futuristico."""
    def __init__(self, master, **kwargs):
        super().__init__(master, bg=COR_FUNDO, **kwargs)
        self.canvas = tk.Canvas(self, bg=COR_FUNDO, highlightthickness=0)
        self.scrollbar = ttk.Scrollbar(self, orient="vertical", command=self.canvas.yview)
        self.scrollable_frame = tk.Frame(self.canvas, bg=COR_FUNDO)
        self.scrollable_frame.bind("<Configure>",
            lambda e: self.canvas.configure(scrollregion=self.canvas.bbox("all")))
        self.canvas.create_window((0, 0), window=self.scrollable_frame, anchor="nw")
        self.canvas.configure(yscrollcommand=self.scrollbar.set)
        self.canvas.pack(side="left", fill="both", expand=True)
        self.scrollbar.pack(side="right", fill="y")

        def _on_mousewheel(event):
            try:
                if not self.winfo_exists() or not self.canvas.winfo_exists():
                    return "break"
                bbox = self.canvas.bbox("all")
                if not bbox:
                    return "break"
                content_height = max(0, bbox[3] - bbox[1])
                canvas_height = max(1, self.canvas.winfo_height())
                if content_height <= canvas_height:
                    return "break"
                if getattr(event, "num", None) == 4:
                    self.canvas.yview_scroll(-3, "units")
                elif getattr(event, "num", None) == 5:
                    self.canvas.yview_scroll(3, "units")
                else:
                    delta = getattr(event, "delta", 0)
                    if delta:
                        self.canvas.yview_scroll(int(-1 * (delta / 120)), "units")
                return "break"
            except Exception:
                return "break"

        def _bind_mousewheel(event=None):
            try:
                if self.canvas.winfo_exists():
                    self.canvas.bind_all("<MouseWheel>", _on_mousewheel)
                    self.canvas.bind_all("<Button-4>", _on_mousewheel)
                    self.canvas.bind_all("<Button-5>", _on_mousewheel)
            except Exception:
                pass

        def _unbind_mousewheel(event=None):
            try:
                if self.canvas.winfo_exists():
                    self.canvas.unbind_all("<MouseWheel>")
                    self.canvas.unbind_all("<Button-4>")
                    self.canvas.unbind_all("<Button-5>")
            except Exception:
                pass

        self.canvas.bind("<Enter>", _bind_mousewheel)
        self.canvas.bind("<Leave>", _unbind_mousewheel)
        self.scrollable_frame.bind("<Enter>", _bind_mousewheel)
        self.scrollable_frame.bind("<Leave>", _unbind_mousewheel)
        self.bind("<Destroy>", lambda e: _unbind_mousewheel() if e.widget is self else None)


class NeonDivider(tk.Canvas):
    """Linha divisoria com efeito neon."""
    def __init__(self, master, color=COR_PRIMARIA, width_pct=0.8, **kwargs):
        super().__init__(master, height=3, bg=self._get_bg(master),
                         highlightthickness=0, **kwargs)
        self._color = color
        self._width_pct = width_pct
        self.bind("<Configure>", self._draw)

    def _get_bg(self, master):
        try:
            return master.cget("bg")
        except Exception:
            return COR_FUNDO

    def _draw(self, event=None):
        self.delete("all")
        w = self.winfo_width()
        margin = int(w * (1 - self._width_pct) / 2)
        # Linha central brilhante
        self.create_line(margin, 1, w - margin, 1, fill=self._color, width=1)
        # Glow sutil
        glow = self._darken(self._color, 80)
        self.create_line(margin + 20, 2, w - margin - 20, 2, fill=glow, width=1)

    def _darken(self, color, amount):
        try:
            r = max(0, int(color[1:3], 16) - amount)
            g = max(0, int(color[3:5], 16) - amount)
            b = max(0, int(color[5:7], 16) - amount)
            return f"#{r:02x}{g:02x}{b:02x}"
        except Exception:
            return color


class DigitalClock(tk.Label):
    """Relogio digital animado futuristico."""
    def __init__(self, master, **kwargs):
        super().__init__(master, text="", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                         font=("Consolas", 12, "bold"), **kwargs)
        self._update()

    def _update(self):
        try:
            if self.winfo_exists():
                now = datetime.datetime.now()
                time_str = now.strftime("%H:%M:%S")
                date_str = now.strftime("%d/%m/%Y")
                self.config(text=f"{Icons.CLOCK} {time_str}  {date_str}")
                self.after(1000, self._update)
        except Exception:
            pass


class StatusIndicator(tk.Canvas):
    """Indicador de status com LED animado."""
    def __init__(self, master, color=COR_SUCESSO, size=10, **kwargs):
        bg = COR_FUNDO2
        try:
            bg = master.cget("bg")
        except Exception:
            pass
        super().__init__(master, width=size + 6, height=size + 6,
                         bg=bg, highlightthickness=0, **kwargs)
        self._color = color
        self._size = size
        # Glow
        glow_r = max(0, int(color[1:3], 16) // 3)
        glow_g = max(0, int(color[3:5], 16) // 3)
        glow_b = max(0, int(color[5:7], 16) // 3)
        glow_color = f"#{glow_r:02x}{glow_g:02x}{glow_b:02x}"
        self.create_oval(1, 1, size + 5, size + 5, fill=glow_color, outline="")
        self.create_oval(3, 3, size + 3, size + 3, fill=color, outline="")


# ============================================================================
# APLICACAO PRINCIPAL - VISUAL FUTURISTICO
# ============================================================================

class PDVApp:
    def __init__(self, root):
        self.root = root
        self.root.title(APP_TITLE)
        self.root.configure(bg=COR_FUNDO)

        # Iniciar maximizado
        try:
            self.root.state("zoomed")
        except tk.TclError:
            try:
                self.root.attributes("-zoomed", True)
            except tk.TclError:
                sw = self.root.winfo_screenwidth()
                sh = self.root.winfo_screenheight()
                self.root.geometry(f"{sw}x{sh}+0+0")

        # Forcar maximizado
        def _forcar_maximizado(event=None):
            try:
                estado = self.root.state()
                if estado != "zoomed":
                    self.root.state("zoomed")
            except tk.TclError:
                try:
                    if not self.root.attributes("-zoomed"):
                        self.root.attributes("-zoomed", True)
                except tk.TclError:
                    pass
        self.root.bind("<Map>", _forcar_maximizado)

        # Estilo ttk FUTURISTICO
        self.style = ttk.Style()
        self.style.theme_use("clam")

        # Treeview futuristica
        self.style.configure("Treeview",
            background=COR_GLASS, foreground=COR_TEXTO,
            fieldbackground=COR_GLASS, font=("Segoe UI", 10),
            rowheight=32, borderwidth=0)
        self.style.configure("Treeview.Heading",
            background=COR_FUNDO3, foreground=COR_PRIMARIA,
            font=("Segoe UI", 10, "bold"), borderwidth=0,
            relief="flat")
        self.style.map("Treeview",
            background=[("selected", "#1a3a5c")],
            foreground=[("selected", COR_PRIMARIA)])
        self.style.layout("Treeview", [
            ("Treeview.treearea", {"sticky": "nswe"})
        ])

        # Combobox futuristica
        self.style.configure("TCombobox",
            fieldbackground=COR_ENTRADA, background=COR_ENTRADA,
            foreground=COR_TEXTO, borderwidth=0,
            arrowcolor=COR_PRIMARIA)
        self.style.map("TCombobox",
            fieldbackground=[("readonly", COR_ENTRADA)],
            foreground=[("readonly", COR_TEXTO)])

        # Notebook futuristico
        self.style.configure("TNotebook", background=COR_FUNDO, borderwidth=0)
        self.style.configure("TNotebook.Tab",
            background=COR_FUNDO2, foreground=COR_TEXTO2,
            padding=[14, 6], font=("Segoe UI", 10, "bold"),
            borderwidth=0)
        self.style.map("TNotebook.Tab",
            background=[("selected", COR_FUNDO3)],
            foreground=[("selected", COR_PRIMARIA)])

        # Scrollbar futuristica
        self.style.configure("Vertical.TScrollbar",
            background=COR_FUNDO2, troughcolor=COR_FUNDO,
            borderwidth=0, arrowcolor=COR_PRIMARIA)

        # Frame raiz dividido: conteudo + barra de status
        self._root_frame = tk.Frame(root, bg=COR_FUNDO)
        self._root_frame.pack(fill="both", expand=True)

        # Container principal
        self.main_container = tk.Frame(self._root_frame, bg=COR_FUNDO)
        self.main_container.pack(fill="both", expand=True)

        # Barra de status inferior
        self.status_bar = StatusBar(self._root_frame)
        self.status_bar.pack(fill="x", side="bottom")
        self.status_bar.bind_theme_click(self._show_theme_selector)

        # Toast manager
        ToastManager.set_root(self.root)

        # Particulas de fundo (referencia)
        self._particle_system = None
        self._bg_canvas = None

        # Variaveis de estado
        self.carrinho = []
        self.pagamentos_venda = []
        self.formas_pagamento_list = []
        self.caixa_aberto = None
        self.web_server = None
        self.web_server_port = 8080
        self._armario_pendente = None
        self._armario_auto_refresh_id = None
        self._ultimo_cupom = None  # Para impressao rapida Ctrl+P

        # ============================================================
        # SISTEMA DE ATUALIZACAO EM TEMPO REAL (Auto-Refresh)
        # Gerencia refresh periodico de TODAS as telas do sistema.
        # Cada tela registra sua funcao de refresh ao ser aberta;
        # clear_container cancela automaticamente ao trocar de tela.
        # ============================================================
        self._auto_refresh_ids = []       # Lista de after_ids ativos
        self._current_screen = None       # Nome da tela ativa
        self._current_refresh_fn = None   # Funcao de refresh da tela ativa
        self._current_refresh_ms = 10000  # Intervalo padrao: 10 segundos
        self._login_attempts = {}  # rate-limit de login {login: [timestamps]}
        self._licenca_recem_ativada = False  # Flag para pular re-verificacao apos ativacao
        self._licenca_monitor_ativo = False  # Flag para controlar o monitor periodico de licenca
        self._licenca_bloqueada_por_monitor = False  # Evita bloqueios duplicados

        # Atalhos de teclado
        self._shortcuts = KeyboardShortcuts(self.root, self)

        # Monitor de conexao ao banco em background
        self._start_connection_monitor()

        # Confirmacao de saida
        self.root.protocol("WM_DELETE_WINDOW", self._on_close)

        # Iniciar com tela de login
        self.show_login()

    def _on_close(self):
        """Confirmacao de fechamento com aviso se caixa estiver aberto."""
        try:
            if Session.caixa_id and self.caixa_aberto:
                if not messagebox.askyesno(
                    "Fechar PDV Pro",
                    f"Atencao! O caixa #{Session.caixa_id} ainda esta aberto.\n\n"
                    "Deseja fechar o programa mesmo assim?\n"
                    "(O caixa continuara aberto para o proximo acesso.)",
                    icon="warning"
                ):
                    return
            else:
                if not messagebox.askyesno(
                    "Fechar PDV Pro",
                    "Deseja fechar o PDV Pro?",
                ):
                    return
        except Exception:
            pass
        AuditLogger.log("SISTEMA_FECHADO",
                        usuario=getattr(Session, "user_login", ""))
        self.root.destroy()

    def _start_connection_monitor(self):
        """Verifica periodicamente a conexao com o banco de dados."""
        def _check():
            while True:
                try:
                    db = DatabaseHelper.get_instance()
                    ok = db.test_connection()
                    host = db.host if ok else ""
                    self.root.after(0, lambda o=ok, h=host:
                        self.status_bar.set_connection(o, h) if self.status_bar.winfo_exists() else None)
                except Exception:
                    self.root.after(0, lambda:
                        self.status_bar.set_connection(False) if self.status_bar.winfo_exists() else None)
                time.sleep(15)
        threading.Thread(target=_check, daemon=True).start()

    def _start_licenca_monitor(self):
        """Inicia monitor periodico de licenca em background.
        Verifica a cada 5 minutos se a licenca ainda e valida.
        Se expirada ou com fraude de data, bloqueia o sistema imediatamente.
        Resolve o problema do sistema ficar aberto apos meia-noite sem bloquear."""
        if self._licenca_monitor_ativo:
            return  # Ja esta rodando
        self._licenca_monitor_ativo = True
        self._licenca_bloqueada_por_monitor = False
        print("[LICENCA] Monitor periodico de licenca INICIADO (intervalo: 300s)")

        def _monitor():
            while self._licenca_monitor_ativo:
                try:
                    time.sleep(300)  # Verificar a cada 5 minutos
                    if not self._licenca_monitor_ativo:
                        break
                    # Verificar se usuario esta logado (so faz sentido verificar se logado)
                    if not getattr(Session, 'user_id', None):
                        continue
                    print("[LICENCA] Monitor periodico: verificando licenca...")
                    valida = LicencaManager.verificar_licenca()
                    fraude = LicencaManager._date_fraud_detected
                    if not valida or fraude:
                        motivo = "fraude de data" if fraude else "expirada/invalida"
                        print(f"[LICENCA] Monitor periodico: licenca {motivo} - BLOQUEANDO SISTEMA")
                        AuditLogger.log("LICENCA_BLOQUEIO_MONITOR",
                                        f"Sistema bloqueado pelo monitor periodico - licenca {motivo}",
                                        usuario=getattr(Session, 'user_login', ''))
                        if not self._licenca_bloqueada_por_monitor:
                            self._licenca_bloqueada_por_monitor = True
                            # Bloquear na thread principal do Tkinter
                            self.root.after(0, self._bloquear_por_licenca_monitor)
                    else:
                        self._licenca_bloqueada_por_monitor = False
                        print("[LICENCA] Monitor periodico: licenca OK")
                except Exception as e:
                    print(f"[LICENCA] Erro no monitor periodico: {e}")
            print("[LICENCA] Monitor periodico de licenca PARADO")

        threading.Thread(target=_monitor, daemon=True).start()

    def _stop_licenca_monitor(self):
        """Para o monitor periodico de licenca."""
        self._licenca_monitor_ativo = False
        print("[LICENCA] Monitor periodico de licenca solicitado a parar")

    def _bloquear_por_licenca_monitor(self):
        """Bloqueia o sistema quando o monitor periodico detecta licenca invalida.
        Chamado na thread principal do Tkinter via root.after()."""
        try:
            print("[LICENCA] Executando bloqueio por monitor periodico...")
            expirada = LicencaManager.is_licenca_expirada()
            data_exp = LicencaManager.get_data_expiracao() if expirada else None
            fraude = LicencaManager._date_fraud_detected
            # Parar o monitor para nao ficar bloqueando repetidamente
            self._stop_licenca_monitor()
            # Mostrar tela de bloqueio
            if fraude:
                # Mensagem especifica para fraude de data
                self._show_tela_bloqueio_licenca_fraude()
            else:
                self._show_tela_bloqueio_licenca(
                    expirada=expirada, data_exp=data_exp)
        except Exception as e:
            print(f"[LICENCA] Erro ao bloquear por monitor: {e}")

    def _show_tela_bloqueio_licenca_fraude(self):
        """Exibe tela de bloqueio especifica para fraude de data detectada."""
        self.clear_container()
        outer = tk.Frame(self.main_container, bg=COR_FUNDO)
        outer.place(relx=0.5, rely=0.5, anchor="center")

        # Icone de alerta
        tk.Label(outer, text="\u26A0", bg=COR_FUNDO, fg="#FF1744",
                 font=("Segoe UI", 48, "bold")).pack(pady=(0, 5))

        # Titulo
        tk.Label(outer, text="SISTEMA BLOQUEADO", bg=COR_FUNDO, fg="#FF1744",
                 font=("Segoe UI", 28, "bold")).pack(pady=(10, 5))

        # Mensagem
        msg = ("Manipulacao de data/hora do sistema detectada!\n\n"
               "O relogio do computador foi alterado para uma data\n"
               "anterior a ultima data registrada pelo sistema.\n\n"
               "Isso e considerado uma tentativa de burlar a licenca.\n\n"
               "Corrija a data e hora do computador e reinicie o sistema.\n"
               "Se o problema persistir, entre em contato com o suporte.")
        tk.Label(outer, text=msg, bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 11), justify="center").pack(pady=(5, 15))

        # Info tecnica
        try:
            data_local = datetime.date.today().strftime("%d/%m/%Y")
            last_known = LicencaManager._last_known_date
            last_str = last_known.strftime("%d/%m/%Y") if last_known else "N/A"
        except Exception:
            data_local = "N/A"
            last_str = "N/A"
        tk.Label(outer, text=f"Data do computador: {data_local}  |  Ultima data registrada: {last_str}",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(pady=(0, 10))

        tk.Label(outer, text="Entre em contato com o suporte tecnico.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(pady=(10, 3))

    def _show_theme_selector(self):
        """Exibe dialog para selecionar tema."""
        try:
            dialog = tk.Toplevel(self.root)
            dialog.title("Selecionar Tema")
            dialog.geometry("340x280")
            dialog.configure(bg=COR_FUNDO)
            dialog.transient(self.root)
            dialog.grab_set()
            dialog.resizable(False, False)

            tk.Label(dialog, text=f"{Icons.THEME} Temas",
                     bg=COR_FUNDO, fg=COR_PRIMARIA,
                     font=("Segoe UI", 14, "bold")).pack(pady=(18, 5))
            tk.Label(dialog, text="Selecione o tema visual da interface:",
                     bg=COR_FUNDO, fg=COR_TEXTO2,
                     font=("Segoe UI", 10)).pack(pady=(0, 12))

            var = tk.StringVar(value=_ACTIVE_THEME)
            for tname in THEMES:
                t = THEMES[tname]
                rb = tk.Radiobutton(dialog, text=tname, variable=var, value=tname,
                                    bg=COR_FUNDO, fg=t["primaria"],
                                    selectcolor=COR_CARD,
                                    activebackground=COR_FUNDO,
                                    font=("Segoe UI", 11, "bold"),
                                    cursor="hand2")
                rb.pack(anchor="w", padx=40, pady=3)

            def aplicar():
                nome = var.get()
                apply_theme(nome)
                # Salvar preferencia
                try:
                    cfg = {}
                    if os.path.exists(CONFIG_FILE):
                        with open(CONFIG_FILE, "r") as f:
                            cfg = json.load(f)
                    cfg["tema"] = nome
                    with open(CONFIG_FILE, "w") as f:
                        json.dump(cfg, f, indent=2)
                except Exception:
                    pass
                self.status_bar.update_theme_label()
                ToastManager.success(f"Tema '{nome}' aplicado! Reinicie para efeito completo.")
                AuditLogger.log("TEMA_ALTERADO", f"Novo tema: {nome}",
                                Session.user_login)
                dialog.destroy()

            StyledButton(dialog, text=f"{Icons.CHECK} Aplicar",
                         command=aplicar, color=COR_BOTAO_VERDE,
                         width=12).pack(pady=14)
        except Exception as e:
            ToastManager.error(str(e))

    def _create_bg_particles(self, parent):
        """Cria fundo com particulas animadas."""
        try:
            if self._particle_system:
                self._particle_system.stop()
        except Exception:
            pass

        canvas = tk.Canvas(parent, bg=COR_FUNDO, highlightthickness=0)
        canvas.place(x=0, y=0, relwidth=1, relheight=1)
        canvas.tk.call('lower', canvas._w)

        # Linhas de grade futuristica sutil
        def _draw_grid(event=None):
            canvas.delete("grid")
            w = canvas.winfo_width()
            h = canvas.winfo_height()
            grid_color = "#0d1225"
            for x in range(0, w, 60):
                canvas.create_line(x, 0, x, h, fill=grid_color, tags="grid")
            for y in range(0, h, 60):
                canvas.create_line(0, y, w, y, fill=grid_color, tags="grid")

        canvas.bind("<Configure>", _draw_grid)

        self._bg_canvas = canvas
        w = self.root.winfo_width() or 1920
        h = self.root.winfo_height() or 1080
        self._particle_system = ParticleSystem(canvas, w, h, num_particles=35, root=self.root)
        self._particle_system.animate()

        def _resize(event):
            if self._particle_system:
                self._particle_system.update_size(event.width, event.height)

        canvas.bind("<Configure>", lambda e: (_draw_grid(e), _resize(e)))

        return canvas

    def clear_container(self):
        # Parar TODOS os auto-refreshs ativos ao trocar de tela
        self._stop_all_auto_refresh()
        self._parar_auto_refresh_armarios()
        try:
            self.root.unbind_all("<MouseWheel>")
            self.root.unbind_all("<Button-4>")
            self.root.unbind_all("<Button-5>")
        except Exception:
            pass
        if self._particle_system:
            self._particle_system.stop()
            self._particle_system = None
        for widget in self.main_container.winfo_children():
            widget.destroy()

    # ====================================================================
    # SISTEMA CENTRALIZADO DE ATUALIZACAO EM TEMPO REAL
    # ====================================================================
    def _start_auto_refresh(self, screen_name, refresh_fn, interval_ms=10000):
        """Inicia atualizacao periodica para a tela atual.
        Args:
            screen_name: Nome identificador da tela (ex: 'cadastro_produto')
            refresh_fn:  Funcao a ser chamada periodicamente para atualizar dados
            interval_ms: Intervalo em milissegundos entre atualizacoes (padrao: 10s)
        """
        self._current_screen = screen_name
        self._current_refresh_fn = refresh_fn
        self._current_refresh_ms = interval_ms

        def _do_refresh():
            try:
                # Verificar se o main_container ainda existe e a tela nao mudou
                if not self.main_container.winfo_exists():
                    return
                if self._current_screen != screen_name:
                    return
                # Executar a funcao de refresh
                refresh_fn()
                # Agendar proximo refresh
                after_id = self.root.after(interval_ms, _do_refresh)
                self._auto_refresh_ids.append(after_id)
            except Exception:
                pass

        # Agendar primeiro refresh
        after_id = self.root.after(interval_ms, _do_refresh)
        self._auto_refresh_ids.append(after_id)

    def _stop_all_auto_refresh(self):
        """Para todos os auto-refreshs ativos."""
        for after_id in self._auto_refresh_ids:
            try:
                self.root.after_cancel(after_id)
            except Exception:
                pass
        self._auto_refresh_ids.clear()
        self._current_screen = None
        self._current_refresh_fn = None

    def show_loading(self, message="Carregando..."):
        self.loading_label = tk.Label(self.main_container, text=f"{Icons.REFRESH} {message}",
            bg=COR_FUNDO, fg=COR_PRIMARIA, font=("Segoe UI", 12, "bold"))
        self.loading_label.place(relx=0.5, rely=0.95, anchor="center")

    def hide_loading(self):
        try:
            if hasattr(self, 'loading_label'):
                self.loading_label.destroy()
        except Exception:
            pass

    def show_error(self, message):
        """Exibe erro via toast (nao-bloqueante) e messagebox com mensagem amigavel.
        A mensagem e exibida de forma clara e profissional."""
        primeira_linha = message.split(chr(10))[0] if message else "Erro desconhecido"
        self.root.after(0, lambda: ToastManager.error(primeira_linha, duration=5000))
        self.root.after(0, lambda: messagebox.showerror("Ops! Algo deu errado", message))

    def show_success(self, message):
        """Exibe sucesso via toast (nao-bloqueante)."""
        self.root.after(0, lambda: ToastManager.success(message))

    def show_info_dialog(self, title, message):
        self.root.after(0, lambda: messagebox.showinfo(title, message))

    def show_toast(self, message, toast_type="info", duration=3000):
        """Exibe apenas toast, sem messagebox (para operacoes silenciosas)."""
        self.root.after(0, lambda: ToastManager.show(message, toast_type, duration))

    def run_async(self, func, callback=None, error_callback=None, retry_on_connection=True):
        """Executa funcao em thread separada com tratamento de erros robusto.
        - callback: chamado com o resultado em caso de sucesso
        - error_callback: chamado com a excecao em caso de erro
        - retry_on_connection: se True, tenta novamente em erros de conexao (1x)
        """
        func_name = func.__name__ if hasattr(func, '__name__') else 'operacao'
        def wrapper():
            tentativas = 2 if retry_on_connection else 1
            ultima_excecao = None
            for tentativa in range(tentativas):
                try:
                    result = func()
                    if callback:
                        def _safe_callback():
                            try:
                                callback(result)
                            except tk.TclError:
                                pass  # Widget destruido antes do callback executar
                            except Exception as cb_err:
                                ErroAmigavel.log_erro(cb_err, f"callback de {func_name}")
                        self.root.after(0, _safe_callback)
                    return  # Sucesso - sair do loop
                except Exception as e:
                    ultima_excecao = e
                    ErroAmigavel.log_erro(e, f"run_async({func_name}) tentativa {tentativa+1}/{tentativas}")
                    # Verificar se e erro de conexao e pode tentar novamente
                    is_conn_error = False
                    if hasattr(e, 'errno') and e.errno in (2002, 2003, 2006, 2013):
                        is_conn_error = True
                    elif type(e).__name__ in ('ConnectionRefusedError', 'ConnectionResetError',
                                               'ConnectionAbortedError', 'BrokenPipeError', 'TimeoutError'):
                        is_conn_error = True
                    elif any(kw in str(e).lower() for kw in ('connection', 'timeout', 'not connected')):
                        is_conn_error = True

                    if is_conn_error and tentativa < tentativas - 1:
                        # Tentar reconectar antes de retry
                        try:
                            db = DatabaseHelper.get_instance()
                            db._safe_reconnect()
                        except Exception:
                            pass
                        time.sleep(1)  # Aguardar 1 segundo antes de tentar novamente
                        continue  # Tentar novamente
                    break  # Nao e erro de conexao ou ultima tentativa

            # Se chegou aqui, todas as tentativas falharam
            e = ultima_excecao
            if error_callback:
                def _safe_error():
                    try:
                        error_callback(e)
                    except tk.TclError:
                        pass
                self.root.after(0, _safe_error)
            else:
                def _show_friendly_error():
                    msg, cod, det = ErroAmigavel.traduzir(e)
                    severidade = ErroAmigavel.obter_severidade(e)
                    # Toast com primeira linha
                    primeira_linha = msg.split(chr(10))[0]
                    try:
                        if severidade in (Severidade.CRITICO, Severidade.ALTO):
                            ToastManager.error(f"{primeira_linha} ({cod})", duration=5000)
                        else:
                            ToastManager.warning(f"{primeira_linha} ({cod})", duration=4000)
                    except Exception:
                        pass
                    # Messagebox com mensagem completa
                    self.show_error(
                        f"{msg}\n\n"
                        f"{'='*40}\n"
                        f"Codigo do erro: {cod}\n"
                        f"Informe este codigo ao suporte se precisar de ajuda.")
                self.root.after(0, _show_friendly_error)
        threading.Thread(target=wrapper, daemon=True).start()

    def _sort_tree(self, tree, col, reverse):
        """Ordena Treeview pelo coluna clicada. Alterna asc/desc."""
        try:
            data = [(tree.set(child, col), child)
                    for child in tree.get_children("")]
            try:
                data.sort(key=lambda x: float(
                    x[0].replace("R$", "").replace(".", "").replace(",", ".").strip()),
                    reverse=reverse)
            except (ValueError, AttributeError):
                data.sort(key=lambda x: x[0].lower(), reverse=reverse)
            for idx, (_, child) in enumerate(data):
                tree.move(child, "", idx)
                tag = "even" if idx % 2 == 0 else "odd"
                tree.item(child, tags=(tag,))
            tree.tag_configure("even", background=COR_GLASS)
            tree.tag_configure("odd", background=COR_CARD)
            tree.heading(col,
                command=lambda c=col: self._sort_tree(tree, c, not reverse))
        except Exception:
            pass

    def _apply_zebra(self, tree):
        """Aplica listras zebra em qualquer Treeview."""
        try:
            for i, child in enumerate(tree.get_children()):
                tag = "even" if i % 2 == 0 else "odd"
                current = list(tree.item(child, "tags"))
                if "even" in current:
                    current.remove("even")
                if "odd" in current:
                    current.remove("odd")
                current.append(tag)
                tree.item(child, tags=current)
            tree.tag_configure("even", background=COR_GLASS)
            tree.tag_configure("odd", background=COR_CARD)
        except Exception:
            pass

    def _create_top_bar(self, title, show_back=True, back_command=None):
        """Cria barra superior futuristica padrao."""
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))

        # Linha neon superior
        neon_line = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        neon_line.pack(fill="x", side="top")
        neon_line.bind("<Configure>", lambda e: (
            neon_line.delete("all"),
            neon_line.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)
        ))

        if show_back:
            cmd = back_command or self.show_main_menu
            StyledButton(top, text=f"{Icons.VOLTAR} Voltar", command=cmd,
                         color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=8)

        tk.Label(top, text=title, bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)

        # Botoes Sair e Trocar de Usuario na barra superior
        btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        btn_sair.pack(side="right", padx=5, pady=8)
        add_tooltip(btn_sair, "Fechar o PDV Pro (Alt+F4)")

        btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        btn_trocar.pack(side="right", padx=5, pady=8)
        add_tooltip(btn_trocar, "Trocar de usuario (Ctrl+L)")

        return top

    def _create_futuristic_header(self, parent, title, subtitle=""):
        """Cria cabecalho futuristico com efeito neon."""
        header = tk.Frame(parent, bg=COR_FUNDO)
        header.pack(fill="x", pady=(10, 5))

        tk.Label(header, text=title, bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(anchor="w")

        if subtitle:
            tk.Label(header, text=subtitle, bg=COR_FUNDO, fg=COR_TEXTO2,
                     font=("Segoe UI", 10)).pack(anchor="w")

        NeonDivider(header, color=COR_PRIMARIA).pack(fill="x", pady=(5, 0))

        return header



    def _solicitar_senha_admin(self, callback, parent_window=None):
        """Exibe um dialog solicitando a senha de um administrador."""
        if Session.is_admin():
            callback()
            return

        parent = parent_window or self.root
        dialog = tk.Toplevel(parent)
        dialog.title("Autorizacao de Administrador")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(parent)
        dialog.grab_set()
        dialog.resizable(False, False)

        dlg_w = 420
        dlg_h = 340
        screen_w = dialog.winfo_screenwidth()
        screen_h = dialog.winfo_screenheight()
        pos_x = screen_w - dlg_w - 30
        pos_y = (screen_h - dlg_h) // 2
        dialog.geometry(f"{dlg_w}x{dlg_h}+{pos_x}+{pos_y}")

        tk.Label(dialog, text=f"{Icons.LOCK} Autorizacao Necessaria", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=(15, 5))
        tk.Label(dialog, text="Esta acao requer permissao de administrador.\n"
                              "Digite a senha de um administrador para continuar.",
                 bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 10),
                 justify="center").pack(pady=(0, 10))

        card = GlassCard(dialog, glow_color=COR_ACCENT)
        card.pack(padx=25, pady=5, fill="x")

        tk.Label(card, text="Senha do Administrador:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 11)).pack(anchor="w", pady=(8, 2))
        et_senha = StyledEntry(card, show="*")
        et_senha.pack(fill="x", pady=(0, 10))
        et_senha.focus_set()

        lbl_erro = tk.Label(dialog, text="", bg=COR_FUNDO, fg=COR_ERRO,
                             font=("Segoe UI", 9, "italic"))
        lbl_erro.pack(pady=(0, 5))

        def validar(event=None):
            senha = et_senha.get().strip()
            if not senha:
                lbl_erro.config(text="Digite a senha.")
                return
            def verificar():
                db = DatabaseHelper.get_instance()
                rows = db.execute_query(
                    "SELECT id, nome FROM usuarios WHERE senha = %s AND nivel = 'admin' AND ativo = 1",
                    (senha,)
                )
                return rows
            def on_result(rows):
                if rows:
                    dialog.destroy()
                    callback()
                else:
                    lbl_erro.config(text="Senha invalida ou nao pertence a um administrador.")
                    et_senha.delete(0, "end")
                    et_senha.focus_set()
            self.run_async(verificar, on_result)

        et_senha.bind("<Return>", validar)

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=25, pady=(0, 15))
        StyledButton(btn_frame, text=f"{Icons.CHECK} Autorizar", command=validar,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.CROSS} Cancelar", command=dialog.destroy,
                     color="#2a3a5c", width=12).pack(side="right", padx=5)

        def trocar_usuario():
            dialog.destroy()
            self.do_logoff()

        StyledButton(btn_frame, text=f"{Icons.TROCAR} Trocar", command=trocar_usuario,
                     color=COR_BOTAO_LARANJA, width=12).pack(side="right", padx=5)

    def _solicitar_senha_admin_obrigatoria(self, callback, parent_window=None):
        """Exibe um dialog solicitando a senha de um administrador.
        SEMPRE exige a senha, mesmo que o usuario logado seja admin.
        Usado para acoes criticas de armario (excluir item, devolver chave, manutencao)."""
        parent = parent_window or self.root
        dialog = tk.Toplevel(parent)
        dialog.title("Autorizacao de Administrador")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(parent)
        dialog.grab_set()
        dialog.resizable(False, False)

        dlg_w = 420
        dlg_h = 340
        screen_w = dialog.winfo_screenwidth()
        screen_h = dialog.winfo_screenheight()
        pos_x = screen_w - dlg_w - 30
        pos_y = (screen_h - dlg_h) // 2
        dialog.geometry(f"{dlg_w}x{dlg_h}+{pos_x}+{pos_y}")

        tk.Label(dialog, text=f"{Icons.LOCK} Autorizacao Obrigatoria", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=(15, 5))
        tk.Label(dialog, text="Esta acao requer autorizacao de administrador.\n"
                              "Digite a senha de um administrador para continuar.",
                 bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 10),
                 justify="center").pack(pady=(0, 10))

        card = GlassCard(dialog, glow_color=COR_ACCENT)
        card.pack(padx=25, pady=5, fill="x")

        tk.Label(card, text="Senha do Administrador:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 11)).pack(anchor="w", pady=(8, 2))
        et_senha = StyledEntry(card, show="*")
        et_senha.pack(fill="x", pady=(0, 10))
        et_senha.focus_set()

        lbl_erro = tk.Label(dialog, text="", bg=COR_FUNDO, fg=COR_ERRO,
                             font=("Segoe UI", 9, "italic"))
        lbl_erro.pack(pady=(0, 5))

        def validar(event=None):
            senha = et_senha.get().strip()
            if not senha:
                lbl_erro.config(text="Digite a senha.")
                return
            def verificar():
                db = DatabaseHelper.get_instance()
                rows = db.execute_query(
                    "SELECT id, nome FROM usuarios WHERE senha = %s AND nivel = 'admin' AND ativo = 1",
                    (senha,)
                )
                return rows
            def on_result(rows):
                if rows:
                    dialog.destroy()
                    callback()
                else:
                    lbl_erro.config(text="Senha invalida ou nao pertence a um administrador.")
                    et_senha.delete(0, "end")
                    et_senha.focus_set()
            self.run_async(verificar, on_result)

        et_senha.bind("<Return>", validar)

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=25, pady=(0, 15))
        StyledButton(btn_frame, text=f"{Icons.CHECK} Autorizar", command=validar,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.CROSS} Cancelar", command=dialog.destroy,
                     color="#2a3a5c", width=12).pack(side="right", padx=5)

    # ========================================================================
    # TELA DE LOGIN - FUTURISTICA
    # ========================================================================
    def show_login(self):
        self.clear_container()
        Session.clear()

        # Fundo com particulas
        self._create_bg_particles(self.main_container)

        # Container central
        frame = tk.Frame(self.main_container, bg=COR_FUNDO)
        frame.place(relx=0.5, rely=0.5, anchor="center")

        # Logo futuristico
        logo_frame = tk.Frame(frame, bg=COR_FUNDO)
        logo_frame.pack(pady=(0, 10))

        # Hexagono decorativo com Canvas
        hex_canvas = tk.Canvas(logo_frame, width=80, height=80, bg=COR_FUNDO, highlightthickness=0)
        hex_canvas.pack()
        # Desenhar hexagono
        cx, cy, r = 40, 40, 35
        points = []
        for i in range(6):
            angle = math.radians(60 * i - 30)
            points.extend([cx + r * math.cos(angle), cy + r * math.sin(angle)])
        hex_canvas.create_polygon(points, outline=COR_PRIMARIA, fill="", width=2)
        hex_canvas.create_polygon(points, outline="", fill=COR_FUNDO3)
        # Icone central
        hex_canvas.create_text(cx, cy, text=Icons.MONEY, fill=COR_PRIMARIA,
                              font=("Segoe UI", 20, "bold"))

        # Titulo com efeito
        title_label = tk.Label(frame, text="PDV Pro", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 40, "bold"))
        title_label.pack(pady=(5, 0))

        # Subtitulo
        tk.Label(frame, text=f"v{APP_VERSION}  |  Sistema de Ponto de Venda",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11)).pack(pady=(0, 5))

        # Linha neon decorativa
        NeonDivider(frame, color=COR_PRIMARIA, width_pct=0.6).pack(fill="x", padx=40, pady=(5, 20))

        # Card de login glassmorphism
        card = GlassCard(frame, glow_color=COR_PRIMARIA)
        card.pack(padx=40, pady=5, ipadx=30, ipady=20)

        # Titulo do card
        tk.Label(card, text=f"{Icons.LOCK} Acesso ao Sistema", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 13, "bold")).pack(anchor="w", pady=(5, 15))

        # Usuario
        tk.Label(card, text=f"{Icons.USER} Usuario:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(anchor="w", pady=(0, 3))
        self.login_combo = ttk.Combobox(card, width=32, state="readonly",
                                         font=("Segoe UI", 11))
        self.login_combo.pack(pady=(0, 12), fill="x")

        # Senha
        tk.Label(card, text=f"{Icons.LOCK} Senha:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(anchor="w", pady=(0, 3))

        senha_frame = tk.Frame(card, bg=COR_GLASS)
        senha_frame.pack(fill="x", pady=(0, 18))
        self.login_senha = StyledEntry(senha_frame, show="\u2022", width=28)
        self.login_senha.pack(side="left", fill="x", expand=True)
        self.senha_visivel = False
        btn_eye = tk.Button(senha_frame, text="\u25C9", bg=COR_ENTRADA, fg=COR_PRIMARIA,
                            relief="flat", font=("Segoe UI", 14), cursor="hand2",
                            activebackground=COR_ENTRADA, activeforeground=COR_ACCENT,
                            bd=0, command=self.toggle_senha)
        btn_eye.pack(side="right", padx=(5, 0))

        # Botao entrar
        StyledButton(card, text=f"{Icons.ARROW_R}  ENTRAR", command=self.do_login,
                     color=COR_PRIMARIA, width=32).pack(pady=(5, 8))

        # Links
        links_frame = tk.Frame(card, bg=COR_GLASS)
        links_frame.pack(fill="x", pady=(8, 0))

        link_config = tk.Label(links_frame, text=f"{Icons.CONFIG} Configurar Banco",
                               bg=COR_GLASS, fg=COR_ACCENT2,
                               font=("Segoe UI", 9, "underline"), cursor="hand2")
        link_config.pack(side="left")
        link_config.bind("<Button-1>", lambda e: self.show_config_banco())
        link_config.bind("<Enter>", lambda e: link_config.config(fg=COR_PRIMARIA))
        link_config.bind("<Leave>", lambda e: link_config.config(fg=COR_ACCENT2))

        link_trocar = tk.Label(links_frame, text=f"{Icons.TROCAR} Trocar Senha",
                               bg=COR_GLASS, fg=COR_ACCENT2,
                               font=("Segoe UI", 9, "underline"), cursor="hand2")
        link_trocar.pack(side="right")
        link_trocar.bind("<Button-1>", lambda e: self.show_trocar_senha_dialog())
        link_trocar.bind("<Enter>", lambda e: link_trocar.config(fg=COR_PRIMARIA))
        link_trocar.bind("<Leave>", lambda e: link_trocar.config(fg=COR_ACCENT2))

        # Bind Enter
        self.login_senha.bind("<Return>", lambda e: self.do_login())

        # Rodape
        tk.Label(frame, text="Powered by PDV Pro Technology", bg=COR_FUNDO, fg="#2a3a5c",
                 font=("Segoe UI", 8)).pack(pady=(20, 0))

        # Carregar usuarios
        self.carregar_usuarios_login()

    def toggle_senha(self):
        self.senha_visivel = not self.senha_visivel
        self.login_senha.config(show="" if self.senha_visivel else "\u2022")

    def carregar_usuarios_login(self):
        def load():
            try:
                db = DatabaseHelper.get_instance()
                db.create_tables()
                return db.execute_query(
                    "SELECT login, nome FROM usuarios WHERE ativo = 1 ORDER BY nome ASC"
                )
            except Exception as e:
                ErroAmigavel.log_erro(e, "carregar_usuarios_login")
                return []

        def on_loaded(users):
            # Verificar se o widget ainda existe (pode ter sido destruido
            # se o usuario navegou para outra tela durante o carregamento)
            try:
                if not hasattr(self, 'login_combo') or not self.login_combo.winfo_exists():
                    return
            except Exception:
                return
            if users:
                items = []
                self._login_map = {}
                for u in users:
                    nome = u.get("nome", "")
                    login = u.get("login", "")
                    if nome and nome.lower() != login.lower():
                        display = f"{nome} ({login})"
                    else:
                        display = login
                    items.append(display)
                    self._login_map[display] = login
                try:
                    self.login_combo["values"] = items
                    if items:
                        self.login_combo.current(0)
                except Exception:
                    pass  # Widget destruido durante a atribuicao
            else:
                try:
                    self.login_combo["values"] = ["Nenhum usuario encontrado"]
                except Exception:
                    pass
                self._login_map = {}

        self.run_async(load, on_loaded)

    def do_login(self):
        sel = self.login_combo.get()
        login = self._login_map.get(sel, "")
        senha = self.login_senha.get().strip()

        if not login:
            self.show_error(
                "Nenhum usuario foi encontrado no sistema.\n\n"
                "Possiveis causas:\n"
                "- O banco de dados nao esta conectado\n"
                "- O banco de dados esta vazio (primeiro uso)\n"
                "- As configuracoes do banco estao incorretas\n\n"
                "Clique em 'Configurar Banco' para verificar a conexao.")
            return
        if not senha:
            ToastManager.warning("Por favor, digite a senha.")
            return

        # Rate-limit: max 5 tentativas em 60 segundos por login
        now = time.time()
        attempts = self._login_attempts.get(login, [])
        attempts = [t for t in attempts if now - t < 60]
        if len(attempts) >= 5:
            msg = ("Voce fez muitas tentativas de login seguidas.\n"
                   "Por seguranca, aguarde 1 minuto antes de tentar novamente.\n\n"
                   "Se esqueceu sua senha, peca ajuda ao administrador do sistema.")
            ToastManager.error(msg, duration=6000)
            AuditLogger.log("LOGIN_BLOQUEADO", f"Login: {login} - rate limit atingido")
            return
        attempts.append(now)
        self._login_attempts[login] = attempts

        def authenticate():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT id, nome, nivel, perfil_id FROM usuarios "
                "WHERE login = %s AND senha = %s AND ativo = 1",
                (login, senha)
            )
            return rows

        def on_success(rows):
            if rows:
                user = rows[0]
                Session.user_id = user["id"]
                Session.user_nome = user["nome"]
                Session.user_nivel = user["nivel"]
                Session.user_login = login
                Session.user_perfil_id = user.get("perfil_id") or 0
                Session.load_permissions()
                # Limpar tentativas apos login bem-sucedido
                self._login_attempts.pop(login, None)
                # Atualizar barra de status
                self.status_bar.set_user(user["nome"], user["nivel"])
                self.status_bar.set_caixa(False)
                AuditLogger.log("LOGIN_OK", f"Usuario: {user['nome']} ({user['nivel']})",
                                usuario=login)
                ToastManager.success(f"Bem-vindo, {user['nome']}!")
                # === VERIFICACAO DE LICENCA APOS LOGIN (igual ao Android) ===
                self._verificar_licenca_pos_login()
            else:
                AuditLogger.log("LOGIN_FALHA", f"Login: {login} - senha incorreta")
                ToastManager.error("Senha incorreta. Verifique e tente novamente.", duration=4000)
                messagebox.showerror(
                    "Senha Incorreta",
                    "A senha digitada esta incorreta.\n\n"
                    "POSSIVEIS CAUSAS:\n"
                    "- O Caps Lock (letras maiusculas) pode estar ativado\n"
                    "- A tecla Num Lock pode estar desativada\n"
                    "- A senha pode ter sido digitada com espacos extras\n\n"
                    "COMO RESOLVER:\n"
                    "1. Verifique se o Caps Lock esta DESLIGADO\n"
                    "2. Digite a senha com cuidado, letra por letra\n"
                    "3. Se esqueceu a senha, peca ao administrador para redefinir\n"
                    "4. O administrador pode trocar sua senha no menu de usuarios")

        self.run_async(authenticate, on_success)

    def show_trocar_senha_dialog(self):
        dialog = tk.Toplevel(self.root)
        dialog.title("Trocar Senha")
        dialog.geometry("440x340")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text=f"{Icons.LOCK} Trocar Senha", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=15)

        card = GlassCard(dialog)
        card.pack(padx=20, pady=10, fill="x")

        tk.Label(card, text="Login:", bg=COR_GLASS, fg=COR_TEXTO).pack(anchor="w")
        et_login = StyledEntry(card)
        et_login.pack(fill="x", pady=(0, 8))

        tk.Label(card, text="Senha Atual:", bg=COR_GLASS, fg=COR_TEXTO).pack(anchor="w")
        et_atual = StyledEntry(card, show="\u2022")
        et_atual.pack(fill="x", pady=(0, 8))

        tk.Label(card, text="Nova Senha:", bg=COR_GLASS, fg=COR_TEXTO).pack(anchor="w")
        et_nova = StyledEntry(card, show="\u2022")
        et_nova.pack(fill="x", pady=(0, 10))

        def trocar():
            lg = et_login.get().strip()
            atual = et_atual.get().strip()
            nova = et_nova.get().strip()
            if not lg or not atual or not nova:
                messagebox.showwarning(
                    "Campos Obrigatorios",
                    "Todos os campos precisam ser preenchidos para trocar a senha.\n\n"
                    "PREENCHA:\n"
                    "1. Login do usuario - seu nome de acesso ao sistema\n"
                    "2. Senha atual - a senha que voce usa hoje\n"
                    "3. Nova senha - a senha que deseja usar\n\n"
                    "Se nao lembra seu login, peca ajuda ao administrador.",
                    parent=dialog)
                return
            try:
                db = DatabaseHelper.get_instance()
                rows = db.execute_query(
                    "SELECT id FROM usuarios WHERE login = %s AND senha = %s AND ativo = 1",
                    (lg, atual)
                )
                if rows:
                    db.execute_update(
                        "UPDATE usuarios SET senha = %s WHERE id = %s",
                        (nova, rows[0]["id"])
                    )
                    messagebox.showinfo(
                        "Senha Alterada",
                        "Sua senha foi alterada com sucesso!\n\n"
                        "A partir de agora, use a nova senha para fazer login.\n"
                        "Guarde sua nova senha em um local seguro.",
                        parent=dialog)
                    dialog.destroy()
                else:
                    messagebox.showerror(
                        "Dados Incorretos",
                        "O login ou a senha atual estao incorretos.\n\n"
                        "COMO RESOLVER:\n"
                        "1. Verifique se o login esta correto\n"
                        "2. Verifique se a senha atual esta correta\n"
                        "3. Cuidado com Caps Lock e espacos extras\n"
                        "4. Se esqueceu a senha, peca ao administrador para redefinir\n\n"
                        "O administrador pode trocar sua senha pelo menu de usuarios.",
                        parent=dialog)
            except Exception as e:
                ErroAmigavel.mostrar_erro(e, "trocar_senha", parent=dialog)

        StyledButton(card, text=f"{Icons.SAVE} Trocar Senha", command=trocar).pack(pady=5)

    # ========================================================================
    # CONFIGURACAO DO BANCO DE DADOS
    # ========================================================================
    def show_config_banco(self):
        dialog = tk.Toplevel(self.root)
        dialog.title("Configuracao do Banco de Dados")
        dialog.geometry("480x440")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text=f"{Icons.CONFIG} Configuracao MySQL", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=15)

        card = GlassCard(dialog)
        card.pack(padx=20, pady=10, fill="x")

        db = DatabaseHelper.get_instance()
        fields = [
            ("Host:", db.host),
            ("Banco:", db.database),
            ("Usuario:", db.username),
            ("Senha:", db.password),
            ("Porta:", str(db.port))
        ]
        entries = []
        for label, value in fields:
            tk.Label(card, text=label, bg=COR_GLASS, fg=COR_TEXTO,
                     font=("Segoe UI", 10)).pack(anchor="w")
            e = StyledEntry(card)
            e.insert(0, value)
            if label == "Senha:":
                e.config(show="\u2022")
            e.pack(fill="x", pady=(0, 8))
            entries.append(e)

        btn_frame = tk.Frame(card, bg=COR_GLASS)
        btn_frame.pack(fill="x", pady=10)

        def testar():
            try:
                ok = db.test_connection(
                    entries[0].get(), entries[1].get(),
                    entries[2].get(), entries[3].get(),
                    int(entries[4].get())
                )
                if ok:
                    messagebox.showinfo(
                        "Conexao OK",
                        "Conexao com o banco de dados realizada com sucesso!\n\n"
                        "O sistema conseguiu se comunicar com o MySQL.\n"
                        "Clique em 'Salvar' para gravar estas configuracoes.",
                        parent=dialog)
                else:
                    messagebox.showerror(
                        "Falha na Conexao",
                        "Nao foi possivel conectar ao banco de dados.\n\n"
                        "VERIFIQUE:\n"
                        "1. O MySQL esta instalado e rodando?\n"
                        "2. O endereco (Host) esta correto?\n"
                        "3. O usuario e senha do banco estao corretos?\n"
                        "4. A porta esta correta? (padrao: 3306)\n"
                        "5. O firewall nao esta bloqueando?\n\n"
                        "DICA: Se o banco esta neste computador,\n"
                        "use 'localhost' no campo Host.",
                        parent=dialog)
            except Exception as e:
                ErroAmigavel.mostrar_erro(e, "operacao_dialog", parent=dialog)

        def salvar():
            try:
                db.save_config(
                    entries[0].get(), entries[1].get(),
                    entries[2].get(), entries[3].get(),
                    int(entries[4].get())
                )
                messagebox.showinfo(
                    "Configuracao Salva",
                    "As configuracoes do banco de dados foram salvas com sucesso!\n\n"
                    "O sistema ira se reconectar usando as novas configuracoes.",
                    parent=dialog)
                dialog.destroy()
                self.carregar_usuarios_login()
            except Exception as e:
                ErroAmigavel.mostrar_erro(e, "operacao_dialog", parent=dialog)

        StyledButton(btn_frame, text=f"{Icons.REFRESH} Testar", command=testar,
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.SAVE} Salvar", command=salvar,
                     color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=5)

    def do_logoff(self):
        """Realiza a troca de usuario, desconectando o atual e retornando a tela de login."""
        if messagebox.askyesno("Trocar Usuario",
                f"Deseja desconectar o usuario '{Session.user_nome}'?\n\n"
                "Voce sera redirecionado para a tela de login."):
            AuditLogger.log("LOGOFF", usuario=Session.user_login)
            self.show_login()

    # ========================================================================
    # SISTEMA DE LICENCA - VERIFICACAO E BLOQUEIO
    # ========================================================================

    def _verificar_licenca_pos_login(self):
        """Verifica a licenca apos login bem-sucedido.
        Equivalente ao fluxo do LoginActivity.java do Android:
        - Se licenca valida: vai para o menu principal
        - Se licenca expirada: mostra tela de bloqueio com info de vencimento
        - Se licenca faltante/invalida: mostra tela de bloqueio"""
        def check():
            try:
                # Forcar recarga do cache do disco (garante que nao usa cache stale em memoria)
                # Isso e essencial apos reiniciar o app, quando _cache e None
                if LicencaManager._cache is None:
                    print("[LICENCA] Cache em memoria vazio - forcando leitura do disco...")
                    LicencaManager._carregar_cache_local()

                valida = LicencaManager.verificar_licenca()
                expirada = False
                data_exp = None
                if not valida:
                    expirada = LicencaManager.is_licenca_expirada()
                    if expirada:
                        data_exp = LicencaManager.get_data_expiracao()
                return {"valida": valida, "expirada": expirada, "data_exp": data_exp}
            except Exception as e:
                print(f"[LICENCA] Erro ao verificar licenca pos-login: {e}")
                import traceback
                traceback.print_exc()
                # Em caso de erro, tentar verificar apenas o cache local
                try:
                    cache_ok = LicencaManager._verificar_cache_local()
                    if cache_ok:
                        print("[LICENCA] Fallback: cache local valido apos erro")
                        return {"valida": True, "expirada": False, "data_exp": None}
                except Exception:
                    pass
                return {"valida": False, "expirada": False, "data_exp": None}

        def on_result(result):
            if result["valida"]:
                # Verificar se houve fraude de data mesmo com licenca "valida"
                if LicencaManager._date_fraud_detected:
                    AuditLogger.log("LICENCA_BLOQUEIO",
                                    "Sistema bloqueado - fraude de data detectada",
                                    usuario=Session.user_login)
                    self._show_tela_bloqueio_licenca_fraude()
                    return
                # Setar flag para pular re-verificacao no show_main_menu
                # (ja verificamos agora, nao precisa verificar de novo)
                self._licenca_recem_ativada = True
                AuditLogger.log("LICENCA_OK", "Licenca valida - sistema habilitado",
                                usuario=Session.user_login)
                # Iniciar monitor periodico de licenca (verifica a cada 5 min)
                self._start_licenca_monitor()
                self.show_main_menu()
            else:
                motivo = "expirada" if result["expirada"] else "invalida/faltante"
                AuditLogger.log("LICENCA_BLOQUEIO",
                                f"Sistema bloqueado - licenca {motivo}",
                                usuario=Session.user_login)
                self._show_tela_bloqueio_licenca(
                    expirada=result["expirada"],
                    data_exp=result["data_exp"]
                )

        self.run_async(check, on_result)

    def _sincronizar_status_caixa(self):
        """Sincroniza o status do caixa com o banco de dados ao entrar no menu principal.
        Garante que Session.caixa_id esteja sempre atualizado, evitando inconsistencias
        caso o caixa tenha sido fechado por outro terminal ou sessao."""
        def check():
            try:
                db = DatabaseHelper.get_instance()
                rows = db.execute_query(
                    "SELECT id FROM caixa WHERE status = 'aberto' ORDER BY id DESC LIMIT 1"
                )
                return rows
            except Exception:
                return None

        def on_result(rows):
            try:
                if rows:
                    Session.caixa_id = rows[0]["id"]
                    self.status_bar.set_caixa(True, Session.caixa_id)
                else:
                    Session.caixa_id = 0
                    self.caixa_aberto = None
                    self.status_bar.set_caixa(False)
            except Exception:
                pass

        self.run_async(check, on_result)

    def _verificar_licenca_menu(self):
        """Verifica a licenca ao acessar o menu principal (equivalente ao onResume do Android).
        Se invalida, bloqueia o sistema.
        Se a licenca foi recem-ativada, pula a re-verificacao para evitar race condition."""
        # Se a licenca acabou de ser ativada, pular a verificacao
        # e resetar o flag para que proximas chamadas verifiquem normalmente
        if self._licenca_recem_ativada:
            self._licenca_recem_ativada = False
            return

        def check():
            try:
                return LicencaManager.verificar_licenca()
            except Exception as e:
                print(f"[LICENCA] Erro em verificar_licenca_menu: {e}")
                # Fallback: se cache local e valido, nao bloquear
                try:
                    if LicencaManager._verificar_cache_local():
                        print("[LICENCA] Fallback menu: cache local valido")
                        return True
                except Exception:
                    pass
                return False

        def on_result(valida):
            if not valida or LicencaManager._date_fraud_detected:
                if LicencaManager._date_fraud_detected:
                    AuditLogger.log("LICENCA_BLOQUEIO",
                                    "Sistema bloqueado durante uso - fraude de data",
                                    usuario=Session.user_login)
                    self._show_tela_bloqueio_licenca_fraude()
                else:
                    expirada = LicencaManager.is_licenca_expirada()
                    data_exp = LicencaManager.get_data_expiracao() if expirada else None
                    AuditLogger.log("LICENCA_BLOQUEIO",
                                    "Sistema bloqueado durante uso - licenca invalida",
                                    usuario=Session.user_login)
                    self._show_tela_bloqueio_licenca(
                        expirada=expirada, data_exp=data_exp)

        self.run_async(check, on_result)

    def _show_tela_bloqueio_licenca(self, expirada=False, data_exp=None):
        """Exibe a tela de bloqueio por licenca invalida/faltante/expirada.
        O usuario NAO pode usar o sistema ate ativar uma licenca valida.
        Equivalente ao comportamento do LicencaActivity + bloqueio do Android."""
        self.clear_container()

        # Fundo com particulas (visual mantido)
        self._create_bg_particles(self.main_container)

        # Container central com scroll
        outer = tk.Frame(self.main_container, bg=COR_FUNDO)
        outer.place(relx=0.5, rely=0.5, anchor="center")

        # ---- Icone de bloqueio ----
        icon_frame = tk.Frame(outer, bg=COR_FUNDO)
        icon_frame.pack(pady=(0, 5))

        block_canvas = tk.Canvas(icon_frame, width=90, height=90, bg=COR_FUNDO,
                                  highlightthickness=0)
        block_canvas.pack()
        # Circulo vermelho
        block_canvas.create_oval(5, 5, 85, 85, outline=COR_ERRO, fill="", width=3)
        block_canvas.create_text(45, 45, text=Icons.BLOCKED, fill=COR_ERRO,
                                  font=("Segoe UI", 32, "bold"))

        # ---- Titulo de bloqueio ----
        tk.Label(outer, text="SISTEMA BLOQUEADO", bg=COR_FUNDO, fg=COR_ERRO,
                 font=("Segoe UI", 28, "bold")).pack(pady=(10, 5))

        # ---- Mensagem principal ----
        if expirada and data_exp:
            # Formatar data para DD/MM/AAAA
            try:
                dt = datetime.datetime.strptime(data_exp, "%Y-%m-%d")
                data_fmt = dt.strftime("%d/%m/%Y")
            except Exception:
                data_fmt = data_exp
            dias = LicencaManager.get_dias_para_vencimento()
            msg_principal = (
                f"Sua licenca esta VENCIDA desde {data_fmt}\n"
                f"({abs(dias)} dia(s) apos o vencimento)\n\n"
                "O sistema esta bloqueado por licenca expirada.\n"
                "Procure o suporte para renovar sua licenca."
            )
        elif expirada:
            msg_principal = (
                "Sua licenca esta VENCIDA.\n\n"
                "O sistema esta bloqueado por licenca expirada.\n"
                "Procure o suporte para renovar sua licenca."
            )
        else:
            msg_principal = (
                "Licenca invalida ou nao encontrada.\n\n"
                "O sistema esta bloqueado por licenca invalida.\n"
                "Procure o suporte para validar sua licenca."
            )

        tk.Label(outer, text=msg_principal, bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 12), justify="center").pack(pady=(5, 15))

        # ---- Card de ativacao ----
        card = GlassCard(outer, glow_color=COR_ERRO)
        card.pack(padx=30, pady=5, ipadx=25, ipady=15)

        # Contra-chave
        contra_chave = LicencaManager.gerar_contra_chave()
        ck_frame = tk.Frame(card, bg=COR_GLASS)
        ck_frame.pack(fill="x", pady=(5, 12))

        tk.Label(ck_frame, text=f"{Icons.KEY} Contra-Chave:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(anchor="w")

        ck_display_frame = tk.Frame(ck_frame, bg=COR_GLASS)
        ck_display_frame.pack(fill="x", pady=(3, 0))

        ck_entry = StyledEntry(ck_display_frame, width=22)
        ck_entry.insert(0, contra_chave)
        ck_entry.config(state="readonly")
        ck_entry.pack(side="left", fill="x", expand=True)

        def copiar_contra_chave():
            try:
                self.root.clipboard_clear()
                self.root.clipboard_append(contra_chave)
                ToastManager.success("Contra-chave copiada!")
            except Exception:
                pass

        btn_copiar = tk.Button(ck_display_frame, text=Icons.COPY, bg=COR_ENTRADA,
                                fg=COR_PRIMARIA, relief="flat", font=("Segoe UI", 12),
                                cursor="hand2", activebackground=COR_ENTRADA,
                                activeforeground=COR_ACCENT, bd=0,
                                command=copiar_contra_chave)
        btn_copiar.pack(side="right", padx=(5, 0))
        add_tooltip(btn_copiar, "Copiar contra-chave para a area de transferencia")

        tk.Label(card, text="Informe esta contra-chave ao suporte para obter a chave de ativacao.",
                 bg=COR_GLASS, fg=COR_ALERTA,
                 font=("Segoe UI", 9, "italic"), wraplength=380).pack(pady=(0, 10))

        NeonDivider(card, color=COR_ERRO, width_pct=0.8).pack(fill="x", padx=10, pady=8)

        # Chave de ativacao
        tk.Label(card, text=f"{Icons.LOCK} Chave de Ativacao:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(anchor="w", pady=(5, 3))
        et_chave = StyledEntry(card, width=40)
        et_chave.pack(fill="x", pady=(0, 10))
        add_tooltip(et_chave, "Cole aqui a chave de ativacao fornecida pelo suporte")

        # Data de expiracao (DatePicker com calendario)
        tk.Label(card, text=f"{Icons.CALENDAR} Data de Expiracao:",
                 bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(anchor="w", pady=(0, 3))
        et_data = DatePickerEntry(card, width=15)
        et_data.pack(anchor="w", pady=(0, 12))
        add_tooltip(et_data, "Clique para selecionar a data de expiracao da licenca")

        # ---- Status da licenca atual ----
        status_frame = tk.Frame(card, bg=COR_GLASS)
        status_frame.pack(fill="x", pady=(0, 5))

        if expirada and data_exp:
            try:
                dt = datetime.datetime.strptime(data_exp, "%Y-%m-%d")
                data_fmt = dt.strftime("%d/%m/%Y")
            except Exception:
                data_fmt = data_exp
            status_text = f"Status: Licenca VENCIDA em {data_fmt}"
            status_color = COR_ERRO
        elif data_exp:
            status_text = "Status: Licenca INVALIDA"
            status_color = COR_ERRO
        else:
            status_text = "Status: Sem licenca cadastrada"
            status_color = COR_ALERTA

        StatusIndicator(status_frame, color=status_color, size=8).pack(side="left", padx=(0, 5))
        tk.Label(status_frame, text=status_text, bg=COR_GLASS, fg=status_color,
                 font=("Segoe UI", 10, "bold")).pack(side="left")

        # ---- Botao Ativar ----
        def ativar_licenca():
            chave = et_chave.get().strip()
            data_str = et_data.get().strip()

            if not chave or not data_str:
                messagebox.showwarning(
                    "Campos Obrigatorios",
                    "Para ativar a licenca, preencha todos os campos.\n\n"
                    "1. Cole a CHAVE DE ATIVACAO fornecida pelo suporte\n"
                    "2. Selecione a DATA DE EXPIRACAO no calendario\n\n"
                    "Se nao tem uma chave, entre em contato com o suporte\n"
                    "para adquirir uma licenca.")
                return

            # Converter DD/MM/AAAA para AAAA-MM-DD
            try:
                dt = datetime.datetime.strptime(data_str, "%d/%m/%Y")
                data_interno = dt.strftime("%Y-%m-%d")
                try:
                    hoje_conf, _ = LicencaManager._obter_data_confiavel()
                except Exception:
                    hoje_conf = datetime.date.today()
                if dt.date() < hoje_conf:
                    hoje_fmt = hoje_conf.strftime("%d/%m/%Y")
                    messagebox.showerror(
                        "Data de Expiracao Invalida",
                        f"A data selecionada ja passou!\n\n"
                        f"Data selecionada: {data_str}\n"
                        f"Data de hoje: {hoje_fmt}\n\n"
                        f"COMO RESOLVER:\n"
                        f"1. Selecione uma data FUTURA no calendario\n"
                        f"2. A data deve ser a mesma informada pelo suporte\n"
                        f"3. Se a data do suporte ja passou, solicite uma nova chave")
                    return
            except ValueError:
                messagebox.showerror(
                    "Data Invalida",
                    "A data de expiracao informada nao e valida.\n\n"
                    "COMO RESOLVER:\n"
                    "1. Use o calendario para selecionar a data\n"
                    "2. O formato correto e DD/MM/AAAA\n"
                    "3. Exemplo: 31/12/2026")
                return

            def do_ativar():
                return LicencaManager.ativar_licenca(contra_chave, chave, data_interno)

            def on_ativado(success):
                if success:
                    # Setar flag para pular re-verificacao no show_main_menu
                    self._licenca_recem_ativada = True
                    # Resetar flag de fraude apos ativacao bem-sucedida
                    LicencaManager._date_fraud_detected = False
                    # Exibir mensagem de sucesso clara ao usuario
                    messagebox.showinfo(
                        "Licenca Ativada",
                        f"Licenca ativada com sucesso!\n\n"
                        f"Sua licenca e valida ate {data_str}.\n\n"
                        f"O sistema sera habilitado agora.")
                    ToastManager.success("Licenca ativada com sucesso! Sistema habilitado.")
                    AuditLogger.log("LICENCA_ATIVADA",
                                    f"Licenca ativada ate {data_str} - sistema habilitado",
                                    usuario=Session.user_login)
                    # Reiniciar monitor periodico de licenca
                    self._start_licenca_monitor()
                    # Ir para a tela principal
                    self.show_main_menu()
                else:
                    messagebox.showerror(
                        "Chave de Ativacao Invalida",
                        "A chave de ativacao informada nao foi aceita.\n\n"
                        "POSSIVEIS CAUSAS:\n"
                        "1. A chave foi digitada ou colada incorretamente\n"
                        "2. A data de expiracao nao corresponde a chave\n"
                        "3. A chave pode ter sido gerada para outro computador\n\n"
                        "COMO RESOLVER:\n"
                        "1. Copie a chave COMPLETA (Ctrl+C) e cole aqui (Ctrl+V)\n"
                        "2. Verifique se a data de expiracao esta correta\n"
                        "3. Confira os dados com o suporte que forneceu a chave\n"
                        "4. Se necessario, solicite uma nova chave ao suporte")

            self.run_async(do_ativar, on_ativado)

        btn_frame = tk.Frame(card, bg=COR_GLASS)
        btn_frame.pack(fill="x", pady=(5, 5))

        StyledButton(btn_frame, text=f"{Icons.CHECK}  ATIVAR LICENCA",
                     command=ativar_licenca,
                     color=COR_BOTAO_VERDE, width=28).pack(pady=(5, 3))

        # ---- Rodape com info de suporte ----
        tk.Label(outer, text="Entre em contato com o suporte tecnico para obter sua licenca.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(pady=(15, 3))
        tk.Label(outer, text="Powered by PDV Pro Technology",
                 bg=COR_FUNDO, fg="#2a3a5c",
                 font=("Segoe UI", 8)).pack(pady=(0, 0))

    # ========================================================================
    # MENU PRINCIPAL (DASHBOARD) - FUTURISTICO v8
    # ========================================================================
    def show_main_menu(self):
        self.clear_container()
        # Atualizar barra de status
        self.status_bar.set_user(Session.user_nome, Session.user_nivel)
        self.status_bar.set_caixa(bool(Session.caixa_id), Session.caixa_id)

        # === VERIFICACAO DE STATUS DO CAIXA NO MENU (sincroniza com o banco) ===
        self._sincronizar_status_caixa()

        # === VERIFICACAO DE LICENCA NO MENU (equivalente ao onResume do Android) ===
        # Verifica em background; se invalida, bloqueia o sistema
        self._verificar_licenca_menu()

        # Fundo com particulas
        self._create_bg_particles(self.main_container)

        # Barra superior futuristica
        top_bar = tk.Frame(self.main_container, bg=COR_FUNDO2, height=56)
        top_bar.pack(fill="x")
        top_bar.pack_propagate(False)

        # Linha neon no topo
        neon_top = tk.Canvas(top_bar, height=2, bg=COR_FUNDO2, highlightthickness=0)
        neon_top.pack(fill="x", side="top")
        neon_top.bind("<Configure>", lambda e: (
            neon_top.delete("all"),
            neon_top.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)
        ))

        # Logo na barra
        tk.Label(top_bar, text=f"{Icons.MONEY} PDV Pro", bg=COR_FUNDO2,
                 fg=COR_PRIMARIA, font=("Segoe UI", 15, "bold")).pack(side="left", padx=15)
        tk.Label(top_bar, text=f"v{APP_VERSION}", bg=COR_FUNDO2, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(0, 20))

        # Info do usuario
        user_frame = tk.Frame(top_bar, bg=COR_FUNDO2)
        user_frame.pack(side="left", padx=10)
        StatusIndicator(user_frame, color=COR_SUCESSO, size=8).pack(side="left", padx=(0, 5))
        tk.Label(user_frame, text=f"{Icons.USER} {Session.user_nome}",
                 bg=COR_FUNDO2, fg=COR_TEXTO,
                 font=("Segoe UI", 10, "bold")).pack(side="left")
        tk.Label(user_frame, text=f"  ({Session.user_nivel})",
                 bg=COR_FUNDO2, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left")

        # Relogio digital
        DigitalClock(top_bar).pack(side="right", padx=15)

        # Botoes da barra
        StyledButton(top_bar, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8).pack(side="right", padx=5, pady=10)
        add_tooltip(top_bar.winfo_children()[-1], "Fechar o PDV Pro (Alt+F4)")

        StyledButton(top_bar, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10).pack(side="right", padx=5, pady=10)
        add_tooltip(top_bar.winfo_children()[-1], "Trocar de usuario (Ctrl+L)")

        # Linha neon inferior
        neon_bottom = tk.Canvas(self.main_container, height=1, bg=COR_FUNDO,
                                highlightthickness=0)
        neon_bottom.pack(fill="x")
        neon_bottom.bind("<Configure>", lambda e: (
            neon_bottom.delete("all"),
            neon_bottom.create_line(0, 0, e.width, 0, fill="#1a2a4a", width=1)
        ))

        # Area de botoes com scroll
        scroll = ScrollableFrame(self.main_container)
        scroll.pack(fill="both", expand=True, padx=10, pady=5)
        content = scroll.scrollable_frame

        # ---- KPI Cards em tempo real ----
        self._render_kpi_cards(content)

        # Titulo do dashboard
        header_frame = tk.Frame(content, bg=COR_FUNDO)
        header_frame.pack(fill="x", pady=(12, 5), padx=20)
        tk.Label(header_frame, text=f"{Icons.DASHBOARD} Menu Principal",
                 bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 20, "bold")).pack(side="left")
        tk.Label(header_frame,
                 text=f"Bem-vindo, {Session.user_nome}  |  F1=Ajuda  F2=Venda  F10=Menu",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=15)

        NeonDivider(content, color=COR_PRIMARIA).pack(fill="x", padx=20, pady=(5, 15))

        # Grid de botoes futuristicos
        buttons_data = [
            ("Nova Venda", self.show_venda, COR_PRIMARIA,
             PermissionConstants.DASHBOARD_BTN_VENDAS, Icons.VENDAS, "F2 - Registrar uma nova venda"),
            ("Comandas", self.show_comandas, COR_BOTAO_LARANJA,
             PermissionConstants.DASHBOARD_BTN_COMANDAS, Icons.COMANDAS, "F4 - Gerenciar comandas"),
            ("Caixa", self.show_caixa, COR_BOTAO_VERDE,
             PermissionConstants.DASHBOARD_BTN_CAIXA, Icons.CAIXA, "F3 - Abrir/fechar caixa"),
            ("Historico", self.show_historico, COR_BOTAO_AZUL,
             PermissionConstants.DASHBOARD_BTN_HISTORICO, Icons.HISTORICO, "F5 - Historico de vendas"),
            ("Produtos", self.show_cadastro_produto, "#7c3aed",
             PermissionConstants.DASHBOARD_BTN_PRODUTOS, Icons.PRODUTOS, "F6 - Cadastro de produtos"),
            ("Ger. Produtos", self.show_gerenciar_produtos, "#9333ea",
             PermissionConstants.DASHBOARD_BTN_GERENCIAR_PRODUTOS, Icons.EDIT, "Gerenciar produtos e estoque"),
            ("Clientes", self.show_cadastro_cliente, "#f72585",
             PermissionConstants.DASHBOARD_BTN_CLIENTES, Icons.CLIENTES, "F8 - Cadastro de clientes"),
            ("Vendedores", self.show_cadastro_vendedor, "#4cc9f0",
             PermissionConstants.DASHBOARD_BTN_VENDEDORES, Icons.USER, "Cadastro de vendedores"),
            ("Entregadores", self.show_cadastro_entregador, "#00BCD4",
             PermissionConstants.DASHBOARD_BTN_ENTREGADORES, Icons.ENTREGA, "Cadastro de entregadores"),
            ("Garcons", self.show_cadastro_garcom, "#8BC34A",
             PermissionConstants.DASHBOARD_BTN_GARCONS, Icons.GARCOM, "Cadastro de garcons"),
            ("Mesas", self.show_gerenciar_mesas, "#FF9800",
             PermissionConstants.DASHBOARD_BTN_GERENCIAR_MESAS, Icons.MESA, "F9 - Gerenciar mesas"),
            ("Armarios", self.show_gerenciar_armarios, "#9C27B0",
             PermissionConstants.DASHBOARD_BTN_GERENCIAR_ARMARIOS_SAUNA, Icons.ARMARIO,
             "Gerenciar armarios de sauna"),
            ("Tipos Produto", self.show_cadastro_tipo_produto, "#607D8B",
             PermissionConstants.DASHBOARD_BTN_TIPOS_PRODUTO, Icons.PRODUTOS, "Tipos/categorias de produto"),
            ("Adicionais", self.show_cadastro_adicional, "#795548",
             PermissionConstants.DASHBOARD_BTN_ADICIONAIS, Icons.ADD, "Adicionais para produtos"),
            ("Formas Pgto", self.show_cadastro_forma_pagamento, "#009688",
             PermissionConstants.DASHBOARD_BTN_FORMAS_PAGAMENTO, Icons.MONEY, "Formas de pagamento"),
            ("Contas Receber", self.show_contas_receber, "#F44336",
             PermissionConstants.DASHBOARD_BTN_CONTAS_RECEBER, Icons.MONEY, "Contas a receber"),
            ("Entrada Notas", self.show_entrada_notas, "#3F51B5",
             PermissionConstants.DASHBOARD_BTN_ENTRADA_NOTAS, Icons.PRODUTOS, "Entrada de notas fiscais"),
            ("Taxa Entrega", self.show_taxa_entrega, "#FF5722",
             PermissionConstants.DASHBOARD_BTN_TAXA_ENTREGA, Icons.ENTREGA, "Taxas de entrega por bairro"),
            ("Relatorios", self.show_relatorios, "#4CAF50",
             PermissionConstants.DASHBOARD_BTN_RELATORIOS, Icons.RELATORIOS, "F7 - Relatorios gerenciais"),
            ("Empresa", self.show_cadastro_empresa, "#2196F3",
             PermissionConstants.DASHBOARD_BTN_EMPRESA, Icons.EMPRESA, "Dados da empresa"),
            ("Usuarios", self.show_cadastro_usuario, "#E91E63",
             PermissionConstants.DASHBOARD_BTN_USUARIOS, Icons.USER, "Cadastro de usuarios"),
            ("Perfis", self.show_gerenciar_perfis, "#673AB7",
             PermissionConstants.DASHBOARD_BTN_PERFIS, Icons.PERFIL, "Perfis e permissoes"),
            ("Obs. Cupom", self.show_cadastro_obs_cupom, "#455A64",
             PermissionConstants.DASHBOARD_BTN_OBSERVACOES, Icons.EDIT, "Observacoes padrao do cupom"),
            ("Painel Cozinha", self.show_painel_cozinha, "#FF6F00",
             PermissionConstants.DASHBOARD_BTN_PAINEL_COZINHA, Icons.COMANDAS, "Painel da cozinha - acompanhar pedidos"),
            ("Painel Chamados", self.show_painel_chamados, "#00897B",
             PermissionConstants.DASHBOARD_BTN_PAINEL_CHAMADOS, Icons.ALERT, "Painel de chamados dos clientes"),
            ("Ger. Chamados", self.show_gerenciador_chamados, "#00695C",
             PermissionConstants.DASHBOARD_BTN_GERENCIADOR_CHAMADOS, Icons.ALERT, "Gerenciador de chamados com voz e sinal sonoro"),
            ("Modo Entregador", self.show_modo_entregador, "#1B5E20",
             PermissionConstants.DASHBOARD_BTN_MODO_ENTREGADOR, Icons.ENTREGA, "Modo entregador - visualizar entregas"),
            ("Ger. Entregas", self.show_gerenciar_entregas, "#2E7D32",
             PermissionConstants.DASHBOARD_BTN_GERENCIAR_ENTREGAS, Icons.ENTREGA, "Gerenciar entregas pendentes"),
            ("Cad. Mesas", self.show_cadastro_mesa, "#E65100",
             PermissionConstants.DASHBOARD_BTN_CADASTRO_MESAS, Icons.MESA, "Cadastro de mesas"),
            ("Cad. Armarios", self.show_cadastro_armario_sauna, "#6A1B9A",
             PermissionConstants.DASHBOARD_BTN_CADASTRO_ARMARIOS_SAUNA, Icons.ARMARIO, "Cadastro de armarios para sauna"),
            ("WhatsApp Bot", self.show_whatsbot, "#25D366",
             PermissionConstants.DASHBOARD_BTN_WHATSBOT, Icons.WEB, "Configurar WhatsApp Bot"),
            ("Backup", self.show_backup, "#37474F",
             PermissionConstants.DASHBOARD_BTN_BACKUP, Icons.BACKUP, "Backup e restauracao do banco"),
            ("Atualizar", self.show_atualizar_sistema, "#0D47A1",
             PermissionConstants.DASHBOARD_BTN_ATUALIZAR, Icons.REFRESH, "Atualizar o sistema via FTP"),
            ("Servidor Web", self.show_servidor_web, "#00ACC1",
             PermissionConstants.DASHBOARD_BTN_SERVIDOR_WEB, Icons.WEB, "Cardapio digital / servidor web"),
            ("Impressora", self.show_config_impressora, "#FF7043",
             PermissionConstants.DASHBOARD_BTN_IMPRESSORA, Icons.IMPRESSORA,
             "F12 - Configurar impressora"),
            ("Multi-Impressoras", self.show_config_multi_impressora, "#E64A19",
             PermissionConstants.DASHBOARD_BTN_IMPRESSORA, Icons.IMPRESSORA,
             "Configurar multiplas impressoras por categoria"),
            ("Licenca", self._show_tela_licenca_menu, "#FF5252",
             PermissionConstants.DASHBOARD_BTN_LICENCA, Icons.KEY, "Gerenciar licenca do sistema"),
            ("Log Auditoria", self._show_audit_log, "#546E7A",
             PermissionConstants.DASHBOARD_BTN_LOG_AUDITORIA, Icons.AUDIT, "Visualizar log de auditoria do sistema"),
            ("Sobre", self.show_sobre, "#78909C",
             PermissionConstants.DASHBOARD_BTN_SOBRE, Icons.SOBRE, "Sobre o PDV Pro"),
            ("Trocar Senha", self.show_trocar_senha_dialog, "#FF9800",
             PermissionConstants.DASHBOARD_BTN_TROCAR_SENHA, Icons.LOCK, "Trocar a senha do usuario atual"),
            ("Trocar Usuario", self.do_logoff, COR_BOTAO_LARANJA,
             None, Icons.TROCAR, "Ctrl+L - Trocar de usuario (logoff)"),
            ("Sair", self._on_close, COR_ERRO,
             None, Icons.SAIR, "Alt+F4 - Fechar o PDV Pro"),
        ]

        grid_frame = tk.Frame(content, bg=COR_FUNDO)
        grid_frame.pack(fill="x", padx=20)

        col = 0
        row = 0
        max_cols = 5
        for item in buttons_data:
            text, cmd, color, perm, icon = item[0], item[1], item[2], item[3], item[4]
            tooltip_text = item[5] if len(item) > 5 else ""
            if perm and not Session.has_permission(perm):
                continue

            # Card-botao futuristico
            btn_card = tk.Frame(grid_frame, bg=COR_GLASS, highlightthickness=1,
                               highlightbackground=COR_GLASS_BORDER,
                               padx=2, pady=2)
            btn_card.grid(row=row, column=col, padx=6, pady=6, sticky="nsew")

            # Canvas interno para o botao
            btn_canvas = tk.Canvas(btn_card, width=150, height=80, bg=COR_GLASS,
                                  highlightthickness=0, cursor="hand2")
            btn_canvas.pack(fill="both", expand=True)

            # Linha de cor no topo
            btn_canvas.create_rectangle(0, 0, 200, 3, fill=color, outline="")

            # Icone
            btn_canvas.create_text(75, 30, text=icon, fill=color,
                                  font=("Segoe UI", 18))

            # Texto
            btn_canvas.create_text(75, 58, text=text, fill=COR_TEXTO,
                                  font=("Segoe UI", 9, "bold"))

            # Tooltip
            if tooltip_text:
                add_tooltip(btn_canvas, tooltip_text)
                add_tooltip(btn_card, tooltip_text)

            # Efeitos hover
            def _make_hover(canvas, card, c):
                def enter(e):
                    card.configure(highlightbackground=c)
                    canvas.configure(bg=COR_CARD_HOVER)
                    canvas.delete("hover_bg")
                    canvas.create_rectangle(0, 0, 200, 3, fill=c, outline="",
                                            tags="hover_bg")
                def leave(e):
                    card.configure(highlightbackground=COR_GLASS_BORDER)
                    canvas.configure(bg=COR_GLASS)
                return enter, leave

            enter_fn, leave_fn = _make_hover(btn_canvas, btn_card, color)
            btn_canvas.bind("<Enter>", enter_fn)
            btn_canvas.bind("<Leave>", leave_fn)

            # Bind click
            def _make_click(command):
                return lambda e: command()
            btn_canvas.bind("<Button-1>", _make_click(cmd))

            col += 1
            if col >= max_cols:
                col = 0
                row += 1

        for c in range(max_cols):
            grid_frame.columnconfigure(c, weight=1)

    # ========================================================================
    # KPI CARDS DO DASHBOARD
    # ========================================================================
    def _render_kpi_cards(self, parent):
        """Renderiza cards de KPI em tempo real no topo do dashboard."""
        kpi_frame = tk.Frame(parent, bg=COR_FUNDO)
        kpi_frame.pack(fill="x", padx=20, pady=(10, 5))

        # Labels que serao atualizadas
        lbl_vendas_dia = [None]
        lbl_total_dia = [None]
        lbl_estoque_critico = [None]
        lbl_contas_pend = [None]
        lbl_comandas_aber = [None]
        lbl_armarios_abertos = [None]

        def _make_kpi(parent_f, title, valor_inicial, color, icon, col,
                       tooltip="", on_click=None):
            card = tk.Frame(parent_f, bg=COR_GLASS, highlightthickness=1,
                            highlightbackground=COR_GLASS_BORDER,
                            cursor="hand2" if on_click else "")
            card.grid(row=0, column=col, padx=6, pady=4, sticky="nsew")
            inner = tk.Frame(card, bg=COR_GLASS, padx=14, pady=8,
                             cursor="hand2" if on_click else "")
            inner.pack(fill="both", expand=True)

            lbl_icon = tk.Label(inner, text=icon, bg=COR_GLASS, fg=color,
                     font=("Segoe UI", 18), cursor="hand2" if on_click else "")
            lbl_icon.pack(anchor="w")
            lbl = tk.Label(inner, text=valor_inicial, bg=COR_GLASS, fg=color,
                           font=("Consolas", 16, "bold"),
                           cursor="hand2" if on_click else "")
            lbl.pack(anchor="w")
            lbl_title = tk.Label(inner, text=title, bg=COR_GLASS, fg=COR_TEXTO2,
                     font=("Segoe UI", 8), cursor="hand2" if on_click else "")
            lbl_title.pack(anchor="w")
            if tooltip:
                add_tooltip(card, tooltip + ("\nClique para ver detalhes" if on_click else ""))
            # Bind click em todos os widgets do card
            if on_click:
                def _hover_enter(e):
                    card.configure(highlightbackground=color)
                    inner.configure(bg=COR_CARD_HOVER)
                    for w in (lbl_icon, lbl, lbl_title):
                        w.configure(bg=COR_CARD_HOVER)
                def _hover_leave(e):
                    card.configure(highlightbackground=COR_GLASS_BORDER)
                    inner.configure(bg=COR_GLASS)
                    for w in (lbl_icon, lbl, lbl_title):
                        w.configure(bg=COR_GLASS)
                for w in (card, inner, lbl_icon, lbl, lbl_title):
                    w.bind("<Button-1>", lambda e, cb=on_click: cb())
                    w.bind("<Enter>", _hover_enter)
                    w.bind("<Leave>", _hover_leave)
            return lbl

        lbl_vendas_dia[0] = _make_kpi(kpi_frame, "Vendas Hoje", "...",
                                       COR_PRIMARIA, Icons.VENDAS, 0,
                                       "Numero de vendas finalizadas hoje",
                                       on_click=self._kpi_click_vendas_hoje)
        lbl_total_dia[0] = _make_kpi(kpi_frame, "Total Hoje (R$)", "...",
                                      COR_SUCESSO, Icons.MONEY, 1,
                                      "Valor total de vendas do dia",
                                      on_click=self._kpi_click_total_hoje)
        lbl_estoque_critico[0] = _make_kpi(kpi_frame, "Estoque Critico", "...",
                                            COR_ALERTA, Icons.ALERT, 2,
                                            "Produtos com estoque abaixo do minimo",
                                            on_click=self._kpi_click_estoque_critico)
        lbl_contas_pend[0] = _make_kpi(kpi_frame, "Contas Pendentes", "...",
                                        COR_ERRO, Icons.MONEY, 3,
                                        "Contas a receber pendentes",
                                        on_click=self._kpi_click_contas_pendentes)
        lbl_comandas_aber[0] = _make_kpi(kpi_frame, "Comandas Abertas", "...",
                                          COR_BOTAO_AZUL, Icons.COMANDAS, 4,
                                          "Comandas em aberto agora",
                                          on_click=self._kpi_click_comandas_abertas)
        lbl_armarios_abertos[0] = _make_kpi(kpi_frame, "Armarios Abertos", "...",
                                             "#9C27B0", Icons.ARMARIO, 5,
                                             "Armarios de sauna em uso agora",
                                             on_click=self._kpi_click_armarios_abertos)

        for c in range(6):
            kpi_frame.columnconfigure(c, weight=1)

        def _load_kpis():
            try:
                db = DatabaseHelper.get_instance()

                r_vend = db.execute_query(
                    "SELECT COUNT(*) as cnt, COALESCE(SUM(total_liquido),0) as tot "
                    "FROM vendas WHERE status='finalizada' AND DATE(data_venda)=CURDATE()"
                )
                qtd_v = int(r_vend[0]["cnt"]) if r_vend else 0
                tot_v = float(r_vend[0]["tot"]) if r_vend else 0.0

                r_est = db.execute_query(
                    "SELECT COUNT(*) as cnt FROM produtos "
                    "WHERE ativo=1 AND estoque <= estoque_minimo AND estoque_minimo > 0"
                )
                qtd_est = int(r_est[0]["cnt"]) if r_est else 0

                r_contas = db.execute_query(
                    "SELECT COUNT(*) as cnt FROM contas_receber WHERE status='pendente'"
                )
                qtd_contas = int(r_contas[0]["cnt"]) if r_contas else 0

                r_com = db.execute_query(
                    "SELECT COUNT(*) as cnt FROM comandas WHERE status='aberta'"
                )
                qtd_com = int(r_com[0]["cnt"]) if r_com else 0

                r_arm = db.execute_query(
                    "SELECT COUNT(*) as cnt FROM uso_armario_sauna WHERE status='ocupado'"
                )
                qtd_arm = int(r_arm[0]["cnt"]) if r_arm else 0

                return qtd_v, tot_v, qtd_est, qtd_contas, qtd_com, qtd_arm
            except Exception:
                return 0, 0.0, 0, 0, 0, 0

        def _apply_kpis(result):
            qtd_v, tot_v, qtd_est, qtd_contas, qtd_com, qtd_arm = result
            try:
                lbl_vendas_dia[0].config(text=str(qtd_v))
                lbl_total_dia[0].config(
                    text=f"R$ {FormatUtils.format_money(tot_v)}")
                lbl_estoque_critico[0].config(
                    text=str(qtd_est),
                    fg=COR_ERRO if qtd_est > 0 else COR_SUCESSO)
                lbl_contas_pend[0].config(
                    text=str(qtd_contas),
                    fg=COR_ERRO if qtd_contas > 0 else COR_SUCESSO)
                lbl_comandas_aber[0].config(text=str(qtd_com))
                lbl_armarios_abertos[0].config(text=str(qtd_arm))
            except Exception:
                pass

        self.run_async(_load_kpis, _apply_kpis)

        # Auto-refresh dos KPIs em tempo real (a cada 20 segundos)
        def _kpi_refresh():
            self.run_async(_load_kpis, _apply_kpis)
        self._start_auto_refresh("dashboard_kpis", _kpi_refresh, 20000)

    # ========================================================================
    # KPI CARDS - FUNCOES DE CLIQUE (DETALHAMENTO)
    # ========================================================================

    def _kpi_click_vendas_hoje(self):
        """Abre dialogo com lista detalhada das vendas finalizadas hoje."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Vendas Hoje - Detalhamento")
        dialog.geometry("900x550")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        # Titulo
        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.VENDAS} Vendas Finalizadas Hoje",
                 bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_resumo = tk.Label(header, text="", bg=COR_FUNDO2, fg=COR_SUCESSO,
                               font=("Segoe UI", 10, "bold"))
        lbl_resumo.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "cliente", "total", "data", "status", "pagamento")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=18)
        tree.heading("id", text="ID")
        tree.heading("cliente", text="Cliente")
        tree.heading("total", text="Total")
        tree.heading("data", text="Data/Hora")
        tree.heading("status", text="Status")
        tree.heading("pagamento", text="Pagamento")
        tree.column("id", width=55, anchor="center")
        tree.column("cliente", width=200)
        tree.column("total", width=110, anchor="e")
        tree.column("data", width=140)
        tree.column("status", width=90, anchor="center")
        tree.column("pagamento", width=180)

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)

        def _ver_cupom():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione uma venda.")
                return
            venda_id = tree.item(sel[0])["values"][0]
            dialog.destroy()
            self.show_historico()

        def _ir_historico():
            dialog.destroy()
            self.show_historico()

        StyledButton(btn_frame, text=f"{Icons.HISTORICO} Abrir Historico",
                     command=_ir_historico,
                     color=COR_BOTAO_AZUL, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT v.id, COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cliente_nome, "
                    "v.total_liquido, v.data_venda, v.status, "
                    "GROUP_CONCAT(fp.descricao SEPARATOR ' + ') as formas "
                    "FROM vendas v "
                    "LEFT JOIN clientes c ON v.cliente_id = c.id "
                    "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                    "LEFT JOIN pagamentos_venda pv ON pv.venda_id = v.id "
                    "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                    "WHERE v.status='finalizada' AND DATE(v.data_venda) = CURDATE() "
                    "GROUP BY v.id ORDER BY v.id DESC"
                )
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                total_valor = 0.0
                for i, r in enumerate(rows):
                    tag = "even" if i % 2 == 0 else "odd"
                    tree.insert("", "end", values=(
                        r["id"],
                        r.get("cliente_nome") or "N/A",
                        f"R$ {FormatUtils.format_money(r['total_liquido'])}",
                        FormatUtils.format_date(r["data_venda"]),
                        r["status"],
                        r.get("formas") or ""
                    ), tags=(tag,))
                    total_valor += float(r.get("total_liquido", 0))
                tree.tag_configure("even", background=COR_GLASS)
                tree.tag_configure("odd", background=COR_CARD)
                lbl_resumo.config(
                    text=f"{len(rows)} vendas  |  Total: R$ {FormatUtils.format_money(total_valor)}")
            self.run_async(load, on_loaded)
        _carregar()

    def _kpi_click_total_hoje(self):
        """Abre dialogo com detalhamento do total de vendas hoje (por forma de pagamento)."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Total Hoje - Detalhamento")
        dialog.geometry("800x500")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.MONEY} Total de Vendas Hoje",
                 bg=COR_FUNDO2, fg=COR_SUCESSO,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_total_geral = tk.Label(header, text="", bg=COR_FUNDO2, fg=COR_SUCESSO,
                                    font=("Segoe UI", 12, "bold"))
        lbl_total_geral.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # Resumo por forma de pagamento
        tk.Label(content, text="Resumo por Forma de Pagamento", bg=COR_FUNDO,
                 fg=COR_PRIMARIA, font=("Segoe UI", 11, "bold")).pack(anchor="w", pady=(5, 3))

        # Cada forma de pagamento e um item expansivel ("+"): ao expandir,
        # mostra os lancamentos (pagamentos) que compoem o total da forma.
        fp_frame = tk.Frame(content, bg=COR_FUNDO)
        fp_frame.pack(fill="x", pady=(0, 10))
        cols_fp = ("qtd", "total")
        tree_fp = ttk.Treeview(fp_frame, columns=cols_fp, show="tree headings", height=8)
        tree_fp.heading("#0", text="Forma de Pagamento  (clique no + para detalhar)")
        tree_fp.heading("qtd", text="Quantidade")
        tree_fp.heading("total", text="Total")
        tree_fp.column("#0", width=340)
        tree_fp.column("qtd", width=100, anchor="center")
        tree_fp.column("total", width=150, anchor="e")
        fp_scroll = ttk.Scrollbar(fp_frame, orient="vertical", command=tree_fp.yview)
        tree_fp.configure(yscrollcommand=fp_scroll.set)
        fp_scroll.pack(side="right", fill="y")
        tree_fp.pack(side="left", fill="both", expand=True)

        # Lista de vendas
        tk.Label(content, text="Vendas do Dia", bg=COR_FUNDO,
                 fg=COR_PRIMARIA, font=("Segoe UI", 11, "bold")).pack(anchor="w", pady=(5, 3))

        cols_v = ("id", "cliente", "total", "hora", "formas")
        tree_v = ttk.Treeview(content, columns=cols_v, show="headings", height=10)
        tree_v.heading("id", text="ID")
        tree_v.heading("cliente", text="Cliente")
        tree_v.heading("total", text="Total")
        tree_v.heading("hora", text="Hora")
        tree_v.heading("formas", text="Pagamento")
        tree_v.column("id", width=55, anchor="center")
        tree_v.column("cliente", width=200)
        tree_v.column("total", width=110, anchor="e")
        tree_v.column("hora", width=80, anchor="center")
        tree_v.column("formas", width=180)

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree_v.yview)
        tree_v.configure(yscrollcommand=vsb.set)
        tree_frame = tk.Frame(content, bg=COR_FUNDO)
        tree_frame.pack(fill="both", expand=True)
        tree_v.pack(in_=tree_frame, side="left", fill="both", expand=True)
        vsb.pack(in_=tree_frame, side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)
        StyledButton(btn_frame, text=f"{Icons.HISTORICO} Abrir Historico",
                     command=lambda: (dialog.destroy(), self.show_historico()),
                     color=COR_BOTAO_AZUL, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.REFRESH} Atualizar",
                     command=lambda: _carregar(),
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                # Resumo por forma de pagamento.
                # ATENCAO: pagamentos_venda.valor guarda o valor ENTREGUE pelo cliente
                # (inclui o excedente pago em dinheiro que gera troco). Por isso a soma
                # bruta por forma fica maior que o total liquido das vendas. O troco e
                # sempre devolvido em dinheiro, entao ele e abatido da(s) forma(s) do
                # tipo 'dinheiro' em on_loaded para que a soma bata com o Total Geral.
                formas = db.execute_query(
                    "SELECT COALESCE(fp.descricao, 'N/A') as forma, "
                    "COALESCE(fp.tipo, '') as tipo, "
                    "COUNT(DISTINCT pv.venda_id) as qtd, SUM(pv.valor) as total "
                    "FROM pagamentos_venda pv "
                    "JOIN vendas v ON pv.venda_id = v.id "
                    "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                    "WHERE v.status='finalizada' AND DATE(v.data_venda) = CURDATE() "
                    "GROUP BY fp.id, fp.descricao, fp.tipo ORDER BY total DESC"
                )
                # Total de troco devolvido no dia (em dinheiro)
                troco_row = db.execute_query(
                    "SELECT COALESCE(SUM(troco),0) as troco FROM vendas "
                    "WHERE status='finalizada' AND DATE(data_venda) = CURDATE()"
                )
                troco_total = float(troco_row[0]["troco"]) if troco_row else 0.0
                # Vendas do dia
                vendas = db.execute_query(
                    "SELECT v.id, COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cliente_nome, "
                    "v.total_liquido, v.data_venda, "
                    "GROUP_CONCAT(fp.descricao SEPARATOR ' + ') as formas "
                    "FROM vendas v "
                    "LEFT JOIN clientes c ON v.cliente_id = c.id "
                    "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                    "LEFT JOIN pagamentos_venda pv ON pv.venda_id = v.id "
                    "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                    "WHERE v.status='finalizada' AND DATE(v.data_venda) = CURDATE() "
                    "GROUP BY v.id ORDER BY v.id DESC"
                )
                # Lancamentos individuais (cada pagamento) que compoem cada forma
                lancamentos = db.execute_query(
                    "SELECT COALESCE(fp.descricao, 'N/A') as forma, "
                    "v.id as venda_id, pv.valor as valor, v.data_venda as data_venda, "
                    "COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cliente "
                    "FROM pagamentos_venda pv "
                    "JOIN vendas v ON pv.venda_id = v.id "
                    "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                    "LEFT JOIN clientes c ON v.cliente_id = c.id "
                    "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                    "WHERE v.status='finalizada' AND DATE(v.data_venda) = CURDATE() "
                    "ORDER BY v.id DESC"
                )
                return formas, vendas, troco_total, lancamentos
            def on_loaded(result):
                formas, vendas, troco_total, lancamentos = result
                # Normaliza os registros para permitir ajuste do troco
                formas = [dict(r) for r in formas]
                for r in formas:
                    r["total"] = float(r.get("total") or 0)
                # Abate o troco devolvido do valor recebido em dinheiro, para que o
                # resumo reflita a receita liquida real e a soma bata com o Total Geral.
                if troco_total > 0 and formas:
                    dinheiro = [r for r in formas
                                if (r.get("tipo") or "").lower() == "dinheiro"]
                    alvo = dinheiro if dinheiro else formas
                    soma_alvo = sum(r["total"] for r in alvo)
                    if soma_alvo > 0:
                        for r in alvo:
                            abate = troco_total * (r["total"] / soma_alvo)
                            r["troco_abatido"] = abate
                            r["total"] -= abate
                # Reordena por total (decrescente) apos o ajuste
                formas.sort(key=lambda r: r["total"], reverse=True)
                # Agrupa os lancamentos por forma de pagamento
                lancamentos_por_forma = {}
                for l in (lancamentos or []):
                    lancamentos_por_forma.setdefault(l.get("forma") or "N/A", []).append(l)
                # Preencher formas de pagamento (cada forma e expansivel via "+")
                tree_fp.delete(*tree_fp.get_children())
                for i, r in enumerate(formas):
                    tag = "even" if i % 2 == 0 else "odd"
                    parent = tree_fp.insert(
                        "", "end", text=r["forma"],
                        values=(r["qtd"], f"R$ {FormatUtils.format_money(r['total'])}"),
                        tags=(tag,), open=False
                    )
                    # Lancamentos (pagamentos) que compoem esta forma
                    for lanc in lancamentos_por_forma.get(r["forma"], []):
                        hora = ""
                        try:
                            dv = lanc.get("data_venda")
                            if isinstance(dv, datetime.datetime):
                                hora = dv.strftime("%H:%M")
                            else:
                                hora = str(dv)[11:16] if len(str(dv)) > 16 else ""
                        except Exception:
                            pass
                        desc = f"Venda #{lanc.get('venda_id')}  -  {lanc.get('cliente') or 'N/A'}"
                        if hora:
                            desc += f"  ({hora})"
                        tree_fp.insert(
                            parent, "end", text="    " + desc,
                            values=("", f"R$ {FormatUtils.format_money(lanc.get('valor') or 0)}"),
                            tags=("lanc",)
                        )
                    # Linha do troco devolvido (abatido do dinheiro), p/ reconciliar
                    # a soma dos lancamentos com o total liquido exibido na forma.
                    ab = float(r.get("troco_abatido") or 0)
                    if ab > 0.001:
                        tree_fp.insert(
                            parent, "end", text="    (-) Troco devolvido no dia",
                            values=("", f"- R$ {FormatUtils.format_money(ab)}"),
                            tags=("troco",)
                        )
                tree_fp.tag_configure("even", background=COR_GLASS)
                tree_fp.tag_configure("odd", background=COR_CARD)
                tree_fp.tag_configure("lanc", background=COR_FUNDO, foreground=COR_TEXTO2)
                tree_fp.tag_configure("troco", background=COR_FUNDO, foreground=COR_ALERTA)
                # Preencher vendas
                tree_v.delete(*tree_v.get_children())
                total_geral = 0.0
                for i, r in enumerate(vendas):
                    tag = "even" if i % 2 == 0 else "odd"
                    hora = ""
                    try:
                        dt = r["data_venda"]
                        if isinstance(dt, datetime.datetime):
                            hora = dt.strftime("%H:%M")
                        else:
                            hora = str(dt)[11:16] if len(str(dt)) > 16 else str(dt)
                    except Exception:
                        pass
                    tree_v.insert("", "end", values=(
                        r["id"],
                        r.get("cliente_nome") or "N/A",
                        f"R$ {FormatUtils.format_money(r['total_liquido'])}",
                        hora,
                        r.get("formas") or ""
                    ), tags=(tag,))
                    total_geral += float(r.get("total_liquido", 0))
                tree_v.tag_configure("even", background=COR_GLASS)
                tree_v.tag_configure("odd", background=COR_CARD)
                lbl_total_geral.config(
                    text=f"Total Geral: R$ {FormatUtils.format_money(total_geral)}")
            self.run_async(load, on_loaded)
        _carregar()

    def _kpi_click_estoque_critico(self):
        """Abre dialogo com lista de produtos com estoque critico, permitindo editar."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Estoque Critico - Detalhamento")
        dialog.geometry("850x500")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.ALERT} Produtos com Estoque Critico",
                 bg=COR_FUNDO2, fg=COR_ALERTA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_count = tk.Label(header, text="", bg=COR_FUNDO2, fg=COR_ERRO,
                              font=("Segoe UI", 10, "bold"))
        lbl_count.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "descricao", "estoque", "estoque_minimo", "preco_venda", "status_est")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=16)
        tree.heading("id", text="ID")
        tree.heading("descricao", text="Produto")
        tree.heading("estoque", text="Estoque Atual")
        tree.heading("estoque_minimo", text="Estoque Minimo")
        tree.heading("preco_venda", text="Preco Venda")
        tree.heading("status_est", text="Situacao")
        tree.column("id", width=50, anchor="center")
        tree.column("descricao", width=280)
        tree.column("estoque", width=100, anchor="center")
        tree.column("estoque_minimo", width=110, anchor="center")
        tree.column("preco_venda", width=100, anchor="e")
        tree.column("status_est", width=100, anchor="center")

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)

        def _editar_produto():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione um produto.")
                return
            dialog.destroy()
            self.show_cadastro_produto()

        def _ir_produtos():
            dialog.destroy()
            self.show_cadastro_produto()

        def _ir_entrada_notas():
            dialog.destroy()
            self.show_entrada_notas()

        StyledButton(btn_frame, text=f"{Icons.EDIT} Editar Produto",
                     command=_editar_produto,
                     color=COR_BOTAO_AZUL, width=14).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.PRODUTOS} Cadastro Produtos",
                     command=_ir_produtos,
                     color=COR_BOTAO_VERDE, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.IMPORT} Entrada de Notas",
                     command=_ir_entrada_notas,
                     color=COR_BOTAO_LARANJA, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT id, descricao, estoque, estoque_minimo, preco_venda "
                    "FROM produtos WHERE ativo=1 AND estoque <= estoque_minimo AND estoque_minimo > 0 "
                    "ORDER BY estoque ASC"
                )
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                for i, r in enumerate(rows):
                    est = float(r.get("estoque", 0))
                    est_min = float(r.get("estoque_minimo", 0))
                    if est <= 0:
                        situacao = "ZERADO"
                    elif est <= est_min * 0.5:
                        situacao = "CRITICO"
                    else:
                        situacao = "BAIXO"
                    tag = "zerado" if est <= 0 else ("critico" if est <= est_min * 0.5 else "baixo")
                    tree.insert("", "end", values=(
                        r["id"],
                        r["descricao"],
                        f"{est:.1f}",
                        f"{est_min:.1f}",
                        f"R$ {FormatUtils.format_money(r['preco_venda'])}",
                        situacao
                    ), tags=(tag,))
                tree.tag_configure("zerado", background="#1a0a0a", foreground="#ff4444")
                tree.tag_configure("critico", background="#1a1a0a", foreground="#ffaa00")
                tree.tag_configure("baixo", background=COR_GLASS, foreground=COR_ALERTA)
                lbl_count.config(text=f"{len(rows)} produto(s) abaixo do minimo")
            self.run_async(load, on_loaded)
        _carregar()

    def _kpi_click_contas_pendentes(self):
        """Abre dialogo com lista de contas pendentes, permitindo receber pagamento."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Contas Pendentes - Detalhamento")
        dialog.geometry("900x520")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.MONEY} Contas Pendentes",
                 bg=COR_FUNDO2, fg=COR_ERRO,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_total_pend = tk.Label(header, text="", bg=COR_FUNDO2, fg=COR_ERRO,
                                   font=("Segoe UI", 10, "bold"))
        lbl_total_pend.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "cliente", "valor_original", "valor_pago", "valor_pendente", "status", "vencimento")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=16)
        for c, h, w in [
            ("id", "ID", 50), ("cliente", "Cliente", 190),
            ("valor_original", "Valor Original", 115), ("valor_pago", "Valor Pago", 110),
            ("valor_pendente", "Pendente", 110), ("status", "Status", 90),
            ("vencimento", "Vencimento", 110),
        ]:
            tree.heading(c, text=h)
            tree.column(c, width=w, anchor="center" if c in ("id", "status", "vencimento") else "e")

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)

        def _receber_pagamento():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione uma conta.")
                return
            conta_id = tree.item(sel[0])["values"][0]
            cliente_nome = tree.item(sel[0])["values"][1]
            pendente = FormatUtils.parse_money(str(tree.item(sel[0])["values"][4]))
            if pendente <= 0:
                ToastManager.info("Esta conta ja esta quitada.")
                return
            valor = simpledialog.askfloat(
                "Receber Pagamento",
                f"Cliente: {cliente_nome}\n"
                f"Valor pendente: R$ {FormatUtils.format_money(pendente)}\n\n"
                f"Valor a receber (R$):",
                initialvalue=pendente, minvalue=0.01, parent=dialog)
            if not valor:
                return
            if not Session.caixa_id:
                self.show_error(
                    "Abra o caixa antes de receber.\n\n"
                    "O recebimento entra como credito (entrada) no caixa atual.")
                return
            caixa_id_receb = Session.caixa_id
            def receber():
                db = DatabaseHelper.get_instance()
                novo_pendente = pendente - valor
                status = "quitada" if novo_pendente <= 0.009 else "parcial"
                db.execute_update(
                    "UPDATE contas_receber SET "
                    "valor_pago = valor_pago + %s, "
                    "valor_pendente = GREATEST(0, valor_pendente - %s), "
                    "status = %s WHERE id = %s",
                    (valor, valor, status, conta_id)
                )
                db.execute_update(
                    "INSERT INTO entradas_caixa (caixa_id, descricao, valor, origem, "
                    "conta_receber_id, operador_nome) VALUES (%s,%s,%s,'conta_receber',%s,%s)",
                    (caixa_id_receb,
                     f"Recebimento conta #{conta_id} - {cliente_nome}",
                     valor, conta_id, getattr(Session, "user_nome", "") or "")
                )
                return status
            def on_recebido(status):
                msg = (f"Recebido R$ {FormatUtils.format_money(valor)} (credito no caixa)!"
                       + (" Conta QUITADA!" if status == "quitada" else ""))
                ToastManager.success(msg)
                _carregar()
            self.run_async(receber, on_recebido)

        def _ir_contas():
            dialog.destroy()
            self.show_contas_receber()

        StyledButton(btn_frame, text=f"{Icons.MONEY} Receber Pagamento",
                     command=_receber_pagamento,
                     color=COR_BOTAO_VERDE, width=18).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.MONEY} Abrir Contas a Receber",
                     command=_ir_contas,
                     color=COR_BOTAO_AZUL, width=20).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT cr.id, c.nome as cliente_nome, cr.valor_original, "
                    "cr.valor_pago, cr.valor_pendente, cr.status, cr.data_vencimento "
                    "FROM contas_receber cr "
                    "LEFT JOIN clientes c ON cr.cliente_id = c.id "
                    "WHERE cr.status IN ('pendente', 'parcial') "
                    "ORDER BY cr.data_vencimento ASC, cr.id DESC"
                )
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                total_pend = 0.0
                for i, r in enumerate(rows):
                    pend = float(r.get("valor_pendente", 0))
                    venc = str(r.get("data_vencimento", "")) or ""
                    if venc and len(venc) >= 10:
                        try:
                            venc = datetime.datetime.strptime(
                                venc[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
                        except Exception:
                            pass
                    # Verificar se vencida
                    vencida = False
                    try:
                        dv = r.get("data_vencimento")
                        if dv:
                            if isinstance(dv, str):
                                dv = datetime.datetime.strptime(dv[:10], "%Y-%m-%d").date()
                            elif isinstance(dv, datetime.datetime):
                                dv = dv.date()
                            if dv < datetime.date.today():
                                vencida = True
                    except Exception:
                        pass
                    tag = "vencida" if vencida else ("even" if i % 2 == 0 else "odd")
                    tree.insert("", "end", values=(
                        r["id"],
                        r.get("cliente_nome") or "N/A",
                        FormatUtils.format_money(r["valor_original"]),
                        FormatUtils.format_money(r["valor_pago"]),
                        FormatUtils.format_money(pend),
                        r.get("status", ""),
                        venc
                    ), tags=(tag,))
                    total_pend += pend
                tree.tag_configure("even", background=COR_GLASS)
                tree.tag_configure("odd", background=COR_CARD)
                tree.tag_configure("vencida", background="#1a0a0a", foreground="#ff6666")
                lbl_total_pend.config(
                    text=f"{len(rows)} conta(s)  |  Total Pendente: R$ {FormatUtils.format_money(total_pend)}")
            self.run_async(load, on_loaded)
        _carregar()

    def _kpi_click_comandas_abertas(self):
        """Abre dialogo com lista de comandas abertas, permitindo abrir detalhe."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Comandas Abertas - Detalhamento")
        dialog.geometry("800x500")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.COMANDAS} Comandas Abertas",
                 bg=COR_FUNDO2, fg=COR_BOTAO_AZUL,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_count_cmd = tk.Label(header, text="", bg=COR_FUNDO2, fg=COR_BOTAO_AZUL,
                                  font=("Segoe UI", 10, "bold"))
        lbl_count_cmd.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "numero", "total", "data", "qtd_itens")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=16)
        tree.heading("id", text="ID")
        tree.heading("numero", text="Numero")
        tree.heading("total", text="Total")
        tree.heading("data", text="Data Abertura")
        tree.heading("qtd_itens", text="Itens")
        tree.column("id", width=55, anchor="center")
        tree.column("numero", width=90, anchor="center")
        tree.column("total", width=120, anchor="e")
        tree.column("data", width=160)
        tree.column("qtd_itens", width=70, anchor="center")

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)

        def _abrir_detalhe():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione uma comanda.")
                return
            comanda_id = tree.item(sel[0])["values"][0]
            self._show_comanda_detalhe(comanda_id)

        def _ir_comandas():
            dialog.destroy()
            self.show_comandas()

        tree.bind("<Double-1>", lambda e: _abrir_detalhe())

        StyledButton(btn_frame, text=f"{Icons.EDIT} Abrir Detalhe",
                     command=_abrir_detalhe,
                     color=COR_BOTAO_AZUL, width=14).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.COMANDAS} Tela de Comandas",
                     command=_ir_comandas,
                     color=COR_BOTAO_VERDE, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT c.id, c.numero, c.total_itens, c.data_abertura, "
                    "(SELECT COUNT(*) FROM itens_comanda ic WHERE ic.comanda_id = c.id) as qtd_itens "
                    "FROM comandas c WHERE c.status='aberta' ORDER BY c.id DESC"
                )
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                total_geral = 0.0
                for i, r in enumerate(rows):
                    tag = "even" if i % 2 == 0 else "odd"
                    total_v = float(r.get("total_itens", 0))
                    tree.insert("", "end", values=(
                        r["id"],
                        r["numero"],
                        f"R$ {FormatUtils.format_money(total_v)}",
                        FormatUtils.format_date(r["data_abertura"]),
                        r.get("qtd_itens", 0)
                    ), tags=(tag,))
                    total_geral += total_v
                tree.tag_configure("even", background=COR_GLASS)
                tree.tag_configure("odd", background=COR_CARD)
                lbl_count_cmd.config(
                    text=f"{len(rows)} comanda(s) aberta(s)  |  Total: R$ {FormatUtils.format_money(total_geral)}")
            self.run_async(load, on_loaded)
        _carregar()

    def _kpi_click_armarios_abertos(self):
        """Abre dialogo com lista de armarios ocupados, permitindo gerenciar."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Armarios Abertos - Detalhamento")
        dialog.geometry("950x550")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        header = tk.Frame(dialog, bg=COR_FUNDO2, height=50)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text=f"{Icons.ARMARIO} Armarios em Uso",
                 bg=COR_FUNDO2, fg="#9C27B0",
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=15, pady=10)
        lbl_count_arm = tk.Label(header, text="", bg=COR_FUNDO2, fg="#9C27B0",
                                  font=("Segoe UI", 10, "bold"))
        lbl_count_arm.pack(side="right", padx=15)

        content = tk.Frame(dialog, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("arm_id", "numero", "cliente", "entrada", "total_consumo", "qtd_itens", "observacao")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=16)
        tree.heading("arm_id", text="ID")
        tree.heading("numero", text="Armario")
        tree.heading("cliente", text="Cliente")
        tree.heading("entrada", text="Entrada")
        tree.heading("total_consumo", text="Total Consumo")
        tree.heading("qtd_itens", text="Itens")
        tree.heading("observacao", text="Observacao")
        tree.column("arm_id", width=50, anchor="center")
        tree.column("numero", width=80, anchor="center")
        tree.column("cliente", width=180)
        tree.column("entrada", width=140)
        tree.column("total_consumo", width=120, anchor="e")
        tree.column("qtd_itens", width=60, anchor="center")
        tree.column("observacao", width=180)

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=10, pady=8)

        # Armazenar dados para uso nos botoes
        _armarios_data = [[]]

        def _gerenciar_armario():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione um armario.")
                return
            dialog.destroy()
            self.show_gerenciar_armarios()

        def _ir_armarios():
            dialog.destroy()
            self.show_gerenciar_armarios()

        tree.bind("<Double-1>", lambda e: _gerenciar_armario())

        StyledButton(btn_frame, text=f"{Icons.ARMARIO} Gerenciar Armario",
                     command=_gerenciar_armario,
                     color="#9C27B0", width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text=f"{Icons.ARMARIO} Tela de Armarios",
                     command=_ir_armarios,
                     color=COR_BOTAO_VERDE, width=16).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color=COR_FUNDO3, width=10).pack(side="right", padx=5)

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT a.id as arm_id, a.numero, a.descricao, a.localizacao, "
                    "u.id as uso_id, u.cliente_nome, u.observacao, u.data_entrada, "
                    "COALESCE(("
                    "  SELECT SUM(COALESCE(i.total,0) + COALESCE(i.adicionais_total,0)) "
                    "  FROM itens_armario_sauna i WHERE i.uso_armario_id = u.id"
                    "), 0) as total_consumo, "
                    "COALESCE(("
                    "  SELECT COUNT(*) FROM itens_armario_sauna i WHERE i.uso_armario_id = u.id"
                    "), 0) as qtd_itens "
                    "FROM armarios_sauna a "
                    "JOIN uso_armario_sauna u ON u.armario_id = a.id AND u.status = 'ocupado' "
                    "WHERE a.ativo = 1 "
                    "ORDER BY a.numero"
                )
            def on_loaded(rows):
                _armarios_data[0] = rows
                tree.delete(*tree.get_children())
                total_geral = 0.0
                for i, r in enumerate(rows):
                    tag = "even" if i % 2 == 0 else "odd"
                    total_c = float(r.get("total_consumo", 0))
                    tree.insert("", "end", values=(
                        r["arm_id"],
                        f"Armario {r['numero']}",
                        r.get("cliente_nome") or "N/A",
                        FormatUtils.format_date(r.get("data_entrada")),
                        f"R$ {FormatUtils.format_money(total_c)}",
                        r.get("qtd_itens", 0),
                        r.get("observacao") or ""
                    ), tags=(tag,))
                    total_geral += total_c
                tree.tag_configure("even", background=COR_GLASS)
                tree.tag_configure("odd", background=COR_CARD)
                lbl_count_arm.config(
                    text=f"{len(rows)} armario(s) em uso  |  Total: R$ {FormatUtils.format_money(total_geral)}")
            self.run_async(load, on_loaded)
        _carregar()

    # ========================================================================
    # TELA DE LICENCA (acessivel pelo menu principal)
    # ========================================================================
    def _show_tela_licenca_menu(self):
        """Exibe a tela de gerenciamento de licenca (acessivel pelo botao no dashboard).
        Mostra status da licenca, contra-chave e permite ativar/renovar."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Gerenciar Licenca")
        dialog.geometry("560x680")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()
        dialog.resizable(False, False)

        # Titulo
        tk.Label(dialog, text=f"{Icons.KEY} Gerenciar Licenca",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=(15, 5))

        NeonDivider(dialog, color=COR_PRIMARIA, width_pct=0.7).pack(fill="x", padx=30, pady=(5, 10))

        # Card de status
        card_status = GlassCard(dialog, glow_color=COR_PRIMARIA)
        card_status.pack(padx=25, pady=5, fill="x", ipadx=15, ipady=10)

        tk.Label(card_status, text="Status da Licenca", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 12, "bold")).pack(anchor="w", pady=(5, 8))

        # Carregar status em background
        status_label = tk.Label(card_status, text="Verificando...", bg=COR_GLASS,
                                fg=COR_TEXTO2, font=("Segoe UI", 10))
        status_label.pack(anchor="w")

        data_label = tk.Label(card_status, text="", bg=COR_GLASS,
                              fg=COR_TEXTO2, font=("Segoe UI", 10))
        data_label.pack(anchor="w")

        dias_label = tk.Label(card_status, text="", bg=COR_GLASS,
                              fg=COR_TEXTO2, font=("Segoe UI", 10))
        dias_label.pack(anchor="w")

        def carregar_status():
            try:
                valida = LicencaManager.verificar_licenca()
                expirada = LicencaManager.is_licenca_expirada()
                data_exp = LicencaManager.get_data_expiracao()
                dias = LicencaManager.get_dias_para_vencimento()
                return {"valida": valida, "expirada": expirada,
                        "data_exp": data_exp, "dias": dias}
            except Exception:
                return {"valida": False, "expirada": False,
                        "data_exp": None, "dias": -1}

        def on_status(result):
            try:
                if not dialog.winfo_exists():
                    return
            except Exception:
                return
            if result["valida"]:
                status_label.config(text="Licenca: ATIVA", fg=COR_SUCESSO)
                if result["data_exp"]:
                    try:
                        dt = datetime.datetime.strptime(result["data_exp"], "%Y-%m-%d")
                        data_label.config(
                            text=f"Vencimento: {dt.strftime('%d/%m/%Y')}",
                            fg=COR_SUCESSO)
                    except Exception:
                        data_label.config(text=f"Vencimento: {result['data_exp']}")
                if result["dias"] >= 0:
                    cor_dias = COR_SUCESSO
                    if result["dias"] <= 7:
                        cor_dias = COR_ALERTA
                    elif result["dias"] <= 30:
                        cor_dias = "#FFB74D"  # laranja
                    dias_label.config(
                        text=f"Dias restantes: {result['dias']}",
                        fg=cor_dias)
            elif result["expirada"]:
                status_label.config(text="Licenca: VENCIDA", fg=COR_ERRO)
                if result["data_exp"]:
                    try:
                        dt = datetime.datetime.strptime(result["data_exp"], "%Y-%m-%d")
                        data_label.config(
                            text=f"Vencida em: {dt.strftime('%d/%m/%Y')}",
                            fg=COR_ERRO)
                    except Exception:
                        data_label.config(text=f"Vencida em: {result['data_exp']}")
                if result["dias"] < 0:
                    dias_label.config(
                        text=f"Vencida ha {abs(result['dias'])} dia(s)",
                        fg=COR_ERRO)
            else:
                status_label.config(text="Licenca: SEM LICENCA", fg=COR_ALERTA)
                data_label.config(text="Nenhuma licenca cadastrada", fg=COR_ALERTA)
                dias_label.config(text="")

        self.run_async(carregar_status, on_status)

        NeonDivider(dialog, color=COR_GLASS_BORDER, width_pct=0.7).pack(
            fill="x", padx=30, pady=8)

        # Card de ativacao
        card_ativ = GlassCard(dialog, glow_color=COR_PRIMARIA)
        card_ativ.pack(padx=25, pady=5, fill="x", ipadx=15, ipady=10)

        # Contra-chave
        contra_chave = LicencaManager.gerar_contra_chave()
        tk.Label(card_ativ, text=f"{Icons.KEY} Contra-Chave:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(anchor="w", pady=(5, 3))

        ck_frame = tk.Frame(card_ativ, bg=COR_GLASS)
        ck_frame.pack(fill="x", pady=(0, 10))
        ck_entry = StyledEntry(ck_frame, width=22)
        ck_entry.insert(0, contra_chave)
        ck_entry.config(state="readonly")
        ck_entry.pack(side="left", fill="x", expand=True)

        def copiar_ck():
            try:
                self.root.clipboard_clear()
                self.root.clipboard_append(contra_chave)
                ToastManager.success("Contra-chave copiada!")
            except Exception:
                pass

        btn_cp = tk.Button(ck_frame, text=Icons.COPY, bg=COR_ENTRADA,
                           fg=COR_PRIMARIA, relief="flat", font=("Segoe UI", 12),
                           cursor="hand2", bd=0, command=copiar_ck)
        btn_cp.pack(side="right", padx=(5, 0))

        # Chave de ativacao
        tk.Label(card_ativ, text=f"{Icons.LOCK} Chave de Ativacao:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(anchor="w", pady=(5, 3))
        et_chave = StyledEntry(card_ativ, width=40)
        et_chave.pack(fill="x", pady=(0, 8))

        # Data de expiracao (DatePicker com calendario)
        tk.Label(card_ativ, text=f"{Icons.CALENDAR} Data de Expiracao:",
                 bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(anchor="w", pady=(0, 3))
        et_data = DatePickerEntry(card_ativ, width=15)
        et_data.pack(anchor="w", pady=(0, 10))

        # Botao ativar
        def ativar():
            chave = et_chave.get().strip()
            data_str = et_data.get().strip()
            if not chave or not data_str:
                messagebox.showwarning(
                    "Campos Obrigatorios",
                    "Para ativar a licenca, preencha todos os campos.\n\n"
                    "1. Cole a CHAVE DE ATIVACAO fornecida pelo suporte\n"
                    "2. Selecione a DATA DE EXPIRACAO no calendario\n\n"
                    "Se nao tem uma chave, entre em contato com o suporte.",
                    parent=dialog)
                return
            try:
                dt = datetime.datetime.strptime(data_str, "%d/%m/%Y")
                data_int = dt.strftime("%Y-%m-%d")
                try:
                    hoje_conf, _ = LicencaManager._obter_data_confiavel()
                except Exception:
                    hoje_conf = datetime.date.today()
                if dt.date() < hoje_conf:
                    messagebox.showerror(
                        "Data Invalida",
                        "A data de expiracao selecionada ja passou.\n\n"
                        "Selecione uma data futura conforme informado pelo suporte.",
                        parent=dialog)
                    return
            except ValueError:
                messagebox.showerror(
                    "Data Invalida",
                    "A data informada nao e valida.\n\n"
                    "Use o calendario para selecionar a data correta.\n"
                    "Formato: DD/MM/AAAA (ex: 31/12/2026)",
                    parent=dialog)
                return

            def do_ativ():
                return LicencaManager.ativar_licenca(contra_chave, chave, data_int)

            def on_ativ(ok):
                if ok:
                    # Setar flag para pular re-verificacao no show_main_menu
                    self._licenca_recem_ativada = True
                    # Resetar flag de fraude apos ativacao bem-sucedida
                    LicencaManager._date_fraud_detected = False
                    # Exibir mensagem de sucesso clara
                    messagebox.showinfo(
                        "Licenca Ativada",
                        f"Licenca ativada com sucesso!\n\n"
                        f"Sua licenca e valida ate {data_str}.\n\n"
                        f"O sistema esta habilitado.",
                        parent=dialog)
                    ToastManager.success("Licenca ativada com sucesso! Sistema habilitado.")
                    AuditLogger.log("LICENCA_ATIVADA",
                                    f"Licenca ativada ate {data_str} - sistema habilitado",
                                    usuario=Session.user_login)
                    # Reiniciar monitor periodico de licenca
                    self._start_licenca_monitor()
                    try:
                        dialog.destroy()
                    except Exception:
                        pass
                    # Recarregar menu principal para refletir novo status
                    self.show_main_menu()
                else:
                    messagebox.showerror(
                        "Chave Invalida",
                        "A chave de ativacao nao foi aceita.\n\n"
                        "COMO RESOLVER:\n"
                        "1. Verifique se copiou a chave completa\n"
                        "2. Confira a data de expiracao com o suporte\n"
                        "3. Solicite uma nova chave se necessario",
                        parent=dialog)

            self.run_async(do_ativ, on_ativ)

        StyledButton(card_ativ, text=f"{Icons.CHECK}  ATIVAR LICENCA",
                     command=ativar, color=COR_BOTAO_VERDE,
                     width=28).pack(pady=(5, 5))

        # Botao fechar
        StyledButton(dialog, text=f"{Icons.CROSS} Fechar",
                     command=dialog.destroy,
                     color="#2a3a5c", width=12).pack(pady=10)

    # ========================================================================
    # LOG DE AUDITORIA
    # ========================================================================
    def _show_audit_log(self):
        """Exibe o log de auditoria do sistema."""
        dialog = tk.Toplevel(self.root)
        dialog.title("Log de Auditoria")
        dialog.geometry("860x560")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text=f"{Icons.AUDIT} Log de Auditoria",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 15, "bold")).pack(pady=(14, 4))
        tk.Label(dialog,
                 text="Registro das acoes criticas do sistema",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack()

        NeonDivider(dialog, color=COR_PRIMARIA).pack(fill="x", padx=20, pady=8)

        # Busca
        busca_f = tk.Frame(dialog, bg=COR_FUNDO)
        busca_f.pack(fill="x", padx=20, pady=(0, 6))
        tk.Label(busca_f, text="Filtrar:", bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left")
        et_filtro = StyledEntry(busca_f, width=40)
        et_filtro.pack(side="left", padx=8)

        # Treeview
        cols = ("timestamp", "usuario", "acao", "detalhes")
        tree = ttk.Treeview(dialog, columns=cols, show="headings", height=18)
        tree.heading("timestamp", text="Data/Hora")
        tree.heading("usuario", text="Usuario")
        tree.heading("acao", text="Acao")
        tree.heading("detalhes", text="Detalhes")
        tree.column("timestamp", width=140, anchor="center")
        tree.column("usuario", width=110, anchor="center")
        tree.column("acao", width=160)
        tree.column("detalhes", width=380)

        vsb = ttk.Scrollbar(dialog, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)

        tree_f = tk.Frame(dialog, bg=COR_FUNDO)
        tree_f.pack(fill="both", expand=True, padx=20, pady=4)
        vsb2 = ttk.Scrollbar(tree_f, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb2.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb2.pack(side="right", fill="y")

        all_entries = [None]

        def _parse_entry(linha):
            # Formato: [2026-03-15 10:30:00] [usuario] ACAO | detalhes
            try:
                ts = linha[1:20] if len(linha) > 20 else ""
                resto = linha[22:] if len(linha) > 22 else linha
                partes = resto.split("]", 1)
                usuario = partes[0].strip("[").strip() if len(partes) > 1 else ""
                resto2 = partes[1].strip() if len(partes) > 1 else resto
                if "|" in resto2:
                    acao, det = resto2.split("|", 1)
                else:
                    acao, det = resto2, ""
                return ts, usuario, acao.strip(), det.strip()
            except Exception:
                return "", "", linha, ""

        def _carregar():
            entradas = AuditLogger.get_entries(500)
            all_entries[0] = entradas
            return entradas

        def _preencher(entradas, filtro=""):
            tree.delete(*tree.get_children())
            for i, linha in enumerate(reversed(entradas)):
                if filtro and filtro.lower() not in linha.lower():
                    continue
                ts, usr, acao, det = _parse_entry(linha)
                tag = "even" if i % 2 == 0 else "odd"
                tree.insert("", "end", values=(ts, usr, acao, det), tags=(tag,))
            tree.tag_configure("even", background=COR_GLASS)
            tree.tag_configure("odd", background=COR_CARD)

        def _on_filtro(event=None):
            if all_entries[0]:
                _preencher(all_entries[0], et_filtro.get().strip())

        et_filtro.bind("<KeyRelease>", _on_filtro)

        def _on_loaded(entradas):
            _preencher(entradas)

        self.run_async(_carregar, _on_loaded)

        # Botoes
        btn_f = tk.Frame(dialog, bg=COR_FUNDO)
        btn_f.pack(fill="x", padx=20, pady=8)

        def _exportar():
            if all_entries[0]:
                conteudo = "\n".join(all_entries[0])
                DataExporter.export_text(conteudo, "audit_log", parent=dialog)

        StyledButton(btn_f, text=f"{Icons.EXPORT} Exportar TXT",
                     command=_exportar,
                     color=COR_BOTAO_AZUL, width=16).pack(side="left", padx=4)
        StyledButton(btn_f, text=f"{Icons.REFRESH} Atualizar",
                     command=lambda: self.run_async(_carregar, _on_loaded),
                     color=COR_FUNDO3, width=12).pack(side="left", padx=4)
        StyledButton(btn_f, text=f"{Icons.CROSS} Fechar",
                     command=dialog.destroy,
                     color="#2a3a5c", width=10).pack(side="right", padx=4)

    # ========================================================================
    # VERIFICACAO DE CAIXA ABERTO PARA VENDAS (TRAVA DE SEGURANCA)
    # ========================================================================
    def _verificar_caixa_aberto_para_venda(self, callback_sucesso, origem="venda"):
        """Verifica em tempo real no banco de dados se existe um caixa aberto.
        Se existir, atualiza Session.caixa_id e executa callback_sucesso.
        Se nao existir, bloqueia a operacao e exibe mensagem ao usuario.
        Parametros:
            callback_sucesso: funcao a ser chamada se o caixa estiver aberto.
            origem: string identificando a origem da verificacao (para log)."""
        def check_caixa_db():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT id FROM caixa WHERE status = 'aberto' ORDER BY id DESC LIMIT 1"
            )
            return rows

        def on_check_result(rows):
            if rows:
                # Caixa aberto encontrado - atualizar Session e prosseguir
                Session.caixa_id = rows[0]["id"]
                self.status_bar.set_caixa(True, Session.caixa_id)
                callback_sucesso()
            else:
                # Nenhum caixa aberto - bloquear operacao
                Session.caixa_id = 0
                self.caixa_aberto = None
                self.status_bar.set_caixa(False)
                AuditLogger.log("VENDA_BLOQUEADA_CAIXA_FECHADO",
                                f"Tentativa de {origem} bloqueada - caixa fechado",
                                usuario=Session.user_login)
                ToastManager.error(
                    "Venda bloqueada! Abra o caixa antes de vender.", duration=5000)
                messagebox.showwarning(
                    "Caixa Fechado - Venda Bloqueada",
                    f"{Icons.BLOCKED} Nao e possivel realizar vendas com o caixa fechado.\n\n"
                    "Para continuar, abra o caixa primeiro:\n"
                    "  1. Acesse o menu 'Caixa' (F3)\n"
                    "  2. Clique em 'Abrir Caixa'\n"
                    "  3. Informe o valor de abertura\n\n"
                    "Apos abrir o caixa, tente novamente.")

        self.run_async(check_caixa_db, on_check_result)

    # ========================================================================
    # TELA DE VENDAS
    # ========================================================================
    def show_venda(self):
        # === TRAVA DE CAIXA: Verificar se o caixa esta aberto antes de abrir a tela de vendas ===
        self._verificar_caixa_aberto_para_venda(
            callback_sucesso=self._show_venda_ui,
            origem="abrir tela de venda"
        )

    def _show_venda_ui(self):
        """Renderiza a tela de vendas (chamada somente apos verificacao de caixa aberto)."""
        self.clear_container()
        self.carrinho = []
        self._armario_pendente = None
        self._editando_venda_id = None
        self.venda_cliente_id = 0
        self.venda_cliente_nome = "Cliente nao informado"
        self.venda_vendedor_id = 0
        self.venda_entregador_id = 0
        # --- Estado do MODULO DE ENTREGA ---
        self.venda_para_entrega = False
        self.venda_entregador_nome = ""
        self.venda_bairro_entrega = ""
        self.venda_taxa_entrega = 0.0
        AuditLogger.log("VENDA_ABERTA", usuario=Session.user_login)

        # Barra superior
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        btn_voltar = StyledButton(top, text="◀ Voltar",
                                  command=self.show_main_menu,
                                  color=COR_FUNDO3, width=10)
        btn_voltar.pack(side="left", padx=10, pady=5)
        add_tooltip(btn_voltar, "Voltar ao Menu Principal (F10)")

        self._lbl_titulo_venda = tk.Label(top, text="▶ Nova Venda", bg=COR_FUNDO2,
                 fg=COR_PRIMARIA, font=("Segoe UI", 14, "bold"))
        self._lbl_titulo_venda.pack(side="left", padx=10)
        tk.Label(top, text="Ctrl+Z=Desfazer  |  Enter=Adicionar  |  F10=Menu",
                 bg=COR_FUNDO2, fg=COR_TEXTO2,
                 font=("Segoe UI", 8)).pack(side="left", padx=10)

        # Botoes Sair e Trocar de Usuario na barra superior da venda
        btn_sair_v = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        btn_sair_v.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_sair_v, "Fechar o PDV Pro (Alt+F4)")

        btn_trocar_v = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        btn_trocar_v.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_trocar_v, "Trocar de usuario (Ctrl+L)")

        # Area principal
        main = tk.Frame(self.main_container, bg=COR_FUNDO)
        main.pack(fill="both", expand=True, padx=10, pady=5)

        # Painel esquerdo - Busca e produtos
        left = tk.Frame(main, bg=COR_FUNDO)
        left.pack(side="left", fill="both", expand=True, padx=(0, 5))

        # Cliente
        cli_frame = tk.Frame(left, bg=COR_GLASS, highlightthickness=1, highlightbackground=COR_GLASS_BORDER)
        cli_frame.pack(fill="x", pady=(0, 5))
        tk.Label(cli_frame, text="Cliente:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left", padx=5)
        self.lbl_cliente_venda = tk.Label(cli_frame, text=self.venda_cliente_nome,
                                          bg=COR_CARD, fg=COR_ALERTA,
                                          font=("Segoe UI", 10, "bold"))
        self.lbl_cliente_venda.pack(side="left", padx=5)
        StyledButton(cli_frame, text="▶ Selecionar", command=self.selecionar_cliente_venda,
                     color=COR_BOTAO_AZUL, width=10).pack(side="right", padx=5, pady=3)

        # Busca produto
        busca_frame = tk.Frame(left, bg=COR_GLASS, highlightthickness=1, highlightbackground=COR_GLASS_BORDER)
        busca_frame.pack(fill="x", pady=(0, 5))
        tk.Label(busca_frame, text="Codigo/Barras:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left", padx=5)
        self.et_codigo_barras = StyledEntry(busca_frame, width=20)
        self.et_codigo_barras.pack(side="left", padx=5, fill="x", expand=True)
        self.et_codigo_barras.bind("<Return>", lambda e: self.buscar_por_codigo())
        StyledButton(busca_frame, text="⌕ Buscar", command=self.buscar_produto_dialog,
                     color=COR_BOTAO_AZUL, width=8).pack(side="right", padx=5, pady=3)
        StyledButton(busca_frame, text="Add", command=self.buscar_por_codigo,
                     color=COR_BOTAO_VERDE, width=6).pack(side="right", padx=2, pady=3)

        # === MONTE SEU PRATO (assistente guiado do cardapio Delicias Food) ===
        monte_frame = tk.Frame(left, bg=COR_FUNDO)
        monte_frame.pack(fill="x", pady=(0, 5))
        btn_monte = StyledButton(
            monte_frame, text="🍽  MONTE SEU PRATO",
            command=self.montar_prato_dialog,
            color=COR_BOTAO_LARANJA, width=30)
        btn_monte.pack(anchor="w")
        add_tooltip(btn_monte,
                    "Montar um prato: tamanho, proteinas, acompanhamentos e extras")

        # Carrinho (Treeview)
        cart_header = tk.Frame(left, bg=COR_FUNDO)
        cart_header.pack(fill="x", pady=(5, 2))
        tk.Label(cart_header, text=f"{Icons.VENDAS} Carrinho:",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 11, "bold")).pack(side="left")
        self._lbl_itens_carrinho = tk.Label(
            cart_header, text="0 produto(s)", bg=COR_FUNDO,
            fg=COR_TEXTO2, font=("Segoe UI", 9))
        self._lbl_itens_carrinho.pack(side="left", padx=10)
        add_tooltip(self._lbl_itens_carrinho,
                    "Total de itens no carrinho. Ctrl+Z = desfazer ultimo.")

        tree_frame = tk.Frame(left, bg=COR_FUNDO)
        tree_frame.pack(fill="both", expand=True)

        cols = ("item", "descricao", "qtd", "preco", "total")
        self.tree_carrinho = ttk.Treeview(tree_frame, columns=cols, show="headings", height=12)
        self.tree_carrinho.heading("item", text="◆")
        self.tree_carrinho.heading("descricao", text="Descricao")
        self.tree_carrinho.heading("qtd", text="Qtd")
        self.tree_carrinho.heading("preco", text="Preco")
        self.tree_carrinho.heading("total", text="Total")
        self.tree_carrinho.column("item", width=40, anchor="center")
        self.tree_carrinho.column("descricao", width=200)
        self.tree_carrinho.column("qtd", width=60, anchor="center")
        self.tree_carrinho.column("preco", width=80, anchor="e")
        self.tree_carrinho.column("total", width=80, anchor="e")

        vsb = ttk.Scrollbar(tree_frame, orient="vertical", command=self.tree_carrinho.yview)
        self.tree_carrinho.configure(yscrollcommand=vsb.set)
        self.tree_carrinho.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        # Botoes do carrinho
        btn_cart = tk.Frame(left, bg=COR_FUNDO)
        btn_cart.pack(fill="x", pady=5)
        StyledButton(btn_cart, text="✎ Editar Qtd", command=self.editar_qtd_carrinho,
                     color=COR_BOTAO_AZUL, width=10).pack(side="left", padx=3)
        StyledButton(btn_cart, text="✖ Remover", command=self.remover_item_carrinho,
                     color=COR_ERRO, width=10).pack(side="left", padx=3)
        StyledButton(btn_cart, text="↻ Limpar", command=self.limpar_carrinho,
                     color="#2a3a5c", width=10).pack(side="left", padx=3)

        # Painel direito - Totais e finalizacao
        right = tk.Frame(main, bg=COR_GLASS, width=320,
                         highlightthickness=1, highlightbackground=COR_GLASS_BORDER)
        right.pack(side="right", fill="y", padx=(5, 0))
        right.pack_propagate(False)

        tk.Label(right, text="◆ Resumo da Venda", bg=COR_GLASS, fg=COR_PRIMARIA,
                 font=("Segoe UI", 13, "bold")).pack(pady=10)

        # Subtotal
        self.lbl_subtotal = tk.Label(right, text="▶ Subtotal: R$ 0,00", bg=COR_GLASS,
                                      fg=COR_TEXTO, font=("Segoe UI", 11))
        self.lbl_subtotal.pack(anchor="w", padx=15, pady=2)

        # Desconto
        desc_frame = tk.Frame(right, bg=COR_GLASS)
        desc_frame.pack(fill="x", padx=15, pady=3)
        tk.Label(desc_frame, text="Desconto:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left")
        self.combo_desc_tipo = ttk.Combobox(desc_frame, values=["Valor (R$)", "Percentual (%)"],
                                             width=12, state="readonly")
        self.combo_desc_tipo.current(0)
        self.combo_desc_tipo.pack(side="left", padx=5)
        self.et_desconto = StyledEntry(desc_frame, width=8)
        self.et_desconto.insert(0, "0")
        self.et_desconto.pack(side="left")
        self.et_desconto.bind("<KeyRelease>", lambda e: self.atualizar_totais_venda())

        # Acrescimo
        acre_frame = tk.Frame(right, bg=COR_GLASS)
        acre_frame.pack(fill="x", padx=15, pady=3)
        tk.Label(acre_frame, text="Acrescimo:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left")
        self.combo_acre_tipo = ttk.Combobox(acre_frame, values=["Valor (R$)", "Percentual (%)"],
                                             width=12, state="readonly")
        self.combo_acre_tipo.current(0)
        self.combo_acre_tipo.pack(side="left", padx=5)
        self.et_acrescimo = StyledEntry(acre_frame, width=8)
        self.et_acrescimo.insert(0, "0")
        self.et_acrescimo.pack(side="left")
        self.et_acrescimo.bind("<KeyRelease>", lambda e: self.atualizar_totais_venda())

        # === MODULO DE ENTREGA ===
        self._entrega_card = tk.Frame(right, bg=COR_GLASS)
        self._entrega_card.pack(fill="x", padx=15, pady=(8, 2))
        self._var_entrega = tk.IntVar(value=0)
        tk.Checkbutton(
            self._entrega_card, text="🛵  Pedido para ENTREGA",
            variable=self._var_entrega, command=self._toggle_entrega,
            bg=COR_GLASS, fg=COR_ALERTA, selectcolor=COR_CARD,
            activebackground=COR_GLASS, activeforeground=COR_PRIMARIA,
            font=("Segoe UI", 10, "bold")).pack(anchor="w")

        # Sub-frame com os dados da entrega (oculto ate marcar a opcao)
        self._entrega_frame = tk.Frame(right, bg=COR_GLASS)

        fb = tk.Frame(self._entrega_frame, bg=COR_GLASS)
        fb.pack(fill="x", pady=1)
        tk.Label(fb, text="Bairro:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 9), width=8, anchor="w").pack(side="left")
        self.lbl_bairro_entrega = tk.Label(fb, text="(nenhum)", bg=COR_CARD,
                                           fg=COR_TEXTO, font=("Segoe UI", 9))
        self.lbl_bairro_entrega.pack(side="left", fill="x", expand=True, padx=3)
        StyledButton(fb, text="Buscar", command=self._selecionar_bairro_entrega,
                     color=COR_BOTAO_AZUL, width=6).pack(side="right")

        fe = tk.Frame(self._entrega_frame, bg=COR_GLASS)
        fe.pack(fill="x", pady=1)
        tk.Label(fe, text="Entreg.:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 9), width=8, anchor="w").pack(side="left")
        self.lbl_entregador_venda = tk.Label(fe, text="(a definir)", bg=COR_CARD,
                                             fg=COR_TEXTO, font=("Segoe UI", 9))
        self.lbl_entregador_venda.pack(side="left", fill="x", expand=True, padx=3)
        StyledButton(fe, text="Buscar", command=self._selecionar_entregador_venda,
                     color=COR_BOTAO_AZUL, width=6).pack(side="right")

        tk.Label(self._entrega_frame, text="Endereco de entrega:", bg=COR_GLASS,
                 fg=COR_TEXTO, font=("Segoe UI", 9)).pack(anchor="w", pady=(4, 0))
        self.et_endereco_entrega = StyledEntry(self._entrega_frame, width=30)
        self.et_endereco_entrega.pack(fill="x")

        tk.Label(self._entrega_frame, text="WhatsApp:", bg=COR_GLASS,
                 fg=COR_TEXTO, font=("Segoe UI", 9)).pack(anchor="w", pady=(4, 0))
        self.et_whatsapp_entrega = StyledEntry(self._entrega_frame, width=30)
        self.et_whatsapp_entrega.pack(fill="x")

        self.lbl_taxa_entrega = tk.Label(self._entrega_frame, text="Taxa de entrega: R$ 0,00",
                                         bg=COR_GLASS, fg=COR_ALERTA,
                                         font=("Segoe UI", 10, "bold"))
        self.lbl_taxa_entrega.pack(anchor="w", pady=4)

        # Total
        self.lbl_total_venda = tk.Label(right, text="TOTAL: R$ 0,00", bg=COR_CARD,
                                         fg=COR_SUCESSO, font=("Segoe UI", 16, "bold"))
        self.lbl_total_venda.pack(pady=15)

        # Observacao
        tk.Label(right, text="Observacao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(anchor="w", padx=15)
        self.et_obs_venda = tk.Text(right, height=3, bg=COR_ENTRADA, fg=COR_TEXTO,
                                     font=("Segoe UI", 10), relief="flat",
                                     insertbackground=COR_TEXTO)
        self.et_obs_venda.pack(fill="x", padx=15, pady=5)

        # Botao Finalizar
        StyledButton(right, text="FINALIZAR VENDA", command=self.finalizar_venda,
                     color=COR_BOTAO_VERDE, width=25).pack(pady=15)

    def selecionar_cliente_venda(self):
        self._selecionar_registro(
            "clientes", "Selecionar Cliente",
            "SELECT id, nome, celular FROM clientes WHERE ativo = 1 ORDER BY nome",
            ["ID", "Nome", "Celular"],
            lambda row: self._set_cliente_venda(row)
        )

    def _set_cliente_venda(self, row):
        if row:
            self.venda_cliente_id = row[0]
            self.venda_cliente_nome = row[1]
            self.lbl_cliente_venda.config(text=self.venda_cliente_nome)
            # Preencher automaticamente os dados de entrega a partir do cadastro
            self._preencher_entrega_do_cliente(row[0])

    def _preencher_entrega_do_cliente(self, cliente_id):
        """Ao selecionar um cliente, puxa endereco, bairro (com a taxa) e
        WhatsApp do cadastro e preenche a secao de entrega automaticamente."""
        if not cliente_id:
            return

        def load():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT nome, celular, endereco, numero, bairro, cidade, uf "
                "FROM clientes WHERE id = %s LIMIT 1", (cliente_id,))
            taxas = db.execute_query(
                "SELECT bairro, taxa FROM taxa_entrega_bairro WHERE ativo = 1")
            return {"cli": rows[0] if rows else None, "taxas": taxas or []}

        def done(data):
            cli = (data or {}).get("cli")
            if not cli:
                return

            # Montar o endereco completo
            partes = []
            if cli.get("endereco"):
                partes.append(str(cli["endereco"]))
            if cli.get("numero"):
                partes.append(f"n {cli['numero']}")
            if cli.get("bairro"):
                partes.append(str(cli["bairro"]))
            if cli.get("cidade"):
                cid = str(cli["cidade"])
                if cli.get("uf"):
                    cid += f"/{cli['uf']}"
                partes.append(cid)
            endereco = ", ".join(partes)
            celular = (cli.get("celular") or "").strip()
            bairro_cli = (cli.get("bairro") or "").strip()

            # So ativa a entrega se o cliente tiver algum dado de entrega
            if not (endereco or celular or bairro_cli):
                return

            # Mostrar a secao de entrega (se ainda nao estiver marcada)
            if not getattr(self, "venda_para_entrega", False):
                try:
                    self._var_entrega.set(1)
                    self._toggle_entrega()
                except Exception:
                    pass

            # Preencher endereco e WhatsApp
            try:
                self.et_endereco_entrega.delete(0, tk.END)
                self.et_endereco_entrega.insert(0, endereco)
            except Exception:
                pass
            try:
                self.et_whatsapp_entrega.delete(0, tk.END)
                self.et_whatsapp_entrega.insert(0, celular)
            except Exception:
                pass

            # Casar o bairro do cliente com a taxa cadastrada (se existir)
            if bairro_cli:
                taxa = 0.0
                bairro_final = bairro_cli
                for t in data.get("taxas", []):
                    if str(t.get("bairro", "")).strip().lower() == bairro_cli.lower():
                        try:
                            taxa = float(t.get("taxa") or 0)
                        except Exception:
                            taxa = 0.0
                        bairro_final = t.get("bairro") or bairro_cli
                        break
                self.venda_bairro_entrega = bairro_final
                self.venda_taxa_entrega = taxa
                try:
                    if taxa > 0:
                        self.lbl_bairro_entrega.config(
                            text=f"{bairro_final}  (R$ {FormatUtils.format_money(taxa)})")
                    else:
                        self.lbl_bairro_entrega.config(
                            text=f"{bairro_final}  (taxa nao cadastrada)")
                except Exception:
                    pass

            self.atualizar_totais_venda()
            ToastManager.info("Dados de entrega preenchidos pelo cadastro do cliente.")

        self.run_async(load, done)

    # ------------------------------------------------------------------
    # MODULO DE ENTREGA  -  captura de dados na tela de venda
    # ------------------------------------------------------------------
    def _toggle_entrega(self):
        """Mostra/oculta a secao de entrega e recalcula o total."""
        self.venda_para_entrega = bool(self._var_entrega.get())
        if self.venda_para_entrega:
            self._entrega_frame.pack(fill="x", padx=15, pady=2,
                                     after=self._entrega_card)
        else:
            self._entrega_frame.pack_forget()
            # Limpar dados de entrega ao desmarcar
            self.venda_taxa_entrega = 0.0
            self.venda_bairro_entrega = ""
            self.venda_entregador_id = 0
            self.venda_entregador_nome = ""
            try:
                self.lbl_bairro_entrega.config(text="(nenhum)")
                self.lbl_entregador_venda.config(text="(a definir)")
            except Exception:
                pass
        self.atualizar_totais_venda()

    def _selecionar_bairro_entrega(self):
        """Seleciona o bairro de entrega (define a taxa automaticamente)."""
        def _on_bairro(row):
            if not row:
                return
            self.venda_bairro_entrega = row[1]
            try:
                self.venda_taxa_entrega = float(row[2])
            except Exception:
                self.venda_taxa_entrega = 0.0
            self.lbl_bairro_entrega.config(
                text=f"{row[1]}  (R$ {FormatUtils.format_money(self.venda_taxa_entrega)})")
            self.atualizar_totais_venda()

        self._selecionar_registro(
            "taxa_entrega_bairro", "Selecionar Bairro / Taxa",
            "SELECT id, bairro, taxa FROM taxa_entrega_bairro WHERE ativo = 1 ORDER BY bairro",
            ["ID", "Bairro", "Taxa (R$)"],
            _on_bairro
        )

    def _selecionar_entregador_venda(self):
        """Seleciona o entregador responsavel pela entrega."""
        def _on_entregador(row):
            if not row:
                return
            self.venda_entregador_id = row[0]
            self.venda_entregador_nome = row[1]
            self.lbl_entregador_venda.config(text=row[1])

        self._selecionar_registro(
            "entregadores", "Selecionar Entregador",
            "SELECT id, nome, celular FROM entregadores WHERE ativo = 1 ORDER BY nome",
            ["ID", "Nome", "Celular"],
            _on_entregador
        )

    def buscar_por_codigo(self):
        codigo = self.et_codigo_barras.get().strip()
        if not codigo:
            return
        def buscar():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT id, descricao, preco_venda, estoque, codigo_barras "
                "FROM produtos WHERE ativo = 1 AND (codigo = %s OR codigo_barras = %s) LIMIT 1",
                (codigo, codigo)
            )
            return rows
        def on_found(rows):
            self.et_codigo_barras.delete(0, tk.END)
            if rows:
                p = rows[0]
                self.adicionar_ao_carrinho(p["id"], p["descricao"], p["preco_venda"], 1)
            else:
                self.show_error("Produto nao encontrado com este codigo.")
        self.run_async(buscar, on_found)

    def buscar_produto_dialog(self):
        self._selecionar_registro(
            "produtos", "Buscar Produto",
            "SELECT id, descricao, preco_venda, estoque FROM produtos WHERE ativo = 1 ORDER BY descricao",
            ["ID", "Descricao", "Preco", "Estoque"],
            lambda row: self.adicionar_ao_carrinho(row[0], row[1], float(row[2]), 1) if row else None
        )

    def adicionar_ao_carrinho(self, produto_id, descricao, preco, qtd,
                               tipo_produto_id=None, tipo_produto_desc=""):
        # Verificar se ja existe no carrinho
        for item in self.carrinho:
            if item.produto_id == produto_id:
                item.quantidade += qtd
                item.total = item.quantidade * item.preco_unitario
                self.atualizar_tree_carrinho()
                return
        item = ItemVenda()
        item.produto_id = produto_id
        item.descricao_produto = descricao
        item.preco_unitario = float(preco)
        item.quantidade = qtd
        item.total = item.quantidade * item.preco_unitario
        item.tipo_produto_id = tipo_produto_id
        item.tipo_produto_desc = tipo_produto_desc
        self.carrinho.append(item)
        self.atualizar_tree_carrinho()
        # Buscar tipo_produto_id do banco se nao foi informado
        if tipo_produto_id is None:
            def _buscar_tipo():
                try:
                    db = DatabaseHelper.get_instance()
                    rows = db.execute_query(
                        "SELECT p.tipo_produto_id, COALESCE(tp.descricao, '') as tipo_desc "
                        "FROM produtos p LEFT JOIN tipos_produto tp ON p.tipo_produto_id = tp.id "
                        "WHERE p.id = %s LIMIT 1",
                        (produto_id,)
                    )
                    return rows
                except Exception:
                    return []
            def _on_tipo(rows):
                if rows and rows[0].get("tipo_produto_id"):
                    item.tipo_produto_id = rows[0]["tipo_produto_id"]
                    item.tipo_produto_desc = rows[0].get("tipo_desc", "")
            self.run_async(_buscar_tipo, _on_tipo)

    def atualizar_tree_carrinho(self):
        self.tree_carrinho.delete(*self.tree_carrinho.get_children())
        for i, item in enumerate(self.carrinho, 1):
            qtd_str = (f"{item.quantidade:.0f}"
                       if item.quantidade == int(item.quantidade)
                       else f"{item.quantidade:.3f}")
            tag = "even" if i % 2 == 0 else "odd"
            self.tree_carrinho.insert("", "end", values=(
                i, item.descricao_produto, qtd_str,
                FormatUtils.format_money(item.preco_unitario),
                FormatUtils.format_money(item.total)
            ), tags=(tag,))
        self.tree_carrinho.tag_configure("even", background=COR_GLASS)
        self.tree_carrinho.tag_configure("odd", background=COR_CARD)
        self.atualizar_totais_venda()
        # Atualizar contador de itens se existir
        try:
            n = len(self.carrinho)
            total_qtd = sum(i.quantidade for i in self.carrinho)
            self._lbl_itens_carrinho.config(
                text=f"{n} produto(s)  |  {total_qtd:.0f} un."
                     if total_qtd == int(total_qtd)
                     else f"{n} produto(s)  |  {total_qtd:.3f} un.")
        except Exception:
            pass

    def atualizar_totais_venda(self):
        subtotal = sum(item.total for item in self.carrinho)
        try:
            desc_val = FormatUtils.parse_money(self.et_desconto.get())
        except Exception:
            desc_val = 0
        try:
            acre_val = FormatUtils.parse_money(self.et_acrescimo.get())
        except Exception:
            acre_val = 0

        desc_tipo = self.combo_desc_tipo.get()
        acre_tipo = self.combo_acre_tipo.get()

        desconto = desc_val if "R$" in desc_tipo else (subtotal * desc_val / 100)
        acrescimo = acre_val if "R$" in acre_tipo else (subtotal * acre_val / 100)

        taxa_entrega = (self.venda_taxa_entrega or 0) if getattr(self, "venda_para_entrega", False) else 0
        total = subtotal - desconto + acrescimo + taxa_entrega
        if total < 0:
            total = 0

        # Atualizar rotulo da taxa de entrega (se a secao existir)
        try:
            self.lbl_taxa_entrega.config(
                text=f"Taxa de entrega: R$ {FormatUtils.format_money(taxa_entrega)}")
        except Exception:
            pass

        self.lbl_subtotal.config(
            text=f"Subtotal: R$ {FormatUtils.format_money(subtotal)}")
        self.lbl_total_venda.config(
            text=f"TOTAL: R$ {FormatUtils.format_money(total)}")

        # Feedback visual: total muda de cor conforme valor
        if total > 0:
            self.lbl_total_venda.config(fg=COR_SUCESSO)
        else:
            self.lbl_total_venda.config(fg=COR_TEXTO2)

    def editar_qtd_carrinho(self):
        sel = self.tree_carrinho.selection()
        if not sel:
            ToastManager.warning("Selecione um item do carrinho.")
            return
        idx = self.tree_carrinho.index(sel[0])
        item = self.carrinho[idx]
        nova_qtd = simpledialog.askfloat(
            "Editar Quantidade",
            f"Nova quantidade para:\n{item.descricao_produto}",
            initialvalue=item.quantidade,
            minvalue=0.001, parent=self.root)
        if nova_qtd:
            self.carrinho[idx].quantidade = nova_qtd
            self.carrinho[idx].total = nova_qtd * self.carrinho[idx].preco_unitario
            self.atualizar_tree_carrinho()
            ToastManager.info(f"Qtd atualizada: {nova_qtd:.3f}")

    def remover_item_carrinho(self):
        sel = self.tree_carrinho.selection()
        if not sel:
            ToastManager.warning("Selecione um item para remover.")
            return
        idx = self.tree_carrinho.index(sel[0])
        nome = self.carrinho[idx].descricao_produto
        del self.carrinho[idx]
        self.atualizar_tree_carrinho()
        ToastManager.info(f"Removido: {nome}")

    # ========================================================================
    # ASSISTENTE "MONTE SEU PRATO"  (DELICIAS FOOD)
    # Fluxo: 1) Tamanho  ->  2) Proteinas (limitado pelo tamanho)
    #        3) Acompanhamentos  ->  4) Revisao/adicionar ao carrinho
    #        5) Extras (sucos, refrigerantes, aguas, sobremesas)
    # ========================================================================
    def montar_prato_dialog(self):
        """Abre o assistente guiado para montar um prato do cardapio."""
        db = DatabaseHelper.get_instance()

        def carregar():
            tamanhos = db.execute_query(
                "SELECT p.id, p.descricao, p.codigo, p.preco_venda, p.tipo_produto_id "
                "FROM produtos p JOIN tipos_produto t ON p.tipo_produto_id = t.id "
                "WHERE t.descricao = %s AND p.ativo = 1 ORDER BY p.id",
                (DatabaseHelper.CAT_MONTE_PRATO,)
            )
            proteinas = db.execute_query(
                "SELECT p.id, p.descricao FROM produtos p "
                "JOIN tipos_produto t ON p.tipo_produto_id = t.id "
                "WHERE t.descricao = %s AND p.ativo = 1 ORDER BY p.descricao",
                (DatabaseHelper.CAT_PROTEINAS,)
            )
            acomp = db.execute_query(
                "SELECT p.id, p.descricao, COALESCE(p.codigo,'') AS codigo FROM produtos p "
                "JOIN tipos_produto t ON p.tipo_produto_id = t.id "
                "WHERE t.descricao = %s AND p.ativo = 1 ORDER BY p.codigo, p.descricao",
                (DatabaseHelper.CAT_ACOMPANHAMENTOS,)
            )
            return {"tamanhos": tamanhos, "proteinas": proteinas, "acomp": acomp}

        def on_loaded(data):
            if not data or not data.get("tamanhos"):
                ErroAmigavel.mostrar_aviso(
                    "O cardapio ainda nao foi cadastrado.\n\n"
                    "Reinicie o sistema uma vez para que o cardapio do "
                    "Delicias Food seja criado automaticamente.",
                    titulo="Monte Seu Prato", parent=self.root)
                return
            self._abrir_wizard_prato(data)

        self.run_async(carregar, on_loaded)

    def _abrir_wizard_prato(self, data):
        tamanhos = data["tamanhos"]
        proteinas = data["proteinas"]
        acomp = data["acomp"]

        # Estado da montagem
        st = {"tam": None, "n_prot": 1, "proteinas": [], "acomp": []}

        dialog = tk.Toplevel(self.root)
        dialog.title("Monte Seu Prato - Delicias Food")
        dialog.geometry("760x620")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        # Cabecalho
        header = tk.Frame(dialog, bg=COR_FUNDO2, height=58)
        header.pack(fill="x")
        header.pack_propagate(False)
        tk.Label(header, text="🍽  MONTE SEU PRATO", bg=COR_FUNDO2,
                 fg=COR_BOTAO_LARANJA, font=("Segoe UI", 16, "bold")).pack(
                     side="left", padx=18, pady=10)
        self._lbl_passo_prato = tk.Label(header, text="", bg=COR_FUNDO2,
                                         fg=COR_TEXTO2, font=("Segoe UI", 10))
        self._lbl_passo_prato.pack(side="right", padx=18)

        # Corpo (re-renderizado a cada etapa)
        body = tk.Frame(dialog, bg=COR_FUNDO)
        body.pack(fill="both", expand=True, padx=12, pady=8)

        # Rodape (botoes de navegacao)
        footer = tk.Frame(dialog, bg=COR_FUNDO2, height=56)
        footer.pack(fill="x")
        footer.pack_propagate(False)

        def _limpar(frame):
            for w in frame.winfo_children():
                w.destroy()

        def _scroll_area(parent):
            """Cria uma area com rolagem e retorna o frame interno."""
            wrap = tk.Frame(parent, bg=COR_FUNDO)
            wrap.pack(fill="both", expand=True)
            canvas = tk.Canvas(wrap, bg=COR_FUNDO, highlightthickness=0)
            sb = ttk.Scrollbar(wrap, orient="vertical", command=canvas.yview)
            inner = tk.Frame(canvas, bg=COR_FUNDO)
            inner.bind("<Configure>",
                       lambda e: canvas.configure(scrollregion=canvas.bbox("all")))
            win = canvas.create_window((0, 0), window=inner, anchor="nw")
            canvas.bind("<Configure>", lambda e: canvas.itemconfig(win, width=e.width))
            canvas.configure(yscrollcommand=sb.set)
            canvas.pack(side="left", fill="both", expand=True)
            sb.pack(side="right", fill="y")

            def _on_wheel(e):
                try:
                    canvas.yview_scroll(int(-1 * (e.delta / 120)), "units")
                except Exception:
                    pass
            canvas.bind_all("<MouseWheel>", _on_wheel)
            dialog.bind("<Destroy>",
                        lambda e: canvas.unbind_all("<MouseWheel>"), add="+")
            return inner

        def _parse_n_prot(tam):
            """Numero de proteinas que o tamanho permite."""
            cod = (tam.get("codigo") or "").strip()
            if cod and cod[-1].isdigit():
                return int(cod[-1])
            m = re.search(r"(\d+)\s*Prote", tam.get("descricao", ""))
            return int(m.group(1)) if m else 1

        # -------------------- ETAPA 1: TAMANHO --------------------
        def render_tamanho():
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text="Etapa 1 de 4  -  Tamanho")
            tk.Label(body, text="Escolha o tamanho do prato:", bg=COR_FUNDO,
                     fg=COR_TEXTO, font=("Segoe UI", 13, "bold")).pack(
                         anchor="w", pady=(4, 12))

            grid = tk.Frame(body, bg=COR_FUNDO)
            grid.pack(fill="x")

            def escolher(tam):
                st["tam"] = tam
                st["n_prot"] = _parse_n_prot(tam)
                st["proteinas"] = []
                render_proteinas()

            for i, tam in enumerate(tamanhos):
                n = _parse_n_prot(tam)
                card = tk.Frame(grid, bg=COR_CARD, highlightthickness=1,
                                highlightbackground=COR_GLASS_BORDER)
                card.grid(row=i // 2, column=i % 2, padx=8, pady=8,
                          sticky="nsew", ipadx=10, ipady=10)
                grid.grid_columnconfigure(i % 2, weight=1)
                tk.Label(card, text=tam["descricao"], bg=COR_CARD, fg=COR_BOTAO_LARANJA,
                         font=("Segoe UI", 13, "bold")).pack(anchor="w", padx=10, pady=(6, 2))
                tk.Label(card, text=f"Escolha {n} proteina(s)", bg=COR_CARD,
                         fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(anchor="w", padx=10)
                StyledButton(card, text="Selecionar",
                             command=lambda t=tam: escolher(t),
                             color=COR_BOTAO_VERDE, width=14).pack(
                                 anchor="e", padx=10, pady=(8, 4))

            StyledButton(footer, text="✖ Cancelar", command=dialog.destroy,
                         color=COR_ERRO, width=12).pack(side="right", padx=12, pady=8)

        # -------------------- ETAPA 2: PROTEINAS --------------------
        def render_proteinas():
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text="Etapa 2 de 4  -  Proteinas")
            n = st["n_prot"]
            tk.Label(body,
                     text=f"{st['tam']['descricao']}  -  escolha {n} proteina(s):",
                     bg=COR_FUNDO, fg=COR_TEXTO,
                     font=("Segoe UI", 13, "bold")).pack(anchor="w", pady=(4, 2))
            lbl_cont = tk.Label(body, text=f"Selecionadas: 0 / {n}", bg=COR_FUNDO,
                                fg=COR_ALERTA, font=("Segoe UI", 10, "bold"))
            lbl_cont.pack(anchor="w", pady=(0, 6))

            # Barra de busca (filtra as proteinas em tempo real)
            busca_frame = tk.Frame(body, bg=COR_FUNDO)
            busca_frame.pack(fill="x", pady=(0, 6))
            tk.Label(busca_frame, text=f"{Icons.SEARCH}", bg=COR_FUNDO,
                     fg=COR_TEXTO2, font=("Segoe UI", 11)).pack(side="left", padx=(2, 4))
            et_busca_prot = StyledEntry(busca_frame)
            et_busca_prot.pack(side="left", fill="x", expand=True)
            add_tooltip(et_busca_prot, "Digite para filtrar as proteinas")
            lbl_vazio = tk.Label(body, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                                 font=("Segoe UI", 9, "italic"))
            lbl_vazio.pack(anchor="w")

            inner = _scroll_area(body)
            prot_vars = {}
            cb_widgets = []

            def _atualizar_contador():
                sel = [pid for pid, (v, _d) in prot_vars.items() if v.get()]
                lbl_cont.config(text=f"Selecionadas: {len(sel)} / {n}")

            def _on_toggle(pid):
                sel = [p for p, (v, _d) in prot_vars.items() if v.get()]
                if len(sel) > n:
                    prot_vars[pid][0].set(0)
                    ToastManager.warning(
                        f"Este tamanho permite apenas {n} proteina(s).")
                _atualizar_contador()

            cols = 2
            for p in proteinas:
                var = tk.IntVar(value=0)
                prot_vars[p["id"]] = (var, p["descricao"])
                cb = tk.Checkbutton(
                    inner, text=p["descricao"], variable=var,
                    command=lambda pid=p["id"]: _on_toggle(pid),
                    bg=COR_FUNDO, fg=COR_TEXTO, selectcolor=COR_CARD,
                    activebackground=COR_FUNDO, activeforeground=COR_PRIMARIA,
                    font=("Segoe UI", 10), anchor="w")
                cb_widgets.append(((p["descricao"] or "").lower(), cb))
            for c in range(cols):
                inner.grid_columnconfigure(c, weight=1)

            def _filtrar_prot(*_):
                termo = et_busca_prot.get().strip().lower()
                for _d, cb in cb_widgets:
                    cb.grid_forget()
                vis = 0
                for desc, cb in cb_widgets:
                    if not termo or termo in desc:
                        cb.grid(row=vis // cols, column=vis % cols,
                                sticky="w", padx=8, pady=2)
                        vis += 1
                lbl_vazio.config(
                    text="" if vis else "Nenhuma proteina encontrada para a busca.")

            et_busca_prot.bind("<KeyRelease>", _filtrar_prot)
            _filtrar_prot()  # layout inicial (mostra todas)
            try:
                et_busca_prot.focus_set()
            except Exception:
                pass

            def avancar():
                sel = [(pid, d) for pid, (v, d) in prot_vars.items() if v.get()]
                if len(sel) != n:
                    ToastManager.warning(
                        f"Selecione exatamente {n} proteina(s).")
                    return
                st["proteinas"] = sel
                render_acompanhamentos()

            StyledButton(footer, text="◀ Voltar", command=render_tamanho,
                         color=COR_FUNDO3, width=10).pack(side="left", padx=12, pady=8)
            StyledButton(footer, text="Avancar ▶", command=avancar,
                         color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=12, pady=8)

        # -------------------- ETAPA 3: ACOMPANHAMENTOS --------------------
        def render_acompanhamentos():
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text="Etapa 3 de 4  -  Acompanhamentos")
            tk.Label(body, text="Escolha os acompanhamentos:", bg=COR_FUNDO,
                     fg=COR_TEXTO, font=("Segoe UI", 13, "bold")).pack(
                         anchor="w", pady=(4, 8))

            inner = _scroll_area(body)
            ac_vars = {}
            grupos = {
                "ARROZ": "Arroz / Massas",
                "FEIJAO": "Feijoes",
                "SALADA": "Saladas",
                "": "Outros",
            }
            # Agrupar por prefixo do codigo (ACOMP-XXX)
            por_grupo = {}
            for a in acomp:
                cod = a.get("codigo") or ""
                g = cod.replace("ACOMP-", "") if cod.startswith("ACOMP-") else ""
                por_grupo.setdefault(g, []).append(a)

            row = 0
            for gkey in ["ARROZ", "FEIJAO", "SALADA", ""]:
                itens = por_grupo.get(gkey)
                if not itens:
                    continue
                tk.Label(inner, text=grupos.get(gkey, "Outros"), bg=COR_FUNDO,
                         fg=COR_PRIMARIA, font=("Segoe UI", 11, "bold")).grid(
                             row=row, column=0, columnspan=2, sticky="w",
                             padx=4, pady=(10, 2))
                row += 1
                for j, a in enumerate(itens):
                    var = tk.IntVar(value=0)
                    ac_vars[a["id"]] = (var, a["descricao"])
                    cb = tk.Checkbutton(
                        inner, text=a["descricao"], variable=var,
                        bg=COR_FUNDO, fg=COR_TEXTO, selectcolor=COR_CARD,
                        activebackground=COR_FUNDO, activeforeground=COR_PRIMARIA,
                        font=("Segoe UI", 10), anchor="w")
                    cb.grid(row=row + j // 2, column=j % 2, sticky="w", padx=8, pady=2)
                row += (len(itens) + 1) // 2
            for c in range(2):
                inner.grid_columnconfigure(c, weight=1)

            def avancar():
                sel = [(aid, d) for aid, (v, d) in ac_vars.items() if v.get()]
                if not sel:
                    ToastManager.warning("Selecione ao menos 1 acompanhamento.")
                    return
                st["acomp"] = sel
                render_revisao()

            StyledButton(footer, text="◀ Voltar", command=render_proteinas,
                         color=COR_FUNDO3, width=10).pack(side="left", padx=12, pady=8)
            StyledButton(footer, text="Avancar ▶", command=avancar,
                         color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=12, pady=8)

        # -------------------- ETAPA 4: REVISAO --------------------
        def render_revisao():
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text="Etapa 4 de 4  -  Revisao")
            tk.Label(body, text="Confira o prato montado:", bg=COR_FUNDO,
                     fg=COR_TEXTO, font=("Segoe UI", 13, "bold")).pack(
                         anchor="w", pady=(4, 8))

            card = tk.Frame(body, bg=COR_CARD, highlightthickness=1,
                            highlightbackground=COR_GLASS_BORDER)
            card.pack(fill="both", expand=True, padx=4, pady=4)

            tk.Label(card, text=st["tam"]["descricao"], bg=COR_CARD,
                     fg=COR_BOTAO_LARANJA, font=("Segoe UI", 14, "bold")).pack(
                         anchor="w", padx=14, pady=(12, 6))

            prot_txt = ", ".join(d for _i, d in st["proteinas"])
            ac_txt = ", ".join(d for _i, d in st["acomp"])
            tk.Label(card, text="Proteinas:", bg=COR_CARD, fg=COR_PRIMARIA,
                     font=("Segoe UI", 11, "bold")).pack(anchor="w", padx=14)
            tk.Label(card, text=prot_txt, bg=COR_CARD, fg=COR_TEXTO,
                     font=("Segoe UI", 11), wraplength=680, justify="left").pack(
                         anchor="w", padx=24, pady=(0, 8))
            tk.Label(card, text="Acompanhamentos:", bg=COR_CARD, fg=COR_PRIMARIA,
                     font=("Segoe UI", 11, "bold")).pack(anchor="w", padx=14)
            tk.Label(card, text=ac_txt, bg=COR_CARD, fg=COR_TEXTO,
                     font=("Segoe UI", 11), wraplength=680, justify="left").pack(
                         anchor="w", padx=24, pady=(0, 8))

            StyledButton(footer, text="◀ Voltar", command=render_acompanhamentos,
                         color=COR_FUNDO3, width=10).pack(side="left", padx=12, pady=8)
            StyledButton(footer, text="✔ Adicionar ao carrinho",
                         command=adicionar_prato, color=COR_BOTAO_VERDE,
                         width=22).pack(side="right", padx=12, pady=8)

        def adicionar_prato():
            tam = st["tam"]
            prot_nomes = [d for _i, d in st["proteinas"]]
            ac_nomes = [d for _i, d in st["acomp"]]

            item = ItemVenda()
            item.produto_id = tam["id"]
            descricao_full = (
                f"{tam['descricao']} | Proteinas: {', '.join(prot_nomes)} "
                f"| Acomp: {', '.join(ac_nomes)}"
            )
            # itens_venda.descricao_produto e VARCHAR(200): limita com seguranca.
            item.descricao_produto = descricao_full[:195]
            item.preco_unitario = float(tam.get("preco_venda") or 0)
            item.quantidade = 1
            item.total = item.preco_unitario
            item.tipo_produto_id = tam.get("tipo_produto_id")
            item.tipo_produto_desc = DatabaseHelper.CAT_MONTE_PRATO
            # adicionais (impressos no cupom de producao/cozinha como "+ ...")
            item.adicionais = (
                [f"Proteina: {p}" for p in prot_nomes]
                + [f"Acomp.: {a}" for a in ac_nomes]
            )
            self.carrinho.append(item)
            self.atualizar_tree_carrinho()
            ToastManager.success("Prato adicionado ao carrinho!")
            render_extras_categorias()

        # -------------------- ETAPA 5: EXTRAS --------------------
        def render_extras_categorias():
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text="Extras (opcional)")
            tk.Label(body, text="Deseja adicionar bebidas ou sobremesas?",
                     bg=COR_FUNDO, fg=COR_TEXTO,
                     font=("Segoe UI", 13, "bold")).pack(anchor="w", pady=(4, 4))
            tk.Label(body, text="Itens que nao fazem parte do prato.",
                     bg=COR_FUNDO, fg=COR_TEXTO2,
                     font=("Segoe UI", 10)).pack(anchor="w", pady=(0, 12))

            grid = tk.Frame(body, bg=COR_FUNDO)
            grid.pack(fill="x")
            categorias = [
                (DatabaseHelper.CAT_SUCOS, "Sucos", COR_BOTAO_LARANJA),
                (DatabaseHelper.CAT_REFRI_200, "Refrigerantes 200ml", COR_BOTAO_AZUL),
                (DatabaseHelper.CAT_REFRI_350, "Refrigerantes Lata 350ml", COR_BOTAO_AZUL),
                (DatabaseHelper.CAT_AGUAS, "Aguas", COR_PRIMARIA),
                (DatabaseHelper.CAT_SOBREMESAS, "Sobremesas", COR_ACCENT),
            ]
            for i, (tipo_desc, rotulo, cor) in enumerate(categorias):
                StyledButton(grid, text=rotulo,
                             command=lambda t=tipo_desc, r=rotulo: render_extras_lista(t, r),
                             color=cor, width=22).grid(
                                 row=i // 2, column=i % 2, padx=8, pady=8, sticky="w")

            StyledButton(footer, text="+ Montar outro prato",
                         command=render_tamanho, color=COR_BOTAO_LARANJA,
                         width=20).pack(side="left", padx=12, pady=8)
            StyledButton(footer, text="✔ Concluir", command=dialog.destroy,
                         color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=12, pady=8)

        def render_extras_lista(tipo_desc, rotulo):
            _limpar(body)
            _limpar(footer)
            self._lbl_passo_prato.config(text=f"Extras  -  {rotulo}")
            tk.Label(body, text=f"{rotulo} - toque para adicionar:", bg=COR_FUNDO,
                     fg=COR_TEXTO, font=("Segoe UI", 13, "bold")).pack(
                         anchor="w", pady=(4, 8))
            inner = _scroll_area(body)

            def carregar_lista():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT p.id, p.descricao, p.preco_venda, p.tipo_produto_id "
                    "FROM produtos p JOIN tipos_produto t ON p.tipo_produto_id = t.id "
                    "WHERE t.descricao = %s AND p.ativo = 1 ORDER BY p.descricao",
                    (tipo_desc,)
                )

            def on_lista(rows):
                for w in inner.winfo_children():
                    w.destroy()
                for i, p in enumerate(rows or []):
                    linha = tk.Frame(inner, bg=COR_CARD if i % 2 else COR_GLASS)
                    linha.pack(fill="x", padx=4, pady=1)
                    tk.Label(linha, text=p["descricao"],
                             bg=linha.cget("bg"), fg=COR_TEXTO,
                             font=("Segoe UI", 10), anchor="w").pack(
                                 side="left", padx=8, pady=4, fill="x", expand=True)
                    StyledButton(
                        linha, text="+ Adicionar",
                        command=lambda pr=p: (
                            self.adicionar_ao_carrinho(
                                pr["id"], pr["descricao"],
                                float(pr.get("preco_venda") or 0), 1,
                                pr.get("tipo_produto_id"), rotulo),
                            ToastManager.info(f"Adicionado: {pr['descricao']}")),
                        color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=6, pady=3)

            self.run_async(carregar_lista, on_lista)

            StyledButton(footer, text="◀ Voltar", command=render_extras_categorias,
                         color=COR_FUNDO3, width=10).pack(side="left", padx=12, pady=8)
            StyledButton(footer, text="✔ Concluir", command=dialog.destroy,
                         color=COR_BOTAO_VERDE, width=12).pack(side="right", padx=12, pady=8)

        render_tamanho()

    def limpar_carrinho(self):
        if self.carrinho:
            if messagebox.askyesno("Confirmar",
                    f"Limpar todo o carrinho?\n({len(self.carrinho)} item(s))"):
                self.carrinho.clear()
                self.atualizar_tree_carrinho()
                ToastManager.info("Carrinho limpo.")

    def finalizar_venda(self):
        if not self.carrinho:
            ToastManager.warning("Adicione itens ao carrinho antes de finalizar.")
            return
        # === TRAVA DE CAIXA: Verificar em tempo real se o caixa esta aberto ===
        self._verificar_caixa_aberto_para_venda(
            callback_sucesso=self.show_pagamento,
            origem="finalizar venda"
        )


    # ========================================================================
    # TELA DE PAGAMENTO
    # ========================================================================
    def show_pagamento(self):
        subtotal = sum(item.total for item in self.carrinho)
        try:
            desc_val = FormatUtils.parse_money(self.et_desconto.get())
        except Exception:
            desc_val = 0
        try:
            acre_val = FormatUtils.parse_money(self.et_acrescimo.get())
        except Exception:
            acre_val = 0

        desc_tipo = self.combo_desc_tipo.get()
        acre_tipo = self.combo_acre_tipo.get()
        desconto = desc_val if "R$" in desc_tipo else (subtotal * desc_val / 100)
        acrescimo = acre_val if "R$" in acre_tipo else (subtotal * acre_val / 100)
        taxa_entrega = (self.venda_taxa_entrega or 0) if getattr(self, "venda_para_entrega", False) else 0
        total = subtotal - desconto + acrescimo + taxa_entrega
        if total < 0:
            total = 0

        self.venda_total_bruto = subtotal
        self.venda_desconto_valor = desconto
        self.venda_desconto_tipo = "valor" if "R$" in desc_tipo else "percentual"
        self.venda_acrescimo_valor = acrescimo
        self.venda_acrescimo_tipo = "valor" if "R$" in acre_tipo else "percentual"
        self.venda_total_liquido = total
        self.venda_observacao = self.et_obs_venda.get("1.0", "end").strip()
        # Capturar dados de entrega (endereco/whatsapp) digitados no painel
        if getattr(self, "venda_para_entrega", False):
            try:
                self.venda_endereco_entrega = self.et_endereco_entrega.get().strip()
            except Exception:
                self.venda_endereco_entrega = ""
            try:
                self.venda_celular_whatsapp = self.et_whatsapp_entrega.get().strip()
            except Exception:
                self.venda_celular_whatsapp = ""
        else:
            self.venda_endereco_entrega = ""
            self.venda_celular_whatsapp = ""
        self.pagamentos_venda = []

        # Carregar formas de pagamento
        def load_formas():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT id, descricao, tipo, permite_parcelamento FROM formas_pagamento WHERE ativo = 1"
            )

        def on_formas(formas):
            self.formas_pagamento_list = formas
            self._show_pagamento_ui()

        self.run_async(load_formas, on_formas)

    def _show_pagamento_ui(self):
        dialog = tk.Toplevel(self.root)
        dialog.title("Pagamento")
        dialog.geometry("600x550")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()
        self._pag_dialog = dialog

        tk.Label(dialog, text="Pagamento", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=10)

        # Total
        tk.Label(dialog, text=f"Total da Venda: R$ {FormatUtils.format_money(self.venda_total_liquido)}",
                 bg=COR_FUNDO, fg=COR_SUCESSO, font=("Segoe UI", 14, "bold")).pack(pady=5)

        self.lbl_saldo = tk.Label(dialog,
            text=f"Saldo Devedor: R$ {FormatUtils.format_money(self.venda_total_liquido)}",
            bg=COR_FUNDO, fg=COR_ALERTA, font=("Segoe UI", 12))
        self.lbl_saldo.pack(pady=3)

        # Adicionar pagamento
        add_frame = CardFrame(dialog)
        add_frame.pack(fill="x", padx=20, pady=10)

        tk.Label(add_frame, text="Forma:", bg=COR_CARD, fg=COR_TEXTO).pack(side="left")
        self.combo_forma_pag = ttk.Combobox(add_frame, width=20, state="readonly")
        formas_nomes = [f.get("descricao", "") for f in self.formas_pagamento_list]
        self.combo_forma_pag["values"] = formas_nomes
        if formas_nomes:
            self.combo_forma_pag.current(0)
        self.combo_forma_pag.pack(side="left", padx=5)

        tk.Label(add_frame, text="Valor:", bg=COR_CARD, fg=COR_TEXTO).pack(side="left", padx=(10, 0))
        self.et_valor_pag = StyledEntry(add_frame, width=12)
        saldo = self.venda_total_liquido - sum(p.valor for p in self.pagamentos_venda)
        self.et_valor_pag.insert(0, FormatUtils.format_money(saldo))
        self.et_valor_pag.pack(side="left", padx=5)

        StyledButton(add_frame, text="Add", command=lambda: self._add_pagamento(dialog),
                     color=COR_BOTAO_VERDE, width=6).pack(side="right", padx=5)

        # Lista de pagamentos
        cols_pag = ("forma", "valor")
        self.tree_pagamentos = ttk.Treeview(dialog, columns=cols_pag, show="headings", height=6)
        self.tree_pagamentos.heading("forma", text="Forma de Pagamento")
        self.tree_pagamentos.heading("valor", text="Valor")
        self.tree_pagamentos.column("forma", width=300)
        self.tree_pagamentos.column("valor", width=150, anchor="e")
        self.tree_pagamentos.pack(fill="x", padx=20, pady=5)

        # Valor recebido e troco
        troco_frame = tk.Frame(dialog, bg=COR_FUNDO)
        troco_frame.pack(fill="x", padx=20, pady=5)
        self.lbl_troco = tk.Label(troco_frame, text="Troco: R$ 0,00",
                                   bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 12))
        self.lbl_troco.pack()

        # Botoes
        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=20, pady=10)
        StyledButton(btn_frame, text="Remover Pagamento",
                     command=self._remover_pagamento,
                     color=COR_ERRO, width=18).pack(side="left", padx=5)
        StyledButton(btn_frame, text="CONFIRMAR VENDA",
                     command=lambda: self._confirmar_venda(dialog),
                     color=COR_BOTAO_VERDE, width=18).pack(side="right", padx=5)

    def _add_pagamento(self, dialog):
        idx = self.combo_forma_pag.current()
        if idx < 0:
            messagebox.showwarning(
                "Selecione a Forma de Pagamento",
                "Nenhuma forma de pagamento foi selecionada.\n\n"
                "COMO FAZER:\n"
                "1. Escolha a forma de pagamento na lista\n"
                "   (Dinheiro, Cartao, Pix, etc.)\n"
                "2. Digite o valor pago pelo cliente\n"
                "3. Clique em 'Adicionar Pagamento'\n\n"
                "Voce pode usar mais de uma forma de pagamento.",
                parent=dialog)
            return
        valor = FormatUtils.parse_money(self.et_valor_pag.get())
        if valor <= 0:
            messagebox.showwarning(
                "Valor Invalido",
                "O valor informado para o pagamento nao e valido.\n\n"
                "COMO CORRIGIR:\n"
                "1. O valor deve ser maior que zero\n"
                "2. Use virgula para centavos (ex: 10,50)\n"
                "3. Nao use letras ou simbolos (R$, etc.)\n"
                "4. Exemplos validos: 10  /  10,50  /  100,00",
                parent=dialog)
            return

        forma = self.formas_pagamento_list[idx]
        tipo = (forma.get("tipo") or "").lower()

        # Troco (pagar a mais) SOMENTE e permitido em Dinheiro. Para PIX,
        # Credito, Debito e qualquer outra forma, o valor nao pode ultrapassar
        # o saldo devedor.
        total_pago_atual = sum(p.valor for p in self.pagamentos_venda)
        saldo_restante = self.venda_total_liquido - total_pago_atual
        if tipo != "dinheiro" and valor > saldo_restante + 0.005:
            messagebox.showwarning(
                "Troco nao permitido nesta forma",
                f"A forma '{forma['descricao']}' nao aceita troco.\n\n"
                f"Apenas DINHEIRO pode receber um valor maior que o devido "
                f"e gerar troco.\n\n"
                f"Saldo devedor: R$ {FormatUtils.format_money(max(0, saldo_restante))}\n"
                f"Valor informado: R$ {FormatUtils.format_money(valor)}\n\n"
                f"COMO RESOLVER:\n"
                f"1. Informe no maximo R$ {FormatUtils.format_money(max(0, saldo_restante))} nesta forma; ou\n"
                f"2. Use Dinheiro caso o cliente pague a mais (com troco).",
                parent=dialog)
            return

        pag = PagamentoVenda()
        pag.forma_pagamento_id = forma["id"]
        pag.forma_descricao = forma["descricao"]
        pag.forma_tipo = tipo
        pag.valor = valor
        self.pagamentos_venda.append(pag)
        self._atualizar_pagamentos_ui()

    def _remover_pagamento(self):
        sel = self.tree_pagamentos.selection()
        if sel:
            idx = self.tree_pagamentos.index(sel[0])
            del self.pagamentos_venda[idx]
            self._atualizar_pagamentos_ui()

    def _atualizar_pagamentos_ui(self):
        self.tree_pagamentos.delete(*self.tree_pagamentos.get_children())
        total_pago = 0
        for pag in self.pagamentos_venda:
            self.tree_pagamentos.insert("", "end", values=(
                pag.forma_descricao, f"R$ {FormatUtils.format_money(pag.valor)}"
            ))
            total_pago += pag.valor

        saldo = self.venda_total_liquido - total_pago
        troco = max(0, -saldo)
        saldo_display = max(0, saldo)

        self.lbl_saldo.config(
            text=f"Saldo Devedor: R$ {FormatUtils.format_money(saldo_display)}",
            fg=COR_SUCESSO if saldo_display == 0 else COR_ALERTA
        )
        self.lbl_troco.config(text=f"Troco: R$ {FormatUtils.format_money(troco)}")

        # Atualizar valor sugerido
        self.et_valor_pag.delete(0, tk.END)
        self.et_valor_pag.insert(0, FormatUtils.format_money(max(0, saldo)))

    def _confirmar_venda(self, dialog):
        total_pago = sum(p.valor for p in self.pagamentos_venda)
        if total_pago < self.venda_total_liquido:
            faltante = self.venda_total_liquido - total_pago
            messagebox.showwarning(
                "Pagamento Incompleto",
                f"O valor pago ainda nao cobre o total da venda.\n\n"
                f"Total da venda: R$ {FormatUtils.format_money(self.venda_total_liquido)}\n"
                f"Total pago: R$ {FormatUtils.format_money(total_pago)}\n"
                f"Faltam: R$ {FormatUtils.format_money(faltante)}\n\n"
                f"COMO RESOLVER:\n"
                f"1. Adicione mais uma forma de pagamento\n"
                f"2. Ou aumente o valor do pagamento atual\n"
                f"3. O cliente pode pagar com formas diferentes\n"
                f"   (ex: parte em dinheiro, parte no cartao)",
                parent=dialog)
            return

        # === TRAVA DE CAIXA (verificacao final de seguranca antes de gravar) ===
        if not Session.caixa_id:
            AuditLogger.log("VENDA_BLOQUEADA_CAIXA_FECHADO",
                            "Tentativa de confirmar venda bloqueada - caixa fechado (verificacao final)",
                            usuario=Session.user_login)
            ToastManager.error("Venda bloqueada! Caixa fechado.", duration=5000)
            messagebox.showerror(
                "Caixa Fechado - Venda Bloqueada",
                f"{Icons.BLOCKED} Nao e possivel confirmar a venda com o caixa fechado.\n\n"
                "O caixa foi fechado durante o processo de pagamento.\n"
                "Abra o caixa (F3) e tente novamente.",
                parent=dialog)
            return

        troco = total_pago - self.venda_total_liquido

        # Seguranca: troco so pode existir se houver pagamento em dinheiro.
        # (O bloqueio principal ja ocorre em _add_pagamento; aqui e reforco.)
        if troco > 0.005:
            tem_dinheiro = any(
                (getattr(p, "forma_tipo", "") or "").lower() == "dinheiro"
                for p in self.pagamentos_venda
            )
            if not tem_dinheiro:
                messagebox.showwarning(
                    "Troco nao permitido",
                    "So e possivel gerar troco quando ha pagamento em Dinheiro.\n\n"
                    "As formas eletronicas (PIX, Credito, Debito, etc.) nao "
                    "aceitam troco. Ajuste os valores para que o total pago "
                    "seja exatamente o valor da venda.",
                    parent=dialog)
                return

        def salvar():
            db = DatabaseHelper.get_instance()
            uso_armario_id_venda = int(((self._armario_pendente or {}).get("uso_id")) or 0)
            nome_cliente_venda = (self.venda_cliente_nome or "").strip() or None
            if nome_cliente_venda and nome_cliente_venda.lower() == "cliente nao informado" and not self.venda_cliente_id:
                nome_cliente_venda = None
            if uso_armario_id_venda > 0 and not nome_cliente_venda:
                try:
                    uso_row = db.execute_query(
                        "SELECT cliente_nome FROM uso_armario_sauna WHERE id = %s",
                        (uso_armario_id_venda,)
                    )
                    if uso_row:
                        nome_armario = (uso_row[0].get("cliente_nome") or "").strip()
                        if nome_armario:
                            nome_cliente_venda = nome_armario
                except Exception:
                    pass
            editando_id = getattr(self, "_editando_venda_id", None)
            if editando_id:
                # === EDICAO: regravar na MESMA venda (mesmo id) ===
                venda_id = int(editando_id)
                # Reverter o estoque dos itens antigos antes de substitui-los
                itens_antigos = db.execute_query(
                    "SELECT produto_id, quantidade FROM itens_venda WHERE venda_id = %s",
                    (venda_id,)
                ) or []
                for _ia in itens_antigos:
                    if _ia.get("produto_id"):
                        db.execute_update(
                            "UPDATE produtos SET estoque = estoque + %s WHERE id = %s",
                            (_ia["quantidade"], _ia["produto_id"])
                        )
                # Remover itens, pagamentos e contas a receber antigos desta venda
                db.execute_update("DELETE FROM itens_venda WHERE venda_id = %s", (venda_id,))
                db.execute_update("DELETE FROM pagamentos_venda WHERE venda_id = %s", (venda_id,))
                db.execute_update("DELETE FROM contas_receber WHERE venda_id = %s", (venda_id,))
                # Atualizar a venda mantendo o mesmo id (data_venda e caixa_id
                # originais sao preservados de proposito).
                db.execute_update(
                    "UPDATE vendas SET cliente_id=%s, cliente_nome=%s, vendedor_id=%s, "
                    "entregador_id=%s, total_bruto=%s, desconto_tipo=%s, desconto_valor=%s, "
                    "acrescimo_tipo=%s, acrescimo_valor=%s, total_liquido=%s, valor_recebido=%s, "
                    "troco=%s, observacao=%s, status='finalizada', para_entrega=%s, taxa_entrega=%s, "
                    "bairro_entrega=%s, endereco_entrega=%s, celular_whatsapp=%s, status_entrega=%s "
                    "WHERE id=%s",
                    (self.venda_cliente_id, nome_cliente_venda, self.venda_vendedor_id,
                     self.venda_entregador_id, self.venda_total_bruto, self.venda_desconto_tipo,
                     self.venda_desconto_valor, self.venda_acrescimo_tipo, self.venda_acrescimo_valor,
                     self.venda_total_liquido, total_pago, troco, self.venda_observacao,
                     1 if getattr(self, "venda_para_entrega", False) else 0,
                     (self.venda_taxa_entrega or 0) if getattr(self, "venda_para_entrega", False) else 0,
                     getattr(self, "venda_bairro_entrega", "") or None,
                     getattr(self, "venda_endereco_entrega", "") or None,
                     getattr(self, "venda_celular_whatsapp", "") or None,
                     "pendente" if getattr(self, "venda_para_entrega", False) else None,
                     venda_id)
                )
            else:
                venda_id = db.execute_update(
                    "INSERT INTO vendas (cliente_id, cliente_nome, vendedor_id, entregador_id, caixa_id, "
                    "total_bruto, desconto_tipo, desconto_valor, acrescimo_tipo, acrescimo_valor, "
                    "total_liquido, valor_recebido, troco, observacao, status, uso_armario_id, "
                    "para_entrega, taxa_entrega, bairro_entrega, endereco_entrega, celular_whatsapp, status_entrega) "
                    "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,'finalizada',%s,%s,%s,%s,%s,%s,%s)",
                    (self.venda_cliente_id, nome_cliente_venda, self.venda_vendedor_id, self.venda_entregador_id,
                     Session.caixa_id, self.venda_total_bruto, self.venda_desconto_tipo,
                     self.venda_desconto_valor, self.venda_acrescimo_tipo, self.venda_acrescimo_valor,
                     self.venda_total_liquido, total_pago, troco, self.venda_observacao, uso_armario_id_venda,
                     1 if getattr(self, "venda_para_entrega", False) else 0,
                     (self.venda_taxa_entrega or 0) if getattr(self, "venda_para_entrega", False) else 0,
                     getattr(self, "venda_bairro_entrega", "") or None,
                     getattr(self, "venda_endereco_entrega", "") or None,
                     getattr(self, "venda_celular_whatsapp", "") or None,
                     "pendente" if getattr(self, "venda_para_entrega", False) else None)
                )

            for item in self.carrinho:
                db.execute_update(
                    "INSERT INTO itens_venda (venda_id, produto_id, descricao_produto, "
                    "quantidade, preco_unitario, desconto, total) VALUES (%s,%s,%s,%s,%s,%s,%s)",
                    (venda_id, item.produto_id, item.descricao_produto,
                     item.quantidade, item.preco_unitario, item.desconto, item.total)
                )
                # Baixar estoque
                db.execute_update(
                    "UPDATE produtos SET estoque = estoque - %s WHERE id = %s",
                    (item.quantidade, item.produto_id)
                )

            for pag in self.pagamentos_venda:
                db.execute_update(
                    "INSERT INTO pagamentos_venda (venda_id, forma_pagamento_id, valor, parcelas) "
                    "VALUES (%s,%s,%s,%s)",
                    (venda_id, pag.forma_pagamento_id, pag.valor, pag.parcelas)
                )

                # Contas a receber
                forma_info = None
                for f in self.formas_pagamento_list:
                    if f["id"] == pag.forma_pagamento_id:
                        forma_info = f
                        break
                if forma_info and forma_info.get("tipo") == "conta_receber" and self.venda_cliente_id > 0:
                    db.execute_update(
                        "INSERT INTO contas_receber (cliente_id, venda_id, valor_original, "
                        "valor_pago, valor_pendente, status) VALUES (%s,%s,%s,0,%s,'pendente')",
                        (self.venda_cliente_id, venda_id, pag.valor, pag.valor)
                    )

            return venda_id

        def on_saved(venda_id):
            dialog.destroy()
            era_edicao = getattr(self, "_editando_venda_id", None)
            self._editando_venda_id = None
            # Gerar cupom
            venda = Venda()
            venda.id = venda_id
            venda.cliente_nome = self.venda_cliente_nome
            venda.total_bruto = self.venda_total_bruto
            venda.desconto_valor = self.venda_desconto_valor
            venda.acrescimo_valor = self.venda_acrescimo_valor
            venda.total_liquido = self.venda_total_liquido
            venda.valor_recebido = total_pago
            venda.troco = troco
            venda.observacao = self.venda_observacao
            # Dados de entrega (para o cupom exibir taxa/endereco)
            venda.para_entrega = bool(getattr(self, "venda_para_entrega", False))
            venda.taxa_entrega = (self.venda_taxa_entrega or 0) if venda.para_entrega else 0.0
            venda.bairro_entrega = getattr(self, "venda_bairro_entrega", "") or ""
            venda.endereco_entrega = getattr(self, "venda_endereco_entrega", "") or ""
            venda.celular_whatsapp = getattr(self, "venda_celular_whatsapp", "") or ""
            venda.entregador_nome = getattr(self, "venda_entregador_nome", "") or ""
            venda.data_venda = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

            # Carregar dados da empresa para o cupom
            empresa = None
            try:
                db = DatabaseHelper.get_instance()
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass

            # Carregar config da impressora para layout e impressao automatica
            printer_cfg = self._load_printer_config()
            cupom = CupomGenerator.gerar_cupom(
                venda, self.carrinho, self.pagamentos_venda,
                empresa=empresa,
                colunas=printer_cfg.get("largura_papel", 48),
                margem_esq=printer_cfg.get("margem_esquerda", 0),
                margem_dir=printer_cfg.get("margem_direita", 0),
                espacamento=printer_cfg.get("espacamento_linhas", 1)
            )

            auto_imp = printer_cfg.get("imprimir_automatico", False)

            # === MULTI-IMPRESSORA: Imprimir cupons parciais por categoria ===
            try:
                multi_cfg = self._load_multi_printer_config()
                if multi_cfg.get("ativo", False):
                    # Se configurado para NAO imprimir cupom completo, desativar auto_imp
                    if not multi_cfg.get("imprimir_cupom_completo", True):
                        auto_imp = False
                    # Despachar impressao por categoria em thread separada
                    self._imprimir_por_categoria(venda, self.carrinho, empresa=empresa)
            except Exception as e:
                _logger.error(f"Erro ao despachar multi-impressora: {e}")

            # Se a venda veio de um armario, liberar o armario e voltar ao menu ao fechar o cupom
            if self._armario_pendente:
                arm_id = self._armario_pendente["arm_id"]
                arm_numero = self._armario_pendente["numero"]
                arm_uso_id = self._armario_pendente.get("uso_id", 0)
                self._armario_pendente = None

                def ao_fechar_cupom_armario():
                    def liberar_armario():
                        db = DatabaseHelper.get_instance()
                        # Limpar usando tabelas do APK (uso_armario_sauna + itens_armario_sauna)
                        if arm_uso_id and arm_uso_id > 0:
                            try:
                                db.execute_update(
                                    "DELETE FROM itens_armario_sauna_adicionais "
                                    "WHERE item_armario_id IN (SELECT id FROM itens_armario_sauna WHERE uso_armario_id = %s)",
                                    (arm_uso_id,))
                            except Exception:
                                pass
                            db.execute_update(
                                "DELETE FROM itens_armario_sauna WHERE uso_armario_id = %s",
                                (arm_uso_id,))
                            db.execute_update(
                                "UPDATE uso_armario_sauna SET status = 'encerrado', data_saida = NOW() WHERE id = %s",
                                (arm_uso_id,))
                    def on_armario_liberado(_):
                        self.show_success(f"Armario {arm_numero} liberado com sucesso! Chave devolvida.")
                        self.show_main_menu()
                    self.run_async(liberar_armario, on_armario_liberado)

                self._mostrar_cupom(cupom, auto_imprimir=auto_imp, on_close=ao_fechar_cupom_armario)
            else:
                self._mostrar_cupom(cupom, auto_imprimir=auto_imp, on_close=self.show_main_menu)

        self.run_async(salvar, on_saved)

    def _mostrar_cupom(self, cupom_text, auto_imprimir=False, on_close=None):
        """Exibe o cupom na tela com opcoes de Copiar, Salvar, Imprimir e Fechar.
        Se auto_imprimir=True, envia automaticamente para a impressora.
        Se on_close for fornecido, sera executado ao fechar o dialog do cupom."""
        # Guardar ultimo cupom para impressao rapida Ctrl+P
        self._ultimo_cupom = cupom_text

        dialog = tk.Toplevel(self.root)
        dialog.title("Cupom da Venda")
        dialog.geometry("500x650")
        dialog.configure(bg=COR_FUNDO)

        tk.Label(dialog, text="Cupom Nao Fiscal", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=10)

        text_widget = scrolledtext.ScrolledText(dialog, bg=COR_ENTRADA, fg=COR_TEXTO,
                                                 font=("Courier New", 10), wrap="word")
        text_widget.pack(fill="both", expand=True, padx=15, pady=5)
        text_widget.insert("1.0", cupom_text)
        text_widget.config(state="disabled")

        # Label de status da impressao
        lbl_status_imp = tk.Label(dialog, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                                   font=("Segoe UI", 9, "italic"))
        lbl_status_imp.pack(pady=(0, 5))

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=15, pady=10)

        def copiar():
            self.root.clipboard_clear()
            self.root.clipboard_append(cupom_text)
            ToastManager.success("Cupom copiado para a area de transferencia!")

        def salvar_arquivo():
            DataExporter.export_text(cupom_text, "cupom", parent=dialog)

        def imprimir_cupom():
            lbl_status_imp.config(text="Enviando para impressora...", fg=COR_ALERTA)
            dialog.update_idletasks()

            def _do_print():
                return self._imprimir_texto(cupom_text, parent_window=dialog)

            def _on_result(resultado):
                sucesso, msg = resultado
                if sucesso:
                    lbl_status_imp.config(text=msg, fg=COR_SUCESSO)
                    messagebox.showinfo(
                        "Impressao Enviada",
                        f"{msg}\n\n"
                        "O documento foi enviado para a impressora com sucesso.",
                        parent=dialog)
                else:
                    lbl_status_imp.config(text=f"Falha: {msg}", fg=COR_ERRO)
                    messagebox.showerror(
                        "Erro na Impressao",
                        f"{msg}\n\n"
                        "COMO RESOLVER:\n"
                        "1. Verifique se a impressora esta LIGADA\n"
                        "2. Verifique se o cabo esta conectado\n"
                        "3. Verifique se ha papel na impressora\n"
                        "4. Abra 'Dispositivos e Impressoras' no Windows\n"
                        "5. Verifique se nao ha documentos travados na fila",
                        parent=dialog)

            def _on_error(e):
                lbl_status_imp.config(text=f"Erro: {e}", fg=COR_ERRO)
                ErroAmigavel.mostrar_erro(e, "impressao_cupom", parent=dialog)

            self.run_async(_do_print, _on_result, _on_error)

        StyledButton(btn_frame, text="⎙ Imprimir", command=imprimir_cupom,
                     color=COR_PRIMARIA, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="◎ Copiar", command=copiar,
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Salvar TXT", command=salvar_arquivo,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        def fechar_cupom():
            dialog.destroy()
            if on_close:
                on_close()

        StyledButton(btn_frame, text="Fechar", command=fechar_cupom,
                     color="#2a3a5c", width=12).pack(side="right", padx=5)

        # Garantir que on_close seja chamado ao fechar pela barra de titulo (X)
        dialog.protocol("WM_DELETE_WINDOW", fechar_cupom)

        # Impressao automatica se configurado
        if auto_imprimir:
            dialog.after(500, imprimir_cupom)

    def _mostrar_cupom_caixa(self, cupom_text, titulo="Cupom do Caixa", auto_imprimir=False):
        """Exibe o cupom de abertura ou fechamento de caixa na tela
        com opcoes de Copiar, Salvar, Imprimir e Fechar.
        Se auto_imprimir=True, envia automaticamente para a impressora."""
        dialog = tk.Toplevel(self.root)
        dialog.title(titulo)
        dialog.geometry("500x650")
        dialog.configure(bg=COR_FUNDO)

        tk.Label(dialog, text=titulo, bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=10)

        text_widget = scrolledtext.ScrolledText(dialog, bg=COR_ENTRADA, fg=COR_TEXTO,
                                                 font=("Courier New", 10), wrap="word")
        text_widget.pack(fill="both", expand=True, padx=15, pady=5)
        text_widget.insert("1.0", cupom_text)
        text_widget.config(state="disabled")

        # Label de status da impressao
        lbl_status_imp = tk.Label(dialog, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                                   font=("Segoe UI", 9, "italic"))
        lbl_status_imp.pack(pady=(0, 5))

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=15, pady=10)

        def copiar():
            self.root.clipboard_clear()
            self.root.clipboard_append(cupom_text)
            ToastManager.success("Cupom copiado para a area de transferencia!")

        def salvar_arquivo():
            path = filedialog.asksaveasfilename(defaultextension=".txt",
                filetypes=[("Arquivo de Texto", "*.txt")], parent=dialog)
            if path:
                with open(path, "w", encoding="utf-8") as f:
                    f.write(cupom_text)
                ToastManager.success("Cupom salvo com sucesso!")
                messagebox.showinfo(
                    "Arquivo Salvo",
                    f"O cupom foi salvo com sucesso!\n\n"
                    f"Local: {path}\n\n"
                    f"Voce pode abrir este arquivo com o Bloco de Notas.",
                    parent=dialog)

        def imprimir_cupom():
            lbl_status_imp.config(text="Enviando para impressora...", fg=COR_ALERTA)
            dialog.update_idletasks()

            def _do_print():
                return self._imprimir_texto(cupom_text, parent_window=dialog)

            def _on_result(resultado):
                sucesso, msg = resultado
                if sucesso:
                    lbl_status_imp.config(text=msg, fg=COR_SUCESSO)
                    messagebox.showinfo(
                        "Impressao Enviada",
                        f"{msg}\n\n"
                        "O documento foi enviado para a impressora com sucesso.",
                        parent=dialog)
                else:
                    lbl_status_imp.config(text=f"Falha: {msg}", fg=COR_ERRO)
                    messagebox.showerror(
                        "Erro na Impressao",
                        f"{msg}\n\n"
                        "COMO RESOLVER:\n"
                        "1. Verifique se a impressora esta LIGADA\n"
                        "2. Verifique se o cabo esta conectado\n"
                        "3. Verifique se ha papel na impressora\n"
                        "4. Abra 'Dispositivos e Impressoras' no Windows\n"
                        "5. Verifique se nao ha documentos travados na fila",
                        parent=dialog)

            def _on_error(e):
                lbl_status_imp.config(text=f"Erro: {e}", fg=COR_ERRO)
                ErroAmigavel.mostrar_erro(e, "impressao_cupom", parent=dialog)

            self.run_async(_do_print, _on_result, _on_error)

        StyledButton(btn_frame, text="⎙ Imprimir", command=imprimir_cupom,
                     color=COR_PRIMARIA, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="◎ Copiar", command=copiar,
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Salvar TXT", command=salvar_arquivo,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Fechar", command=dialog.destroy,
                     color="#2a3a5c", width=12).pack(side="right", padx=5)

        dialog.protocol("WM_DELETE_WINDOW", dialog.destroy)

        # Impressao automatica se configurado
        if auto_imprimir:
            dialog.after(500, imprimir_cupom)

    # ========================================================================
    # TELA DE CAIXA
    # ========================================================================
    def show_caixa(self):
        self.clear_container()

        top = self._create_top_bar(f"{Icons.CAIXA} Controle de Caixa")
        # Hint de atalhos
        tk.Label(top, text="F3=Caixa  |  Sangria requer senha Admin",
                 bg=COR_FUNDO2, fg=COR_TEXTO2,
                 font=("Segoe UI", 8)).pack(side="left", padx=8)

        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=16, pady=8)

        # ---- Painel esquerdo: status + botoes ----
        left_panel = tk.Frame(content, bg=COR_FUNDO)
        left_panel.pack(side="left", fill="y", padx=(0, 10))

        # Card de status visual do caixa
        status_card = GlassCard(left_panel, glow_color=COR_PRIMARIA)
        status_card.pack(fill="x", pady=(0, 8))

        # Indicador visual animado
        status_canvas = tk.Canvas(status_card, width=16, height=16,
                                   bg=COR_GLASS, highlightthickness=0)
        status_canvas.pack(side="left")
        self._caixa_dot = status_canvas.create_oval(2, 2, 14, 14,
                                                     fill=COR_ALERTA, outline="")

        self.lbl_caixa_status = tk.Label(status_card,
                                          text="Verificando...",
                                          bg=COR_GLASS, fg=COR_ALERTA,
                                          font=("Segoe UI", 16, "bold"))
        self.lbl_caixa_status.pack(side="left", padx=8, pady=6)

        self.lbl_caixa_info = tk.Label(left_panel, text="",
                                        bg=COR_FUNDO, fg=COR_TEXTO2,
                                        font=("Segoe UI", 9),
                                        wraplength=320, justify="left")
        self.lbl_caixa_info.pack(anchor="w", pady=(0, 8))

        # Mini KPIs do caixa
        kpi_caixa = tk.Frame(left_panel, bg=COR_FUNDO)
        kpi_caixa.pack(fill="x", pady=(0, 10))

        self._lbl_kpi_vendas = self._mini_kpi(kpi_caixa, "Vendas", "—", COR_PRIMARIA, 0)
        self._lbl_kpi_total  = self._mini_kpi(kpi_caixa, "Total R$", "—", COR_SUCESSO, 1)
        self._lbl_kpi_vales  = self._mini_kpi(kpi_caixa, "Vales", "—", COR_ALERTA, 2)
        self._lbl_kpi_sangrias = self._mini_kpi(kpi_caixa, "Sangrias", "—", COR_ERRO, 3)
        for c in range(4):
            kpi_caixa.columnconfigure(c, weight=1)

        # Botoes de acao
        btn_frame = tk.Frame(left_panel, bg=COR_FUNDO)
        btn_frame.pack(fill="x", pady=4)

        self.btn_abrir_caixa = StyledButton(btn_frame,
            text=f"{Icons.CAIXA} Abrir Caixa",
            command=self.abrir_caixa,
            color=COR_BOTAO_VERDE, width=16)
        self.btn_abrir_caixa.pack(fill="x", pady=3)
        add_tooltip(self.btn_abrir_caixa, "Abrir um novo caixa com valor inicial")

        self.btn_fechar_caixa = StyledButton(btn_frame,
            text=f"{Icons.CAIXA} Fechar Caixa",
            command=self.fechar_caixa,
            color=COR_ERRO, width=16)
        self.btn_fechar_caixa.pack(fill="x", pady=3)
        add_tooltip(self.btn_fechar_caixa, "Fechar caixa atual e imprimir relatorio")

        StyledButton(btn_frame,
            text=f"{Icons.MONEY} Vale/Debito",
            command=self.add_vale_debito,
            color=COR_BOTAO_LARANJA, width=16).pack(fill="x", pady=3)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Registrar vale ou debito no caixa atual")

        StyledButton(btn_frame,
            text=f"{Icons.MONEY} Sangria",
            command=self.add_sangria,
            color="#9C27B0", width=16).pack(fill="x", pady=3)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Registrar sangria (requer senha de administrador)")

        StyledButton(btn_frame,
            text=f"{Icons.REFRESH} Atualizar",
            command=self.carregar_status_caixa,
            color=COR_FUNDO3, width=16).pack(fill="x", pady=3)
        add_tooltip(btn_frame.winfo_children()[-1], "Recarregar status do caixa")

        # ---- Painel direito: resumo detalhado ----
        right_panel = tk.Frame(content, bg=COR_FUNDO)
        right_panel.pack(side="left", fill="both", expand=True)

        tk.Label(right_panel,
                 text=f"{Icons.CHART} Movimentacao do Caixa",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).pack(anchor="w", pady=(0, 4))

        resumo_card = CardFrame(right_panel)
        resumo_card.pack(fill="both", expand=True)

        self.text_resumo_caixa = scrolledtext.ScrolledText(
            resumo_card, bg=COR_ENTRADA, fg=COR_TEXTO,
            font=("Courier New", 10), state="disabled",
            relief="flat", bd=0)
        self.text_resumo_caixa.pack(fill="both", expand=True)

        # Botao exportar resumo
        StyledButton(right_panel,
            text=f"{Icons.EXPORT} Exportar Resumo",
            command=lambda: DataExporter.export_text(
                self.text_resumo_caixa.get("1.0", "end"),
                "resumo_caixa", parent=self.root),
            color=COR_FUNDO3, width=18).pack(anchor="e", pady=4)

        self.carregar_status_caixa()

        # Auto-refresh em tempo real (a cada 15 segundos)
        self._start_auto_refresh("caixa", self.carregar_status_caixa, 15000)

    def _mini_kpi(self, parent, label, valor, color, col):
        """Cria um mini card KPI para a tela de caixa."""
        card = tk.Frame(parent, bg=COR_GLASS, highlightthickness=1,
                        highlightbackground=COR_GLASS_BORDER,
                        padx=8, pady=6)
        card.grid(row=0, column=col, padx=3, pady=2, sticky="nsew")
        lbl_v = tk.Label(card, text=valor, bg=COR_GLASS, fg=color,
                         font=("Consolas", 13, "bold"))
        lbl_v.pack()
        tk.Label(card, text=label, bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 8)).pack()
        return lbl_v

    def carregar_status_caixa(self):
        def load():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT c.*, u.nome as usuario_nome FROM caixa c "
                "LEFT JOIN usuarios u ON c.usuario_id = u.id "
                "WHERE c.status = 'aberto' ORDER BY c.id DESC LIMIT 1"
            )
            return rows

        def on_loaded(rows):
            if rows:
                cx = rows[0]
                Session.caixa_id = cx["id"]
                self.caixa_aberto = cx
                self.lbl_caixa_status.config(text="CAIXA ABERTO", fg=COR_SUCESSO)
                try:
                    self._caixa_dot.master.itemconfig(
                        self._caixa_dot, fill=COR_SUCESSO)
                except Exception:
                    pass
                info = (f"Caixa #{cx['id']}  |  Operador: {cx.get('usuario_nome','N/A')}  |  "
                        f"Abertura: {FormatUtils.format_date(cx.get('data_abertura'))}  |  "
                        f"Valor Abertura: R$ {FormatUtils.format_money(cx.get('valor_abertura',0))}")
                self.lbl_caixa_info.config(text=info)
                self.btn_abrir_caixa.config(state="disabled")
                self.btn_fechar_caixa.config(state="normal")
                self.status_bar.set_caixa(True, cx["id"])
                self.carregar_resumo_caixa(cx["id"])
            else:
                Session.caixa_id = 0
                self.caixa_aberto = None
                self.lbl_caixa_status.config(text="CAIXA FECHADO", fg=COR_ERRO)
                try:
                    self._caixa_dot.master.itemconfig(
                        self._caixa_dot, fill=COR_ERRO)
                except Exception:
                    pass
                self.lbl_caixa_info.config(
                    text="Nenhum caixa aberto no momento. Clique em 'Abrir Caixa' para iniciar.")
                self.btn_abrir_caixa.config(state="normal")
                self.btn_fechar_caixa.config(state="disabled")
                self.status_bar.set_caixa(False)

        self.run_async(load, on_loaded)

    def carregar_resumo_caixa(self, caixa_id):
        def load():
            db = DatabaseHelper.get_instance()
            resumo = []
            sep = "-" * 52

            # Vendas do caixa
            rows = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(total_liquido),0) as total "
                "FROM vendas WHERE caixa_id = %s AND status = 'finalizada'",
                (caixa_id,)
            )
            qtd_v = int(rows[0]["qtd"]) if rows else 0
            tot_v = float(rows[0]["total"]) if rows else 0.0
            resumo.append(sep)
            resumo.append(f"  VENDAS FINALIZADAS: {qtd_v:>4}  |  "
                          f"TOTAL: R$ {FormatUtils.format_money(tot_v)}")
            resumo.append(sep)

            # Por forma de pagamento
            rows = db.execute_query(
                "SELECT fp.descricao, COALESCE(SUM(pv.valor),0) as total "
                "FROM pagamentos_venda pv "
                "JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "JOIN vendas v ON pv.venda_id = v.id "
                "WHERE v.caixa_id = %s AND v.status = 'finalizada' "
                "GROUP BY fp.id, fp.descricao ORDER BY total DESC", (caixa_id,)
            )
            if rows:
                resumo.append("  Por Forma de Pagamento:")
                for r in rows:
                    resumo.append(
                        f"    {r['descricao']:<22} R$ "
                        f"{FormatUtils.format_money(r['total']):>10}")
                resumo.append(sep)

            # Vales
            rows = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total "
                "FROM vales_debito WHERE caixa_id = %s", (caixa_id,)
            )
            qtd_vales = int(rows[0]["qtd"]) if rows else 0
            tot_vales = float(rows[0]["total"]) if rows else 0.0
            if qtd_vales > 0:
                resumo.append(
                    f"  Vales/Debitos: {qtd_vales}  |  "
                    f"Total: R$ {FormatUtils.format_money(tot_vales)}")

            # Sangrias
            rows_s = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total "
                "FROM sangrias WHERE caixa_id = %s", (caixa_id,)
            )
            qtd_sang = int(rows_s[0]["qtd"]) if rows_s else 0
            tot_sang = float(rows_s[0]["total"]) if rows_s else 0.0
            if qtd_sang > 0:
                resumo.append(
                    f"  Sangrias: {qtd_sang}  |  "
                    f"Total: R$ {FormatUtils.format_money(tot_sang)}")

            # Entradas / Recebimentos (credito no caixa)
            rows_e = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total "
                "FROM entradas_caixa WHERE caixa_id = %s", (caixa_id,)
            )
            qtd_ent = int(rows_e[0]["qtd"]) if rows_e else 0
            tot_ent = float(rows_e[0]["total"]) if rows_e else 0.0
            if qtd_ent > 0:
                resumo.append(
                    f"  Entradas/Receb.: {qtd_ent}  |  "
                    f"Total: R$ {FormatUtils.format_money(tot_ent)}  (credito)")

            if qtd_vales > 0 or qtd_sang > 0 or qtd_ent > 0:
                resumo.append(sep)

            # Ultimas 10 vendas
            ultimas = db.execute_query(
                "SELECT v.id, COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cli, "
                "v.total_liquido, v.data_venda "
                "FROM vendas v "
                "LEFT JOIN clientes c ON v.cliente_id = c.id "
                "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                "WHERE v.caixa_id = %s AND v.status='finalizada' "
                "ORDER BY v.id DESC LIMIT 10",
                (caixa_id,)
            )
            if ultimas:
                resumo.append("  Ultimas Vendas:")
                for u in ultimas:
                    resumo.append(
                        f"    #{u['id']:<5} {str(u.get('cli',''))[:18]:<18} "
                        f"R$ {FormatUtils.format_money(u['total_liquido']):>9}  "
                        f"{FormatUtils.format_date(u['data_venda'])}")
                resumo.append(sep)

            return {
                "texto": "\n".join(resumo) if resumo else "Nenhuma movimentacao.",
                "qtd_v": qtd_v, "tot_v": tot_v,
                "qtd_vales": qtd_vales, "qtd_sang": qtd_sang,
            }

        def on_loaded(result):
            try:
                self.text_resumo_caixa.config(state="normal")
                self.text_resumo_caixa.delete("1.0", "end")
                self.text_resumo_caixa.insert("1.0", result["texto"])
                self.text_resumo_caixa.config(state="disabled")
                # Atualizar mini KPIs
                self._lbl_kpi_vendas.config(text=str(result["qtd_v"]))
                self._lbl_kpi_total.config(
                    text=f"R$ {FormatUtils.format_money(result['tot_v'])}")
                self._lbl_kpi_vales.config(text=str(result["qtd_vales"]))
                self._lbl_kpi_sangrias.config(text=str(result["qtd_sang"]))
            except Exception:
                pass

        self.run_async(load, on_loaded)


    def abrir_caixa(self):
        valor = simpledialog.askfloat("Abrir Caixa", "Valor de abertura (R$):",
                                       initialvalue=0, minvalue=0, parent=self.root)
        if valor is None:
            return
        def abrir():
            db = DatabaseHelper.get_instance()
            caixa_id = db.execute_update(
                "INSERT INTO caixa (usuario_id, valor_abertura, status) VALUES (%s, %s, 'aberto')",
                (Session.user_id, valor)
            )
            # Carregar dados da empresa para o cupom
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass
            return {"caixa_id": caixa_id, "empresa": empresa}
        def on_aberto(result):
            caixa_id = result["caixa_id"]
            empresa = result["empresa"]
            Session.caixa_id = caixa_id
            data_abertura = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

            # Gerar cupom de abertura de caixa
            printer_cfg = self._load_printer_config()
            cupom = CupomGenerator.gerar_cupom_abertura_caixa(
                caixa_id=caixa_id,
                usuario_nome=Session.user_nome,
                valor_abertura=valor,
                data_abertura=data_abertura,
                empresa=empresa,
                colunas=printer_cfg.get("largura_papel", 48),
                margem_esq=printer_cfg.get("margem_esquerda", 0),
                margem_dir=printer_cfg.get("margem_direita", 0),
                espacamento=printer_cfg.get("espacamento_linhas", 1)
            )
            auto_imp = printer_cfg.get("imprimir_automatico", False)
            self._mostrar_cupom_caixa(cupom, "Abertura de Caixa",
                                       auto_imprimir=auto_imp)
            self.status_bar.set_caixa(True, caixa_id)
            ToastManager.success(
                f"Caixa #{caixa_id} aberto com R$ {FormatUtils.format_money(valor)}")
            AuditLogger.log("CAIXA_ABERTO",
                            f"Caixa #{caixa_id} | Valor: R$ {FormatUtils.format_money(valor)}",
                            usuario=Session.user_login)
            self.carregar_status_caixa()
        self.run_async(abrir, on_aberto)

    def fechar_caixa(self):
        if not self.caixa_aberto:
            return
        if not messagebox.askyesno("Confirmar", "Deseja fechar o caixa?"):
            return
        caixa_id_fechando = Session.caixa_id
        caixa_data = self.caixa_aberto
        def fechar():
            db = DatabaseHelper.get_instance()
            # Calcular total de vendas
            rows = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(total_liquido),0) as total FROM vendas "
                "WHERE caixa_id = %s AND status = 'finalizada'", (caixa_id_fechando,)
            )
            total_vendas = float(rows[0]["total"]) if rows else 0
            qtd_vendas = int(rows[0]["qtd"]) if rows else 0
            valor_abertura = float(caixa_data.get("valor_abertura", 0))

            # Vales
            rows_v = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total FROM vales_debito WHERE caixa_id = %s",
                (caixa_id_fechando,)
            )
            total_vales = float(rows_v[0]["total"]) if rows_v else 0
            qtd_vales = int(rows_v[0]["qtd"]) if rows_v else 0

            # Sangrias
            rows_s = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total FROM sangrias WHERE caixa_id = %s",
                (caixa_id_fechando,)
            )
            total_sangrias = float(rows_s[0]["total"]) if rows_s else 0
            qtd_sangrias = int(rows_s[0]["qtd"]) if rows_s else 0

            # Entradas (recebimentos de contas a receber = credito no caixa)
            rows_e = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total FROM entradas_caixa WHERE caixa_id = %s",
                (caixa_id_fechando,)
            )
            total_entradas = float(rows_e[0]["total"]) if rows_e else 0
            qtd_entradas = int(rows_e[0]["qtd"]) if rows_e else 0

            valor_fechamento = valor_abertura + total_vendas + total_entradas - total_vales - total_sangrias

            # Resumo por forma de pagamento
            pag_rows = db.execute_query(
                "SELECT fp.descricao, COALESCE(SUM(pv.valor),0) as total "
                "FROM pagamentos_venda pv "
                "JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "JOIN vendas v ON pv.venda_id = v.id "
                "WHERE v.caixa_id = %s AND v.status = 'finalizada' "
                "GROUP BY fp.id, fp.descricao ORDER BY total DESC", (caixa_id_fechando,)
            )
            pagamentos_resumo = []
            if pag_rows:
                for pr in pag_rows:
                    pagamentos_resumo.append({
                        "descricao": pr.get("descricao", "N/A"),
                        "total": float(pr.get("total", 0))
                    })

            # Carregar dados da empresa para o cupom
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass

            db.execute_update(
                "UPDATE caixa SET data_fechamento = NOW(), valor_fechamento = %s, "
                "status = 'fechado' WHERE id = %s",
                (valor_fechamento, caixa_id_fechando)
            )

            return {
                "valor_fechamento": valor_fechamento,
                "valor_abertura": valor_abertura,
                "total_vendas": total_vendas,
                "qtd_vendas": qtd_vendas,
                "total_vales": total_vales,
                "qtd_vales": qtd_vales,
                "total_sangrias": total_sangrias,
                "qtd_sangrias": qtd_sangrias,
                "total_entradas": total_entradas,
                "qtd_entradas": qtd_entradas,
                "pagamentos_resumo": pagamentos_resumo,
                "empresa": empresa,
                "data_abertura": caixa_data.get("data_abertura", ""),
            }

        def on_fechado(result):
            valor_fechamento = result["valor_fechamento"]
            data_fechamento = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")
            caixa_id_tmp = caixa_id_fechando
            Session.caixa_id = 0

            # Gerar cupom de fechamento de caixa
            printer_cfg = self._load_printer_config()
            cupom = CupomGenerator.gerar_cupom_fechamento_caixa(
                caixa_id=caixa_id_tmp,
                usuario_nome=Session.user_nome,
                valor_abertura=result["valor_abertura"],
                valor_fechamento=valor_fechamento,
                data_abertura=result["data_abertura"],
                data_fechamento=data_fechamento,
                total_vendas=result["total_vendas"],
                qtd_vendas=result["qtd_vendas"],
                total_vales=result["total_vales"],
                qtd_vales=result["qtd_vales"],
                total_sangrias=result.get("total_sangrias", 0),
                qtd_sangrias=result.get("qtd_sangrias", 0),
                total_entradas=result.get("total_entradas", 0),
                qtd_entradas=result.get("qtd_entradas", 0),
                pagamentos_resumo=result["pagamentos_resumo"],
                empresa=result["empresa"],
                colunas=printer_cfg.get("largura_papel", 48),
                margem_esq=printer_cfg.get("margem_esquerda", 0),
                margem_dir=printer_cfg.get("margem_direita", 0),
                espacamento=printer_cfg.get("espacamento_linhas", 1)
            )
            auto_imp = printer_cfg.get("imprimir_automatico", False)
            self._mostrar_cupom_caixa(cupom, "Fechamento de Caixa",
                                       auto_imprimir=auto_imp)
            self.status_bar.set_caixa(False)
            ToastManager.info(
                f"Caixa #{caixa_id_tmp} fechado. "
                f"Total: R$ {FormatUtils.format_money(valor_fechamento)}")
            AuditLogger.log(
                "CAIXA_FECHADO",
                f"Caixa #{caixa_id_tmp} | "
                f"Total Vendas: R$ {FormatUtils.format_money(result['total_vendas'])} | "
                f"Fechamento: R$ {FormatUtils.format_money(valor_fechamento)}",
                usuario=Session.user_login)
            self.carregar_status_caixa()

        self.run_async(fechar, on_fechado)

    def add_vale_debito(self):
        if not Session.caixa_id:
            self.show_error("Abra o caixa antes de registrar vales.")
            return
        dialog = tk.Toplevel(self.root)
        dialog.title("Vale/Debito")
        dialog.geometry("400x250")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        card = CardFrame(dialog)
        card.pack(padx=20, pady=20, fill="x")

        tk.Label(card, text="Descricao:", bg=COR_CARD, fg=COR_TEXTO).pack(anchor="w")
        et_desc = StyledEntry(card)
        et_desc.pack(fill="x", pady=(0, 8))

        tk.Label(card, text="Valor (R$):", bg=COR_CARD, fg=COR_TEXTO).pack(anchor="w")
        et_valor = StyledEntry(card)
        et_valor.pack(fill="x", pady=(0, 10))

        def salvar():
            desc = et_desc.get().strip()
            valor = FormatUtils.parse_money(et_valor.get())
            if not desc or valor <= 0:
                messagebox.showwarning(
                    "Campos Obrigatorios",
                    "Preencha todos os campos para registrar o vale/debito.\n\n"
                    "1. Descricao - motivo do vale (ex: 'Vale funcionario Joao')\n"
                    "2. Valor - valor em reais (ex: 50,00)\n\n"
                    "O valor deve ser maior que zero.",
                    parent=dialog)
                return
            try:
                db = DatabaseHelper.get_instance()
                db.execute_update(
                    "INSERT INTO vales_debito (caixa_id, descricao, valor) VALUES (%s,%s,%s)",
                    (Session.caixa_id, desc, valor)
                )
                messagebox.showinfo(
                    "Vale Registrado",
                    "O vale/debito foi registrado com sucesso no caixa!\n\n"
                    "O valor sera descontado no fechamento do caixa.",
                    parent=dialog)
                dialog.destroy()
                self.carregar_status_caixa()
            except Exception as e:
                ErroAmigavel.mostrar_erro(e, "operacao_dialog", parent=dialog)

        StyledButton(card, text="✔ Salvar", command=salvar, color=COR_BOTAO_VERDE).pack(pady=5)


    def add_sangria(self):
        """Registra uma sangria no caixa. Exige senha de administrador."""
        if not Session.caixa_id:
            self.show_error("Abra o caixa antes de registrar sangrias.")
            return

        def _abrir_dialog_sangria():
            dialog = tk.Toplevel(self.root)
            dialog.title("Sangria de Caixa")
            dialog.configure(bg=COR_FUNDO)
            dialog.transient(self.root)
            dialog.grab_set()
            dialog.resizable(False, False)

            # Posicionar o dialog a direita da tela
            dlg_w = 450
            dlg_h = 320
            screen_w = dialog.winfo_screenwidth()
            screen_h = dialog.winfo_screenheight()
            pos_x = screen_w - dlg_w - 30
            pos_y = (screen_h - dlg_h) // 2
            dialog.geometry(f"{dlg_w}x{dlg_h}+{pos_x}+{pos_y}")

            tk.Label(dialog, text="Sangria de Caixa", bg=COR_FUNDO, fg=COR_PRIMARIA,
                     font=("Segoe UI", 16, "bold")).pack(pady=(15, 5))
            tk.Label(dialog, text="Informe os dados da sangria.",
                     bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 10),
                     justify="center").pack(pady=(0, 10))

            card = CardFrame(dialog)
            card.pack(padx=25, pady=5, fill="x")

            tk.Label(card, text="Descricao/Motivo:", bg=COR_CARD, fg=COR_TEXTO,
                     font=("Segoe UI", 11)).pack(anchor="w", pady=(8, 2))
            et_desc = StyledEntry(card)
            et_desc.pack(fill="x", pady=(0, 8))
            et_desc.focus_set()

            tk.Label(card, text="Valor (R$):", bg=COR_CARD, fg=COR_TEXTO,
                     font=("Segoe UI", 11)).pack(anchor="w", pady=(0, 2))
            et_valor = StyledEntry(card)
            et_valor.pack(fill="x", pady=(0, 10))

            lbl_erro = tk.Label(dialog, text="", bg=COR_FUNDO, fg=COR_ERRO,
                                 font=("Segoe UI", 9, "italic"))
            lbl_erro.pack(pady=(0, 5))

            def salvar_sangria():
                desc = et_desc.get().strip()
                valor = FormatUtils.parse_money(et_valor.get())
                if not desc:
                    lbl_erro.config(text="Informe a descricao/motivo da sangria.")
                    return
                if valor <= 0:
                    lbl_erro.config(text="Informe um valor valido para a sangria.")
                    return

                def _registrar():
                    db = DatabaseHelper.get_instance()
                    # Buscar nome do admin que autorizou (usuario logado se for admin,
                    # ou o admin que autorizou via _solicitar_senha_admin)
                    sangria_id = db.execute_update(
                        "INSERT INTO sangrias (caixa_id, descricao, valor, operador_nome) "
                        "VALUES (%s, %s, %s, %s)",
                        (Session.caixa_id, desc, valor, Session.user_nome)
                    )
                    # Carregar dados da empresa para o cupom
                    empresa = None
                    try:
                        emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                        if emp_rows:
                            empresa = emp_rows[0]
                    except Exception:
                        pass
                    return {"sangria_id": sangria_id, "empresa": empresa,
                            "desc": desc, "valor": valor}

                def _on_registrado(result):
                    dialog.destroy()
                    data_sangria = datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

                    # Gerar cupom de sangria
                    printer_cfg = self._load_printer_config()
                    cupom = CupomGenerator.gerar_cupom_sangria(
                        sangria_id=result["sangria_id"],
                        caixa_id=Session.caixa_id,
                        operador_nome=Session.user_nome,
                        descricao=result["desc"],
                        valor=result["valor"],
                        data_sangria=data_sangria,
                        empresa=result["empresa"],
                        colunas=printer_cfg.get("largura_papel", 48),
                        margem_esq=printer_cfg.get("margem_esquerda", 0),
                        margem_dir=printer_cfg.get("margem_direita", 0),
                        espacamento=printer_cfg.get("espacamento_linhas", 1)
                    )
                    auto_imp = printer_cfg.get("imprimir_automatico", False)
                    self._mostrar_cupom_caixa(cupom, "Comprovante de Sangria", auto_imprimir=auto_imp)
                    self.carregar_status_caixa()

                self.run_async(_registrar, _on_registrado)

            btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
            btn_frame.pack(fill="x", padx=25, pady=(0, 15))
            StyledButton(btn_frame, text="Registrar Sangria", command=salvar_sangria,
                         color="#9C27B0", width=15).pack(side="left", padx=5)
            StyledButton(btn_frame, text="✘ Cancelar", command=dialog.destroy,
                         color="#2a3a5c", width=12).pack(side="right", padx=5)

        # Solicitar senha de administrador antes de abrir o dialog
        self._solicitar_senha_admin(_abrir_dialog_sangria, parent_window=self.root)

    # ========================================================================
    # DIALOG GENERICO DE SELECAO DE REGISTROS
    # ========================================================================
    def _selecionar_registro(self, tabela, titulo, sql, colunas, callback, params=None):
        dialog = tk.Toplevel(self.root)
        dialog.title(titulo)
        dialog.geometry("650x450")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text=titulo, bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=10)

        # Busca
        busca_frame = tk.Frame(dialog, bg=COR_FUNDO)
        busca_frame.pack(fill="x", padx=15, pady=5)
        et_busca = StyledEntry(busca_frame, width=40)
        et_busca.pack(side="left", fill="x", expand=True)

        tree = ttk.Treeview(dialog, columns=[f"c{i}" for i in range(len(colunas))],
                            show="headings", height=12)
        for i, col in enumerate(colunas):
            tree.heading(f"c{i}", text=col)
            tree.column(f"c{i}", width=120)
        tree.pack(fill="both", expand=True, padx=15, pady=5)

        vsb = ttk.Scrollbar(dialog, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)

        def carregar(filtro=""):
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(sql, params)
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                for row in rows:
                    values = list(row.values())
                    if filtro:
                        match = any(filtro.lower() in str(v).lower() for v in values)
                        if not match:
                            continue
                    tree.insert("", "end", values=values)
            self.run_async(load, on_loaded)

        et_busca.bind("<KeyRelease>", lambda e: carregar(et_busca.get()))

        def selecionar():
            sel = tree.selection()
            if sel:
                values = tree.item(sel[0])["values"]
                dialog.destroy()
                callback(values)

        tree.bind("<Double-1>", lambda e: selecionar())
        StyledButton(dialog, text="▶ Selecionar", command=selecionar,
                     color=COR_BOTAO_VERDE, width=15).pack(pady=5)

        carregar()

    # ========================================================================
    # TELA GENERICA DE CADASTRO (CRUD)
    # ========================================================================
    def _show_cadastro_generico(self, titulo, table_name, campos, sql_list, sql_insert,
                                 sql_update, sql_delete=None, extra_buttons=None,
                                 on_after_save=None):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text=titulo, bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # Formulario
        form_card = CardFrame(content)
        form_card.pack(fill="x", pady=5)

        entries = {}
        row_idx = 0
        for campo in campos:
            nome = campo["nome"]
            label = campo.get("label", nome.replace("_", " ").title())
            tipo = campo.get("tipo", "text")

            tk.Label(form_card, text=f"{label}:", bg=COR_CARD, fg=COR_TEXTO,
                     font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)

            if tipo == "combo":
                widget = ttk.Combobox(form_card, values=campo.get("values", []),
                                       width=30, state="readonly")
                if campo.get("values"):
                    widget.current(0)
            elif tipo == "check":
                var = tk.BooleanVar(value=campo.get("default", False))
                widget = tk.Checkbutton(form_card, variable=var, bg=COR_CARD,
                                         fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                                         activebackground=COR_CARD)
                widget._var = var
            else:
                widget = StyledEntry(form_card, width=35)
                if campo.get("default"):
                    widget.insert(0, str(campo["default"]))

            widget.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
            entries[nome] = widget
            row_idx += 1

        # Botoes do formulario
        btn_form = tk.Frame(form_card, bg=COR_CARD)
        btn_form.grid(row=row_idx, column=0, columnspan=2, pady=10)

        selected_id = [None]

        def limpar_form():
            selected_id[0] = None
            for campo in campos:
                w = entries[campo["nome"]]
                tipo = campo.get("tipo", "text")
                if tipo == "combo":
                    if w["values"]:
                        w.current(0)
                elif tipo == "check":
                    w._var.set(campo.get("default", False))
                else:
                    w.delete(0, tk.END)
                    if campo.get("default"):
                        w.insert(0, str(campo["default"]))

        def get_values():
            vals = []
            for campo in campos:
                w = entries[campo["nome"]]
                tipo = campo.get("tipo", "text")
                if tipo == "combo":
                    vals.append(w.get())
                elif tipo == "check":
                    vals.append(1 if w._var.get() else 0)
                elif tipo == "money":
                    vals.append(FormatUtils.parse_money(w.get()))
                elif tipo == "int":
                    try:
                        vals.append(int(w.get()))
                    except ValueError:
                        vals.append(0)
                elif tipo == "float":
                    try:
                        vals.append(float(w.get().replace(",", ".")))
                    except ValueError:
                        vals.append(0.0)
                else:
                    vals.append(w.get().strip())
            return vals

        def salvar():
            vals = get_values()
            # Validar campo obrigatorio (primeiro campo geralmente)
            if campos[0].get("required", True) and not vals[0]:
                self.show_error(f"Preencha o campo {campos[0].get('label', campos[0]['nome'])}.")
                return
            def do_save():
                db = DatabaseHelper.get_instance()
                last_id = None
                if selected_id[0]:
                    db.execute_update(sql_update, tuple(vals) + (selected_id[0],))
                    last_id = selected_id[0]
                else:
                    last_id = db.execute_update(sql_insert, tuple(vals))
                return last_id
            def on_saved(last_id):
                if on_after_save and last_id:
                    try:
                        on_after_save(last_id, vals, campos)
                    except Exception as e:
                        print(f"Aviso on_after_save: {e}")
                limpar_form()
                carregar_lista()
                self.show_success("Registro salvo com sucesso!")
            self.run_async(do_save, on_saved)

        def excluir():
            if not selected_id[0]:
                self.show_error("Por favor, selecione um registro na lista antes de excluir.\n\nClique sobre o registro desejado e tente novamente.")
                return
            if not messagebox.askyesno("Confirmar", "Deseja excluir este registro?"):
                return
            def do_del():
                db = DatabaseHelper.get_instance()
                if sql_delete:
                    db.execute_update(sql_delete, (selected_id[0],))
                else:
                    db.execute_update(f"DELETE FROM {table_name} WHERE id = %s", (selected_id[0],))
            def on_deleted(_):
                limpar_form()
                carregar_lista()
            self.run_async(do_del, on_deleted)

        StyledButton(btn_form, text="✔ Salvar", command=salvar,
                     color=COR_BOTAO_VERDE, width=10).pack(side="left", padx=5)
        StyledButton(btn_form, text="✚ Novo", command=limpar_form,
                     color=COR_BOTAO_AZUL, width=10).pack(side="left", padx=5)
        StyledButton(btn_form, text="✖ Excluir", command=excluir,
                     color=COR_ERRO, width=10).pack(side="left", padx=5)

        if extra_buttons:
            for btn_text, btn_cmd in extra_buttons:
                StyledButton(btn_form, text=btn_text, command=lambda c=btn_cmd: c(selected_id[0]),
                             color=COR_BOTAO_LARANJA, width=12).pack(side="left", padx=5)

        # Lista
        col_names = [c.get("label", c["nome"].replace("_", " ").title()) for c in campos]
        col_ids = [f"c{i}" for i in range(len(campos) + 1)]  # +1 para ID

        tree = ttk.Treeview(content, columns=col_ids, show="headings", height=15)
        tree.heading("c0", text="ID")
        tree.column("c0", width=50, anchor="center")
        for i, col in enumerate(col_names):
            tree.heading(f"c{i+1}", text=col)
            tree.column(f"c{i+1}", width=120)

        vsb = ttk.Scrollbar(content, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True, pady=5)
        vsb.pack(side="right", fill="y", pady=5)

        def on_select(event):
            sel = tree.selection()
            if sel:
                values = tree.item(sel[0])["values"]
                selected_id[0] = values[0]
                for i, campo in enumerate(campos):
                    w = entries[campo["nome"]]
                    tipo = campo.get("tipo", "text")
                    val = values[i + 1] if i + 1 < len(values) else ""
                    if tipo == "combo":
                        try:
                            w.set(str(val))
                        except Exception:
                            pass
                    elif tipo == "check":
                        w._var.set(bool(val))
                    else:
                        w.delete(0, tk.END)
                        w.insert(0, str(val) if val else "")

        tree.bind("<<TreeviewSelect>>", on_select)

        def carregar_lista():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(sql_list)
            def on_loaded(rows):
                # Preservar selecao atual
                sel = tree.selection()
                sel_id = None
                if sel:
                    try:
                        sel_id = tree.item(sel[0])["values"][0]
                    except Exception:
                        pass
                tree.delete(*tree.get_children())
                new_sel_iid = None
                for row in rows:
                    vals = list(row.values())
                    iid = tree.insert("", "end", values=vals)
                    if sel_id is not None and vals and vals[0] == sel_id:
                        new_sel_iid = iid
                # Restaurar selecao
                if new_sel_iid:
                    tree.selection_set(new_sel_iid)
                    tree.see(new_sel_iid)
            self.run_async(load, on_loaded)

        carregar_lista()

        # Auto-refresh em tempo real (a cada 10 segundos)
        self._start_auto_refresh(f"cadastro_{table_name}", carregar_lista, 10000)

    # ========================================================================
    # CADASTROS ESPECIFICOS
    # ========================================================================
    def show_cadastro_produto(self):
        """Tela de Cadastro de Produtos - Alinhada com CadastroProdutoActivity.java do Android.
        Campos: Foto, Codigo (automatico), Codigo de Barras + Gerar EAN-13, Descricao,
        Tipo do Produto (combobox), Unidade, Preco Custo, Preco Venda, Estoque, Estoque Minimo.
        Funcionalidades: Busca, Novo, Editar, Inativar (nao exclui), Foto em base64.
        """
        if not Session.has_permission(PermissionConstants.PRODUTOS_ACESSAR):
            self.show_error("Voce nao tem permissao para acessar o cadastro de produtos.")
            return

        self.clear_container()
        self._produto_foto_base64_temp = None
        self._produto_foto_tk = None  # Referencia para evitar garbage collection

        # --- Barra superior ---
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Cadastro de Produtos", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        # --- Area principal com scroll ---
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # --- Busca (igual ao Android: etBusca + btnBuscar) ---
        busca_frame = tk.Frame(content, bg=COR_FUNDO)
        busca_frame.pack(fill="x", pady=(5, 5))
        tk.Label(busca_frame, text="Buscar:", bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left", padx=(0, 5))
        et_busca = StyledEntry(busca_frame, width=40)
        et_busca.pack(side="left", padx=5, fill="x", expand=True)
        btn_buscar = StyledButton(busca_frame, text="⌕ Buscar", color=COR_BOTAO_AZUL, width=8)
        btn_buscar.pack(side="left", padx=5)

        # --- Botao Novo (controlado por permissao PRODUTOS_CRIAR) ---
        btn_novo = StyledButton(busca_frame, text="+ Novo Produto", color=COR_BOTAO_VERDE, width=14)
        if Session.has_permission(PermissionConstants.PRODUTOS_CRIAR):
            btn_novo.pack(side="left", padx=5)

        # --- Painel dividido: formulario (esquerda) + lista (direita) ---
        paned = tk.PanedWindow(content, orient=tk.HORIZONTAL, bg=COR_FUNDO,
                               sashwidth=4, sashrelief="flat")
        paned.pack(fill="both", expand=True, pady=5)

        # === FORMULARIO (esquerda) ===
        form_outer = tk.Frame(paned, bg=COR_FUNDO)
        paned.add(form_outer, minsize=350, width=400)

        # Canvas com scroll para o formulario
        form_canvas = tk.Canvas(form_outer, bg=COR_CARD, highlightthickness=0)
        form_scrollbar = ttk.Scrollbar(form_outer, orient="vertical", command=form_canvas.yview)
        form_card = tk.Frame(form_canvas, bg=COR_CARD, padx=15, pady=10)
        form_card.bind("<Configure>", lambda e: form_canvas.configure(scrollregion=form_canvas.bbox("all")))
        form_canvas.create_window((0, 0), window=form_card, anchor="nw")
        form_canvas.configure(yscrollcommand=form_scrollbar.set)
        form_canvas.pack(side="left", fill="both", expand=True)
        form_scrollbar.pack(side="right", fill="y")
        # Bind mousewheel para o formulario
        def _on_form_mousewheel(event):
            form_canvas.yview_scroll(int(-1*(event.delta/120)), "units")
        form_canvas.bind("<Enter>", lambda e: form_canvas.bind_all("<MouseWheel>", _on_form_mousewheel))
        form_canvas.bind("<Leave>", lambda e: form_canvas.unbind_all("<MouseWheel>"))

        row_idx = 0

        # --- Foto do Produto (igual ao Android: ivFoto + btnFoto) ---
        foto_frame = tk.Frame(form_card, bg=COR_CARD)
        foto_frame.grid(row=row_idx, column=0, columnspan=2, pady=(5, 10), sticky="ew")
        iv_foto = tk.Label(foto_frame, bg="#263238", width=16, height=8,
                           relief="flat", bd=1, highlightthickness=1,
                           highlightbackground="#37474F")
        iv_foto.pack(pady=(0, 5))
        btn_foto = StyledButton(foto_frame, text="Escolher Foto", color=COR_BOTAO_AZUL, width=14)
        btn_foto.pack()
        btn_remover_foto = StyledButton(foto_frame, text="Remover Foto", color=COR_ERRO, width=14)
        btn_remover_foto.pack(pady=(3, 0))
        row_idx += 1

        # --- Codigo (automatico, igual ao Android) ---
        tk.Label(form_card, text="Codigo (automatico):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_codigo = StyledEntry(form_card, width=35)
        et_codigo.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Codigo de Barras + Botao Gerar EAN-13 (igual ao Android) ---
        tk.Label(form_card, text="Cod. Barras (EAN-13):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        barras_frame = tk.Frame(form_card, bg=COR_CARD)
        barras_frame.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        et_codigo_barras = StyledEntry(barras_frame, width=20)
        et_codigo_barras.pack(side="left")
        btn_gerar_ean = StyledButton(barras_frame, text="Gerar", color=COR_BOTAO_LARANJA, width=6)
        btn_gerar_ean.pack(side="left", padx=(5, 0))
        row_idx += 1

        # --- Descricao ---
        tk.Label(form_card, text="Descricao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_descricao = StyledEntry(form_card, width=35)
        et_descricao.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Tipo do Produto (Spinner/Combobox, igual ao Android) ---
        tk.Label(form_card, text="Tipo do Produto:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        combo_tipo_produto = ttk.Combobox(form_card, width=32, state="readonly")
        combo_tipo_produto.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Unidade ---
        tk.Label(form_card, text="Unidade (UN, KG, etc):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_unidade = StyledEntry(form_card, width=35)
        et_unidade.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Preco de Custo ---
        tk.Label(form_card, text="Preco de Custo:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_preco_custo = StyledEntry(form_card, width=35)
        et_preco_custo.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Preco de Venda ---
        tk.Label(form_card, text="Preco de Venda:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_preco_venda = StyledEntry(form_card, width=35)
        et_preco_venda.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Estoque ---
        tk.Label(form_card, text="Estoque:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_estoque = StyledEntry(form_card, width=35)
        et_estoque.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Estoque Minimo ---
        tk.Label(form_card, text="Estoque Minimo:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=row_idx, column=0, sticky="w", padx=5, pady=3)
        et_estoque_min = StyledEntry(form_card, width=35)
        et_estoque_min.grid(row=row_idx, column=1, sticky="w", padx=5, pady=3)
        row_idx += 1

        # --- Botoes do formulario ---
        btn_form = tk.Frame(form_card, bg=COR_CARD)
        btn_form.grid(row=row_idx, column=0, columnspan=2, pady=10)

        btn_salvar = StyledButton(btn_form, text="✔ Salvar", color=COR_BOTAO_VERDE, width=10)
        btn_salvar.pack(side="left", padx=5)
        btn_limpar = StyledButton(btn_form, text="✚ Novo", color=COR_BOTAO_AZUL, width=10)
        btn_limpar.pack(side="left", padx=5)
        btn_inativar = StyledButton(btn_form, text="Inativar", color=COR_ERRO, width=10)
        btn_inativar.pack(side="left", padx=5)

        # === LISTA (direita) ===
        list_frame = tk.Frame(paned, bg=COR_FUNDO)
        paned.add(list_frame, minsize=400)

        col_ids = ("id", "descricao", "codigo", "preco_venda", "estoque", "codigo_barras", "tipo", "ativo")
        col_labels = ("ID", "Descricao", "Codigo", "Preco Venda", "Estoque", "Cod.Barras", "Tipo", "Ativo")
        col_widths = (40, 180, 60, 80, 60, 100, 100, 50)

        tree = ttk.Treeview(list_frame, columns=col_ids, show="headings", height=20)
        for cid, clabel, cwidth in zip(col_ids, col_labels, col_widths):
            tree.heading(cid, text=clabel)
            tree.column(cid, width=cwidth, anchor="center" if cid in ("id", "ativo") else "w")

        vsb = ttk.Scrollbar(list_frame, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True, pady=5)
        vsb.pack(side="right", fill="y", pady=5)

        # ---- Variaveis de estado ----
        selected_id = [None]
        tipos_produto_cache = [[]]

        # ---- Funcoes auxiliares ----

        def _load_foto_into_label(b64_data):
            """Carrega imagem base64 no label iv_foto (igual ao Android ImageUtils.loadBase64IntoImageView)."""
            if not b64_data:
                iv_foto.config(image="", text="Sem Foto", fg=COR_TEXTO2)
                self._produto_foto_tk = None
                return
            try:
                if HAS_PIL:
                    img_data = base64.b64decode(b64_data)
                    img = Image.open(io.BytesIO(img_data))
                    img = img.resize((120, 120), Image.LANCZOS)
                    photo = ImageTk.PhotoImage(img)
                    iv_foto.config(image=photo, text="")
                    self._produto_foto_tk = photo  # Manter referencia
                else:
                    iv_foto.config(image="", text="[Foto OK]", fg=COR_SUCESSO)
            except Exception:
                iv_foto.config(image="", text="[Erro foto]", fg=COR_ERRO)

        def _escolher_foto():
            """Abre dialog para selecionar imagem (igual ao Android imagePickerLauncher)."""
            filepath = filedialog.askopenfilename(
                title="Selecionar Foto do Produto",
                filetypes=[("Imagens", "*.png *.jpg *.jpeg *.gif *.bmp *.webp"), ("Todos", "*.*")]
            )
            if filepath:
                try:
                    if HAS_PIL:
                        img = Image.open(filepath)
                        # Redimensionar para max 400x400 para nao sobrecarregar o banco
                        img.thumbnail((400, 400), Image.LANCZOS)
                        buffer = io.BytesIO()
                        img.save(buffer, format="PNG")
                        b64 = base64.b64encode(buffer.getvalue()).decode("utf-8")
                    else:
                        with open(filepath, "rb") as f:
                            b64 = base64.b64encode(f.read()).decode("utf-8")
                    self._produto_foto_base64_temp = b64
                    _load_foto_into_label(b64)
                except Exception as e:
                    self.show_error(f"Erro ao carregar imagem: {e}")

        def _remover_foto():
            """Remove a foto do produto."""
            self._produto_foto_base64_temp = None
            _load_foto_into_label(None)

        def _carregar_tipos_produto():
            """Carrega tipos de produto do banco para o combobox (igual ao Android carregarTiposProduto)."""
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query("SELECT id, descricao FROM tipos_produto WHERE ativo = 1 ORDER BY descricao")
            def on_loaded(rows):
                tipos = [{"id": 0, "descricao": "-- Selecione o Tipo --"}]
                for r in rows:
                    tipos.append({"id": r["id"], "descricao": r["descricao"]})
                tipos_produto_cache[0] = tipos
                combo_tipo_produto["values"] = [t["descricao"] for t in tipos]
                combo_tipo_produto.current(0)
            self.run_async(load, on_loaded)

        def _get_tipo_produto_id_selecionado():
            """Retorna o ID do tipo de produto selecionado no combobox."""
            idx = combo_tipo_produto.current()
            if idx >= 0 and idx < len(tipos_produto_cache[0]):
                return tipos_produto_cache[0][idx]["id"]
            return 0

        def _set_tipo_produto_por_id(tipo_id):
            """Seleciona o tipo de produto no combobox pelo ID."""
            if not tipo_id:
                combo_tipo_produto.current(0)
                return
            for i, t in enumerate(tipos_produto_cache[0]):
                if t["id"] == tipo_id:
                    combo_tipo_produto.current(i)
                    return
            combo_tipo_produto.current(0)

        def _gerar_proximo_codigo():
            """Gera o proximo codigo sequencial (igual ao Android gerarProximoCodigo)."""
            def load():
                db = DatabaseHelper.get_instance()
                rows = db.execute_query(
                    "SELECT MAX(CAST(codigo AS UNSIGNED)) AS max_codigo FROM produtos WHERE codigo REGEXP '^[0-9]+$'")
                max_codigo = 0
                if rows and rows[0].get("max_codigo"):
                    max_codigo = int(rows[0]["max_codigo"])
                proximo = max_codigo + 1
                return f"{proximo:03d}" if proximo < 1000 else str(proximo)
            def on_loaded(codigo):
                et_codigo.delete(0, tk.END)
                et_codigo.insert(0, codigo)
            self.run_async(load, on_loaded)

        def _gerar_ean13_unico():
            """Gera EAN-13 unico verificando no banco (igual ao Android gerarEan13Unico)."""
            btn_gerar_ean.config(state="disabled", text="...")
            def load():
                db = DatabaseHelper.get_instance()
                for _ in range(50):
                    ean = EanUtils.gerar_ean13()
                    rows = db.execute_query("SELECT COUNT(*) as cnt FROM produtos WHERE codigo_barras = %s", (ean,))
                    if rows and rows[0].get("cnt", 0) == 0:
                        return ean
                return EanUtils.gerar_ean13()
            def on_loaded(ean):
                et_codigo_barras.delete(0, tk.END)
                et_codigo_barras.insert(0, ean)
                btn_gerar_ean.config(state="normal", text="Gerar")
                self.show_success(f"EAN-13 gerado: {ean}")
            def on_error(e):
                ean = EanUtils.gerar_ean13()
                et_codigo_barras.delete(0, tk.END)
                et_codigo_barras.insert(0, ean)
                btn_gerar_ean.config(state="normal", text="Gerar")
                self.show_success(f"EAN-13 gerado: {ean}")
            self.run_async(load, on_loaded, on_error)

        def limpar_form():
            """Limpa o formulario e prepara para novo produto (igual ao Android showEditDialog(null))."""
            selected_id[0] = None
            self._produto_foto_base64_temp = None
            _load_foto_into_label(None)
            et_codigo.delete(0, tk.END)
            et_codigo_barras.delete(0, tk.END)
            et_descricao.delete(0, tk.END)
            combo_tipo_produto.current(0)
            et_unidade.delete(0, tk.END)
            et_unidade.insert(0, "UN")
            et_preco_custo.delete(0, tk.END)
            et_preco_venda.delete(0, tk.END)
            et_estoque.delete(0, tk.END)
            et_estoque_min.delete(0, tk.END)
            # Gerar codigo automatico (igual ao Android)
            _gerar_proximo_codigo()

        def preencher_form(produto_dict):
            """Preenche o formulario com dados do produto selecionado (igual ao Android showEditDialog(produto))."""
            selected_id[0] = produto_dict.get("id")
            et_codigo.delete(0, tk.END)
            et_codigo.insert(0, FormatUtils.safe_string(str(produto_dict.get("codigo", ""))))
            et_codigo_barras.delete(0, tk.END)
            et_codigo_barras.insert(0, FormatUtils.safe_string(str(produto_dict.get("codigo_barras", ""))))
            et_descricao.delete(0, tk.END)
            et_descricao.insert(0, FormatUtils.safe_string(str(produto_dict.get("descricao", ""))))
            _set_tipo_produto_por_id(produto_dict.get("tipo_produto_id"))
            et_unidade.delete(0, tk.END)
            et_unidade.insert(0, FormatUtils.safe_string(str(produto_dict.get("unidade", "UN"))))
            et_preco_custo.delete(0, tk.END)
            et_preco_custo.insert(0, FormatUtils.format_money(produto_dict.get("preco_custo", 0)))
            et_preco_venda.delete(0, tk.END)
            et_preco_venda.insert(0, FormatUtils.format_money(produto_dict.get("preco_venda", 0)))
            et_estoque.delete(0, tk.END)
            et_estoque.insert(0, FormatUtils.format_quantidade(produto_dict.get("estoque", 0)))
            et_estoque_min.delete(0, tk.END)
            et_estoque_min.insert(0, FormatUtils.format_quantidade(produto_dict.get("estoque_minimo", 0)))
            # Foto
            foto_b64 = produto_dict.get("foto_base64")
            self._produto_foto_base64_temp = foto_b64
            _load_foto_into_label(foto_b64)

        def salvar_produto():
            """Salva o produto (igual ao Android saveProduto)."""
            descricao = et_descricao.get().strip()
            if not descricao:
                self.show_error("Por favor, preencha o campo Descricao antes de salvar.")
                return

            # Verificar permissao
            if selected_id[0]:
                if not Session.has_permission(PermissionConstants.PRODUTOS_EDITAR):
                    self.show_error("Voce nao tem permissao para editar produtos.")
                    return
            else:
                if not Session.has_permission(PermissionConstants.PRODUTOS_CRIAR):
                    self.show_error("Voce nao tem permissao para criar produtos.")
                    return

            codigo = et_codigo.get().strip()
            codigo_barras = et_codigo_barras.get().strip()
            unidade = et_unidade.get().strip() or "UN"
            preco_custo = FormatUtils.parse_money(et_preco_custo.get())
            preco_venda = FormatUtils.parse_money(et_preco_venda.get())
            estoque_val = et_estoque.get().strip().replace(",", ".")
            estoque_min_val = et_estoque_min.get().strip().replace(",", ".")
            try:
                estoque = float(estoque_val) if estoque_val else 0.0
            except ValueError:
                estoque = 0.0
            try:
                estoque_min = float(estoque_min_val) if estoque_min_val else 0.0
            except ValueError:
                estoque_min = 0.0
            tipo_produto_id = _get_tipo_produto_id_selecionado()
            foto_b64 = self._produto_foto_base64_temp

            def do_save():
                db = DatabaseHelper.get_instance()
                codigo_final = codigo
                if not codigo_final or codigo_final == "...":
                    rows = db.execute_query(
                        "SELECT MAX(CAST(codigo AS UNSIGNED)) AS max_codigo FROM produtos WHERE codigo REGEXP '^[0-9]+$'")
                    max_c = 0
                    if rows and rows[0].get("max_codigo"):
                        max_c = int(rows[0]["max_codigo"])
                    prox = max_c + 1
                    codigo_final = f"{prox:03d}" if prox < 1000 else str(prox)

                if selected_id[0]:
                    # UPDATE (igual ao Android)
                    db.execute_update(
                        "UPDATE produtos SET codigo=%s, codigo_barras=%s, descricao=%s, unidade=%s, "
                        "tipo_produto_id=%s, preco_custo=%s, preco_venda=%s, estoque=%s, "
                        "estoque_minimo=%s, foto_base64=%s WHERE id=%s",
                        (codigo_final, codigo_barras if codigo_barras else None, descricao, unidade,
                         tipo_produto_id if tipo_produto_id > 0 else None,
                         preco_custo, preco_venda, estoque, estoque_min, foto_b64,
                         selected_id[0])
                    )
                else:
                    # INSERT (igual ao Android)
                    db.execute_update(
                        "INSERT INTO produtos (codigo, codigo_barras, descricao, unidade, tipo_produto_id, "
                        "preco_custo, preco_venda, estoque, estoque_minimo, foto_base64, ativo) "
                        "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,1)",
                        (codigo_final, codigo_barras if codigo_barras else None, descricao, unidade,
                         tipo_produto_id if tipo_produto_id > 0 else None,
                         preco_custo, preco_venda, estoque, estoque_min, foto_b64)
                    )
            def on_saved(_):
                limpar_form()
                carregar_lista()
                self.show_success("Produto salvo!")
            self.run_async(do_save, on_saved)

        def inativar_produto():
            """Inativa o produto selecionado (igual ao Android inativarProduto, NAO exclui)."""
            if not selected_id[0]:
                self.show_error("Selecione um produto para inativar.")
                return
            if not Session.has_permission(PermissionConstants.PRODUTOS_EXCLUIR):
                self.show_error("Voce nao tem permissao para inativar produtos.")
                return
            if not messagebox.askyesno("Inativar", "Deseja inativar este produto?\n\nO produto nao aparecera mais nas vendas, mas podera ser reativado depois."):
                return
            def do_inativar():
                db = DatabaseHelper.get_instance()
                db.execute_update("UPDATE produtos SET ativo = 0 WHERE id = %s", (selected_id[0],))
            def on_inativado(_):
                limpar_form()
                carregar_lista()
                self.show_success("Produto inativado!")
            self.run_async(do_inativar, on_inativado)

        def on_select(event):
            """Ao selecionar um item na lista, preenche o formulario."""
            sel = tree.selection()
            if sel:
                item_id = tree.item(sel[0])["values"][0]
                # Carregar dados completos do banco (incluindo foto_base64)
                def load():
                    db = DatabaseHelper.get_instance()
                    rows = db.execute_query(
                        "SELECT p.*, tp.descricao AS tipo_desc FROM produtos p "
                        "LEFT JOIN tipos_produto tp ON p.tipo_produto_id = tp.id "
                        "WHERE p.id = %s", (item_id,))
                    return rows[0] if rows else None
                def on_loaded(row):
                    if row:
                        preencher_form(row)
                self.run_async(load, on_loaded)

        def carregar_lista():
            """Carrega a lista de produtos (igual ao Android loadData)."""
            busca = et_busca.get().strip()
            def load():
                db = DatabaseHelper.get_instance()
                sql = ("SELECT p.id, p.descricao, p.codigo, p.preco_venda, p.estoque, "
                       "p.codigo_barras, tp.descricao AS tipo_desc, p.ativo "
                       "FROM produtos p LEFT JOIN tipos_produto tp ON p.tipo_produto_id = tp.id "
                       "WHERE p.ativo = 1")
                params = []
                if busca:
                    sql += " AND (p.descricao LIKE %s OR p.codigo LIKE %s OR p.codigo_barras LIKE %s)"
                    params = [f"%{busca}%", f"%{busca}%", f"%{busca}%"]
                sql += " ORDER BY p.descricao"
                return db.execute_query(sql, tuple(params) if params else None)
            def on_loaded(rows):
                # Preservar selecao
                sel = tree.selection()
                sel_id = None
                if sel:
                    try:
                        sel_id = tree.item(sel[0])["values"][0]
                    except Exception:
                        pass
                tree.delete(*tree.get_children())
                new_sel_iid = None
                for row in rows:
                    preco_fmt = FormatUtils.format_money(row.get("preco_venda", 0))
                    estoque_fmt = FormatUtils.format_quantidade(row.get("estoque", 0))
                    tipo_desc = FormatUtils.safe_string(str(row.get("tipo_desc", "") or ""))
                    cod_barras = FormatUtils.safe_string(str(row.get("codigo_barras", "") or ""))
                    ativo_txt = "Sim" if row.get("ativo", 1) else "Nao"
                    iid = tree.insert("", "end", values=(
                        row["id"], row.get("descricao", ""), row.get("codigo", ""),
                        preco_fmt, estoque_fmt, cod_barras, tipo_desc, ativo_txt
                    ))
                    if sel_id is not None and row["id"] == sel_id:
                        new_sel_iid = iid
                if new_sel_iid:
                    tree.selection_set(new_sel_iid)
                    tree.see(new_sel_iid)
            self.run_async(load, on_loaded)

        # ---- Conectar eventos ----
        btn_foto.config(command=_escolher_foto)
        btn_remover_foto.config(command=_remover_foto)
        btn_gerar_ean.config(command=_gerar_ean13_unico)
        btn_salvar.config(command=salvar_produto)
        btn_limpar.config(command=limpar_form)
        btn_inativar.config(command=inativar_produto)
        btn_novo.config(command=limpar_form)
        btn_buscar.config(command=carregar_lista)
        et_busca.bind("<Return>", lambda e: carregar_lista())
        tree.bind("<<TreeviewSelect>>", on_select)

        # ---- Carregar dados iniciais ----
        _carregar_tipos_produto()
        carregar_lista()
        _gerar_proximo_codigo()
        _load_foto_into_label(None)

        # Auto-refresh em tempo real (a cada 10 segundos)
        self._start_auto_refresh("cadastro_produto", carregar_lista, 10000)

    def show_gerenciar_produtos(self):
        """Gerenciar Produtos aponta para o mesmo cadastro (igual ao Android que compartilha a mesma tela)."""
        self.show_cadastro_produto()

    def show_cadastro_cliente(self):
        self._show_cadastro_generico(
            "Cadastro de Clientes", "clientes",
            [
                {"nome": "nome", "label": "Nome", "required": True},
                {"nome": "cpf_cnpj", "label": "CPF/CNPJ"},
                {"nome": "celular", "label": "Celular"},
                {"nome": "endereco", "label": "Endereco"},
                {"nome": "numero", "label": "Numero"},
                {"nome": "bairro", "label": "Bairro"},
                {"nome": "cidade", "label": "Cidade"},
                {"nome": "uf", "label": "UF"},
                {"nome": "cep", "label": "CEP"},
                {"nome": "email", "label": "E-mail"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, nome, cpf_cnpj, celular, endereco, numero, bairro, cidade, uf, cep, email, ativo FROM clientes ORDER BY nome",
            "INSERT INTO clientes (nome, cpf_cnpj, celular, endereco, numero, bairro, cidade, uf, cep, email, ativo) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
            "UPDATE clientes SET nome=%s, cpf_cnpj=%s, celular=%s, endereco=%s, numero=%s, bairro=%s, cidade=%s, uf=%s, cep=%s, email=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_vendedor(self):
        self._show_cadastro_generico(
            "Cadastro de Vendedores", "vendedores",
            [
                {"nome": "nome", "label": "Nome", "required": True},
                {"nome": "celular", "label": "Celular"},
                {"nome": "comissao", "label": "Comissao (%)", "tipo": "float"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, nome, celular, comissao, ativo FROM vendedores ORDER BY nome",
            "INSERT INTO vendedores (nome, celular, comissao, ativo) VALUES (%s,%s,%s,%s)",
            "UPDATE vendedores SET nome=%s, celular=%s, comissao=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_entregador(self):
        self._show_cadastro_generico(
            "Cadastro de Entregadores", "entregadores",
            [
                {"nome": "nome", "label": "Nome", "required": True},
                {"nome": "celular", "label": "Celular"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, nome, celular, ativo FROM entregadores ORDER BY nome",
            "INSERT INTO entregadores (nome, celular, ativo) VALUES (%s,%s,%s)",
            "UPDATE entregadores SET nome=%s, celular=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_garcom(self):
        self._show_cadastro_generico(
            "Cadastro de Garcons", "garcons",
            [
                {"nome": "nome", "label": "Nome", "required": True},
                {"nome": "celular", "label": "Celular"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, nome, celular, ativo FROM garcons ORDER BY nome",
            "INSERT INTO garcons (nome, celular, ativo) VALUES (%s,%s,%s)",
            "UPDATE garcons SET nome=%s, celular=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_tipo_produto(self):
        """Tela de Cadastro de Tipos de Produto (Categorias) - Alinhada com
        CadastroTipoProdutoActivity.java do Android.
        Campos: Descricao + Secao de Adicionais vinculados.
        Funcionalidades: Listar, Novo, Editar, Inativar, Vincular/Desvincular Adicionais.
        """
        if not Session.has_permission(PermissionConstants.TIPOS_PRODUTO_ACESSAR):
            self.show_error("Voce nao tem permissao para acessar Tipos de Produto.")
            return

        self.clear_container()

        # --- Barra superior ---
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Tipos de Produto (Categorias)", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        # --- Conteudo principal ---
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # === FORMULARIO ===
        form_card = CardFrame(content)
        form_card.pack(fill="x", pady=5)

        # Campo Descricao
        tk.Label(form_card, text="Descricao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=0, column=0, sticky="w", padx=5, pady=3)
        et_descricao = StyledEntry(form_card, width=40)
        et_descricao.grid(row=0, column=1, sticky="w", padx=5, pady=3)

        # === SECAO DE ADICIONAIS VINCULADOS ===
        tk.Label(form_card, text="Adicionais vinculados:", bg=COR_CARD, fg="#00BCD4",
                 font=("Segoe UI", 10, "bold")).grid(row=1, column=0, columnspan=2,
                                                       sticky="w", padx=5, pady=(10, 3))

        # Frame para lista de adicionais vinculados
        adicionais_frame = tk.Frame(form_card, bg=COR_CARD)
        adicionais_frame.grid(row=2, column=0, columnspan=2, sticky="ew", padx=5, pady=3)

        # Combobox para selecionar adicional + botao Adicionar
        add_frame = tk.Frame(form_card, bg=COR_CARD)
        add_frame.grid(row=3, column=0, columnspan=2, sticky="ew", padx=5, pady=3)

        tk.Label(add_frame, text="Adicionar adicional:", bg=COR_CARD, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(0, 5))
        combo_adicionais = ttk.Combobox(add_frame, width=35, state="readonly")
        combo_adicionais.pack(side="left", padx=5)
        btn_add_adicional = StyledButton(add_frame, text="+ Adicionar",
                                          color=COR_BOTAO_VERDE, width=12)
        btn_add_adicional.pack(side="left", padx=5)

        # === BOTOES DO FORMULARIO ===
        btn_form = tk.Frame(form_card, bg=COR_CARD)
        btn_form.grid(row=4, column=0, columnspan=2, pady=10)

        # --- Estado interno ---
        selected_id = [None]
        adicionais_selecionados = []  # lista de dicts {"id": int, "descricao": str, "preco": float}
        todos_adicionais_cache = []   # todos os adicionais ativos do banco

        def _carregar_todos_adicionais():
            """Carrega todos os adicionais ativos do banco para o combobox."""
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT id, descricao, preco FROM adicionais WHERE ativo = 1 ORDER BY descricao")
            def on_loaded(rows):
                todos_adicionais_cache.clear()
                for r in rows:
                    todos_adicionais_cache.append({
                        "id": r["id"],
                        "descricao": r["descricao"],
                        "preco": float(r.get("preco", 0) or 0)
                    })
                _atualizar_combo_adicionais()
            self.run_async(load, on_loaded)

        def _atualizar_combo_adicionais():
            """Atualiza o combobox de adicionais removendo os ja vinculados."""
            ids_vinculados = {a["id"] for a in adicionais_selecionados}
            disponiveis = [a for a in todos_adicionais_cache if a["id"] not in ids_vinculados]
            nomes = ["-- Selecione um adicional --"]
            for a in disponiveis:
                nome = a["descricao"]
                if a["preco"] > 0:
                    nome += f" (R$ {FormatUtils.format_money(a['preco'])})"
                nomes.append(nome)
            combo_adicionais["values"] = nomes
            combo_adicionais.current(0)

        def _atualizar_lista_adicionais_ui():
            """Atualiza a exibicao visual dos adicionais vinculados."""
            for w in adicionais_frame.winfo_children():
                w.destroy()

            if not adicionais_selecionados:
                tk.Label(adicionais_frame, text="Nenhum adicional vinculado",
                         bg=COR_CARD, fg=COR_TEXTO2,
                         font=("Segoe UI", 9)).pack(anchor="w", padx=5, pady=2)
                return

            for ad in list(adicionais_selecionados):
                row_f = tk.Frame(adicionais_frame, bg="#1a237e",
                                 highlightthickness=1, highlightbackground="#37474F")
                row_f.pack(fill="x", padx=2, pady=1)

                texto = ad["descricao"]
                if ad["preco"] > 0:
                    texto += f" - R$ {FormatUtils.format_money(ad['preco'])}"
                tk.Label(row_f, text=texto, bg="#1a237e", fg=COR_TEXTO,
                         font=("Segoe UI", 10)).pack(side="left", padx=8, pady=4)

                ad_id_ref = ad["id"]
                btn_rem = tk.Button(row_f, text="X", bg=COR_ERRO, fg=COR_TEXTO,
                                    font=("Segoe UI", 9, "bold"), relief="flat",
                                    cursor="hand2", padx=6, pady=1,
                                    command=lambda aid=ad_id_ref: _remover_adicional(aid))
                btn_rem.pack(side="right", padx=5, pady=2)

        def _adicionar_adicional():
            """Adiciona um adicional a lista de vinculados."""
            idx = combo_adicionais.current()
            if idx <= 0:
                self.show_error("Selecione um adicional para vincular.")
                return
            # Mapear indice do combo para o adicional correto (descontando os ja vinculados)
            ids_vinculados = {a["id"] for a in adicionais_selecionados}
            disponiveis = [a for a in todos_adicionais_cache if a["id"] not in ids_vinculados]
            if idx - 1 < len(disponiveis):
                ad = disponiveis[idx - 1]
                # Verificar duplicata
                if any(s["id"] == ad["id"] for s in adicionais_selecionados):
                    self.show_error("Este adicional ja foi adicionado.")
                    return
                adicionais_selecionados.append(dict(ad))
                _atualizar_lista_adicionais_ui()
                _atualizar_combo_adicionais()

        def _remover_adicional(ad_id):
            """Remove um adicional da lista de vinculados."""
            for i, a in enumerate(adicionais_selecionados):
                if a["id"] == ad_id:
                    adicionais_selecionados.pop(i)
                    break
            _atualizar_lista_adicionais_ui()
            _atualizar_combo_adicionais()

        btn_add_adicional.configure(command=_adicionar_adicional)

        def _carregar_adicionais_do_tipo(tipo_id):
            """Carrega os adicionais vinculados a um tipo de produto especifico."""
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT a.id, a.descricao, a.preco FROM tipo_produto_adicionais tpa "
                    "INNER JOIN adicionais a ON tpa.adicional_id = a.id "
                    "WHERE tpa.tipo_produto_id = %s AND a.ativo = 1 ORDER BY a.descricao",
                    (tipo_id,))
            def on_loaded(rows):
                adicionais_selecionados.clear()
                for r in rows:
                    adicionais_selecionados.append({
                        "id": r["id"],
                        "descricao": r["descricao"],
                        "preco": float(r.get("preco", 0) or 0)
                    })
                _atualizar_lista_adicionais_ui()
                _atualizar_combo_adicionais()
            self.run_async(load, on_loaded)

        def limpar_form():
            selected_id[0] = None
            et_descricao.delete(0, tk.END)
            adicionais_selecionados.clear()
            _atualizar_lista_adicionais_ui()
            _atualizar_combo_adicionais()

        def salvar():
            descricao = et_descricao.get().strip()
            if not descricao:
                self.show_error("Por favor, preencha o campo Descricao antes de salvar.")
                return
            ad_ids = [a["id"] for a in adicionais_selecionados]
            tipo_id_ref = selected_id[0]

            def do_save():
                db = DatabaseHelper.get_instance()
                if tipo_id_ref:
                    # UPDATE
                    db.execute_update(
                        "UPDATE tipos_produto SET descricao=%s WHERE id=%s",
                        (descricao, tipo_id_ref))
                    new_id = tipo_id_ref
                else:
                    # INSERT
                    new_id = db.execute_update(
                        "INSERT INTO tipos_produto (descricao, ativo) VALUES (%s, 1)",
                        (descricao,))
                # Atualizar adicionais vinculados: remove todos e reinsere
                db.execute_update(
                    "DELETE FROM tipo_produto_adicionais WHERE tipo_produto_id = %s",
                    (new_id,))
                for ad_id in ad_ids:
                    db.execute_update(
                        "INSERT INTO tipo_produto_adicionais (tipo_produto_id, adicional_id) VALUES (%s, %s)",
                        (new_id, ad_id))
                return new_id

            def on_saved(result):
                limpar_form()
                carregar_lista()
                self.show_success("Tipo de produto salvo com sucesso!")

            self.run_async(do_save, on_saved)

        def inativar():
            if not selected_id[0]:
                self.show_error("Selecione um tipo de produto para inativar.")
                return
            if not messagebox.askyesno("Confirmar", "Deseja inativar este tipo de produto?"):
                return
            ref_id = selected_id[0]

            def do_inativar():
                db = DatabaseHelper.get_instance()
                db.execute_update(
                    "UPDATE tipos_produto SET ativo = 0 WHERE id = %s", (ref_id,))

            def on_inativado(_):
                limpar_form()
                carregar_lista()
                self.show_success("Tipo de produto inativado com sucesso!")

            self.run_async(do_inativar, on_inativado)

        StyledButton(btn_form, text="✔ Salvar", command=salvar,
                     color=COR_BOTAO_VERDE, width=10).pack(side="left", padx=5)
        StyledButton(btn_form, text="✚ Novo", command=limpar_form,
                     color=COR_BOTAO_AZUL, width=10).pack(side="left", padx=5)
        StyledButton(btn_form, text="Inativar", command=inativar,
                     color=COR_ERRO, width=10).pack(side="left", padx=5)

        # === LISTA (TREEVIEW) ===
        list_frame = tk.Frame(content, bg=COR_FUNDO)
        list_frame.pack(fill="both", expand=True, pady=5)

        cols = ("c0", "c1", "c2")
        tree = ttk.Treeview(list_frame, columns=cols, show="headings", height=15)
        tree.heading("c0", text="ID")
        tree.column("c0", width=50, anchor="center")
        tree.heading("c1", text="Descricao")
        tree.column("c1", width=250)
        tree.heading("c2", text="Adicionais")
        tree.column("c2", width=400)

        vsb = ttk.Scrollbar(list_frame, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=vsb.set)
        tree.pack(side="left", fill="both", expand=True)
        vsb.pack(side="right", fill="y")

        def on_select(event):
            sel = tree.selection()
            if sel:
                values = tree.item(sel[0])["values"]
                selected_id[0] = values[0]
                et_descricao.delete(0, tk.END)
                et_descricao.insert(0, str(values[1]) if values[1] else "")
                # Carregar adicionais vinculados a este tipo
                _carregar_adicionais_do_tipo(values[0])

        tree.bind("<<TreeviewSelect>>", on_select)

        def _carregar_adicionais_str(conn_db, tipo_id):
            """Retorna string descritiva dos adicionais vinculados a um tipo."""
            rows = conn_db.execute_query(
                "SELECT a.descricao, a.preco FROM tipo_produto_adicionais tpa "
                "INNER JOIN adicionais a ON tpa.adicional_id = a.id "
                "WHERE tpa.tipo_produto_id = %s AND a.ativo = 1 ORDER BY a.descricao",
                (tipo_id,))
            if not rows:
                return "Sem adicionais"
            partes = []
            for r in rows:
                nome = r["descricao"]
                preco = float(r.get("preco", 0) or 0)
                if preco > 0:
                    nome += f" (R$ {FormatUtils.format_money(preco)})"
                partes.append(nome)
            return "Adicionais: " + ", ".join(partes)

        def carregar_lista():
            def load():
                db = DatabaseHelper.get_instance()
                tipos = db.execute_query(
                    "SELECT id, descricao FROM tipos_produto WHERE ativo = 1 ORDER BY id DESC")
                result = []
                for t in tipos:
                    ad_str = _carregar_adicionais_str(db, t["id"])
                    result.append((t["id"], t["descricao"], ad_str))
                return result

            def on_loaded(rows):
                # Preservar selecao
                sel = tree.selection()
                sel_id = None
                if sel:
                    try:
                        sel_id = tree.item(sel[0])["values"][0]
                    except Exception:
                        pass
                tree.delete(*tree.get_children())
                new_sel_iid = None
                for r in rows:
                    iid = tree.insert("", "end", values=r)
                    if sel_id is not None and r[0] == sel_id:
                        new_sel_iid = iid
                if new_sel_iid:
                    tree.selection_set(new_sel_iid)
                    tree.see(new_sel_iid)

            self.run_async(load, on_loaded)

        # --- Inicializacao ---
        _carregar_todos_adicionais()
        carregar_lista()

        # Auto-refresh em tempo real (a cada 10 segundos)
        self._start_auto_refresh("cadastro_tipo_produto", carregar_lista, 10000)

    def show_cadastro_adicional(self):
        self._show_cadastro_generico(
            "Cadastro de Adicionais", "adicionais",
            [
                {"nome": "descricao", "label": "Descricao", "required": True},
                {"nome": "preco", "label": "Preco", "tipo": "money"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, descricao, preco, ativo FROM adicionais ORDER BY descricao",
            "INSERT INTO adicionais (descricao, preco, ativo) VALUES (%s,%s,%s)",
            "UPDATE adicionais SET descricao=%s, preco=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_forma_pagamento(self):
        self._show_cadastro_generico(
            "Formas de Pagamento", "formas_pagamento",
            [
                {"nome": "descricao", "label": "Descricao", "required": True},
                {"nome": "tipo", "label": "Tipo", "tipo": "combo",
                 "values": ["dinheiro", "credito", "debito", "pix", "conta_receber", "outro"]},
                {"nome": "permite_parcelamento", "label": "Permite Parcelamento", "tipo": "check"},
                {"nome": "exige_cliente", "label": "Exige Cliente", "tipo": "check"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, descricao, tipo, permite_parcelamento, exige_cliente, ativo FROM formas_pagamento ORDER BY descricao",
            "INSERT INTO formas_pagamento (descricao, tipo, permite_parcelamento, exige_cliente, ativo) VALUES (%s,%s,%s,%s,%s)",
            "UPDATE formas_pagamento SET descricao=%s, tipo=%s, permite_parcelamento=%s, exige_cliente=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_obs_cupom(self):
        self._show_cadastro_generico(
            "Observacoes do Cupom", "observacoes_cupom",
            [
                {"nome": "texto", "label": "Texto", "required": True},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, texto, ativo FROM observacoes_cupom ORDER BY id",
            "INSERT INTO observacoes_cupom (texto, ativo) VALUES (%s,%s)",
            "UPDATE observacoes_cupom SET texto=%s, ativo=%s WHERE id=%s"
        )

    def show_cadastro_usuario(self):
        # Niveis alinhados com o Android
        self._show_cadastro_generico(
            "Cadastro de Usuarios", "usuarios",
            [
                {"nome": "nome", "label": "Nome", "required": True},
                {"nome": "login", "label": "Login", "required": True},
                {"nome": "senha", "label": "Senha", "required": True},
                {"nome": "nivel", "label": "Nivel", "tipo": "combo",
                 "values": ["admin", "gerente", "operador", "caixa", "atendente",
                            "garcom", "balcao", "vendedor", "estoquista", "entregador"]},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, nome, login, senha, nivel, ativo FROM usuarios ORDER BY nome",
            "INSERT INTO usuarios (nome, login, senha, nivel, ativo) VALUES (%s,%s,%s,%s,%s)",
            "UPDATE usuarios SET nome=%s, login=%s, senha=%s, nivel=%s, ativo=%s WHERE id=%s",
            on_after_save=self._vincular_perfil_ao_usuario
        )

    def show_cadastro_empresa(self):
        self._show_cadastro_generico(
            "Dados da Empresa", "empresa",
            [
                {"nome": "razao_social", "label": "Razao Social", "required": True},
                {"nome": "nome_fantasia", "label": "Nome Fantasia"},
                {"nome": "cnpj", "label": "CNPJ"},
                {"nome": "ie", "label": "IE"},
                {"nome": "endereco", "label": "Endereco"},
                {"nome": "numero", "label": "Numero"},
                {"nome": "bairro", "label": "Bairro"},
                {"nome": "cidade", "label": "Cidade"},
                {"nome": "uf", "label": "UF"},
                {"nome": "cep", "label": "CEP"},
                {"nome": "telefone", "label": "Telefone"},
                {"nome": "email", "label": "E-mail"},
            ],
            "SELECT id, razao_social, nome_fantasia, cnpj, ie, endereco, numero, bairro, cidade, uf, cep, telefone, email FROM empresa ORDER BY id",
            "INSERT INTO empresa (razao_social, nome_fantasia, cnpj, ie, endereco, numero, bairro, cidade, uf, cep, telefone, email) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
            "UPDATE empresa SET razao_social=%s, nome_fantasia=%s, cnpj=%s, ie=%s, endereco=%s, numero=%s, bairro=%s, cidade=%s, uf=%s, cep=%s, telefone=%s, email=%s WHERE id=%s"
        )

    def show_taxa_entrega(self):
        self._show_cadastro_generico(
            "Taxa de Entrega por Bairro", "taxa_entrega_bairro",
            [
                {"nome": "bairro", "label": "Bairro", "required": True},
                {"nome": "taxa", "label": "Taxa (R$)", "tipo": "money"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, bairro, taxa, ativo FROM taxa_entrega_bairro ORDER BY bairro",
            "INSERT INTO taxa_entrega_bairro (bairro, taxa, ativo) VALUES (%s,%s,%s)",
            "UPDATE taxa_entrega_bairro SET bairro=%s, taxa=%s, ativo=%s WHERE id=%s"
        )

    # ========================================================================
    # COMANDAS
    # ========================================================================
    def show_comandas(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="☰ Comandas", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)

        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")

        btn_nova_comanda = StyledButton(top, text="✚ Nova Comanda", command=self.nova_comanda,
                     color=COR_BOTAO_VERDE, width=15)
        btn_nova_comanda.pack(side="right", padx=10, pady=5)
        add_tooltip(btn_nova_comanda, "Criar uma nova comanda")

        btn_assistente_comandas = StyledButton(top, text="Assistente", command=self.assistente_criar_comandas,
                     color=COR_BOTAO_AZUL, width=12)
        btn_assistente_comandas.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_assistente_comandas, "Criar varias comandas em sequencia")

        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "numero", "status", "total", "data")
        self.tree_comandas = ttk.Treeview(content, columns=cols, show="headings", height=18)
        self.tree_comandas.heading("id", text="ID")
        self.tree_comandas.heading("numero", text="Numero")
        self.tree_comandas.heading("status", text="Status")
        self.tree_comandas.heading("total", text="Total")
        self.tree_comandas.heading("data", text="Data Abertura")
        self.tree_comandas.column("id", width=50, anchor="center")
        self.tree_comandas.column("numero", width=80, anchor="center")
        self.tree_comandas.column("status", width=100, anchor="center")
        self.tree_comandas.column("total", width=100, anchor="e")
        self.tree_comandas.column("data", width=150)
        self.tree_comandas.pack(fill="both", expand=True)
        self.tree_comandas.bind("<Double-1>", lambda e: self.abrir_comanda_detalhe())

        btn_frame = tk.Frame(content, bg=COR_FUNDO)
        btn_frame.pack(fill="x", pady=5)
        StyledButton(btn_frame, text="Abrir Detalhe", command=self.abrir_comanda_detalhe,
                     color=COR_BOTAO_AZUL, width=15).pack(side="left", padx=5)
        StyledButton(btn_frame, text="\u2714 Fechar Comanda", command=self.fechar_comanda,
                     color=COR_ERRO, width=15).pack(side="left", padx=5)

        self.carregar_comandas()

        # Auto-refresh em tempo real (a cada 8 segundos)
        self._start_auto_refresh("comandas", self.carregar_comandas, 8000)

    def carregar_comandas(self):
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT id, numero, status, total_itens, data_abertura FROM comandas ORDER BY id DESC"
            )
        def on_loaded(rows):
            # Preservar selecao
            sel = self.tree_comandas.selection()
            sel_id = None
            if sel:
                try:
                    sel_id = self.tree_comandas.item(sel[0])["values"][0]
                except Exception:
                    pass
            self.tree_comandas.delete(*self.tree_comandas.get_children())
            new_sel_iid = None
            for r in rows:
                iid = self.tree_comandas.insert("", "end", values=(
                    r["id"], r["numero"], r["status"],
                    f"R$ {FormatUtils.format_money(r['total_itens'])}",
                    FormatUtils.format_date(r["data_abertura"])
                ))
                if sel_id is not None and r["id"] == sel_id:
                    new_sel_iid = iid
            if new_sel_iid:
                self.tree_comandas.selection_set(new_sel_iid)
                self.tree_comandas.see(new_sel_iid)
        self.run_async(load, on_loaded)

    def _solicitar_intervalo_criacao_lote(self, titulo, item_plural):
        quantidade = simpledialog.askinteger(
            titulo,
            f"Quantas {item_plural} deseja adicionar?",
            minvalue=1,
            parent=self.root
        )
        if quantidade is None:
            return None, None, None

        numero_inicial = simpledialog.askinteger(
            titulo,
            f"A partir de qual numero deseja adicionar as {item_plural}?",
            minvalue=1,
            parent=self.root
        )
        if numero_inicial is None:
            return None, None, None

        numero_final = numero_inicial + quantidade - 1
        confirmar = messagebox.askyesno(
            titulo,
            f"Deseja adicionar {quantidade} {item_plural} do numero {numero_inicial} ao numero {numero_final}?",
            parent=self.root
        )
        if not confirmar:
            return None, None, None

        return quantidade, numero_inicial, numero_final

    def _formatar_resumo_numeros(self, numeros, limite=15):
        if not numeros:
            return ""
        if len(numeros) <= limite:
            return ", ".join(str(n) for n in numeros)
        return ", ".join(str(n) for n in numeros[:limite]) + f" ... (+{len(numeros) - limite})"

    def _executar_assistente_criacao_lote(self, titulo, item_singular, item_plural,
                                          criar_callback, refresh_callback=None):
        quantidade, numero_inicial, numero_final = self._solicitar_intervalo_criacao_lote(
            titulo, item_plural
        )
        if quantidade is None:
            return

        def criar_lote():
            return criar_callback(numero_inicial, numero_final)

        def on_criado(resultado):
            resultado = resultado or {}
            criados = resultado.get("criados", []) or []
            ignorados = resultado.get("ignorados", []) or []

            if refresh_callback:
                try:
                    refresh_callback()
                except Exception:
                    pass

            total_criados = len(criados)
            linhas = [
                f"{total_criados} {item_plural if total_criados != 1 else item_singular} criada(s) com sucesso."
            ]
            if criados:
                if len(criados) == 1:
                    linhas.append(f"Numero criado: {criados[0]}")
                else:
                    linhas.append(f"Intervalo criado: {criados[0]} ao {criados[-1]}")
            if ignorados:
                linhas.append(
                    "Ja existentes/ignorados: " + self._formatar_resumo_numeros(ignorados)
                )
            self.show_success("\n".join(linhas))

        self.run_async(criar_lote, on_criado)

    def assistente_criar_comandas(self, *_):
        def criar_comandas(numero_inicial, numero_final):
            db = DatabaseHelper.get_instance()
            criados = []
            for numero in range(numero_inicial, numero_final + 1):
                db.execute_update(
                    "INSERT INTO comandas (numero, status) VALUES (%s, 'aberta')",
                    (numero,)
                )
                criados.append(numero)
            return {"criados": criados, "ignorados": []}

        self._executar_assistente_criacao_lote(
            "Assistente de Criacao de Comandas",
            "comanda",
            "comandas",
            criar_comandas,
            refresh_callback=self.carregar_comandas
        )

    def assistente_criar_mesas(self, *_):
        def criar_mesas(numero_inicial, numero_final):
            db = DatabaseHelper.get_instance()
            existentes = db.execute_query(
                "SELECT numero FROM mesas WHERE numero BETWEEN %s AND %s",
                (numero_inicial, numero_final)
            )
            numeros_existentes = {int(r["numero"]) for r in (existentes or [])}
            criados = []
            ignorados = []
            for numero in range(numero_inicial, numero_final + 1):
                if numero in numeros_existentes:
                    ignorados.append(numero)
                    continue
                db.execute_update(
                    "INSERT INTO mesas (numero, ativa) VALUES (%s, 1)",
                    (numero,)
                )
                criados.append(numero)
            return {"criados": criados, "ignorados": ignorados}

        self._executar_assistente_criacao_lote(
            "Assistente de Criacao de Mesas",
            "mesa",
            "mesas",
            criar_mesas,
            refresh_callback=lambda: (
                self.carregar_mesas() if hasattr(self, "mesas_frame") and self.mesas_frame.winfo_exists()
                else self.show_cadastro_mesa()
            )
        )

    def assistente_criar_armarios(self, *_):
        def criar_armarios(numero_inicial, numero_final):
            db = DatabaseHelper.get_instance()
            existentes = db.execute_query(
                "SELECT numero FROM armarios_sauna WHERE numero BETWEEN %s AND %s",
                (numero_inicial, numero_final)
            )
            numeros_existentes = {int(r["numero"]) for r in (existentes or [])}
            criados = []
            ignorados = []
            for numero in range(numero_inicial, numero_final + 1):
                if numero in numeros_existentes:
                    ignorados.append(numero)
                    continue
                db.execute_update(
                    "INSERT INTO armarios_sauna (numero, descricao, localizacao, ativo) VALUES (%s, NULL, NULL, 1)",
                    (numero,)
                )
                criados.append(numero)
            return {"criados": criados, "ignorados": ignorados}

        self._executar_assistente_criacao_lote(
            "Assistente de Criacao de Armarios",
            "armario",
            "armarios",
            criar_armarios,
            refresh_callback=lambda: (
                self.carregar_armarios() if hasattr(self, "armarios_frame") and self.armarios_frame.winfo_exists()
                else self.show_cadastro_armario_sauna()
            )
        )

    def nova_comanda(self):
        num = simpledialog.askinteger("Nova Comanda", "Numero da comanda:",
                                       minvalue=1, parent=self.root)
        if num is None:
            return
        def criar():
            db = DatabaseHelper.get_instance()
            return db.execute_update(
                "INSERT INTO comandas (numero, status) VALUES (%s, 'aberta')", (num,)
            )
        def on_criada(cid):
            self.show_success(f"Comanda #{num} criada!")
            self.carregar_comandas()
        self.run_async(criar, on_criada)

    def abrir_comanda_detalhe(self):
        sel = self.tree_comandas.selection()
        if not sel:
            self.show_error("Por favor, selecione uma comanda na lista.\n\nClique sobre a comanda desejada e tente novamente.")
            return
        comanda_id = self.tree_comandas.item(sel[0])["values"][0]
        self._show_comanda_detalhe(comanda_id)

    def _show_comanda_detalhe(self, comanda_id):
        dialog = tk.Toplevel(self.root)
        dialog.title(f"Comanda #{comanda_id}")
        dialog.geometry("700x500")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text=f"Comanda #{comanda_id}", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=10)

        # Itens da comanda
        cols = ("id", "produto", "qtd", "preco", "total")
        tree = ttk.Treeview(dialog, columns=cols, show="headings", height=12)
        tree.heading("id", text="ID")
        tree.heading("produto", text="Produto")
        tree.heading("qtd", text="Qtd")
        tree.heading("preco", text="Preco")
        tree.heading("total", text="Total")
        tree.column("id", width=50, anchor="center")
        tree.column("produto", width=250)
        tree.column("qtd", width=60, anchor="center")
        tree.column("preco", width=80, anchor="e")
        tree.column("total", width=80, anchor="e")
        tree.pack(fill="both", expand=True, padx=15, pady=5)

        lbl_total = tk.Label(dialog, text="Total: R$ 0,00", bg=COR_FUNDO, fg=COR_SUCESSO,
                              font=("Segoe UI", 14, "bold"))
        lbl_total.pack(pady=5)

        def carregar_itens():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT id, descricao_produto, quantidade, preco_unitario, total "
                    "FROM itens_comanda WHERE comanda_id = %s ORDER BY id", (comanda_id,)
                )
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                total = 0
                for r in rows:
                    tree.insert("", "end", values=(
                        r["id"], r["descricao_produto"], r["quantidade"],
                        FormatUtils.format_money(r["preco_unitario"]),
                        FormatUtils.format_money(r["total"])
                    ))
                    total += float(r["total"])
                lbl_total.config(text=f"Total: R$ {FormatUtils.format_money(total)}")
            self.run_async(load, on_loaded)

        def add_item():
            self._selecionar_registro(
                "produtos", "Adicionar Produto",
                "SELECT id, descricao, preco_venda FROM produtos WHERE ativo = 1 ORDER BY descricao",
                ["ID", "Descricao", "Preco"],
                lambda row: self._add_item_comanda(comanda_id, row, carregar_itens) if row else None
            )

        def remover_item():
            sel_item = tree.selection()
            if not sel_item:
                messagebox.showwarning(
                    "Selecione um Item",
                    "Nenhum item foi selecionado.\n\n"
                    "Clique sobre o item desejado na lista\n"
                    "e tente novamente.",
                    parent=dialog)
                return
            item_id = tree.item(sel_item[0])["values"][0]
            def do_remove():
                db = DatabaseHelper.get_instance()
                db.execute_update("DELETE FROM itens_comanda WHERE id = %s", (item_id,))
                self._atualizar_total_comanda(comanda_id)
            self.run_async(do_remove, lambda _: carregar_itens())

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=15, pady=10)
        StyledButton(btn_frame, text="Adicionar Item", command=add_item,
                     color=COR_BOTAO_VERDE, width=15).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Remover Item", command=remover_item,
                     color=COR_ERRO, width=15).pack(side="left", padx=5)

        carregar_itens()

    def _add_item_comanda(self, comanda_id, row, refresh_callback):
        produto_id = row[0]
        descricao = row[1]
        preco = float(row[2])
        qtd = simpledialog.askfloat("Quantidade", f"Quantidade de {descricao}:",
                                     initialvalue=1, minvalue=0.001, parent=self.root)
        if not qtd:
            return
        total = qtd * preco
        def salvar():
            db = DatabaseHelper.get_instance()
            db.execute_update(
                "INSERT INTO itens_comanda (comanda_id, produto_id, descricao_produto, "
                "quantidade, preco_unitario, total) VALUES (%s,%s,%s,%s,%s,%s)",
                (comanda_id, produto_id, descricao, qtd, preco, total)
            )
            self._atualizar_total_comanda(comanda_id)
        self.run_async(salvar, lambda _: refresh_callback())

    def _atualizar_total_comanda(self, comanda_id):
        db = DatabaseHelper.get_instance()
        total = db.execute_scalar(
            "SELECT COALESCE(SUM(total),0) FROM itens_comanda WHERE comanda_id = %s",
            (comanda_id,)
        )
        db.execute_update(
            "UPDATE comandas SET total_itens = %s WHERE id = %s", (total, comanda_id)
        )

    def fechar_comanda(self):
        sel = self.tree_comandas.selection()
        if not sel:
            self.show_error("Por favor, selecione uma comanda na lista.\n\nClique sobre a comanda desejada e tente novamente.")
            return
        comanda_id = self.tree_comandas.item(sel[0])["values"][0]
        status = self.tree_comandas.item(sel[0])["values"][2]
        if status != "aberta":
            self.show_error("Esta comanda ja esta fechada.")
            return
        if not messagebox.askyesno("Confirmar", f"Fechar comanda #{comanda_id}?"):
            return
        def fechar():
            db = DatabaseHelper.get_instance()
            db.execute_update(
                "UPDATE comandas SET status = 'fechada', data_fechamento = NOW() WHERE id = %s",
                (comanda_id,)
            )
        self.run_async(fechar, lambda _: self.carregar_comandas())

    # ========================================================================
    # HISTORICO DE VENDAS
    # ========================================================================
    def show_historico(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text=f"{Icons.HISTORICO} Historico de Vendas",
                 bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # --- Filtros ---
        filtro_card = tk.Frame(content, bg=COR_GLASS, highlightthickness=1,
                               highlightbackground=COR_GLASS_BORDER)
        filtro_card.pack(fill="x", pady=(0, 6))

        tk.Label(filtro_card, text="Buscar:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(10, 4), pady=6)
        et_busca = StyledEntry(filtro_card, width=20)
        et_busca.pack(side="left", padx=4, pady=6)
        add_tooltip(et_busca, "Buscar por ID, cliente ou status")

        tk.Label(filtro_card, text="Data Inicio:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(8, 4))
        # "Hoje" deve usar a data do SERVIDOR MySQL, a mesma fonte usada na tela
        # "Total de Vendas Hoje" (que filtra por CURDATE()) e no proprio
        # data_venda (DEFAULT CURRENT_TIMESTAMP). Se usarmos a data local do
        # terminal (datetime.date.today()) e o relogio/fuso do terminal estiver
        # diferente do servidor, uma venda feita perto da virada do dia aparece
        # no total do dia mas nao no historico (ou vice-versa).
        try:
            _hoje_dt = LicencaManager._obter_hora_mysql() or datetime.date.today()
        except Exception:
            _hoje_dt = datetime.date.today()
        _hoje_str = _hoje_dt.strftime("%d/%m/%Y")
        et_data_ini = StyledEntry(filtro_card, width=11)
        et_data_ini.insert(0, _hoje_str)
        et_data_ini.pack(side="left", padx=4)
        add_tooltip(et_data_ini, "Data inicial no formato DD/MM/AAAA")

        tk.Label(filtro_card, text="Fim:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(8, 4))
        et_data_fim = StyledEntry(filtro_card, width=11)
        et_data_fim.insert(0, _hoje_str)
        et_data_fim.pack(side="left", padx=4)

        tk.Label(filtro_card, text="Status:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(8, 4))
        combo_status = ttk.Combobox(filtro_card,
                                     values=["Todos", "finalizada", "cancelada"],
                                     width=10, state="readonly",
                                     font=("Segoe UI", 9))
        combo_status.current(0)
        combo_status.pack(side="left", padx=4)

        # Filtro por Forma de Pagamento
        tk.Label(filtro_card, text="Forma:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(8, 4))
        formas_pgto_map = {"Todas": None}
        try:
            _fp_rows = DatabaseHelper.get_instance().execute_query(
                "SELECT id, descricao FROM formas_pagamento ORDER BY descricao")
            for _fp in (_fp_rows or []):
                formas_pgto_map[_fp.get("descricao") or "N/A"] = _fp.get("id")
        except Exception:
            pass
        combo_forma = ttk.Combobox(filtro_card, values=list(formas_pgto_map.keys()),
                                    width=14, state="readonly", font=("Segoe UI", 9))
        combo_forma.current(0)
        combo_forma.pack(side="left", padx=4)
        add_tooltip(combo_forma, "Filtrar vendas por forma de pagamento")

        # Label de resumo (Total dinamico conforme os filtros)
        lbl_resumo = tk.Label(filtro_card, text="", bg=COR_GLASS, fg=COR_PRIMARIA,
                               font=("Segoe UI", 11, "bold"))
        lbl_resumo.pack(side="right", padx=12)

        # --- Treeview ---
        cols = ("id", "cliente", "total", "data", "status", "pagamento")
        self.tree_historico = ttk.Treeview(content, columns=cols,
                                           show="headings", height=16)
        self.tree_historico.heading("id", text="ID",
            command=lambda: self._sort_tree(self.tree_historico, "id", False))
        self.tree_historico.heading("cliente", text="Cliente",
            command=lambda: self._sort_tree(self.tree_historico, "cliente", False))
        self.tree_historico.heading("total", text="Total",
            command=lambda: self._sort_tree(self.tree_historico, "total", False))
        self.tree_historico.heading("data", text="Data",
            command=lambda: self._sort_tree(self.tree_historico, "data", True))
        self.tree_historico.heading("status", text="Status",
            command=lambda: self._sort_tree(self.tree_historico, "status", False))
        self.tree_historico.heading("pagamento", text="Pagamento")
        self.tree_historico.column("id", width=55, anchor="center")
        self.tree_historico.column("cliente", width=180)
        self.tree_historico.column("total", width=110, anchor="e")
        self.tree_historico.column("data", width=140)
        self.tree_historico.column("status", width=100, anchor="center")
        self.tree_historico.column("pagamento", width=160)

        vsb = ttk.Scrollbar(content, orient="vertical",
                             command=self.tree_historico.yview)
        self.tree_historico.configure(yscrollcommand=vsb.set)

        tree_frame = tk.Frame(content, bg=COR_FUNDO)
        tree_frame.pack(fill="both", expand=True)
        vsb2 = ttk.Scrollbar(tree_frame, orient="vertical",
                              command=self.tree_historico.yview)
        self.tree_historico.configure(yscrollcommand=vsb2.set)
        self.tree_historico.pack(side="left", fill="both", expand=True)
        vsb2.pack(side="right", fill="y")

        def _on_historico_mousewheel(event):
            try:
                if not self.tree_historico.winfo_exists():
                    return "break"
                if getattr(event, "num", None) == 4:
                    self.tree_historico.yview_scroll(-3, "units")
                elif getattr(event, "num", None) == 5:
                    self.tree_historico.yview_scroll(3, "units")
                else:
                    delta = getattr(event, "delta", 0)
                    if delta:
                        self.tree_historico.yview_scroll(int(-1 * (delta / 120)), "units")
                return "break"
            except Exception:
                return "break"

        def _bind_historico_mousewheel(event=None):
            try:
                if self.tree_historico.winfo_exists():
                    self.tree_historico.bind_all("<MouseWheel>", _on_historico_mousewheel)
                    self.tree_historico.bind_all("<Button-4>", _on_historico_mousewheel)
                    self.tree_historico.bind_all("<Button-5>", _on_historico_mousewheel)
            except Exception:
                pass

        def _unbind_historico_mousewheel(event=None):
            try:
                if hasattr(self, "tree_historico") and self.tree_historico.winfo_exists():
                    self.tree_historico.unbind_all("<MouseWheel>")
                    self.tree_historico.unbind_all("<Button-4>")
                    self.tree_historico.unbind_all("<Button-5>")
            except Exception:
                pass

        self.tree_historico.bind("<Enter>", _bind_historico_mousewheel)
        self.tree_historico.bind("<Leave>", _unbind_historico_mousewheel)

        btn_frame = tk.Frame(content, bg=COR_FUNDO)
        btn_frame.pack(fill="x", pady=5)

        StyledButton(btn_frame, text=f"{Icons.PRINT} Ver Cupom",
                     command=self.ver_cupom_historico,
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1], "Ver cupom da venda selecionada")

        StyledButton(btn_frame, text=f"{Icons.EDIT} Reabrir/Editar",
                     command=self.reabrir_venda_historico,
                     color=COR_BOTAO_LARANJA, width=15).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Reabrir a venda para editar itens/pagamento/cliente/entrega e "
                    "regravar no MESMO numero")

        StyledButton(btn_frame, text=f"{Icons.CROSS} Cancelar Venda",
                     command=self.cancelar_venda_historico,
                     color=COR_ERRO, width=15).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1], "Cancelar venda e devolver estoque")

        StyledButton(btn_frame, text=f"{Icons.EXPORT} Exportar CSV",
                     command=lambda: DataExporter.export_treeview_csv(
                         self.tree_historico, "historico_vendas", parent=self.root),
                     color=COR_BOTAO_VERDE, width=14).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1], "Exportar historico para CSV")

        StyledButton(btn_frame, text=f"{Icons.REFRESH} Atualizar",
                     command=lambda: _buscar(),
                     color=COR_FUNDO3, width=10).pack(side="left", padx=5)

        _cache_rows = [None]

        def _buscar(event=None):
            busca = et_busca.get().strip().lower()
            status_f = combo_status.get()
            forma_id_f = formas_pgto_map.get(combo_forma.get())
            data_ini_str = et_data_ini.get().strip()
            data_fim_str = et_data_fim.get().strip()

            def _parse_date(s):
                try:
                    return datetime.datetime.strptime(s, "%d/%m/%Y").strftime("%Y-%m-%d")
                except Exception:
                    return None

            d_ini = _parse_date(data_ini_str)
            d_fim = _parse_date(data_fim_str)

            def load():
                db = DatabaseHelper.get_instance()
                params = []
                # Quando uma forma de pagamento e escolhida, tambem trazemos o
                # valor pago NESSA forma por venda (valor_forma), para que o
                # "Total" do rodape reflita o dinheiro daquela forma e nao o
                # total cheio da venda (importante em pagamentos mistos).
                if forma_id_f is not None:
                    valor_forma_col = (
                        "(SELECT COALESCE(SUM(pv3.valor),0) FROM pagamentos_venda pv3 "
                        "WHERE pv3.venda_id = v.id AND pv3.forma_pagamento_id = %s) as valor_forma"
                    )
                    params.append(forma_id_f)
                else:
                    valor_forma_col = "NULL as valor_forma"
                sql = (
                    "SELECT v.id, COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cliente_nome, "
                    "v.total_liquido, v.data_venda, v.status, "
                    "GROUP_CONCAT(fp.descricao SEPARATOR ' + ') as formas, "
                    + valor_forma_col + " "
                    "FROM vendas v "
                    "LEFT JOIN clientes c ON v.cliente_id = c.id "
                    "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                    "LEFT JOIN pagamentos_venda pv ON pv.venda_id = v.id "
                    "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                    "WHERE 1=1"
                )
                if status_f != "Todos":
                    sql += " AND v.status = %s"
                    params.append(status_f)
                if forma_id_f is not None:
                    # Vendas que possuem um pagamento nesta forma. EXISTS preserva
                    # o GROUP_CONCAT (a coluna Pagamento continua mostrando todas
                    # as formas da venda, mesmo em pagamentos mistos).
                    sql += (" AND EXISTS (SELECT 1 FROM pagamentos_venda pv2 "
                            "WHERE pv2.venda_id = v.id AND pv2.forma_pagamento_id = %s)")
                    params.append(forma_id_f)
                if d_ini:
                    sql += " AND DATE(v.data_venda) >= %s"
                    params.append(d_ini)
                if d_fim:
                    sql += " AND DATE(v.data_venda) <= %s"
                    params.append(d_fim)
                sql += " GROUP BY v.id ORDER BY v.id DESC LIMIT 500"
                return db.execute_query(sql, params if params else None)

            def on_loaded(rows):
                _cache_rows[0] = rows
                _preencher(rows, busca)

            self.run_async(load, on_loaded)

        def _preencher(rows, filtro=""):
            # Preservar selecao
            sel = self.tree_historico.selection()
            sel_id = None
            if sel:
                try:
                    sel_id = self.tree_historico.item(sel[0])["values"][0]
                except Exception:
                    pass
            self.tree_historico.delete(*self.tree_historico.get_children())
            total_valor = 0.0
            count = 0
            new_sel_iid = None
            for i, r in enumerate(rows):
                cliente = r.get("cliente_nome") or "N/A"
                status = r.get("status", "")
                total_v = float(r.get("total_liquido", 0))
                data_s = FormatUtils.format_date(r["data_venda"])
                formas = r.get("formas") or ""

                if filtro:
                    campo_busca = f"{r['id']} {cliente} {status}".lower()
                    if filtro not in campo_busca:
                        continue

                tag = "even" if i % 2 == 0 else "odd"
                if status == "cancelada":
                    tag = "cancelada"
                iid = self.tree_historico.insert("", "end", values=(
                    r["id"], cliente,
                    f"R$ {FormatUtils.format_money(total_v)}",
                    data_s, status, formas
                ), tags=(tag,))
                if sel_id is not None and r["id"] == sel_id:
                    new_sel_iid = iid
                if status == "finalizada":
                    # Se ha uma forma de pagamento filtrada, soma o valor pago
                    # naquela forma; caso contrario, soma o total liquido da venda.
                    vf = r.get("valor_forma")
                    total_valor += float(vf) if vf is not None else total_v
                    count += 1
            if new_sel_iid:
                self.tree_historico.selection_set(new_sel_iid)
                self.tree_historico.see(new_sel_iid)

            self.tree_historico.tag_configure("even", background=COR_GLASS)
            self.tree_historico.tag_configure("odd", background=COR_CARD)
            self.tree_historico.tag_configure(
                "cancelada", background="#1a0a0a", foreground="#ff6666")

            _forma_sel = combo_forma.get()
            if _forma_sel and _forma_sel != "Todas":
                _rotulo = f"Total ({_forma_sel})"
            else:
                _rotulo = "Total"
            lbl_resumo.config(
                text=f"{count} vendas  |  {_rotulo}: R$ {FormatUtils.format_money(total_valor)}")

        et_busca.bind("<KeyRelease>",
                      lambda e: _preencher(_cache_rows[0] or [],
                                           et_busca.get().strip().lower()))

        # Carregar ao abrir (periodo de hoje)
        _buscar()

        # Aplicar filtro automaticamente ao trocar Status ou Forma de Pagamento
        combo_status.bind("<<ComboboxSelected>>", lambda e: _buscar())
        combo_forma.bind("<<ComboboxSelected>>", lambda e: _buscar())

        # Botao buscar
        StyledButton(filtro_card, text=f"{Icons.SEARCH} Buscar",
                     command=_buscar,
                     color=COR_BOTAO_AZUL, width=8).pack(side="left", padx=6, pady=6)

        # Auto-refresh em tempo real (a cada 15 segundos)
        self._start_auto_refresh("historico", _buscar, 15000)



    def ver_cupom_historico(self):
        sel = self.tree_historico.selection()
        if not sel:
            self.show_error("Selecione uma venda.")
            return
        venda_id = self.tree_historico.item(sel[0])["values"][0]

        def load():
            db = DatabaseHelper.get_instance()
            venda_rows = db.execute_query("SELECT * FROM vendas WHERE id = %s", (venda_id,))
            if not venda_rows:
                return None
            vr = venda_rows[0]
            venda = Venda()
            venda.id = vr["id"]
            venda.total_bruto = float(vr.get("total_bruto", 0))
            venda.desconto_valor = float(vr.get("desconto_valor", 0))
            venda.acrescimo_valor = float(vr.get("acrescimo_valor", 0))
            venda.total_liquido = float(vr.get("total_liquido", 0))
            venda.valor_recebido = float(vr.get("valor_recebido", 0))
            venda.troco = float(vr.get("troco", 0))
            venda.observacao = vr.get("observacao", "")
            venda.data_venda = str(vr.get("data_venda", ""))
            venda.para_entrega = bool(vr.get("para_entrega", 0))
            venda.taxa_entrega = float(vr.get("taxa_entrega", 0))

            # Cliente
            venda.cliente_nome = (vr.get("cliente_nome") or "").strip()
            if (not venda.cliente_nome) and vr.get("uso_armario_id"):
                uso = db.execute_query("SELECT cliente_nome FROM uso_armario_sauna WHERE id = %s", (vr["uso_armario_id"],))
                venda.cliente_nome = (uso[0]["cliente_nome"] or "").strip() if uso else ""
            if (not venda.cliente_nome) and vr.get("cliente_id"):
                cli = db.execute_query("SELECT nome FROM clientes WHERE id = %s", (vr["cliente_id"],))
                venda.cliente_nome = cli[0]["nome"] if cli else ""

            # Itens
            itens_rows = db.execute_query(
                "SELECT * FROM itens_venda WHERE venda_id = %s", (venda_id,)
            )
            itens = []
            for ir in itens_rows:
                item = ItemVenda()
                item.descricao_produto = ir.get("descricao_produto", "")
                item.quantidade = float(ir.get("quantidade", 1))
                item.preco_unitario = float(ir.get("preco_unitario", 0))
                item.total = float(ir.get("total", 0))
                itens.append(item)

            # Pagamentos
            pag_rows = db.execute_query(
                "SELECT pv.*, fp.descricao as forma_descricao FROM pagamentos_venda pv "
                "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "WHERE pv.venda_id = %s", (venda_id,)
            )
            pags = []
            for pr in pag_rows:
                pag = PagamentoVenda()
                pag.forma_descricao = pr.get("forma_descricao", "")
                pag.valor = float(pr.get("valor", 0))
                pag.parcelas = int(pr.get("parcelas", 1))
                pags.append(pag)

            # Empresa
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass

            return venda, itens, pags, empresa

        def on_loaded(result):
            if result:
                venda, itens, pags, empresa = result
                printer_cfg = self._load_printer_config()
                cupom = CupomGenerator.gerar_cupom(
                    venda, itens, pags,
                    empresa=empresa,
                    colunas=printer_cfg.get("largura_papel", 48),
                    margem_esq=printer_cfg.get("margem_esquerda", 0),
                    margem_dir=printer_cfg.get("margem_direita", 0),
                    espacamento=printer_cfg.get("espacamento_linhas", 1)
                )
                self._mostrar_cupom(cupom)
            else:
                self.show_error("Venda nao encontrada.")

        self.run_async(load, on_loaded)

    def cancelar_venda_historico(self):
        sel = self.tree_historico.selection()
        if not sel:
            self.show_error("Selecione uma venda.")
            return
        venda_id = self.tree_historico.item(sel[0])["values"][0]
        status = self.tree_historico.item(sel[0])["values"][4]
        if status == "cancelada":
            ToastManager.warning("Esta venda ja esta cancelada.")
            return
        if not messagebox.askyesno("Confirmar",
                f"Cancelar venda #{venda_id}?\n\n"
                "O estoque dos itens sera devolvido automaticamente."):
            return
        def cancelar():
            db = DatabaseHelper.get_instance()
            # Marca a venda como cancelada PRIMEIRO: assim ela deixa de contar
            # imediatamente nos totais e no resumo por forma de pagamento, mesmo
            # que a devolucao de estoque abaixo falhe por qualquer motivo.
            db.execute_update(
                "UPDATE vendas SET status = 'cancelada' WHERE id = %s", (venda_id,)
            )
            # Devolve o estoque dos itens da venda
            itens = db.execute_query(
                "SELECT produto_id, quantidade FROM itens_venda WHERE venda_id = %s",
                (venda_id,)
            )
            for item in itens:
                if item.get("produto_id"):
                    db.execute_update(
                        "UPDATE produtos SET estoque = estoque + %s WHERE id = %s",
                        (item["quantidade"], item["produto_id"])
                    )
        def on_cancelado(_):
            AuditLogger.log("VENDA_CANCELADA",
                            f"Venda #{venda_id}",
                            usuario=Session.user_login)
            ToastManager.info(f"Venda #{venda_id} cancelada. Estoque devolvido.")
            self.show_historico()
        self.run_async(cancelar, on_cancelado)

    def reabrir_venda_historico(self):
        """Reabre uma venda finalizada para edicao. O operador pode adicionar/
        remover produtos, trocar a forma de pagamento, o cliente e os dados de
        entrega; ao finalizar, a venda e regravada com o MESMO id."""
        sel = self.tree_historico.selection()
        if not sel:
            self.show_error("Selecione uma venda para reabrir.")
            return
        venda_id = self.tree_historico.item(sel[0])["values"][0]
        status = self.tree_historico.item(sel[0])["values"][4]
        if str(status).lower() == "cancelada":
            ToastManager.warning(
                "Nao e possivel reabrir uma venda cancelada.")
            return
        if not messagebox.askyesno(
                "Reabrir / Editar Venda",
                f"Reabrir a venda #{venda_id} para edicao?\n\n"
                "Voce podera adicionar/remover produtos, trocar a forma de "
                "pagamento, o cliente e os dados de entrega.\n\n"
                f"Ao finalizar, a venda sera REGRAVADA no mesmo numero (#{venda_id}), "
                "e o estoque sera ajustado automaticamente.\n\n"
                "(E necessario ter um caixa aberto para regravar.)"):
            return

        def load():
            db = DatabaseHelper.get_instance()
            vr_rows = db.execute_query("SELECT * FROM vendas WHERE id = %s", (venda_id,))
            if not vr_rows:
                return None
            itens = db.execute_query(
                "SELECT * FROM itens_venda WHERE venda_id = %s", (venda_id,)) or []
            return {"venda": vr_rows[0], "itens": itens}

        def on_loaded(data):
            if not data:
                self.show_error("Venda nao encontrada.")
                return
            # Exige caixa aberto (para poder regravar) e so entao popula a tela
            self._verificar_caixa_aberto_para_venda(
                callback_sucesso=lambda: self._popular_venda_edicao(venda_id, data),
                origem=f"editar venda #{venda_id}")

        self.run_async(load, on_loaded)

    def _popular_venda_edicao(self, venda_id, data):
        """Abre a tela de venda ja preenchida com os dados da venda que esta
        sendo editada."""
        vr = data["venda"]
        # Renderiza a tela de venda (reseta o estado, incl. _editando_venda_id)
        self._show_venda_ui()
        # Ativa o modo edicao
        self._editando_venda_id = venda_id

        # Cliente
        self.venda_cliente_id = vr.get("cliente_id") or 0
        self.venda_cliente_nome = (vr.get("cliente_nome") or "").strip() or "Cliente nao informado"
        try:
            self.lbl_cliente_venda.config(text=self.venda_cliente_nome)
        except Exception:
            pass
        self.venda_vendedor_id = vr.get("vendedor_id") or 0
        self.venda_entregador_id = vr.get("entregador_id") or 0

        # Itens -> carrinho
        self.carrinho = []
        for ir in data["itens"]:
            item = ItemVenda()
            item.produto_id = ir.get("produto_id") or 0
            item.descricao_produto = ir.get("descricao_produto") or ""
            item.quantidade = float(ir.get("quantidade") or 1)
            item.preco_unitario = float(ir.get("preco_unitario") or 0)
            item.desconto = float(ir.get("desconto") or 0)
            item.total = float(ir.get("total") or 0)
            self.carrinho.append(item)
        self.atualizar_tree_carrinho()

        # Desconto / acrescimo: repovoados como "Valor (R$)" para preservar o
        # total exato (o percentual original nao e armazenado, apenas o valor).
        try:
            self.combo_desc_tipo.set("Valor (R$)")
            self.et_desconto.delete(0, tk.END)
            self.et_desconto.insert(0, FormatUtils.format_money(float(vr.get("desconto_valor") or 0)))
        except Exception:
            pass
        try:
            self.combo_acre_tipo.set("Valor (R$)")
            self.et_acrescimo.delete(0, tk.END)
            self.et_acrescimo.insert(0, FormatUtils.format_money(float(vr.get("acrescimo_valor") or 0)))
        except Exception:
            pass

        # Observacao
        try:
            self.et_obs_venda.delete("1.0", tk.END)
            self.et_obs_venda.insert("1.0", vr.get("observacao") or "")
        except Exception:
            pass

        # Entrega
        if int(vr.get("para_entrega") or 0) == 1:
            self.venda_para_entrega = True
            self.venda_taxa_entrega = float(vr.get("taxa_entrega") or 0)
            self.venda_bairro_entrega = vr.get("bairro_entrega") or ""
            self.venda_endereco_entrega = vr.get("endereco_entrega") or ""
            self.venda_celular_whatsapp = vr.get("celular_whatsapp") or ""
            try:
                self._var_entrega.set(1)
                self._toggle_entrega()
            except Exception:
                pass
            try:
                self.et_endereco_entrega.delete(0, tk.END)
                self.et_endereco_entrega.insert(0, self.venda_endereco_entrega)
            except Exception:
                pass
            try:
                self.et_whatsapp_entrega.delete(0, tk.END)
                self.et_whatsapp_entrega.insert(0, self.venda_celular_whatsapp)
            except Exception:
                pass
            try:
                if self.venda_bairro_entrega:
                    self.lbl_bairro_entrega.config(
                        text=f"{self.venda_bairro_entrega}  "
                             f"(R$ {FormatUtils.format_money(self.venda_taxa_entrega)})")
            except Exception:
                pass

        self.atualizar_totais_venda()

        # Titulo indicando modo edicao
        try:
            self._lbl_titulo_venda.config(
                text=f"{Icons.EDIT} Editando Venda #{venda_id}", fg=COR_ALERTA)
        except Exception:
            pass
        ToastManager.info(
            f"Venda #{venda_id} reaberta. Edite e finalize para regravar no mesmo numero.")


    # ========================================================================
    # GERENCIAR MESAS
    # ========================================================================
    def show_gerenciar_mesas(self):
        self.clear_container()

        top = self._create_top_bar(f"{Icons.MESA} Gerenciar Mesas")
        btn_nova_mesa = StyledButton(top, text=f"{Icons.ADD} Nova Mesa",
                     command=self.nova_mesa,
                     color=COR_BOTAO_VERDE, width=12)
        btn_nova_mesa.pack(side="right", padx=10, pady=5)
        add_tooltip(btn_nova_mesa, "Criar uma nova mesa")

        btn_assistente_mesas = StyledButton(top, text="Assistente",
                     command=self.assistente_criar_mesas,
                     color=COR_BOTAO_AZUL, width=12)
        btn_assistente_mesas.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_assistente_mesas, "Criar varias mesas em sequencia")

        btn_refresh = StyledButton(top, text=f"{Icons.REFRESH} Atualizar",
                                   command=self.carregar_mesas,
                                   color=COR_FUNDO3, width=10)
        btn_refresh.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_refresh, "Recarregar status das mesas (auto: 30s)")

        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=6)

        # Legenda
        leg_frame = tk.Frame(content, bg=COR_FUNDO)
        leg_frame.pack(fill="x", pady=(0, 8))
        for cor, texto in [
            (COR_BOTAO_VERDE, "Livre"),
            (COR_ERRO,        "Ocupada"),
            (COR_ALERTA,      "Reservada"),
            ("#42A5F5",       "Pronta"),
        ]:
            dot = tk.Canvas(leg_frame, width=14, height=14,
                            bg=COR_FUNDO, highlightthickness=0)
            dot.create_oval(2, 2, 12, 12, fill=cor, outline="")
            dot.pack(side="left", padx=(8, 2))
            tk.Label(leg_frame, text=texto, bg=COR_FUNDO, fg=COR_TEXTO2,
                     font=("Segoe UI", 9)).pack(side="left", padx=(0, 12))

        # Contadores
        self._lbl_mesas_info = tk.Label(
            leg_frame, text="", bg=COR_FUNDO,
            fg=COR_PRIMARIA, font=("Segoe UI", 9, "bold"))
        self._lbl_mesas_info.pack(side="right", padx=12)

        scroll_mesas = ScrollableFrame(content)
        scroll_mesas.pack(fill="both", expand=True)
        self.mesas_frame = scroll_mesas.scrollable_frame

        self.carregar_mesas()

        # Auto-refresh em tempo real (a cada 10 segundos) via sistema centralizado
        self._start_auto_refresh("gerenciar_mesas", self.carregar_mesas, 10000)

    def carregar_mesas(self):
        """Carrega mesas usando ocupacao_mesa para status (alinhado com Android)."""
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT m.id, m.numero, m.descricao, m.capacidade, "
                "COALESCE(om.garcom_id, 0) as garcom_id, "
                "COALESCE(g.nome, '') as garcom_nome, "
                "COALESCE(om.qtd_pessoas, 0) as qtd_pessoas, "
                "COALESCE(om.status, 'livre') as status, "
                "COALESCE(om.id, 0) as ocupacao_id, "
                "COALESCE(om.reservado_por_usuario_id, 0) as reservado_por_usuario_id, "
                "COALESCE(om.reservado_por_usuario_nome, '') as reservado_por_usuario_nome "
                "FROM mesas m "
                "LEFT JOIN ocupacao_mesa om ON m.id = om.mesa_id AND om.status != 'encerrada' "
                "LEFT JOIN garcons g ON om.garcom_id = g.id "
                "WHERE m.ativa = 1 "
                "ORDER BY m.numero ASC"
            )

        def on_loaded(mesas):
            try:
                for w in self.mesas_frame.winfo_children():
                    w.destroy()
            except Exception:
                return

            # Calcular totais por ocupacao
            for mesa in mesas:
                ocupacao_id = int(mesa.get("ocupacao_id", 0))
                if ocupacao_id > 0:
                    try:
                        db = DatabaseHelper.get_instance()
                        res = db.execute_query(
                            "SELECT COUNT(*) as qtd, COALESCE(SUM(total + COALESCE(adicionais_total, 0)), 0) as total_val "
                            "FROM itens_mesa WHERE ocupacao_id = %s", (ocupacao_id,))
                        if res:
                            mesa["qtd_itens"] = int(res[0].get("qtd", 0))
                            mesa["total"] = float(res[0].get("total_val", 0))
                        else:
                            mesa["qtd_itens"] = 0
                            mesa["total"] = 0.0
                    except Exception:
                        mesa["qtd_itens"] = 0
                        mesa["total"] = 0.0
                else:
                    mesa["qtd_itens"] = 0
                    mesa["total"] = 0.0

            livres = sum(1 for m in mesas if m.get("status") == "livre")
            ocupadas = sum(1 for m in mesas if m.get("status") == "ocupada")
            reservadas = sum(1 for m in mesas if m.get("status") == "reservada")
            prontas = sum(1 for m in mesas if m.get("status") == "pronta")
            try:
                self._lbl_mesas_info.config(
                    text=f"{Icons.PIN} {len(mesas)} mesas  |  "
                         f"{Icons.ONLINE} {livres} livres  |  "
                         f"{Icons.OFFLINE} {ocupadas} ocupadas"
                         + (f"  |  {reservadas} reservadas" if reservadas else "")
                         + (f"  |  {prontas} prontas" if prontas else ""))
            except Exception:
                pass

            col = 0
            row = 0
            max_cols = 7
            for mesa in mesas:
                status = mesa.get("status", "livre")
                total = float(mesa.get("total", 0))
                qtd_itens = int(mesa.get("qtd_itens", 0))
                garcom_nome = mesa.get("garcom_nome", "")
                qtd_pessoas = int(mesa.get("qtd_pessoas", 0))
                reservado_por = mesa.get("reservado_por_usuario_nome", "")

                # Cores por status (alinhado com Android MesaAdapter)
                if status == "livre":
                    bg_cor = "#0a1f0a"
                    borda_cor = COR_BOTAO_VERDE
                    status_cor = COR_BOTAO_VERDE
                elif status == "ocupada":
                    bg_cor = "#1f0a0a"
                    borda_cor = COR_ERRO
                    status_cor = COR_ERRO
                elif status == "reservada":
                    bg_cor = "#1a1500"
                    borda_cor = COR_ALERTA
                    status_cor = COR_ALERTA
                elif status == "pronta":
                    bg_cor = "#0a0a2f"
                    borda_cor = "#42A5F5"
                    status_cor = "#42A5F5"
                else:
                    bg_cor = "#1a1500"
                    borda_cor = COR_ALERTA
                    status_cor = COR_ALERTA

                # Card visual da mesa
                card = tk.Frame(self.mesas_frame, bg=bg_cor,
                                highlightthickness=2,
                                highlightbackground=borda_cor,
                                cursor="hand2")
                card.grid(row=row, column=col, padx=7, pady=7,
                          sticky="nsew")

                # Numero da mesa grande
                tk.Label(card, text=str(mesa["numero"]),
                         bg=bg_cor, fg=borda_cor,
                         font=("Segoe UI", 22, "bold"),
                         width=5, height=2).pack(pady=(6, 0))

                # Status label (alinhado com Android)
                status_text = status.upper()
                if status == "pronta":
                    status_text = "PRONTA P/ COBRANCA"
                tk.Label(card, text=status_text,
                         bg=bg_cor, fg=status_cor,
                         font=("Segoe UI", 8, "bold")).pack()

                # Reservado por (alinhado com Android)
                if status == "reservada" and reservado_por:
                    tk.Label(card, text=f"Reservado: {reservado_por}",
                             bg=bg_cor, fg=COR_ALERTA,
                             font=("Segoe UI", 7)).pack()

                # Garcom
                if garcom_nome:
                    tk.Label(card, text=f"Garcom: {garcom_nome}",
                             bg=bg_cor, fg=COR_TEXTO2,
                             font=("Segoe UI", 7)).pack()

                # Pessoas
                if qtd_pessoas > 0:
                    tk.Label(card, text=f"{qtd_pessoas} pessoa(s)",
                             bg=bg_cor, fg=COR_TEXTO2,
                             font=("Segoe UI", 7)).pack()

                # Total e itens
                if total > 0:
                    tk.Label(card,
                             text=f"R$ {FormatUtils.format_money(total)}",
                             bg=bg_cor, fg=COR_ALERTA,
                             font=("Consolas", 9, "bold")).pack(pady=(0, 2))
                if qtd_itens > 0:
                    tk.Label(card, text=f"{qtd_itens} item(ns)",
                             bg=bg_cor, fg=COR_TEXTO2,
                             font=("Segoe UI", 7)).pack(pady=(0, 4))
                elif total <= 0:
                    tk.Label(card, text="", bg=bg_cor,
                             font=("Segoe UI", 9)).pack(pady=(0, 4))

                # Hover effect
                def _hover_on(e, c=card, b=borda_cor):
                    c.configure(bg=COR_CARD_HOVER)
                    for w in c.winfo_children():
                        try:
                            w.configure(bg=COR_CARD_HOVER)
                        except Exception:
                            pass

                def _hover_off(e, c=card, bg=bg_cor):
                    c.configure(bg=bg)
                    for w in c.winfo_children():
                        try:
                            w.configure(bg=bg)
                        except Exception:
                            pass

                card.bind("<Enter>", _hover_on)
                card.bind("<Leave>", _hover_off)
                for child in card.winfo_children():
                    child.bind("<Enter>", _hover_on)
                    child.bind("<Leave>", _hover_off)
                    child.bind("<Button-1>",
                               lambda e, m=mesa: self._acao_mesa(m))

                card.bind("<Button-1>", lambda e, m=mesa: self._acao_mesa(m))

                tooltip_text = f"Mesa {mesa['numero']} — {status.upper()}"
                if garcom_nome:
                    tooltip_text += f" — Garcom: {garcom_nome}"
                if total > 0:
                    tooltip_text += f" — R$ {FormatUtils.format_money(total)}"
                if status == "reservada" and reservado_por:
                    tooltip_text += f" — Reservado por: {reservado_por}"
                add_tooltip(card, tooltip_text)

                col += 1
                if col >= max_cols:
                    col = 0
                    row += 1

            for c in range(max_cols):
                self.mesas_frame.columnconfigure(c, weight=1)

        self.run_async(load, on_loaded)

    def nova_mesa(self):
        num = simpledialog.askinteger(
            "Nova Mesa", "Numero da nova mesa:", minvalue=1, parent=self.root)
        if not num:
            return
        def criar():
            db = DatabaseHelper.get_instance()
            # Verificar se ja existe
            existe = db.execute_query(
                "SELECT id FROM mesas WHERE numero = %s", (num,))
            if existe:
                return False
            db.execute_update(
                "INSERT INTO mesas (numero, ativa) VALUES (%s, 1)", (num,))
            return True
        def on_criado(ok):
            if ok:
                ToastManager.success(f"Mesa {num} criada com sucesso!")
                AuditLogger.log("MESA_CRIADA", f"Mesa #{num}",
                                usuario=Session.user_login)
                self.carregar_mesas()
            else:
                ToastManager.warning(f"Mesa {num} ja existe!")
        self.run_async(criar, on_criado)

    def _acao_mesa(self, mesa):
        """Acao ao clicar em uma mesa - abre detalhe (alinhado com Android)."""
        status = mesa.get("status", "livre")
        mesa_id = mesa["id"]
        ocupacao_id = int(mesa.get("ocupacao_id", 0))
        self._show_mesa_detalhe(mesa_id, mesa["numero"], ocupacao_id, status, mesa)

    def _show_mesa_detalhe(self, mesa_id, numero, ocupacao_id, status_atual, mesa_data):
        """Dialog de detalhe da mesa - alinhado com Android GerenciarMesasActivity."""
        dialog = tk.Toplevel(self.root)
        dialog.title(f"Mesa {numero}")
        dialog.geometry("700x600")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        # Dados de reserva
        reservado_por_id = int(mesa_data.get("reservado_por_usuario_id", 0))
        reservado_por_nome = mesa_data.get("reservado_por_usuario_nome", "")
        mesa_reservada = status_atual == "reservada" and reservado_por_id > 0
        usuario_eh_dono = (reservado_por_id == Session.user_id) if hasattr(Session, 'user_id') else True
        pode_editar = not mesa_reservada or usuario_eh_dono

        # Header
        tk.Label(dialog, text=f"Mesa {numero}", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=5)

        # Info de reserva/pronta (alinhado com Android)
        if mesa_reservada:
            info_frame = tk.Frame(dialog, bg="#1a1500")
            info_frame.pack(fill="x", padx=15, pady=3)
            tk.Label(info_frame, text="MESA RESERVADA" + (" (BLOQUEADA)" if not pode_editar else ""),
                     bg="#1a1500", fg=COR_ALERTA if pode_editar else COR_ERRO,
                     font=("Segoe UI", 10, "bold")).pack(pady=2)
            tk.Label(info_frame, text=f"Reservado por: {reservado_por_nome}",
                     bg="#1a1500", fg=COR_TEXTO2, font=("Segoe UI", 9)).pack(pady=2)
        elif status_atual == "pronta":
            info_frame = tk.Frame(dialog, bg="#0a0a2f")
            info_frame.pack(fill="x", padx=15, pady=3)
            tk.Label(info_frame, text="PRONTA PARA COBRANCA",
                     bg="#0a0a2f", fg="#42A5F5",
                     font=("Segoe UI", 10, "bold")).pack(pady=2)
            tk.Label(info_frame, text="Esta mesa esta pronta para ser cobrada e fechada.",
                     bg="#0a0a2f", fg="#90CAF9", font=("Segoe UI", 9)).pack(pady=2)

        # Garcom selector (alinhado com Android)
        garcom_frame = tk.Frame(dialog, bg=COR_FUNDO)
        garcom_frame.pack(fill="x", padx=15, pady=3)
        tk.Label(garcom_frame, text="Garcom:", bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left")
        self._mesa_garcom_var = tk.StringVar(value="-- Selecione Garcom --")
        self._mesa_garcons_list = []
        garcom_combo = ttk.Combobox(garcom_frame, textvariable=self._mesa_garcom_var,
                                    state="readonly", width=30)
        garcom_combo.pack(side="left", padx=5)

        # Qtd pessoas
        tk.Label(garcom_frame, text="Pessoas:", bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left", padx=(15, 0))
        self._mesa_pessoas_var = tk.StringVar(value=str(int(mesa_data.get("qtd_pessoas", 0))))
        pessoas_entry = tk.Entry(garcom_frame, textvariable=self._mesa_pessoas_var,
                                  width=5, bg=COR_FUNDO2, fg=COR_TEXTO, insertbackground=COR_TEXTO)
        pessoas_entry.pack(side="left", padx=5)

        # Carregar garcons
        def load_garcons():
            db = DatabaseHelper.get_instance()
            return db.execute_query("SELECT id, nome FROM garcons WHERE ativo = 1 ORDER BY nome")
        def on_garcons(garcons):
            self._mesa_garcons_list = garcons or []
            nomes = ["-- Selecione Garcom --"] + [g["nome"] for g in self._mesa_garcons_list]
            garcom_combo["values"] = nomes
            garcom_id_atual = int(mesa_data.get("garcom_id", 0))
            if garcom_id_atual > 0:
                for i, g in enumerate(self._mesa_garcons_list):
                    if g["id"] == garcom_id_atual:
                        garcom_combo.current(i + 1)
                        break
        self.run_async(load_garcons, on_garcons)

        # Tabela de itens
        cols = ("id", "produto", "qtd", "preco", "total", "adicionais")
        tree = ttk.Treeview(dialog, columns=cols, show="headings", height=10)
        for c, h, w in [("id","ID",50),("produto","Produto",220),("qtd","Qtd",60),
                         ("preco","Preco",80),("total","Total",80),("adicionais","Adicionais",120)]:
            tree.heading(c, text=h)
            tree.column(c, width=w, anchor="center" if c in ("id","qtd") else "e" if c in ("preco","total") else "w")
        tree.pack(fill="both", expand=True, padx=15, pady=5)

        lbl_total = tk.Label(dialog, text="Total: R$ 0,00", bg=COR_FUNDO, fg=COR_SUCESSO,
                              font=("Segoe UI", 14, "bold"))
        lbl_total.pack(pady=5)

        # Estado local da ocupacao
        _ocupacao = {"id": ocupacao_id}

        def carregar():
            """Carrega itens da mesa via ocupacao_id (alinhado com Android)."""
            def load():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]
                if oc_id > 0:
                    return db.execute_query(
                        "SELECT id, descricao_produto, quantidade, preco_unitario, total, "
                        "adicionais_descricao, COALESCE(adicionais_total, 0) as adicionais_total "
                        "FROM itens_mesa WHERE ocupacao_id = %s ORDER BY id ASC", (oc_id,)
                    )
                return []
            def on_loaded(rows):
                tree.delete(*tree.get_children())
                total = 0
                for r in rows:
                    ad_desc = r.get("adicionais_descricao", "") or ""
                    ad_total = float(r.get("adicionais_total", 0))
                    tree.insert("", "end", values=(
                        r["id"], r["descricao_produto"], r["quantidade"],
                        FormatUtils.format_money(r["preco_unitario"]),
                        FormatUtils.format_money(r["total"]),
                        ad_desc if ad_desc else "-"
                    ))
                    total += float(r["total"]) + ad_total
                lbl_total.config(text=f"Total: R$ {FormatUtils.format_money(total)}")
            self.run_async(load, on_loaded)

        def add_item():
            if not pode_editar:
                ToastManager.warning("Mesa reservada por outro usuario. Nao e possivel editar.")
                return
            self._selecionar_registro(
                "produtos", "Adicionar Produto",
                "SELECT id, descricao, preco_venda FROM produtos WHERE ativo = 1 ORDER BY descricao",
                ["ID", "Descricao", "Preco"],
                lambda row: self._add_item_mesa(_ocupacao, mesa_id, row, carregar, dialog) if row else None
            )

        def remover_item():
            if not pode_editar:
                ToastManager.warning("Mesa reservada por outro usuario. Nao e possivel editar.")
                return
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione um item para remover.")
                return
            item_id = tree.item(sel[0])["values"][0]
            if messagebox.askyesno("Confirmar", "Remover este item?", parent=dialog):
                def do_remover():
                    db = DatabaseHelper.get_instance()
                    try:
                        db.execute_update("DELETE FROM itens_mesa_adicionais WHERE item_mesa_id = %s", (item_id,))
                    except Exception:
                        pass
                    db.execute_update("DELETE FROM itens_mesa WHERE id = %s", (item_id,))
                self.run_async(do_remover, lambda _: carregar())

        def salvar_mesa():
            """Salva garcom, pessoas e status via ocupacao_mesa (alinhado com Android)."""
            garcom_idx = garcom_combo.current()
            garcom_id = 0
            if garcom_idx > 0 and garcom_idx <= len(self._mesa_garcons_list):
                garcom_id = self._mesa_garcons_list[garcom_idx - 1]["id"]
            try:
                qtd_pessoas = int(self._mesa_pessoas_var.get())
            except (ValueError, TypeError):
                qtd_pessoas = 0

            def do_salvar():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]

                # Contar itens para determinar status
                itens_count = 0
                if oc_id > 0:
                    res = db.execute_query(
                        "SELECT COUNT(*) as qtd FROM itens_mesa WHERE ocupacao_id = %s", (oc_id,))
                    if res:
                        itens_count = int(res[0].get("qtd", 0))

                # Determinar novo status (alinhado com Android)
                if itens_count > 0:
                    # Preservar status 'pronta' se ja estiver marcada
                    if oc_id > 0:
                        res_status = db.execute_query(
                            "SELECT status FROM ocupacao_mesa WHERE id = %s", (oc_id,))
                        if res_status and res_status[0].get("status") == "pronta":
                            novo_status = "pronta"
                        else:
                            novo_status = "ocupada"
                    else:
                        novo_status = "ocupada"
                else:
                    # Preservar reserva se existir
                    if oc_id > 0:
                        res_status = db.execute_query(
                            "SELECT status, reservado_por_usuario_id FROM ocupacao_mesa WHERE id = %s", (oc_id,))
                        if res_status:
                            st = res_status[0].get("status", "livre")
                            rpid = int(res_status[0].get("reservado_por_usuario_id", 0))
                            if st == "reservada" and rpid > 0:
                                novo_status = "reservada"
                            else:
                                novo_status = "livre"
                        else:
                            novo_status = "livre"
                    else:
                        novo_status = "livre"

                if oc_id == 0:
                    # Criar nova ocupacao se necessario
                    if itens_count > 0 or garcom_id > 0 or qtd_pessoas > 0:
                        db.execute_update(
                            "INSERT INTO ocupacao_mesa (mesa_id, garcom_id, qtd_pessoas, status, data_abertura) "
                            "VALUES (%s, %s, %s, %s, NOW())",
                            (mesa_id, garcom_id, qtd_pessoas, novo_status))
                        res = db.execute_query("SELECT LAST_INSERT_ID() as lid")
                        if res:
                            _ocupacao["id"] = int(res[0].get("lid", 0))
                else:
                    # Atualizar ocupacao existente
                    db.execute_update(
                        "UPDATE ocupacao_mesa SET garcom_id=%s, qtd_pessoas=%s, status=%s WHERE id=%s",
                        (garcom_id, qtd_pessoas, novo_status, oc_id))

            def on_salvo(_):
                ToastManager.success("Mesa salva com sucesso!")
                dialog.destroy()
                self.carregar_mesas()
            self.run_async(do_salvar, on_salvo)

        def limpar_mesa():
            """Limpa mesa encerrando ocupacao (alinhado com Android)."""
            if messagebox.askyesno("Confirmar", f"Limpar mesa {numero}? Todos os itens serao removidos.", parent=dialog):
                def do_limpar():
                    db = DatabaseHelper.get_instance()
                    oc_id = _ocupacao["id"]
                    if oc_id > 0:
                        # Remover adicionais dos itens
                        try:
                            db.execute_update(
                                "DELETE FROM itens_mesa_adicionais WHERE item_mesa_id IN "
                                "(SELECT id FROM itens_mesa WHERE ocupacao_id = %s)", (oc_id,))
                        except Exception:
                            pass
                        # Remover itens
                        db.execute_update("DELETE FROM itens_mesa WHERE ocupacao_id = %s", (oc_id,))
                        # Encerrar ocupacao e limpar reserva
                        db.execute_update(
                            "UPDATE ocupacao_mesa SET status = 'encerrada', data_fechamento = NOW(), "
                            "reservado_por_usuario_id = 0, reservado_por_usuario_nome = NULL WHERE id = %s",
                            (oc_id,))
                def on_limpa(_):
                    ToastManager.success(f"Mesa {numero} limpa com sucesso!")
                    dialog.destroy()
                    self.carregar_mesas()
                self.run_async(do_limpar, on_limpa)

        def reservar_mesa():
            """Reserva a mesa para o usuario logado (alinhado com Android)."""
            def do_reservar():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]
                user_id = Session.user_id if hasattr(Session, 'user_id') else 0
                user_nome = Session.user_nome if hasattr(Session, 'user_nome') else ""
                if oc_id == 0:
                    db.execute_update(
                        "INSERT INTO ocupacao_mesa (mesa_id, garcom_id, qtd_pessoas, status, data_abertura, "
                        "reservado_por_usuario_id, reservado_por_usuario_nome) VALUES (%s, 0, 0, 'reservada', NOW(), %s, %s)",
                        (mesa_id, user_id, user_nome))
                else:
                    db.execute_update(
                        "UPDATE ocupacao_mesa SET status = 'reservada', reservado_por_usuario_id = %s, "
                        "reservado_por_usuario_nome = %s WHERE id = %s",
                        (user_id, user_nome, oc_id))
            def on_reservada(_):
                ToastManager.success(f"Mesa {numero} reservada!")
                dialog.destroy()
                self.carregar_mesas()
            self.run_async(do_reservar, on_reservada)

        def cancelar_reserva():
            """Cancela reserva da mesa (alinhado com Android)."""
            def do_cancelar():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]
                if oc_id > 0:
                    res = db.execute_query(
                        "SELECT COUNT(*) as qtd FROM itens_mesa WHERE ocupacao_id = %s", (oc_id,))
                    qtd_itens = int(res[0].get("qtd", 0)) if res else 0
                    if qtd_itens > 0:
                        db.execute_update(
                            "UPDATE ocupacao_mesa SET status = 'ocupada', reservado_por_usuario_id = 0, "
                            "reservado_por_usuario_nome = NULL WHERE id = %s", (oc_id,))
                    else:
                        db.execute_update(
                            "UPDATE ocupacao_mesa SET status = 'encerrada', data_fechamento = NOW(), "
                            "reservado_por_usuario_id = 0, reservado_por_usuario_nome = NULL WHERE id = %s",
                            (oc_id,))
            def on_cancelada(_):
                ToastManager.success("Reserva cancelada!")
                dialog.destroy()
                self.carregar_mesas()
            self.run_async(do_cancelar, on_cancelada)

        def marcar_pronta():
            """Marca mesa como pronta para cobranca (alinhado com Android)."""
            def do_marcar():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]
                if oc_id > 0:
                    db.execute_update(
                        "UPDATE ocupacao_mesa SET status = 'pronta' WHERE id = %s", (oc_id,))
            def on_marcada(_):
                ToastManager.success(f"Mesa {numero} marcada como pronta para cobranca!")
                dialog.destroy()
                self.carregar_mesas()
            self.run_async(do_marcar, on_marcada)

        def cancelar_pronta():
            """Cancela status pronta (alinhado com Android)."""
            def do_cancelar():
                db = DatabaseHelper.get_instance()
                oc_id = _ocupacao["id"]
                if oc_id > 0:
                    db.execute_update(
                        "UPDATE ocupacao_mesa SET status = 'ocupada' WHERE id = %s", (oc_id,))
            def on_cancelada(_):
                ToastManager.success("Status pronta cancelado. Mesa voltou para ocupada.")
                dialog.destroy()
                self.carregar_mesas()
            self.run_async(do_cancelar, on_cancelada)

        # Botoes de acao (alinhado com Android)
        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=15, pady=5)

        if pode_editar:
            StyledButton(btn_frame, text=f"{Icons.ADD} Add Item", command=add_item,
                         color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=3)
            StyledButton(btn_frame, text=f"{Icons.REMOVE} Remover", command=remover_item,
                         color=COR_ERRO, width=12).pack(side="left", padx=3)

        btn_frame2 = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame2.pack(fill="x", padx=15, pady=5)

        if pode_editar:
            StyledButton(btn_frame2, text=f"{Icons.SAVE} Salvar", command=salvar_mesa,
                         color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=3)

        # Botao Reservar/Cancelar Reserva
        if status_atual == "livre":
            StyledButton(btn_frame2, text="Reservar Mesa", command=reservar_mesa,
                         color=COR_ALERTA, width=14).pack(side="left", padx=3)
        elif mesa_reservada and usuario_eh_dono:
            StyledButton(btn_frame2, text="Cancelar Reserva", command=cancelar_reserva,
                         color=COR_ALERTA, width=14).pack(side="left", padx=3)

        # Botao Pronta p/ Cobranca
        if status_atual in ("ocupada", "reservada") and pode_editar:
            StyledButton(btn_frame2, text="Pronta p/ Cobranca", command=marcar_pronta,
                         color="#42A5F5", width=16).pack(side="left", padx=3)
        elif status_atual == "pronta":
            StyledButton(btn_frame2, text="Cancelar Pronta", command=cancelar_pronta,
                         color="#42A5F5", width=14).pack(side="left", padx=3)

        # Botao Limpar Mesa
        if status_atual != "livre" and pode_editar:
            StyledButton(btn_frame2, text="Limpar Mesa", command=limpar_mesa,
                         color=COR_ERRO, width=12).pack(side="right", padx=3)

        carregar()

    def _add_item_mesa(self, ocupacao_ref, mesa_id, row, refresh, dialog):
        """Adiciona item a mesa via ocupacao_id (alinhado com Android)."""
        produto_id, descricao, preco = row[0], row[1], float(row[2])
        qtd = simpledialog.askfloat("Quantidade", f"Qtd de {descricao}:",
                                     initialvalue=1, minvalue=0.001, parent=dialog)
        if not qtd:
            return
        def salvar():
            db = DatabaseHelper.get_instance()
            oc_id = ocupacao_ref["id"]
            # Se nao tem ocupacao, criar uma
            if oc_id == 0:
                db.execute_update(
                    "INSERT INTO ocupacao_mesa (mesa_id, garcom_id, qtd_pessoas, status, data_abertura) "
                    "VALUES (%s, 0, 0, 'ocupada', NOW())",
                    (mesa_id,))
                res = db.execute_query("SELECT LAST_INSERT_ID() as lid")
                if res:
                    oc_id = int(res[0].get("lid", 0))
                    ocupacao_ref["id"] = oc_id
            # Inserir item vinculado a ocupacao (sem mesa_id, alinhado com Android)
            db.execute_update(
                "INSERT INTO itens_mesa (ocupacao_id, produto_id, descricao_produto, "
                "quantidade, preco_unitario, total) VALUES (%s,%s,%s,%s,%s,%s)",
                (oc_id, produto_id, descricao, qtd, preco, qtd * preco)
            )
            # Atualizar status da ocupacao para ocupada se estava livre
            if oc_id > 0:
                res_st = db.execute_query(
                    "SELECT status FROM ocupacao_mesa WHERE id = %s", (oc_id,))
                if res_st and res_st[0].get("status") in ("livre", None):
                    db.execute_update(
                        "UPDATE ocupacao_mesa SET status = 'ocupada' WHERE id = %s", (oc_id,))
        self.run_async(salvar, lambda _: refresh())

    # ========================================================================
    # GERENCIAR ARMARIOS (SAUNA) - Compativel com APK
    # Usa as mesmas tabelas do APK: armarios_sauna, uso_armario_sauna,
    # itens_armario_sauna, itens_armario_sauna_adicionais
    # ========================================================================
    def _iniciar_auto_refresh_armarios(self):
        """Inicia o auto-refresh periodico da tela de armarios (a cada 5 segundos).
        Agora usa o sistema centralizado de auto-refresh."""
        self._parar_auto_refresh_armarios()  # Garante que nao haja duplicidade
        # Usar sistema centralizado
        self._start_auto_refresh("gerenciar_armarios", self.carregar_armarios, 5000)
        # Manter compatibilidade com o _armario_auto_refresh_id
        self._armario_auto_refresh_id = True  # Flag para indicar que esta ativo

    def _parar_auto_refresh_armarios(self):
        """Para o auto-refresh periodico da tela de armarios."""
        if self._armario_auto_refresh_id is not None:
            # O sistema centralizado cuida do cancelamento via _stop_all_auto_refresh
            # Apenas resetar a flag de compatibilidade
            self._armario_auto_refresh_id = None

    def show_gerenciar_armarios(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=55)
        top.pack(fill="x")
        top.pack_propagate(False)
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Armarios Sauna", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        # Resumo
        self.lbl_resumo_armarios = tk.Label(top, text="", bg=COR_FUNDO2, fg=COR_TEXTO2,
                                             font=("Segoe UI", 9))
        self.lbl_resumo_armarios.pack(side="left", padx=20)

        btn_novo_armario = StyledButton(top, text="Novo Armario", command=self.novo_armario,
                     color=COR_BOTAO_VERDE, width=12)
        btn_novo_armario.pack(side="right", padx=10, pady=5)
        add_tooltip(btn_novo_armario, "Criar um novo armario")

        btn_assistente_armarios = StyledButton(top, text="Assistente", command=self.assistente_criar_armarios,
                     color=COR_BOTAO_AZUL, width=12)
        btn_assistente_armarios.pack(side="right", padx=5, pady=5)
        add_tooltip(btn_assistente_armarios, "Criar varios armarios em sequencia")

        # Campo de busca rapida por numero do armario
        busca_frame = tk.Frame(top, bg=COR_FUNDO2)
        busca_frame.pack(side="right", padx=10, pady=5)
        tk.Label(busca_frame, text="N\u00ba:", bg=COR_FUNDO2, fg=COR_TEXTO,
                 font=("Segoe UI", 10, "bold")).pack(side="left", padx=(0, 4))
        self._entry_busca_armario = StyledEntry(busca_frame, width=6)
        self._entry_busca_armario.pack(side="left", padx=(0, 4), ipady=2)
        self._entry_busca_armario.bind("<Return>", lambda e: self._ir_para_armario())
        StyledButton(busca_frame, text="Ir", command=self._ir_para_armario,
                     color=COR_BOTAO_AZUL, width=4).pack(side="left")

        # Indicador grande de armarios livres (bem visivel)
        self.lbl_livres_grande = tk.Label(top, text="LIVRES: 0", bg=COR_BOTAO_VERDE,
                                           fg=COR_TEXTO, font=("Segoe UI", 20, "bold"),
                                           padx=20, pady=2)
        self.lbl_livres_grande.pack(side="right", padx=15, pady=3)

        # ---- Filtros de status (legendas clicaveis) ----
        self._armario_filtro_atual = "todos"  # Filtro ativo: todos, ocupado, livre, manutencao
        self._armarios_dados_cache = []  # Cache dos dados carregados

        filtro_frame = tk.Frame(self.main_container, bg=COR_FUNDO2, height=42)
        filtro_frame.pack(fill="x", padx=10, pady=(5, 0))
        filtro_frame.pack_propagate(False)

        tk.Label(filtro_frame, text="Filtrar:", bg=COR_FUNDO2, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(side="left", padx=(10, 8), pady=5)

        self._filtro_buttons = {}
        filtros_config = [
            ("todos",       "Todos",        COR_PRIMARIA,     None),
            ("livre",       "Livres",       COR_BOTAO_VERDE,  None),
            ("ocupado",     "Ocupados",     "#9C27B0",        None),
            ("manutencao",  "Manutencao",   COR_ALERTA,       None),
        ]

        for filtro_key, filtro_label, filtro_cor, _ in filtros_config:
            btn = tk.Label(filtro_frame, text=f"  {filtro_label}  ",
                           bg=COR_FUNDO3, fg=filtro_cor,
                           font=("Segoe UI", 10, "bold"),
                           cursor="hand2", padx=12, pady=4,
                           relief="flat",
                           highlightthickness=2,
                           highlightbackground=COR_FUNDO3)
            btn.pack(side="left", padx=4, pady=5)
            self._filtro_buttons[filtro_key] = (btn, filtro_cor)

            def _make_filtro_click(key):
                return lambda e: self._aplicar_filtro_armarios(key)
            def _make_hover(b, cor):
                def enter(e):
                    b.configure(bg=cor, fg=COR_TEXTO)
                def leave(e):
                    # Manter destaque se for o filtro ativo
                    if self._armario_filtro_atual == [k for k, v in self._filtro_buttons.items() if v[0] == b][0]:
                        return
                    b.configure(bg=COR_FUNDO3, fg=cor)
                return enter, leave

            enter_fn, leave_fn = _make_hover(btn, filtro_cor)
            btn.bind("<Button-1>", _make_filtro_click(filtro_key))
            btn.bind("<Enter>", enter_fn)
            btn.bind("<Leave>", leave_fn)

        # Indicador de contagem por filtro
        self._filtro_count_label = tk.Label(filtro_frame, text="", bg=COR_FUNDO2,
                                             fg=COR_TEXTO2, font=("Segoe UI", 9))
        self._filtro_count_label.pack(side="right", padx=15)

        # Marcar "Todos" como ativo inicialmente
        self._atualizar_visual_filtro("todos")

        # ---- Layout de dois paineis: esquerdo (armarios) e direito (detalhes) ----
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=10)
        content.columnconfigure(0, weight=1)  # Painel esquerdo (armarios)
        content.columnconfigure(1, weight=1)  # Painel direito (detalhes)
        content.rowconfigure(0, weight=1)

        # --- Painel esquerdo: grid de armarios com scroll ---
        left_panel = tk.Frame(content, bg=COR_FUNDO)
        left_panel.grid(row=0, column=0, sticky="nsew", padx=(0, 5))

        self._armarios_canvas = tk.Canvas(left_panel, bg=COR_FUNDO, highlightthickness=0)
        self._armarios_scrollbar = ttk.Scrollbar(left_panel, orient="vertical",
                                                  command=self._armarios_canvas.yview)
        self._armarios_canvas.configure(yscrollcommand=self._armarios_scrollbar.set)

        self._armarios_scrollbar.pack(side="right", fill="y")
        self._armarios_canvas.pack(side="left", fill="both", expand=True)

        self.armarios_frame = tk.Frame(self._armarios_canvas, bg=COR_FUNDO)
        self._armarios_canvas_window = self._armarios_canvas.create_window(
            (0, 0), window=self.armarios_frame, anchor="nw")

        # Atualizar scrollregion quando o frame interno muda de tamanho
        def _on_armarios_frame_configure(event):
            self._armarios_canvas.configure(scrollregion=self._armarios_canvas.bbox("all"))
        self.armarios_frame.bind("<Configure>", _on_armarios_frame_configure)

        # Ajustar largura do frame interno para acompanhar a largura do canvas
        def _on_armarios_canvas_configure(event):
            self._armarios_canvas.itemconfig(self._armarios_canvas_window, width=event.width)
        self._armarios_canvas.bind("<Configure>", _on_armarios_canvas_configure)

        # Scroll com roda do mouse (Windows/Mac e Linux)
        def _on_armarios_mousewheel(event):
            try:
                if not self._armarios_canvas.winfo_exists():
                    return
                bbox = self._armarios_canvas.bbox("all")
                if bbox is None:
                    return
                canvas_height = self._armarios_canvas.winfo_height()
                content_height = bbox[3] - bbox[1]
                if content_height <= canvas_height:
                    return
                if event.num == 4:
                    self._armarios_canvas.yview_scroll(-3, "units")
                elif event.num == 5:
                    self._armarios_canvas.yview_scroll(3, "units")
                else:
                    self._armarios_canvas.yview_scroll(int(-1 * (event.delta / 120)), "units")
            except Exception:
                pass

        def _bind_armarios_mousewheel(event):
            self._armarios_canvas.bind_all("<MouseWheel>", _on_armarios_mousewheel)
            self._armarios_canvas.bind_all("<Button-4>", _on_armarios_mousewheel)
            self._armarios_canvas.bind_all("<Button-5>", _on_armarios_mousewheel)

        def _unbind_armarios_mousewheel(event):
            self._armarios_canvas.unbind_all("<MouseWheel>")
            self._armarios_canvas.unbind_all("<Button-4>")
            self._armarios_canvas.unbind_all("<Button-5>")

        self._armarios_canvas.bind("<Enter>", _bind_armarios_mousewheel)
        self._armarios_canvas.bind("<Leave>", _unbind_armarios_mousewheel)

        # --- Painel direito: area de detalhes (inicialmente com mensagem) ---
        self._armario_right_panel = tk.Frame(content, bg=COR_FUNDO, highlightthickness=1,
                                              highlightbackground="#37474F")
        self._armario_right_panel.grid(row=0, column=1, sticky="nsew", padx=(5, 0))

        self._armario_right_placeholder = tk.Label(
            self._armario_right_panel,
            text="Clique em um armario\npara ver os detalhes aqui",
            bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 14),
            justify="center"
        )
        self._armario_right_placeholder.place(relx=0.5, rely=0.5, anchor="center")

        self.carregar_armarios()
        self._iniciar_auto_refresh_armarios()

    def _limpar_painel_direito_armarios(self):
        """Limpa o conteudo do painel direito da tela de armarios."""
        try:
            if hasattr(self, '_armario_right_panel') and self._armario_right_panel.winfo_exists():
                for w in self._armario_right_panel.winfo_children():
                    w.destroy()
        except Exception:
            pass

    def _mostrar_placeholder_direito(self):
        """Mostra a mensagem padrao no painel direito."""
        self._limpar_painel_direito_armarios()
        self._armario_right_placeholder = tk.Label(
            self._armario_right_panel,
            text="Clique em um armario\npara ver os detalhes aqui",
            bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 14),
            justify="center"
        )
        self._armario_right_placeholder.place(relx=0.5, rely=0.5, anchor="center")

    def _ir_para_armario(self):
        """Busca o armario pelo numero digitado e abre a acao correspondente."""
        try:
            texto = self._entry_busca_armario.get().strip()
            if not texto:
                messagebox.showwarning(
                    "Numero do Armario",
                    "Por favor, digite o numero do armario no campo de busca.\n\n"
                    "Digite apenas o numero (ex: 1, 2, 3...) e pressione Enter.",
                    parent=self.root)
                return
            try:
                numero = int(texto)
            except ValueError:
                messagebox.showwarning(
                    "Numero Invalido",
                    "O valor digitado nao e um numero valido.\n\n"
                    "Digite apenas numeros inteiros (ex: 1, 2, 3...).\n"
                    "Nao use letras, espacos ou simbolos.",
                    parent=self.root)
                self._entry_busca_armario.delete(0, tk.END)
                self._entry_busca_armario.focus_set()
                return

            def buscar():
                db = DatabaseHelper.get_instance()
                armario = db.execute_query(
                    "SELECT a.id, a.numero, a.descricao, a.localizacao, "
                    "u.id as uso_id, u.cliente_nome, u.observacao, u.status as uso_status, "
                    "u.data_entrada, u.data_saida, "
                    "COALESCE(("
                    "  SELECT SUM(COALESCE(i.total,0) + COALESCE(i.adicionais_total,0)) "
                    "  FROM itens_armario_sauna i WHERE i.uso_armario_id = u.id"
                    "), 0) as total_consumo "
                    "FROM armarios_sauna a "
                    "LEFT JOIN uso_armario_sauna u ON u.armario_id = a.id "
                    "  AND u.status IN ('ocupado','manutencao') "
                    "WHERE a.ativo = 1 AND a.numero = %s "
                    "ORDER BY a.numero LIMIT 1",
                    (numero,)
                )
                return armario

            def on_found(armario):
                self._entry_busca_armario.delete(0, tk.END)
                if not armario:
                    messagebox.showwarning(
                        "Armario Nao Encontrado",
                        f"O armario numero {numero} nao foi encontrado no sistema.\n\n"
                        f"VERIFIQUE:\n"
                        f"1. O numero digitado esta correto?\n"
                        f"2. O armario esta cadastrado no sistema?\n"
                        f"3. O armario esta ativo?",
                        parent=self.root)
                    self._entry_busca_armario.focus_set()
                    return
                arm = armario[0]
                uso_status = arm.get("uso_status") or ""
                if uso_status == "ocupado":
                    status = "ocupado"
                elif uso_status == "manutencao":
                    status = "manutencao"
                else:
                    status = "livre"
                self._acao_armario(arm, status)

            self.run_async(buscar, on_found)
        except Exception as e:
            self.show_error(str(e))

    def _atualizar_visual_filtro(self, filtro_ativo):
        """Atualiza o visual dos botoes de filtro, destacando o ativo."""
        try:
            if not hasattr(self, '_filtro_buttons'):
                return
            for key, (btn, cor) in self._filtro_buttons.items():
                if key == filtro_ativo:
                    btn.configure(bg=cor, fg=COR_TEXTO, highlightbackground=cor)
                else:
                    btn.configure(bg=COR_FUNDO3, fg=cor, highlightbackground=COR_FUNDO3)
        except Exception:
            pass

    def _aplicar_filtro_armarios(self, filtro):
        """Aplica o filtro selecionado e re-renderiza os armarios."""
        self._armario_filtro_atual = filtro
        self._atualizar_visual_filtro(filtro)
        self._renderizar_armarios_filtrados()

    def _renderizar_armarios_filtrados(self):
        """Renderiza os armarios no grid aplicando o filtro atual."""
        try:
            if not hasattr(self, 'armarios_frame') or not self.armarios_frame.winfo_exists():
                return
        except Exception:
            return

        for w in self.armarios_frame.winfo_children():
            w.destroy()

        armarios = getattr(self, '_armarios_dados_cache', [])
        if not armarios:
            tk.Label(self.armarios_frame,
                     text="Nenhum armario cadastrado.\nCadastre armarios pelo app APK ou clique em 'Novo Armario'.",
                     bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 12),
                     justify="center").pack(pady=40)
            return

        filtro = getattr(self, '_armario_filtro_atual', 'todos')

        col = 0
        row = 0
        exibidos = 0

        for arm in armarios:
            uso_status = arm.get("uso_status") or ""
            if uso_status == "ocupado":
                status = "ocupado"
            elif uso_status == "manutencao":
                status = "manutencao"
            else:
                status = "livre"

            # Aplicar filtro
            if filtro != "todos" and status != filtro:
                continue

            if status == "livre":
                cor = COR_BOTAO_VERDE
            elif status == "manutencao":
                cor = COR_ALERTA
            else:
                cor = "#9C27B0"

            total = float(arm.get("total_consumo", 0))
            desc = arm.get("descricao") or ""
            loc = arm.get("localizacao") or ""
            text = f"Armario {arm['numero']}\n{status.upper()}"
            if loc:
                loc_short = loc[:18] + ".." if len(loc) > 18 else loc
                text += f"\n{loc_short}"
            elif desc:
                desc_short = desc[:18] + ".." if len(desc) > 18 else desc
                text += f"\n{desc_short}"
            if status == "ocupado":
                text += f"\nR$ {FormatUtils.format_money(total)}"
                cliente = arm.get("cliente_nome") or ""
                if cliente:
                    cli_short = cliente[:15] + ".." if len(cliente) > 15 else cliente
                    text += f"\n{cli_short}"
                if arm.get("data_entrada"):
                    abertura_str = FormatUtils.format_date(arm["data_entrada"])
                    text += f"\n{abertura_str}"
            elif status == "manutencao":
                obs = arm.get("observacao") or ""
                if obs:
                    obs_short = obs[:18] + ".." if len(obs) > 18 else obs
                    text += f"\n{obs_short}"

            btn = tk.Button(self.armarios_frame, text=text, bg=cor, fg=COR_TEXTO,
                           font=("Segoe UI", 10, "bold"), width=14, height=6,
                           relief="flat", cursor="hand2",
                           command=lambda a=arm, s=status: self._acao_armario(a, s))
            btn.grid(row=row, column=col, padx=8, pady=8)

            # Tooltip rico com todos os dados do armario
            RichTooltipArmario(btn, arm, status)

            exibidos += 1
            col += 1
            if col >= 4:
                col = 0
                row += 1

        if exibidos == 0:
            filtro_nome = {"todos": "Todos", "ocupado": "Ocupados", "livre": "Livres", "manutencao": "Manutencao"}.get(filtro, filtro)
            tk.Label(self.armarios_frame,
                     text=f"Nenhum armario com status '{filtro_nome}'.",
                     bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 12),
                     justify="center").pack(pady=40)

        # Atualizar label de contagem do filtro
        try:
            filtro_nome = {"todos": "Todos", "ocupado": "Ocupados", "livre": "Livres", "manutencao": "Manutencao"}.get(filtro, filtro)
            self._filtro_count_label.config(
                text=f"Exibindo: {exibidos} armario(s) [{filtro_nome}]")
        except Exception:
            pass

    def carregar_armarios(self):
        """Carrega armarios diretamente da tabela armarios_sauna (mesma do APK).
        O status e derivado de uso_armario_sauna (como o APK faz).
        Tambem carrega itens de cada armario ocupado para exibir no tooltip."""
        def load():
            db = DatabaseHelper.get_instance()
            armarios = db.execute_query(
                "SELECT a.id, a.numero, a.descricao, a.localizacao, "
                "u.id as uso_id, u.cliente_nome, u.observacao, u.status as uso_status, "
                "u.data_entrada, u.data_saida, "
                "COALESCE(("
                "  SELECT SUM(COALESCE(i.total,0) + COALESCE(i.adicionais_total,0)) "
                "  FROM itens_armario_sauna i WHERE i.uso_armario_id = u.id"
                "), 0) as total_consumo "
                "FROM armarios_sauna a "
                "LEFT JOIN uso_armario_sauna u ON u.armario_id = a.id "
                "  AND u.status IN ('ocupado','manutencao') "
                "WHERE a.ativo = 1 "
                "ORDER BY a.numero"
            )
            # Carregar itens de cada armario ocupado para tooltip
            for arm in (armarios or []):
                uso_id = arm.get("uso_id") or 0
                uso_status = arm.get("uso_status") or ""
                if uso_status == "ocupado" and uso_id > 0:
                    try:
                        itens = db.execute_query(
                            "SELECT descricao_produto, quantidade, preco_unitario, total, "
                            "adicionais_descricao, adicionais_total "
                            "FROM itens_armario_sauna WHERE uso_armario_id = %s ORDER BY id",
                            (uso_id,)
                        )
                        arm["_itens_tooltip"] = itens or []
                    except Exception:
                        arm["_itens_tooltip"] = []
                else:
                    arm["_itens_tooltip"] = []
            return armarios

        def on_loaded(armarios):
            try:
                if not hasattr(self, 'armarios_frame') or not self.armarios_frame.winfo_exists():
                    return
            except Exception:
                return

            # Guardar dados no cache
            self._armarios_dados_cache = armarios or []

            # Calcular contagens totais (independente do filtro)
            livres = 0
            ocupados = 0
            manutencao_count = 0
            for arm in (armarios or []):
                uso_status = arm.get("uso_status") or ""
                if uso_status == "ocupado":
                    ocupados += 1
                elif uso_status == "manutencao":
                    manutencao_count += 1
                else:
                    livres += 1

            # Atualizar resumo e indicador grande
            try:
                self.lbl_resumo_armarios.config(
                    text=f"Livres: {livres} | Ocupados: {ocupados} | Manutencao: {manutencao_count} | Total: {len(armarios or [])}"
                )
            except Exception:
                pass
            try:
                self.lbl_livres_grande.config(text=f"LIVRES: {livres}")
            except Exception:
                pass

            # Atualizar contagens nos botoes de filtro
            try:
                contagens = {"todos": len(armarios or []), "ocupado": ocupados,
                             "livre": livres, "manutencao": manutencao_count}
                for key, (btn, cor) in self._filtro_buttons.items():
                    nome = {"todos": "Todos", "ocupado": "Ocupados", "livre": "Livres", "manutencao": "Manutencao"}[key]
                    btn.config(text=f"  {nome} ({contagens[key]})  ")
            except Exception:
                pass

            # Renderizar com filtro atual
            self._renderizar_armarios_filtrados()

        self.run_async(load, on_loaded)

    def novo_armario(self):
        """Cria novo armario na tabela armarios_sauna (mesma do APK)."""
        num = simpledialog.askinteger("Novo Armario", "Numero:", minvalue=1, parent=self.root)
        if not num:
            return
        desc = simpledialog.askstring("Novo Armario", "Descricao (opcional):",
                                       parent=self.root) or ""
        loc = simpledialog.askstring("Novo Armario", "Localizacao (opcional, ex: Ala A, Andar 1):",
                                      parent=self.root) or ""
        def criar():
            db = DatabaseHelper.get_instance()
            existente = db.execute_query(
                "SELECT id FROM armarios_sauna WHERE numero = %s AND ativo = 1", (num,)
            )
            if existente:
                raise Exception(f"Ja existe um armario ativo com o numero {num}.")
            db.execute_update(
                "INSERT INTO armarios_sauna (numero, descricao, localizacao, ativo) VALUES (%s, %s, %s, 1)",
                (num, desc or None, loc or None)
            )
        self.run_async(criar, lambda _: self.carregar_armarios())

    def _acao_armario(self, arm, status):
        """Acao ao clicar em um armario. Exibe no painel direito em vez de popup."""
        arm_id = arm["id"]
        numero = arm["numero"]
        uso_id = arm.get("uso_id") or 0

        if status == "livre":
            # Exibir opcoes de Entregar Chave / Manutencao no painel direito
            self._limpar_painel_direito_armarios()
            panel = self._armario_right_panel

            tk.Label(panel, text=f"Armario {numero}", bg=COR_FUNDO, fg=COR_PRIMARIA,
                     font=("Segoe UI", 18, "bold")).pack(pady=(20, 5))
            tk.Label(panel, text="LIVRE", bg=COR_FUNDO, fg=COR_BOTAO_VERDE,
                     font=("Segoe UI", 14, "bold")).pack(pady=(0, 10))

            sep = tk.Frame(panel, bg="#37474F", height=1)
            sep.pack(fill="x", padx=20, pady=5)

            tk.Label(panel, text="O que deseja fazer?", bg=COR_FUNDO, fg=COR_TEXTO,
                     font=("Segoe UI", 12)).pack(pady=10)

            # Campo para nome do cliente
            cli_frame = tk.Frame(panel, bg=COR_FUNDO)
            cli_frame.pack(fill="x", padx=30, pady=5)
            tk.Label(cli_frame, text="Cliente (opcional):", bg=COR_FUNDO, fg=COR_TEXTO,
                     font=("Segoe UI", 10)).pack(anchor="w")
            et_cliente = StyledEntry(cli_frame)
            et_cliente.pack(fill="x", pady=(2, 0))
            et_cliente.focus_set()

            btn_frame = tk.Frame(panel, bg=COR_FUNDO)
            btn_frame.pack(fill="x", padx=30, pady=20)

            def entregar_chave():
                cliente = et_cliente.get().strip()
                def abrir():
                    db = DatabaseHelper.get_instance()
                    db.execute_update(
                        "INSERT INTO uso_armario_sauna (armario_id, cliente_nome, status, data_entrada) "
                        "VALUES (%s, %s, 'ocupado', NOW())",
                        (arm_id, cliente or None)
                    )
                def on_aberto(_):
                    self._mostrar_placeholder_direito()
                    self.carregar_armarios()
                self.run_async(abrir, on_aberto)

            def colocar_manutencao():
                """Coloca armario em manutencao.
                SEMPRE exige senha de administrador, independente de permissoes."""
                obs = et_cliente.get().strip()
                def executar_manutencao():
                    def manutencao():
                        db = DatabaseHelper.get_instance()
                        db.execute_update(
                            "INSERT INTO uso_armario_sauna (armario_id, observacao, status, data_entrada) "
                            "VALUES (%s, %s, 'manutencao', NOW())",
                            (arm_id, obs or "Em manutencao")
                        )
                    def on_manutencao(_):
                        self._mostrar_placeholder_direito()
                        self.carregar_armarios()
                    self.run_async(manutencao, on_manutencao)
                self._solicitar_senha_admin_obrigatoria(executar_manutencao, parent_window=self.root)

            StyledButton(btn_frame, text="Entregar Chave", command=entregar_chave,
                         color=COR_BOTAO_VERDE, width=18).pack(side="left", padx=5, pady=5)
            StyledButton(btn_frame, text="Manutencao", command=colocar_manutencao,
                         color=COR_ALERTA, width=18).pack(side="right", padx=5, pady=5)

            # Botao fechar painel
            StyledButton(panel, text="Fechar", command=self._mostrar_placeholder_direito,
                         color="#2a3a5c", width=10).pack(side="bottom", pady=10)

        elif status == "manutencao":
            # Exibir opcao de liberar manutencao no painel direito
            self._limpar_painel_direito_armarios()
            panel = self._armario_right_panel

            tk.Label(panel, text=f"Armario {numero}", bg=COR_FUNDO, fg=COR_PRIMARIA,
                     font=("Segoe UI", 18, "bold")).pack(pady=(20, 5))
            tk.Label(panel, text="MANUTENCAO", bg=COR_FUNDO, fg=COR_ALERTA,
                     font=("Segoe UI", 14, "bold")).pack(pady=(0, 10))

            obs = arm.get("observacao") or ""
            if obs:
                tk.Label(panel, text=f"Obs: {obs}", bg=COR_FUNDO, fg=COR_TEXTO2,
                         font=("Segoe UI", 10)).pack(pady=5)

            sep = tk.Frame(panel, bg="#37474F", height=1)
            sep.pack(fill="x", padx=20, pady=10)

            def liberar():
                if messagebox.askyesno("Manutencao",
                        f"Liberar armario {numero} da manutencao?",
                        parent=self.root):
                    def lib():
                        db = DatabaseHelper.get_instance()
                        db.execute_update(
                            "UPDATE uso_armario_sauna SET status = 'encerrado', data_saida = NOW() "
                            "WHERE armario_id = %s AND status = 'manutencao'",
                            (arm_id,)
                        )
                    def on_lib(_):
                        self._mostrar_placeholder_direito()
                        self.carregar_armarios()
                    self.run_async(lib, on_lib)

            StyledButton(panel, text="Liberar da Manutencao", command=liberar,
                         color=COR_BOTAO_VERDE, width=22).pack(pady=20)

            StyledButton(panel, text="Fechar", command=self._mostrar_placeholder_direito,
                         color="#2a3a5c", width=10).pack(side="bottom", pady=10)

        else:
            # Armario ocupado - mostrar detalhe no painel direito
            self._show_armario_detalhe(arm_id, numero, uso_id,
                                       arm.get("data_entrada"),
                                       arm.get("descricao"),
                                       arm.get("localizacao"),
                                       arm.get("cliente_nome"),
                                       arm.get("observacao"))

    def _show_armario_detalhe(self, arm_id, numero, uso_id, data_entrada=None,
                               descricao=None, localizacao=None, cliente_nome=None,
                               observacao=None):
        """Mostra detalhe do armario ocupado no painel direito (sem popup)."""
        self._limpar_painel_direito_armarios()
        panel = self._armario_right_panel

        titulo_text = f"Armario {numero}"
        if descricao:
            titulo_text += f" - {descricao}"
        if localizacao:
            titulo_text += f" ({localizacao})"
        tk.Label(panel, text=titulo_text, bg=COR_FUNDO, fg="#9C27B0",
                 font=("Segoe UI", 16, "bold")).pack(side="top", pady=(10, 2))

        # Info do uso
        info_text = ""
        if cliente_nome:
            info_text += f"Cliente: {cliente_nome}"
        if data_entrada:
            abertura_str = FormatUtils.format_date(data_entrada)
            if info_text:
                info_text += f"  |  "
            info_text += f"Entrada: {abertura_str}"
        if observacao:
            if info_text:
                info_text += f"  |  "
            info_text += f"Obs: {observacao}"
        if info_text:
            tk.Label(panel, text=info_text, bg=COR_FUNDO, fg=COR_ALERTA,
                     font=("Segoe UI", 10), wraplength=450).pack(side="top", pady=(0, 5))

        # Frame de botoes fixo na parte inferior (pack side=bottom primeiro)
        btn_frame = tk.Frame(panel, bg=COR_FUNDO)
        btn_frame.pack(side="bottom", fill="x", padx=10, pady=(5, 10))

        # Label de total fixo acima dos botoes
        lbl_total = tk.Label(panel, text="Total: R$ 0,00", bg=COR_FUNDO, fg=COR_SUCESSO,
                              font=("Segoe UI", 14, "bold"))
        lbl_total.pack(side="bottom", pady=(5, 5))

        # Frame da treeview com scrollbar (expande no espaco restante)
        tree_frame = tk.Frame(panel, bg=COR_FUNDO)
        tree_frame.pack(side="top", fill="both", expand=True, padx=10, pady=5)

        cols = ("id", "produto", "qtd", "preco", "total", "adicionais")
        tree_scroll = ttk.Scrollbar(tree_frame, orient="vertical")
        tree = ttk.Treeview(tree_frame, columns=cols, show="headings", height=10,
                            yscrollcommand=tree_scroll.set)
        tree_scroll.config(command=tree.yview)
        for c, h, w in [("id","ID",40),("produto","Produto",180),("qtd","Qtd",50),
                         ("preco","Preco",70),("total","Total",70),("adicionais","Adicionais",100)]:
            tree.heading(c, text=h)
            tree.column(c, width=w)
        tree_scroll.pack(side="right", fill="y")
        tree.pack(side="left", fill="both", expand=True)

        def carregar():
            """Carrega itens de itens_armario_sauna (mesma tabela do APK)."""
            def load():
                db = DatabaseHelper.get_instance()
                if not uso_id or uso_id <= 0:
                    return []
                return db.execute_query(
                    "SELECT id, descricao_produto, quantidade, preco_unitario, total, "
                    "adicionais_descricao, adicionais_total, impresso "
                    "FROM itens_armario_sauna WHERE uso_armario_id = %s ORDER BY id",
                    (uso_id,)
                )
            def on_loaded(rows):
                try:
                    if not tree.winfo_exists():
                        return
                except Exception:
                    return
                tree.delete(*tree.get_children())
                total = 0
                for r in rows:
                    item_total = float(r.get("total", 0)) + float(r.get("adicionais_total", 0))
                    total += item_total
                    ad_desc = r.get("adicionais_descricao") or ""
                    tree.insert("", "end", values=(
                        r["id"], r["descricao_produto"], r["quantidade"],
                        FormatUtils.format_money(r["preco_unitario"]),
                        FormatUtils.format_money(item_total),
                        ad_desc
                    ))
                lbl_total.config(text=f"Total: R$ {FormatUtils.format_money(total)}")
            self.run_async(load, on_loaded)

        def add_item():
            self._selecionar_produto_armario_inline(
                uso_id, arm_id, numero, data_entrada, descricao,
                localizacao, cliente_nome, observacao, carregar
            )

        def remover_item():
            sel = tree.selection()
            if not sel:
                messagebox.showwarning(
                    "Selecione um Item",
                    "Nenhum item foi selecionado para remover.\n\n"
                    "Clique sobre o item que deseja remover na lista\n"
                    "e tente novamente.",
                    parent=self.root)
                return
            values = tree.item(sel[0])["values"]
            item_id = values[0]
            item_desc = values[1] if len(values) > 1 else "item"
            if not messagebox.askyesno("Confirmar", f"Remover '{item_desc}'?", parent=self.root):
                return

            def executar_remocao():
                def remover():
                    db = DatabaseHelper.get_instance()
                    try:
                        db.execute_update(
                            "DELETE FROM itens_armario_sauna_adicionais WHERE item_armario_id = %s",
                            (item_id,))
                    except Exception:
                        pass
                    db.execute_update(
                        "DELETE FROM itens_armario_sauna WHERE id = %s", (item_id,))
                self.run_async(remover, lambda _: carregar())

            self._solicitar_senha_admin_obrigatoria(executar_remocao, parent_window=self.root)

        def fechar_armario():
            if messagebox.askyesno("Confirmar",
                    f"Fechar armario {numero}?\nOs itens serao enviados para a tela de vendas para pagamento.",
                    parent=self.root):
                self._fechar_armario_via_venda(arm_id, numero, uso_id, None)

        def devolver_chave_sem_consumo():
            """Devolve a chave sem pagamento (armario sem itens ou cancelamento).
            SEMPRE exige senha de administrador, independente de permissoes."""
            if messagebox.askyesno("Confirmar",
                    f"Devolver chave do armario {numero} sem pagamento?\nTodos os itens serao removidos.",
                    parent=self.root):
                def executar_devolucao():
                    def devolver():
                        db = DatabaseHelper.get_instance()
                        if uso_id and uso_id > 0:
                            try:
                                db.execute_update(
                                    "DELETE FROM itens_armario_sauna_adicionais "
                                    "WHERE item_armario_id IN (SELECT id FROM itens_armario_sauna WHERE uso_armario_id = %s)",
                                    (uso_id,))
                            except Exception:
                                pass
                            db.execute_update(
                                "DELETE FROM itens_armario_sauna WHERE uso_armario_id = %s", (uso_id,))
                            db.execute_update(
                                "UPDATE uso_armario_sauna SET status = 'encerrado', data_saida = NOW() WHERE id = %s",
                                (uso_id,))
                    def on_devolvido(_):
                        self._mostrar_placeholder_direito()
                        self.show_success(f"Armario {numero} liberado!")
                        self.carregar_armarios()
                    self.run_async(devolver, on_devolvido)
                self._solicitar_senha_admin_obrigatoria(executar_devolucao, parent_window=self.root)

        StyledButton(btn_frame, text="Add Item", command=add_item,
                     color=COR_BOTAO_VERDE, width=10).pack(side="left", padx=3)
        StyledButton(btn_frame, text="Remover Item", command=remover_item,
                     color=COR_ALERTA, width=11).pack(side="left", padx=3)
        StyledButton(btn_frame, text="Devolver Chave", command=devolver_chave_sem_consumo,
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=3)
        StyledButton(btn_frame, text="Fechar e Pagar", command=fechar_armario,
                     color=COR_ERRO, width=12).pack(side="right", padx=3)

        carregar()

    def _selecionar_produto_armario_inline(self, uso_id, arm_id, numero, data_entrada,
                                              descricao, localizacao, cliente_nome,
                                              observacao, refresh_callback):
        """Exibe a tela de selecao de produto no painel direito (sem popup).
        Ao selecionar, pede quantidade e adiciona o item, depois volta ao detalhe."""
        self._limpar_painel_direito_armarios()
        panel = self._armario_right_panel

        tk.Label(panel, text="Adicionar Produto", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(pady=(10, 5))
        tk.Label(panel, text=f"Armario {numero}", bg=COR_FUNDO, fg="#9C27B0",
                 font=("Segoe UI", 11)).pack(pady=(0, 5))

        # Busca
        busca_frame = tk.Frame(panel, bg=COR_FUNDO)
        busca_frame.pack(fill="x", padx=15, pady=5)
        tk.Label(busca_frame, text="Buscar:", bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left", padx=(0, 5))
        et_busca = StyledEntry(busca_frame, width=30)
        et_busca.pack(side="left", fill="x", expand=True)
        et_busca.focus_set()

        # Treeview de produtos
        tree_frame = tk.Frame(panel, bg=COR_FUNDO)
        tree_frame.pack(fill="both", expand=True, padx=15, pady=5)

        prod_cols = ("c0", "c1", "c2")
        prod_scroll = ttk.Scrollbar(tree_frame, orient="vertical")
        prod_tree = ttk.Treeview(tree_frame, columns=prod_cols, show="headings", height=12,
                                  yscrollcommand=prod_scroll.set)
        prod_scroll.config(command=prod_tree.yview)
        for c, h, w in [("c0", "ID", 50), ("c1", "Descricao", 250), ("c2", "Preco", 80)]:
            prod_tree.heading(c, text=h)
            prod_tree.column(c, width=w)
        prod_scroll.pack(side="right", fill="y")
        prod_tree.pack(side="left", fill="both", expand=True)

        # Botoes na parte inferior
        btn_frame_sel = tk.Frame(panel, bg=COR_FUNDO)
        btn_frame_sel.pack(fill="x", padx=15, pady=(5, 10))

        def carregar_produtos(filtro=""):
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT id, descricao, preco_venda FROM produtos WHERE ativo = 1 ORDER BY descricao"
                )
            def on_loaded(rows):
                try:
                    if not prod_tree.winfo_exists():
                        return
                except Exception:
                    return
                prod_tree.delete(*prod_tree.get_children())
                for row in rows:
                    values = list(row.values())
                    if filtro:
                        match = any(filtro.lower() in str(v).lower() for v in values)
                        if not match:
                            continue
                    prod_tree.insert("", "end", values=values)
            self.run_async(load, on_loaded)

        et_busca.bind("<KeyRelease>", lambda e: carregar_produtos(et_busca.get()))

        def selecionar_produto():
            sel = prod_tree.selection()
            if not sel:
                messagebox.showwarning(
                    "Selecione um Produto",
                    "Nenhum produto foi selecionado.\n\n"
                    "COMO SELECIONAR:\n"
                    "1. Use o campo de busca para encontrar o produto\n"
                    "2. Digite o nome, codigo ou codigo de barras\n"
                    "3. Clique sobre o produto desejado na lista\n"
                    "4. Ou de um duplo-clique para selecionar rapidamente",
                    parent=self.root)
                return
            values = prod_tree.item(sel[0])["values"]
            # Pedir quantidade
            produto_id, desc_prod, preco = values[0], values[1], float(values[2])
            qtd = simpledialog.askfloat("Quantidade", f"Qtd de {desc_prod}:",
                                         initialvalue=1, minvalue=0.001, parent=self.root)
            if not qtd:
                return
            # Salvar item
            def salvar():
                db = DatabaseHelper.get_instance()
                db.execute_update(
                    "INSERT INTO itens_armario_sauna (uso_armario_id, produto_id, descricao_produto, "
                    "quantidade, preco_unitario, total, adicionais_descricao, adicionais_total, impresso) "
                    "VALUES (%s,%s,%s,%s,%s,%s,NULL,0,0)",
                    (uso_id, produto_id, desc_prod, qtd, preco, qtd * preco)
                )
            def on_salvo(_):
                # Voltar para a tela de detalhe do armario (atualizada)
                self._show_armario_detalhe(arm_id, numero, uso_id, data_entrada,
                                           descricao, localizacao, cliente_nome, observacao)
            self.run_async(salvar, on_salvo)

        prod_tree.bind("<Double-1>", lambda e: selecionar_produto())

        StyledButton(btn_frame_sel, text="▶ Selecionar", command=selecionar_produto,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame_sel, text="Voltar", command=lambda: self._show_armario_detalhe(
                         arm_id, numero, uso_id, data_entrada, descricao,
                         localizacao, cliente_nome, observacao),
                     color="#2a3a5c", width=10).pack(side="right", padx=5)

        carregar_produtos()

    def _add_item_armario_sauna(self, uso_id, row, refresh):
        """Adiciona item em itens_armario_sauna (mesma tabela do APK)."""
        if not uso_id or uso_id <= 0:
            self.show_error("Erro: uso do armario nao encontrado.")
            return
        produto_id, descricao, preco = row[0], row[1], float(row[2])
        qtd = simpledialog.askfloat("Quantidade", f"Qtd de {descricao}:",
                                     initialvalue=1, minvalue=0.001, parent=self.root)
        if not qtd:
            return
        def salvar():
            db = DatabaseHelper.get_instance()
            db.execute_update(
                "INSERT INTO itens_armario_sauna (uso_armario_id, produto_id, descricao_produto, "
                "quantidade, preco_unitario, total, adicionais_descricao, adicionais_total, impresso) "
                "VALUES (%s,%s,%s,%s,%s,%s,NULL,0,0)",
                (uso_id, produto_id, descricao, qtd, preco, qtd * preco)
            )
        self.run_async(salvar, lambda _: refresh())

    def _fechar_armario_via_venda(self, arm_id, numero, uso_id, dialog):
        """Envia os itens do armario para a tela de vendas para pagamento.
        Usa itens_armario_sauna (mesma tabela do APK).
        O armario so sera liberado apos a venda ser confirmada."""
        # === TRAVA DE CAIXA: Verificar se o caixa esta aberto antes de enviar para venda ===
        if not Session.caixa_id:
            AuditLogger.log("VENDA_BLOQUEADA_CAIXA_FECHADO",
                            f"Tentativa de fechar armario {numero} via venda bloqueada - caixa fechado",
                            usuario=Session.user_login)
            ToastManager.error("Venda bloqueada! Abra o caixa antes.", duration=5000)
            messagebox.showwarning(
                "Caixa Fechado - Venda Bloqueada",
                "Nao e possivel faturar o armario com o caixa fechado.\n\n"
                "COMO RESOLVER:\n"
                "1. Acesse o menu 'Caixa' (pressione F3)\n"
                "2. Clique em 'Abrir Caixa'\n"
                "3. Informe o valor de abertura\n"
                "4. Depois volte e tente novamente",
                parent=dialog if dialog else self.root)
            return

        def load_dados():
            db = DatabaseHelper.get_instance()
            if not uso_id or uso_id <= 0:
                return {"itens": [], "data_entrada": None}
            itens = db.execute_query(
                "SELECT produto_id, descricao_produto, quantidade, preco_unitario, "
                "total, adicionais_descricao, adicionais_total "
                "FROM itens_armario_sauna WHERE uso_armario_id = %s ORDER BY id",
                (uso_id,)
            )
            uso_data = db.execute_query(
                "SELECT data_entrada, cliente_nome FROM uso_armario_sauna WHERE id = %s",
                (uso_id,)
            )
            data_entrada = None
            cliente = ""
            if uso_data:
                data_entrada = uso_data[0].get("data_entrada")
                cliente = uso_data[0].get("cliente_nome") or ""
            return {"itens": itens, "data_entrada": data_entrada, "cliente": cliente}

        def on_dados_loaded(dados):
            itens = dados["itens"]
            data_entrada = dados["data_entrada"]
            cliente = dados.get("cliente", "")

            if not itens:
                messagebox.showwarning(
                    "Armario Sem Itens",
                    f"O armario {numero} nao possui itens para faturar.\n\n"
                    f"Adicione produtos ao armario antes de fechar a conta\n"
                    f"ou use 'Devolver Chave' para liberar sem cobranca.",
                    parent=self.root)
                return

            # Fechar o dialog do armario (se existir - compatibilidade)
            if dialog:
                try:
                    dialog.destroy()
                except Exception:
                    pass

            # Abrir a tela de vendas DIRETAMENTE (sem passar pela verificacao
            # assincrona de caixa, pois ja foi verificado no inicio desta funcao).
            # Isso garante que a UI e renderizada de forma SINCRONA antes de
            # popular o carrinho, evitando que _show_venda_ui() limpe os dados.
            self._show_venda_ui()

            try:
                self.venda_cliente_id = 0
                self.venda_cliente_nome = cliente.strip() if cliente else "Cliente nao informado"
                if hasattr(self, "lbl_cliente_venda") and self.lbl_cliente_venda.winfo_exists():
                    self.lbl_cliente_venda.config(text=self.venda_cliente_nome)
            except Exception:
                pass

            # Salvar referencia do armario pendente APOS _show_venda_ui
            self._armario_pendente = {
                "arm_id": arm_id,
                "numero": numero,
                "uso_id": uso_id
            }

            # Popular o carrinho com os itens do armario
            for item_data in itens:
                item = ItemVenda()
                item.produto_id = item_data["produto_id"]
                item.descricao_produto = item_data["descricao_produto"]
                item.quantidade = float(item_data["quantidade"])
                item.preco_unitario = float(item_data["preco_unitario"])
                item_total = float(item_data["total"])
                ad_total = float(item_data.get("adicionais_total", 0) or 0)
                item.total = item_total + ad_total
                if ad_total > 0 and item.quantidade > 0:
                    item.preco_unitario = item.total / item.quantidade
                self.carrinho.append(item)

            # Atualizar a treeview do carrinho e totais
            self.atualizar_tree_carrinho()

            # Colocar observacao indicando origem com data de abertura
            try:
                self.et_obs_venda.delete("1.0", tk.END)
                obs_text = f"Armario Sauna {numero}"
                if cliente:
                    obs_text += f" | Cliente: {cliente}"
                if data_entrada:
                    abertura_str = FormatUtils.format_date(data_entrada)
                    obs_text += f" | Entrada: {abertura_str}"
                self.et_obs_venda.insert("1.0", obs_text)
            except Exception:
                pass

        self.run_async(load_dados, on_dados_loaded)

    # ========================================================================
    # CONTAS A RECEBER
    # ========================================================================
    def show_contas_receber(self):
        self.clear_container()

        top = self._create_top_bar(f"{Icons.MONEY} Contas a Receber")

        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # Filtros
        filtro_card = tk.Frame(content, bg=COR_GLASS,
                               highlightthickness=1,
                               highlightbackground=COR_GLASS_BORDER)
        filtro_card.pack(fill="x", pady=(0, 6))

        tk.Label(filtro_card, text="Status:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 9)).pack(side="left", padx=(10, 4), pady=6)
        combo_status_c = ttk.Combobox(filtro_card,
                                       values=["Todos", "pendente", "parcial", "quitada"],
                                       width=10, state="readonly",
                                       font=("Segoe UI", 9))
        combo_status_c.current(0)
        combo_status_c.pack(side="left", padx=4)

        tk.Label(filtro_card, text="Buscar:", bg=COR_GLASS,
                 fg=COR_TEXTO2, font=("Segoe UI", 9)).pack(side="left", padx=(8, 4))
        et_busca_c = StyledEntry(filtro_card, width=20)
        et_busca_c.pack(side="left", padx=4)

        lbl_total_c = tk.Label(filtro_card, text="", bg=COR_GLASS,
                                fg=COR_ERRO, font=("Segoe UI", 9, "bold"))
        lbl_total_c.pack(side="right", padx=12)

        # Barra de botoes FIXADA no rodape (empacotada antes da tabela para
        # ficar sempre visivel, mesmo em telas menores).
        btn_frame = tk.Frame(content, bg=COR_FUNDO)
        btn_frame.pack(side="bottom", fill="x", pady=5)

        StyledButton(btn_frame, text=f"{Icons.MONEY} Receber Pagamento",
                     command=self.receber_conta,
                     color=COR_BOTAO_VERDE, width=20).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Registrar pagamento da conta selecionada")

        StyledButton(btn_frame, text=f"{Icons.REFRESH} Atualizar",
                     command=lambda: _carregar(),
                     color=COR_FUNDO3, width=10).pack(side="left", padx=5)

        StyledButton(btn_frame, text=f"{Icons.EXPORT} Exportar CSV",
                     command=lambda: DataExporter.export_treeview_csv(
                         self.tree_contas, "contas_receber", parent=self.root),
                     color=COR_BOTAO_AZUL, width=14).pack(side="left", padx=5)

        StyledButton(btn_frame, text=f"{Icons.IMPRESSORA} Imprimir Saldo Cliente",
                     command=self.imprimir_saldo_cliente,
                     color=COR_BOTAO_LARANJA, width=22).pack(side="left", padx=5)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Imprime o extrato/saldo de todas as contas do cliente selecionado")

        # Treeview
        tree_frame = tk.Frame(content, bg=COR_FUNDO)
        tree_frame.pack(fill="both", expand=True)

        cols = ("id", "cliente", "valor_original", "valor_pago", "valor_pendente", "status", "vencimento")
        self.tree_contas = ttk.Treeview(tree_frame, columns=cols,
                                         show="headings", height=17)
        for c, h, w in [
            ("id",             "ID",             50),
            ("cliente",        "Cliente",        190),
            ("valor_original", "Valor Original", 115),
            ("valor_pago",     "Valor Pago",     110),
            ("valor_pendente", "Pendente",        110),
            ("status",         "Status",          90),
            ("vencimento",     "Vencimento",      110),
        ]:
            self.tree_contas.heading(c, text=h,
                command=lambda col=c: self._sort_tree(self.tree_contas, col, False))
            self.tree_contas.column(c, width=w,
                anchor="center" if c in ("id", "status", "vencimento") else "e")

        vsb_c = ttk.Scrollbar(tree_frame, orient="vertical",
                               command=self.tree_contas.yview)
        self.tree_contas.configure(yscrollcommand=vsb_c.set)
        self.tree_contas.pack(side="left", fill="both", expand=True)
        vsb_c.pack(side="right", fill="y")

        _cache_c = [None]

        def _preencher_contas(rows, filtro="", status_f="Todos"):
            # Preservar selecao
            sel = self.tree_contas.selection()
            sel_id = None
            if sel:
                try:
                    sel_id = self.tree_contas.item(sel[0])["values"][0]
                except Exception:
                    pass
            self.tree_contas.delete(*self.tree_contas.get_children())
            tot_pend = 0.0
            new_sel_iid = None
            for i, r in enumerate(rows):
                st = r.get("status", "")
                cli = r.get("cliente_nome", "N/A") or "N/A"
                pend = float(r.get("valor_pendente", 0))
                venc = str(r.get("data_vencimento", "")) or ""
                if venc and len(venc) >= 10:
                    try:
                        venc = datetime.datetime.strptime(
                            venc[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
                    except Exception:
                        pass

                if status_f != "Todos" and st != status_f:
                    continue
                if filtro and filtro.lower() not in cli.lower():
                    continue

                tag = "quitada" if st == "quitada" else (
                    "even" if i % 2 == 0 else "odd")
                iid = self.tree_contas.insert("", "end", values=(
                    r["id"], cli,
                    FormatUtils.format_money(r["valor_original"]),
                    FormatUtils.format_money(r["valor_pago"]),
                    FormatUtils.format_money(pend),
                    st, venc
                ), tags=(tag,))
                if sel_id is not None and r["id"] == sel_id:
                    new_sel_iid = iid
                if st in ("pendente", "parcial"):
                    tot_pend += pend
            if new_sel_iid:
                self.tree_contas.selection_set(new_sel_iid)
                self.tree_contas.see(new_sel_iid)

            self.tree_contas.tag_configure("even", background=COR_GLASS)
            self.tree_contas.tag_configure("odd",  background=COR_CARD)
            self.tree_contas.tag_configure(
                "quitada", background="#0a1a0a", foreground="#44bb44")
            lbl_total_c.config(
                text=f"Pendente Total: R$ {FormatUtils.format_money(tot_pend)}")

        def _carregar():
            def load():
                db = DatabaseHelper.get_instance()
                return db.execute_query(
                    "SELECT cr.id, c.nome as cliente_nome, cr.valor_original, "
                    "cr.valor_pago, cr.valor_pendente, cr.status, "
                    "cr.data_vencimento "
                    "FROM contas_receber cr "
                    "LEFT JOIN clientes c ON cr.cliente_id = c.id "
                    "ORDER BY cr.id DESC"
                )
            def on_loaded(rows):
                _cache_c[0] = rows
                _preencher_contas(rows,
                                  et_busca_c.get().strip(),
                                  combo_status_c.get())
            self.run_async(load, on_loaded)

        combo_status_c.bind("<<ComboboxSelected>>",
            lambda e: _preencher_contas(
                _cache_c[0] or [],
                et_busca_c.get().strip(),
                combo_status_c.get()))
        et_busca_c.bind("<KeyRelease>",
            lambda e: _preencher_contas(
                _cache_c[0] or [],
                et_busca_c.get().strip(),
                combo_status_c.get()))

        StyledButton(filtro_card, text=f"{Icons.SEARCH} Buscar",
                     command=_carregar,
                     color=COR_BOTAO_AZUL, width=8).pack(side="left", padx=6, pady=6)

        _carregar()

        # Auto-refresh em tempo real (a cada 12 segundos)
        self._start_auto_refresh("contas_receber", _carregar, 12000)



    def receber_conta(self):
        sel = self.tree_contas.selection()
        if not sel:
            ToastManager.warning("Selecione uma conta.")
            return
        if not Session.caixa_id:
            self.show_error(
                "Abra o caixa antes de receber.\n\n"
                "O recebimento da conta entra como CREDITO (entrada) no caixa "
                "atual, por isso e necessario ter um caixa aberto.")
            return
        conta_id = self.tree_contas.item(sel[0])["values"][0]
        cliente_nome = self.tree_contas.item(sel[0])["values"][1]
        pendente = FormatUtils.parse_money(
            str(self.tree_contas.item(sel[0])["values"][4]))
        if pendente <= 0:
            ToastManager.info("Esta conta ja esta quitada.")
            return
        valor = simpledialog.askfloat(
            "Receber Pagamento",
            f"Cliente: {cliente_nome}\n"
            f"Valor pendente: R$ {FormatUtils.format_money(pendente)}\n\n"
            f"Valor a receber (R$):",
            initialvalue=pendente, minvalue=0.01, parent=self.root)
        if not valor:
            return

        caixa_id_receb = Session.caixa_id

        def receber():
            db = DatabaseHelper.get_instance()
            novo_pendente = pendente - valor
            status = "quitada" if novo_pendente <= 0.009 else "parcial"
            db.execute_update(
                "UPDATE contas_receber SET "
                "valor_pago = valor_pago + %s, "
                "valor_pendente = GREATEST(0, valor_pendente - %s), "
                "status = %s "
                "WHERE id = %s",
                (valor, valor, status, conta_id)
            )
            # Entrada (credito positivo) no caixa referente ao recebimento
            db.execute_update(
                "INSERT INTO entradas_caixa (caixa_id, descricao, valor, origem, "
                "conta_receber_id, operador_nome) VALUES (%s,%s,%s,'conta_receber',%s,%s)",
                (caixa_id_receb,
                 f"Recebimento conta #{conta_id} - {cliente_nome}",
                 valor, conta_id, getattr(Session, "user_nome", "") or "")
            )
            return status

        def on_recebido(status):
            msg = (f"Recebido R$ {FormatUtils.format_money(valor)} "
                   f"(credito no caixa)!"
                   + (" Conta QUITADA!" if status == "quitada" else ""))
            ToastManager.success(msg)
            AuditLogger.log("CONTA_RECEBIDA",
                            f"Conta #{conta_id} | Cliente: {cliente_nome} | "
                            f"Valor: R$ {FormatUtils.format_money(valor)} | Status: {status} | "
                            f"Entrada no caixa #{caixa_id_receb}",
                            usuario=Session.user_login)
            self.show_contas_receber()

        self.run_async(receber, on_recebido)

    def imprimir_saldo_cliente(self):
        """Imprime o extrato/saldo de todas as contas a receber do cliente
        da conta selecionada (usa o sistema de impressao central: respeita
        tamanho de fonte, logo, corte e servidor .fr3)."""
        sel = self.tree_contas.selection()
        if not sel:
            ToastManager.warning(
                "Selecione uma conta do cliente para imprimir o saldo.")
            return
        try:
            conta_id = self.tree_contas.item(sel[0])["values"][0]
        except Exception:
            ToastManager.warning("Selecao invalida.")
            return

        def tarefa():
            db = DatabaseHelper.get_instance()
            base = db.execute_query(
                "SELECT cr.cliente_id, COALESCE(c.nome, 'N/A') as cliente_nome "
                "FROM contas_receber cr LEFT JOIN clientes c ON cr.cliente_id = c.id "
                "WHERE cr.id = %s", (conta_id,))
            if not base:
                return (False, "Conta nao encontrada.")
            cid = base[0].get("cliente_id")
            nome = base[0].get("cliente_nome") or "N/A"
            if cid:
                contas = db.execute_query(
                    "SELECT cr.id, cr.valor_original, cr.valor_pago, "
                    "cr.valor_pendente, cr.status, cr.data_vencimento "
                    "FROM contas_receber cr WHERE cr.cliente_id = %s "
                    "ORDER BY cr.id", (cid,))
            else:
                contas = db.execute_query(
                    "SELECT cr.id, cr.valor_original, cr.valor_pago, "
                    "cr.valor_pendente, cr.status, cr.data_vencimento "
                    "FROM contas_receber cr LEFT JOIN clientes c "
                    "ON cr.cliente_id = c.id WHERE COALESCE(c.nome,'N/A') = %s "
                    "ORDER BY cr.id", (nome,))
            if not contas:
                return (False, "Nenhuma conta encontrada para o cliente.")
            texto = self._montar_texto_saldo_cliente(nome, contas)
            return self._imprimir_texto(texto)

        def on_done(res):
            ok, msg = res if res else (False, "Falha ao imprimir.")
            if ok:
                ToastManager.success("Saldo do cliente enviado para impressao.")
            else:
                self.show_error(msg)

        self.run_async(tarefa, on_done)

    def _montar_texto_saldo_cliente(self, nome, contas):
        """Monta o texto do extrato de saldo do cliente para impressao."""
        cfg = self._load_printer_config()
        try:
            largura = int(cfg.get("largura_papel", 48) or 48)
        except Exception:
            largura = 48
        largura = max(24, min(largura, 64))
        linha = "=" * largura
        tracos = "-" * largura

        def centro(t):
            t = str(t)[:largura]
            return t.center(largura)

        L = []
        L.append(centro("SALDO DO CLIENTE"))
        L.append(linha)
        L.append(f"Cliente: {nome}")
        L.append("Data: " + datetime.datetime.now().strftime("%d/%m/%Y %H:%M"))
        L.append(tracos)

        tot_orig = tot_pago = tot_pend = 0.0
        for c in contas:
            vo = float(c.get("valor_original", 0) or 0)
            vp = float(c.get("valor_pago", 0) or 0)
            pe = float(c.get("valor_pendente", 0) or 0)
            st = c.get("status", "") or ""
            venc = str(c.get("data_vencimento", "") or "")
            if venc and len(venc) >= 10:
                try:
                    venc = datetime.datetime.strptime(
                        venc[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
                except Exception:
                    pass
            tot_orig += vo
            tot_pago += vp
            tot_pend += pe
            L.append(f"Conta #{c.get('id')}  [{st}]")
            L.append(f"  Original: R$ {FormatUtils.format_money(vo)}")
            L.append(f"  Pago....: R$ {FormatUtils.format_money(vp)}")
            L.append(f"  Pendente: R$ {FormatUtils.format_money(pe)}")
            if venc and venc.lower() != "none":
                L.append(f"  Vencimento: {venc}")
            L.append(tracos)

        L.append(f"TOTAL ORIGINAL : R$ {FormatUtils.format_money(tot_orig)}")
        L.append(f"TOTAL PAGO.....: R$ {FormatUtils.format_money(tot_pago)}")
        L.append(f"SALDO DEVEDOR..: R$ {FormatUtils.format_money(tot_pend)}")
        L.append(linha)
        L.append(centro("Documento nao fiscal"))
        return "\n".join(L)



    # ========================================================================
    # ENTRADA DE NOTAS (ESTOQUE)
    # ========================================================================
    def show_entrada_notas(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Entrada de Notas / Estoque", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=10)

        card = CardFrame(content)
        card.pack(fill="x", pady=10)

        tk.Label(card, text="Selecione o produto e informe a quantidade a dar entrada:",
                 bg=COR_CARD, fg=COR_TEXTO, font=("Segoe UI", 11)).pack(anchor="w", pady=5)

        form = tk.Frame(card, bg=COR_CARD)
        form.pack(fill="x", pady=5)

        tk.Label(form, text="Produto:", bg=COR_CARD, fg=COR_TEXTO).grid(row=0, column=0, sticky="w", padx=5)
        self.lbl_produto_entrada = tk.Label(form, text="Nenhum selecionado", bg=COR_CARD,
                                             fg=COR_ALERTA, font=("Segoe UI", 10, "bold"))
        self.lbl_produto_entrada.grid(row=0, column=1, sticky="w", padx=5)
        self._entrada_produto_id = None

        StyledButton(form, text="▶ Selecionar", command=self._selecionar_produto_entrada,
                     color=COR_BOTAO_AZUL, width=10).grid(row=0, column=2, padx=5)

        tk.Label(form, text="Quantidade:", bg=COR_CARD, fg=COR_TEXTO).grid(row=1, column=0, sticky="w", padx=5, pady=5)
        self.et_qtd_entrada = StyledEntry(form, width=15)
        self.et_qtd_entrada.grid(row=1, column=1, sticky="w", padx=5, pady=5)

        tk.Label(form, text="Custo Unit.:", bg=COR_CARD, fg=COR_TEXTO).grid(row=2, column=0, sticky="w", padx=5)
        self.et_custo_entrada = StyledEntry(form, width=15)
        self.et_custo_entrada.grid(row=2, column=1, sticky="w", padx=5)

        def dar_entrada():
            if not self._entrada_produto_id:
                self.show_error("Por favor, selecione um produto na lista.\n\nDigite o nome, codigo ou codigo de barras para buscar\ne clique sobre o produto desejado.")
                return
            try:
                qtd = float(self.et_qtd_entrada.get().replace(",", "."))
            except ValueError:
                self.show_error("Quantidade invalida.")
                return
            custo = FormatUtils.parse_money(self.et_custo_entrada.get())

            def salvar():
                db = DatabaseHelper.get_instance()
                db.execute_update(
                    "UPDATE produtos SET estoque = estoque + %s WHERE id = %s",
                    (qtd, self._entrada_produto_id)
                )
                if custo > 0:
                    db.execute_update(
                        "UPDATE produtos SET preco_custo = %s WHERE id = %s",
                        (custo, self._entrada_produto_id)
                    )
                db.execute_update(
                    "INSERT INTO entrada_notas (produto_id, quantidade, custo_unitario, usuario_id) "
                    "VALUES (%s,%s,%s,%s)",
                    (self._entrada_produto_id, qtd, custo, Session.user_id)
                )

            def on_saved(_):
                self.show_success(f"Entrada de {qtd} unidades realizada!")
                self._entrada_produto_id = None
                self.lbl_produto_entrada.config(text="Nenhum selecionado")
                self.et_qtd_entrada.delete(0, tk.END)
                self.et_custo_entrada.delete(0, tk.END)

            self.run_async(salvar, on_saved)

        StyledButton(card, text="Dar Entrada", command=dar_entrada,
                     color=COR_BOTAO_VERDE, width=15).pack(pady=10)

    def _selecionar_produto_entrada(self):
        self._selecionar_registro(
            "produtos", "Selecionar Produto",
            "SELECT id, descricao, estoque, preco_custo FROM produtos WHERE ativo = 1 ORDER BY descricao",
            ["ID", "Descricao", "Estoque", "Custo"],
            lambda row: self._set_produto_entrada(row) if row else None
        )

    def _set_produto_entrada(self, row):
        self._entrada_produto_id = row[0]
        self.lbl_produto_entrada.config(text=f"{row[1]} (Estoque: {row[2]})")

    # ========================================================================
    # RELATORIOS
    # ========================================================================
    def show_relatorios(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text=f"{Icons.RELATORIOS} Relatorios Gerenciais",
                 bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=10)

        # Filtros de data
        filtro_frame = CardFrame(content)
        filtro_frame.pack(fill="x", pady=8)

        tk.Label(filtro_frame, text="Data Inicio:", bg=COR_CARD,
                 fg=COR_TEXTO).pack(side="left", padx=5)
        # "Hoje" baseado na data do SERVIDOR MySQL (mesma fonte de CURDATE e do
        # data_venda), evitando divergencia com a tela "Total de Vendas Hoje"
        # quando o relogio/fuso do terminal difere do servidor.
        try:
            _rel_hoje_dt = LicencaManager._obter_hora_mysql() or datetime.date.today()
        except Exception:
            _rel_hoje_dt = datetime.date.today()
        self._rel_hoje_dt = _rel_hoje_dt
        _rel_hoje_str = _rel_hoje_dt.strftime("%d/%m/%Y")
        self.et_data_ini = StyledEntry(filtro_frame, width=12)
        self.et_data_ini.insert(0, _rel_hoje_str)
        self.et_data_ini.pack(side="left", padx=5)
        add_tooltip(self.et_data_ini, "Data inicial (DD/MM/AAAA)")

        tk.Label(filtro_frame, text="Data Fim:", bg=COR_CARD,
                 fg=COR_TEXTO).pack(side="left", padx=5)
        self.et_data_fim = StyledEntry(filtro_frame, width=12)
        self.et_data_fim.insert(0, _rel_hoje_str)
        self.et_data_fim.pack(side="left", padx=5)
        add_tooltip(self.et_data_fim, "Data final (DD/MM/AAAA)")

        # Botoes de periodo rapido
        def _set_periodo(dias):
            fim = getattr(self, "_rel_hoje_dt", None) or datetime.date.today()
            ini = fim - datetime.timedelta(days=dias)
            self.et_data_ini.delete(0, tk.END)
            self.et_data_ini.insert(0, ini.strftime("%d/%m/%Y"))
            self.et_data_fim.delete(0, tk.END)
            self.et_data_fim.insert(0, fim.strftime("%d/%m/%Y"))

        for label, dias in [("Hoje", 0), ("7 dias", 7),
                              ("30 dias", 30), ("Mes", 30)]:
            btn = StyledButton(filtro_frame, text=label, width=7,
                               command=lambda d=dias: _set_periodo(d),
                               color=COR_FUNDO3)
            btn.pack(side="left", padx=3)

        relatorios = [
            ("Vendas por Periodo",   self._rel_vendas_periodo,       COR_BOTAO_AZUL),
            ("Vendas por Produto",   self._rel_vendas_produto,        COR_BOTAO_AZUL),
            ("Vendas por Forma Pgto",self._rel_vendas_forma_pgto,     COR_BOTAO_AZUL),
            ("Vendas por Vendedor",  self._rel_vendas_vendedor,       COR_BOTAO_AZUL),
            ("Vendas por Cliente",   self._rel_vendas_cliente,        COR_BOTAO_AZUL),
            ("Mais Vendidos",        self._rel_produtos_mais_vendidos, "#7c3aed"),
            ("Estoque Atual",        self._rel_estoque,               COR_BOTAO_VERDE),
            ("Estoque Critico",      self._rel_estoque_minimo,        COR_ALERTA),
            ("Lucro por Produto",    self._rel_lucro_produto,         COR_SUCESSO),
            ("Resumo Diario",        self._rel_resumo_diario,         "#f72585"),
            ("Reimprimir Cupom",     self._rel_reimprimir_cupom,      COR_BOTAO_LARANJA),
            ("Reimpr. Fech. Caixa",  self._rel_reimprimir_fechamento, COR_BOTAO_LARANJA),
            ("Produtos Vendidos",    self._rel_impressao_produtos_vendidos, "#6366f1"),
            ("Fatur. Forma Pgto",    self._rel_impressao_faturamento_pgto,  "#0891b2"),
        ]

        grid = tk.Frame(content, bg=COR_FUNDO)
        grid.pack(fill="x", pady=6)
        col = 0
        row = 0
        for text, cmd, cor in relatorios:
            btn = StyledButton(grid, text=text, command=cmd,
                               color=cor, width=20)
            btn.grid(row=row, column=col, padx=5, pady=4)
            col += 1
            if col >= 5:
                col = 0
                row += 1
        for c in range(5):
            grid.columnconfigure(c, weight=1)

        # Barra de acoes do relatorio
        acoes_frame = tk.Frame(content, bg=COR_FUNDO)
        acoes_frame.pack(fill="x", pady=(0, 4))

        StyledButton(acoes_frame, text=f"{Icons.EXPORT} Exportar TXT",
                     command=lambda: DataExporter.export_text(
                         self.text_relatorio.get("1.0", "end"),
                         "relatorio", parent=self.root),
                     color=COR_BOTAO_VERDE, width=16).pack(side="left", padx=4)
        add_tooltip(acoes_frame.winfo_children()[-1],
                    "Salvar relatorio exibido como arquivo .txt")

        StyledButton(acoes_frame, text=f"{Icons.COPY} Copiar",
                     command=lambda: (
                         self.root.clipboard_clear(),
                         self.root.clipboard_append(
                             self.text_relatorio.get("1.0", "end")),
                         ToastManager.success("Relatorio copiado!")),
                     color=COR_FUNDO3, width=10).pack(side="left", padx=4)

        StyledButton(acoes_frame, text=f"{Icons.PRINT} Imprimir",
                     command=lambda: self._imprimir_relatorio_exibido(),
                     color=COR_PRIMARIA, width=12).pack(side="left", padx=4)
        add_tooltip(acoes_frame.winfo_children()[-1],
                    "Imprimir o relatorio exibido na impressora configurada")

        # Area de resultado
        self.text_relatorio = scrolledtext.ScrolledText(
            content, bg=COR_ENTRADA, fg=COR_TEXTO,
            font=("Courier New", 10), state="disabled",
            height=18)
        self.text_relatorio.pack(fill="both", expand=True, pady=6)



    def _get_datas_filtro(self):
        try:
            di = self.et_data_ini.get().strip()
            df = self.et_data_fim.get().strip()
            data_ini = datetime.datetime.strptime(di, "%d/%m/%Y").strftime("%Y-%m-%d 00:00:00")
            data_fim = datetime.datetime.strptime(df, "%d/%m/%Y").strftime("%Y-%m-%d 23:59:59")
            return data_ini, data_fim
        except Exception:
            self.show_error("Formato de data invalido. Use DD/MM/AAAA.")
            return None, None

    def _exibir_relatorio(self, titulo, linhas):
        self.text_relatorio.config(state="normal")
        self.text_relatorio.delete("1.0", "end")
        sep = "=" * 60
        self.text_relatorio.insert("end", f"{sep}\n{titulo}\n{sep}\n\n")
        for linha in linhas:
            self.text_relatorio.insert("end", linha + "\n")
        self.text_relatorio.config(state="disabled")

    def _rel_vendas_periodo(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT v.id, COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'N/A') as cliente, v.total_liquido, v.data_venda, v.status "
                "FROM vendas v LEFT JOIN clientes c ON v.cliente_id = c.id "
                "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                "WHERE v.data_venda BETWEEN %s AND %s ORDER BY v.data_venda", (di, df)
            )
        def on_loaded(rows):
            linhas = []
            total = 0
            for r in rows:
                linhas.append(
                    f"#{r['id']:>5} | {r.get('cliente','N/A'):<25} | "
                    f"R$ {FormatUtils.format_money(r['total_liquido']):>10} | "
                    f"{FormatUtils.format_date(r['data_venda'])} | {r['status']}"
                )
                if r["status"] == "finalizada":
                    total += float(r["total_liquido"])
            linhas.append(f"\nTotal de vendas finalizadas: {len([r for r in rows if r['status']=='finalizada'])}")
            linhas.append(f"Valor total: R$ {FormatUtils.format_money(total)}")
            self._exibir_relatorio("VENDAS POR PERIODO", linhas)
        self.run_async(load, on_loaded)

    def _rel_vendas_produto(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT iv.descricao_produto, SUM(iv.quantidade) as qtd, SUM(iv.total) as total "
                "FROM itens_venda iv JOIN vendas v ON iv.venda_id = v.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY iv.descricao_produto ORDER BY total DESC", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Produto':<30} {'Qtd':>8} {'Total':>12}"]
            linhas.append("-" * 52)
            for r in rows:
                linhas.append(
                    f"{r['descricao_produto']:<30} {r['qtd']:>8.0f} R$ {FormatUtils.format_money(r['total']):>10}"
                )
            self._exibir_relatorio("VENDAS POR PRODUTO", linhas)
        self.run_async(load, on_loaded)

    def _rel_vendas_forma_pgto(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT fp.descricao, COUNT(*) as qtd, SUM(pv.valor) as total "
                "FROM pagamentos_venda pv JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "JOIN vendas v ON pv.venda_id = v.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY fp.id, fp.descricao ORDER BY total DESC", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Forma Pagamento':<25} {'Qtd':>6} {'Total':>12}"]
            linhas.append("-" * 45)
            for r in rows:
                linhas.append(f"{r['descricao']:<25} {r['qtd']:>6} R$ {FormatUtils.format_money(r['total']):>10}")
            self._exibir_relatorio("VENDAS POR FORMA DE PAGAMENTO", linhas)
        self.run_async(load, on_loaded)

    def _rel_vendas_vendedor(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT COALESCE(vd.nome,'Sem vendedor') as vendedor, COUNT(*) as qtd, "
                "SUM(v.total_liquido) as total FROM vendas v "
                "LEFT JOIN vendedores vd ON v.vendedor_id = vd.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY v.vendedor_id, vd.nome ORDER BY total DESC", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Vendedor':<25} {'Qtd':>6} {'Total':>12}"]
            linhas.append("-" * 45)
            for r in rows:
                linhas.append(f"{r['vendedor']:<25} {r['qtd']:>6} R$ {FormatUtils.format_money(r['total']):>10}")
            self._exibir_relatorio("VENDAS POR VENDEDOR", linhas)
        self.run_async(load, on_loaded)

    def _rel_vendas_cliente(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT COALESCE(NULLIF(v.cliente_nome,''), NULLIF(u.cliente_nome,''), c.nome,'Sem cliente') as cliente, COUNT(*) as qtd, "
                "SUM(v.total_liquido) as total FROM vendas v "
                "LEFT JOIN clientes c ON v.cliente_id = c.id "
                "LEFT JOIN uso_armario_sauna u ON v.uso_armario_id = u.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY cliente ORDER BY total DESC", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Cliente':<30} {'Qtd':>6} {'Total':>12}"]
            linhas.append("-" * 50)
            for r in rows:
                linhas.append(f"{r['cliente']:<30} {r['qtd']:>6} R$ {FormatUtils.format_money(r['total']):>10}")
            self._exibir_relatorio("VENDAS POR CLIENTE", linhas)
        self.run_async(load, on_loaded)

    def _rel_produtos_mais_vendidos(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT iv.descricao_produto, SUM(iv.quantidade) as qtd "
                "FROM itens_venda iv JOIN vendas v ON iv.venda_id = v.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY iv.descricao_produto ORDER BY qtd DESC LIMIT 50", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'#':>3} {'Produto':<30} {'Qtd Vendida':>12}"]
            linhas.append("-" * 47)
            for i, r in enumerate(rows, 1):
                linhas.append(f"{i:>3} {r['descricao_produto']:<30} {r['qtd']:>12.0f}")
            self._exibir_relatorio("PRODUTOS MAIS VENDIDOS", linhas)
        self.run_async(load, on_loaded)

    def _rel_estoque(self):
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT descricao, codigo, estoque, preco_custo, preco_venda "
                "FROM produtos WHERE ativo = 1 ORDER BY descricao"
            )
        def on_loaded(rows):
            linhas = [f"{'Produto':<30} {'Codigo':<12} {'Estoque':>8} {'Custo':>10} {'Venda':>10}"]
            linhas.append("-" * 72)
            for r in rows:
                linhas.append(
                    f"{r['descricao']:<30} {r.get('codigo',''):<12} {r['estoque']:>8.1f} "
                    f"R${FormatUtils.format_money(r['preco_custo']):>8} R${FormatUtils.format_money(r['preco_venda']):>8}"
                )
            self._exibir_relatorio("ESTOQUE ATUAL", linhas)
        self.run_async(load, on_loaded)

    def _rel_estoque_minimo(self):
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT descricao, estoque, estoque_minimo FROM produtos "
                "WHERE ativo = 1 AND estoque <= estoque_minimo AND estoque_minimo > 0 "
                "ORDER BY estoque ASC"
            )
        def on_loaded(rows):
            if not rows:
                linhas = ["Nenhum produto abaixo do estoque minimo."]
            else:
                linhas = [f"{'Produto':<30} {'Estoque':>8} {'Minimo':>8}"]
                linhas.append("-" * 48)
                for r in rows:
                    linhas.append(f"{r['descricao']:<30} {r['estoque']:>8.1f} {r['estoque_minimo']:>8.1f}")
            self._exibir_relatorio("PRODUTOS ABAIXO DO ESTOQUE MINIMO", linhas)
        self.run_async(load, on_loaded)

    def _rel_lucro_produto(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT iv.descricao_produto, SUM(iv.quantidade) as qtd, "
                "SUM(iv.total) as receita, "
                "SUM(iv.quantidade * p.preco_custo) as custo "
                "FROM itens_venda iv JOIN vendas v ON iv.venda_id = v.id "
                "LEFT JOIN produtos p ON iv.produto_id = p.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY iv.produto_id, iv.descricao_produto, p.preco_custo ORDER BY (SUM(iv.total) - SUM(iv.quantidade * COALESCE(p.preco_custo,0))) DESC",
                (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Produto':<25} {'Receita':>10} {'Custo':>10} {'Lucro':>10} {'Margem':>8}"]
            linhas.append("-" * 65)
            for r in rows:
                receita = float(r["receita"])
                custo = float(r["custo"]) if r["custo"] else 0
                lucro = receita - custo
                margem = (lucro / receita * 100) if receita > 0 else 0
                linhas.append(
                    f"{r['descricao_produto']:<25} R${FormatUtils.format_money(receita):>8} "
                    f"R${FormatUtils.format_money(custo):>8} R${FormatUtils.format_money(lucro):>8} "
                    f"{margem:>6.1f}%"
                )
            self._exibir_relatorio("LUCRO POR PRODUTO", linhas)
        self.run_async(load, on_loaded)

    def _rel_resumo_diario(self):
        di, df = self._get_datas_filtro()
        if not di:
            return
        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT DATE(data_venda) as dia, COUNT(*) as qtd, "
                "SUM(total_liquido) as total FROM vendas "
                "WHERE data_venda BETWEEN %s AND %s AND status = 'finalizada' "
                "GROUP BY DATE(data_venda) ORDER BY dia", (di, df)
            )
        def on_loaded(rows):
            linhas = [f"{'Data':<12} {'Vendas':>8} {'Total':>12}"]
            linhas.append("-" * 34)
            total_geral = 0
            for r in rows:
                dia = str(r["dia"])
                linhas.append(f"{dia:<12} {r['qtd']:>8} R$ {FormatUtils.format_money(r['total']):>10}")
                total_geral += float(r["total"])
            linhas.append("-" * 34)
            linhas.append(f"{'TOTAL':<12} {'':>8} R$ {FormatUtils.format_money(total_geral):>10}")
            self._exibir_relatorio("RESUMO DIARIO", linhas)
        self.run_async(load, on_loaded)

    # ========================================================================
    # METODOS DE IMPRESSAO DE RELATORIOS E REIMPRESSAO
    # ========================================================================

    def _imprimir_relatorio_exibido(self):
        """Imprime o conteudo do relatorio atualmente exibido na area de texto."""
        texto = self.text_relatorio.get("1.0", "end").strip()
        if not texto:
            self.show_error("Nenhum relatorio para imprimir. Gere um relatorio primeiro.")
            return
        self._mostrar_cupom_caixa(texto, titulo="Imprimir Relatorio")

    def _rel_reimprimir_cupom(self):
        """Permite selecionar uma venda pelo ID e reimprimir o cupom."""
        venda_id = simpledialog.askinteger(
            "Reimprimir Cupom",
            "Digite o numero (ID) da venda para reimprimir o cupom:",
            parent=self.root, minvalue=1
        )
        if not venda_id:
            return

        def load():
            db = DatabaseHelper.get_instance()
            venda_rows = db.execute_query("SELECT * FROM vendas WHERE id = %s", (venda_id,))
            if not venda_rows:
                return None
            vr = venda_rows[0]
            venda = Venda()
            venda.id = vr["id"]
            venda.total_bruto = float(vr.get("total_bruto", 0))
            venda.desconto_valor = float(vr.get("desconto_valor", 0))
            venda.acrescimo_valor = float(vr.get("acrescimo_valor", 0))
            venda.total_liquido = float(vr.get("total_liquido", 0))
            venda.valor_recebido = float(vr.get("valor_recebido", 0))
            venda.troco = float(vr.get("troco", 0))
            venda.observacao = vr.get("observacao", "")
            venda.data_venda = str(vr.get("data_venda", ""))
            venda.para_entrega = bool(vr.get("para_entrega", 0))
            venda.taxa_entrega = float(vr.get("taxa_entrega", 0))

            # Cliente
            venda.cliente_nome = (vr.get("cliente_nome") or "").strip()
            if (not venda.cliente_nome) and vr.get("uso_armario_id"):
                uso = db.execute_query("SELECT cliente_nome FROM uso_armario_sauna WHERE id = %s", (vr["uso_armario_id"],))
                venda.cliente_nome = (uso[0]["cliente_nome"] or "").strip() if uso else ""
            if (not venda.cliente_nome) and vr.get("cliente_id"):
                cli = db.execute_query("SELECT nome FROM clientes WHERE id = %s", (vr["cliente_id"],))
                venda.cliente_nome = cli[0]["nome"] if cli else ""

            # Vendedor
            if vr.get("vendedor_id"):
                vend = db.execute_query("SELECT nome FROM vendedores WHERE id = %s", (vr["vendedor_id"],))
                venda.vendedor_nome = vend[0]["nome"] if vend else ""

            # Itens
            itens_rows = db.execute_query(
                "SELECT * FROM itens_venda WHERE venda_id = %s", (venda_id,)
            )
            itens = []
            for ir in itens_rows:
                item = ItemVenda()
                item.descricao_produto = ir.get("descricao_produto", "")
                item.quantidade = float(ir.get("quantidade", 1))
                item.preco_unitario = float(ir.get("preco_unitario", 0))
                item.total = float(ir.get("total", 0))
                itens.append(item)

            # Pagamentos
            pag_rows = db.execute_query(
                "SELECT pv.*, fp.descricao as forma_descricao FROM pagamentos_venda pv "
                "LEFT JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "WHERE pv.venda_id = %s", (venda_id,)
            )
            pags = []
            for pr in pag_rows:
                pag = PagamentoVenda()
                pag.forma_descricao = pr.get("forma_descricao", "")
                pag.valor = float(pr.get("valor", 0))
                pag.parcelas = int(pr.get("parcelas", 1))
                pags.append(pag)

            # Empresa
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass

            return venda, itens, pags, empresa

        def on_loaded(result):
            if result:
                venda, itens, pags, empresa = result
                printer_cfg = self._load_printer_config()
                cupom = CupomGenerator.gerar_cupom(
                    venda, itens, pags,
                    empresa=empresa,
                    colunas=printer_cfg.get("largura_papel", 48),
                    margem_esq=printer_cfg.get("margem_esquerda", 0),
                    margem_dir=printer_cfg.get("margem_direita", 0),
                    espacamento=printer_cfg.get("espacamento_linhas", 1)
                )
                # Exibir no text_relatorio tambem
                self._exibir_relatorio(f"REIMPRESSAO - CUPOM VENDA #{venda_id}",
                                       cupom.split("\n"))
                # Abrir dialog de impressao
                self._mostrar_cupom(cupom)
            else:
                self.show_error(f"Venda #{venda_id} nao encontrada.")

        self.run_async(load, on_loaded)

    def _rel_reimprimir_fechamento(self):
        """Permite selecionar um caixa pelo ID e reimprimir o cupom de fechamento."""
        caixa_id = simpledialog.askinteger(
            "Reimprimir Fechamento de Caixa",
            "Digite o numero (ID) do caixa para reimprimir o fechamento:\n\n"
            "(Dica: o ID do caixa aparece na tela de Caixa)",
            parent=self.root, minvalue=1
        )
        if not caixa_id:
            return

        def load():
            db = DatabaseHelper.get_instance()
            caixa_rows = db.execute_query("SELECT * FROM caixa WHERE id = %s", (caixa_id,))
            if not caixa_rows:
                return None
            cx = caixa_rows[0]

            if cx.get("status") != "fechado":
                return "NAO_FECHADO"

            # Dados do operador
            usuario_nome = "N/A"
            if cx.get("usuario_id"):
                user_rows = db.execute_query("SELECT nome FROM usuarios WHERE id = %s", (cx["usuario_id"],))
                if user_rows:
                    usuario_nome = user_rows[0]["nome"]

            valor_abertura = float(cx.get("valor_abertura", 0))
            valor_fechamento = float(cx.get("valor_fechamento", 0))
            data_abertura = str(cx.get("data_abertura", ""))
            data_fechamento = str(cx.get("data_fechamento", ""))

            # Total de vendas
            rows = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(total_liquido),0) as total "
                "FROM vendas WHERE caixa_id = %s AND status = 'finalizada'", (caixa_id,)
            )
            total_vendas = float(rows[0]["total"]) if rows else 0
            qtd_vendas = int(rows[0]["qtd"]) if rows else 0

            # Vales
            rows_v = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total FROM vales_debito WHERE caixa_id = %s",
                (caixa_id,)
            )
            total_vales = float(rows_v[0]["total"]) if rows_v else 0
            qtd_vales = int(rows_v[0]["qtd"]) if rows_v else 0

            # Sangrias
            rows_s = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(valor),0) as total FROM sangrias WHERE caixa_id = %s",
                (caixa_id,)
            )
            total_sangrias = float(rows_s[0]["total"]) if rows_s else 0
            qtd_sangrias = int(rows_s[0]["qtd"]) if rows_s else 0

            # Resumo por forma de pagamento
            pag_rows = db.execute_query(
                "SELECT fp.descricao, COALESCE(SUM(pv.valor),0) as total "
                "FROM pagamentos_venda pv "
                "JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "JOIN vendas v ON pv.venda_id = v.id "
                "WHERE v.caixa_id = %s AND v.status = 'finalizada' "
                "GROUP BY fp.id, fp.descricao ORDER BY total DESC", (caixa_id,)
            )
            pagamentos_resumo = []
            if pag_rows:
                for pr in pag_rows:
                    pagamentos_resumo.append({
                        "descricao": pr.get("descricao", "N/A"),
                        "total": float(pr.get("total", 0))
                    })

            # Empresa
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass

            return {
                "caixa_id": caixa_id,
                "usuario_nome": usuario_nome,
                "valor_abertura": valor_abertura,
                "valor_fechamento": valor_fechamento,
                "data_abertura": data_abertura,
                "data_fechamento": data_fechamento,
                "total_vendas": total_vendas,
                "qtd_vendas": qtd_vendas,
                "total_vales": total_vales,
                "qtd_vales": qtd_vales,
                "total_sangrias": total_sangrias,
                "qtd_sangrias": qtd_sangrias,
                "pagamentos_resumo": pagamentos_resumo,
                "empresa": empresa,
            }

        def on_loaded(result):
            if result is None:
                self.show_error(f"Caixa #{caixa_id} nao encontrado.")
                return
            if result == "NAO_FECHADO":
                self.show_error(f"Caixa #{caixa_id} ainda nao foi fechado.\n"
                                "Somente caixas fechados podem ser reimpressos.")
                return

            printer_cfg = self._load_printer_config()
            cupom = CupomGenerator.gerar_cupom_fechamento_caixa(
                caixa_id=result["caixa_id"],
                usuario_nome=result["usuario_nome"],
                valor_abertura=result["valor_abertura"],
                valor_fechamento=result["valor_fechamento"],
                data_abertura=result["data_abertura"],
                data_fechamento=result["data_fechamento"],
                total_vendas=result["total_vendas"],
                qtd_vendas=result["qtd_vendas"],
                total_vales=result["total_vales"],
                qtd_vales=result["qtd_vales"],
                total_sangrias=result["total_sangrias"],
                qtd_sangrias=result["qtd_sangrias"],
                pagamentos_resumo=result["pagamentos_resumo"],
                empresa=result["empresa"],
                colunas=printer_cfg.get("largura_papel", 48),
                margem_esq=printer_cfg.get("margem_esquerda", 0),
                margem_dir=printer_cfg.get("margem_direita", 0),
                espacamento=printer_cfg.get("espacamento_linhas", 1)
            )
            # Exibir no text_relatorio tambem
            self._exibir_relatorio(f"REIMPRESSAO - FECHAMENTO CAIXA #{caixa_id}",
                                   cupom.split("\n"))
            # Abrir dialog de impressao
            self._mostrar_cupom_caixa(cupom, titulo=f"Reimpr. Fechamento Caixa #{caixa_id}")

        self.run_async(load, on_loaded)

    def _rel_impressao_produtos_vendidos(self):
        """Gera relatorio de produtos vendidos no periodo com opcao de impressao.
        Lista todos os produtos vendidos com quantidade e valor total."""
        di, df = self._get_datas_filtro()
        if not di:
            return

        def load():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT iv.descricao_produto, SUM(iv.quantidade) as qtd, "
                "SUM(iv.total) as total, "
                "ROUND(SUM(iv.total)/SUM(iv.quantidade),2) as preco_medio "
                "FROM itens_venda iv JOIN vendas v ON iv.venda_id = v.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY iv.descricao_produto ORDER BY total DESC", (di, df)
            )
            # Empresa para cabecalho
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass
            return rows, empresa

        def on_loaded(result):
            rows, empresa = result
            if not rows:
                self._exibir_relatorio("PRODUTOS VENDIDOS",
                                       ["Nenhum produto vendido no periodo selecionado."])
                return

            # Formatar datas para exibicao
            try:
                di_fmt = datetime.datetime.strptime(di[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
                df_fmt = datetime.datetime.strptime(df[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
            except Exception:
                di_fmt, df_fmt = di, df

            linhas = []
            linhas.append(f"Periodo: {di_fmt} a {df_fmt}")
            linhas.append("")
            linhas.append(f"{'#':>3} {'Produto':<28} {'Qtd':>8} {'Preco Med':>10} {'Total':>12}")
            linhas.append("-" * 63)

            total_geral = 0
            qtd_geral = 0
            for i, r in enumerate(rows, 1):
                qtd = float(r['qtd'])
                total = float(r['total'])
                preco_medio = float(r.get('preco_medio', 0))
                linhas.append(
                    f"{i:>3} {r['descricao_produto']:<28} {qtd:>8.0f} "
                    f"R${FormatUtils.format_money(preco_medio):>8} "
                    f"R${FormatUtils.format_money(total):>10}"
                )
                total_geral += total
                qtd_geral += qtd

            linhas.append("-" * 63)
            linhas.append(
                f"{'':>3} {'TOTAL GERAL':<28} {qtd_geral:>8.0f} "
                f"{'':>10} R${FormatUtils.format_money(total_geral):>10}"
            )
            linhas.append(f"\nTotal de produtos distintos: {len(rows)}")

            self._exibir_relatorio("PRODUTOS VENDIDOS", linhas)

            # Gerar cupom formatado para impressao
            printer_cfg = self._load_printer_config()
            largura = printer_cfg.get("largura_papel", 48)
            cupom_linhas = []
            cupom_linhas.append("=" * largura)
            if empresa:
                nome_emp = empresa.get("nome_fantasia") or empresa.get("razao_social") or ""
                if nome_emp:
                    cupom_linhas.append(nome_emp.center(largura))
                cnpj = empresa.get("cnpj", "")
                if cnpj:
                    cupom_linhas.append(f"CNPJ: {cnpj}".center(largura))
            cupom_linhas.append("-" * largura)
            cupom_linhas.append("RELATORIO: PRODUTOS VENDIDOS".center(largura))
            cupom_linhas.append(f"Periodo: {di_fmt} a {df_fmt}".center(largura))
            cupom_linhas.append("-" * largura)
            for i, r in enumerate(rows, 1):
                desc = (r['descricao_produto'] or "")[:largura - 20]
                qtd = float(r['qtd'])
                total = float(r['total'])
                cupom_linhas.append(f"{i:>3}. {desc}")
                cupom_linhas.append(f"     Qtd: {qtd:.0f}  Total: R$ {FormatUtils.format_money(total)}".rjust(largura))
            cupom_linhas.append("-" * largura)
            cupom_linhas.append(f"TOTAL GERAL: R$ {FormatUtils.format_money(total_geral)}".rjust(largura))
            cupom_linhas.append(f"Produtos distintos: {len(rows)}")
            cupom_linhas.append("=" * largura)

            self._ultimo_relatorio_cupom = "\n".join(cupom_linhas)

        self.run_async(load, on_loaded)

    def _rel_impressao_faturamento_pgto(self):
        """Gera relatorio de faturamento por forma de pagamento no periodo com opcao de impressao.
        Mostra o total faturado em cada forma de pagamento."""
        di, df = self._get_datas_filtro()
        if not di:
            return

        def load():
            db = DatabaseHelper.get_instance()
            rows = db.execute_query(
                "SELECT fp.descricao, COUNT(*) as qtd_transacoes, "
                "SUM(pv.valor) as total, "
                "ROUND(AVG(pv.valor),2) as ticket_medio "
                "FROM pagamentos_venda pv "
                "JOIN formas_pagamento fp ON pv.forma_pagamento_id = fp.id "
                "JOIN vendas v ON pv.venda_id = v.id "
                "WHERE v.data_venda BETWEEN %s AND %s AND v.status = 'finalizada' "
                "GROUP BY fp.id, fp.descricao ORDER BY total DESC", (di, df)
            )
            # Total geral de vendas no periodo
            total_row = db.execute_query(
                "SELECT COUNT(*) as qtd, COALESCE(SUM(total_liquido),0) as total "
                "FROM vendas WHERE data_venda BETWEEN %s AND %s AND status = 'finalizada'",
                (di, df)
            )
            # Empresa para cabecalho
            empresa = None
            try:
                emp_rows = db.execute_query("SELECT * FROM empresa LIMIT 1")
                if emp_rows:
                    empresa = emp_rows[0]
            except Exception:
                pass
            return rows, total_row, empresa

        def on_loaded(result):
            rows, total_row, empresa = result
            if not rows:
                self._exibir_relatorio("FATURAMENTO POR FORMA DE PAGAMENTO",
                                       ["Nenhum faturamento no periodo selecionado."])
                return

            # Formatar datas para exibicao
            try:
                di_fmt = datetime.datetime.strptime(di[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
                df_fmt = datetime.datetime.strptime(df[:10], "%Y-%m-%d").strftime("%d/%m/%Y")
            except Exception:
                di_fmt, df_fmt = di, df

            total_vendas = float(total_row[0]["total"]) if total_row else 0
            qtd_vendas = int(total_row[0]["qtd"]) if total_row else 0

            linhas = []
            linhas.append(f"Periodo: {di_fmt} a {df_fmt}")
            linhas.append(f"Total de vendas finalizadas: {qtd_vendas}")
            linhas.append(f"Faturamento total: R$ {FormatUtils.format_money(total_vendas)}")
            linhas.append("")
            linhas.append(f"{'Forma de Pagamento':<25} {'Transacoes':>10} {'Ticket Med':>12} {'Total':>14} {'%':>6}")
            linhas.append("-" * 69)

            total_pgto = 0
            for r in rows:
                total = float(r['total'])
                qtd = int(r['qtd_transacoes'])
                ticket = float(r.get('ticket_medio', 0))
                pct = (total / total_vendas * 100) if total_vendas > 0 else 0
                linhas.append(
                    f"{r['descricao']:<25} {qtd:>10} "
                    f"R${FormatUtils.format_money(ticket):>10} "
                    f"R${FormatUtils.format_money(total):>12} "
                    f"{pct:>5.1f}%"
                )
                total_pgto += total

            linhas.append("-" * 69)
            linhas.append(
                f"{'TOTAL':<25} {'':>10} {'':>12} "
                f"R${FormatUtils.format_money(total_pgto):>12} {'100.0%':>6}"
            )

            self._exibir_relatorio("FATURAMENTO POR FORMA DE PAGAMENTO", linhas)

            # Gerar cupom formatado para impressao
            printer_cfg = self._load_printer_config()
            largura = printer_cfg.get("largura_papel", 48)
            cupom_linhas = []
            cupom_linhas.append("=" * largura)
            if empresa:
                nome_emp = empresa.get("nome_fantasia") or empresa.get("razao_social") or ""
                if nome_emp:
                    cupom_linhas.append(nome_emp.center(largura))
                cnpj = empresa.get("cnpj", "")
                if cnpj:
                    cupom_linhas.append(f"CNPJ: {cnpj}".center(largura))
            cupom_linhas.append("-" * largura)
            cupom_linhas.append("FATURAMENTO POR FORMA PGTO".center(largura))
            cupom_linhas.append(f"Periodo: {di_fmt} a {df_fmt}".center(largura))
            cupom_linhas.append(f"Vendas: {qtd_vendas}  Total: R$ {FormatUtils.format_money(total_vendas)}".center(largura))
            cupom_linhas.append("-" * largura)
            for r in rows:
                total = float(r['total'])
                qtd = int(r['qtd_transacoes'])
                pct = (total / total_vendas * 100) if total_vendas > 0 else 0
                desc = (r['descricao'] or "")[:largura - 25]
                cupom_linhas.append(f"{desc}")
                cupom_linhas.append(f"  {qtd} transacoes  R$ {FormatUtils.format_money(total)}  ({pct:.1f}%)".rjust(largura))
            cupom_linhas.append("-" * largura)
            cupom_linhas.append(f"TOTAL: R$ {FormatUtils.format_money(total_pgto)}".rjust(largura))
            cupom_linhas.append("=" * largura)

            self._ultimo_relatorio_cupom = "\n".join(cupom_linhas)

        self.run_async(load, on_loaded)

    # ========================================================================
    # PERFIS E PERMISSOES GRANULARES (alinhado com PermissionManager.java)
    # ========================================================================

    # --- Cores para indicadores de permissao granular ---
    _PERM_COR_HERDADA = "#4CAF50"       # Verde - permissao herdada do perfil
    _PERM_COR_ADICIONADA = "#2196F3"    # Azul - override: adicionada ao usuario
    _PERM_COR_REMOVIDA = "#F44336"      # Vermelho - override: removida do usuario
    _PERM_COR_INATIVA = "#555555"       # Cinza - permissao nao concedida
    _PERM_COR_PERFIL_ATIVA = "#4CAF50"  # Verde - permissao ativa no perfil

    def show_gerenciar_perfis(self):
        self.clear_container()

        top = self._create_top_bar(f"{Icons.PERFIL} Perfis e Permissoes Granulares")

        # === CONTEUDO PRINCIPAL ===
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=10, pady=5)

        # ================================================================
        # PAINEL ESQUERDO: Perfis + Usuarios + Acoes
        # ================================================================
        left = tk.Frame(content, bg=COR_FUNDO, width=310)
        left.pack(side="left", fill="y", padx=(0, 8))
        left.pack_propagate(False)

        # --- Card: Perfis ---
        perfis_card = GlassCard(left, glow_color=COR_PRIMARIA)
        perfis_card.pack(fill="x", pady=(0, 5))

        perfis_header = tk.Frame(perfis_card, bg=COR_GLASS)
        perfis_header.pack(fill="x")
        tk.Label(perfis_header, text=f"{Icons.PERFIL} Perfis", bg=COR_GLASS, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).pack(side="left")
        self._lbl_perfis_count = tk.Label(perfis_header, text="(0)", bg=COR_GLASS, fg=COR_TEXTO2,
                                           font=("Segoe UI", 9))
        self._lbl_perfis_count.pack(side="right")

        self.list_perfis = tk.Listbox(perfis_card, bg=COR_ENTRADA, fg=COR_TEXTO,
                                       font=("Segoe UI", 10), selectbackground=COR_PRIMARIA,
                                       height=7, relief="flat", bd=0,
                                       highlightthickness=1, highlightbackground=COR_ENTRADA_BORDER)
        self.list_perfis.pack(fill="x", pady=(5, 0))

        # --- Card: Usuarios ---
        users_card = GlassCard(left, glow_color=COR_SECUNDARIA)
        users_card.pack(fill="both", expand=True, pady=(5, 5))

        users_header = tk.Frame(users_card, bg=COR_GLASS)
        users_header.pack(fill="x")
        tk.Label(users_header, text=f"{Icons.USER} Usuarios", bg=COR_GLASS, fg=COR_SECUNDARIA,
                 font=("Segoe UI", 12, "bold")).pack(side="left")
        self._lbl_users_count = tk.Label(users_header, text="(0)", bg=COR_GLASS, fg=COR_TEXTO2,
                                          font=("Segoe UI", 9))
        self._lbl_users_count.pack(side="right")

        # Busca de usuario
        busca_user_frame = tk.Frame(users_card, bg=COR_GLASS)
        busca_user_frame.pack(fill="x", pady=(5, 3))
        tk.Label(busca_user_frame, text="Buscar:", bg=COR_GLASS, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack(side="left")
        self._et_busca_usuario = tk.Entry(busca_user_frame, bg=COR_ENTRADA, fg=COR_TEXTO,
                                           insertbackground=COR_TEXTO, font=("Segoe UI", 9),
                                           relief="flat", bd=0, highlightthickness=1,
                                           highlightbackground=COR_ENTRADA_BORDER)
        self._et_busca_usuario.pack(side="left", fill="x", expand=True, padx=(5, 0))
        self._et_busca_usuario.bind("<KeyRelease>", self._filtrar_usuarios_perfil)

        self.list_usuarios_perfil = tk.Listbox(users_card, bg=COR_ENTRADA, fg=COR_TEXTO,
                                                font=("Segoe UI", 10), selectbackground=COR_SECUNDARIA,
                                                relief="flat", bd=0,
                                                highlightthickness=1, highlightbackground=COR_ENTRADA_BORDER)
        self.list_usuarios_perfil.pack(fill="both", expand=True, pady=(3, 0))

        # --- Card: Acoes rapidas ---
        acoes_card = GlassCard(left, glow_color=COR_ACCENT)
        acoes_card.pack(fill="x", pady=(5, 0))
        tk.Label(acoes_card, text="Acoes Rapidas", bg=COR_GLASS, fg=COR_ACCENT,
                 font=("Segoe UI", 10, "bold")).pack(anchor="w", pady=(0, 5))

        acoes_btns = tk.Frame(acoes_card, bg=COR_GLASS)
        acoes_btns.pack(fill="x")
        StyledButton(acoes_btns, text="Resetar Overrides", command=self._resetar_overrides_usuario,
                     color="#9C27B0", width=14).pack(side="left", padx=2)
        add_tooltip(acoes_btns.winfo_children()[-1], "Remove todos os overrides individuais do usuario selecionado")
        StyledButton(acoes_btns, text="Copiar Permissoes", command=self._copiar_permissoes_de_perfil,
                     color=COR_BOTAO_AZUL, width=14).pack(side="left", padx=2)
        add_tooltip(acoes_btns.winfo_children()[-1], "Copia as permissoes de outro perfil para o selecionado")

        # ================================================================
        # PAINEL DIREITO: Permissoes com indicadores granulares
        # ================================================================
        right = tk.Frame(content, bg=COR_FUNDO)
        right.pack(side="right", fill="both", expand=True)

        # --- Info do perfil/usuario selecionado ---
        info_frame = tk.Frame(right, bg=COR_FUNDO)
        info_frame.pack(fill="x", pady=(0, 3))

        self._lbl_perfil_info = tk.Label(info_frame, text="Selecione um perfil ou usuario",
                                          bg=COR_FUNDO, fg=COR_ALERTA, font=("Segoe UI", 12, "bold"))
        self._lbl_perfil_info.pack(side="left")

        # Contadores de permissoes
        self._lbl_perm_counters = tk.Label(info_frame, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                                            font=("Segoe UI", 9))
        self._lbl_perm_counters.pack(side="right")

        # --- Legenda visual ---
        legenda_frame = tk.Frame(right, bg=COR_FUNDO)
        legenda_frame.pack(fill="x", pady=(0, 3))

        legendas = [
            (self._PERM_COR_HERDADA, "Herdada do Perfil"),
            (self._PERM_COR_ADICIONADA, "+Override Adicionada"),
            (self._PERM_COR_REMOVIDA, "-Override Removida"),
            (self._PERM_COR_INATIVA, "Nao Concedida"),
        ]
        for cor, texto in legendas:
            item = tk.Frame(legenda_frame, bg=COR_FUNDO)
            item.pack(side="left", padx=(0, 12))
            dot = tk.Canvas(item, width=10, height=10, bg=COR_FUNDO, highlightthickness=0)
            dot.pack(side="left", padx=(0, 3))
            dot.create_oval(1, 1, 9, 9, fill=cor, outline="")
            tk.Label(item, text=texto, bg=COR_FUNDO, fg=COR_TEXTO2,
                     font=("Segoe UI", 8)).pack(side="left")

        # --- Barra de busca e filtro de permissoes ---
        filtro_frame = tk.Frame(right, bg=COR_FUNDO)
        filtro_frame.pack(fill="x", pady=(0, 5))

        tk.Label(filtro_frame, text="Filtrar:", bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left")
        self._et_filtro_perm = tk.Entry(filtro_frame, bg=COR_ENTRADA, fg=COR_TEXTO,
                                         insertbackground=COR_TEXTO, font=("Segoe UI", 10),
                                         relief="flat", bd=0, highlightthickness=1,
                                         highlightbackground=COR_ENTRADA_BORDER)
        self._et_filtro_perm.pack(side="left", fill="x", expand=True, padx=5)
        self._et_filtro_perm.bind("<KeyRelease>", self._filtrar_permissoes)
        add_tooltip(self._et_filtro_perm, "Digite para filtrar por modulo, chave ou descricao")

        # Combo de modulo
        tk.Label(filtro_frame, text="Modulo:", bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).pack(side="left", padx=(10, 0))
        modulos_list = ["Todos"] + list(PermissionConstants.get_permissoes_por_modulo().keys())
        self._combo_modulo_filtro = ttk.Combobox(filtro_frame, values=modulos_list,
                                                   state="readonly", width=18)
        self._combo_modulo_filtro.set("Todos")
        self._combo_modulo_filtro.pack(side="left", padx=5)
        self._combo_modulo_filtro.bind("<<ComboboxSelected>>", self._filtrar_permissoes)

        # --- Scroll de permissoes ---
        perm_scroll = ScrollableFrame(right)
        perm_scroll.pack(fill="both", expand=True)
        self._perm_scroll_frame = perm_scroll.scrollable_frame

        # Construir checkboxes agrupados por modulo com indicadores visuais
        self.perm_vars = {}          # chave -> BooleanVar
        self._perm_checkbuttons = {} # chave -> Checkbutton widget
        self._perm_indicators = {}   # chave -> Canvas (indicador de cor)
        self._perm_labels = {}       # chave -> Label (texto descritivo)
        self._perm_modulo_frames = {}  # modulo -> Frame container
        self._perm_modulo_headers = {} # modulo -> Frame header (com botoes marcar/desmarcar)
        self._perm_item_frames = {}    # chave -> Frame do item individual
        self._perm_perfil_set = set()  # permissoes do perfil base (para calculo de overrides)
        self._perm_overrides_add = set()  # overrides tipo 'adicionar'
        self._perm_overrides_rem = set()  # overrides tipo 'remover'

        perms_por_modulo = PermissionConstants.get_permissoes_por_modulo()
        for modulo, perms_lista in perms_por_modulo.items():
            # Frame do modulo
            mod_frame = tk.Frame(self._perm_scroll_frame, bg=COR_FUNDO)
            mod_frame.pack(fill="x", padx=2, pady=(6, 0))
            self._perm_modulo_frames[modulo] = mod_frame

            # Header do modulo com botoes de acao
            mod_header = tk.Frame(mod_frame, bg=COR_FUNDO2, padx=5, pady=3)
            mod_header.pack(fill="x")
            self._perm_modulo_headers[modulo] = mod_header

            tk.Label(mod_header, text=f"{modulo}", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                     font=("Segoe UI", 10, "bold")).pack(side="left")

            # Contagem de permissoes do modulo
            lbl_mod_count = tk.Label(mod_header, text=f"({len(perms_lista)})",
                                      bg=COR_FUNDO2, fg=COR_TEXTO2, font=("Segoe UI", 8))
            lbl_mod_count.pack(side="left", padx=5)

            # Botoes marcar/desmarcar modulo
            btn_marcar_mod = tk.Label(mod_header, text="[Marcar Todos]", bg=COR_FUNDO2,
                                       fg=COR_SUCESSO, font=("Segoe UI", 8, "bold"), cursor="hand2")
            btn_marcar_mod.pack(side="right", padx=3)
            btn_marcar_mod.bind("<Button-1>",
                lambda e, m=modulo: self._marcar_modulo(m, True))

            btn_desmarcar_mod = tk.Label(mod_header, text="[Desmarcar Todos]", bg=COR_FUNDO2,
                                          fg=COR_ERRO, font=("Segoe UI", 8, "bold"), cursor="hand2")
            btn_desmarcar_mod.pack(side="right", padx=3)
            btn_desmarcar_mod.bind("<Button-1>",
                lambda e, m=modulo: self._marcar_modulo(m, False))

            # Itens de permissao
            for p in perms_lista:
                item_frame = tk.Frame(mod_frame, bg=COR_FUNDO)
                item_frame.pack(fill="x", padx=10, pady=1)
                self._perm_item_frames[p.chave] = item_frame

                # Indicador de cor (bolinha)
                indicator = tk.Canvas(item_frame, width=12, height=12,
                                      bg=COR_FUNDO, highlightthickness=0)
                indicator.pack(side="left", padx=(0, 4), pady=2)
                indicator.create_oval(2, 2, 10, 10, fill=self._PERM_COR_INATIVA,
                                       outline="", tags="dot")
                self._perm_indicators[p.chave] = indicator

                # Checkbox
                var = tk.BooleanVar()
                cb = tk.Checkbutton(item_frame, variable=var,
                                    bg=COR_FUNDO, selectcolor=COR_ENTRADA,
                                    activebackground=COR_FUNDO)
                cb.pack(side="left")
                # Ao mudar o checkbox, atualizar indicador
                var.trace_add("write", lambda *args, ch=p.chave: self._on_perm_toggle(ch))
                self.perm_vars[p.chave] = var
                self._perm_checkbuttons[p.chave] = cb

                # Label com chave e descricao
                lbl = tk.Label(item_frame, text=f"{p.chave}", bg=COR_FUNDO,
                               fg=COR_TEXTO, font=("Segoe UI", 9))
                lbl.pack(side="left", padx=(0, 5))
                self._perm_labels[p.chave] = lbl

                # Descricao em cinza
                tk.Label(item_frame, text=f"({p.descricao})", bg=COR_FUNDO,
                         fg=COR_TEXTO2, font=("Segoe UI", 8)).pack(side="left")

        # --- Barra de botoes de acao ---
        btn_frame = tk.Frame(right, bg=COR_FUNDO)
        btn_frame.pack(fill="x", pady=(8, 3))

        StyledButton(btn_frame, text=f"{Icons.SAVE} Salvar Permissoes do Perfil",
                     command=self._salvar_permissoes_perfil,
                     color=COR_BOTAO_VERDE, width=24).pack(side="left", padx=3)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Salva as permissoes marcadas diretamente no perfil selecionado")

        StyledButton(btn_frame, text=f"{Icons.SAVE} Salvar Permissoes do Usuario",
                     command=self._salvar_overrides_usuario,
                     color="#FF9800", width=24).pack(side="left", padx=3)
        add_tooltip(btn_frame.winfo_children()[-1],
                    "Salva overrides individuais para o usuario (diferencas em relacao ao perfil)")

        StyledButton(btn_frame, text="Marcar Todos",
                     command=lambda: self._marcar_todos(True),
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=3)

        StyledButton(btn_frame, text="Desmarcar Todos",
                     command=lambda: self._marcar_todos(False),
                     color="#2a3a5c", width=14).pack(side="left", padx=3)

        # --- Bindings ---
        self.list_perfis.bind("<<ListboxSelect>>", self._on_select_perfil)
        self.list_usuarios_perfil.bind("<<ListboxSelect>>", self._on_select_usuario_perfil)

        # --- Inicializar estado ---
        self._perfil_mode = "perfil"  # 'perfil' ou 'usuario'
        self._perfil_selected_id = None
        self._perfil_selected_user_id = None
        self._perfil_list = []
        self._perfil_users = []
        self._perfil_users_filtered = []
        self._perm_perfil_set = set()
        self._perm_overrides_add = set()
        self._perm_overrides_rem = set()
        self._carregar_perfis_e_usuarios()

        # Auto-refresh em tempo real (a cada 15 segundos)
        self._start_auto_refresh("gerenciar_perfis", self._carregar_perfis_e_usuarios, 15000)

    # ---- Helpers de UI para permissoes granulares ----

    def _marcar_todos(self, valor):
        """Marca ou desmarca todas as permissoes visiveis."""
        for chave, var in self.perm_vars.items():
            frame = self._perm_item_frames.get(chave)
            if frame and frame.winfo_ismapped():
                var.set(valor)
        self._atualizar_contadores()

    def _marcar_modulo(self, modulo, valor):
        """Marca ou desmarca todas as permissoes de um modulo."""
        perms_por_modulo = PermissionConstants.get_permissoes_por_modulo()
        for p in perms_por_modulo.get(modulo, []):
            if p.chave in self.perm_vars:
                self.perm_vars[p.chave].set(valor)
        self._atualizar_contadores()

    def _on_perm_toggle(self, chave):
        """Chamado quando um checkbox de permissao e alterado. Atualiza indicador visual."""
        self._atualizar_indicador(chave)
        self._atualizar_contadores()

    def _atualizar_indicador(self, chave):
        """Atualiza a cor do indicador de uma permissao baseado no estado."""
        indicator = self._perm_indicators.get(chave)
        lbl = self._perm_labels.get(chave)
        if not indicator:
            return

        marcada = self.perm_vars[chave].get()
        no_perfil = chave in self._perm_perfil_set

        if self._perfil_mode == "perfil":
            # Modo perfil: simples - verde se ativa, cinza se inativa
            cor = self._PERM_COR_PERFIL_ATIVA if marcada else self._PERM_COR_INATIVA
            fg = COR_TEXTO if marcada else COR_TEXTO2
        else:
            # Modo usuario: indicar overrides
            if marcada and no_perfil:
                # Herdada do perfil (ativa no perfil e marcada)
                cor = self._PERM_COR_HERDADA
                fg = COR_TEXTO
            elif marcada and not no_perfil:
                # Override: adicionada (nao esta no perfil, mas foi marcada)
                cor = self._PERM_COR_ADICIONADA
                fg = self._PERM_COR_ADICIONADA
            elif not marcada and no_perfil:
                # Override: removida (esta no perfil, mas foi desmarcada)
                cor = self._PERM_COR_REMOVIDA
                fg = self._PERM_COR_REMOVIDA
            else:
                # Nao concedida (nao esta no perfil e nao foi marcada)
                cor = self._PERM_COR_INATIVA
                fg = COR_TEXTO2

        try:
            indicator.delete("dot")
            indicator.create_oval(2, 2, 10, 10, fill=cor, outline="", tags="dot")
            if lbl:
                lbl.config(fg=fg)
        except tk.TclError:
            pass

    def _atualizar_todos_indicadores(self):
        """Atualiza os indicadores visuais de todas as permissoes."""
        for chave in self.perm_vars:
            self._atualizar_indicador(chave)
        self._atualizar_contadores()

    def _atualizar_contadores(self):
        """Atualiza os contadores de permissoes na barra de info."""
        try:
            total = len(self.perm_vars)
            ativas = sum(1 for v in self.perm_vars.values() if v.get())
            inativas = total - ativas

            if self._perfil_mode == "usuario":
                overrides_add = 0
                overrides_rem = 0
                for chave, var in self.perm_vars.items():
                    marcada = var.get()
                    no_perfil = chave in self._perm_perfil_set
                    if marcada and not no_perfil:
                        overrides_add += 1
                    elif not marcada and no_perfil:
                        overrides_rem += 1
                texto = (f"Total: {total}  |  Ativas: {ativas}  |  Inativas: {inativas}  |  "
                         f"+Adicionadas: {overrides_add}  |  -Removidas: {overrides_rem}")
            else:
                texto = f"Total: {total}  |  Ativas: {ativas}  |  Inativas: {inativas}"

            self._lbl_perm_counters.config(text=texto)
        except Exception:
            pass

    def _filtrar_permissoes(self, event=None):
        """Filtra as permissoes exibidas com base no texto digitado e modulo selecionado."""
        try:
            filtro_texto = self._et_filtro_perm.get().strip().lower()
            modulo_sel = self._combo_modulo_filtro.get()

            perms_por_modulo = PermissionConstants.get_permissoes_por_modulo()

            for modulo, perms_lista in perms_por_modulo.items():
                mod_frame = self._perm_modulo_frames.get(modulo)
                if not mod_frame:
                    continue

                # Verificar filtro de modulo
                if modulo_sel != "Todos" and modulo != modulo_sel:
                    mod_frame.pack_forget()
                    continue

                # Verificar se algum item do modulo passa no filtro de texto
                algum_visivel = False
                for p in perms_lista:
                    item_frame = self._perm_item_frames.get(p.chave)
                    if not item_frame:
                        continue

                    if filtro_texto:
                        texto_busca = f"{p.chave} {p.descricao} {p.modulo} {p.acao}".lower()
                        if filtro_texto not in texto_busca:
                            item_frame.pack_forget()
                            continue

                    # Item visivel
                    item_frame.pack(fill="x", padx=10, pady=1)
                    algum_visivel = True

                if algum_visivel:
                    mod_frame.pack(fill="x", padx=2, pady=(6, 0))
                else:
                    mod_frame.pack_forget()
        except Exception:
            pass

    def _filtrar_usuarios_perfil(self, event=None):
        """Filtra a lista de usuarios com base no texto digitado."""
        try:
            filtro = self._et_busca_usuario.get().strip().lower()
            self.list_usuarios_perfil.delete(0, tk.END)
            self._perfil_users_filtered = []

            for u in self._perfil_users:
                perfil_nome = ""
                if u.get("perfil_id"):
                    for p in self._perfil_list:
                        if p["id"] == u["perfil_id"]:
                            perfil_nome = p["nome"]
                            break
                display = f"{u['nome']} ({u['nivel']}) [{perfil_nome}]"

                if filtro and filtro not in display.lower():
                    continue

                self.list_usuarios_perfil.insert(tk.END, display)
                self._perfil_users_filtered.append(u)

            self._lbl_users_count.config(text=f"({len(self._perfil_users_filtered)})")
        except Exception:
            pass

    # ---- Carregar dados ----

    def _carregar_perfis_e_usuarios(self):
        def load():
            db = DatabaseHelper.get_instance()
            perfis = db.execute_query(
                "SELECT id, nome, descricao, personalizavel FROM perfis WHERE ativo = 1 ORDER BY id")
            users = db.execute_query(
                "SELECT id, nome, login, nivel, perfil_id FROM usuarios WHERE ativo = 1 ORDER BY nome")
            return (perfis, users)

        def on_loaded(result):
            perfis, users = result
            self._perfil_list = perfis
            self._perfil_users = users
            self._perfil_users_filtered = list(users)

            # Atualizar lista de perfis
            self.list_perfis.delete(0, tk.END)
            for p in perfis:
                tag = " [P]" if p.get("personalizavel") else ""
                self.list_perfis.insert(tk.END, f"{p['nome']}{tag}")
            self._lbl_perfis_count.config(text=f"({len(perfis)})")

            # Atualizar lista de usuarios
            self.list_usuarios_perfil.delete(0, tk.END)
            for u in users:
                perfil_nome = ""
                if u.get("perfil_id"):
                    for p in perfis:
                        if p["id"] == u["perfil_id"]:
                            perfil_nome = p["nome"]
                            break
                self.list_usuarios_perfil.insert(tk.END,
                    f"{u['nome']} ({u['nivel']}) [{perfil_nome}]")
            self._lbl_users_count.config(text=f"({len(users)})")

        self.run_async(load, on_loaded)

    # ---- Selecao de perfil ----

    def _on_select_perfil(self, event):
        sel = self.list_perfis.curselection()
        if not sel:
            return
        perfil = self._perfil_list[sel[0]]
        self._perfil_mode = "perfil"
        self._perfil_selected_id = perfil["id"]
        self._perfil_selected_user_id = None
        self._perm_perfil_set = set()
        self._perm_overrides_add = set()
        self._perm_overrides_rem = set()

        desc = perfil.get('descricao', '') or ''
        self._lbl_perfil_info.config(
            text=f"Perfil: {perfil['nome']} (ID: {perfil['id']})  -  {desc}",
            fg=COR_PRIMARIA)
        # Desselecionar usuario
        self.list_usuarios_perfil.selection_clear(0, tk.END)

        def load():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT p.chave FROM permissoes p "
                "JOIN perfil_permissoes pp ON pp.permissao_id = p.id "
                "WHERE pp.perfil_id = %s", (perfil["id"],)
            )

        def on_loaded(rows):
            perms = {r["chave"] for r in rows}
            self._perm_perfil_set = set(perms)
            for chave, var in self.perm_vars.items():
                var.set(chave in perms)
            self._atualizar_todos_indicadores()

        self.run_async(load, on_loaded)

    # ---- Selecao de usuario ----

    def _on_select_usuario_perfil(self, event):
        sel = self.list_usuarios_perfil.curselection()
        if not sel:
            return
        # Usar lista filtrada
        if sel[0] >= len(self._perfil_users_filtered):
            return
        user = self._perfil_users_filtered[sel[0]]
        user_id = user["id"]
        self._perfil_mode = "usuario"
        self._perfil_selected_user_id = user_id
        self._perfil_selected_id = None
        perfil_nome = ""
        perfil_id = user.get("perfil_id", 0) or 0
        if perfil_id:
            for p in self._perfil_list:
                if p["id"] == perfil_id:
                    perfil_nome = p["nome"]
                    break
        self._lbl_perfil_info.config(
            text=f"Usuario: {user['nome']} (Nivel: {user['nivel']}) | Perfil Base: {perfil_nome or 'Nenhum'}",
            fg=COR_SECUNDARIA)
        # Desselecionar perfil
        self.list_perfis.selection_clear(0, tk.END)

        def load():
            db = DatabaseHelper.get_instance()
            # 1. Carregar permissoes BASE do perfil
            perms_perfil = set()
            if perfil_id:
                rows = db.execute_query(
                    "SELECT p.chave FROM permissoes p "
                    "JOIN perfil_permissoes pp ON pp.permissao_id = p.id "
                    "WHERE pp.perfil_id = %s", (perfil_id,)
                )
                perms_perfil = {r["chave"] for r in rows}

            # 2. Carregar overrides individuais
            overrides = db.execute_query(
                "SELECT p.chave, up.tipo FROM usuario_permissoes up "
                "JOIN permissoes p ON p.id = up.permissao_id "
                "WHERE up.usuario_id = %s", (user_id,)
            )
            overrides_add = set()
            overrides_rem = set()
            for ov in overrides:
                if ov["tipo"] == "adicionar":
                    overrides_add.add(ov["chave"])
                elif ov["tipo"] == "remover":
                    overrides_rem.add(ov["chave"])

            # 3. Calcular permissoes efetivas
            perms_efetivas = set(perms_perfil)
            perms_efetivas |= overrides_add
            perms_efetivas -= overrides_rem

            return {
                "perms_perfil": perms_perfil,
                "overrides_add": overrides_add,
                "overrides_rem": overrides_rem,
                "perms_efetivas": perms_efetivas,
            }

        def on_loaded(result):
            self._perm_perfil_set = result["perms_perfil"]
            self._perm_overrides_add = result["overrides_add"]
            self._perm_overrides_rem = result["overrides_rem"]

            for chave, var in self.perm_vars.items():
                var.set(chave in result["perms_efetivas"])
            self._atualizar_todos_indicadores()

        self.run_async(load, on_loaded)

    # ---- Salvar permissoes do perfil ----

    def _salvar_permissoes_perfil(self):
        """Salva permissoes diretamente no perfil selecionado com confirmacao."""
        if self._perfil_mode != "perfil" or not self._perfil_selected_id:
            self.show_error("Selecione um perfil na lista de perfis.")
            return

        if not Session.has_permission(PermissionConstants.PERFIS_GERENCIAR_PERMISSOES) and not Session.is_admin():
            self.show_error("Voce nao tem permissao para gerenciar permissoes de perfis.")
            return

        perfil_id = self._perfil_selected_id
        chaves_marcadas = [ch for ch, v in self.perm_vars.items() if v.get()]
        total_marcadas = len(chaves_marcadas)
        total_perms = len(self.perm_vars)

        # Encontrar nome do perfil
        nome_perfil = "Desconhecido"
        for p in self._perfil_list:
            if p["id"] == perfil_id:
                nome_perfil = p["nome"]
                break

        # Calcular diferencas em relacao ao estado anterior
        adicionadas = set(chaves_marcadas) - self._perm_perfil_set
        removidas = self._perm_perfil_set - set(chaves_marcadas)

        # Dialog de confirmacao
        msg = (f"Salvar permissoes do perfil '{nome_perfil}'?\n\n"
               f"Total de permissoes ativas: {total_marcadas} de {total_perms}\n")
        if adicionadas:
            msg += f"\nPermissoes ADICIONADAS ({len(adicionadas)}):\n"
            for ch in sorted(adicionadas)[:10]:
                msg += f"  + {ch}\n"
            if len(adicionadas) > 10:
                msg += f"  ... e mais {len(adicionadas) - 10}\n"
        if removidas:
            msg += f"\nPermissoes REMOVIDAS ({len(removidas)}):\n"
            for ch in sorted(removidas)[:10]:
                msg += f"  - {ch}\n"
            if len(removidas) > 10:
                msg += f"  ... e mais {len(removidas) - 10}\n"
        if not adicionadas and not removidas:
            msg += "\nNenhuma alteracao detectada."

        if not messagebox.askyesno("Confirmar Alteracoes", msg):
            return

        def salvar():
            db = DatabaseHelper.get_instance()
            db.execute_update("DELETE FROM perfil_permissoes WHERE perfil_id = %s", (perfil_id,))
            for chave in chaves_marcadas:
                db.execute_update(
                    "INSERT IGNORE INTO perfil_permissoes (perfil_id, permissao_id) "
                    "SELECT %s, id FROM permissoes WHERE chave = %s",
                    (perfil_id, chave))
            # Log de auditoria
            AuditLogger.log(
                "PERFIL_PERMISSOES_SALVAS",
                f"Perfil: {nome_perfil} (ID: {perfil_id}) | "
                f"Ativas: {total_marcadas}/{total_perms} | "
                f"+{len(adicionadas)} -{len(removidas)}",
                usuario=Session.user_login)

        def on_saved(_):
            self._perm_perfil_set = set(chaves_marcadas)
            self._atualizar_todos_indicadores()
            self.show_success(
                f"Permissoes do perfil '{nome_perfil}' salvas! "
                f"({total_marcadas} ativas, +{len(adicionadas)} -{len(removidas)})")

        self.run_async(salvar, on_saved)

    # ---- Salvar overrides do usuario ----

    def _salvar_overrides_usuario(self):
        """Salva overrides individuais para o usuario selecionado.
        Compara com as permissoes do perfil e salva apenas as diferencas."""
        if self._perfil_mode != "usuario" or not self._perfil_selected_user_id:
            self.show_error("Selecione um usuario na lista de usuarios.")
            return

        if not Session.has_permission(PermissionConstants.PERFIS_PERMISSOES_USUARIO) and not Session.is_admin():
            self.show_error("Voce nao tem permissao para gerenciar permissoes individuais de usuarios.")
            return

        user_id = self._perfil_selected_user_id
        user = None
        for u in self._perfil_users:
            if u["id"] == user_id:
                user = u
                break
        if not user:
            return

        chaves_marcadas = {ch for ch, v in self.perm_vars.items() if v.get()}

        # Calcular overrides
        todas_chaves = set(PermissionConstants.get_todas_chaves())
        overrides_adicionar = set()
        overrides_remover = set()
        for chave in todas_chaves:
            no_perfil = chave in self._perm_perfil_set
            marcada = chave in chaves_marcadas
            if marcada and not no_perfil:
                overrides_adicionar.add(chave)
            elif not marcada and no_perfil:
                overrides_remover.add(chave)

        # Dialog de confirmacao
        msg = (f"Salvar permissoes individuais de '{user['nome']}'?\n\n"
               f"Permissoes efetivas: {len(chaves_marcadas)} de {len(todas_chaves)}\n")

        if overrides_adicionar:
            msg += f"\nOverrides ADICIONADOS ({len(overrides_adicionar)}):\n"
            msg += f"  (Permissoes extras alem do perfil)\n"
            for ch in sorted(overrides_adicionar)[:8]:
                msg += f"  + {ch}\n"
            if len(overrides_adicionar) > 8:
                msg += f"  ... e mais {len(overrides_adicionar) - 8}\n"

        if overrides_remover:
            msg += f"\nOverrides REMOVIDOS ({len(overrides_remover)}):\n"
            msg += f"  (Permissoes do perfil bloqueadas para este usuario)\n"
            for ch in sorted(overrides_remover)[:8]:
                msg += f"  - {ch}\n"
            if len(overrides_remover) > 8:
                msg += f"  ... e mais {len(overrides_remover) - 8}\n"

        if not overrides_adicionar and not overrides_remover:
            msg += "\nNenhum override individual. O usuario seguira exatamente o perfil."

        if not messagebox.askyesno("Confirmar Overrides", msg):
            return

        def salvar():
            db = DatabaseHelper.get_instance()
            # Limpar overrides anteriores
            db.execute_update("DELETE FROM usuario_permissoes WHERE usuario_id = %s", (user_id,))
            # Inserir novos overrides
            for chave in overrides_adicionar:
                db.execute_update(
                    "INSERT IGNORE INTO usuario_permissoes (usuario_id, permissao_id, tipo, criado_por) "
                    "SELECT %s, id, 'adicionar', %s FROM permissoes WHERE chave = %s",
                    (user_id, Session.user_id, chave))
            for chave in overrides_remover:
                db.execute_update(
                    "INSERT IGNORE INTO usuario_permissoes (usuario_id, permissao_id, tipo, criado_por) "
                    "SELECT %s, id, 'remover', %s FROM permissoes WHERE chave = %s",
                    (user_id, Session.user_id, chave))
            # Log de auditoria
            AuditLogger.log(
                "USUARIO_OVERRIDES_SALVOS",
                f"Usuario: {user['nome']} (ID: {user_id}) | "
                f"+{len(overrides_adicionar)} -{len(overrides_remover)}",
                usuario=Session.user_login)

        def on_saved(_):
            self._perm_overrides_add = overrides_adicionar
            self._perm_overrides_rem = overrides_remover
            self._atualizar_todos_indicadores()
            self.show_success(
                f"Permissoes de '{user['nome']}' salvas! "
                f"(+{len(overrides_adicionar)} adicionadas, -{len(overrides_remover)} removidas)")

        self.run_async(salvar, on_saved)

    # ---- Resetar overrides do usuario ----

    def _resetar_overrides_usuario(self):
        """Remove todos os overrides individuais do usuario, voltando ao perfil base."""
        if not self._perfil_selected_user_id:
            self.show_error("Selecione um usuario para resetar os overrides.")
            return

        user_id = self._perfil_selected_user_id
        user = None
        for u in self._perfil_users:
            if u["id"] == user_id:
                user = u
                break
        if not user:
            return

        if not messagebox.askyesno(
            "Resetar Overrides",
            f"Deseja remover TODOS os overrides individuais de '{user['nome']}'?\n\n"
            f"O usuario passara a seguir exatamente as permissoes do seu perfil base.\n"
            f"Esta acao nao pode ser desfeita."):
            return

        def resetar():
            db = DatabaseHelper.get_instance()
            db.execute_update("DELETE FROM usuario_permissoes WHERE usuario_id = %s", (user_id,))
            AuditLogger.log(
                "USUARIO_OVERRIDES_RESETADOS",
                f"Usuario: {user['nome']} (ID: {user_id}) - Todos os overrides removidos",
                usuario=Session.user_login)

        def on_resetado(_):
            self._perm_overrides_add = set()
            self._perm_overrides_rem = set()
            # Recarregar permissoes do perfil base
            for chave, var in self.perm_vars.items():
                var.set(chave in self._perm_perfil_set)
            self._atualizar_todos_indicadores()
            self.show_success(f"Overrides de '{user['nome']}' resetados com sucesso!")

        self.run_async(resetar, on_resetado)

    # ---- Copiar permissoes de outro perfil ----

    def _copiar_permissoes_de_perfil(self):
        """Copia as permissoes de outro perfil para o perfil atualmente selecionado."""
        if self._perfil_mode != "perfil" or not self._perfil_selected_id:
            self.show_error("Selecione um perfil de destino na lista de perfis.")
            return

        perfil_destino_id = self._perfil_selected_id
        nome_destino = "Desconhecido"
        for p in self._perfil_list:
            if p["id"] == perfil_destino_id:
                nome_destino = p["nome"]
                break

        # Dialog para escolher perfil de origem
        dialog = tk.Toplevel(self.root)
        dialog.title("Copiar Permissoes de Outro Perfil")
        dialog.geometry("400x350")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text="Copiar Permissoes", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=(15, 5))
        tk.Label(dialog, text=f"Destino: {nome_destino}\n\nSelecione o perfil de ORIGEM:",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10),
                 justify="center").pack(pady=(0, 10))

        list_origem = tk.Listbox(dialog, bg=COR_ENTRADA, fg=COR_TEXTO,
                                  font=("Segoe UI", 10), selectbackground=COR_PRIMARIA,
                                  height=8)
        list_origem.pack(fill="x", padx=20, pady=5)

        perfis_origem = []
        for p in self._perfil_list:
            if p["id"] != perfil_destino_id:
                list_origem.insert(tk.END, p["nome"])
                perfis_origem.append(p)

        def confirmar():
            sel = list_origem.curselection()
            if not sel:
                messagebox.showwarning(
                    "Selecione um Perfil",
                    "Nenhum perfil de origem foi selecionado.\n\n"
                    "Clique sobre o perfil que deseja copiar na lista\n"
                    "e tente novamente.",
                    parent=dialog)
                return
            perfil_origem = perfis_origem[sel[0]]
            dialog.destroy()

            def copiar():
                db = DatabaseHelper.get_instance()
                # Carregar permissoes do perfil de origem
                rows = db.execute_query(
                    "SELECT p.chave FROM permissoes p "
                    "JOIN perfil_permissoes pp ON pp.permissao_id = p.id "
                    "WHERE pp.perfil_id = %s", (perfil_origem["id"],)
                )
                return {r["chave"] for r in rows}

            def on_copiado(perms_origem):
                # Aplicar nos checkboxes
                for chave, var in self.perm_vars.items():
                    var.set(chave in perms_origem)
                self._atualizar_todos_indicadores()
                ToastManager.info(
                    f"Permissoes de '{perfil_origem['nome']}' carregadas. "
                    f"Clique em 'Salvar Permissoes do Perfil' para confirmar.")

            self.run_async(copiar, on_copiado)

        btn_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_frame.pack(fill="x", padx=20, pady=10)
        StyledButton(btn_frame, text="Copiar", command=confirmar,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=5)
        StyledButton(btn_frame, text="Cancelar", command=dialog.destroy,
                     color=COR_FUNDO3, width=12).pack(side="right", padx=5)

    # ---- Vincular perfil ao usuario (callback do CRUD de usuarios) ----

    def _vincular_perfil_ao_usuario(self, user_id, vals, campos):
        """Callback chamado apos salvar usuario. Vincula o perfil_id baseado no nivel."""
        try:
            nivel_idx = None
            for i, c in enumerate(campos):
                if c["nome"] == "nivel":
                    nivel_idx = i
                    break
            if nivel_idx is None:
                return
            nivel = vals[nivel_idx]
            nivel_perfil_map = {
                "admin": "Administrador", "gerente": "Gerente", "operador": "Operacional",
                "caixa": "Caixa", "atendente": "Atendente", "garcom": "Garcom",
                "balcao": "Balcao", "vendedor": "Vendedor", "estoquista": "Estoquista",
                "entregador": "Entregador",
            }
            nome_perfil = nivel_perfil_map.get(nivel, "Operacional")
            db = DatabaseHelper.get_instance()
            perfil_id = PermissionDatabaseSetup._get_perfil_id(db, nome_perfil)
            if perfil_id > 0:
                db.execute_update("UPDATE usuarios SET perfil_id = %s WHERE id = %s", (perfil_id, user_id))
        except Exception as e:
            print(f"Aviso vincular perfil: {e}")

    # ========================================================================
    # SERVIDOR WEB (CARDAPIO DIGITAL / CHAMADOS)
    # ========================================================================
    def show_servidor_web(self):
        self.clear_container()

        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Servidor Web / Cardapio Digital", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)

        card = CardFrame(content)
        card.pack(fill="x", pady=10)

        self.lbl_server_status = tk.Label(card, text="Servidor: PARADO", bg=COR_CARD,
                                           fg=COR_ERRO, font=("Segoe UI", 14, "bold"))
        self.lbl_server_status.pack(pady=10)

        port_frame = tk.Frame(card, bg=COR_CARD)
        port_frame.pack(fill="x", padx=20, pady=5)
        tk.Label(port_frame, text="Porta:", bg=COR_CARD, fg=COR_TEXTO).pack(side="left")
        self.et_server_port = StyledEntry(port_frame, width=8)
        self.et_server_port.insert(0, str(self.web_server_port))
        self.et_server_port.pack(side="left", padx=10)

        btn_frame = tk.Frame(card, bg=COR_CARD)
        btn_frame.pack(fill="x", padx=20, pady=10)

        self.btn_start_server = StyledButton(btn_frame, text="▶ Iniciar Servidor",
            command=self.start_web_server, color=COR_BOTAO_VERDE, width=15)
        self.btn_start_server.pack(side="left", padx=5)

        self.btn_stop_server = StyledButton(btn_frame, text="✖ Parar Servidor",
            command=self.stop_web_server, color=COR_ERRO, width=15)
        self.btn_stop_server.pack(side="left", padx=5)
        self.btn_stop_server.config(state="disabled")

        # Info
        info_card = CardFrame(content)
        info_card.pack(fill="x", pady=10)
        tk.Label(info_card, text="Funcionalidades do Servidor Web:", bg=COR_CARD,
                 fg=COR_TEXTO, font=("Segoe UI", 11, "bold")).pack(anchor="w", pady=5)
        funcionalidades = [
            "- Cardapio Digital acessivel via navegador",
            "- Pedidos online dos clientes",
            "- Chamados de mesa (garcom)",
            "- API REST para integracao",
            "- Painel de pedidos em tempo real",
        ]
        for f in funcionalidades:
            tk.Label(info_card, text=f, bg=COR_CARD, fg=COR_TEXTO2,
                     font=("Segoe UI", 10)).pack(anchor="w", padx=10)

        if self.web_server and self.web_server.is_alive():
            self.lbl_server_status.config(text=f"Servidor: ATIVO (porta {self.web_server_port})",
                                           fg=COR_SUCESSO)
            self.btn_start_server.config(state="disabled")
            self.btn_stop_server.config(state="normal")

    def start_web_server(self):
        try:
            port = int(self.et_server_port.get())
        except ValueError:
            self.show_error("Porta invalida.")
            return
        self.web_server_port = port

        from http.server import HTTPServer, BaseHTTPRequestHandler

        app_ref = self

        class PDVHandler(BaseHTTPRequestHandler):
            def do_GET(self):
                if self.path == "/" or self.path == "/cardapio":
                    self.send_response(200)
                    self.send_header("Content-Type", "text/html; charset=utf-8")
                    self.end_headers()
                    html = app_ref._gerar_cardapio_html()
                    self.wfile.write(html.encode("utf-8"))
                elif self.path == "/api/produtos":
                    self.send_response(200)
                    self.send_header("Content-Type", "application/json; charset=utf-8")
                    self.end_headers()
                    try:
                        db = DatabaseHelper.get_instance()
                        rows = db.execute_query(
                            "SELECT id, descricao, preco_venda FROM produtos WHERE ativo = 1 ORDER BY descricao"
                        )
                        self.wfile.write(json.dumps(rows, default=str).encode("utf-8"))
                    except Exception as e:
                        self.wfile.write(json.dumps({"error": str(e)}).encode("utf-8"))
                elif self.path == "/api/chamados":
                    self.send_response(200)
                    self.send_header("Content-Type", "application/json; charset=utf-8")
                    self.end_headers()
                    try:
                        db = DatabaseHelper.get_instance()
                        rows = db.execute_query(
                            "SELECT * FROM chamados WHERE status = 'pendente' ORDER BY id DESC"
                        )
                        self.wfile.write(json.dumps(rows, default=str).encode("utf-8"))
                    except Exception as e:
                        self.wfile.write(json.dumps({"error": str(e)}).encode("utf-8"))
                else:
                    self.send_response(404)
                    self.end_headers()

            def do_POST(self):
                content_length = int(self.headers.get("Content-Length", 0))
                body = self.rfile.read(content_length).decode("utf-8")
                if self.path == "/api/chamado":
                    self.send_response(200)
                    self.send_header("Content-Type", "application/json; charset=utf-8")
                    self.end_headers()
                    try:
                        data = json.loads(body)
                        db = DatabaseHelper.get_instance()
                        db.execute_update(
                            "INSERT INTO chamados (mesa_numero, tipo, mensagem, status) "
                            "VALUES (%s, %s, %s, 'pendente')",
                            (data.get("mesa", 0), data.get("tipo", "garcom"), data.get("mensagem", ""))
                        )
                        self.wfile.write(json.dumps({"ok": True}).encode("utf-8"))
                    except Exception as e:
                        self.wfile.write(json.dumps({"error": str(e)}).encode("utf-8"))
                elif self.path == "/api/pedido":
                    self.send_response(200)
                    self.send_header("Content-Type", "application/json; charset=utf-8")
                    self.end_headers()
                    try:
                        data = json.loads(body)
                        db = DatabaseHelper.get_instance()
                        db.execute_update(
                            "INSERT INTO pedidos_web (mesa_numero, itens_json, observacao, status) "
                            "VALUES (%s, %s, %s, 'pendente')",
                            (data.get("mesa", 0), json.dumps(data.get("itens", [])),
                             data.get("observacao", ""))
                        )
                        self.wfile.write(json.dumps({"ok": True}).encode("utf-8"))
                    except Exception as e:
                        self.wfile.write(json.dumps({"error": str(e)}).encode("utf-8"))
                else:
                    self.send_response(404)
                    self.end_headers()

            def log_message(self, format, *args):
                pass  # Silenciar logs

        def run_server():
            try:
                server = HTTPServer(("0.0.0.0", port), PDVHandler)
                server.serve_forever()
            except Exception as e:
                print(f"Erro no servidor: {e}")

        self.web_server = threading.Thread(target=run_server, daemon=True)
        self.web_server.start()

        self.lbl_server_status.config(text=f"Servidor: ATIVO (porta {port})", fg=COR_SUCESSO)
        self.btn_start_server.config(state="disabled")
        self.btn_stop_server.config(state="normal")
        self.show_success(f"Servidor web iniciado na porta {port}!")

    def stop_web_server(self):
        self.web_server = None
        self.lbl_server_status.config(text="Servidor: PARADO", fg=COR_ERRO)
        self.btn_start_server.config(state="normal")
        self.btn_stop_server.config(state="disabled")
        self.show_info_dialog("Info", "O servidor sera parado ao fechar o aplicativo.")

    def _gerar_cardapio_html(self):
        try:
            db = DatabaseHelper.get_instance()
            produtos = db.execute_query(
                "SELECT p.descricao, p.preco_venda, "
                "COALESCE(tp.descricao,'Geral') as categoria "
                "FROM produtos p "
                "LEFT JOIN tipos_produto tp ON p.tipo_produto_id = tp.id "
                "WHERE p.ativo = 1 ORDER BY categoria, p.descricao"
            )
            empresa = db.execute_query("SELECT * FROM empresa LIMIT 1")
            emp = empresa[0] if empresa else {}
            nome_empresa = emp.get("nome_fantasia") or emp.get("razao_social") or "PDV Pro"
            telefone = emp.get("telefone", "")
            endereco_parts = [emp.get("endereco",""), emp.get("numero",""),
                              emp.get("bairro",""), emp.get("cidade","")]
            endereco = ", ".join(p for p in endereco_parts if p)
        except Exception:
            produtos = []
            nome_empresa = "PDV Pro"
            telefone = ""
            endereco = ""

        # Agrupar por categoria
        cats = {}
        for p in produtos:
            cat = p.get("categoria", "Geral")
            if cat not in cats:
                cats[cat] = []
            cats[cat].append(p)

        cats_html = ""
        for cat, items in cats.items():
            itens_html = ""
            for p in items:
                itens_html += f"""
          <div class="item" data-nome="{p['descricao'].lower()}">
            <div class="item-info">
              <span class="item-nome">{p['descricao']}</span>
            </div>
            <div class="item-right">
              <span class="item-preco">R$ {FormatUtils.format_money(p['preco_venda'])}</span>
              <button class="btn-add" onclick="addItem('{p['descricao'].replace(chr(39),'').replace(chr(34),'')}',
                {float(p['preco_venda']):.2f})">+ Adicionar</button>
            </div>
          </div>"""
            cats_html += f"""
        <div class="categoria">
          <div class="cat-header">{cat}</div>
          {itens_html}
        </div>"""

        info_bar = ""
        if telefone:
            info_bar += f'<span>&#128222; {telefone}</span>'
        if endereco:
            info_bar += f'<span>&#128205; {endereco}</span>'

        return f"""<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cardapio Digital - {nome_empresa}</title>
<style>
  * {{ box-sizing: border-box; margin: 0; padding: 0; }}
  body {{ font-family: 'Segoe UI', sans-serif; background: #0a0e1a;
         color: #e8eaed; min-height: 100vh; }}
  header {{ background: linear-gradient(135deg,#0d1225,#111830);
            border-bottom: 2px solid #00d4ff; padding: 18px 20px;
            position: sticky; top: 0; z-index: 100; }}
  .header-top {{ display: flex; align-items: center;
                  justify-content: space-between; }}
  h1 {{ color: #00d4ff; font-size: 1.8rem; }}
  .badge {{ background:#00d4ff; color:#0a0e1a; padding:4px 12px;
             border-radius:20px; font-size:.75rem; font-weight:700; }}
  .info-bar {{ display:flex; gap:20px; margin-top:8px;
               font-size:.85rem; color:#8892a4; flex-wrap:wrap; }}
  .search-bar {{ margin-top:10px; }}
  .search-bar input {{ width:100%; padding:10px 16px; border-radius:8px;
    border:1px solid #2a3a5c; background:#0f1629; color:#e8eaed;
    font-size:1rem; outline:none; transition:.2s; }}
  .search-bar input:focus {{ border-color:#00d4ff; }}
  main {{ max-width:860px; margin:0 auto; padding:20px; }}
  .categoria {{ margin-bottom:24px; }}
  .cat-header {{ background:#111830; color:#00d4ff; font-weight:700;
                  padding:8px 14px; border-radius:6px; margin-bottom:8px;
                  border-left:4px solid #00d4ff; }}
  .item {{ display:flex; justify-content:space-between; align-items:center;
            padding:12px 16px; background:#131a2e; border-radius:8px;
            margin-bottom:6px; border:1px solid #1e2d4a;
            transition:background .2s; }}
  .item:hover {{ background:#1a2340; border-color:#00d4ff33; }}
  .item-nome {{ font-size:1rem; font-weight:500; }}
  .item-right {{ display:flex; align-items:center; gap:14px; flex-shrink:0; }}
  .item-preco {{ color:#00f5a0; font-weight:700; font-size:1rem; white-space:nowrap; }}
  .btn-add {{ background:#00d4ff; color:#0a0e1a; border:none;
               padding:6px 14px; border-radius:6px; font-weight:700;
               cursor:pointer; font-size:.85rem; transition:.2s; white-space:nowrap; }}
  .btn-add:hover {{ background:#33ddff; transform:scale(1.04); }}
  .btn-add:active {{ transform:scale(.97); }}
  .cart-fab {{ position:fixed; bottom:24px; right:24px; background:#00d4ff;
                color:#0a0e1a; border:none; border-radius:50px; padding:14px 24px;
                font-size:1rem; font-weight:700; cursor:pointer; box-shadow:0 4px 20px #00d4ff55;
                display:none; align-items:center; gap:8px; transition:.2s; z-index:200; }}
  .cart-fab.visible {{ display:flex; }}
  .cart-fab:hover {{ background:#33ddff; transform:translateY(-2px); }}
  .cart-modal {{ display:none; position:fixed; top:0; left:0; right:0; bottom:0;
                  background:#000000aa; z-index:300; align-items:flex-end;
                  justify-content:center; }}
  .cart-modal.open {{ display:flex; }}
  .cart-box {{ background:#111830; border-radius:16px 16px 0 0; padding:24px;
               width:100%; max-width:600px; max-height:80vh; overflow-y:auto;
               border-top:2px solid #00d4ff; }}
  .cart-box h2 {{ color:#00d4ff; margin-bottom:16px; }}
  .cart-item {{ display:flex; justify-content:space-between; align-items:center;
                 padding:10px 0; border-bottom:1px solid #1e2d4a; }}
  .cart-item-nome {{ flex:1; }}
  .cart-item-preco {{ color:#00f5a0; font-weight:700; margin:0 16px; }}
  .btn-rm {{ background:#ff3860; color:#fff; border:none; border-radius:4px;
              padding:4px 10px; cursor:pointer; font-size:.8rem; }}
  .cart-total {{ font-size:1.3rem; font-weight:700; color:#00d4ff;
                  text-align:right; margin-top:14px; }}
  .btn-pedido {{ width:100%; padding:14px; background:#00c853; color:#fff;
                  border:none; border-radius:10px; font-size:1.1rem;
                  font-weight:700; cursor:pointer; margin-top:14px; transition:.2s; }}
  .btn-pedido:hover {{ background:#00e676; }}
  .btn-fechar {{ background:transparent; border:none; color:#8892a4;
                  cursor:pointer; font-size:1.5rem; float:right; line-height:1; }}
  .empty {{ text-align:center; color:#8892a4; padding:40px; font-size:1.1rem; }}
  @media(max-width:600px){{
    h1 {{ font-size:1.4rem; }}
    .item {{ flex-direction:column; align-items:flex-start; gap:8px; }}
    .item-right {{ width:100%; justify-content:space-between; }}
  }}
</style>
</head>
<body>
<header>
  <div class="header-top">
    <h1>&#127859; {nome_empresa}</h1>
    <span class="badge">Cardapio Digital</span>
  </div>
  <div class="info-bar">{info_bar}</div>
  <div class="search-bar">
    <input type="text" id="busca" placeholder="&#128269; Buscar produto..."
           oninput="filtrar(this.value)">
  </div>
</header>

<main id="cardapio">
  {cats_html if cats_html else '<div class="empty">Nenhum produto disponivel no momento.</div>'}
</main>

<button class="cart-fab" id="cartFab" onclick="toggleCart()">
  &#128722; Carrinho <span id="cartCount">0</span>
</button>

<div class="cart-modal" id="cartModal" onclick="if(event.target===this)toggleCart()">
  <div class="cart-box">
    <button class="btn-fechar" onclick="toggleCart()">&#10005;</button>
    <h2>&#128722; Meu Pedido</h2>
    <div id="cartItems"></div>
    <div class="cart-total" id="cartTotal"></div>
    <button class="btn-pedido" onclick="enviarPedido()">
      &#10003; Enviar Pedido para a Cozinha
    </button>
  </div>
</div>

<script>
var cart = [];
function addItem(nome, preco) {{
  var idx = cart.findIndex(function(i){{ return i.nome===nome; }});
  if (idx >= 0) {{ cart[idx].qtd++; }}
  else {{ cart.push({{nome:nome, preco:preco, qtd:1}}); }}
  updateCart();
  var fab = document.getElementById('cartFab');
  fab.style.transform = 'scale(1.15)';
  setTimeout(function(){{ fab.style.transform=''; }}, 180);
}}
function removeItem(idx) {{
  cart.splice(idx, 1);
  updateCart();
}}
function updateCart() {{
  var count = cart.reduce(function(a,i){{return a+i.qtd;}},0);
  document.getElementById('cartCount').textContent = count;
  var fab = document.getElementById('cartFab');
  if (count > 0) {{ fab.classList.add('visible'); }}
  else {{ fab.classList.remove('visible'); }}
  var html = '';
  var total = 0;
  cart.forEach(function(item, idx) {{
    var subtotal = item.preco * item.qtd;
    total += subtotal;
    html += '<div class="cart-item">' +
      '<span class="cart-item-nome">' + item.qtd + 'x ' + item.nome + '</span>' +
      '<span class="cart-item-preco">R$ ' + subtotal.toFixed(2).replace('.',',') + '</span>' +
      '<button class="btn-rm" onclick="removeItem(' + idx + ')">&#10005;</button>' +
      '</div>';
  }});
  document.getElementById('cartItems').innerHTML = html || '<p style="color:#8892a4;text-align:center;padding:20px">Carrinho vazio</p>';
  document.getElementById('cartTotal').textContent = 'Total: R$ ' + total.toFixed(2).replace('.',',');
}}
function toggleCart() {{
  var m = document.getElementById('cartModal');
  m.classList.toggle('open');
}}
function filtrar(v) {{
  v = v.toLowerCase();
  document.querySelectorAll('.item').forEach(function(el) {{
    el.style.display = el.dataset.nome.includes(v) ? '' : 'none';
  }});
  document.querySelectorAll('.categoria').forEach(function(cat) {{
    var visible = Array.from(cat.querySelectorAll('.item')).some(
      function(i){{ return i.style.display !== 'none'; }});
    cat.style.display = visible ? '' : 'none';
  }});
}}
function enviarPedido() {{
  if (cart.length === 0) {{ alert('Adicione itens ao carrinho primeiro!'); return; }}
  var mesa = prompt('Numero da sua mesa:');
  if (!mesa) return;
  var nome = prompt('Seu nome:') || 'Cliente';
  var total = cart.reduce(function(a,i){{return a+i.preco*i.qtd;}},0);
  fetch('/api/pedido', {{
    method: 'POST',
    headers: {{'Content-Type':'application/json'}},
    body: JSON.stringify({{
      mesa: parseInt(mesa),
      cliente_nome: nome,
      itens: cart,
      total: total,
      observacao: ''
    }})
  }}).then(function(r){{ return r.json(); }})
    .then(function(d) {{
      if (d.ok) {{
        alert('Pedido enviado com sucesso! Mesa ' + mesa);
        cart = []; updateCart(); toggleCart();
      }} else {{ alert('Erro: ' + (d.error||'Tente novamente')); }}
    }}).catch(function(){{ alert('Erro de conexao com o servidor.'); }});
}}
</script>
</body>
</html>"""



    def _enviar_cupom_servidor_fr3(self, texto, config):
        """Envia o cupom (texto ja formatado) para o Servidor de Impressao .fr3
        (print_server.py) via HTTP POST. O servidor preenche o modelo .fr3 e,
        se configurado, imprime. Retorna (sucesso, mensagem)."""
        url = (config.get("servidor_impressao_url") or "").strip()
        if not url:
            return False, ("Servidor de impressao habilitado, mas a URL nao foi "
                           "configurada em Configuracoes de Impressora.")
        payload = {
            "texto": texto,
            "titulo": "CUPOM NAO FISCAL",
            "copias": int(config.get("num_copias", 1) or 1),
            "impressora": config.get("nome_impressora", ""),
            "largura": int(config.get("largura_papel", 48) or 48),
            "corte": bool(config.get("corte_automatico", True)),
            "gaveta": bool(config.get("abrir_gaveta", False)),
            "origem": "PDV Pro",
            "data_hora": datetime.datetime.now().strftime("%d/%m/%Y %H:%M:%S"),
        }
        try:
            import urllib.request
            data = json.dumps(payload).encode("utf-8")
            req = urllib.request.Request(
                url, data=data,
                headers={"Content-Type": "application/json"}, method="POST")
            with urllib.request.urlopen(req, timeout=8) as resp:
                corpo = resp.read().decode("utf-8", errors="replace")
                try:
                    r = json.loads(corpo)
                except Exception:
                    r = {}
                if getattr(resp, "status", 200) == 200 and r.get("ok", True):
                    arq = r.get("arquivo_fr3") or ""
                    extra = f"\nArquivo .fr3: {arq}" if arq else ""
                    return True, f"Enviado ao Servidor de Impressao (.fr3).{extra}"
                return False, f"Servidor de impressao retornou erro: {r.get('erro', corpo[:200])}"
        except Exception as e:
            return False, (
                f"Nao foi possivel conectar ao Servidor de Impressao em:\n{url}\n\n"
                f"Verifique se o 'print_server.py' esta em execucao naquele "
                f"computador e se a URL/porta estao corretas.\n\nDetalhe: {e}")

    def _logo_para_escpos(self, caminho, largura_dots=576):
        """Converte uma imagem (PNG/JPG/BMP...) em comando ESC/POS raster
        (GS v 0) para imprimir no topo do cupom termico. Retorna bytes
        (vazio se PIL/Pillow indisponivel ou imagem invalida)."""
        try:
            if not HAS_PIL:
                _logger.warning("Logo ignorado: biblioteca Pillow (PIL) nao instalada.")
                return b""
            if not caminho or not os.path.exists(caminho):
                _logger.warning(f"Logo ignorado: arquivo nao encontrado ({caminho}).")
                return b""
            from PIL import Image
            img = Image.open(caminho).convert("L")
            w, h = img.size
            # Redimensiona mantendo proporcao, limitando a largura do papel
            if w > largura_dots:
                nh = max(1, int(h * largura_dots / float(w)))
                img = img.resize((largura_dots, nh))
                w, h = img.size
            width_bytes = (w + 7) // 8
            data = bytearray()
            for y in range(h):
                for xb in range(width_bytes):
                    byte = 0
                    for bit in range(8):
                        x = xb * 8 + bit
                        if x < w and img.getpixel((x, y)) < 128:
                            byte |= (0x80 >> bit)  # pixel escuro => imprime
                    data.append(byte)
            xL, xH = width_bytes & 0xff, (width_bytes >> 8) & 0xff
            yL, yH = h & 0xff, (h >> 8) & 0xff
            # GS v 0 m xL xH yL yH data   (m=0 => normal)
            return b"\x1d\x76\x30\x00" + bytes([xL, xH, yL, yH]) + bytes(data)
        except Exception as e:
            _logger.warning(f"Falha ao gerar logo ESC/POS: {e}")
            return b""

    def _render_cupom_imagem(self, texto, config, width_px=576):
        """Renderiza o cupom (texto) numa imagem PIL, com a fonte dimensionada
        para PREENCHER a largura conforme o numero de colunas (Largura do
        Papel): menos colunas = fonte maior. Usada tanto na impressao grafica
        quanto na PREVIA da tela (o que se ve = o que sai no papel).
        Retorna a imagem PIL (modo 'L') ou None se o Pillow faltar."""
        try:
            from PIL import Image, ImageDraw, ImageFont
        except Exception:
            return None
        try:
            try:
                cols = int(float(config.get("largura_papel", 48) or 48))
            except Exception:
                cols = 48
            cols = max(16, min(cols, 64))
            # Monospace ~0.6*px de largura -> px que preenche a largura
            px = max(14, int((float(width_px) / cols) / 0.6))
            fonte = None
            for fp in (r"C:\Windows\Fonts\consolab.ttf", r"C:\Windows\Fonts\courbd.ttf",
                       r"C:\Windows\Fonts\consola.ttf", r"C:\Windows\Fonts\cour.ttf",
                       r"C:\Windows\Fonts\lucon.ttf",
                       "/usr/share/fonts/truetype/dejavu/DejaVuSansMono.ttf"):
                try:
                    fonte = ImageFont.truetype(fp, px)
                    break
                except Exception:
                    continue
            if fonte is None:
                fonte = ImageFont.load_default()
            tmp = Image.new("L", (10, 10), 255)
            dt = ImageDraw.Draw(tmp)
            try:
                bb = dt.textbbox((0, 0), "Ag", font=fonte)
                ch = bb[3] - bb[1]
            except Exception:
                ch = px
            line_h = int(ch * 1.15) + 2
            linhas = texto.split("\n")
            logo_img = None
            if config.get("imprimir_logo") and (config.get("caminho_logo") or "").strip():
                try:
                    lg = Image.open(config["caminho_logo"].strip()).convert("L")
                    if lg.width > width_px:
                        nh = int(lg.height * width_px / lg.width)
                        lg = lg.resize((width_px, nh))
                    logo_img = lg
                except Exception:
                    logo_img = None
            top = (logo_img.height + 10) if logo_img else 0
            altura = top + line_h * max(1, len(linhas)) + int(line_h * 1.5)
            img = Image.new("L", (width_px, altura), 255)
            draw = ImageDraw.Draw(img)
            y = 0
            if logo_img:
                img.paste(logo_img, ((width_px - logo_img.width) // 2, 0))
                y = logo_img.height + 10
            for ln in linhas:
                draw.text((0, y), ln.rstrip("\r"), font=fonte, fill=0)
                y += line_h
            return img
        except Exception as e:
            _logger.warning(f"Falha ao renderizar imagem do cupom: {e}")
            return None

    def _imprimir_grafico_windows(self, texto, config):
        """Imprime o cupom como IMAGEM via GDI (win32ui), desenhando o texto
        com a fonte dimensionada para PREENCHER a largura do papel conforme o
        numero de colunas configurado.

        Funciona em QUALQUER driver (mesmo quando o ESC/POS e ignorado), pois
        nos mesmos desenhamos as letras. Para AUMENTAR a fonte, diminua o
        numero de colunas (Largura do Papel): menos colunas = fonte maior.
        A imagem e impressa em resolucao NATIVA (1:1 em pontos do
        dispositivo), sem re-escala horizontal, para o tamanho ser fiel.

        Retorna (sucesso: bool, mensagem: str).
        """
        try:
            import win32ui, win32con, win32print
            from PIL import Image, ImageDraw, ImageFont, ImageWin
        except Exception as e:
            return False, ("O modo grafico precisa dos pacotes 'pywin32' e "
                           f"'Pillow' instalados.\n\nDetalhe: {e}")
        hdc = None
        try:
            # Abre o contexto da impressora (tenta a configurada e, se falhar,
            # a impressora padrao do Windows).
            nome_cfg = (config.get("nome_impressora") or "").strip()
            candidatos = []
            if nome_cfg:
                candidatos.append(nome_cfg)
            try:
                padrao = win32print.GetDefaultPrinter()
                if padrao and padrao not in candidatos:
                    candidatos.append(padrao)
            except Exception:
                pass
            printer = None
            erro_dc = None
            for cand in candidatos:
                try:
                    hdc = win32ui.CreateDC()
                    hdc.CreatePrinterDC(cand)
                    printer = cand
                    break
                except Exception as e:
                    erro_dc = e
                    try:
                        if hdc is not None:
                            hdc.DeleteDC()
                    except Exception:
                        pass
                    hdc = None
            if hdc is None or printer is None:
                return False, (
                    "Nao foi possivel abrir a impressora no modo grafico.\n"
                    f"Impressora configurada: '{nome_cfg or '(padrao)'}'.\n"
                    f"Detalhe: {erro_dc}\n\n"
                    "Verifique o nome exato da impressora em Configuracoes de "
                    "Impressora (deve ser igual ao do Windows).")
            # Largura imprimivel em PONTOS (dots) reais do dispositivo
            try:
                dev_w = int(hdc.GetDeviceCaps(win32con.HORZRES))
            except Exception:
                dev_w = 0
            tam = str(config.get("tamanho_papel", "80mm"))
            padrao_w = 576 if "80" in tam else 384
            # Usa a largura do dispositivo quando plausivel (evita paginas A4)
            width_px = dev_w if 200 <= dev_w <= 1300 else padrao_w

            # Numero de colunas define o tamanho da fonte (menos colunas =
            # fonte maior). A renderizacao e feita por _render_cupom_imagem,
            # a MESMA usada na previa da tela (o que voce ve = o que sai).
            try:
                cols = int(float(config.get("largura_papel", 48) or 48))
            except Exception:
                cols = 48
            cols = max(16, min(cols, 64))
            img = self._render_cupom_imagem(texto, config, width_px)
            if img is None:
                return False, "Falha ao gerar a imagem do cupom (Pillow indisponivel)."

            hdc.StartDoc("PDV Pro Cupom")
            hdc.StartPage()
            dib = ImageWin.Dib(img.convert("RGB"))
            # 1:1 em pontos do dispositivo (sem re-escala horizontal)
            dib.draw(hdc.GetHandleOutput(), (0, 0, img.width, img.height))
            hdc.EndPage()
            hdc.EndDoc()
            return True, f"Impresso (modo grafico, {cols} colunas) em: {printer}"
        except Exception as e:
            return False, f"Falha na impressao grafica: {e}"
        finally:
            try:
                if hdc is not None:
                    hdc.DeleteDC()
            except Exception:
                pass

    # ========================================================================
    # SISTEMA DE IMPRESSAO CENTRALIZADO
    # ========================================================================

    def _imprimir_texto(self, texto, config=None, parent_window=None):
        """Metodo centralizado de impressao.
        Envia texto para a impressora configurada usando o metodo mais adequado:
        - Windows: win32print (RAW) > PowerShell Out-Printer > copy /b porta > Notepad /p
        - Linux: lp -d impressora > lpr -P impressora
        - macOS: lpr -P impressora
        Aplica codepage, comandos ESC/POS (corte, gaveta) e numero de copias.
        Retorna (sucesso: bool, mensagem: str).
        NOTA: Este metodo pode ser chamado de thread secundaria (via run_async).
        Para tipo PDF (Arquivo), salva automaticamente em pasta padrao.
        """
        if config is None:
            config = self._load_printer_config()

        nome_impressora = config.get("nome_impressora", "").strip()
        porta = config.get("porta_impressora", "").strip()
        codepage = config.get("codepage", "UTF-8").strip()
        tipo = config.get("tipo_impressora", "Termica")
        num_copias = config.get("num_copias", 1)
        corte_auto = config.get("corte_automatico", False)
        abrir_gaveta = config.get("abrir_gaveta", False)

        if num_copias < 1:
            num_copias = 1

        # === Servidor de Impressao (.fr3) ===
        # Se habilitado, SUBSTITUI a impressao direta: envia o cupom para o
        # servidor de impressao local, que preenche o modelo .fr3 e imprime.
        if config.get("usar_servidor_impressao"):
            return self._enviar_cupom_servidor_fr3(texto, config)

        # === Metodo GRAFICO (imagem) ===
        # Desenha o cupom como imagem com a fonte no tamanho exato. Funciona
        # em qualquer driver (mesmo quando o ESC/POS e ignorado). E a forma
        # mais confiavel de controlar o tamanho da fonte impressa.
        import platform as _plat_g
        if (str(config.get("metodo_impressao", "")).lower().startswith("graf")
                and _plat_g.system() == "Windows"):
            _ult = (False, "Modo grafico indisponivel.")
            for _ in range(num_copias):
                _ult = self._imprimir_grafico_windows(texto, config)
                if not _ult[0]:
                    break
            if _ult[0]:
                return True, _ult[1]
            # Nao volta silenciosamente ao ESC/POS: mostra o erro real para
            # o operador poder corrigir (senao parece que "nada muda").
            return False, ("Modo GRAFICO de impressao falhou.\n\n"
                           f"{_ult[1]}")

        # Tipo PDF => salvar arquivo automaticamente
        if tipo == "PDF (Arquivo)":
            try:
                import platform
                if platform.system() == "Windows":
                    pasta = os.path.join(os.environ.get("USERPROFILE", "."), "Documents", "PDV_Cupons")
                else:
                    pasta = os.path.join(os.path.expanduser("~"), "PDV_Cupons")
                os.makedirs(pasta, exist_ok=True)
                timestamp = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
                filepath = os.path.join(pasta, f"cupom_{timestamp}.txt")
                enc = "utf-8" if codepage.upper() == "UTF-8" else codepage
                with open(filepath, "w", encoding=enc, errors="replace") as f:
                    f.write(texto)
                return True, f"Arquivo salvo em:\n{filepath}"
            except Exception as e:
                return False, f"Erro ao salvar arquivo: {e}"

        # Codificar texto no codepage configurado
        try:
            enc_name = codepage if codepage.upper() != "UTF-8" else "utf-8"
            texto_bytes = texto.encode(enc_name, errors="replace")
        except (LookupError, UnicodeEncodeError):
            texto_bytes = texto.encode("utf-8", errors="replace")

        # Montar bytes com comandos ESC/POS para impressoras termicas
        modo_fonte = config.get("modo_fonte", "Normal")
        dados_envio = b""
        if tipo == "Termica":
            # ESC @ - Inicializar impressora
            dados_envio += b"\x1b\x40"

            # Abrir gaveta antes (ESC p 0)
            if abrir_gaveta:
                dados_envio += b"\x1b\x70\x00\x19\xfa"

            # Configurar codepage na impressora (ESC t n)
            codepage_map = {
                "CP437": 0,
                "CP850": 2,
                "CP860": 3,
                "CP863": 4,
                "CP865": 5,
                "ISO-8859-1": 6,
                "CP858": 19,
                "Windows-1252": 16,
            }
            cp_upper = codepage.upper().replace("-", "").replace("_", "")
            for cp_name, cp_num in codepage_map.items():
                if cp_upper == cp_name.upper().replace("-", "").replace("_", ""):
                    dados_envio += b"\x1b\x74" + bytes([cp_num])
                    break

            # Logo (imagem) centralizado no topo do cupom, se habilitado
            if config.get("imprimir_logo") and (config.get("caminho_logo") or "").strip():
                tam_papel = str(config.get("tamanho_papel", "80mm"))
                largura_dots = 576 if "80" in tam_papel else 384
                logo_bytes = self._logo_para_escpos(
                    config.get("caminho_logo").strip(), largura_dots)
                if logo_bytes:
                    dados_envio += b"\x1b\x61\x01"   # ESC a 1 = centralizar
                    dados_envio += logo_bytes
                    dados_envio += b"\x1b\x61\x00"   # ESC a 0 = alinhar a esquerda
                    dados_envio += b"\n"

            # ================= FONTE / TAMANHO DA IMPRESSAO =================
            # "Modo da Fonte" define a familia (Font A normal x Font B
            # condensada) e presets. "Tamanho da Fonte" define a AMPLIACAO
            # (altura) via GS ! -> agora controla de fato o tamanho impresso.
            try:
                tam_fonte = int(float(config.get("tamanho_fonte", 12) or 12))
            except Exception:
                tam_fonte = 12
            # Numero -> multiplicador de ALTURA (mantem as colunas/largura)
            if tam_fonte <= 12:
                mult_h = 1
            elif tam_fonte <= 18:
                mult_h = 2
            elif tam_fonte <= 26:
                mult_h = 3
            else:
                mult_h = 4
            mult_w = 1  # largura padrao (preserva o numero de colunas)

            condensada = modo_fonte in ("Condensada", "Comprimida")
            # Presets do "Modo da Fonte" definem um piso de ampliacao
            if modo_fonte == "Grande":
                mult_h = max(mult_h, 2)
            elif modo_fonte == "Extra Grande":
                mult_h = max(mult_h, 2)
                mult_w = max(mult_w, 2)   # dobra tambem a largura (metade das colunas)

            mult_h = max(1, min(8, mult_h))
            mult_w = max(1, min(8, mult_w))

            # Familia: Font B (condensada) x Font A (normal)
            dados_envio += b"\x1b\x4d" + (b"\x01" if condensada else b"\x00")  # ESC M
            # Ampliacao: GS ! (nibble alto = largura, baixo = altura)
            gs_n = ((mult_w - 1) << 4) | (mult_h - 1)
            dados_envio += b"\x1d\x21" + bytes([gs_n])
            # Negrito quando a fonte esta ampliada (mais corpo/legibilidade)
            if mult_h >= 2 or mult_w >= 2:
                dados_envio += b"\x1b\x45\x01"  # ESC E 1 = negrito ON
            else:
                dados_envio += b"\x1b\x45\x00"  # ESC E 0 = negrito OFF
            # Espacamento entre linhas conforme a altura
            if modo_fonte == "Comprimida" and mult_h == 1:
                dados_envio += b"\x1b\x33\x12"  # compacto (18/180")
            elif mult_h >= 2:
                dados_envio += b"\x1b\x33\x40"  # 64/180" p/ fonte alta nao sobrepor
            else:
                dados_envio += b"\x1b\x32"      # espacamento padrao

        dados_envio += texto_bytes

        if tipo == "Termica":
            # Avanco generoso de linhas para o texto nao ser cortado
            # 8 linhas garante que o texto fique acima da lamina de corte
            dados_envio += b"\n\n\n\n\n\n\n\n"
            # Corte automatico (GS V 66 0 = corte parcial com feed)
            if corte_auto:
                dados_envio += b"\x1d\x56\x42\x00"
        else:
            # Impressora comum: avanco de 3 linhas
            dados_envio += b"\n\n\n"

        import platform
        sistema = platform.system()

        for copia in range(num_copias):
            sucesso, msg = self._enviar_para_impressora_os(
                dados_envio, nome_impressora, porta, sistema, texto, codepage
            )
            if not sucesso:
                return False, msg

        copias_txt = f" ({num_copias} copia(s))" if num_copias > 1 else ""
        destino = nome_impressora or "impressora padrao"
        return True, f"Impressao enviada para: {destino}{copias_txt}"

    def _enviar_para_impressora_os(self, dados_bytes, nome_impressora, porta, sistema, texto_original, codepage):
        """Envia dados binarios para a impressora usando o metodo do SO.
        Tenta multiplos metodos em cascata ate um funcionar.
        Retorna (sucesso: bool, mensagem: str).
        """
        import subprocess

        if sistema == "Windows":
            return self._imprimir_windows(dados_bytes, nome_impressora, porta, texto_original, codepage)
        elif sistema == "Linux":
            return self._imprimir_linux(dados_bytes, nome_impressora, texto_original, codepage)
        elif sistema == "Darwin":
            return self._imprimir_macos(dados_bytes, nome_impressora, texto_original, codepage)
        else:
            return False, f"Sistema operacional '{sistema}' nao suportado."

    def _imprimir_windows(self, dados_bytes, nome_impressora, porta, texto_original, codepage):
        """Impressao no Windows com multiplos metodos em cascata:
        1. win32print RAW (melhor para termicas)
        2. PowerShell Out-Printer
        3. Envio direto na porta (copy /b)
        4. Notepad silencioso
        """
        import subprocess

        # ---- METODO 1: win32print RAW (mais confiavel para termicas) ----
        try:
            import win32print
            printer_name = nome_impressora
            if not printer_name:
                try:
                    printer_name = win32print.GetDefaultPrinter()
                except Exception:
                    # Se nao houver padrao, tenta listar as instaladas
                    printers = win32print.EnumPrinters(win32print.PRINTER_ENUM_LOCAL | win32print.PRINTER_ENUM_CONNECTIONS)
                    if printers:
                        printer_name = printers[0][2]
            
            if printer_name:
                hprinter = win32print.OpenPrinter(printer_name)
                try:
                    # Tenta RAW primeiro, se falhar tenta TEXT
                    try:
                        win32print.StartDocPrinter(hprinter, 1, ("PDV Pro Cupom", None, "RAW"))
                    except Exception:
                        win32print.StartDocPrinter(hprinter, 1, ("PDV Pro Cupom", None, "TEXT"))
                        
                    try:
                        win32print.StartPagePrinter(hprinter)
                        win32print.WritePrinter(hprinter, dados_bytes)
                        win32print.EndPagePrinter(hprinter)
                    finally:
                        win32print.EndDocPrinter(hprinter)
                finally:
                    win32print.ClosePrinter(hprinter)
                return True, f"Impresso via win32print em: {printer_name}"
        except ImportError:
            pass
        except Exception as e:
            erro_win32 = str(e)
            _logger.warning(f"win32print falhou: {erro_win32}")

        # ---- METODO 2: PowerShell Out-Printer ----
        try:
            temp_file = os.path.join(os.environ.get("TEMP", "."), "pdv_cupom_temp.txt")
            enc = "utf-8" if codepage.upper() == "UTF-8" else codepage
            with open(temp_file, "w", encoding=enc, errors="replace") as f:
                f.write(texto_original)

            if nome_impressora:
                ps_cmd = (
                    f'Get-Content -Path "{temp_file}" -Encoding UTF8 | '
                    f'Out-Printer -Name "{nome_impressora}"'
                )
            else:
                ps_cmd = f'Get-Content -Path "{temp_file}" -Encoding UTF8 | Out-Printer'

            result = subprocess.run(
                ["powershell", "-NoProfile", "-Command", ps_cmd],
                capture_output=True, text=True, timeout=30,
                creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
            )
            try:
                os.remove(temp_file)
            except Exception:
                pass

            if result.returncode == 0:
                return True, f"Impresso via PowerShell Out-Printer"
        except FileNotFoundError:
            pass
        except subprocess.TimeoutExpired:
            pass
        except Exception:
            pass

        # ---- METODO 3: Envio direto na porta (copy /b para USB/COM/LPT) ----
        if porta:
            try:
                temp_file = os.path.join(os.environ.get("TEMP", "."), "pdv_cupom_raw.bin")
                with open(temp_file, "wb") as f:
                    f.write(dados_bytes)

                result = subprocess.run(
                    f'copy /b "{temp_file}" "{porta}"',
                    shell=True, capture_output=True, text=True, timeout=15,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                try:
                    os.remove(temp_file)
                except Exception:
                    pass

                if result.returncode == 0:
                    return True, f"Impresso via porta direta: {porta}"
            except Exception:
                pass

        # ---- METODO 4: Notepad silencioso ----
        try:
            temp_file = os.path.join(os.environ.get("TEMP", "."), "pdv_cupom_temp.txt")
            enc = "utf-8" if codepage.upper() == "UTF-8" else codepage
            with open(temp_file, "w", encoding=enc, errors="replace") as f:
                f.write(texto_original)

            cmd = ["notepad.exe", "/p", temp_file]
            if nome_impressora:
                # Notepad nao aceita selecionar impressora, mas pelo menos imprime na padrao
                pass
            subprocess.run(cmd, timeout=30,
                creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000))
            try:
                os.remove(temp_file)
            except Exception:
                pass
            return True, "Impresso via Notepad (impressora padrao)"
        except Exception as e:
            pass

        return False, (
            "Nenhum metodo de impressao funcionou no Windows.\n"
            "Instale o pacote 'pywin32' para melhor compatibilidade:\n"
            "  pip install pywin32\n"
            "Ou verifique se a impressora esta instalada e ligada."
        )

    def _imprimir_linux(self, dados_bytes, nome_impressora, texto_original, codepage):
        """Impressao no Linux via lp ou lpr."""
        import subprocess
        import tempfile

        # Tentar lp primeiro (CUPS)
        try:
            temp_file = "/tmp/pdv_cupom_temp.bin"
            with open(temp_file, "wb") as f:
                f.write(dados_bytes)

            cmd = ["lp", "-o", "raw"]
            if nome_impressora:
                cmd.extend(["-d", nome_impressora])
            cmd.append(temp_file)

            result = subprocess.run(cmd, capture_output=True, text=True, timeout=15)
            try:
                os.remove(temp_file)
            except Exception:
                pass

            if result.returncode == 0:
                return True, f"Impresso via lp: {result.stdout.strip()}"
            # Se lp falhou, tentar lpr
        except FileNotFoundError:
            pass
        except Exception:
            pass

        # Tentar lpr
        try:
            temp_file = "/tmp/pdv_cupom_temp.bin"
            with open(temp_file, "wb") as f:
                f.write(dados_bytes)

            cmd = ["lpr", "-l"]  # -l = raw/binary
            if nome_impressora:
                cmd.extend(["-P", nome_impressora])
            cmd.append(temp_file)

            result = subprocess.run(cmd, capture_output=True, text=True, timeout=15)
            try:
                os.remove(temp_file)
            except Exception:
                pass

            if result.returncode == 0:
                return True, "Impresso via lpr"
        except FileNotFoundError:
            pass
        except Exception:
            pass

        return False, (
            "Nenhum metodo de impressao funcionou no Linux.\n"
            "Verifique se o CUPS esta instalado e a impressora configurada.\n"
            "  sudo apt install cups"
        )

    def _imprimir_macos(self, dados_bytes, nome_impressora, texto_original, codepage):
        """Impressao no macOS via lpr."""
        import subprocess

        try:
            temp_file = "/tmp/pdv_cupom_temp.bin"
            with open(temp_file, "wb") as f:
                f.write(dados_bytes)

            cmd = ["lpr", "-l"]
            if nome_impressora:
                cmd.extend(["-P", nome_impressora])
            cmd.append(temp_file)

            result = subprocess.run(cmd, capture_output=True, text=True, timeout=15)
            try:
                os.remove(temp_file)
            except Exception:
                pass

            if result.returncode == 0:
                return True, "Impresso via lpr (macOS)"
            else:
                return False, f"Erro lpr: {result.stderr}"
        except FileNotFoundError:
            return False, "Comando lpr nao encontrado no macOS."
        except Exception as e:
            return False, f"Erro ao imprimir no macOS: {e}"

    # ========================================================================
    # CONFIGURACAO DE IMPRESSORA
    # ========================================================================
    def _load_printer_config(self):
        """Carrega configuracoes da impressora do arquivo JSON."""
        defaults = {
            "tipo_impressora": "Termica",
            "nome_impressora": "",
            "tamanho_papel": "80mm",
            "largura_papel": 42,
            "modo_fonte": "Grande",
            "tamanho_fonte": 12,
            "corte_automatico": True,
            "abrir_gaveta": False,
            "imprimir_logo": False,
            "caminho_logo": "",
            "num_copias": 1,
            "margem_esquerda": 0,
            "margem_direita": 0,
            "espacamento_linhas": 1,
            "codepage": "UTF-8",
            "velocidade_impressao": "Normal",
            "imprimir_automatico": False,
            "porta_impressora": "",
            "usar_servidor_impressao": False,
            "servidor_impressao_url": "http://127.0.0.1:8899/print",
            "metodo_impressao": "Texto (ESC/POS)"
        }
        try:
            if os.path.exists(PRINTER_CONFIG_FILE):
                with open(PRINTER_CONFIG_FILE, "r", encoding="utf-8") as f:
                    saved = json.load(f)
                for k, v in defaults.items():
                    if k not in saved:
                        saved[k] = v
                # Migracao unica: aumenta a fonte das impressoes para instalacoes
                # que ainda estavam num modo pequeno (padrao antigo). Ocorre uma
                # unica vez (flag fonte_migrada_v2); se depois o usuario escolher
                # outro modo, sua escolha e respeitada.
                if not saved.get("fonte_migrada_v2"):
                    modo_atual = str(saved.get("modo_fonte", "")).strip()
                    if modo_atual in ("", "Normal", "Condensada"):
                        saved["modo_fonte"] = "Grande"
                        # Font A cabe no maximo ~48 colunas em 80mm; evita corte
                        try:
                            if int(float(saved.get("largura_papel", 42) or 42)) > 48:
                                saved["largura_papel"] = 48
                        except Exception:
                            saved["largura_papel"] = 42
                    saved["fonte_migrada_v2"] = True
                    self._save_printer_config(saved)
                return saved
        except Exception as e:
            print(f"Erro ao carregar config impressora: {e}")
        return defaults

    def _save_printer_config(self, config):
        """Salva configuracoes da impressora no arquivo JSON."""
        try:
            with open(PRINTER_CONFIG_FILE, "w", encoding="utf-8") as f:
                json.dump(config, f, indent=2, ensure_ascii=False)
            return True
        except Exception as e:
            print(f"Erro ao salvar config impressora: {e}")
            return False

    def _buscar_impressoras_instaladas(self):
        """Busca automaticamente as impressoras instaladas no sistema.
        Utiliza multiplos metodos para garantir compatibilidade:
        1. win32print (se disponivel)
        2. PowerShell (Get-Printer)
        3. WMI via PowerShell (Win32_Printer)
        4. wmic (legado)
        5. Registro do Windows via reg query
        6. Linux (lpstat)
        No Windows, tambem identifica a impressora padrao.
        """
        impressoras = []
        impressora_padrao = ""

        import platform
        sistema = platform.system()

        if sistema == "Windows":
            # ---- METODO 1: win32print (mais confiavel, se instalado) ----
            try:
                import win32print
                # Impressora padrao
                try:
                    impressora_padrao = win32print.GetDefaultPrinter()
                except Exception:
                    pass
                # Listar todas
                flags = win32print.PRINTER_ENUM_LOCAL | win32print.PRINTER_ENUM_CONNECTIONS
                printers = win32print.EnumPrinters(flags, None, 2)
                for p in printers:
                    nome = p.get("pPrinterName", "") if isinstance(p, dict) else ""
                    if not nome:
                        # EnumPrinters nivel 2 retorna tupla
                        try:
                            nome = p[2] if len(p) > 2 else ""
                        except (IndexError, TypeError):
                            continue
                    if nome and nome not in impressoras:
                        impressoras.append(nome)
                if impressoras:
                    return impressoras, impressora_padrao
            except ImportError:
                pass
            except Exception as e:
                print(f"win32print erro: {e}")

            # ---- METODO 2: PowerShell Get-Printer ----
            try:
                import subprocess
                result = subprocess.run(
                    ["powershell", "-NoProfile", "-Command",
                     "Get-Printer | Select-Object -ExpandProperty Name"],
                    capture_output=True, text=True, timeout=10,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        nome = line.strip()
                        if nome and nome not in impressoras:
                            impressoras.append(nome)
                # Buscar impressora padrao via PowerShell
                if not impressora_padrao:
                    result_def = subprocess.run(
                        ["powershell", "-NoProfile", "-Command",
                         "(Get-CimInstance -ClassName Win32_Printer "
                         "| Where-Object {$_.Default -eq $true}).Name"],
                        capture_output=True, text=True, timeout=10,
                        creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                    )
                    if result_def.returncode == 0 and result_def.stdout.strip():
                        impressora_padrao = result_def.stdout.strip().split("\n")[0].strip()
                if impressoras:
                    return impressoras, impressora_padrao
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"PowerShell Get-Printer erro: {e}")

            # ---- METODO 3: WMI via PowerShell (Win32_Printer) ----
            try:
                import subprocess
                result = subprocess.run(
                    ["powershell", "-NoProfile", "-Command",
                     "Get-CimInstance -ClassName Win32_Printer "
                     "| Select-Object -ExpandProperty Name"],
                    capture_output=True, text=True, timeout=10,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        nome = line.strip()
                        if nome and nome not in impressoras:
                            impressoras.append(nome)
                if impressoras:
                    return impressoras, impressora_padrao
            except Exception as e:
                print(f"WMI PowerShell erro: {e}")

            # ---- METODO 4: wmic (legado, funciona em Windows 10 e anteriores) ----
            try:
                import subprocess
                result = subprocess.run(
                    ["wmic", "printer", "get", "Name"],
                    capture_output=True, text=True, timeout=10,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        nome = line.strip()
                        if nome and nome.lower() != "name" and nome not in impressoras:
                            impressoras.append(nome)
                # Buscar padrao via wmic
                if not impressora_padrao:
                    result_def = subprocess.run(
                        ["wmic", "printer", "where", "Default=TRUE", "get", "Name"],
                        capture_output=True, text=True, timeout=10,
                        creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                    )
                    if result_def.returncode == 0 and result_def.stdout.strip():
                        for line in result_def.stdout.strip().split("\n"):
                            nome = line.strip()
                            if nome and nome.lower() != "name":
                                impressora_padrao = nome
                                break
                if impressoras:
                    return impressoras, impressora_padrao
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"wmic erro: {e}")

            # ---- METODO 5: Registro do Windows ----
            try:
                import subprocess
                result = subprocess.run(
                    ["reg", "query",
                     r"HKEY_CURRENT_USER\Software\Microsoft\Windows NT\CurrentVersion\Devices"],
                    capture_output=True, text=True, timeout=10,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        line = line.strip()
                        if line and "REG_SZ" in line:
                            # Formato: "NomePrinter    REG_SZ    winspool,PortName"
                            parts = line.split("    ")
                            if parts:
                                nome = parts[0].strip()
                                if nome and nome not in impressoras:
                                    impressoras.append(nome)
                # Buscar padrao via registro
                if not impressora_padrao:
                    result_def = subprocess.run(
                        ["reg", "query",
                         r"HKEY_CURRENT_USER\Software\Microsoft\Windows NT\CurrentVersion\Windows",
                         "/v", "Device"],
                        capture_output=True, text=True, timeout=10,
                        creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                    )
                    if result_def.returncode == 0 and result_def.stdout.strip():
                        for line in result_def.stdout.strip().split("\n"):
                            if "Device" in line and "REG_SZ" in line:
                                parts = line.split("REG_SZ")
                                if len(parts) > 1:
                                    dev = parts[1].strip()
                                    # Formato: "NomePrinter,winspool,PortName"
                                    nome = dev.split(",")[0].strip()
                                    if nome:
                                        impressora_padrao = nome
                if impressoras:
                    return impressoras, impressora_padrao
            except Exception as e:
                print(f"Registro Windows erro: {e}")

        elif sistema == "Linux":
            # ---- Linux: lpstat ----
            try:
                import subprocess
                result = subprocess.run(
                    ["lpstat", "-a"],
                    capture_output=True, text=True, timeout=10
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        parts = line.split(" ")
                        if parts:
                            nome = parts[0].strip()
                            if nome and nome not in impressoras:
                                impressoras.append(nome)
                # Impressora padrao no Linux
                result_def = subprocess.run(
                    ["lpstat", "-d"],
                    capture_output=True, text=True, timeout=10
                )
                if result_def.returncode == 0 and result_def.stdout.strip():
                    # Formato: "system default destination: NomePrinter"
                    parts = result_def.stdout.strip().split(":")
                    if len(parts) > 1:
                        impressora_padrao = parts[1].strip()
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"lpstat erro: {e}")

        elif sistema == "Darwin":  # macOS
            try:
                import subprocess
                result = subprocess.run(
                    ["lpstat", "-a"],
                    capture_output=True, text=True, timeout=10
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        parts = line.split(" ")
                        if parts:
                            nome = parts[0].strip()
                            if nome and nome not in impressoras:
                                impressoras.append(nome)
            except Exception as e:
                print(f"macOS lpstat erro: {e}")

        return impressoras, impressora_padrao

    def _obter_detalhes_impressoras(self):
        """Busca detalhes (porta, driver, tipo) de cada impressora instalada.
        Retorna um dicionario: {nome_impressora: {porta, driver, tipo_sugerido, codepage_sugerido}}
        Utiliza multiplos metodos para compatibilidade com Windows, Linux e macOS.
        """
        detalhes = {}
        import platform
        sistema = platform.system()

        def _inferir_tipo_por_driver(driver_name):
            """Infere o tipo de impressora com base no nome do driver."""
            if not driver_name:
                return "Termica"
            drv = driver_name.lower()
            # Termica / POS / Receipt
            termica_keywords = ["pos", "thermal", "receipt", "termic", "tm-", "tm20",
                                "tm80", "tm88", "epson tm", "bematech", "elgin",
                                "daruma", "sweda", "diebold", "star tsp", "star sp",
                                "citizen ct", "citizen cl", "mp-", "mp20", "mp40",
                                "i7", "i9", "generic / text only", "generic/text",
                                "raw", "esc/pos", "escpos", "zj-", "xprinter",
                                "sewoo", "sam4s", "custom", "hprt", "rongta",
                                "munbyn", "goojprt", "nyear", "ocpp", "58mm", "80mm"]
            for kw in termica_keywords:
                if kw in drv:
                    return "Termica"
            # Matricial / Dot Matrix
            matricial_keywords = ["dot matrix", "matricial", "fx-", "lx-",
                                  "lq-", "dfx", "impact", "epson fx", "epson lx",
                                  "epson lq", "oki ", "okidata"]
            for kw in matricial_keywords:
                if kw in drv:
                    return "Matricial"
            # Laser
            laser_keywords = ["laser", "laserjet", "pcl", "postscript",
                              "brother hl", "samsung m", "xerox", "ricoh",
                              "lexmark", "canon lbp", "hp color"]
            for kw in laser_keywords:
                if kw in drv:
                    return "Laser"
            # Jato de Tinta
            jato_keywords = ["inkjet", "ink jet", "jato", "deskjet", "officejet",
                             "photosmart", "envy", "pixma", "ecotank", "workforce",
                             "stylus", "expression", "canon ip", "canon mg",
                             "canon ts", "brother dcp", "brother mfc"]
            for kw in jato_keywords:
                if kw in drv:
                    return "Jato de Tinta"
            # PDF
            pdf_keywords = ["pdf", "xps", "onenote", "fax", "microsoft print"]
            for kw in pdf_keywords:
                if kw in drv:
                    return "PDF (Arquivo)"
            return "Termica"

        def _inferir_codepage(tipo_sugerido, driver_name):
            """Infere o codepage mais adequado com base no tipo e driver."""
            if not driver_name:
                driver_name = ""
            drv = driver_name.lower()
            if tipo_sugerido == "Termica":
                # Impressoras termicas brasileiras geralmente usam CP850
                br_keywords = ["bematech", "elgin", "daruma", "sweda",
                               "diebold", "mp-", "mp20", "mp40", "i7", "i9"]
                for kw in br_keywords:
                    if kw in drv:
                        return "CP850"
                # Impressoras termicas genericas/chinesas geralmente usam CP437 ou UTF-8
                if any(kw in drv for kw in ["generic", "raw", "text only"]):
                    return "CP850"
                return "UTF-8"
            elif tipo_sugerido == "Matricial":
                return "CP850"
            elif tipo_sugerido == "PDF (Arquivo)":
                return "UTF-8"
            else:
                return "UTF-8"

        if sistema == "Windows":
            # ---- METODO 1: win32print (mais detalhado) ----
            try:
                import win32print
                flags = win32print.PRINTER_ENUM_LOCAL | win32print.PRINTER_ENUM_CONNECTIONS
                printers = win32print.EnumPrinters(flags, None, 2)
                for p in printers:
                    nome = ""
                    porta = ""
                    driver = ""
                    if isinstance(p, dict):
                        nome = p.get("pPrinterName", "")
                        porta = p.get("pPortName", "")
                        driver = p.get("pDriverName", "")
                    else:
                        # EnumPrinters nivel 2 retorna tupla:
                        # (flags, desc, name, comment, ...) ou
                        # indice 1=name, 2=name, 5=port, 6=driver (varia)
                        try:
                            nome = p[2] if len(p) > 2 else ""
                            porta = p[5] if len(p) > 5 else ""
                            driver = p[6] if len(p) > 6 else ""
                        except (IndexError, TypeError):
                            continue
                    if nome:
                        tipo_sug = _inferir_tipo_por_driver(driver)
                        codepage_sug = _inferir_codepage(tipo_sug, driver)
                        detalhes[nome] = {
                            "porta": porta,
                            "driver": driver,
                            "tipo_sugerido": tipo_sug,
                            "codepage_sugerido": codepage_sug
                        }
                if detalhes:
                    return detalhes
            except ImportError:
                pass
            except Exception as e:
                print(f"win32print detalhes erro: {e}")

            # ---- METODO 2: PowerShell Get-Printer com detalhes ----
            try:
                import subprocess
                result = subprocess.run(
                    ["powershell", "-NoProfile", "-Command",
                     "Get-Printer | Select-Object Name, PortName, DriverName "
                     "| ForEach-Object { $_.Name + '|' + $_.PortName + '|' + $_.DriverName }"],
                    capture_output=True, text=True, timeout=15,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        parts = line.strip().split("|")
                        if len(parts) >= 3:
                            nome = parts[0].strip()
                            porta = parts[1].strip()
                            driver = parts[2].strip()
                            if nome and nome not in detalhes:
                                tipo_sug = _inferir_tipo_por_driver(driver)
                                codepage_sug = _inferir_codepage(tipo_sug, driver)
                                detalhes[nome] = {
                                    "porta": porta,
                                    "driver": driver,
                                    "tipo_sugerido": tipo_sug,
                                    "codepage_sugerido": codepage_sug
                                }
                    if detalhes:
                        return detalhes
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"PowerShell detalhes erro: {e}")

            # ---- METODO 3: WMI via PowerShell ----
            try:
                import subprocess
                result = subprocess.run(
                    ["powershell", "-NoProfile", "-Command",
                     "Get-CimInstance -ClassName Win32_Printer "
                     "| Select-Object Name, PortName, DriverName "
                     "| ForEach-Object { $_.Name + '|' + $_.PortName + '|' + $_.DriverName }"],
                    capture_output=True, text=True, timeout=15,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        parts = line.strip().split("|")
                        if len(parts) >= 3:
                            nome = parts[0].strip()
                            porta = parts[1].strip()
                            driver = parts[2].strip()
                            if nome and nome not in detalhes:
                                tipo_sug = _inferir_tipo_por_driver(driver)
                                codepage_sug = _inferir_codepage(tipo_sug, driver)
                                detalhes[nome] = {
                                    "porta": porta,
                                    "driver": driver,
                                    "tipo_sugerido": tipo_sug,
                                    "codepage_sugerido": codepage_sug
                                }
                    if detalhes:
                        return detalhes
            except Exception as e:
                print(f"WMI detalhes erro: {e}")

            # ---- METODO 4: wmic com detalhes ----
            try:
                import subprocess
                result = subprocess.run(
                    ["wmic", "printer", "get", "Name,PortName,DriverName",
                     "/format:csv"],
                    capture_output=True, text=True, timeout=15,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    lines = result.stdout.strip().split("\n")
                    header = None
                    for line in lines:
                        line = line.strip()
                        if not line:
                            continue
                        parts = line.split(",")
                        if header is None:
                            header = [h.strip().lower() for h in parts]
                            continue
                        if len(parts) >= len(header):
                            row = {}
                            for i, h in enumerate(header):
                                row[h] = parts[i].strip() if i < len(parts) else ""
                            nome = row.get("name", "")
                            porta = row.get("portname", "")
                            driver = row.get("drivername", "")
                            if nome and nome not in detalhes:
                                tipo_sug = _inferir_tipo_por_driver(driver)
                                codepage_sug = _inferir_codepage(tipo_sug, driver)
                                detalhes[nome] = {
                                    "porta": porta,
                                    "driver": driver,
                                    "tipo_sugerido": tipo_sug,
                                    "codepage_sugerido": codepage_sug
                                }
                    if detalhes:
                        return detalhes
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"wmic detalhes erro: {e}")

            # ---- METODO 5: Registro do Windows (porta apenas) ----
            try:
                import subprocess
                result = subprocess.run(
                    ["reg", "query",
                     r"HKEY_CURRENT_USER\Software\Microsoft\Windows NT\CurrentVersion\Devices"],
                    capture_output=True, text=True, timeout=10,
                    creationflags=getattr(subprocess, 'CREATE_NO_WINDOW', 0x08000000)
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        line = line.strip()
                        if line and "REG_SZ" in line:
                            # Formato: "NomePrinter    REG_SZ    winspool,PortName"
                            parts = line.split("    ")
                            if len(parts) >= 3:
                                nome = parts[0].strip()
                                valor = parts[-1].strip()
                                # valor = "winspool,PortName" ou similar
                                porta_parts = valor.split(",")
                                porta = porta_parts[1].strip() if len(porta_parts) > 1 else ""
                                if nome and nome not in detalhes:
                                    tipo_sug = _inferir_tipo_por_driver(nome)
                                    codepage_sug = _inferir_codepage(tipo_sug, nome)
                                    detalhes[nome] = {
                                        "porta": porta,
                                        "driver": "",
                                        "tipo_sugerido": tipo_sug,
                                        "codepage_sugerido": codepage_sug
                                    }
                    if detalhes:
                        return detalhes
            except Exception as e:
                print(f"Registro detalhes erro: {e}")

        elif sistema == "Linux":
            # ---- Linux: lpstat -v (mostra dispositivo/porta) ----
            try:
                import subprocess
                # Obter portas/URIs das impressoras
                result = subprocess.run(
                    ["lpstat", "-v"],
                    capture_output=True, text=True, timeout=10
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        # Formato: "device for NomePrinter: uri://..."
                        if "device for" in line.lower():
                            try:
                                after_for = line.split("for", 1)[1]
                                nome_porta = after_for.split(":", 1)
                                nome = nome_porta[0].strip()
                                porta = nome_porta[1].strip() if len(nome_porta) > 1 else ""
                                if nome and nome not in detalhes:
                                    # Inferir tipo pela URI
                                    tipo_sug = "Termica"
                                    if "usb://" in porta.lower():
                                        tipo_sug = "Termica"
                                    elif "socket://" in porta.lower() or "ipp://" in porta.lower():
                                        tipo_sug = "Laser"
                                    elif "parallel://" in porta.lower() or "serial://" in porta.lower():
                                        tipo_sug = "Matricial"
                                    elif "pdf" in porta.lower() or "cups-pdf" in porta.lower():
                                        tipo_sug = "PDF (Arquivo)"
                                    codepage_sug = _inferir_codepage(tipo_sug, porta)
                                    detalhes[nome] = {
                                        "porta": porta,
                                        "driver": "",
                                        "tipo_sugerido": tipo_sug,
                                        "codepage_sugerido": codepage_sug
                                    }
                            except (IndexError, ValueError):
                                continue
                # Tentar obter driver via lpoptions
                for nome_imp in list(detalhes.keys()):
                    try:
                        result_opt = subprocess.run(
                            ["lpoptions", "-p", nome_imp, "-l"],
                            capture_output=True, text=True, timeout=5
                        )
                        if result_opt.returncode == 0 and result_opt.stdout.strip():
                            # Procurar informacoes de driver/modelo
                            for opt_line in result_opt.stdout.strip().split("\n"):
                                if "printer-make-and-model" in opt_line.lower():
                                    driver_info = opt_line.split(":", 1)
                                    if len(driver_info) > 1:
                                        detalhes[nome_imp]["driver"] = driver_info[1].strip()
                                        # Reclassificar com driver
                                        detalhes[nome_imp]["tipo_sugerido"] = _inferir_tipo_por_driver(
                                            detalhes[nome_imp]["driver"])
                                        detalhes[nome_imp]["codepage_sugerido"] = _inferir_codepage(
                                            detalhes[nome_imp]["tipo_sugerido"],
                                            detalhes[nome_imp]["driver"])
                    except Exception:
                        pass
            except FileNotFoundError:
                pass
            except Exception as e:
                print(f"Linux detalhes erro: {e}")

        elif sistema == "Darwin":  # macOS
            try:
                import subprocess
                result = subprocess.run(
                    ["lpstat", "-v"],
                    capture_output=True, text=True, timeout=10
                )
                if result.returncode == 0 and result.stdout.strip():
                    for line in result.stdout.strip().split("\n"):
                        if "device for" in line.lower():
                            try:
                                after_for = line.split("for", 1)[1]
                                nome_porta = after_for.split(":", 1)
                                nome = nome_porta[0].strip()
                                porta = nome_porta[1].strip() if len(nome_porta) > 1 else ""
                                if nome and nome not in detalhes:
                                    tipo_sug = _inferir_tipo_por_driver(nome)
                                    codepage_sug = _inferir_codepage(tipo_sug, nome)
                                    detalhes[nome] = {
                                        "porta": porta,
                                        "driver": "",
                                        "tipo_sugerido": tipo_sug,
                                        "codepage_sugerido": codepage_sug
                                    }
                            except (IndexError, ValueError):
                                continue
            except Exception as e:
                print(f"macOS detalhes erro: {e}")

        return detalhes

    def show_config_impressora(self):
        """Tela de configuracao de impressora."""
        self.clear_container()

        # Barra superior
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        # Linha neon superior
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="◀ Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Configuracao de Impressora", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)
        # Botoes Sair e Trocar de Usuario
        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_sair, "Fechar o PDV Pro (Alt+F4)")

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)
        add_tooltip(_btn_trocar, "Trocar de usuario (Ctrl+L)")


        # Carregar config atual
        config = self._load_printer_config()

        # Area com scroll
        scroll = ScrollableFrame(self.main_container)
        scroll.pack(fill="both", expand=True, padx=10, pady=10)
        content = scroll.scrollable_frame

        # === CARD 1: Tipo e Nome da Impressora ===
        card1 = CardFrame(content)
        card1.pack(fill="x", padx=20, pady=8)

        tk.Label(card1, text="Configuracoes Gerais", bg=COR_CARD, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).grid(row=0, column=0, columnspan=2,
                 sticky="w", padx=5, pady=(5, 10))

        # Tipo de Impressora
        tk.Label(card1, text="Tipo de Impressora:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=1, column=0, sticky="w", padx=5, pady=4)
        tipos_impressora = ["Termica", "Matricial", "Jato de Tinta", "Laser", "PDF (Arquivo)"]
        combo_tipo = ttk.Combobox(card1, values=tipos_impressora, width=30, state="readonly",
                                   font=("Segoe UI", 10))
        combo_tipo.set(config.get("tipo_impressora", "Termica"))
        combo_tipo.grid(row=1, column=1, sticky="w", padx=5, pady=4)

        # Impressora Selecionada (Combobox com busca automatica)
        tk.Label(card1, text="Impressora:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=2, column=0, sticky="w", padx=5, pady=4)

        impressora_frame = tk.Frame(card1, bg=COR_CARD)
        impressora_frame.grid(row=2, column=1, sticky="w", padx=5, pady=4)

        combo_impressora = ttk.Combobox(impressora_frame, width=32, font=("Segoe UI", 10))
        combo_impressora.pack(side="left")

        # Label para status da busca
        lbl_status_busca = tk.Label(card1, text="", bg=COR_CARD, fg=COR_TEXTO2,
                                     font=("Segoe UI", 8, "italic"))
        lbl_status_busca.grid(row=3, column=1, sticky="w", padx=5, pady=(0, 2))

        # Label para impressora padrao
        lbl_impressora_padrao = tk.Label(card1, text="", bg=COR_CARD, fg=COR_SUCESSO,
                                          font=("Segoe UI", 9))
        lbl_impressora_padrao.grid(row=4, column=1, sticky="w", padx=5, pady=(0, 4))

        # Dicionario para armazenar detalhes das impressoras encontradas
        # Sera preenchido pela busca e usado no preenchimento automatico
        detalhes_impressoras_cache = {}

        def buscar_impressoras():
            """Busca impressoras instaladas e atualiza o combobox.
            Tambem busca detalhes (porta, driver, tipo, codepage) de cada impressora."""
            lbl_status_busca.config(text="Buscando impressoras...", fg=COR_ALERTA)
            lbl_impressora_padrao.config(text="")
            self.root.update_idletasks()

            def _buscar():
                lista_e_padrao = self._buscar_impressoras_instaladas()
                detalhes = self._obter_detalhes_impressoras()
                return lista_e_padrao, detalhes

            def _on_resultado(resultado):
                (impressoras_encontradas, padrao), detalhes = resultado
                valor_atual = combo_impressora.get().strip()

                # Atualizar cache de detalhes
                detalhes_impressoras_cache.clear()
                detalhes_impressoras_cache.update(detalhes)

                if impressoras_encontradas:
                    # Montar lista com indicacao da padrao
                    lista_display = []
                    for imp in impressoras_encontradas:
                        lista_display.append(imp)

                    combo_impressora["values"] = lista_display

                    # Selecionar a impressora salva na config, ou a padrao
                    impressora_selecionada = ""
                    if valor_atual and valor_atual in lista_display:
                        combo_impressora.set(valor_atual)
                        impressora_selecionada = valor_atual
                    elif config.get("nome_impressora") and config.get("nome_impressora") in lista_display:
                        combo_impressora.set(config.get("nome_impressora"))
                        impressora_selecionada = config.get("nome_impressora")
                    elif padrao and padrao in lista_display:
                        combo_impressora.set(padrao)
                        impressora_selecionada = padrao
                    elif lista_display:
                        combo_impressora.set(lista_display[0])
                        impressora_selecionada = lista_display[0]

                    lbl_status_busca.config(
                        text=f"{len(impressoras_encontradas)} impressora(s) encontrada(s)",
                        fg=COR_SUCESSO
                    )
                    if padrao:
                        lbl_impressora_padrao.config(
                            text=f"Impressora padrao: {padrao}",
                            fg=COR_SUCESSO
                        )
                    else:
                        lbl_impressora_padrao.config(text="")

                    # Preencher automaticamente os campos da impressora selecionada
                    # Somente se os campos estiverem vazios (primeira busca) ou
                    # se nao havia impressora salva na config
                    if impressora_selecionada and impressora_selecionada in detalhes_impressoras_cache:
                        _preencher_detalhes_impressora(impressora_selecionada)
                else:
                    combo_impressora["values"] = []
                    lbl_status_busca.config(
                        text="Nenhuma impressora encontrada. Digite o nome manualmente.",
                        fg=COR_ALERTA
                    )
                    lbl_impressora_padrao.config(text="")

            def _on_erro(e):
                lbl_status_busca.config(
                    text=f"Erro ao buscar: {e}",
                    fg=COR_ERRO
                )

            self.run_async(_buscar, _on_resultado, _on_erro)

        btn_buscar = tk.Button(impressora_frame, text="⌕ Buscar",
                               bg=COR_BOTAO_AZUL, fg=COR_TEXTO,
                               font=("Segoe UI", 9, "bold"), relief="flat",
                               cursor="hand2", padx=8, pady=2,
                               command=buscar_impressoras,
                               activebackground="#448AFF", activeforeground=COR_TEXTO)
        btn_buscar.pack(side="left", padx=(5, 0))

        # Referencia para uso posterior (et_nome agora e combo_impressora)
        et_nome = combo_impressora

        # Porta da Impressora
        tk.Label(card1, text="Porta (ex: COM1, USB, LPT1):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=5, column=0, sticky="w", padx=5, pady=4)
        et_porta = StyledEntry(card1, width=35)
        et_porta.insert(0, config.get("porta_impressora", ""))
        et_porta.grid(row=5, column=1, sticky="w", padx=5, pady=4)

        # Label para exibir driver detectado
        lbl_driver_info = tk.Label(card1, text="", bg=COR_CARD, fg=COR_TEXTO2,
                                    font=("Segoe UI", 8, "italic"))
        lbl_driver_info.grid(row=7, column=1, sticky="w", padx=5, pady=(0, 2))

        # Codepage
        tk.Label(card1, text="Codificacao (Codepage):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=6, column=0, sticky="w", padx=5, pady=4)
        codepages = ["UTF-8", "CP850", "CP437", "ISO-8859-1", "Windows-1252"]
        combo_codepage = ttk.Combobox(card1, values=codepages, width=30, state="readonly",
                                       font=("Segoe UI", 10))
        combo_codepage.set(config.get("codepage", "UTF-8"))
        combo_codepage.grid(row=6, column=1, sticky="w", padx=5, pady=4)

        def _preencher_detalhes_impressora(nome_impressora):
            """Preenche automaticamente porta, codificacao e tipo quando
            uma impressora e selecionada na busca."""
            if nome_impressora in detalhes_impressoras_cache:
                info = detalhes_impressoras_cache[nome_impressora]
                porta_detectada = info.get("porta", "")
                driver_detectado = info.get("driver", "")
                tipo_sugerido = info.get("tipo_sugerido", "")
                codepage_sugerido = info.get("codepage_sugerido", "")

                # Preencher porta
                et_porta.delete(0, tk.END)
                if porta_detectada:
                    et_porta.insert(0, porta_detectada)

                # Preencher tipo de impressora
                if tipo_sugerido and tipo_sugerido in tipos_impressora:
                    combo_tipo.set(tipo_sugerido)

                # Preencher codepage
                if codepage_sugerido and codepage_sugerido in codepages:
                    combo_codepage.set(codepage_sugerido)

                # Exibir informacao do driver detectado
                if driver_detectado:
                    lbl_driver_info.config(
                        text=f"Driver: {driver_detectado}",
                        fg=COR_TEXTO2
                    )
                else:
                    lbl_driver_info.config(text="")
            else:
                lbl_driver_info.config(text="")

        def _on_impressora_selecionada(event=None):
            """Evento disparado quando o usuario seleciona uma impressora no combobox.
            Preenche automaticamente todos os campos com os dados detectados."""
            nome_selecionado = combo_impressora.get().strip()
            if nome_selecionado:
                _preencher_detalhes_impressora(nome_selecionado)

        # Vincular evento de selecao do combobox para preenchimento automatico
        combo_impressora.bind("<<ComboboxSelected>>", _on_impressora_selecionada)

        # Carregar impressora salva no combobox
        nome_salvo = config.get("nome_impressora", "")
        if nome_salvo:
            combo_impressora.set(nome_salvo)

        # Buscar impressoras automaticamente ao abrir a tela
        self.root.after(300, buscar_impressoras)

        # === CARD 2: Papel e Formatacao ===
        card2 = CardFrame(content)
        card2.pack(fill="x", padx=20, pady=8)

        tk.Label(card2, text="Papel e Formatacao", bg=COR_CARD, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).grid(row=0, column=0, columnspan=2,
                 sticky="w", padx=5, pady=(5, 10))

        # Tamanho do Papel (58mm / 80mm)
        tk.Label(card2, text="Tamanho do Papel:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=1, column=0, sticky="w", padx=5, pady=4)
        tamanhos_papel = ["58mm", "80mm"]
        combo_tamanho_papel = ttk.Combobox(card2, values=tamanhos_papel, width=10, state="readonly",
                                            font=("Segoe UI", 10))
        combo_tamanho_papel.set(config.get("tamanho_papel", "80mm"))
        combo_tamanho_papel.grid(row=1, column=1, sticky="w", padx=5, pady=4)

        # Modo da Fonte (Normal / Condensada / Comprimida)
        tk.Label(card2, text="Modo da Fonte ESC/POS:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=2, column=0, sticky="w", padx=5, pady=4)
        modos_fonte = ["Normal", "Condensada", "Comprimida", "Grande", "Extra Grande"]
        combo_modo_fonte = ttk.Combobox(card2, values=modos_fonte, width=15, state="readonly",
                                         font=("Segoe UI", 10))
        combo_modo_fonte.set(config.get("modo_fonte", "Grande"))
        combo_modo_fonte.grid(row=2, column=1, sticky="w", padx=5, pady=4)

        # Label informativo sobre modo de fonte
        lbl_fonte_info = tk.Label(card2, text="", bg=COR_CARD, fg=COR_TEXTO2,
                                    font=("Segoe UI", 8, "italic"))
        lbl_fonte_info.grid(row=3, column=1, sticky="w", padx=5, pady=(0, 2))

        # Tabela de referencia de colunas
        # 58mm: Normal=32, Condensada=42, Comprimida=42, Grande=32, ExtraGrande=16
        # 80mm: Normal=42, Condensada=56, Comprimida=56, Grande=42, ExtraGrande=21
        # (Grande dobra so a ALTURA => mesmas colunas; Extra Grande dobra tambem
        #  a LARGURA => metade das colunas)
        PRESETS_COLUNAS = {
            ("58mm", "Normal"): 32,
            ("58mm", "Condensada"): 42,
            ("58mm", "Comprimida"): 42,
            ("58mm", "Grande"): 32,
            ("58mm", "Extra Grande"): 16,
            ("80mm", "Normal"): 42,
            ("80mm", "Condensada"): 56,
            ("80mm", "Comprimida"): 56,
            ("80mm", "Grande"): 42,
            ("80mm", "Extra Grande"): 21,
        }

        def _atualizar_preset_colunas(event=None):
            """Atualiza automaticamente a largura de colunas com base no
            tamanho do papel e modo de fonte selecionados."""
            tam = combo_tamanho_papel.get()
            modo = combo_modo_fonte.get()
            colunas = PRESETS_COLUNAS.get((tam, modo), 48)
            et_largura.delete(0, tk.END)
            et_largura.insert(0, str(colunas))
            lbl_fonte_info.config(
                text=f"Preset: {tam} + {modo} = {colunas} colunas",
                fg=COR_SUCESSO
            )

        combo_tamanho_papel.bind("<<ComboboxSelected>>", _atualizar_preset_colunas)
        combo_modo_fonte.bind("<<ComboboxSelected>>", _atualizar_preset_colunas)

        # Largura do Papel (colunas) - agora na row 4
        tk.Label(card2, text="Largura do Papel (colunas):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=4, column=0, sticky="w", padx=5, pady=4)
        et_largura = StyledEntry(card2, width=10)
        et_largura.insert(0, str(config.get("largura_papel", 48)))
        et_largura.grid(row=4, column=1, sticky="w", padx=5, pady=4)

        # Tamanho da Fonte (agora afeta a IMPRESSAO) - row 5
        tk.Label(card2, text="Tamanho da Fonte (impressao):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=5, column=0, sticky="w", padx=5, pady=4)
        et_fonte = StyledEntry(card2, width=10)
        et_fonte.insert(0, str(config.get("tamanho_fonte", 12)))
        et_fonte.grid(row=5, column=1, sticky="w", padx=5, pady=4)
        add_tooltip(et_fonte,
                    "Controla o tamanho impresso (altura):\n"
                    "ate 12 = normal | 13-18 = 2x | 19-26 = 3x | 27+ = 4x.\n"
                    "A largura/colunas nao muda (use 'Extra Grande' p/ dobrar a largura).")

        # Margem Esquerda
        tk.Label(card2, text="Margem Esquerda (colunas):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=6, column=0, sticky="w", padx=5, pady=4)
        et_margem_esq = StyledEntry(card2, width=10)
        et_margem_esq.insert(0, str(config.get("margem_esquerda", 0)))
        et_margem_esq.grid(row=6, column=1, sticky="w", padx=5, pady=4)

        # Margem Direita
        tk.Label(card2, text="Margem Direita (colunas):", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=7, column=0, sticky="w", padx=5, pady=4)
        et_margem_dir = StyledEntry(card2, width=10)
        et_margem_dir.insert(0, str(config.get("margem_direita", 0)))
        et_margem_dir.grid(row=7, column=1, sticky="w", padx=5, pady=4)

        # Espacamento entre Linhas
        tk.Label(card2, text="Espacamento entre Linhas:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=8, column=0, sticky="w", padx=5, pady=4)
        et_espacamento = StyledEntry(card2, width=10)
        et_espacamento.insert(0, str(config.get("espacamento_linhas", 1)))
        et_espacamento.grid(row=8, column=1, sticky="w", padx=5, pady=4)

        # Numero de Copias
        tk.Label(card2, text="Numero de Copias:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=9, column=0, sticky="w", padx=5, pady=4)
        et_copias = StyledEntry(card2, width=10)
        et_copias.insert(0, str(config.get("num_copias", 1)))
        et_copias.grid(row=9, column=1, sticky="w", padx=5, pady=4)

        # Velocidade de Impressao
        tk.Label(card2, text="Velocidade de Impressao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=10, column=0, sticky="w", padx=5, pady=4)
        velocidades = ["Lenta", "Normal", "Rapida"]
        combo_velocidade = ttk.Combobox(card2, values=velocidades, width=30, state="readonly",
                                         font=("Segoe UI", 10))
        combo_velocidade.set(config.get("velocidade_impressao", "Normal"))
        combo_velocidade.grid(row=10, column=1, sticky="w", padx=5, pady=4)

        # === CARD 3: Opcoes Avancadas ===
        card3 = CardFrame(content)
        card3.pack(fill="x", padx=20, pady=8)

        tk.Label(card3, text="Opcoes Avancadas", bg=COR_CARD, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).grid(row=0, column=0, columnspan=2,
                 sticky="w", padx=5, pady=(5, 10))

        # Corte Automatico
        var_corte = tk.BooleanVar(value=config.get("corte_automatico", True))
        tk.Checkbutton(card3, text="Corte Automatico do Papel", variable=var_corte,
                       bg=COR_CARD, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_CARD, font=("Segoe UI", 10)).grid(
                       row=1, column=0, columnspan=2, sticky="w", padx=5, pady=3)

        # Abrir Gaveta
        var_gaveta = tk.BooleanVar(value=config.get("abrir_gaveta", False))
        tk.Checkbutton(card3, text="Abrir Gaveta ao Imprimir", variable=var_gaveta,
                       bg=COR_CARD, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_CARD, font=("Segoe UI", 10)).grid(
                       row=2, column=0, columnspan=2, sticky="w", padx=5, pady=3)

        # Imprimir Logo
        var_logo = tk.BooleanVar(value=config.get("imprimir_logo", False))
        tk.Checkbutton(card3, text="Imprimir Logo no Cupom", variable=var_logo,
                       bg=COR_CARD, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_CARD, font=("Segoe UI", 10)).grid(
                       row=3, column=0, columnspan=2, sticky="w", padx=5, pady=3)

        # Caminho do Logo
        tk.Label(card3, text="Caminho do Logo:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=4, column=0, sticky="w", padx=5, pady=4)
        logo_frame = tk.Frame(card3, bg=COR_CARD)
        logo_frame.grid(row=4, column=1, sticky="w", padx=5, pady=4)
        et_logo = StyledEntry(logo_frame, width=28)
        et_logo.insert(0, config.get("caminho_logo", ""))
        et_logo.pack(side="left")

        def selecionar_logo():
            path = filedialog.askopenfilename(
                title="Selecionar Logo",
                filetypes=[("Imagens", "*.png *.jpg *.jpeg *.bmp *.gif"), ("Todos", "*.*")]
            )
            if path:
                et_logo.delete(0, tk.END)
                et_logo.insert(0, path)

        tk.Button(logo_frame, text="...", bg=COR_ENTRADA, fg=COR_TEXTO,
                  relief="flat", font=("Segoe UI", 10), cursor="hand2",
                  command=selecionar_logo, width=3).pack(side="left", padx=(5, 0))

        # Impressao Automatica
        var_auto = tk.BooleanVar(value=config.get("imprimir_automatico", False))
        tk.Checkbutton(card3, text="Imprimir Automaticamente ao Finalizar Venda",
                       variable=var_auto, bg=COR_CARD, fg=COR_TEXTO,
                       selectcolor=COR_ENTRADA, activebackground=COR_CARD,
                       font=("Segoe UI", 10)).grid(
                       row=5, column=0, columnspan=2, sticky="w", padx=5, pady=3)

        # === Servidor de Impressao (.fr3) ===
        var_servidor = tk.BooleanVar(value=config.get("usar_servidor_impressao", False))
        tk.Checkbutton(card3,
                       text="Enviar impressao para o Servidor de Impressao (.fr3)",
                       variable=var_servidor, bg=COR_CARD, fg=COR_TEXTO,
                       selectcolor=COR_ENTRADA, activebackground=COR_CARD,
                       font=("Segoe UI", 10, "bold")).grid(
                       row=6, column=0, columnspan=2, sticky="w", padx=5, pady=(10, 3))
        tk.Label(card3,
                 text="(Quando marcado, o cupom NAO vai direto para a impressora: "
                      "vai para o servidor que gera o .fr3)",
                 bg=COR_CARD, fg=COR_TEXTO2, font=("Segoe UI", 8, "italic")).grid(
                 row=7, column=0, columnspan=2, sticky="w", padx=25, pady=(0, 3))
        tk.Label(card3, text="URL do Servidor de Impressao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=8, column=0, sticky="w", padx=5, pady=4)
        et_servidor_url = StyledEntry(card3, width=35)
        et_servidor_url.insert(0, config.get("servidor_impressao_url",
                                             "http://127.0.0.1:8899/print"))
        et_servidor_url.grid(row=8, column=1, sticky="w", padx=5, pady=4)

        # === Metodo de Impressao ===
        tk.Label(card3, text="Metodo de Impressao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10, "bold")).grid(row=9, column=0, sticky="w",
                 padx=5, pady=(10, 4))
        combo_metodo_imp = ttk.Combobox(
            card3, values=["Texto (ESC/POS)", "Grafico (imagem)"],
            width=20, state="readonly", font=("Segoe UI", 10))
        combo_metodo_imp.set(config.get("metodo_impressao", "Texto (ESC/POS)"))
        combo_metodo_imp.grid(row=9, column=1, sticky="w", padx=5, pady=(10, 4))
        tk.Label(card3,
                 text="Grafico (imagem): desenha o cupom com a fonte no tamanho exato e "
                      "imprime como imagem.\nUse este modo se a fonte NAO aumenta no modo "
                      "Texto (a impressora ignora ESC/POS).\nRequer pywin32 + Pillow.",
                 bg=COR_CARD, fg=COR_TEXTO2, font=("Segoe UI", 8, "italic"),
                 justify="left").grid(row=10, column=0, columnspan=2, sticky="w",
                 padx=25, pady=(0, 4))

        # === CARD 4: Botoes de Acao ===
        card4 = CardFrame(content)
        card4.pack(fill="x", padx=20, pady=8)

        btn_frame = tk.Frame(card4, bg=COR_CARD)
        btn_frame.pack(fill="x", pady=5)

        def coletar_config():
            """Coleta todos os valores dos campos e retorna um dict."""
            try:
                largura = int(et_largura.get().strip())
            except (ValueError, TypeError):
                largura = 48
            try:
                fonte = int(et_fonte.get().strip())
            except (ValueError, TypeError):
                fonte = 10
            try:
                copias = int(et_copias.get().strip())
            except (ValueError, TypeError):
                copias = 1
            try:
                m_esq = int(et_margem_esq.get().strip())
            except (ValueError, TypeError):
                m_esq = 0
            try:
                m_dir = int(et_margem_dir.get().strip())
            except (ValueError, TypeError):
                m_dir = 0
            try:
                espac = int(et_espacamento.get().strip())
            except (ValueError, TypeError):
                espac = 1

            if largura < 20:
                largura = 20
            if largura > 120:
                largura = 120
            if fonte < 6:
                fonte = 6
            if fonte > 30:
                fonte = 30
            if copias < 1:
                copias = 1
            if copias > 10:
                copias = 10

            return {
                "tipo_impressora": combo_tipo.get(),
                "nome_impressora": et_nome.get().strip(),
                "porta_impressora": et_porta.get().strip(),
                "codepage": combo_codepage.get(),
                "tamanho_papel": combo_tamanho_papel.get(),
                "largura_papel": largura,
                "modo_fonte": combo_modo_fonte.get(),
                "tamanho_fonte": fonte,
                "margem_esquerda": m_esq,
                "margem_direita": m_dir,
                "espacamento_linhas": espac,
                "num_copias": copias,
                "velocidade_impressao": combo_velocidade.get(),
                "corte_automatico": var_corte.get(),
                "abrir_gaveta": var_gaveta.get(),
                "imprimir_logo": var_logo.get(),
                "caminho_logo": et_logo.get().strip(),
                "imprimir_automatico": var_auto.get(),
                "usar_servidor_impressao": var_servidor.get(),
                "servidor_impressao_url": et_servidor_url.get().strip(),
                "metodo_impressao": combo_metodo_imp.get(),
                "fonte_migrada_v2": True
            }

        def salvar_config():
            cfg = coletar_config()
            if self._save_printer_config(cfg):
                self.show_success("Configuracoes de impressora salvas com sucesso!")
            else:
                self.show_error("Erro ao salvar configuracoes de impressora.")

        def testar_impressao():
            cfg = coletar_config()
            largura = cfg["largura_papel"]
            m_esq = cfg.get("margem_esquerda", 0)
            m_dir = cfg.get("margem_direita", 0)
            largura_util = max(20, largura - m_esq - m_dir)
            prefixo = " " * m_esq

            line = prefixo + "-" * largura_util
            line_eq = prefixo + "=" * largura_util

            def _t(texto):
                """Texto alinhado a esquerda, truncado na largura util."""
                return prefixo + texto[:largura_util]

            def _tc(texto):
                """Texto centralizado na largura util."""
                return prefixo + CupomGenerator._center(texto, largura_util)

            def _tw(texto):
                """Texto com word-wrap automatico."""
                lines = []
                for ln in CupomGenerator._wrap_text(texto, largura_util):
                    lines.append(prefixo + ln)
                return lines

            cupom = []
            cupom.append(line_eq)
            cupom.append(_tc("TESTE DE IMPRESSAO"))
            cupom.append(_tc(f"PDV Pro v{APP_VERSION}"))
            cupom.append(line)
            # Informacoes da impressora - cada campo em sua propria linha
            cupom.append(_t(f"Tipo: {cfg['tipo_impressora']}"))
            cupom.extend(_tw(f"Impressora: {cfg['nome_impressora'] or '(padrao)'}"))
            cupom.append(_t(f"Porta: {cfg['porta_impressora'] or '(auto)'}"))
            cupom.append(_t(f"Papel: {cfg.get('tamanho_papel', '80mm')}"))
            cupom.append(_t(f"Colunas: {largura} (util: {largura_util})"))
            cupom.append(_t(f"Fonte: {cfg.get('modo_fonte', 'Normal')}"))
            cupom.append(_t(f"Margens: E={m_esq} D={m_dir}"))
            cupom.append(_t(f"Codepage: {cfg['codepage']}"))
            cupom.append(_t(f"Copias: {cfg['num_copias']}"))
            cupom.append(_t(f"Velocidade: {cfg['velocidade_impressao']}"))
            cupom.append(line)
            corte_s = "Sim" if cfg['corte_automatico'] else "Nao"
            gaveta_s = "Sim" if cfg['abrir_gaveta'] else "Nao"
            logo_s = "Sim" if cfg['imprimir_logo'] else "Nao"
            auto_s = "Sim" if cfg['imprimir_automatico'] else "Nao"
            cupom.append(_t(f"Corte: {corte_s}  Gaveta: {gaveta_s}"))
            cupom.append(_t(f"Logo: {logo_s}  Auto: {auto_s}"))
            cupom.append(line)
            # Teste de caracteres (respeitando largura util)
            abc_upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
            abc_lower = "abcdefghijklmnopqrstuvwxyz"
            nums = "0123456789"
            symbols = "!@#$%&*()-+=[]{}|;:',.<>?/"
            cupom.append(_t(abc_upper[:largura_util]))
            cupom.append(_t(abc_lower[:largura_util]))
            cupom.append(_t(nums[:largura_util]))
            cupom.append(_t(symbols[:largura_util]))
            cupom.append(line)
            cupom.append(_tc("Teste de acentuacao:"))
            cupom.append(_tc("aeiou AEIOU cC"))
            cupom.append(line)
            # Teste de regua de largura
            regua = "".join([str(i % 10) for i in range(1, largura_util + 1)])
            cupom.append(_t(regua))
            cupom.append(line)
            data_hora = datetime.datetime.now().strftime("%d/%m/%Y %H:%M:%S")
            cupom.append(_tc(f"Data/Hora: {data_hora}"))
            cupom.append(line_eq)
            cupom.append(_tc("FIM DO TESTE"))
            cupom.append(line_eq)

            texto_cupom = "\n".join(cupom)

            # Exibir preview do teste
            preview = tk.Toplevel(self.root)
            preview.title("Teste de Impressao - Preview")
            preview.geometry("550x650")
            preview.configure(bg=COR_FUNDO)
            preview.transient(self.root)
            preview.grab_set()

            tk.Label(preview, text="Preview do Teste de Impressao", bg=COR_FUNDO,
                     fg=COR_PRIMARIA, font=("Segoe UI", 14, "bold")).pack(pady=10)

            text_area = scrolledtext.ScrolledText(preview, width=60, height=25,
                                                   bg=COR_ENTRADA, fg=COR_TEXTO,
                                                   font=("Courier New", cfg["tamanho_fonte"]),
                                                   relief="flat", bd=0)
            text_area.pack(fill="both", expand=True, padx=15, pady=5)
            text_area.insert("1.0", texto_cupom)
            text_area.config(state="disabled")

            # Se o metodo for GRAFICO, mostra a PREVIA REAL (imagem) que sera
            # impressa - controlada pela Largura do Papel (colunas). Assim o
            # que se ve na tela e igual ao que sai no papel.
            try:
                if str(cfg.get("metodo_impressao", "")).lower().startswith("graf"):
                    from PIL import ImageTk
                    _tam = str(cfg.get("tamanho_papel", "80mm"))
                    _wpx = 576 if "80" in _tam else 384
                    _img = self._render_cupom_imagem(texto_cupom, cfg, _wpx)
                    if _img is not None:
                        text_area.pack_forget()
                        _disp_w = 480
                        _esc = _disp_w / float(_img.width)
                        _img_disp = _img.resize((_disp_w, max(1, int(_img.height * _esc))))
                        _photo = ImageTk.PhotoImage(_img_disp)
                        _cv = tk.Canvas(preview, bg="white", highlightthickness=0)
                        _vsb = ttk.Scrollbar(preview, orient="vertical", command=_cv.yview)
                        _cv.configure(yscrollcommand=_vsb.set,
                                      scrollregion=(0, 0, _disp_w, _img_disp.height))
                        _cv.create_image(0, 0, anchor="nw", image=_photo)
                        _cv.image = _photo  # mantem referencia
                        _vsb.pack(side="right", fill="y")
                        _cv.pack(fill="both", expand=True, padx=15, pady=5)
            except Exception as _epv:
                _logger.warning(f"Previa grafica falhou: {_epv}")

            # Label de status
            lbl_status_teste = tk.Label(preview, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                                         font=("Segoe UI", 9, "italic"))
            lbl_status_teste.pack(pady=(5, 0))

            def enviar_para_impressora():
                """Envia teste para a impressora usando o metodo centralizado."""
                lbl_status_teste.config(text="Enviando para impressora...", fg=COR_ALERTA)
                preview.update_idletasks()

                def _do_print():
                    return self._imprimir_texto(texto_cupom, config=cfg, parent_window=preview)

                def _on_result(resultado):
                    sucesso, msg = resultado
                    if sucesso:
                        lbl_status_teste.config(text=msg, fg=COR_SUCESSO)
                        messagebox.showinfo(
                            "Teste de Impressao OK",
                            f"{msg}\n\n"
                            "A pagina de teste foi enviada para a impressora.\n"
                            "Verifique se saiu corretamente na impressora.",
                            parent=preview)
                    else:
                        lbl_status_teste.config(text=f"Falha: {msg}", fg=COR_ERRO)
                        messagebox.showerror(
                            "Erro no Teste de Impressao",
                            f"{msg}\n\n"
                            "COMO RESOLVER:\n"
                            "1. Verifique se a impressora esta LIGADA e conectada\n"
                            "2. Verifique se o nome da impressora esta correto\n"
                            "3. Teste imprimir algo pelo Bloco de Notas do Windows\n"
                            "4. Se funcionar no Bloco de Notas, revise as configuracoes",
                            parent=preview)

                def _on_error(e):
                    lbl_status_teste.config(text=f"Erro: {e}", fg=COR_ERRO)
                    ErroAmigavel.mostrar_erro(e, "teste_impressao", parent=preview)

                self.run_async(_do_print, _on_result, _on_error)

            btn_preview_frame = tk.Frame(preview, bg=COR_FUNDO)
            btn_preview_frame.pack(fill="x", padx=15, pady=10)

            StyledButton(btn_preview_frame, text="Enviar para Impressora",
                         command=enviar_para_impressora,
                         color=COR_BOTAO_VERDE, width=20).pack(side="left", padx=5)
            StyledButton(btn_preview_frame, text="Fechar",
                         command=preview.destroy,
                         color="#2a3a5c", width=10).pack(side="right", padx=5)

        def restaurar_padroes():
            if messagebox.askyesno("Confirmar",
                "Deseja restaurar todas as configuracoes de impressora para os valores padrao?"):
                combo_tipo.set("Termica")
                combo_impressora.set("")
                lbl_status_busca.config(text="")
                lbl_impressora_padrao.config(text="")
                lbl_driver_info.config(text="")
                et_porta.delete(0, tk.END)
                combo_codepage.set("UTF-8")
                combo_tamanho_papel.set("80mm")
                combo_modo_fonte.set("Grande")
                lbl_fonte_info.config(text="")
                et_largura.delete(0, tk.END)
                et_largura.insert(0, "42")
                et_fonte.delete(0, tk.END)
                et_fonte.insert(0, "12")
                et_margem_esq.delete(0, tk.END)
                et_margem_esq.insert(0, "0")
                et_margem_dir.delete(0, tk.END)
                et_margem_dir.insert(0, "0")
                et_espacamento.delete(0, tk.END)
                et_espacamento.insert(0, "1")
                et_copias.delete(0, tk.END)
                et_copias.insert(0, "1")
                combo_velocidade.set("Normal")
                var_corte.set(True)
                var_gaveta.set(False)
                var_logo.set(False)
                et_logo.delete(0, tk.END)
                var_auto.set(False)
                self.show_success("Configuracoes restauradas para os valores padrao.")

        StyledButton(btn_frame, text="Salvar Configuracoes",
                     command=salvar_config,
                     color=COR_BOTAO_VERDE, width=20).pack(side="left", padx=8)
        StyledButton(btn_frame, text="⎙ Testar Impressao",
                     command=testar_impressao,
                     color=COR_BOTAO_AZUL, width=18).pack(side="left", padx=8)
        StyledButton(btn_frame, text="Restaurar Padroes",
                     command=restaurar_padroes,
                     color=COR_BOTAO_LARANJA, width=18).pack(side="left", padx=8)
        StyledButton(btn_frame, text="Voltar",
                     command=self.show_main_menu,
                     color="#2a3a5c", width=10).pack(side="right", padx=8)

    # ========================================================================
    # CONFIGURACAO DE MULTIPLAS IMPRESSORAS POR CATEGORIA
    # ========================================================================

    def _load_multi_printer_config(self):
        """Carrega configuracao de multiplas impressoras do arquivo JSON."""
        defaults = {
            "ativo": False,
            "modo_impressao": "local",
            "servidor_host": "localhost",
            "servidor_porta": 9876,
            "impressora_padrao": "",
            "impressoras": [],
            "imprimir_cupom_completo": True,
            "impressora_cupom_completo": ""
        }
        try:
            if os.path.exists(MULTI_PRINTER_CONFIG_FILE):
                with open(MULTI_PRINTER_CONFIG_FILE, "r", encoding="utf-8") as f:
                    saved = json.load(f)
                for k, v in defaults.items():
                    if k not in saved:
                        saved[k] = v
                return saved
        except Exception as e:
            print(f"Erro ao carregar config multi-impressora: {e}")
        return defaults

    def _save_multi_printer_config(self, config):
        """Salva configuracao de multiplas impressoras no arquivo JSON."""
        try:
            with open(MULTI_PRINTER_CONFIG_FILE, "w", encoding="utf-8") as f:
                json.dump(config, f, indent=2, ensure_ascii=False)
            return True
        except Exception as e:
            print(f"Erro ao salvar config multi-impressora: {e}")
            return False

    def _imprimir_por_categoria(self, venda, itens, empresa=None):
        """Separa itens por categoria e envia para impressoras mapeadas.
        Chamado apos finalizar a venda, em paralelo ao cupom principal."""
        try:
            multi_cfg = self._load_multi_printer_config()
            if not multi_cfg.get("ativo", False):
                return  # Multi-impressora desativado

            impressoras_cfg = multi_cfg.get("impressoras", [])
            if not impressoras_cfg:
                return  # Nenhuma impressora configurada

            modo = multi_cfg.get("modo_impressao", "local")
            printer_cfg_base = self._load_printer_config()

            # Agrupar itens por tipo_produto_id
            itens_por_tipo = {}
            itens_sem_tipo = []
            for item in itens:
                tipo_id = item.tipo_produto_id
                if tipo_id:
                    if tipo_id not in itens_por_tipo:
                        itens_por_tipo[tipo_id] = []
                    itens_por_tipo[tipo_id].append(item)
                else:
                    itens_sem_tipo.append(item)

            # Para cada impressora configurada, coletar os itens que ela deve imprimir
            for imp_cfg in impressoras_cfg:
                if not imp_cfg.get("ativa", True):
                    continue

                categorias_ids = imp_cfg.get("categorias", [])
                if not categorias_ids:
                    continue

                # Coletar itens desta impressora
                itens_impressora = []
                for cat_id in categorias_ids:
                    if cat_id in itens_por_tipo:
                        itens_impressora.extend(itens_por_tipo[cat_id])

                if not itens_impressora:
                    continue  # Nenhum item para esta impressora

                # Gerar cupom parcial
                largura = imp_cfg.get("largura_papel", printer_cfg_base.get("largura_papel", 48))
                margem_esq = imp_cfg.get("margem_esquerda", printer_cfg_base.get("margem_esquerda", 0))
                margem_dir = imp_cfg.get("margem_direita", printer_cfg_base.get("margem_direita", 0))
                espacamento = imp_cfg.get("espacamento_linhas", printer_cfg_base.get("espacamento_linhas", 1))

                cupom_parcial = CupomGenerator.gerar_cupom_parcial(
                    venda, itens_impressora,
                    nome_impressora=imp_cfg.get("nome", ""),
                    nome_setor=imp_cfg.get("nome_setor", imp_cfg.get("nome", "")),
                    empresa=empresa,
                    colunas=largura,
                    margem_esq=margem_esq,
                    margem_dir=margem_dir,
                    espacamento=espacamento
                )

                # Enviar para impressora
                if modo == "servidor":
                    self._enviar_para_servidor_impressao(
                        cupom_parcial, imp_cfg, multi_cfg
                    )
                else:
                    # Modo local - usar _imprimir_texto com config da impressora especifica
                    config_imp = dict(printer_cfg_base)
                    config_imp["nome_impressora"] = imp_cfg.get("nome_sistema", "")
                    config_imp["porta_impressora"] = imp_cfg.get("porta", "")
                    config_imp["tipo_impressora"] = imp_cfg.get("tipo_impressora", "Termica")
                    config_imp["codepage"] = imp_cfg.get("codepage", "UTF-8")
                    config_imp["largura_papel"] = largura
                    config_imp["corte_automatico"] = imp_cfg.get("corte_automatico", True)
                    config_imp["abrir_gaveta"] = imp_cfg.get("abrir_gaveta", False)
                    config_imp["modo_fonte"] = imp_cfg.get("modo_fonte", printer_cfg_base.get("modo_fonte", "Condensada"))
                    config_imp["num_copias"] = imp_cfg.get("num_copias", 1)

                    def _do_print(texto=cupom_parcial, cfg=config_imp):
                        return self._imprimir_texto(texto, config=cfg)

                    def _on_ok(result, nome=imp_cfg.get("nome", "")):
                        sucesso, msg = result
                        if sucesso:
                            _logger.info(f"Multi-impressora OK: {nome} - {msg}")
                        else:
                            _logger.error(f"Multi-impressora ERRO: {nome} - {msg}")
                            ToastManager.error(f"Erro ao imprimir em '{nome}': {msg}")

                    def _on_err(e, nome=imp_cfg.get("nome", "")):
                        _logger.error(f"Multi-impressora EXCECAO: {nome} - {e}")
                        ToastManager.error(f"Erro na impressora '{nome}': {e}")

                    self.run_async(_do_print, _on_ok, _on_err)

            # Itens sem tipo mapeado -> impressora padrao (se configurada)
            imp_padrao_nome = multi_cfg.get("impressora_padrao", "")
            if itens_sem_tipo and imp_padrao_nome:
                # Verificar se ha impressora padrao configurada que nao seja nenhuma das mapeadas
                cupom_sem_tipo = CupomGenerator.gerar_cupom_parcial(
                    venda, itens_sem_tipo,
                    nome_impressora=imp_padrao_nome,
                    nome_setor="SEM CATEGORIA",
                    empresa=empresa,
                    colunas=printer_cfg_base.get("largura_papel", 48),
                    margem_esq=printer_cfg_base.get("margem_esquerda", 0),
                    margem_dir=printer_cfg_base.get("margem_direita", 0),
                    espacamento=printer_cfg_base.get("espacamento_linhas", 1)
                )
                if modo == "servidor":
                    self._enviar_para_servidor_impressao(
                        cupom_sem_tipo,
                        {"nome_sistema": imp_padrao_nome, "porta": "",
                         "tipo_impressora": "Termica", "codepage": "UTF-8",
                         "corte_automatico": True, "abrir_gaveta": False},
                        multi_cfg
                    )
                else:
                    config_padrao = dict(printer_cfg_base)
                    config_padrao["nome_impressora"] = imp_padrao_nome

                    def _do_print_padrao(texto=cupom_sem_tipo, cfg=config_padrao):
                        return self._imprimir_texto(texto, config=cfg)

                    def _on_ok_padrao(result):
                        sucesso, msg = result
                        if not sucesso:
                            ToastManager.error(f"Erro impressora padrao: {msg}")

                    self.run_async(_do_print_padrao, _on_ok_padrao)

        except Exception as e:
            _logger.error(f"Erro em _imprimir_por_categoria: {e}")
            ToastManager.error(f"Erro ao imprimir por categoria: {e}")

    def _enviar_para_servidor_impressao(self, texto, imp_cfg, multi_cfg):
        """Envia job de impressao para o PrintServerPDV via HTTP."""
        try:
            import urllib.request
            host = multi_cfg.get("servidor_host", "localhost")
            porta = multi_cfg.get("servidor_porta", 9876)
            url = f"http://{host}:{porta}/print"

            payload = {
                "texto": texto,
                "impressora": imp_cfg.get("nome_sistema", ""),
                "porta": imp_cfg.get("porta", ""),
                "config": {
                    "tipo_impressora": imp_cfg.get("tipo_impressora", "Termica"),
                    "codepage": imp_cfg.get("codepage", "UTF-8"),
                    "modo_fonte": imp_cfg.get("modo_fonte", "Condensada"),
                    "corte_automatico": imp_cfg.get("corte_automatico", True),
                    "abrir_gaveta": imp_cfg.get("abrir_gaveta", False),
                    "largura_papel": imp_cfg.get("largura_papel", 48)
                },
                "titulo": f"PDV Pro - {imp_cfg.get('nome', 'Impressao')}"
            }

            data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
            req = urllib.request.Request(
                url, data=data,
                headers={"Content-Type": "application/json; charset=utf-8"},
                method="POST"
            )

            with urllib.request.urlopen(req, timeout=10) as resp:
                result = json.loads(resp.read().decode("utf-8"))
                if result.get("success"):
                    _logger.info(f"Servidor impressao OK: {result.get('message', '')}")
                else:
                    _logger.error(f"Servidor impressao ERRO: {result.get('error', '')}")
                    ToastManager.error(f"Erro servidor: {result.get('error', '')}")

        except Exception as e:
            _logger.error(f"Erro ao enviar para servidor de impressao: {e}")
            ToastManager.error(f"Erro ao conectar ao servidor de impressao: {e}")

    def _testar_conexao_servidor(self, host, porta, callback_ok=None, callback_err=None):
        """Testa conexao com o PrintServerPDV."""
        def _testar():
            try:
                import urllib.request
                url = f"http://{host}:{porta}/status"
                req = urllib.request.Request(url, method="GET")
                with urllib.request.urlopen(req, timeout=5) as resp:
                    result = json.loads(resp.read().decode("utf-8"))
                    return True, result
            except Exception as e:
                return False, str(e)

        def _on_result(result):
            sucesso, data = result
            if sucesso and callback_ok:
                callback_ok(data)
            elif not sucesso and callback_err:
                callback_err(data)

        self.run_async(_testar, _on_result)

    def show_config_multi_impressora(self):
        """Tela de configuracao de multiplas impressoras por categoria de produto."""
        self.clear_container()

        # Barra superior
        top = tk.Frame(self.main_container, bg=COR_FUNDO2, height=52)
        top.pack(fill="x")
        top.pack_propagate(False)
        _neon = tk.Canvas(top, height=2, bg=COR_FUNDO2, highlightthickness=0)
        _neon.pack(fill="x", side="top")
        _neon.bind("<Configure>", lambda e: (
            _neon.delete("all"),
            _neon.create_line(0, 1, e.width, 1, fill=COR_PRIMARIA, width=1)))
        StyledButton(top, text="\u25c0 Voltar", command=self.show_main_menu,
                     color=COR_FUNDO3, width=10).pack(side="left", padx=10, pady=5)
        tk.Label(top, text="Configuracao de Multiplas Impressoras", bg=COR_FUNDO2, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(side="left", padx=10)

        _btn_sair = StyledButton(top, text=f"{Icons.SAIR} Sair",
                     command=self._on_close,
                     color=COR_ERRO, width=8)
        _btn_sair.pack(side="right", padx=5, pady=5)

        _btn_trocar = StyledButton(top, text=f"{Icons.TROCAR} Trocar",
                     command=self.do_logoff,
                     color=COR_BOTAO_LARANJA, width=10)
        _btn_trocar.pack(side="right", padx=5, pady=5)

        # Carregar config
        multi_cfg = self._load_multi_printer_config()

        # Area com scroll
        scroll = ScrollableFrame(self.main_container)
        scroll.pack(fill="both", expand=True, padx=10, pady=10)
        content = scroll.scrollable_frame

        # === CARD 1: Configuracao Geral ===
        card1 = CardFrame(content)
        card1.pack(fill="x", padx=20, pady=8)

        tk.Label(card1, text="Configuracao Geral", bg=COR_CARD, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).grid(row=0, column=0, columnspan=3,
                 sticky="w", padx=5, pady=(5, 10))

        # Ativar/Desativar
        var_ativo = tk.BooleanVar(value=multi_cfg.get("ativo", False))
        tk.Checkbutton(card1, text="Ativar impressao em multiplas impressoras por categoria",
                       variable=var_ativo, bg=COR_CARD, fg=COR_TEXTO,
                       selectcolor=COR_ENTRADA, activebackground=COR_CARD,
                       font=("Segoe UI", 10, "bold")).grid(
                       row=1, column=0, columnspan=3, sticky="w", padx=5, pady=3)

        # Modo de impressao
        tk.Label(card1, text="Modo de Impressao:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=2, column=0, sticky="w", padx=5, pady=4)
        modos = ["local", "servidor"]
        combo_modo = ttk.Combobox(card1, values=modos, width=15, state="readonly",
                                   font=("Segoe UI", 10))
        combo_modo.set(multi_cfg.get("modo_impressao", "local"))
        combo_modo.grid(row=2, column=1, sticky="w", padx=5, pady=4)

        lbl_modo_info = tk.Label(card1, text="", bg=COR_CARD, fg=COR_TEXTO2,
                                  font=("Segoe UI", 8, "italic"))
        lbl_modo_info.grid(row=2, column=2, sticky="w", padx=5, pady=4)

        def _on_modo_change(event=None):
            modo = combo_modo.get()
            if modo == "local":
                lbl_modo_info.config(text="Impressao direta nas impressoras deste PC")
                et_srv_host.config(state="disabled")
                et_srv_porta.config(state="disabled")
                btn_testar_srv.config(state="disabled")
            else:
                lbl_modo_info.config(text="Envia para o PrintServerPDV (outro PC ou servico)")
                et_srv_host.config(state="normal")
                et_srv_porta.config(state="normal")
                btn_testar_srv.config(state="normal")

        combo_modo.bind("<<ComboboxSelected>>", _on_modo_change)

        # Servidor de Impressao
        tk.Label(card1, text="Host do Servidor:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=3, column=0, sticky="w", padx=5, pady=4)
        et_srv_host = StyledEntry(card1, width=20)
        et_srv_host.insert(0, multi_cfg.get("servidor_host", "localhost"))
        et_srv_host.grid(row=3, column=1, sticky="w", padx=5, pady=4)

        tk.Label(card1, text="Porta do Servidor:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=4, column=0, sticky="w", padx=5, pady=4)
        et_srv_porta = StyledEntry(card1, width=10)
        et_srv_porta.insert(0, str(multi_cfg.get("servidor_porta", 9876)))
        et_srv_porta.grid(row=4, column=1, sticky="w", padx=5, pady=4)

        lbl_srv_status = tk.Label(card1, text="", bg=COR_CARD, fg=COR_TEXTO2,
                                    font=("Segoe UI", 9))
        lbl_srv_status.grid(row=5, column=1, columnspan=2, sticky="w", padx=5, pady=2)

        def _testar_servidor():
            host = et_srv_host.get().strip() or "localhost"
            try:
                porta = int(et_srv_porta.get().strip())
            except ValueError:
                porta = 9876
            lbl_srv_status.config(text="Testando conexao...", fg=COR_ALERTA)

            def _ok(data):
                lbl_srv_status.config(
                    text=f"Conectado! v{data.get('version', '?')} - {data.get('platform', '?')} - "
                         f"Jobs: {data.get('stats', {}).get('total_jobs', 0)}",
                    fg=COR_SUCESSO
                )

            def _err(msg):
                lbl_srv_status.config(text=f"Falha: {msg}", fg=COR_ERRO)

            self._testar_conexao_servidor(host, porta, _ok, _err)

        btn_testar_srv = tk.Button(card1, text="Testar Conexao",
                                    bg=COR_BOTAO_AZUL, fg=COR_TEXTO,
                                    font=("Segoe UI", 9, "bold"), relief="flat",
                                    cursor="hand2", padx=8, pady=2,
                                    command=_testar_servidor)
        btn_testar_srv.grid(row=5, column=0, sticky="w", padx=5, pady=4)

        # Cupom completo
        var_cupom_completo = tk.BooleanVar(value=multi_cfg.get("imprimir_cupom_completo", True))
        tk.Checkbutton(card1, text="Tambem imprimir cupom COMPLETO na impressora principal",
                       variable=var_cupom_completo, bg=COR_CARD, fg=COR_TEXTO,
                       selectcolor=COR_ENTRADA, activebackground=COR_CARD,
                       font=("Segoe UI", 10)).grid(
                       row=6, column=0, columnspan=3, sticky="w", padx=5, pady=3)

        # Impressora padrao (itens sem categoria)
        tk.Label(card1, text="Impressora p/ itens sem categoria:", bg=COR_GLASS, fg=COR_TEXTO,
                 font=("Segoe UI", 10)).grid(row=7, column=0, sticky="w", padx=5, pady=4)
        et_imp_padrao = StyledEntry(card1, width=30)
        et_imp_padrao.insert(0, multi_cfg.get("impressora_padrao", ""))
        et_imp_padrao.grid(row=7, column=1, columnspan=2, sticky="w", padx=5, pady=4)

        _on_modo_change()  # Atualizar estado inicial

        # === CARD 2: Lista de Impressoras ===
        card2 = CardFrame(content)
        card2.pack(fill="x", padx=20, pady=8)

        tk.Label(card2, text="Impressoras Configuradas", bg=COR_CARD, fg=COR_PRIMARIA,
                 font=("Segoe UI", 12, "bold")).pack(anchor="w", padx=5, pady=(5, 10))

        # Treeview de impressoras
        cols_imp = ("nome", "nome_sistema", "categorias", "tipo", "ativa")
        tree_imp = ttk.Treeview(card2, columns=cols_imp, show="headings", height=6)
        tree_imp.heading("nome", text="Nome/Setor")
        tree_imp.heading("nome_sistema", text="Impressora (Sistema)")
        tree_imp.heading("categorias", text="Categorias")
        tree_imp.heading("tipo", text="Tipo")
        tree_imp.heading("ativa", text="Ativa")
        tree_imp.column("nome", width=150)
        tree_imp.column("nome_sistema", width=180)
        tree_imp.column("categorias", width=250)
        tree_imp.column("tipo", width=80)
        tree_imp.column("ativa", width=50, anchor="center")
        tree_imp.pack(fill="x", padx=5, pady=5)

        # Lista interna de impressoras
        impressoras_lista = list(multi_cfg.get("impressoras", []))

        def _atualizar_tree_impressoras():
            tree_imp.delete(*tree_imp.get_children())
            for i, imp in enumerate(impressoras_lista):
                cats_desc = ", ".join(imp.get("categorias_desc", []))
                if not cats_desc:
                    cats_desc = "(nenhuma)"
                ativa_str = "Sim" if imp.get("ativa", True) else "Nao"
                tag = "even" if i % 2 == 0 else "odd"
                tree_imp.insert("", "end", values=(
                    imp.get("nome", ""),
                    imp.get("nome_sistema", ""),
                    cats_desc,
                    imp.get("tipo_impressora", "Termica"),
                    ativa_str
                ), tags=(tag,))
            tree_imp.tag_configure("even", background=COR_GLASS)
            tree_imp.tag_configure("odd", background=COR_CARD)

        _atualizar_tree_impressoras()

        # Botoes de acao para impressoras
        btn_imp_frame = tk.Frame(card2, bg=COR_CARD)
        btn_imp_frame.pack(fill="x", padx=5, pady=5)

        def _adicionar_impressora():
            self._dialog_editar_impressora(None, impressoras_lista, _atualizar_tree_impressoras)

        def _editar_impressora():
            sel = tree_imp.selection()
            if not sel:
                ToastManager.warning("Selecione uma impressora para editar.")
                return
            idx = tree_imp.index(sel[0])
            self._dialog_editar_impressora(idx, impressoras_lista, _atualizar_tree_impressoras)

        def _remover_impressora():
            sel = tree_imp.selection()
            if not sel:
                ToastManager.warning("Selecione uma impressora para remover.")
                return
            idx = tree_imp.index(sel[0])
            nome = impressoras_lista[idx].get("nome", "")
            if messagebox.askyesno("Confirmar", f"Remover a impressora '{nome}'?"):
                del impressoras_lista[idx]
                _atualizar_tree_impressoras()
                ToastManager.success(f"Impressora '{nome}' removida.")

        def _duplicar_impressora():
            sel = tree_imp.selection()
            if not sel:
                ToastManager.warning("Selecione uma impressora para duplicar.")
                return
            idx = tree_imp.index(sel[0])
            copia = dict(impressoras_lista[idx])
            copia["nome"] = copia.get("nome", "") + " (copia)"
            copia["id"] = f"imp_{int(time.time()*1000)}"
            copia["categorias"] = list(copia.get("categorias", []))
            copia["categorias_desc"] = list(copia.get("categorias_desc", []))
            impressoras_lista.append(copia)
            _atualizar_tree_impressoras()
            ToastManager.success("Impressora duplicada.")

        StyledButton(btn_imp_frame, text="+ Adicionar",
                     command=_adicionar_impressora,
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=3)
        StyledButton(btn_imp_frame, text="Editar",
                     command=_editar_impressora,
                     color=COR_BOTAO_AZUL, width=10).pack(side="left", padx=3)
        StyledButton(btn_imp_frame, text="Remover",
                     command=_remover_impressora,
                     color=COR_ERRO, width=10).pack(side="left", padx=3)
        StyledButton(btn_imp_frame, text="Duplicar",
                     command=_duplicar_impressora,
                     color=COR_BOTAO_LARANJA, width=10).pack(side="left", padx=3)

        # === CARD 3: Botoes Salvar/Voltar ===
        card3 = CardFrame(content)
        card3.pack(fill="x", padx=20, pady=8)
        btn_final = tk.Frame(card3, bg=COR_CARD)
        btn_final.pack(fill="x", pady=5)

        def _salvar_tudo():
            try:
                srv_porta = int(et_srv_porta.get().strip())
            except ValueError:
                srv_porta = 9876

            cfg = {
                "ativo": var_ativo.get(),
                "modo_impressao": combo_modo.get(),
                "servidor_host": et_srv_host.get().strip() or "localhost",
                "servidor_porta": srv_porta,
                "impressora_padrao": et_imp_padrao.get().strip(),
                "impressoras": impressoras_lista,
                "imprimir_cupom_completo": var_cupom_completo.get(),
                "impressora_cupom_completo": ""
            }
            if self._save_multi_printer_config(cfg):
                self.show_success("Configuracoes de multiplas impressoras salvas com sucesso!")
            else:
                self.show_error("Erro ao salvar configuracoes.")

        StyledButton(btn_final, text="Salvar Configuracoes",
                     command=_salvar_tudo,
                     color=COR_BOTAO_VERDE, width=22).pack(side="left", padx=8)
        StyledButton(btn_final, text="Voltar",
                     command=self.show_main_menu,
                     color="#2a3a5c", width=10).pack(side="right", padx=8)

    def _dialog_editar_impressora(self, idx, impressoras_lista, callback_atualizar):
        """Dialog para adicionar ou editar uma impressora na lista de multi-impressoras."""
        is_new = idx is None
        imp = {} if is_new else dict(impressoras_lista[idx])

        dialog = tk.Toplevel(self.root)
        dialog.title("Adicionar Impressora" if is_new else "Editar Impressora")
        dialog.geometry("650x700")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()

        tk.Label(dialog, text="Adicionar Impressora" if is_new else "Editar Impressora",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 14, "bold")).pack(pady=10)

        # Scroll area
        scroll_frame = tk.Frame(dialog, bg=COR_FUNDO)
        scroll_frame.pack(fill="both", expand=True, padx=15, pady=5)

        canvas = tk.Canvas(scroll_frame, bg=COR_FUNDO, highlightthickness=0)
        scrollbar = ttk.Scrollbar(scroll_frame, orient="vertical", command=canvas.yview)
        inner = tk.Frame(canvas, bg=COR_FUNDO)
        inner.bind("<Configure>", lambda e: canvas.configure(scrollregion=canvas.bbox("all")))
        canvas.create_window((0, 0), window=inner, anchor="nw")
        canvas.configure(yscrollcommand=scrollbar.set)
        canvas.pack(side="left", fill="both", expand=True)
        scrollbar.pack(side="right", fill="y")

        # Campos
        row = 0

        tk.Label(inner, text="Nome/Setor (ex: Cozinha, Bar, Confeitaria):",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        et_nome = StyledEntry(inner, width=40)
        et_nome.insert(0, imp.get("nome", ""))
        et_nome.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Nome da Impressora no Sistema (ex: EPSON TM-T20):",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        et_nome_sistema = StyledEntry(inner, width=40)
        et_nome_sistema.insert(0, imp.get("nome_sistema", ""))
        et_nome_sistema.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        # Buscar impressoras instaladas
        imp_frame = tk.Frame(inner, bg=COR_FUNDO)
        imp_frame.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        def _buscar_imp():
            def _buscar():
                return self._buscar_impressoras_instaladas()
            def _on_result(result):
                impressoras_encontradas, padrao = result
                if impressoras_encontradas:
                    et_nome_sistema.delete(0, tk.END)
                    # Mostrar dialog de selecao
                    sel_dialog = tk.Toplevel(dialog)
                    sel_dialog.title("Selecionar Impressora")
                    sel_dialog.geometry("400x300")
                    sel_dialog.configure(bg=COR_FUNDO)
                    sel_dialog.transient(dialog)
                    sel_dialog.grab_set()
                    tk.Label(sel_dialog, text="Selecione a impressora:",
                             bg=COR_FUNDO, fg=COR_PRIMARIA,
                             font=("Segoe UI", 12, "bold")).pack(pady=10)
                    listbox = tk.Listbox(sel_dialog, bg=COR_ENTRADA, fg=COR_TEXTO,
                                          font=("Segoe UI", 10), selectmode="single")
                    for imp_nome in impressoras_encontradas:
                        marcador = " [PADRAO]" if imp_nome == padrao else ""
                        listbox.insert("end", f"{imp_nome}{marcador}")
                    listbox.pack(fill="both", expand=True, padx=10, pady=5)
                    def _selecionar():
                        sel = listbox.curselection()
                        if sel:
                            nome_sel = impressoras_encontradas[sel[0]]
                            et_nome_sistema.delete(0, tk.END)
                            et_nome_sistema.insert(0, nome_sel)
                        sel_dialog.destroy()
                    StyledButton(sel_dialog, text="Selecionar",
                                 command=_selecionar,
                                 color=COR_BOTAO_VERDE, width=15).pack(pady=10)
                else:
                    ToastManager.warning("Nenhuma impressora encontrada.")
            self.run_async(_buscar, _on_result)

        tk.Button(imp_frame, text="Buscar Impressoras",
                  bg=COR_BOTAO_AZUL, fg=COR_TEXTO,
                  font=("Segoe UI", 9, "bold"), relief="flat",
                  cursor="hand2", padx=8, pady=2,
                  command=_buscar_imp).pack(side="left")

        tk.Label(inner, text="Porta (ex: COM1, USB001, LPT1):",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        et_porta = StyledEntry(inner, width=20)
        et_porta.insert(0, imp.get("porta", ""))
        et_porta.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Tipo de Impressora:",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        tipos = ["Termica", "Matricial", "Jato de Tinta", "Laser"]
        combo_tipo = ttk.Combobox(inner, values=tipos, width=20, state="readonly",
                                   font=("Segoe UI", 10))
        combo_tipo.set(imp.get("tipo_impressora", "Termica"))
        combo_tipo.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Codificacao (Codepage):",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        codepages = ["UTF-8", "CP850", "CP437", "ISO-8859-1", "Windows-1252"]
        combo_codepage = ttk.Combobox(inner, values=codepages, width=20, state="readonly",
                                       font=("Segoe UI", 10))
        combo_codepage.set(imp.get("codepage", "UTF-8"))
        combo_codepage.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Largura do Papel (colunas):",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        et_largura = StyledEntry(inner, width=10)
        et_largura.insert(0, str(imp.get("largura_papel", 48)))
        et_largura.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Modo da Fonte:",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        modos_fonte = ["Normal", "Condensada", "Comprimida", "Grande", "Extra Grande"]
        combo_modo_fonte = ttk.Combobox(inner, values=modos_fonte, width=15, state="readonly",
                                         font=("Segoe UI", 10))
        combo_modo_fonte.set(imp.get("modo_fonte", "Condensada"))
        combo_modo_fonte.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        # Opcoes
        var_corte = tk.BooleanVar(value=imp.get("corte_automatico", True))
        tk.Checkbutton(inner, text="Corte Automatico", variable=var_corte,
                       bg=COR_FUNDO, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_FUNDO, font=("Segoe UI", 10)).grid(
                       row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        var_gaveta = tk.BooleanVar(value=imp.get("abrir_gaveta", False))
        tk.Checkbutton(inner, text="Abrir Gaveta", variable=var_gaveta,
                       bg=COR_FUNDO, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_FUNDO, font=("Segoe UI", 10)).grid(
                       row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        var_ativa = tk.BooleanVar(value=imp.get("ativa", True))
        tk.Checkbutton(inner, text="Impressora Ativa", variable=var_ativa,
                       bg=COR_FUNDO, fg=COR_TEXTO, selectcolor=COR_ENTRADA,
                       activebackground=COR_FUNDO, font=("Segoe UI", 10)).grid(
                       row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        tk.Label(inner, text="Numero de Copias:",
                 bg=COR_FUNDO, fg=COR_TEXTO, font=("Segoe UI", 10)).grid(
                 row=row, column=0, sticky="w", padx=5, pady=4)
        row += 1
        et_copias = StyledEntry(inner, width=5)
        et_copias.insert(0, str(imp.get("num_copias", 1)))
        et_copias.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        # === CATEGORIAS ===
        tk.Label(inner, text="Categorias (Tipos de Produto) para esta impressora:",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 11, "bold")).grid(
                 row=row, column=0, sticky="w", padx=5, pady=(10, 5))
        row += 1

        cat_frame = tk.Frame(inner, bg=COR_FUNDO)
        cat_frame.grid(row=row, column=0, sticky="nsew", padx=5, pady=5)
        row += 1

        # Listbox com checkboxes simulados
        cat_listbox = tk.Listbox(cat_frame, bg=COR_ENTRADA, fg=COR_TEXTO,
                                  font=("Segoe UI", 10), selectmode="multiple",
                                  height=8, width=50)
        cat_listbox.pack(fill="both", expand=True, pady=2)

        lbl_cat_status = tk.Label(inner, text="Carregando categorias...",
                                   bg=COR_FUNDO, fg=COR_TEXTO2,
                                   font=("Segoe UI", 8, "italic"))
        lbl_cat_status.grid(row=row, column=0, sticky="w", padx=5, pady=2)
        row += 1

        # Carregar categorias do banco
        categorias_cache = []
        categorias_selecionadas = list(imp.get("categorias", []))

        def _carregar_categorias():
            def _buscar():
                try:
                    db = DatabaseHelper.get_instance()
                    return db.execute_query(
                        "SELECT id, descricao FROM tipos_produto WHERE ativo = 1 ORDER BY descricao"
                    )
                except Exception:
                    return []

            def _on_result(tipos):
                cat_listbox.delete(0, "end")
                categorias_cache.clear()
                if tipos:
                    for t in tipos:
                        categorias_cache.append({"id": t["id"], "descricao": t["descricao"]})
                        cat_listbox.insert("end", f"  {t['descricao']}  (ID: {t['id']})")
                    # Selecionar categorias ja mapeadas
                    for i, t in enumerate(categorias_cache):
                        if t["id"] in categorias_selecionadas:
                            cat_listbox.selection_set(i)
                    lbl_cat_status.config(
                        text=f"{len(tipos)} categoria(s) encontrada(s). Selecione as desejadas.",
                        fg=COR_SUCESSO
                    )
                else:
                    lbl_cat_status.config(
                        text="Nenhuma categoria encontrada. Cadastre tipos de produto primeiro.",
                        fg=COR_ALERTA
                    )

            self.run_async(_buscar, _on_result)

        _carregar_categorias()

        # Botoes
        btn_dialog_frame = tk.Frame(dialog, bg=COR_FUNDO)
        btn_dialog_frame.pack(fill="x", padx=15, pady=10)

        def _salvar_impressora():
            nome = et_nome.get().strip()
            if not nome:
                messagebox.showwarning("Campo Obrigatorio",
                    "O campo 'Nome/Setor' e obrigatorio.", parent=dialog)
                return

            nome_sistema = et_nome_sistema.get().strip()

            # Coletar categorias selecionadas
            selecionados = cat_listbox.curselection()
            cats_ids = []
            cats_desc = []
            for i in selecionados:
                if i < len(categorias_cache):
                    cats_ids.append(categorias_cache[i]["id"])
                    cats_desc.append(categorias_cache[i]["descricao"])

            try:
                largura = int(et_largura.get().strip())
                if largura < 20:
                    largura = 20
                if largura > 120:
                    largura = 120
            except ValueError:
                largura = 48

            try:
                copias = int(et_copias.get().strip())
                if copias < 1:
                    copias = 1
                if copias > 10:
                    copias = 10
            except ValueError:
                copias = 1

            nova_imp = {
                "id": imp.get("id", f"imp_{int(time.time()*1000)}"),
                "nome": nome,
                "nome_setor": nome,
                "nome_sistema": nome_sistema,
                "porta": et_porta.get().strip(),
                "categorias": cats_ids,
                "categorias_desc": cats_desc,
                "tipo_impressora": combo_tipo.get(),
                "codepage": combo_codepage.get(),
                "largura_papel": largura,
                "modo_fonte": combo_modo_fonte.get(),
                "corte_automatico": var_corte.get(),
                "abrir_gaveta": var_gaveta.get(),
                "ativa": var_ativa.get(),
                "num_copias": copias
            }

            if is_new:
                impressoras_lista.append(nova_imp)
            else:
                impressoras_lista[idx] = nova_imp

            callback_atualizar()
            dialog.destroy()
            ToastManager.success(f"Impressora '{nome}' {'adicionada' if is_new else 'atualizada'}!")

        StyledButton(btn_dialog_frame, text="Salvar",
                     command=_salvar_impressora,
                     color=COR_BOTAO_VERDE, width=15).pack(side="left", padx=5)
        StyledButton(btn_dialog_frame, text="Cancelar",
                     command=dialog.destroy,
                     color="#2a3a5c", width=10).pack(side="right", padx=5)

    # ========================================================================
    # SOBRE
    # ========================================================================
    # ========================================================================
    # TELAS ADICIONAIS (stubs para funcoes do dashboard)
    # ========================================================================

    def show_painel_cozinha(self):
        """Painel da Cozinha - acompanhar pedidos em preparo."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.COMANDAS} Painel da Cozinha")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)
        tk.Label(content, text="Painel da Cozinha",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=20)
        tk.Label(content, text="Funcionalidade em desenvolvimento.\nEm breve voce podera acompanhar os pedidos em preparo aqui.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11), justify="center").pack(pady=10)

    def show_painel_chamados(self):
        """Painel de Chamados - visualizar e gerenciar chamados dos clientes."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.ALERT} Painel de Chamados")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)
        tk.Label(content, text="Painel de Chamados",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=20)
        tk.Label(content, text="Funcionalidade em desenvolvimento.\nEm breve voce podera gerenciar os chamados dos clientes aqui.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11), justify="center").pack(pady=10)

    def show_gerenciador_chamados(self):
        """Gerenciador de Chamados - chamadas com voz e sinal sonoro."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.ALERT} Gerenciador de Chamados")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)
        tk.Label(content, text="Gerenciador de Chamados",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=20)
        tk.Label(content, text="Funcionalidade em desenvolvimento.\nEm breve voce podera gerenciar chamados com voz e sinal sonoro.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11), justify="center").pack(pady=10)

    # ====================================================================
    # MODULO DE ENTREGA - helpers compartilhados
    # ====================================================================
    def _entrega_status_label(self, status):
        return {
            "pendente": "Pendente",
            "em_rota": "Em rota",
            "entregue": "Entregue",
        }.get((status or "pendente"), status or "Pendente")

    def _entrega_set_status(self, venda_id, status, on_done=None):
        """Atualiza o status de entrega de uma venda (pendente/em_rota/entregue)."""
        db = DatabaseHelper.get_instance()

        def upd():
            return db.execute_update(
                "UPDATE vendas SET status_entrega = %s WHERE id = %s",
                (status, venda_id))

        def done(_r):
            try:
                AuditLogger.log("ENTREGA_STATUS",
                                f"Venda #{venda_id} -> {status}",
                                usuario=Session.user_login)
            except Exception:
                pass
            ToastManager.success(
                f"Venda #{venda_id}: {self._entrega_status_label(status)}")
            if on_done:
                on_done()

        self.run_async(upd, done)

    def show_modo_entregador(self):
        """Modo Entregador - o entregador visualiza e conclui suas entregas."""
        self.clear_container()
        self._create_top_bar(f"{Icons.ENTREGA} Modo Entregador")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=12)

        tk.Label(content, text="Modo Entregador", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(anchor="w")
        tk.Label(content,
                 text="Selecione o entregador para ver as entregas pendentes e a caminho.",
                 bg=COR_FUNDO, fg=COR_TEXTO2, font=("Segoe UI", 10)).pack(
                     anchor="w", pady=(0, 8))

        sel_frame = tk.Frame(content, bg=COR_FUNDO)
        sel_frame.pack(fill="x", pady=4)
        tk.Label(sel_frame, text="Entregador:", bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 11)).pack(side="left", padx=(0, 6))
        combo = ttk.Combobox(sel_frame, width=30, state="readonly")
        combo.pack(side="left", padx=4)

        cols = ("id", "data", "cliente", "bairro", "endereco", "whatsapp", "total", "status")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=13)
        defs = [("id", "#", 50), ("data", "Data", 130), ("cliente", "Cliente", 150),
                ("bairro", "Bairro", 110), ("endereco", "Endereco", 230),
                ("whatsapp", "WhatsApp", 120), ("total", "Total", 90),
                ("status", "Status", 90)]
        for key, txt, w in defs:
            tree.heading(key, text=txt)
            tree.column(key, width=w,
                        anchor="center" if key in ("id", "total", "status", "data") else "w")
        tree.pack(fill="both", expand=True, pady=8)

        entregadores_cache = []

        def carregar_entregas():
            i = combo.current()
            if i < 0 or i >= len(entregadores_cache):
                return []
            ent = entregadores_cache[i]
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT v.id, v.data_venda, COALESCE(c.nome, v.cliente_nome, 'Cliente') AS cliente, "
                "COALESCE(v.bairro_entrega,'') AS bairro, COALESCE(v.endereco_entrega,'') AS endereco, "
                "COALESCE(v.celular_whatsapp,'') AS whatsapp, v.total_liquido, "
                "COALESCE(v.status_entrega,'pendente') AS status_entrega "
                "FROM vendas v LEFT JOIN clientes c ON v.cliente_id = c.id "
                "WHERE v.para_entrega = 1 AND v.entregador_id = %s "
                "AND COALESCE(v.status_entrega,'pendente') IN ('pendente','em_rota') "
                "ORDER BY v.data_venda DESC", (ent["id"],))

        def render(rows):
            tree.delete(*tree.get_children())
            for r in rows or []:
                tree.insert("", "end", values=(
                    r["id"], FormatUtils.format_date(r["data_venda"]), r["cliente"],
                    r["bairro"], r["endereco"], r["whatsapp"],
                    "R$ " + FormatUtils.format_money(r["total_liquido"]),
                    self._entrega_status_label(r["status_entrega"])))
            if not rows:
                tree.insert("", "end", values=("", "", "Nenhuma entrega pendente", "", "", "", "", ""))

        def reload():
            self.run_async(carregar_entregas, render)

        def on_entregadores(rows):
            entregadores_cache.clear()
            entregadores_cache.extend(rows or [])
            combo["values"] = [e["nome"] for e in entregadores_cache]
            if entregadores_cache:
                combo.current(0)
                reload()
            else:
                ToastManager.warning("Nenhum entregador cadastrado.")

        combo.bind("<<ComboboxSelected>>", lambda e: reload())

        def _sel_id():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione uma entrega.")
                return None
            val = tree.item(sel[0])["values"][0]
            if not val:
                return None
            return val

        def marcar_em_rota():
            vid = _sel_id()
            if vid:
                self._entrega_set_status(vid, "em_rota", on_done=reload)

        def marcar_entregue():
            vid = _sel_id()
            if vid:
                self._entrega_set_status(vid, "entregue", on_done=reload)

        btns = tk.Frame(content, bg=COR_FUNDO)
        btns.pack(fill="x", pady=4)
        StyledButton(btns, text="↻ Atualizar", command=reload,
                     color="#2a3a5c", width=12).pack(side="left", padx=4)
        StyledButton(btns, text="🛵 Saiu para entrega", command=marcar_em_rota,
                     color=COR_BOTAO_AZUL, width=18).pack(side="right", padx=4)
        StyledButton(btns, text="✔ Marcar ENTREGUE", command=marcar_entregue,
                     color=COR_BOTAO_VERDE, width=18).pack(side="right", padx=4)

        def load_entregadores():
            db = DatabaseHelper.get_instance()
            return db.execute_query(
                "SELECT id, nome FROM entregadores WHERE ativo = 1 ORDER BY nome")
        self.run_async(load_entregadores, on_entregadores)

    def show_gerenciar_entregas(self):
        """Gerenciar Entregas - controle de todas as entregas (pendentes/em rota/entregues)."""
        self.clear_container()
        self._create_top_bar(f"{Icons.ENTREGA} Gerenciar Entregas")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=12)

        tk.Label(content, text="Gerenciar Entregas", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 16, "bold")).pack(anchor="w")

        state = {"filtro": "pendente"}

        filtro_frame = tk.Frame(content, bg=COR_FUNDO)
        filtro_frame.pack(fill="x", pady=6)

        cols = ("id", "data", "cliente", "bairro", "endereco", "whatsapp",
                "entregador", "taxa", "total", "status")
        tree = ttk.Treeview(content, columns=cols, show="headings", height=12)
        defs = [("id", "#", 45), ("data", "Data", 125), ("cliente", "Cliente", 140),
                ("bairro", "Bairro", 105), ("endereco", "Endereco", 190),
                ("whatsapp", "WhatsApp", 105), ("entregador", "Entregador", 115),
                ("taxa", "Taxa", 70), ("total", "Total", 85), ("status", "Status", 85)]
        for key, txt, w in defs:
            tree.heading(key, text=txt)
            tree.column(key, width=w,
                        anchor="center" if key in ("id", "taxa", "total", "status", "data") else "w")
        tree.pack(fill="both", expand=True, pady=8)

        lbl_info = tk.Label(content, text="", bg=COR_FUNDO, fg=COR_TEXTO2,
                            font=("Segoe UI", 9))
        lbl_info.pack(anchor="w")

        def carregar():
            db = DatabaseHelper.get_instance()
            base = (
                "SELECT v.id, v.data_venda, COALESCE(c.nome, v.cliente_nome, 'Cliente') AS cliente, "
                "COALESCE(v.bairro_entrega,'') AS bairro, COALESCE(v.endereco_entrega,'') AS endereco, "
                "COALESCE(v.celular_whatsapp,'') AS whatsapp, COALESCE(e.nome,'(a definir)') AS entregador, "
                "v.taxa_entrega, v.total_liquido, COALESCE(v.status_entrega,'pendente') AS status_entrega "
                "FROM vendas v LEFT JOIN clientes c ON v.cliente_id = c.id "
                "LEFT JOIN entregadores e ON v.entregador_id = e.id WHERE v.para_entrega = 1 ")
            if state["filtro"] != "todas":
                return db.execute_query(
                    base + "AND COALESCE(v.status_entrega,'pendente') = %s ORDER BY v.data_venda DESC",
                    (state["filtro"],))
            return db.execute_query(base + "ORDER BY v.data_venda DESC")

        def render(rows):
            tree.delete(*tree.get_children())
            for r in rows or []:
                tree.insert("", "end", values=(
                    r["id"], FormatUtils.format_date(r["data_venda"]), r["cliente"],
                    r["bairro"], r["endereco"], r["whatsapp"], r["entregador"],
                    "R$ " + FormatUtils.format_money(r["taxa_entrega"]),
                    "R$ " + FormatUtils.format_money(r["total_liquido"]),
                    self._entrega_status_label(r["status_entrega"])))
            lbl_info.config(text=f"{len(rows or [])} entrega(s) | filtro: {state['filtro']}")

        def reload():
            self.run_async(carregar, render)

        def set_filtro(f):
            state["filtro"] = f
            reload()

        for txt, fval, cor in [("Pendentes", "pendente", COR_ALERTA),
                               ("Em Rota", "em_rota", COR_BOTAO_AZUL),
                               ("Entregues", "entregue", COR_BOTAO_VERDE),
                               ("Todas", "todas", "#2a3a5c")]:
            StyledButton(filtro_frame, text=txt, command=lambda f=fval: set_filtro(f),
                         color=cor, width=10).pack(side="left", padx=3)
        StyledButton(filtro_frame, text="↻ Atualizar", command=reload,
                     color="#2a3a5c", width=11).pack(side="right", padx=3)

        def _sel_id():
            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione uma entrega na lista.")
                return None
            return tree.item(sel[0])["values"][0]

        def atribuir():
            vid = _sel_id()
            if not vid:
                return

            def _on(row):
                if not row:
                    return
                db = DatabaseHelper.get_instance()

                def upd():
                    return db.execute_update(
                        "UPDATE vendas SET entregador_id = %s WHERE id = %s",
                        (row[0], vid))
                self.run_async(upd, lambda _r: (
                    ToastManager.success(f"Entregador definido: {row[1]}"), reload()))

            self._selecionar_registro(
                "entregadores", "Atribuir Entregador",
                "SELECT id, nome, celular FROM entregadores WHERE ativo = 1 ORDER BY nome",
                ["ID", "Nome", "Celular"], _on)

        def marcar(status):
            vid = _sel_id()
            if vid:
                self._entrega_set_status(vid, status, on_done=reload)

        btns = tk.Frame(content, bg=COR_FUNDO)
        btns.pack(fill="x", pady=4)
        StyledButton(btns, text="👤 Atribuir Entregador", command=atribuir,
                     color=COR_BOTAO_LARANJA, width=18).pack(side="left", padx=3)
        StyledButton(btns, text="🛵 Em Rota", command=lambda: marcar("em_rota"),
                     color=COR_BOTAO_AZUL, width=12).pack(side="left", padx=3)
        StyledButton(btns, text="✔ Entregue", command=lambda: marcar("entregue"),
                     color=COR_BOTAO_VERDE, width=12).pack(side="left", padx=3)
        StyledButton(btns, text="↩ Reabrir", command=lambda: marcar("pendente"),
                     color=COR_ERRO, width=10).pack(side="left", padx=3)

        reload()

    def show_cadastro_mesa(self):
        """Cadastro de Mesas - cadastrar, editar e excluir mesas."""
        self._show_cadastro_generico(
            "Cadastro de Mesas", "mesas",
            [
                {"nome": "numero", "label": "Numero", "required": True},
                {"nome": "descricao", "label": "Descricao"},
                {"nome": "capacidade", "label": "Capacidade"},
                {"nome": "ativa", "label": "Ativa", "tipo": "check", "default": True},
            ],
            "SELECT id, numero, descricao, capacidade, ativa FROM mesas ORDER BY numero",
            "INSERT INTO mesas (numero, descricao, capacidade, ativa) VALUES (%s,%s,%s,%s)",
            "UPDATE mesas SET numero=%s, descricao=%s, capacidade=%s, ativa=%s WHERE id=%s",
            extra_buttons=[("Assistente", self.assistente_criar_mesas)]
        )

    def show_cadastro_armario_sauna(self):
        """Cadastro de Armarios para Sauna - cadastrar, editar e excluir armarios."""
        self._show_cadastro_generico(
            "Cadastro de Armarios Sauna", "armarios_sauna",
            [
                {"nome": "numero", "label": "Numero", "required": True},
                {"nome": "descricao", "label": "Descricao"},
                {"nome": "localizacao", "label": "Localizacao"},
                {"nome": "ativo", "label": "Ativo", "tipo": "check", "default": True},
            ],
            "SELECT id, numero, descricao, localizacao, ativo FROM armarios_sauna ORDER BY numero",
            "INSERT INTO armarios_sauna (numero, descricao, localizacao, ativo) VALUES (%s,%s,%s,%s)",
            "UPDATE armarios_sauna SET numero=%s, descricao=%s, localizacao=%s, ativo=%s WHERE id=%s",
            extra_buttons=[("Assistente", self.assistente_criar_armarios)]
        )

    def show_whatsbot(self):
        """WhatsApp Bot - configurar integracao com WhatsApp."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.WEB} WhatsApp Bot")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)
        tk.Label(content, text="WhatsApp Bot",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=20)
        tk.Label(content, text="Funcionalidade em desenvolvimento.\nEm breve voce podera configurar o WhatsApp Bot aqui.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11), justify="center").pack(pady=10)

    # ====================================================================
    # BACKUP E RESTAURACAO - Implementacao completa via SQL puro
    # ====================================================================

    def _backup_get_all_tables(self):
        """Retorna lista de todas as tabelas do banco de dados atual."""
        db = DatabaseHelper.get_instance()
        rows = db.execute_query("SHOW TABLES")
        if not rows:
            return []
        key = list(rows[0].keys())[0]
        return [r[key] for r in rows]

    def _backup_generate_sql(self, progress_callback=None):
        """Gera o conteudo SQL completo do backup (estrutura + dados).
        Usa SQL puro via conexao existente, sem depender de mysqldump.
        Retorna a string SQL do backup."""
        db = DatabaseHelper.get_instance()
        tables = self._backup_get_all_tables()
        if not tables:
            raise Exception("Nenhuma tabela encontrada no banco de dados.")

        lines = []
        lines.append(f"-- ============================================================")
        lines.append(f"-- PDV Pro v{APP_VERSION} - Backup do Banco de Dados")
        lines.append(f"-- Data: {datetime.datetime.now().strftime('%d/%m/%Y %H:%M:%S')}")
        lines.append(f"-- Banco: {db.database}")
        lines.append(f"-- Host: {db.host}:{db.port}")
        lines.append(f"-- Tabelas: {len(tables)}")
        lines.append(f"-- ============================================================")
        lines.append("")
        lines.append("SET FOREIGN_KEY_CHECKS = 0;")
        lines.append("SET SQL_MODE = 'NO_AUTO_VALUE_ON_ZERO';")
        lines.append("SET AUTOCOMMIT = 0;")
        lines.append("START TRANSACTION;")
        lines.append("")

        total = len(tables)
        for idx, table in enumerate(tables):
            if progress_callback:
                progress_callback(idx, total, table)

            # Estrutura da tabela
            try:
                create_rows = db.execute_query(f"SHOW CREATE TABLE `{table}`")
                if create_rows:
                    create_sql = create_rows[0].get("Create Table", "")
                    if create_sql:
                        lines.append(f"-- -------------------------------------------")
                        lines.append(f"-- Tabela: {table}")
                        lines.append(f"-- -------------------------------------------")
                        lines.append(f"DROP TABLE IF EXISTS `{table}`;")
                        lines.append(f"{create_sql};")
                        lines.append("")
            except Exception as e:
                lines.append(f"-- ERRO ao obter estrutura de {table}: {e}")
                lines.append("")
                continue

            # Dados da tabela
            try:
                data_rows = db.execute_query(f"SELECT * FROM `{table}`")
                if data_rows:
                    cols = list(data_rows[0].keys())
                    cols_escaped = ", ".join([f"`{c}`" for c in cols])

                    batch_size = 100
                    for batch_start in range(0, len(data_rows), batch_size):
                        batch = data_rows[batch_start:batch_start + batch_size]
                        values_list = []
                        for row in batch:
                            vals = []
                            for c in cols:
                                v = row[c]
                                if v is None:
                                    vals.append("NULL")
                                elif isinstance(v, (int, float)):
                                    vals.append(str(v))
                                elif isinstance(v, Decimal):
                                    vals.append(str(v))
                                elif isinstance(v, (datetime.datetime, datetime.date)):
                                    vals.append(f"'{v}'")
                                elif isinstance(v, bytes):
                                    hex_str = v.hex()
                                    vals.append(f"X'{hex_str}'")
                                elif isinstance(v, bytearray):
                                    hex_str = bytes(v).hex()
                                    vals.append(f"X'{hex_str}'")
                                else:
                                    escaped = str(v).replace("\\", "\\\\").replace("'", "\\'")
                                    escaped = escaped.replace("\n", "\\n").replace("\r", "\\r")
                                    vals.append(f"'{escaped}'")
                            values_list.append(f"({', '.join(vals)})")

                        insert_sql = (f"INSERT INTO `{table}` ({cols_escaped}) VALUES\n"
                                      + ",\n".join(values_list) + ";")
                        lines.append(insert_sql)

                    lines.append("")
            except Exception as e:
                lines.append(f"-- ERRO ao exportar dados de {table}: {e}")
                lines.append("")

        lines.append("COMMIT;")
        lines.append("SET FOREIGN_KEY_CHECKS = 1;")
        lines.append("")
        lines.append(f"-- Fim do backup - {datetime.datetime.now().strftime('%d/%m/%Y %H:%M:%S')}")

        return "\n".join(lines)

    def _backup_realizar(self, destino=None):
        """Realiza o backup e salva no arquivo destino.
        Se destino for None, salva no BACKUP_DIR com nome automatico.
        Retorna (sucesso: bool, mensagem: str, caminho: str)."""
        try:
            ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
            db = DatabaseHelper.get_instance()
            nome_arquivo = f"backup_{db.database}_{ts}.sql"

            if destino is None:
                destino = os.path.join(BACKUP_DIR, nome_arquivo)

            sql_content = self._backup_generate_sql()

            with open(destino, "w", encoding="utf-8") as f:
                f.write(sql_content)

            tamanho = os.path.getsize(destino)
            tamanho_fmt = self._backup_format_size(tamanho)

            return True, f"Backup realizado com sucesso!\nArquivo: {os.path.basename(destino)}\nTamanho: {tamanho_fmt}", destino

        except Exception as e:
            ErroAmigavel.log_erro(e, "backup_realizar")
            return False, f"Erro ao realizar backup:\n{str(e)}", ""

    def _backup_restaurar(self, caminho):
        """Restaura o banco de dados a partir de um arquivo SQL.
        Retorna (sucesso: bool, mensagem: str)."""
        try:
            if not os.path.exists(caminho):
                return False, "Arquivo de backup nao encontrado."

            with open(caminho, "r", encoding="utf-8") as f:
                sql_content = f.read()

            if not sql_content.strip():
                return False, "Arquivo de backup esta vazio."

            db = DatabaseHelper.get_instance()
            conn = db.get_connection()
            cursor = conn.cursor()

            cursor.execute("SET FOREIGN_KEY_CHECKS = 0")

            statements = []
            current = []
            for line in sql_content.split("\n"):
                stripped = line.strip()
                if stripped.startswith("--") or stripped == "":
                    continue
                current.append(line)
                if stripped.endswith(";"):
                    stmt = "\n".join(current).strip()
                    if stmt and stmt != ";":
                        statements.append(stmt)
                    current = []

            erros = 0
            executados = 0
            for stmt in statements:
                try:
                    stmt_upper = stmt.upper().strip().rstrip(";")
                    if stmt_upper in ("START TRANSACTION", "COMMIT",
                                      "SET FOREIGN_KEY_CHECKS = 0",
                                      "SET FOREIGN_KEY_CHECKS = 1",
                                      "SET AUTOCOMMIT = 0"):
                        cursor.execute(stmt)
                        continue
                    cursor.execute(stmt)
                    executados += 1
                except Exception as e:
                    erros += 1
                    _logger.warning(f"Erro ao executar statement de restauracao: {e}")

            conn.commit()
            cursor.execute("SET FOREIGN_KEY_CHECKS = 1")
            cursor.close()

            msg = f"Restauracao concluida com sucesso!\n{executados} comandos executados."
            if erros > 0:
                msg += f"\n{erros} comando(s) com erro (verifique o log)."

            return True, msg

        except Exception as e:
            ErroAmigavel.log_erro(e, "backup_restaurar")
            return False, f"Erro ao restaurar backup:\n{str(e)}"

    def _backup_listar(self):
        """Lista todos os backups disponiveis no diretorio de backups.
        Retorna lista de dicts com info de cada backup."""
        backups = []
        try:
            if not os.path.exists(BACKUP_DIR):
                return backups
            for f in os.listdir(BACKUP_DIR):
                if f.endswith(".sql"):
                    caminho = os.path.join(BACKUP_DIR, f)
                    stat = os.stat(caminho)
                    tamanho = stat.st_size
                    data_mod = datetime.datetime.fromtimestamp(stat.st_mtime)
                    backups.append({
                        "nome": f,
                        "caminho": caminho,
                        "tamanho": tamanho,
                        "tamanho_fmt": self._backup_format_size(tamanho),
                        "data": data_mod,
                        "data_fmt": data_mod.strftime("%d/%m/%Y %H:%M:%S")
                    })
            backups.sort(key=lambda x: x["data"], reverse=True)
        except Exception as e:
            ErroAmigavel.log_erro(e, "backup_listar")
        return backups

    def _backup_excluir(self, caminho):
        """Exclui um arquivo de backup. Retorna (sucesso, mensagem)."""
        try:
            if os.path.exists(caminho):
                os.remove(caminho)
                return True, "Backup excluido com sucesso."
            return False, "Arquivo nao encontrado."
        except Exception as e:
            ErroAmigavel.log_erro(e, "backup_excluir")
            return False, f"Erro ao excluir backup: {e}"

    @staticmethod
    def _backup_format_size(size_bytes):
        """Formata tamanho em bytes para formato legivel."""
        if size_bytes < 1024:
            return f"{size_bytes} B"
        elif size_bytes < 1024 * 1024:
            return f"{size_bytes / 1024:.1f} KB"
        elif size_bytes < 1024 * 1024 * 1024:
            return f"{size_bytes / (1024 * 1024):.1f} MB"
        else:
            return f"{size_bytes / (1024 * 1024 * 1024):.2f} GB"

    def show_backup(self):
        """Backup - realizar e restaurar backups do banco de dados."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.BACKUP} Backup e Restauracao")

        # ScrollableFrame para conteudo
        scroll = ScrollableFrame(self.main_container)
        scroll.pack(fill="both", expand=True, padx=10, pady=5)
        content = scroll.scrollable_frame

        # Titulo
        tk.Label(content, text=f"{Icons.BACKUP} Backup e Restauracao",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 20, "bold")).pack(pady=(15, 5))
        tk.Label(content, text="Realize backups do banco de dados e restaure quando necessario.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 10)).pack(pady=(0, 10))

        NeonDivider(content, color=COR_PRIMARIA).pack(fill="x", padx=20, pady=(5, 15))

        # ---- Frame de acoes ----
        acoes_frame = tk.Frame(content, bg=COR_CARD, highlightbackground=COR_CARD_BORDER,
                               highlightthickness=1)
        acoes_frame.pack(fill="x", padx=20, pady=(0, 15))

        acoes_inner = tk.Frame(acoes_frame, bg=COR_CARD, padx=15, pady=15)
        acoes_inner.pack(fill="x")

        tk.Label(acoes_inner, text="Acoes Rapidas",
                 bg=COR_CARD, fg=COR_TEXTO,
                 font=("Segoe UI", 13, "bold")).pack(anchor="w", pady=(0, 10))

        btns_frame = tk.Frame(acoes_inner, bg=COR_CARD)
        btns_frame.pack(fill="x")

        # Variavel para status
        status_var = tk.StringVar(value="")
        lbl_status = tk.Label(acoes_inner, textvariable=status_var,
                              bg=COR_CARD, fg=COR_TEXTO2,
                              font=("Segoe UI", 10), wraplength=600, justify="left")
        lbl_status.pack(anchor="w", pady=(10, 0))

        # Treeview para listar backups
        lista_frame = tk.Frame(content, bg=COR_CARD, highlightbackground=COR_CARD_BORDER,
                                highlightthickness=1)
        lista_frame.pack(fill="both", expand=True, padx=20, pady=(0, 15))

        lista_inner = tk.Frame(lista_frame, bg=COR_CARD, padx=15, pady=15)
        lista_inner.pack(fill="both", expand=True)

        tk.Label(lista_inner, text="Backups Disponiveis",
                 bg=COR_CARD, fg=COR_TEXTO,
                 font=("Segoe UI", 13, "bold")).pack(anchor="w", pady=(0, 10))

        tree_frame = tk.Frame(lista_inner, bg=COR_CARD)
        tree_frame.pack(fill="both", expand=True)

        cols = ("nome", "data", "tamanho")
        tree = ttk.Treeview(tree_frame, columns=cols, show="headings", height=10)
        tree.heading("nome", text="Arquivo")
        tree.heading("data", text="Data")
        tree.heading("tamanho", text="Tamanho")
        tree.column("nome", width=350, minwidth=200)
        tree.column("data", width=160, minwidth=120, anchor="center")
        tree.column("tamanho", width=100, minwidth=80, anchor="center")

        scrollbar = ttk.Scrollbar(tree_frame, orient="vertical", command=tree.yview)
        tree.configure(yscrollcommand=scrollbar.set)
        tree.pack(side="left", fill="both", expand=True)
        scrollbar.pack(side="right", fill="y")

        # Estilizar Treeview
        style = ttk.Style()
        style.configure("Treeview",
                        background=COR_FUNDO3,
                        foreground=COR_TEXTO,
                        fieldbackground=COR_FUNDO3,
                        font=("Segoe UI", 10),
                        rowheight=28)
        style.configure("Treeview.Heading",
                        background=COR_FUNDO2,
                        foreground=COR_PRIMARIA,
                        font=("Segoe UI", 10, "bold"))
        style.map("Treeview", background=[("selected", COR_PRIMARIA)],
                  foreground=[("selected", "#000000")])

        def carregar_lista():
            """Carrega a lista de backups na Treeview."""
            for item in tree.get_children():
                tree.delete(item)
            backups = self._backup_listar()
            for bk in backups:
                tree.insert("", "end", values=(bk["nome"], bk["data_fmt"], bk["tamanho_fmt"]),
                            tags=(bk["caminho"],))
            if not backups:
                status_var.set("Nenhum backup encontrado na pasta de backups.")
            else:
                status_var.set(f"{len(backups)} backup(s) encontrado(s).")

        def realizar_backup():
            """Realiza backup do banco de dados."""
            if not Session.has_permission(PermissionConstants.BACKUP_REALIZAR):
                ToastManager.warning("Voce nao tem permissao para realizar backup.")
                return

            status_var.set(f"{Icons.REFRESH} Realizando backup... Aguarde.")
            lbl_status.config(fg=COR_ALERTA)
            self.root.update_idletasks()

            def _do_backup():
                return self._backup_realizar()

            def _on_success(result):
                ok, msg, caminho = result
                if ok:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_SUCESSO)
                    ToastManager.success("Backup realizado com sucesso!")
                    carregar_lista()
                else:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_ERRO)
                    ToastManager.error("Falha ao realizar backup.")

            def _on_error(e):
                status_var.set(f"Erro: {str(e)}")
                lbl_status.config(fg=COR_ERRO)
                ToastManager.error("Erro ao realizar backup.")

            self.run_async(_do_backup, callback=_on_success, error_callback=_on_error)

        def realizar_backup_externo():
            """Realiza backup e permite escolher onde salvar."""
            if not Session.has_permission(PermissionConstants.BACKUP_REALIZAR):
                ToastManager.warning("Voce nao tem permissao para realizar backup.")
                return

            db = DatabaseHelper.get_instance()
            ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
            default_name = f"backup_{db.database}_{ts}.sql"
            destino = filedialog.asksaveasfilename(
                initialdir=BACKUP_DIR,
                initialfile=default_name,
                defaultextension=".sql",
                filetypes=[("SQL", "*.sql"), ("Todos", "*.*")]
            )
            if not destino:
                return

            status_var.set(f"{Icons.REFRESH} Realizando backup... Aguarde.")
            lbl_status.config(fg=COR_ALERTA)
            self.root.update_idletasks()

            def _do():
                return self._backup_realizar(destino=destino)

            def _on_ok(result):
                ok, msg, caminho = result
                if ok:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_SUCESSO)
                    ToastManager.success("Backup salvo com sucesso!")
                    carregar_lista()
                else:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_ERRO)
                    ToastManager.error("Falha ao salvar backup.")

            self.run_async(_do, callback=_on_ok)

        def restaurar_backup():
            """Restaura um backup selecionado na lista."""
            if not Session.has_permission(PermissionConstants.BACKUP_RESTAURAR):
                ToastManager.warning("Voce nao tem permissao para restaurar backup.")
                return

            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione um backup na lista para restaurar.")
                return

            item = tree.item(sel[0])
            nome = item["values"][0]
            caminho = item["tags"][0] if item["tags"] else ""

            if not caminho or not os.path.exists(caminho):
                ToastManager.error("Arquivo de backup nao encontrado.")
                return

            confirm = messagebox.askyesno(
                "Confirmar Restauracao",
                f"ATENCAO: Esta operacao ira SUBSTITUIR todos os dados atuais "
                f"do banco de dados pelos dados do backup:\n\n"
                f"{nome}\n\n"
                f"Esta acao NAO pode ser desfeita.\n"
                f"Deseja continuar?",
                icon="warning"
            )
            if not confirm:
                return

            status_var.set(f"{Icons.REFRESH} Restaurando backup... Aguarde.")
            lbl_status.config(fg=COR_ALERTA)
            self.root.update_idletasks()

            def _do():
                return self._backup_restaurar(caminho)

            def _on_ok(result):
                ok, msg = result
                if ok:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_SUCESSO)
                    ToastManager.success("Backup restaurado com sucesso!")
                    messagebox.showinfo("Restauracao Concluida", msg)
                else:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_ERRO)
                    ToastManager.error("Falha na restauracao.")
                    messagebox.showerror("Erro na Restauracao", msg)

            def _on_err(e):
                status_var.set(f"Erro: {str(e)}")
                lbl_status.config(fg=COR_ERRO)
                ToastManager.error("Erro ao restaurar backup.")

            self.run_async(_do, callback=_on_ok, error_callback=_on_err)

        def restaurar_arquivo_externo():
            """Restaura a partir de um arquivo SQL externo."""
            if not Session.has_permission(PermissionConstants.BACKUP_RESTAURAR):
                ToastManager.warning("Voce nao tem permissao para restaurar backup.")
                return

            caminho = filedialog.askopenfilename(
                title="Selecionar Arquivo de Backup",
                initialdir=BACKUP_DIR,
                filetypes=[("SQL", "*.sql"), ("Todos", "*.*")]
            )
            if not caminho:
                return

            confirm = messagebox.askyesno(
                "Confirmar Restauracao",
                f"ATENCAO: Esta operacao ira SUBSTITUIR todos os dados atuais "
                f"do banco de dados pelos dados do backup:\n\n"
                f"{os.path.basename(caminho)}\n\n"
                f"Esta acao NAO pode ser desfeita.\n"
                f"Deseja continuar?",
                icon="warning"
            )
            if not confirm:
                return

            status_var.set(f"{Icons.REFRESH} Restaurando backup externo... Aguarde.")
            lbl_status.config(fg=COR_ALERTA)
            self.root.update_idletasks()

            def _do():
                return self._backup_restaurar(caminho)

            def _on_ok(result):
                ok, msg = result
                if ok:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_SUCESSO)
                    ToastManager.success("Backup restaurado com sucesso!")
                    messagebox.showinfo("Restauracao Concluida", msg)
                else:
                    status_var.set(msg)
                    lbl_status.config(fg=COR_ERRO)
                    ToastManager.error("Falha na restauracao.")

            self.run_async(_do, callback=_on_ok)

        def excluir_backup():
            """Exclui o backup selecionado."""
            if not Session.has_permission(PermissionConstants.BACKUP_CONFIGURAR):
                ToastManager.warning("Voce nao tem permissao para excluir backups.")
                return

            sel = tree.selection()
            if not sel:
                ToastManager.warning("Selecione um backup na lista para excluir.")
                return

            item = tree.item(sel[0])
            nome = item["values"][0]
            caminho = item["tags"][0] if item["tags"] else ""

            confirm = messagebox.askyesno(
                "Confirmar Exclusao",
                f"Deseja realmente excluir o backup:\n\n{nome}\n\n"
                f"Esta acao nao pode ser desfeita.",
                icon="warning"
            )
            if not confirm:
                return

            ok, msg = self._backup_excluir(caminho)
            if ok:
                ToastManager.success(msg)
                carregar_lista()
            else:
                ToastManager.error(msg)

        def abrir_pasta_backups():
            """Abre a pasta de backups no explorador de arquivos."""
            try:
                if _platform.system() == "Windows":
                    os.startfile(BACKUP_DIR)
                elif _platform.system() == "Darwin":
                    subprocess.Popen(["open", BACKUP_DIR])
                else:
                    subprocess.Popen(["xdg-open", BACKUP_DIR])
            except Exception as e:
                ToastManager.error(f"Nao foi possivel abrir a pasta: {e}")

        # ---- Botoes de acao ----
        btn_row1 = tk.Frame(btns_frame, bg=COR_CARD)
        btn_row1.pack(fill="x", pady=(0, 8))

        StyledButton(btn_row1, text=f"{Icons.BACKUP} Realizar Backup",
                     command=realizar_backup,
                     color=COR_BOTAO_VERDE, width=22).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row1.winfo_children()[-1], "Realizar backup do banco de dados na pasta padrao")

        StyledButton(btn_row1, text=f"{Icons.EXPORT} Salvar Como...",
                     command=realizar_backup_externo,
                     color=COR_BOTAO_AZUL, width=20).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row1.winfo_children()[-1], "Realizar backup e escolher onde salvar")

        StyledButton(btn_row1, text=f"{Icons.RESTORE} Restaurar Selecionado",
                     command=restaurar_backup,
                     color=COR_BOTAO_LARANJA, width=24).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row1.winfo_children()[-1], "Restaurar o backup selecionado na lista")

        btn_row2 = tk.Frame(btns_frame, bg=COR_CARD)
        btn_row2.pack(fill="x")

        StyledButton(btn_row2, text=f"{Icons.IMPORT} Restaurar Arquivo...",
                     command=restaurar_arquivo_externo,
                     color="#FF9800", width=24).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row2.winfo_children()[-1], "Restaurar a partir de um arquivo SQL externo")

        StyledButton(btn_row2, text=f"{Icons.DELETE} Excluir Backup",
                     command=excluir_backup,
                     color=COR_ERRO, width=20).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row2.winfo_children()[-1], "Excluir o backup selecionado")

        StyledButton(btn_row2, text=f"{Icons.SEARCH} Abrir Pasta",
                     command=abrir_pasta_backups,
                     color="#607D8B", width=18).pack(side="left", padx=(0, 8))
        add_tooltip(btn_row2.winfo_children()[-1], "Abrir a pasta de backups no explorador de arquivos")

        # ---- Info do diretorio de backup ----
        info_frame = tk.Frame(content, bg=COR_CARD, highlightbackground=COR_CARD_BORDER,
                              highlightthickness=1)
        info_frame.pack(fill="x", padx=20, pady=(0, 15))
        info_inner = tk.Frame(info_frame, bg=COR_CARD, padx=15, pady=10)
        info_inner.pack(fill="x")

        tk.Label(info_inner, text=f"{Icons.INFO} Informacoes",
                 bg=COR_CARD, fg=COR_TEXTO,
                 font=("Segoe UI", 11, "bold")).pack(anchor="w", pady=(0, 5))
        tk.Label(info_inner,
                 text=f"Pasta de backups: {BACKUP_DIR}\n"
                      f"Banco de dados: {DatabaseHelper.get_instance().database}\n"
                      f"Servidor: {DatabaseHelper.get_instance().host}:{DatabaseHelper.get_instance().port}",
                 bg=COR_CARD, fg=COR_TEXTO2,
                 font=("Segoe UI", 9), justify="left").pack(anchor="w")

        # Carregar lista inicial
        carregar_lista()

    def show_atualizar_sistema(self):
        """Atualizar Sistema - atualizar o sistema via FTP."""
        self.clear_container()
        top = self._create_top_bar(f"{Icons.REFRESH} Atualizar Sistema")
        content = tk.Frame(self.main_container, bg=COR_FUNDO)
        content.pack(fill="both", expand=True, padx=20, pady=20)
        tk.Label(content, text="Atualizar Sistema",
                 bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 18, "bold")).pack(pady=20)
        tk.Label(content, text="Funcionalidade em desenvolvimento.\nEm breve voce podera atualizar o sistema via FTP aqui.",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 11), justify="center").pack(pady=10)

    def show_sobre(self):
        dialog = tk.Toplevel(self.root)
        dialog.title("Sobre o PDV Pro")
        dialog.geometry("560x620")
        dialog.configure(bg=COR_FUNDO)
        dialog.transient(self.root)
        dialog.grab_set()
        dialog.resizable(False, False)

        # Hexagono animado
        hex_canvas = tk.Canvas(dialog, width=80, height=80,
                               bg=COR_FUNDO, highlightthickness=0)
        hex_canvas.pack(pady=(20, 0))
        cx, cy, r = 40, 40, 34
        points = []
        for i in range(6):
            angle = math.radians(60 * i - 30)
            points.extend([cx + r * math.cos(angle), cy + r * math.sin(angle)])
        hex_canvas.create_polygon(points, outline=COR_PRIMARIA,
                                  fill=COR_FUNDO3, width=2)
        hex_canvas.create_text(cx, cy, text=Icons.MONEY,
                               fill=COR_PRIMARIA, font=("Segoe UI", 18, "bold"))
        # Glow pulsante no hexagono
        AnimationEngine.pulse_glow(hex_canvas,
            hex_canvas.create_oval(cx-36, cy-36, cx+36, cy+36,
                                   outline=COR_PRIMARIA, fill="", width=1),
            COR_PRIMARIA, COR_PRIMARIA_GLOW, root=dialog)

        tk.Label(dialog, text="PDV Pro", bg=COR_FUNDO, fg=COR_PRIMARIA,
                 font=("Segoe UI", 34, "bold")).pack(pady=(4, 0))
        tk.Label(dialog, text=f"v{APP_VERSION}  ULTIMATE  |  Build {APP_BUILD}",
                 bg=COR_FUNDO, fg=COR_ACCENT2,
                 font=("Consolas", 11)).pack(pady=2)
        tk.Label(dialog, text=f"Tema: {_ACTIVE_THEME}",
                 bg=COR_FUNDO, fg=COR_TEXTO2,
                 font=("Segoe UI", 9)).pack()

        NeonDivider(dialog, color=COR_PRIMARIA, width_pct=0.6).pack(
            fill="x", padx=60, pady=8)

        tk.Label(dialog, text="Sistema de Ponto de Venda Profissional ULTIMATE",
                 bg=COR_FUNDO, fg=COR_TEXTO,
                 font=("Segoe UI", 11, "bold")).pack(pady=4)

        # Notebook com abas
        notebook = ttk.Notebook(dialog)
        notebook.pack(padx=20, pady=6, fill="both", expand=True)

        # Aba Funcionalidades
        tab_func = tk.Frame(notebook, bg=COR_GLASS, padx=10, pady=6)
        notebook.add(tab_func, text="Funcionalidades")

        info = [
            (Icons.VENDAS,     "Vendas, PDV e Carrinho inteligente"),
            (Icons.COMANDAS,   "Comandas e Mesas com status visual"),
            (Icons.CAIXA,      "Caixa com Sangria e Vale-debito"),
            (Icons.MESA,       "Mesas e Armarios de Sauna"),
            (Icons.RELATORIOS, "Relatorios com exportacao CSV/TXT"),
            (Icons.WEB,        "Cardapio Digital / Servidor Web"),
            (Icons.PERFIL,     "Usuarios, Perfis e Permissoes"),
            (Icons.MONEY,      "Contas a Receber e Cobrancas"),
            (Icons.PRODUTOS,   "Entrada de Notas e Estoque"),
            (Icons.AUDIT,      "Log de Auditoria completo"),
            (Icons.SHORTCUT,   "Atalhos F1-F12 globais"),
            (Icons.THEME,      "3 Temas visuais alternaveis"),
        ]
        for icon, item in info:
            row = tk.Frame(tab_func, bg=COR_GLASS)
            row.pack(fill="x", pady=1)
            tk.Label(row, text=f"  {icon}  {item}", bg=COR_GLASS,
                     fg=COR_TEXTO2, font=("Segoe UI", 10),
                     anchor="w").pack(anchor="w")

        # Aba Novidades v8
        tab_new = tk.Frame(notebook, bg=COR_GLASS, padx=10, pady=6)
        notebook.add(tab_new, text="Novidades v8")

        novidades = [
            "✚ KPIs em tempo real no Dashboard",
            "✚ Toast Notifications nao-bloqueantes",
            "✚ Atalhos de teclado F1-F12",
            "✚ Barra de status com conexao BD ao vivo",
            "✚ 3 temas visuais (Dark Cyber, Navy Pro, Midnight)",
            "✚ Historico com filtros por data/status",
            "✚ Exportacao CSV e TXT em todas as telas",
            "✚ Zebra nas listas + ordenacao por coluna",
            "✚ Tooltips em botoes e campos",
            "✚ Log de auditoria com exportacao",
            "✚ Ctrl+Z desfaz ultimo item do carrinho",
            "✚ Ctrl+P impressao rapida do ultimo cupom",
            "✚ Rate-limit de login (anti-brute-force)",
            "✚ Confirmacao inteligente ao fechar",
            "✚ Monitor de conexao em background",
        ]
        sc = scrolledtext.ScrolledText(tab_new, bg=COR_GLASS, fg=COR_SUCESSO,
                                        font=("Segoe UI", 10), height=12,
                                        state="normal", relief="flat",
                                        bd=0, wrap="word")
        sc.pack(fill="both", expand=True)
        for n in novidades:
            sc.insert("end", n + "\n")
        sc.config(state="disabled")

        # Aba Sistema
        tab_sys = tk.Frame(notebook, bg=COR_GLASS, padx=10, pady=8)
        notebook.add(tab_sys, text="Sistema")

        plat = _platform.system()
        bits = "32-bit" if sys.maxsize <= 2**31 else "64-bit"
        py_ver = sys.version.split()[0]

        sys_info = [
            ("Versao PDV Pro", f"v{APP_VERSION} ULTIMATE"),
            ("Build", APP_BUILD),
            ("Python", f"{py_ver} ({bits})"),
            ("Plataforma", f"{plat}"),
            ("Diretorio", APP_DIR),
            ("Config BD", CONFIG_FILE),
            ("Log Auditoria", AUDIT_LOG_FILE),
            ("Backups", BACKUP_DIR),
            ("Exportacoes", EXPORT_DIR),
            ("Tema Ativo", _ACTIVE_THEME),
        ]
        for k, v in sys_info:
            row = tk.Frame(tab_sys, bg=COR_GLASS)
            row.pack(fill="x", pady=2)
            tk.Label(row, text=f"{k}:", bg=COR_GLASS, fg=COR_TEXTO2,
                     font=("Segoe UI", 9), width=18, anchor="w").pack(side="left")
            tk.Label(row, text=v, bg=COR_GLASS, fg=COR_PRIMARIA,
                     font=("Consolas", 9), anchor="w").pack(side="left")

        tk.Label(dialog, text="Powered by PDV Pro Technology v8",
                 bg=COR_FUNDO, fg="#2a3a5c",
                 font=("Segoe UI", 8)).pack(pady=(4, 0))

        StyledButton(dialog, text=f"{Icons.CROSS} Fechar",
                     command=dialog.destroy,
                     color="#2a3a5c", width=12).pack(pady=8)


# ============================================================================
# PONTO DE ENTRADA
# ============================================================================

def main():
    """Ponto de entrada principal com tratamento global de erros robusto.
    Inclui diagnostico automatico, mensagens claras e recuperacao inteligente."""
    try:
        root = tk.Tk()

        try:
            root.iconname("PDV Pro")
        except Exception:
            pass

        # Handler global para excecoes nao tratadas no Tkinter
        def _tk_exception_handler(exc_type, exc_value, exc_tb):
            """Captura excecoes nao tratadas e exibe mensagem amigavel.
            O sistema continua funcionando apos o erro."""
            try:
                ErroAmigavel.log_erro(exc_value, "excecao_global_tkinter")
                msg, cod, det = ErroAmigavel.traduzir(exc_value)
                severidade = ErroAmigavel.obter_severidade(exc_value)

                if severidade == Severidade.CRITICO:
                    titulo = "Erro Critico no Sistema"
                    complemento = (
                        "\n\nATENCAO: Este e um erro grave.\n"
                        "Salve seu trabalho e reinicie o sistema o mais rapido possivel.")
                else:
                    titulo = "Ops! Algo deu errado"
                    complemento = (
                        "\n\nO sistema continuara funcionando normalmente.\n"
                        "Se o problema persistir, reinicie o sistema.")

                messagebox.showerror(
                    titulo,
                    f"{msg}\n\n"
                    f"{'='*40}\n"
                    f"Codigo do erro: {cod}\n"
                    f"Anote este codigo caso precise de suporte.{complemento}")
            except Exception:
                pass

        # Registrar handler global
        root.report_callback_exception = _tk_exception_handler

        # Handler global para excecoes nao tratadas em threads
        def _thread_exception_handler(args):
            """Captura excecoes em threads daemon."""
            try:
                ErroAmigavel.log_erro(args.exc_value, f"excecao_thread_{args.thread.name}")
            except Exception:
                pass

        try:
            import threading
            threading.excepthook = _thread_exception_handler
        except (AttributeError, ImportError):
            pass  # Python < 3.8

        app = PDVApp(root)
        root.mainloop()

    except mysql.connector.Error as e:
        ErroAmigavel.log_erro(e, "inicializacao_mysql")
        msg, cod, det = ErroAmigavel.traduzir(e)

        # Diagnostico automatico
        diagnostico = ""
        try:
            db = DatabaseHelper.get_instance()
            ok, diag = ErroAmigavel.diagnosticar_conexao(db.host, db.port)
            diagnostico = f"\n\nDIAGNOSTICO AUTOMATICO:\n{diag}"
        except Exception:
            diagnostico = ""

        try:
            messagebox.showerror(
                "Erro ao Iniciar - Banco de Dados",
                f"O PDV Pro nao conseguiu conectar ao banco de dados.\n\n"
                f"{msg}\n\n"
                f"{'='*40}\n"
                f"Codigo: {cod}\n\n"
                f"PASSO A PASSO PARA RESOLVER:\n"
                f"1. Verifique se o MySQL esta instalado neste computador\n"
                f"2. Abra 'Servicos' do Windows (services.msc)\n"
                f"3. Procure 'MySQL' na lista e clique em 'Iniciar'\n"
                f"4. Tente abrir o PDV Pro novamente\n"
                f"5. Se nao funcionar, reinicie o computador\n"
                f"6. Se nada resolver, entre em contato com o suporte"
                f"{diagnostico}")
        except Exception:
            print(f"\n{'='*50}")
            print(f"ERRO CRITICO - PDV Pro nao pode iniciar")
            print(f"{'='*50}")
            print(f"Problema: {msg}")
            print(f"Codigo: {cod}")
            print(f"\nVerifique se o MySQL esta instalado e rodando.")
            print(f"{'='*50}")

    except Exception as e:
        try:
            ErroAmigavel.log_erro(e, "inicializacao_geral")
        except Exception:
            pass

        try:
            msg, cod, det = ErroAmigavel.traduzir(e)
        except Exception:
            msg = "Erro desconhecido ao iniciar o sistema."
            cod = "ERRO-INIT-999"
            det = str(e)

        try:
            messagebox.showerror(
                "Erro ao Iniciar o Sistema",
                f"Ocorreu um erro ao iniciar o PDV Pro.\n\n"
                f"{msg}\n\n"
                f"{'='*40}\n"
                f"Codigo: {cod}\n\n"
                f"COMO RESOLVER:\n"
                f"1. Feche todos os programas e tente novamente\n"
                f"2. Reinicie o computador\n"
                f"3. Verifique se o antivirus nao esta bloqueando\n"
                f"4. Execute como Administrador (clique direito > Executar como admin)\n"
                f"5. Se nada funcionar, entre em contato com o suporte\n"
                f"   e informe o codigo: {cod}")
        except Exception:
            print(f"\n{'='*50}")
            print(f"ERRO CRITICO - PDV Pro nao pode iniciar")
            print(f"{'='*50}")
            print(f"Erro: {det}")
            print(f"Codigo: {cod}")
            print(f"\nTente reiniciar o programa ou entre em contato com o suporte.")
            print(f"{'='*50}")
            traceback.print_exc()


if __name__ == "__main__":
    main()


